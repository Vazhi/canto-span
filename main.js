// GENERATED FILE — DO NOT EDIT DIRECTLY.
// Canonical source: src/** and src/runtime-resources/**
// Regenerate with: npm run build:runtime
var __getOwnPropNames = Object.getOwnPropertyNames;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};

// src/runtime-resources/presentation/learner-glosses.js
var require_learner_glosses = __commonJS({
  "src/runtime-resources/presentation/learner-glosses.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      contextual: {
        "TimeNP|上": ["last / previous", "Used with a time word."],
        "TimeNP|下": ["next / following", "Used with a time word."],
        "TimeNP|個": ["measure word", "Used before this time word."],
        "TimeNP|禮拜": ["week", "Time word."],
        "TimeNP|星期五": ["Friday", "Time word."],
        "TimeNP|次": ["time / occasion", "Used in last time / next time."],
        "DirectionalMotionVP|上": ["up / upward", "Direction in a movement phrase."],
        "CompoundDirectionalMotionVP|上": ["up / upward", "Direction in a movement phrase."],
        "DirectionalMotionVP|落": ["down / downward", "Direction in a movement phrase."],
        "CompoundDirectionalMotionVP|落": ["down / downward", "Direction in a movement phrase."],
        "DirectionalMotionVP|嚟": ["come / toward the speaker", "Movement word."],
        "CompoundDirectionalMotionVP|嚟": ["come / toward the speaker", "Movement word."],
        "DirectionalMotionVP|返": ["return / go back", "Movement word."],
        "CompoundDirectionalMotionVP|返": ["return / go back", "Movement word."],
        "DegreeMannerAdverbial|快": ["quickly / fast", "Tells how the action happens."],
        "DegreeMannerAdverbial|啲": ["a bit / more", "Makes the description stronger or softer."],
        "DiMarkedNP|啲": ["some / these", "Used before a noun."],
        "QuantifiedTimeNP|字": ["five-minute unit", "In this duration expression, each 字 means five minutes."],
        "ApproximateQuantity|度": ["about / approximately", "Makes the preceding quantity approximate."],
        "NeedsContext|唔好食": ["needs context", "Could mean do not eat or not good to eat."],
        "PolarQuestionFrame|咩": ["really? / is that so?", "Sentence-final particle asking a biased yes-or-no question."],
        "PolarQuestionFrame|㗎": ["assertive / currently relevant", "Inner particle in an ordered question cluster; kept as one visible token."],
        "DiscourseParticleFrame|啩": ["probably / perhaps", "Sentence-final particle marking uncertainty about the preceding statement."],
        "DiscourseParticleFrame|喎": ["reportedly / noteworthy", "Sentence-final particle marking broad reportative, noteworthy, reminder, or counterexpectation stance; exact subtype depends on tone and context."],
        "DiscourseParticleFrame|啦": ["please / let's / then", "Sentence-final 啦 marking a directive, suggestion, invitation, or interpersonal closure."],
        "DiscourseParticleFrame|喇": ["now / changed situation", "Sentence-final 喇 marking a new or currently relevant situation; it is separate from perfective 咗."],
        "DiscourseParticleFrame|㗎": ["assertive / currently relevant", "Inner sentence-final particle in an ordered cluster; kept as one visible token."],
        "DiscourseParticleFrame|啫": ["only / just", "Restrictive or minimizing particle inside an ordered cluster."],
        "DiscourseParticleFrame|咋": ["only / that's all", "Restrictive or exhaustive-limit particle inside an ordered cluster."],
        "FocusParticleFrame|得": ["only / just", "Limits the following amount or scalar expression."],
        "FocusParticleFrame|啫": ["only / just", "Marks the visible amount as small or limited."],
        "FocusParticleFrame|咋": ["only / that's all", "Marks the visible amount as the limit."],
        "FragmentQuestion|咩": ["really? / what?", "Reaction question whose claim is understood from context."],
        "ModifiedNP|咩": ["what / what kind of", "Directly modifies the following noun."],
        "ProhibitiveImperative|唔好": ["don't", "Tells someone not to do the following action."],
        "DesiderativeVP|好": ["really / very", "Strengthens the following want expression."],
        "ExperientialQuestion|未": ["yet? / ever?", "Asks whether the experience has happened yet."]
      },
      surface: {
        // Authorized Glossika Week 16 hobbies/free-time surfaces.
        "睇戲": ["watch a movie", "Activity phrase."],
        "行山": ["go hiking", "Activity phrase."],
        "游水": ["swim", "Activity phrase."],
        "跑步": ["run", "Activity phrase."],
        "影相": ["take photos", "Activity phrase."],
        "聽歌": ["listen to music", "Activity phrase."],
        "睇書": ["read", "Activity phrase."],
        "打機": ["play video games", "Activity phrase."],
        "煮嘢食": ["cook", "Activity phrase; literally cook food."],
        "唱K": ["do karaoke", "Code-switched activity phrase."],
        "行街": ["go shopping / browse shops", "Conventional activity phrase."],
        "去沙灘": ["go to the beach", "Movement plus destination."],
        "做運動": ["exercise", "Activity phrase."],
        "畫畫": ["draw / paint", "Activity phrase."],
        "踢波": ["play football / soccer", "Activity phrase; exact sport depends on context."],
        "打波": ["play ball", "Activity phrase; exact ball game depends on context."],
        "彈琴": ["play piano / a stringed keyboard instrument", "Activity phrase."],
        "釣魚": ["fish", "Activity phrase."],
        "旅行": ["travel", "Activity phrase."],
        "唱歌": ["sing", "Activity phrase."],
        "睇波": ["watch a game", "Activity phrase; normally a ball game."],
        "瑜伽": ["yoga", "Activity noun."],
        "露營": ["go camping", "Activity phrase."],
        "下棋": ["play chess / a board game", "Activity phrase; exact game depends on context."],
        "行公園": ["walk in the park", "Conventional activity phrase in the supplied lesson."],
        "上個禮拜": ["last week", "Time phrase."],
        "上次": ["last time", "Time phrase."],
        "下次": ["next time", "Time phrase."],
        "下個星期五": ["next Friday", "Time phrase."],
        "上嚟": ["come up", "Movement phrase."],
        "落嚟": ["come down", "Movement phrase."],
        "返嚟": ["come back", "Movement phrase."],
        "返上嚟": ["come back up", "Movement phrase."],
        "快啲": ["quickly / hurry up", "Tells how the action happens."],
        "啲芒果": ["some mangoes", "Noun phrase."],
        "可唔可以": ["can / may", "Asks whether something is allowed or possible."],
        "一個": ["one", "One item with a measure word."],
        "一個故仔": ["one story", "Thing being talked about."],
        "一個蘋果": ["one apple", "Thing being acted on."],
        "第二個": ["another / other one", "A context-supplied choice; the exact reading depends on context."],
        "第二個故仔": ["the second story", "Thing being talked about."],
        "你要記得準時還返啲書": ["you need to remember to return the books on time", "VP complement frame."],
        "記得準時還返啲書": ["remember to return the books on time", "VP complement frame."],
        "呢個": ["this one", "Demonstrative and classifier used as a context-supplied noun phrase."]
      },
      construction: {
        TimeNP: ["time phrase", "Says when something happens."],
        DirectionalMotionVP: ["movement phrase", "Shows movement and direction."],
        DurativeVP: ["continuing-state phrase", "Shows that a state continues with 住."],
        PerfectiveDirectionalVP: ["completed movement phrase", "Shows completed movement with a final toward/away direction."],
        ProgressiveDirectionalVP: ["ongoing movement phrase", "Shows movement in progress along a visible path."],
        PotentialDirectionalVP: ["can-move phrase", "Says the movement can follow or reach the visible direction."],
        NegativePotentialDirectionalVP: ["cannot-move phrase", "Says the movement cannot follow or reach the visible direction."],
        RestorativeComplementVP: ["restore-state phrase", "Shows an action restoring a prior or expected state."],
        RepetitiveComplementVP: ["repeat-action phrase", "Shows an action being repeated or resumed."],
        CompoundDirectionalMotionVP: ["movement phrase", "Shows return plus direction."],
        NegatedDirectionalMotionVP: ["negative movement phrase", "Says not to move that way."],
        NegatedVP: ["negative action phrase", "Says that an action does not happen."],
        DegreeMannerAdverbial: ["how phrase", "Tells how the action happens."],
        VocativeAddressTerm: ["address phrase", "Names the person being spoken to."],
        ANotAQuestion: ["positive-or-negative question", "Asks which of two alternatives is true, such as eat or not eat."],
        ModalANotAQuestion: ["modal question", "Asks about wanting, permission, ability, or possibility using A-not-A modal structure."],
        CopularANotAQuestion: ["is-or-isn't question", "Asks whether something is or belongs as stated."],
        ExperientialYesNoQuestion: ["experience question", "Asks whether someone has ever done or experienced something."],
        PolarQuestionFrame: ["biased yes-or-no question", "Asks whether a complete statement is true, often with surprise or doubt."],
        DiscourseParticleFrame: ["particle-marked statement", "Adds one or more visible sentence-final particles to a complete statement."],
        FocusParticleFrame: ["restrictive focus phrase", "Limits a visible amount or scalar expression with only / just."],
        ExistentialClause: ["have / exist clause", "Says that someone has something or that something exists."],
        ExistentialPresentationalClause: ["existential presentation", "Introduces a participant and then states where that participant is."],
        ExistentialQuestion: ["have / exist question", "Asks whether something is present, available, or possessed."],
        ExistentialWhQuestion: ["what exists / what is available question", "Asks what thing or kind of thing is present, available, or possessed."],
        CompletionVP: ["finished-action phrase", "Shows that an action is finished or completed."],
        CompletionQuestion: ["finished-yet question", "Asks whether an action has been completed yet."],
        PerfectiveVP: ["perfective action phrase", "Marks an action as a viewed whole with 咗; context supplies its time interpretation."],
        PostverbalZoPerfectiveVP: ["perfective action phrase", "Marks a visible action + 咗 + overt object as perfective. Time, completion, and discourse interpretation are handled separately."],
        ExperientialVP: ["past-experience phrase", "Shows that someone has done or experienced the action before."],
        ReduplicatedVP: ["light-action phrase", "Repeating the verb makes the action sound brief, light, or casual."],
        CoverbFrame: ["pre-action relation phrase", "Introduces a participant, place, source, or tool before the main action."],
        PostThemeParticipantRelation: ["for / to", "Links the preceding action and thing to a following person. The exact link depends on the verb and context."],
        ReportedSpeech: ["reported speech", "Says what someone said or reported."],
        OpinionStanceFrame: ["opinion frame", "Introduces what someone thinks or feels."],
        ModalVP: ["modal action phrase", "Shows possibility, permission, ability, desire, or obligation before an action."],
        StativePredicate: ["state or quality", "Describes a state or quality."],
        DegreeStativePredicate: ["degree and quality phrase", "Says how strongly a state or quality applies."],
        ActionStativeVP: ["action plus quality phrase", "Links an action-like predicate to a following state or quality."],
        StativeNominalComplement: ["descriptive nominal complement", "Turns a visible description into the complement of is / be."],
        CopularRelationFrame: ["is / relation phrase", "Relates something to a description, identity, or category."],
        ComplementEllipsisFragment: ["context-supplied complement", "Leaves a selected complement or domain understood from discourse."],
        SubjectPredicateClause: ["subject-and-predicate clause", "Says what the subject does, is, or experiences."],
        FragmentQuestion: ["fragment question", "Asks about a context-supplied topic, alternative, or missing item."],
        MannerAdverbialVP: ["manner phrase", "Shows how an action is carried out."],
        QuantityNP: ["quantity noun phrase", "A quantity expression with a visible noun head."],
        MeasureExpression: ["measure expression", "A visible number and unit used to state age, price, area, or length."],
        NominalPredicateClause: ["measure-predicate clause", "States an age, price, area, or length without inserting a hidden copula."],
        QuantifiedClassifierNP: ["counted noun phrase", "A numeral, measure word, and visible noun head."],
        DemonstrativeClassifierNP: ["demonstrative noun phrase", "A phrase such as this one or this story, with a demonstrative, measure word, and noun."],
        OvertHeadDemonstrativeClassifierNP: ["demonstrative-classifier noun phrase", "A phrase with a visible demonstrative, visible measure word, and visible noun, such as 呢本書."],
        HeadlessDemonstrativeClassifierNP: ["headless demonstrative-classifier phrase", "A phrase such as 呢個 or 嗰本 with no visible noun. No missing noun is inserted."],
        QuantifiedTimeNP: ["quantified time phrase", "A time expression quantified by every/each or a numeral."],
        ApproximateQuantity: ["approximate quantity phrase", "A numeral and measure word marked as about or approximately that amount."],
        SourceMotionClause: ["source-motion clause", "Shows a movement beginning from a source place."],
        DiMarkedNP: ["noun phrase", "Uses 啲 before a noun: some / the / these items."],
        AssociativeNP: ["linked noun phrase", "A modifier or possessor joined to a visible noun by 嘅."],
        CoordinatedNP: ["coordinated noun phrase", "Two noun phrases joined by and."],
        ModifiedNP: ["modified noun phrase", "A determiner or modifier plus a visible noun head."],
        NominalHeadSpan: ["noun phrase", "A noun used as a complete noun phrase."],
        MotionGoalVP: ["motion goal", "Movement toward a place or destination."],
        DirectedMannerMotionVP: ["directed movement", "Shows how someone moves along a path or toward a destination."],
        GoalAttainmentMotionVP: ["reached-destination movement", "Shows movement successfully reaching a visible destination."],
        MotionPurposeChain: ["movement for a purpose", "Shows movement followed by the action it is done for."],
        SerialVerbPurposeChain: ["linked action-purpose chain", "Shows visible actions in order, with a later purpose action."],
        PathPhrase: ["path or direction phrase", "Shows the route or direction of movement."],
        LocativeWhQuestion: ["where question", "Asks where someone went or where something happened."],
        LocativePostureVP: ["posture and place", "Says someone is sitting, standing, or lying in a place."],
        LocativePlacePhrase: ["location phrase", "Introduces or states a place."],
        TransitiveVP: ["action phrase", "An action with a thing after it."],
        ProductiveVO: ["action phrase", "An action with a thing after it."],
        ProgressiveVP: ["ongoing action", "Shows an action happening now."],
        LexicalGiveRelation: ["give", "Shows lexical GIVE with two overt participants; only the reviewed baseline receives candidate roles."],
        NegatedExistentialClause: ["no / not have", "Says something is not there or not available."],
        FragmentAnswer: ["short answer", "A short answer whose full meaning comes from context."],
        NegatedExistentialFragment: ["negative existence answer", "A short no / do not have / there is not response whose domain comes from context."],
        FormulaDiscourseUnit: ["set response or social phrase", "A conventional response or social expression used as one discourse unit."],
        MalformedCandidate: ["needs checking", "This line looks incomplete or unusual."],
        ProhibitiveImperative: ["don't-do command", "Tells someone not to do the following action."],
        IdentificationFragment: ["identification phrase", "Points out or identifies the context-supplied item, such as this one."],
        ExperientialQuestion: ["experience question", "Asks whether the experience has happened."],
        DesiderativeVP: ["wanting phrase", "Expresses wanting to do the following action."],
        AcceptabilityANotA: ["okay-or-not question", "Asks whether something is okay, possible, or acceptable."],
        LocativeFragment: ["location fragment", "States a place while the person or thing located there is understood from context."],
        NeedsContext: ["needs context", "Meaning depends on the situation around it."],
        VerbComplementVP: ["verb-complement phrase", "A main verb plus a complement, with an object when present."],
        ResultComplement: ["result phrase", "Says an action reaches or attains a result."],
        ResultComplementVP: ["result-complement phrase", "Shows an action and its visible resulting state."],
        NegativePotentialComplement: ["cannot-result phrase", "Says an action cannot reach or attain the result."],
        ClauseRelationGraph: ["linked clauses", "Two connected Cantonese phrases or clauses."],
        ConditionalClause: ["if-clause", "States the condition for a following result."],
        AcceptabilityClause: ["okay / acceptable", "Says that an action or situation is okay or allowed."],
        IntendedFunctionRelation: ["use or function", "Connects a thing or resource to what it is used for."],
        PassivePermissiveRelation: ["passive or let / allow relation", "Connects a participant, 畀/俾, another participant, and an event while preserving passive-versus-permission ambiguity."]
      }
    };
  }
});

// src/render/learner-display.js
var require_learner_display = __commonJS({
  "src/render/learner-display.js"(exports2, module2) {
    "use strict";
    module2.exports = function createLearnerDisplay2(dependencies = {}) {
      const { TOKEN_LEXICON: TOKEN_LEXICON2, LEARNER_CONTEXTUAL_GLOSSES: LEARNER_CONTEXTUAL_GLOSSES2, LEARNER_SURFACE_GLOSSES: LEARNER_SURFACE_GLOSSES2, LEARNER_CONSTRUCTION_GLOSSES: LEARNER_CONSTRUCTION_GLOSSES2, diagnosticCompatibilityConstructionType: diagnosticCompatibilityConstructionType2, nodeDisplaySurface: nodeDisplaySurface2 } = dependencies;
      function cleanLearnerNote2(note) {
        let text = String(note || "").trim();
        if (!text) return "";
        text = text.split(/[.;]/)[0].trim();
        if (/\b(parser|diagnostic|template|construction|contextual|affordance|slot|syntax|role|internal|retained|transparent|protected|fallback|registry|tokeni[sz]ation|surface|native-speech|implementation)\b/i.test(text)) return "";
        return text;
      }
      function learnerGlossFromLexicon2(surface) {
        const entry = TOKEN_LEXICON2[String(surface || "")] || {};
        return cleanLearnerNote2(entry.note);
      }
      function contextualLearnerGlossLinesForToken2(node) {
        const surface = node && node.surface ? String(node.surface) : "";
        const syntax = node && node.syntax ? String(node.syntax) : "";
        if (surface === "吓" && /(^|\s)delimitative_aspect(\s|$)/.test(syntax)) {
          return ["try briefly / do a little", "Marks a light action."];
        }
        if (surface === "幾" && /(^|\s)scalar_value_question(\s|$)/.test(syntax)) {
          return ["how much / to what degree", "Asks about a value on this scale."];
        }
        if (surface === "呢" && /(^|\s)discourse_fragment_question(\s|$)/.test(syntax)) {
          return ["what about…? / and…?", "Returns to a topic or asks about a context-supplied alternative."];
        }
        if (surface === "呢" && /(^|\s)sentence_final_question_particle(\s|$)/.test(syntax)) {
          return ["question particle", "Softly asks for an answer or confirmation."];
        }
        if (/(^|\s)have_or_not_experiential_question_marker(\s|$)/.test(syntax)) {
          return ["ever ... or not?", "Asks whether the experience happened."];
        }
        if (/(^|\s)have_or_not_event_question_marker(\s|$)/.test(syntax)) {
          return ["did ... or not?", "Asks whether the event happened."];
        }
        if (surface === "嘅話" && /(^|\s)conditional_marker(\s|$)/.test(syntax)) {
          return ["if / in the case that", "Introduces a condition for a result."];
        }
        if (surface === "用" && /(^|\s)intended_function_use_marker(\s|$)/.test(syntax)) {
          return ["used for", "Part of 用嚟/用來, linking a thing to its stated use or function."];
        }
        if (["嚟", "來"].includes(surface) && /(^|\s)intended_function_lai_linker(\s|$)/.test(syntax)) {
          return ["for / to", "Links the thing to its stated use or function; it is not a movement verb here."];
        }
        return null;
      }
      function learnerGlossLinesForToken2(node) {
        const parent = node && node.parent ? node.parent : "";
        const surface = node && node.surface ? node.surface : "";
        const traceLearnerGloss = node && node.trace && Array.isArray(node.trace.learner_gloss_lines) ? node.trace.learner_gloss_lines.filter(Boolean) : [];
        if (traceLearnerGloss.length) return traceLearnerGloss;
        const syntaxContextual = contextualLearnerGlossLinesForToken2(node);
        if (syntaxContextual) return syntaxContextual;
        const contextual = LEARNER_CONTEXTUAL_GLOSSES2[`${parent}|${surface}`];
        if (contextual) return contextual;
        const surfaceGloss = LEARNER_SURFACE_GLOSSES2[surface];
        if (surfaceGloss) return surfaceGloss;
        const lexiconGloss = cleanLearnerNote2(node && node.note) || learnerGlossFromLexicon2(surface);
        if (lexiconGloss) return [lexiconGloss];
        if (node && node.jyutping) return ["Cantonese word."];
        return ["Cantonese text."];
      }
      function learnerGlossLinesForConstruction2(node) {
        if (!node || node.kind !== "construction") return ["Cantonese phrase."];
        const surfaceGloss = LEARNER_SURFACE_GLOSSES2[node.surface || ""];
        if (surfaceGloss) return surfaceGloss;
        const trace = node.trace || {};
        const traceLearnerGloss = Array.isArray(trace.learner_gloss_lines) ? trace.learner_gloss_lines.filter(Boolean) : [];
        if (traceLearnerGloss.length) return traceLearnerGloss;
        if (node.type === "ClauseRelationGraph" && trace.topic_chain_status === "licensed_overt_topic_chain") {
          return ["topic chain", "An overt topic supplies the understood object of later actions in the linked sequence."];
        }
        if (node.type === "ClauseRelationGraph" && trace.topic_chain_status === "antecedent_predicate_compatibility_review") {
          return ["possible topic chain — needs context", "A topic is present, but it does not clearly fit the omitted object of the later action."];
        }
        if (trace.particle_cluster_root) {
          if (node.type === "PolarQuestionFrame") {
            return ["biased yes-or-no question", "Asks whether the statement is true while preserving the visible inner particle sequence."];
          }
          return ["ordered particle phrase", "Shows several sentence-final particles in their visible inside-to-outside scope order."];
        }
        if (node.type === "DiscourseParticleFrame") {
          const family = String(trace.discourse_particle_family || "");
          if (family === "epistemic_stance") return ["uncertainty-marked statement", "Marks the statement as probable, possible, or uncertain."];
          if (family === "evidential_noteworthiness") return ["report or notice-marked statement", "Presents the statement as reported, noteworthy, a reminder, or unexpected."];
          if (family === "directive_interpersonal_closure") return ["prompt or closure phrase", "Uses a final particle for a directive, suggestion, invitation, or conversational closure."];
          if (family === "change_state_current_relevance") return ["changed-situation statement", "Marks a new, changed, or currently relevant situation."];
        }
        const constructionGlossType = node.compatibility_alias || diagnosticCompatibilityConstructionType2(node.type || "");
        const constructionGloss = LEARNER_CONSTRUCTION_GLOSSES2[constructionGlossType] || LEARNER_CONSTRUCTION_GLOSSES2[node.type || ""];
        if (constructionGloss) return constructionGloss;
        const noteGloss = cleanLearnerNote2(node.note || "");
        if (noteGloss) return [noteGloss];
        return ["Cantonese phrase."];
      }
      function compactLearnerHoverLines2(lines) {
        return [...new Set((lines || []).map((line) => String(line || "").trim()).filter(Boolean))];
      }
      function learnerVisibleSyntax2(syntax = "") {
        return String(syntax || "").split(/\s+/).filter(Boolean).filter((part) => !/^phase\d+_/i.test(part)).join(" ");
      }
      function learnerUiHoverTitleForToken2(node) {
        if (!node || node.kind !== "token") return "";
        const role = node.role || node.label || "";
        const syntax = learnerVisibleSyntax2(node.syntax || "");
        const displaySurface = node.display_surface || node.surface || "";
        return compactLearnerHoverLines2([
          displaySurface,
          role ? `role: ${role}` : "",
          syntax ? `syntax: ${syntax}` : "",
          node.jyutping || "",
          ...learnerGlossLinesForToken2(node)
        ]).join("\n");
      }
      function learnerUiHoverTitleForConstruction2(node) {
        if (!node || node.kind !== "construction") return "";
        return compactLearnerHoverLines2([
          node.display_surface || nodeDisplaySurface2(node) || node.surface || "",
          ...learnerGlossLinesForConstruction2(node)
        ]).join("\n");
      }
      function learnerUiHoverTitleForConstructionLayer2(node) {
        if (!node || node.kind !== "construction") return "";
        return learnerUiHoverTitleForConstruction2(node);
      }
      return {
        cleanLearnerNote: cleanLearnerNote2,
        learnerGlossFromLexicon: learnerGlossFromLexicon2,
        contextualLearnerGlossLinesForToken: contextualLearnerGlossLinesForToken2,
        learnerGlossLinesForToken: learnerGlossLinesForToken2,
        learnerGlossLinesForConstruction: learnerGlossLinesForConstruction2,
        compactLearnerHoverLines: compactLearnerHoverLines2,
        learnerVisibleSyntax: learnerVisibleSyntax2,
        learnerUiHoverTitleForToken: learnerUiHoverTitleForToken2,
        learnerUiHoverTitleForConstruction: learnerUiHoverTitleForConstruction2,
        learnerUiHoverTitleForConstructionLayer: learnerUiHoverTitleForConstructionLayer2
      };
    };
  }
});

// src/render/block-renderer.js
var require_block_renderer = __commonJS({
  "src/render/block-renderer.js"(exports2, module2) {
    "use strict";
    module2.exports = function createBlockRenderer(dependencies = {}) {
      const {
        Notice,
        diagnosticSummary: diagnosticSummary2,
        jyutpingAuditSummary: jyutpingAuditSummary2,
        formatDiagnosticMarkdown: formatDiagnosticMarkdown2,
        foldedLexicalRepairDisplayRows: foldedLexicalRepairDisplayRows2,
        normalizationReviewSuggestionDisplayRows: normalizationReviewSuggestionDisplayRows2,
        learnerUiHoverAuditSummary: learnerUiHoverAuditSummary2,
        learnerUiHoverAuditRows: learnerUiHoverAuditRows2,
        diagnosticTokenRows: diagnosticTokenRows2,
        diagnosticFinalRows: diagnosticFinalRows2,
        diagnosticLegend: diagnosticLegend2,
        normalizeSurface: normalizeSurface2,
        safeClass: safeClass2,
        nodeDisplaySurface: nodeDisplaySurface2,
        learnerUiHoverTitleForToken: learnerUiHoverTitleForToken2,
        learnerUiHoverTitleForConstruction: learnerUiHoverTitleForConstruction2,
        learnerUiHoverTitleForConstructionLayer: learnerUiHoverTitleForConstructionLayer2,
        shouldCollapseClauseSequenceForDisplay: shouldCollapseClauseSequenceForDisplay2,
        shouldCollapseGreedyWrapperForDisplay: shouldCollapseGreedyWrapperForDisplay2,
        parserShadowRepairDisplayRows: parserShadowRepairDisplayRows2,
        parserShadowRepairKindLabel: parserShadowRepairKindLabel2,
        splitJyutping: splitJyutping2
      } = dependencies;
      function createEl(tag, className, text) {
        const element = document.createElement(tag);
        if (className) element.className = className;
        if (text !== void 0 && text !== null) element.textContent = String(text);
        return element;
      }
      function renderParserShadowRepairs(analysis, container) {
        const rows = parserShadowRepairDisplayRows2(analysis);
        if (!rows.length) return;
        const box = createEl("div", "canto-normalization-shadow-repairs canto-normalization-shadow-repairs-compact");
        const label = rows.length > 1 ? `Shadow ×${rows.length}:` : "Shadow:";
        box.appendChild(createEl("span", "canto-normalization-shadow-label", label));
        box.setAttribute("title", rows.map((row) => {
          const kindLabel = parserShadowRepairKindLabel2(row);
          const note = row.note || "Parser-shadow repair applied to parser shadow only.";
          return `${row.display} — ${kindLabel}. ${note} Raw learner-visible text is not replaced.`;
        }).join("\n"));
        rows.forEach((row) => {
          const item = createEl("span", "canto-normalization-shadow-repair");
          const kindLabel = parserShadowRepairKindLabel2(row);
          const titleParts = [
            `${row.display} — ${kindLabel}.`,
            row.note || "Parser-shadow repair applied to parser shadow only.",
            "Raw learner-visible text is not replaced."
          ];
          item.setAttribute("title", titleParts.join(" "));
          item.appendChild(createEl("span", "canto-normalization-shadow-raw", row.raw));
          item.appendChild(createEl("span", "canto-normalization-shadow-arrow", "→"));
          item.appendChild(createEl("span", "canto-normalization-shadow-normalized", row.normalized));
          box.appendChild(item);
        });
        container.appendChild(box);
      }
      async function copyTextToClipboard(text) {
        if (typeof navigator !== "undefined" && navigator.clipboard && navigator.clipboard.writeText) {
          await navigator.clipboard.writeText(text);
          return;
        }
        const textarea = document.createElement("textarea");
        textarea.value = text;
        textarea.setAttribute("readonly", "");
        textarea.style.position = "fixed";
        textarea.style.left = "-9999px";
        document.body.appendChild(textarea);
        textarea.select();
        document.execCommand("copy");
        textarea.remove();
      }
      function renderCopyButton(parent, textFactory, label, successMessage) {
        const button = createEl("button", "canto-diagnostic-copy canto-diagnostic-ui-only", label);
        button.type = "button";
        button.addEventListener("click", async (event) => {
          event.preventDefault();
          event.stopPropagation();
          try {
            const text = await Promise.resolve(textFactory());
            if (!String(text || "").trim()) {
              new Notice("No Canto Span diagnostics found to copy.");
              return;
            }
            await copyTextToClipboard(text);
            new Notice(successMessage || "Copied Canto Span diagnostic.");
          } catch (error) {
            console.error("Canto Span diagnostic copy failed", error);
            new Notice("Could not copy diagnostic text.");
          }
        });
        parent.appendChild(button);
        return button;
      }
      function renderActionButton(parent, action, label) {
        const button = createEl("button", "canto-diagnostic-copy canto-diagnostic-ui-only", label);
        button.type = "button";
        button.addEventListener("click", async (event) => {
          event.preventDefault();
          event.stopPropagation();
          try {
            await action();
          } catch (error) {
            console.error("Canto Span diagnostic action failed", error);
            new Notice("Could not run Canto Span diagnostic action.");
          }
        });
        parent.appendChild(button);
        return button;
      }
      function renderPre(parent, className, value) {
        const pre = createEl("pre", className);
        pre.textContent = typeof value === "string" ? value : JSON.stringify(value, null, 2);
        parent.appendChild(pre);
      }
      function renderDiagnosticDetails(analysis, container, expanded = false) {
        const details = createEl("details", "canto-diagnostic");
        details.open = Boolean(expanded);
        const summary = diagnosticSummary2(analysis);
        const jyutping = jyutpingAuditSummary2(analysis);
        const reviewNote = summary.review_count ? ` — ${summary.review_count} review trace(s)` : " — no review traces";
        const jyutpingNote = jyutping.status === "WARN" ? ` — Jyutping WARN (${jyutping.items_needing_review.length})` : " — Jyutping PASS";
        details.appendChild(createEl("summary", "canto-diagnostic-summary", `Parse diagnostic${reviewNote}${jyutpingNote}`));
        const body = createEl("div", "canto-diagnostic-body");
        const toolbar = createEl("div", "canto-diagnostic-toolbar");
        renderCopyButton(toolbar, () => formatDiagnosticMarkdown2(analysis), "Copy diagnostic", "Copied this Canto Span diagnostic.");
        body.appendChild(toolbar);
        body.appendChild(createEl("div", "canto-diagnostic-caption", "Summary"));
        renderPre(body, "canto-diagnostic-json", summary);
        const foldedRepairRows = foldedLexicalRepairDisplayRows2(analysis);
        if (foldedRepairRows.length) {
          body.appendChild(createEl("div", "canto-diagnostic-caption", "Folded pinyin-fallout lexical repairs"));
          renderPre(body, "canto-diagnostic-json", {
            status: "PASS",
            policy: "Repairs are applied to parser shadow only. Raw learner-visible text is preserved.",
            repairs: foldedRepairRows
          });
        }
        const reviewSuggestionRows = normalizationReviewSuggestionDisplayRows2(analysis);
        if (reviewSuggestionRows.length) {
          body.appendChild(createEl("div", "canto-diagnostic-caption", "Review-only normalization suggestions"));
          renderPre(body, "canto-diagnostic-json", {
            status: "PASS",
            policy: "Compatibility lane only. Current v0.5.107 pinyin-fallout repairs should not depend on review-only suggestions.",
            suggestions: reviewSuggestionRows
          });
        }
        body.appendChild(createEl("div", "canto-diagnostic-caption", "Jyutping audit"));
        renderPre(body, "canto-diagnostic-json", jyutpingAuditSummary2(analysis));
        body.appendChild(createEl("div", "canto-diagnostic-caption", "Learner UI hover audit"));
        renderPre(body, "canto-diagnostic-json", learnerUiHoverAuditSummary2(analysis));
        body.appendChild(createEl("div", "canto-diagnostic-caption", "Learner UI hover rows"));
        renderPre(body, "canto-diagnostic-json", learnerUiHoverAuditRows2(analysis));
        body.appendChild(createEl("div", "canto-diagnostic-caption", "Tokenization before construction wrapping"));
        renderPre(body, "canto-diagnostic-json", diagnosticTokenRows2(analysis));
        body.appendChild(createEl("div", "canto-diagnostic-caption", "Final construction tree"));
        renderPre(body, "canto-diagnostic-json", diagnosticFinalRows2(analysis));
        body.appendChild(createEl("div", "canto-diagnostic-caption", "Legend"));
        renderPre(body, "canto-diagnostic-json", diagnosticLegend2());
        details.appendChild(body);
        container.appendChild(details);
      }
      function parseContextDirectiveLine(line, sourceLineIndex) {
        const raw = String(line || "");
        const trimmed = raw.trim().replace(/^[\uFEFF\u200B-\u200D\u2060]+/u, "");
        const prefix = trimmed.slice(0, 8);
        if (prefix.normalize("NFKC").toLowerCase() !== "@context") return null;
        let remainder = trimmed.slice(8).replace(/^[\s\u00A0\u3000]+/u, "");
        const separator = remainder.charAt(0);
        if (separator !== ":" && separator !== "：") return null;
        remainder = remainder.slice(1);
        const contextSource = remainder.trim();
        return {
          id: `block-context-${sourceLineIndex}`,
          directive_kind: "explicit_context_next_source",
          directive_line_index: sourceLineIndex,
          raw_directive: raw,
          separator_code_point: separator.codePointAt(0).toString(16).toUpperCase().padStart(4, "0"),
          source: contextSource
        };
      }
      function parseBlockOptions2(source) {
        const lines = String(source || "").split(/\r?\n/);
        const options = {
          lines: [],
          entries: [],
          context_directives: [],
          directive_warnings: []
        };
        let pendingContext = null;
        for (let sourceLineIndex = 0; sourceLineIndex < lines.length; sourceLineIndex += 1) {
          const line = lines[sourceLineIndex];
          const oneBasedLineIndex = sourceLineIndex + 1;
          const trimmed = line.trim();
          if (trimmed === "@diagnostic" || trimmed === "@debug" || trimmed === "@trace") continue;
          const contextDirective = parseContextDirectiveLine(line, oneBasedLineIndex);
          if (contextDirective) {
            if (!normalizeSurface2(contextDirective.source)) {
              options.directive_warnings.push({
                kind: "empty_context_directive",
                directive_line_index: oneBasedLineIndex,
                raw_directive: line
              });
              pendingContext = null;
              continue;
            }
            if (pendingContext) {
              options.directive_warnings.push({
                kind: "unconsumed_context_directive_replaced",
                directive_line_index: pendingContext.directive_line_index,
                replacement_directive_line_index: oneBasedLineIndex,
                raw_directive: pendingContext.raw_directive
              });
            }
            pendingContext = contextDirective;
            options.context_directives.push({ ...contextDirective });
            continue;
          }
          options.lines.push(line);
          const isSourceRow = Boolean(normalizeSurface2(line));
          const appliedContext = pendingContext && isSourceRow ? {
            ...pendingContext,
            target_source_line_index: oneBasedLineIndex
          } : null;
          const entry = {
            line,
            source_line_index: oneBasedLineIndex,
            context_directive: appliedContext,
            context: appliedContext ? { turns: [{ ...appliedContext }] } : null
          };
          options.entries.push(entry);
          if (isSourceRow) pendingContext = null;
        }
        if (pendingContext) {
          options.directive_warnings.push({
            kind: "dangling_context_directive",
            directive_line_index: pendingContext.directive_line_index,
            raw_directive: pendingContext.raw_directive,
            source: pendingContext.source
          });
        }
        return options;
      }
      function renderJyutping(container, jyutping) {
        const wrapper = createEl("span", "canto-jyutping");
        for (const syllable of splitJyutping2(jyutping)) {
          const match = syllable.match(/^(.*?)([1-6])$/);
          const syl = createEl("span", `vjp-syl ${match ? `vjp-tone-${match[2]}` : "vjp-no-tone"}`);
          if (match) {
            syl.appendChild(createEl("span", "vjp-base", match[1]));
            syl.appendChild(createEl("span", "vjp-num", match[2]));
          } else {
            syl.appendChild(createEl("span", "vjp-base", syllable));
          }
          wrapper.appendChild(syl);
        }
        container.appendChild(wrapper);
      }
      function renderToken(node, container) {
        const cls = [`canto-token`, `cs-${safeClass2(node.label)}`];
        if (node.label === "neutral") cls.push("canto-token-neutral", "canto-lexical-unknown");
        const displaySurface = node.display_surface || node.surface;
        const span = createEl("span", cls.join(" "));
        span.setAttribute("tabindex", "0");
        span.setAttribute("data-canto-kind", "token");
        span.setAttribute("data-canto-surface", displaySurface);
        span.setAttribute("data-canto-parser-surface", node.surface);
        span.setAttribute("data-canto-role", node.label);
        span.setAttribute("data-canto-syntax", node.syntax);
        span.setAttribute("data-canto-review", node.review);
        span.setAttribute("title", learnerUiHoverTitleForToken2(node));
        if (node.jyutping) renderJyutping(span, node.jyutping);
        span.appendChild(createEl("span", "canto-hanzi", displaySurface));
        container.appendChild(span);
      }
      function collectConstructionLayout(node) {
        const leaves = [];
        const spans = [];
        function walk(current, depth) {
          if (!current) return { start: leaves.length, end: leaves.length };
          if (current.kind !== "construction") {
            const start2 = leaves.length;
            leaves.push(current);
            return { start: start2, end: leaves.length };
          }
          const start = leaves.length;
          for (const child of current.children || []) walk(child, depth + 1);
          const end = leaves.length;
          spans.push({ node: current, start, end, depth, width: end - start });
          return { start, end };
        }
        walk(node, 0);
        return { leaves, spans };
      }
      function renderConstructionLeaf(node, container) {
        if (!node) return;
        if (node.kind === "text") {
          container.appendChild(document.createTextNode(node.display_text || node.text));
          return;
        }
        if (node.kind === "token") {
          renderToken(node, container);
        }
      }
      function renderConstruction(node, container) {
        const { leaves, spans } = collectConstructionLayout(node);
        const cls = ["canto-pattern", "canto-pattern-layered", `canto-construction-${safeClass2(node.type)}`];
        if (node.primary) cls.push("canto-primary-phrase", `canto-primary-${safeClass2(node.primary)}`);
        const outer = createEl("span", cls.join(" "));
        outer.setAttribute("data-canto-kind", "construction");
        outer.setAttribute("data-canto-construction", node.type);
        outer.setAttribute("title", learnerUiHoverTitleForConstruction2(node));
        outer.style.gridTemplateColumns = leaves.length ? `repeat(${leaves.length}, max-content)` : "max-content";
        leaves.forEach((leaf, index) => {
          const cell = createEl("span", "canto-pattern-leaf");
          cell.style.gridColumn = `${index + 1} / ${index + 2}`;
          cell.style.gridRow = "1";
          renderConstructionLeaf(leaf, cell);
          outer.appendChild(cell);
        });
        spans.filter((span) => span.width > 0).sort((a, b) => {
          if (b.width !== a.width) return b.width - a.width;
          if (a.start !== b.start) return a.start - b.start;
          return a.depth - b.depth;
        }).forEach((span, layerIndex) => {
          const layer = createEl("span", `canto-construction-layer canto-construction-depth-${Math.min(layerIndex, 7)}`);
          layer.style.gridColumn = `${span.start + 1} / ${span.end + 1}`;
          layer.style.gridRow = `${layerIndex + 2}`;
          layer.setAttribute("data-canto-construction", span.node.type);
          layer.setAttribute("title", learnerUiHoverTitleForConstructionLayer2(span.node));
          layer.appendChild(createEl("span", "canto-construction-line"));
          layer.appendChild(createEl("span", "canto-pattern-label", span.node.label));
          outer.appendChild(layer);
        });
        container.appendChild(outer);
      }
      function renderNode(node, container, options = {}) {
        if (!node) return;
        if (node.kind === "text") {
          container.appendChild(document.createTextNode(node.text));
          return;
        }
        if (node.kind === "token") {
          renderToken(node, container);
          return;
        }
        if (node.kind === "construction") {
          if (shouldCollapseClauseSequenceForDisplay2(node, options) || shouldCollapseGreedyWrapperForDisplay2(node, options)) {
            for (const child of node.children || []) renderNode(child, container, options);
            return;
          }
          renderConstruction(node, container);
        }
      }
      return {
        createEl,
        copyTextToClipboard,
        renderCopyButton,
        renderActionButton,
        renderPre,
        renderDiagnosticDetails,
        parseContextDirectiveLine,
        parseBlockOptions: parseBlockOptions2,
        splitJyutping: splitJyutping2,
        renderJyutping,
        renderToken,
        collectConstructionLayout,
        renderConstructionLeaf,
        renderConstruction,
        renderNode,
        renderParserShadowRepairs
      };
    };
  }
});

// src/runtime-resources/presentation/role-colors.js
var require_role_colors = __commonJS({
  "src/runtime-resources/presentation/role-colors.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      { key: "who", label: "who", cssVars: ["who"], defaultColor: "#FF80AB" },
      { key: "doing", label: "doing", cssVars: ["doing"], defaultColor: "#FF3B30" },
      { key: "what", label: "what", cssVars: ["what"], defaultColor: "#00E5FF" },
      { key: "where", label: "where", cssVars: ["where"], defaultColor: "#40A9FF" },
      { key: "when", label: "when", cssVars: ["when"], defaultColor: "#FFF500" },
      { key: "why", label: "why", cssVars: ["why"], defaultColor: "#B967FF" },
      { key: "how", label: "how", cssVars: ["how"], defaultColor: "#FF9500" },
      { key: "like", label: "like", cssVars: ["like"], defaultColor: "#39FF14" },
      { key: "measure_word", label: "measure word / classifier", cssVars: ["measure-word", "classifier"], defaultColor: "#FF00D4" },
      { key: "particle", label: "particle", cssVars: ["particle"], defaultColor: "#BFD7EA" },
      { key: "func", label: "func", cssVars: ["func"], defaultColor: "#FFFFFF" }
    ];
  }
});

// src/plugin/settings.js
var require_settings = __commonJS({
  "src/plugin/settings.js"(exports2, module2) {
    "use strict";
    var BLOCK_CLASS = "canto-study-block canto-span-block";
    var GLOBAL_COLOR_ACCESSIBILITY_CLASS = "canto-global-color-accessibility-enabled";
    var GLOBAL_DIAGNOSTICS_CLASS = "canto-global-diagnostics-enabled";
    var GLOBAL_DIAGNOSTICS_EXPANDED_CLASS = "canto-global-diagnostics-expanded";
    var ROLE_COLOR_SETTINGS = require_role_colors();
    var DEFAULT_ROLE_COLORS = Object.fromEntries(
      ROLE_COLOR_SETTINGS.map((role) => [role.key, role.defaultColor])
    );
    var DEFAULT_SETTINGS = {
      showDiagnostics: true,
      expandDiagnostics: false,
      showColorAccessibilityMarkers: false,
      roleColors: { ...DEFAULT_ROLE_COLORS }
    };
    function normalizeHexColor(value) {
      const raw = String(value || "").trim();
      const short = raw.match(/^#([0-9a-f]{3})$/i);
      if (short) {
        return `#${short[1].split("").map((ch) => ch + ch).join("")}`.toUpperCase();
      }
      const long = raw.match(/^#([0-9a-f]{6})$/i);
      return long ? `#${long[1]}`.toUpperCase() : "";
    }
    function getMergedRoleColors(settings) {
      const userColors = settings && settings.roleColors && typeof settings.roleColors === "object" ? settings.roleColors : {};
      const merged = { ...DEFAULT_ROLE_COLORS };
      for (const role of ROLE_COLOR_SETTINGS) {
        let normalized = normalizeHexColor(userColors[role.key]);
        merged[role.key] = normalized || role.defaultColor;
      }
      return merged;
    }
    module2.exports = {
      BLOCK_CLASS,
      GLOBAL_COLOR_ACCESSIBILITY_CLASS,
      GLOBAL_DIAGNOSTICS_CLASS,
      GLOBAL_DIAGNOSTICS_EXPANDED_CLASS,
      ROLE_COLOR_SETTINGS,
      DEFAULT_ROLE_COLORS,
      DEFAULT_SETTINGS,
      normalizeHexColor,
      getMergedRoleColors
    };
  }
});

// src/plugin/canto-span-plugin.js
var require_canto_span_plugin = __commonJS({
  "src/plugin/canto-span-plugin.js"(exports2, module2) {
    "use strict";
    var { Plugin, PluginSettingTab, Setting, Notice } = require("obsidian");
    var createBlockRenderer = require_block_renderer();
    var {
      BLOCK_CLASS,
      GLOBAL_COLOR_ACCESSIBILITY_CLASS,
      GLOBAL_DIAGNOSTICS_CLASS,
      GLOBAL_DIAGNOSTICS_EXPANDED_CLASS,
      ROLE_COLOR_SETTINGS,
      DEFAULT_ROLE_COLORS,
      DEFAULT_SETTINGS,
      normalizeHexColor,
      getMergedRoleColors
    } = require_settings();
    module2.exports = function createCantoSpanPlugin2(dependencies = {}) {
      const {
        CANTO_SPAN_RUNTIME_VERSION: CANTO_SPAN_RUNTIME_VERSION2,
        analyzeLine: analyzeLine2,
        normalizeSurface: normalizeSurface2,
        formatDiagnosticMarkdownForNote: formatDiagnosticMarkdownForNote2,
        diagnosticExportPayloadsForNote: diagnosticExportPayloadsForNote2,
        formatNoteMarkdownWithDiagnostics: formatNoteMarkdownWithDiagnostics2,
        formatDiagnosticMarkdown: formatDiagnosticMarkdown2,
        diagnosticSummary: diagnosticSummary2,
        jyutpingAuditSummary: jyutpingAuditSummary2,
        foldedLexicalRepairDisplayRows: foldedLexicalRepairDisplayRows2,
        normalizationReviewSuggestionDisplayRows: normalizationReviewSuggestionDisplayRows2,
        learnerUiHoverAuditSummary: learnerUiHoverAuditSummary2,
        learnerUiHoverAuditRows: learnerUiHoverAuditRows2,
        diagnosticTokenRows: diagnosticTokenRows2,
        diagnosticFinalRows: diagnosticFinalRows2,
        diagnosticLegend: diagnosticLegend2,
        safeClass: safeClass2,
        nodeDisplaySurface: nodeDisplaySurface2,
        learnerUiHoverTitleForToken: learnerUiHoverTitleForToken2,
        learnerUiHoverTitleForConstruction: learnerUiHoverTitleForConstruction2,
        learnerUiHoverTitleForConstructionLayer: learnerUiHoverTitleForConstructionLayer2,
        shouldCollapseClauseSequenceForDisplay: shouldCollapseClauseSequenceForDisplay2,
        shouldCollapseGreedyWrapperForDisplay: shouldCollapseGreedyWrapperForDisplay2,
        parserShadowRepairDisplayRows: parserShadowRepairDisplayRows2,
        parserShadowRepairKindLabel: parserShadowRepairKindLabel2,
        splitJyutping: splitJyutping2
      } = dependencies;
      const {
        createEl,
        renderCopyButton,
        renderActionButton,
        renderDiagnosticDetails,
        parseBlockOptions: parseBlockOptions2,
        renderNode,
        renderParserShadowRepairs
      } = createBlockRenderer({
        Notice,
        diagnosticSummary: diagnosticSummary2,
        jyutpingAuditSummary: jyutpingAuditSummary2,
        formatDiagnosticMarkdown: formatDiagnosticMarkdown2,
        foldedLexicalRepairDisplayRows: foldedLexicalRepairDisplayRows2,
        normalizationReviewSuggestionDisplayRows: normalizationReviewSuggestionDisplayRows2,
        learnerUiHoverAuditSummary: learnerUiHoverAuditSummary2,
        learnerUiHoverAuditRows: learnerUiHoverAuditRows2,
        diagnosticTokenRows: diagnosticTokenRows2,
        diagnosticFinalRows: diagnosticFinalRows2,
        diagnosticLegend: diagnosticLegend2,
        normalizeSurface: normalizeSurface2,
        safeClass: safeClass2,
        nodeDisplaySurface: nodeDisplaySurface2,
        learnerUiHoverTitleForToken: learnerUiHoverTitleForToken2,
        learnerUiHoverTitleForConstruction: learnerUiHoverTitleForConstruction2,
        learnerUiHoverTitleForConstructionLayer: learnerUiHoverTitleForConstructionLayer2,
        shouldCollapseClauseSequenceForDisplay: shouldCollapseClauseSequenceForDisplay2,
        shouldCollapseGreedyWrapperForDisplay: shouldCollapseGreedyWrapperForDisplay2,
        parserShadowRepairDisplayRows: parserShadowRepairDisplayRows2,
        parserShadowRepairKindLabel: parserShadowRepairKindLabel2,
        splitJyutping: splitJyutping2
      });
      class CantoSpanPlugin extends Plugin {
        async onload() {
          await this.loadSettings();
          this.renderedCantoBlocks = /* @__PURE__ */ new Map();
          this.syncGlobalSettingClasses();
          this.startDiagnosticDetailsObserver();
          this.registerMarkdownCodeBlockProcessor("canto-span", (source, element) => {
            this.renderedCantoBlocks.set(element, source);
            this.renderBlock(source, element);
          });
          this.addSettingTab(new CantoSpanSettingTab(this.app, this));
        }
        onunload() {
          if (this.diagnosticDetailsObserver) {
            this.diagnosticDetailsObserver.disconnect();
            this.diagnosticDetailsObserver = null;
          }
          if (typeof document !== "undefined" && document.body) {
            document.body.classList.remove(GLOBAL_COLOR_ACCESSIBILITY_CLASS);
            document.body.classList.remove(GLOBAL_DIAGNOSTICS_CLASS);
            document.body.classList.remove(GLOBAL_DIAGNOSTICS_EXPANDED_CLASS);
            this.clearRoleColorVariables();
          }
        }
        async loadSettings() {
          const loaded = await this.loadData();
          this.settings = Object.assign({}, DEFAULT_SETTINGS, loaded || {});
          this.settings.roleColors = getMergedRoleColors(this.settings);
        }
        async saveSettings() {
          await this.saveData(this.settings);
          this.syncGlobalSettingClasses();
        }
        syncGlobalSettingClasses() {
          if (typeof document === "undefined" || !document.body) return;
          this.pruneRenderedCantoBlocks();
          document.body.classList.toggle(
            GLOBAL_COLOR_ACCESSIBILITY_CLASS,
            Boolean(this.settings && this.settings.showColorAccessibilityMarkers)
          );
          document.body.classList.toggle(
            GLOBAL_DIAGNOSTICS_CLASS,
            Boolean(this.settings && this.settings.showDiagnostics)
          );
          document.body.classList.toggle(
            GLOBAL_DIAGNOSTICS_EXPANDED_CLASS,
            Boolean(this.settings && this.settings.expandDiagnostics)
          );
          this.syncRoleColorVariables();
          this.scheduleRenderedDiagnosticDetailsSync();
        }
        syncRoleColorVariables() {
          if (typeof document === "undefined" || !document.body) return;
          const colors = getMergedRoleColors(this.settings);
          this.settings.roleColors = colors;
          for (const role of ROLE_COLOR_SETTINGS) {
            const color = colors[role.key];
            for (const cssVar of role.cssVars) {
              document.body.style.setProperty(`--canto-user-color-${cssVar}`, color);
            }
          }
        }
        clearRoleColorVariables() {
          if (typeof document === "undefined" || !document.body) return;
          for (const role of ROLE_COLOR_SETTINGS) {
            for (const cssVar of role.cssVars) {
              document.body.style.removeProperty(`--canto-user-color-${cssVar}`);
            }
          }
        }
        pruneRenderedCantoBlocks() {
          if (!this.renderedCantoBlocks) return 0;
          let pruned = 0;
          for (const [element] of Array.from(this.renderedCantoBlocks.entries())) {
            if (!element || !element.isConnected) {
              this.renderedCantoBlocks.delete(element);
              pruned += 1;
            }
          }
          return pruned;
        }
        getRenderedDiagnosticDetails() {
          this.pruneRenderedCantoBlocks();
          const detailsSet = /* @__PURE__ */ new Set();
          if (this.renderedCantoBlocks) {
            for (const element of this.renderedCantoBlocks.keys()) {
              if (!element || !element.isConnected || typeof element.querySelectorAll !== "function") continue;
              for (const details of Array.from(element.querySelectorAll(".canto-study-block details.canto-diagnostic"))) {
                detailsSet.add(details);
              }
            }
          }
          if (typeof document !== "undefined" && typeof document.querySelectorAll === "function") {
            for (const details of Array.from(document.querySelectorAll(".canto-study-block details.canto-diagnostic"))) {
              detailsSet.add(details);
            }
          }
          return Array.from(detailsSet);
        }
        syncRenderedDiagnosticDetails() {
          const shouldOpen = Boolean(this.settings && this.settings.expandDiagnostics);
          for (const details of this.getRenderedDiagnosticDetails()) {
            if (shouldOpen) {
              details.open = true;
              details.setAttribute("open", "");
              details.setAttribute("data-canto-expanded-by-setting", "true");
            } else {
              details.open = false;
              details.removeAttribute("open");
              details.setAttribute("data-canto-expanded-by-setting", "false");
            }
          }
        }
        scheduleRenderedDiagnosticDetailsSync() {
          this.syncRenderedDiagnosticDetails();
          if (typeof requestAnimationFrame === "function") {
            requestAnimationFrame(() => this.syncRenderedDiagnosticDetails());
          }
          if (typeof window !== "undefined" && typeof window.setTimeout === "function") {
            window.setTimeout(() => this.syncRenderedDiagnosticDetails(), 0);
          }
        }
        queueDiagnosticDetailsSync() {
          if (this.diagnosticDetailsSyncQueued) return;
          this.diagnosticDetailsSyncQueued = true;
          const run = () => {
            this.diagnosticDetailsSyncQueued = false;
            this.syncRenderedDiagnosticDetails();
          };
          if (typeof requestAnimationFrame === "function") {
            requestAnimationFrame(run);
          } else if (typeof window !== "undefined" && typeof window.setTimeout === "function") {
            window.setTimeout(run, 0);
          } else {
            run();
          }
        }
        startDiagnosticDetailsObserver() {
          if (typeof MutationObserver === "undefined" || typeof document === "undefined" || !document.body) return;
          this.diagnosticDetailsObserver = new MutationObserver((mutations) => {
            for (const mutation of mutations) {
              for (const node of Array.from(mutation.addedNodes || [])) {
                if (!node || node.nodeType !== 1) continue;
                const element = node;
                if (element.matches && element.matches("details.canto-diagnostic")) {
                  this.queueDiagnosticDetailsSync();
                  return;
                }
                if (element.querySelector && element.querySelector("details.canto-diagnostic")) {
                  this.queueDiagnosticDetailsSync();
                  return;
                }
              }
            }
          });
          this.diagnosticDetailsObserver.observe(document.body, { childList: true, subtree: true });
        }
        refreshRenderedBlocks() {
          if (!this.renderedCantoBlocks) return;
          this.pruneRenderedCantoBlocks();
          for (const [element, source] of Array.from(this.renderedCantoBlocks.entries())) {
            if (!element || !element.isConnected) continue;
            this.renderBlock(source, element);
          }
        }
        async readActiveNoteMarkdown() {
          const file = this.app.workspace && this.app.workspace.getActiveFile ? this.app.workspace.getActiveFile() : null;
          if (!file) return { file: null, markdown: "" };
          const markdown = await this.app.vault.read(file);
          return { file, markdown };
        }
        async formatActiveNoteDiagnostics() {
          const { markdown } = await this.readActiveNoteMarkdown();
          return formatDiagnosticMarkdownForNote2(markdown);
        }
        async saveActiveNoteDiagnosticsJson() {
          const { file, markdown } = await this.readActiveNoteMarkdown();
          if (!file || !file.path) {
            new Notice("No active note found for Canto Span diagnostics JSON.");
            return { summaryPath: "", fullPath: "" };
          }
          const bundle = diagnosticExportPayloadsForNote2(markdown, file.path);
          const { summaryPath, fullPath } = bundle.paths;
          if (!summaryPath || !fullPath) {
            new Notice("Could not determine Canto Span diagnostics JSON paths.");
            return { summaryPath: "", fullPath: "" };
          }
          for (const outputPath of [summaryPath, fullPath]) {
            const existing = this.app.vault.getAbstractFileByPath(outputPath);
            if (existing && existing.children) {
              new Notice(`Cannot write Canto Span diagnostics JSON: ${outputPath} is a folder.`);
              return { summaryPath: "", fullPath: "" };
            }
          }
          const writeJson = async (outputPath, payload) => {
            const json = JSON.stringify(payload, null, 2);
            const existing = this.app.vault.getAbstractFileByPath(outputPath);
            if (existing) await this.app.vault.modify(existing, json);
            else await this.app.vault.create(outputPath, json);
          };
          await writeJson(fullPath, bundle.full);
          await writeJson(summaryPath, bundle.summary);
          new Notice(`Saved Canto Span acceptance summary and full diagnostics JSON beside the note.`);
          return { summaryPath, fullPath };
        }
        async formatActiveNoteWithDiagnostics() {
          const { file, markdown } = await this.readActiveNoteMarkdown();
          return formatNoteMarkdownWithDiagnostics2(markdown, file && file.path ? file.path : "");
        }
        renderBlock(source, element) {
          element.empty();
          const options = parseBlockOptions2(source);
          const expandDiagnostics = Boolean(this.settings && this.settings.expandDiagnostics);
          const block = createEl("div", BLOCK_CLASS);
          block.setAttribute("data-canto-runtime-version", CANTO_SPAN_RUNTIME_VERSION2);
          const titleRow = createEl("div", "canto-title-row");
          const title = createEl("div", "canto-title");
          title.appendChild(createEl("span", "canto-title-normal", "Canto Span"));
          title.appendChild(createEl("span", "canto-title-diagnostic", `Canto Span — Diagnostic v${CANTO_SPAN_RUNTIME_VERSION2}`));
          titleRow.appendChild(title);
          const analyzedLines = options.entries.map((entry) => ({
            line: entry.line,
            source_line_index: entry.source_line_index,
            context_directive: entry.context_directive || null,
            analysis: analyzeLine2(entry.line, entry.context)
          }));
          const diagnosticLines = analyzedLines.filter((entry) => normalizeSurface2(entry.line));
          if (diagnosticLines.length) {
            renderCopyButton(
              titleRow,
              () => this.formatActiveNoteWithDiagnostics(),
              "Copy note + diagnostics",
              "Copied the active note with Canto Span diagnostics at the top."
            );
            renderCopyButton(
              titleRow,
              () => this.formatActiveNoteDiagnostics(),
              "Copy diagnostics only",
              "Copied all Canto Span diagnostics in this note."
            );
            renderActionButton(
              titleRow,
              () => this.saveActiveNoteDiagnosticsJson(),
              "Save diagnostics JSON"
            );
            renderCopyButton(
              titleRow,
              () => diagnosticLines.map((entry) => formatDiagnosticMarkdown2(entry.analysis)).join("\n\n---\n\n"),
              diagnosticLines.length === 1 ? "Copy block diagnostic" : "Copy block diagnostics",
              diagnosticLines.length === 1 ? "Copied this Canto Span block diagnostic." : "Copied this Canto Span block's diagnostics."
            );
          }
          const legend = createEl("div", "canto-context-review-boundaries");
          legend.appendChild(createEl("span", "canto-legend-normal", "Parent construction spans wrap child learner-role tokens. Hover/tap tokens for Jyutping."));
          legend.appendChild(createEl("span", "canto-legend-diagnostic", "Diagnostic mode is enabled in plugin settings: tokenization, Jyutping audit, generated slots, construction match source, and review traces are shown. Use Copy note + diagnostics to copy the whole active note with generated diagnostics at the top, Copy diagnostics only for the generated batch, Save diagnostics JSON to write both a compact acceptance summary and a companion full-diagnostics file beside the active note, or Copy block diagnostic for one block."));
          const rendered = createEl("div", "canto-rendered");
          for (const { line, analysis, context_directive: contextDirective } of analyzedLines) {
            if (contextDirective) {
              const contextMeta = createEl("div", "canto-context-directive-diagnostic canto-diagnostic-ui-only");
              contextMeta.setAttribute("data-canto-context-turn-id", contextDirective.id);
              contextMeta.setAttribute("data-canto-context-directive-line", String(contextDirective.directive_line_index));
              contextMeta.setAttribute("data-canto-context-target-line", String(contextDirective.target_source_line_index));
              contextMeta.textContent = `Explicit context → ${contextDirective.source}`;
              rendered.appendChild(contextMeta);
            }
            for (const warning of analysis.warnings) {
              const warningEl = createEl("div", "canto-context-review-warning", warning);
              rendered.appendChild(warningEl);
            }
            renderParserShadowRepairs(analysis, rendered);
            const lineNormal = createEl("div", "canto-line canto-line-normal");
            for (const node of analysis.nodes) renderNode(node, lineNormal, { showDiagnostics: false });
            rendered.appendChild(lineNormal);
            const lineDiagnostic = createEl("div", "canto-line canto-line-diagnostic");
            for (const node of analysis.nodes) renderNode(node, lineDiagnostic, { showDiagnostics: true });
            rendered.appendChild(lineDiagnostic);
            if (normalizeSurface2(line)) renderDiagnosticDetails(analysis, rendered, expandDiagnostics);
          }
          block.appendChild(titleRow);
          block.appendChild(legend);
          block.appendChild(rendered);
          element.appendChild(block);
          this.scheduleRenderedDiagnosticDetailsSync();
        }
      }
      ;
      class CantoSpanSettingTab extends PluginSettingTab {
        constructor(app, plugin) {
          super(app, plugin);
          this.plugin = plugin;
        }
        display() {
          const { containerEl } = this;
          containerEl.empty();
          containerEl.createEl("h2", { text: "Canto Span" });
          new Setting(containerEl).setName("Show parse diagnostics").setDesc("Show tokenization, Jyutping audit, generated slot candidates, final construction tree, and trace source below each rendered line.").addToggle((toggle) => toggle.setValue(Boolean(this.plugin.settings.showDiagnostics)).onChange(async (value) => {
            this.plugin.settings.showDiagnostics = value;
            await this.plugin.saveSettings();
            this.display();
          }));
          new Setting(containerEl).setName("Expand diagnostics by default").setDesc("Open diagnostic details automatically when parse diagnostics are shown. This can be set before enabling parse diagnostics.").addToggle((toggle) => toggle.setValue(Boolean(this.plugin.settings.expandDiagnostics)).onChange(async (value) => {
            this.plugin.settings.expandDiagnostics = value;
            await this.plugin.saveSettings();
            this.display();
          }));
          new Setting(containerEl).setName("Show color accessibility markers").setDesc("Adds non-color learner-role markers for protanopia, deuteranopia, tritanopia, and grayscale support. When off, learner-role styling uses color only.").addToggle((toggle) => toggle.setValue(Boolean(this.plugin.settings.showColorAccessibilityMarkers)).onChange(async (value) => {
            this.plugin.settings.showColorAccessibilityMarkers = value;
            await this.plugin.saveSettings();
            this.display();
          }));
          containerEl.createEl("h3", { text: "Role colors" });
          const colorDesc = containerEl.createEl("p", { cls: "setting-item-description" });
          colorDesc.textContent = "Choose learner-role colors. Changes apply to already-rendered canto-span blocks immediately. Reset restores the neon default for that role.";
          this.plugin.settings.roleColors = getMergedRoleColors(this.plugin.settings);
          for (const role of ROLE_COLOR_SETTINGS) {
            new Setting(containerEl).setName(role.label).setDesc(`CSS variable${role.cssVars.length > 1 ? "s" : ""}: ${role.cssVars.map((cssVar) => `--canto-color-${cssVar}`).join(", ")}`).addColorPicker((picker) => picker.setValue(this.plugin.settings.roleColors[role.key] || role.defaultColor).onChange(async (value) => {
              const normalized = normalizeHexColor(value) || role.defaultColor;
              this.plugin.settings.roleColors[role.key] = normalized;
              await this.plugin.saveSettings();
            })).addButton((button) => button.setButtonText("Reset").setTooltip(`Reset ${role.label} to ${role.defaultColor}`).onClick(async () => {
              this.plugin.settings.roleColors[role.key] = role.defaultColor;
              await this.plugin.saveSettings();
              this.display();
            }));
          }
          new Setting(containerEl).setName("Reset all role colors").setDesc("Restore every learner-role color to the v0.5.4 neon defaults.").addButton((button) => button.setButtonText("Reset all").setWarning().onClick(async () => {
            this.plugin.settings.roleColors = { ...DEFAULT_ROLE_COLORS };
            await this.plugin.saveSettings();
            this.display();
          }));
          const note = containerEl.createEl("p", { cls: "setting-item-description" });
          note.textContent = "Review signal: prefer generative_template and slot-based traces. Treat legacy_surface_rule and surface_specific_phrase_rule as places to simplify later.";
        }
      }
      return CantoSpanPlugin;
    };
  }
});

// src/runtime-resources/constructions/runtime-label-registry.js
var require_runtime_label_registry = __commonJS({
  "src/runtime-resources/constructions/runtime-label-registry.js"(exports2, module2) {
    "use strict";
    var runtimeConstructionRegistryVersion = "0.5.198";
    var constructionLabelRegistry2 = [
      "ComplementEllipsisFragment",
      "DirectedMannerMotionVP",
      "ExistentialPresentationalClause",
      "GoalAttainmentMotionVP",
      "ImpersonalEnvironmentalClause",
      "LocativeFrameClause",
      "NominalPredicateClause",
      "PotentialResultVP",
      "RelativeClauseNP",
      "ResultComplementVP",
      "ANotAQuestion",
      "AcceptabilityANotA",
      "AssociativeNP",
      "ActionStativeVP",
      "ApproximateQuantity",
      "PostThemeParticipantRelation",
      "VocativeAddressTerm",
      "ChangeIntoPredicate",
      "ClassifierObjectNP",
      "ClauseRelationGraph",
      "ClauseRelationEdge",
      "ClauseRelationMemberSpan",
      "ClauseSpan",
      "ConditionalClause",
      "CoverbFrame",
      "CognitionContentFrame",
      "CognitionStatementClause",
      "CompletionQuestion",
      "CompletionVP",
      "CompoundDirectionalMotionVP",
      "CoordinatedNP",
      "CopularIdentificationFrame",
      "CopularANotAQuestion",
      "CopularRelationFrame",
      "DiscourseParticleFrame",
      "FocusParticleFrame",
      "DegreeMannerAdverbial",
      "DegreeMannerModifiedVP",
      "DegreeModifiedLexicalStative",
      "DegreeStativePredicate",
      "DelimitedVP",
      "DemonstrativeClassifierNP",
      "OvertHeadDemonstrativeClassifierNP",
      "HeadlessDemonstrativeClassifierNP",
      "FragmentQuestion",
      "DesiderativeVP",
      "DirectionalMotionVP",
      "DurativeVP",
      "PerfectiveDirectionalVP",
      "ProgressiveDirectionalVP",
      "PotentialDirectionalVP",
      "NegativePotentialDirectionalVP",
      "RestorativeComplementVP",
      "RepetitiveComplementVP",
      "DitransitiveSpeechVP",
      "ExistentialClause",
      "ExistentialQuestion",
      "ExistentialWhQuestion",
      "ExperientialClause",
      "ExperientialMotionGoalVP",
      "ExperientialQuestion",
      "ExperientialVP",
      "ExperientialYesNoQuestion",
      "FormulaDiscourseUnit",
      "FragmentAnswer",
      "NominalHeadSpan",
      "IntentionFrame",
      "IntransitiveVP",
      "JauDakMouDakAvailabilityPredicate",
      "LocativeExistentialClause",
      "LocativePostureVP",
      "MannerAdverbialVP",
      "LocativePlacePhrase",
      "LocativeWhQuestion",
      "MalformedCandidate",
      "MeasureExpression",
      "ModalVP",
      "ModifiedNP",
      "MotionGoalVP",
      "OrdinalClassifierNP",
      "ModifierNP",
      "MotionPurposeChain",
      "NamingClause",
      "NeedsContext",
      "NegatedDirectionalMotionVP",
      "NegatedExistentialClause",
      "NegatedExistentialFragment",
      "NegatedVP",
      "NegatedStativePredicate",
      "NegativeCognitionFragment",
      "NegativeExperiential",
      "NegativePotentialComplement",
      "OpinionQuestion",
      "OpinionStanceFrame",
      "PathPhrase",
      "PerfectiveVP",
      "PostverbalZoPerfectiveVP",
      "ModalANotAQuestion",
      "AcceptabilityClause",
      "DiMarkedNP",
      "PolarQuestionFrame",
      "PoliteImperativeClause",
      "PossessiveClassifierNP",
      "PreferenceVP",
      "ScalarEvaluation",
      "ScalarValueQuestion",
      "PriorityMarkerClause",
      "ProductiveVO",
      "ProgressiveVP",
      "ProgressiveWhObjectQuestion",
      "ProhibitiveImperative",
      "QuantifiedClassifierNP",
      "QuantifiedTimeNP",
      "QuantityNP",
      "QuantifiedPersonNP",
      "ReduplicatedVP",
      "VerbComplementVP",
      "ReportedSpeech",
      "ResultComplement",
      "SerialVerbPurposeChain",
      "SourceMotionClause",
      "StativeNominalComplement",
      "StativePredicate",
      "SuggestionQuestion",
      "TemporalClause",
      "TimeNP",
      "Topic",
      "TopicComment",
      "LexicalGiveRelation",
      "PassivePermissiveRelation",
      "TransitiveVP",
      "IntendedFunctionRelation",
      "ResourceUseLaiFunctionRelation",
      "WhClassifierQuestion"
    ];
    var clauseSpanCompatibilityInputs2 = [
      "SubjectPredicateClause",
      "SubjectModalPredicateClause",
      "LocativeModalPredicateClause",
      "TopicModalPredicateClause",
      "CoordinatedSubjectModalPredicateClause"
    ];
    var clauseRelationSubtypeRegistry2 = [
      "conditional",
      "causal",
      "concessive",
      "committed_preference",
      "ordered_preference",
      "premise_response",
      "sequential",
      "temporal_subordinate",
      "asyndetic_sequence",
      "relative_nominal_modifier"
    ];
    var retiredConstructionLabelRegistry2 = [
      ["ClauseLinkingSequence", "Retired public/internal graph-container name. Use ClauseRelationGraph internally; ClauseLinkingSequence survives only as diagnostic compatibility metadata and cannot assign a relation subtype or semantic relation."],
      ["ClauseRelation", "Retired public/internal edge name. Use ClauseRelationEdge internally; ClauseRelation survives only as diagnostic compatibility metadata and cannot license a subtype without inherited mapped provenance."],
      ["ClauseRelationMember", "Retired public/internal member-container name. Use ClauseRelationMemberSpan internally; ClauseRelationMember survives only as diagnostic compatibility metadata and cannot create a relation, missing member, or context resolution."],
      ["SubjectPredicateClause", "Retired active internal wrapper. Use ClauseSpan internally; SubjectPredicateClause survives only as public diagnostic compatibility metadata. Typed predicate children retain all language-sensitive content."],
      ["SubjectModalPredicateClause", "Retired combined modal wrapper. Use ClauseSpan internally with a preserved ModalVP child; this alias cannot license modal meaning or subjecthood."],
      ["LocativeModalPredicateClause", "Retired combined modal wrapper. Use ClauseSpan internally with preserved locative and ModalVP children; this alias cannot license modal or locative meaning."],
      ["TopicModalPredicateClause", "Retired combined modal wrapper. Use ClauseSpan internally with preserved topic and ModalVP children; this alias cannot license topic or modal meaning."],
      ["CoordinatedSubjectModalPredicateClause", "Retired combined wrapper. Use ClauseSpan internally with preserved CoordinatedNP and ModalVP children; this alias cannot license coordination, subjecthood, or modal meaning."],
      ["HeadNP", "Retired public/internal name. Use NominalHeadSpan internally; HeadNP survives only as diagnostic migration metadata and cannot license nominality, ellipsis, argument roles, or fragment status."],
      ["ModifierPhrase", "Retired transparent wrapper. Preserve its overt modifier/linker material directly inside the typed nominal analysis; do not emit ModifierPhrase as a public construction."],
      ["LibraryBorrowSourceClause", "Retired as corpus/place-specific; use a broad approved clause label only if a reusable grammar pattern is approved."],
      ["NamingSelfIntroductionFrame", "Retired as discourse/speaker-specific; use NamingClause."],
      ["PolitePathImperative", "Retired as path-lexeme-specific; use PoliteImperativeClause."],
      ["InteriorExistentialFrame", "Retired as object/interior-specific; use LocativeExistentialClause."],
      ["WeatherSeemingResultFrame", "Retired as domain/weather-specific. Its former SeemingPerfectiveResultClause target is also retired because it fused location, stance, time, perfective aspect, and a weather object into one exact-string wrapper."],
      ["WeatherResultPredicate", "Retired as domain/weather-specific. Its former PerfectiveObjectResultPredicate target is also retired because ordinary perfective transitive composition preserves the visible predicate, aspect, and object."],
      ["FocusedActionSourceClause", "Retired; CP018 also removed the exact ActionSourceFocusClause bridge. Await a dedicated broad Cantonese cleft/focus audit."],
      ["CandidateNamedAddressTerm", "Retired because Candidate encoded review status rather than grammar category; use VocativeAddressTerm for Cantonese address/vocative terms."],
      ["FoodNP", "Retired as semantic-domain-specific rather than syntactic; use ModifiedNP or NominalHeadSpan according to noun-phrase structure."],
      ["PersonNP", "Retired as semantic-domain-specific rather than syntactic; use ModifiedNP or NominalHeadSpan according to noun-phrase structure."],
      ["ProgressiveCookingPurposeClause", "Retired as object/activity-domain-specific. Its former ProgressivePurposeClause target is also retired because unlinked predicate adjacency did not establish a purpose relation."],
      ["DropBenefactivePurposeFrame", "Retired; CP018 removed the exact modal-goal-benefactive-purpose bridge. Preserve components or review status pending separate relation audits."],
      ["DropGoalPredicate", "Retired; CP018 also removed GoalDirectedActionPredicate. Await lexical/path/caused-motion research."],
      ["UrgencyMarker", "Retired as too surface/pragmatic-specific; use DegreeMannerAdverbial for the productive degree/manner + 啲 pattern."],
      ["AgreementResponseFormula", "Retired as formula-subtype-specific; use FormulaDiscourseUnit with trace_detail.retired_label_alias='AgreementResponseFormula' for transparent 係呀 agreement responses."],
      ["ClauseSequence", "Retired as the older broad wrapper name; use ClauseRelationGraph internally for comma/connector-governed clause linking with explicit wrapper coverage; diagnostics may retain ClauseLinkingSequence as a compatibility alias."],
      ["DefinitionTopic", "Retired as frame-role-specific; use Topic with definition_topic/function metadata inside DefinitionExplanatoryFrame."],
      ["LeaveTakingFormula", "Retired as formula-subtype-specific; use FormulaDiscourseUnit with trace_detail.retired_label_alias='LeaveTakingFormula'."],
      ["NegativeLexicalizedStative", "Retired as a polarity-wording duplicate. Its former target NegatedLexicalizedStative is also retired because lexical 難X, compositional 唔 + lexicalized property predicates, and prohibitive 唔好 are distinct."],
      ["DesiderativeANotAQuestion", "Retired as modal-subtype-specific; use ModalANotAQuestion with trace_detail.modal_subtype='desiderative'."],
      ["PermissionANotAQuestion", "Retired as modal-subtype-specific; use ModalANotAQuestion with trace_detail.modal_subtype='permission'."],
      ["PermissionAcceptabilityClause", "Retired as permission-subtype-specific; use AcceptabilityClause with trace_detail.acceptability_subtype='permission' for 都得-style acceptability clauses."],
      ["InterestQuestion", "Retired as semantic-domain-specific; use ExistentialQuestion with trace_detail.existential_subtype='abstract_object' and abstract_object_domain='interest' for 有冇興趣-style questions."],
      ["DeicticClassifierTopic", "Retired as a fused form+role label; use Topic with trace_detail.retired_label_alias='DeicticClassifierTopic' and deictic/classifier metadata, while preserving DemonstrativeClassifierNP for full demonstrative-classifier-head noun phrases."],
      ["BenefactivePurposeVP", "Retired as benefactive-subtype-specific; use SerialVerbPurposeChain with recipient/beneficiary slots for 煮飯畀我食-style chains."],
      ["BenefactiveRecipientFrame", "Retired as benefactive-subtype-specific. CP021B also retires RecipientFrame; no alias redirects this old semantic claim to either new relation."],
      ["NegativePriceEvaluation", "Retired as semantic-domain-specific. Source-linked negative 算 evaluation uses ScalarEvaluation; price remains contextual metadata rather than an alias boundary."],
      ["PriceEvaluation", "Retired as semantic-domain-specific. Ordinary price-noun predication remains nominal/topic-comment structure and does not redirect to ScalarEvaluation."],
      ["PriceQuestion", "Retired as semantic-domain-specific; use ScalarValueQuestion with scalar_domain='price' metadata for 幾錢-type value questions."],
      ["PriceRangeFragment", "Retired as semantic-domain-specific. 價位 remains ordinary nominal/scalar material unless independently supported fragment evidence is established."],
      ["SubjectNegatedPredicateClause", "Retired as polarity-specific; use SubjectPredicateClause with trace_detail.retired_label_alias='SubjectNegatedPredicateClause' and negated-predicate slots."],
      ["SubjectStativePredicateClause", "Retired as predicate-subtype-specific; use SubjectPredicateClause with trace_detail.predicate_subtype='stative'."],
      ["SubjectTimeLocationPredicateClause", "Retired as modifier-stack-specific; use SubjectPredicateClause with assigned time/location slots."],
      ["SubjectTimePredicateClause", "Retired as time-modifier-specific; use SubjectPredicateClause with assigned time slot."],
      ["TopicCommentEvaluation", "Retired as evaluation-subtype-specific; use TopicComment with predicate subtype/evaluation metadata."],
      ["UseForPurposeFrame", "Retired by CP019: three-level hierarchy consolidated into IntendedFunctionRelation."],
      ["UseForPurposeVP", "Retired by CP019: internal VP wrapper was not an independent Cantonese construction."],
      ["UseForPurposePredicate", "Retired by CP019: transparent predicate material remains inside IntendedFunctionRelation."],
      ["AffectednessFrame", "Retired by CP020: generic affectedness merged canonical passive, indirect passive, and permissive readings; use PassivePermissiveRelation with explicit reading candidates."],
      ["RecipientFrame", "Retired by CP021B: the label asserted one recipient/beneficiary relation across predicate-dependent 畀/俾 surfaces; reviewed profiles now use PostThemeParticipantRelation without a fixed semantic role or marker category."],
      ["TransferDitransitiveVP", "Retired by CP021B: the label overcommitted to a ditransitive VP and forced roles in nonbaseline orders; reviewed lexical GIVE surfaces now use LexicalGiveRelation with bounded role policy."],
      ["UseForPurposeTopic", "Retired as frame-role-specific; use Topic with intended-function metadata inside IntendedFunctionRelation."],
      ["TemporalAdverbialClause", "Retired at v0.5.191: no constructor, fixture, standardized case, or parser output used this label. Preserve explicit temporal-subordination research separately; current runtime uses TemporalClause or typed ClauseRelationEdge structures."],
      ["Comment", "Retired at v0.5.197: the standalone child wrapper never survived complete parser output and duplicated the comment/comment_predicate role metadata already carried by TopicComment."],
      ["PerfectiveResultPredicate", "Retired at v0.5.197: the lexical-item-specific 解決咗 wrapper was shadowed by ordinary PerfectiveVP composition and added no independently supported boundary."],
      ["ComparativeStative", "Retired at v0.5.202: the residual stative + 啲 fallback mislabeled degree adjustment as a generic comparative. Source-supported property + 啲 forms use DegreeMannerAdverbial; explicit surpass comparatives require separate structure."],
      ["ScalarRangeFragment", "Retired at v0.5.209: the 價位-triggered fallback added unsupported fragment status to ordinary nominal/scalar material and had no source-linked positive or accepted fixture."],
      ["NegatedLexicalizedStative", "Retired at v0.5.212: the label conflated lexical negative meaning in opaque 難X units with compositional 唔 negation and the distinct prohibitive 唔好 profile."],
      ["ConditionResult", "Retired at v0.5.213-r1: the action + 就 + stative wrapper lacked an invariant sourced construction; typed conditional ClauseRelationEdge structure now preserves independently parsed antecedent and consequent members."],
      ["CompletionThenClause", "Retired at v0.5.213-r2: completion + 就 sequences are represented as typed sequential ClauseRelationEdge structure with independently parsed earlier and later members."],
      ["SchedulingQuestion", "Retired at v0.5.213-r5: the source-attested 想 + 約 + object + 幾時 question already composes as ordinary modal, action, object, and wh-time structure; the dedicated wrapper only covered an unsourced 覺得…好 cross-product."],
      ["CausativeResultFrame", "Retired at v0.5.214-r1: the outer subject-plus-predicate wrapper was transparent and inherited all of its analysis from an unsourced exact 整冧咗 predicate fallback."],
      ["CausativeResultPredicate", "Retired at v0.5.214-r1: the exact 整 + 冧 + 咗 bundle did not establish a reusable causative-result predicate and ordinary predicate/aspect/object structure remains available."],
      ["DisposalChangeIntoResultFrame", "Retired at v0.5.214-r1: the exact 會將…變成 wrapper fused independently visible modal, disposal, object, and lexical change-predicate material."],
      ["ModalChangeIntoResultFrame", "Retired at v0.5.214-r1: modal change clauses compose from an ordinary clause/modal frame plus the retained source-linked ChangeIntoPredicate."],
      ["PerfectiveObjectResultPredicate", "Retired at v0.5.214-r1: the weather-specific 停咗雨 bundle duplicated ordinary perfective transitive composition."],
      ["ResultStateClause", "Retired at v0.5.214-r1: the exact optional 好多/都 + 熟咗 wrapper conflated scope, lexical state, perfective aspect, and sentence-final particles."],
      ["SeemingPerfectiveResultClause", "Retired at v0.5.214-r1: the six-part location + 好似 + time + 停咗雨 wrapper was an unsourced cross-category template."],
      ["TransformationResultFrame", "Retired at v0.5.214-r1: the outer topic wrapper added no independent structure beyond the unsupported 變咗做 predicate bundle."],
      ["TransformationResultPredicate", "Retired at v0.5.214-r1: the exact 變咗做 bundle lacked direct source support and must not be conflated with sourced lexical 變成."],
      ["CognitionDelimitedVP", "Retired at v0.5.214-r2: cognition + 吓 is represented by ordinary DelimitedVP composition; a cognition-specific parent added no overt structure."],
      ["CognitionDelimitedObjectVP", "Retired at v0.5.214-r2: the wrapper treated arbitrary post-吓 material as an object despite source evidence for lexeme-sensitive nominal and clausal complements."],
      ["MotionDelimitedVP", "Retired at v0.5.214-r2: the movement + action + 吓 cross-product conflated a purpose relation with delimitative aspect on the second predicate."],
      ["ProgressivePlaceQuestion", "Retired at v0.5.214-r3: progressive aspect and an in-situ locative wh phrase compose independently; no special combined construction is established."],
      ["ProgressivePurposeClause", "Retired at v0.5.214-r3: the exact 喺度 + V緊O + unlinked VO fallback inferred purpose from adjacency without an overt or independently licensed relation."],
      ["ProgressiveTransitivePredicate", "Retired at v0.5.214-r3: the internal V緊O child duplicated the active ProgressiveVP analysis."],
      ["PurposePredicate", "Retired at v0.5.214-r3: ordinary V+O form is not intrinsically purposive; purpose belongs to an evidenced inter-event relation."],
      ["EvaluationWithDouSyun", "Retired at v0.5.214-r4: the exact 都算 + price-material wrapper was unsourced; sourced lexical 算 evaluation remains ScalarEvaluation."],
      ["IdentityWhQuestion", "Retired at v0.5.214-r4: the exact 邊個嚟 fallback lacked direct support; sourced copular identity and explanatory 嚟 structures remain independently represented."],
      ["PostposedExistentialQuestion", "Retired at v0.5.214-r4: no inspected source established the exact 有 + NP + 冇 template; adjacent 有冇 questions remain ExistentialQuestion."],
      ["TimeToActionFrame", "Retired at v0.5.214-r4: component evidence for 係 and 時候 did not establish a dedicated 係時候 + action construction."],
      ["SpeechTransferClause", "Retired at v0.5.214-r5: the outer subject/particle wrapper added no structure beyond ordinary clause composition around the retained lexical communication-effect predicate."],
      ["PossessiveTransferClause", "Retired at v0.5.214-r5: the seven-part wrapper combined independently typed possession, classifier NP, modality, transfer, and recipient material without direct construction evidence."],
      ["VPComplementFrame", "Retired at v0.5.214-r5: the source-free generic matcher allowed a complement-taking predicate category to combine with arbitrary VP material; complement licensing must be lexeme-specific."],
      ["DefinitionComplement", "Retired at v0.5.214-r6: the non-licensing internal child duplicated ordinary wh/NP complement material and had no independent source."],
      ["DefinitionExplanatoryFrame", "Retired at v0.5.214-r6: the exact 係…嚟㗎 wrapper was a source-free parser heuristic; visible copular, wh/NP, linker, and particle material remains compositional."],
      ["IdentificationFragment", "Retired at v0.5.214-r6: the broad 就係 fallback inferred autonomous fragment status without a source-backed boundary."],
      ["LocativeFragment", "Retired at v0.5.214-r6: a bare locative phrase does not independently establish fragment or ellipsis structure; LocativePlacePhrase preserves the overt material."]
    ];
    var retiredConstructionLabelAliases2 = [
      ["AgreementResponseFormula", "FormulaDiscourseUnit"],
      ["ClauseSequence", "ClauseRelationGraph"],
      ["DefinitionTopic", "Topic"],
      ["LeaveTakingFormula", "FormulaDiscourseUnit"],
      ["DesiderativeANotAQuestion", "ModalANotAQuestion"],
      ["PermissionANotAQuestion", "ModalANotAQuestion"],
      ["PermissionAcceptabilityClause", "AcceptabilityClause"],
      ["InterestQuestion", "ExistentialQuestion"],
      ["DeicticClassifierTopic", "Topic"],
      ["BenefactivePurposeVP", "SerialVerbPurposeChain"],
      ["PriceQuestion", "ScalarValueQuestion"],
      ["SubjectNegatedPredicateClause", "SubjectPredicateClause"],
      ["SubjectStativePredicateClause", "SubjectPredicateClause"],
      ["SubjectTimeLocationPredicateClause", "SubjectPredicateClause"],
      ["SubjectTimePredicateClause", "SubjectPredicateClause"],
      ["TopicCommentEvaluation", "TopicComment"],
      ["UseForPurposeTopic", "Topic"],
      ["PerfectiveResultPredicate", "PerfectiveVP"]
    ];
    var internalConstructionCompatibilityAliases2 = {
      NominalHeadSpan: "HeadNP",
      ClauseRelationGraph: "ClauseLinkingSequence",
      ClauseRelationEdge: "ClauseRelation",
      ClauseRelationMemberSpan: "ClauseRelationMember",
      ClauseSpan: "SubjectPredicateClause",
      PostverbalZoPerfectiveVP: "PerfectiveVP",
      ResourceUseLaiFunctionRelation: "IntendedFunctionRelation",
      OvertHeadDemonstrativeClassifierNP: "DemonstrativeClassifierNP"
    };
    var internalOnlyConstructionScopes2 = {
      NominalHeadSpan: "neutral_nominal_head_span",
      MeasureExpression: "overt_measure_child_span",
      ClauseRelationGraph: "neutral_clause_relation_graph",
      ClauseRelationEdge: "typed_clause_relation_edge",
      ClauseRelationMemberSpan: "overt_clause_relation_member_span",
      ClauseSpan: "neutral_complete_clause_span",
      PostverbalZoPerfectiveVP: "source_linked_postverbal_zo_overt_object_perfective",
      ResourceUseLaiFunctionRelation: "source_linked_direct_resource_use_lai_function_relation"
    };
    var constructionLabelPolicy2 = {
      approval_required: "only_for_project_specific_unproven_labels",
      label_scope: "broad_reusable_grammar_concept",
      allowed_without_prior_approval_when_justified_by: ["real_world_cantonese_grammar", "broader_chinese_grammar_analysis", "recognized_linguistics_concept", "confirmed_recurring_grammar_pattern"],
      approval_required_when: "useful_project_specific_label_lacks_external_or_recurring_grammar_evidence",
      disallow_specificity: ["person", "place", "object", "story", "corpus_line", "single_lexical_item", "review_status"],
      required_justification: "grammar_reference_or_linguistic_concept_or_recurring_pattern_evidence"
    };
    module2.exports = {
      runtimeConstructionRegistryVersion,
      constructionLabelRegistry: constructionLabelRegistry2,
      clauseSpanCompatibilityInputs: clauseSpanCompatibilityInputs2,
      clauseRelationSubtypeRegistry: clauseRelationSubtypeRegistry2,
      retiredConstructionLabelRegistry: retiredConstructionLabelRegistry2,
      retiredConstructionLabelAliases: retiredConstructionLabelAliases2,
      internalConstructionCompatibilityAliases: internalConstructionCompatibilityAliases2,
      internalOnlyConstructionScopes: internalOnlyConstructionScopes2,
      constructionLabelPolicy: constructionLabelPolicy2
    };
  }
});

// src/runtime-resources/pronunciation/jyutping-review-expectations.js
var require_jyutping_review_expectations = __commonJS({
  "src/runtime-resources/pronunciation/jyutping-review-expectations.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Optional audit guardrail table. This lane is diagnostic-only: parser coverage
      // stays separate from pronunciation quality warnings. Use arrays when common
      // tone-change or reading variants are acceptable.
      ["我", ["ngo5"]],
      ["你", ["nei5"]],
      ["我哋", ["ngo5 dei6"]],
      ["你哋", ["nei5 dei6"]],
      ["大家", ["daai6 gaa1"]],
      ["嘅話", ["ge3 waa6"]],
      // Address-form Jyutping seeds. Diagnostic-only expectations keep parser coverage
      // separate from pronunciation quality while making fallback address/name parts auditable.
      ["阿", ["aa3"]],
      ["霞", ["haa4"]],
      ["姐", ["ze2"]],
      ["明", ["ming4"]],
      ["哥", ["go1"]],
      ["陳", ["can4"]],
      ["生", ["saang1"]],
      ["先生", ["sin1 saang1"]],
      ["家姐", ["gaa1 ze2"]],
      ["小姐", ["siu2 ze2"]],
      ["姐姐", ["ze4 ze2", "ze2 ze2"]],
      ["姐夫", ["ze2 fu1"]],
      ["姐妹", ["ze2 mui6"]],
      ["大姐", ["daai6 ze2"]],
      ["阿姐", ["aa3 ze2"]],
      ["學生", ["hok6 saang1"]],
      ["快", ["faai3"]],
      ["啲", ["di1"]],
      ["落", ["lok6"]],
      ["雨", ["jyu5"]],
      ["風", ["fung1"]],
      ["天光", ["tin1 gwong1"]],
      ["出面", ["ceot1 min6"]],
      ["上", ["soeng5", "soeng6"]],
      ["返", ["faan1"]],
      ["嚟", ["lai4"]],
      ["摘", ["zaak6"]],
      ["芒果", ["mong1 gwo2", "mong4 gwo2"]],
      ["食", ["sik6"]],
      ["飯", ["faan6"]],
      ["高", ["gou1"]],
      ["低", ["dai1"]],
      ["耐", ["noi6"]],
      ["而家", ["ji4 gaa1"]],
      ["想", ["soeng2"]],
      ["先", ["sin1"]],
      ["等陣", ["dang2 zan6"]],
      ["熟", ["suk6"]],
      ["咗", ["zo2"]],
      ["都", ["dou1"]],
      ["得", ["dak1"]],
      ["喇", ["laa3"]],
      ["啦", ["laa1"]],
      ["嘞", ["laak3", "lak3", "laa3"]],
      ["噉", ["gam2"]],
      ["呀", ["aa3"]],
      ["吖", ["aa1"]],
      ["晒", ["saai3"]],
      ["煮", ["zyu2"]],
      ["甜", ["tim4"]],
      ["好甜", ["hou2 tim4"]],
      ["故事", ["gu3 si6"]],
      ["笑", ["siu3"]],
      ["好笑", ["hou2 siu3"]],
      // Authorized Glossika Week 16 hobbies/free-time lexical audit.
      ["游", ["jau4"]],
      ["跑", ["paau2"]],
      ["步", ["bou6"]],
      ["影", ["jing2"]],
      ["相", ["soeng2"]],
      ["嘢食", ["je5 sik6"]],
      ["唱", ["coeng3"]],
      ["街", ["gaai1"]],
      ["沙灘", ["saa1 taan1"]],
      ["踢", ["tek3"]],
      ["波", ["bo1"]],
      ["彈", ["taan4"]],
      ["琴", ["kam4"]],
      ["釣", ["diu3"]],
      ["旅行", ["leoi5 hang4"]],
      ["瑜伽", ["jyu4 gaa1"]],
      ["露營", ["lou6 jing4"]],
      ["棋", ["kei2"]],
      ["公園", ["gung1 jyun2", "gung1 jyun4"]],
      ["畫", ["waak6", "waa2", "waa6"]],
      ["畫畫", ["waak6 waa2"]],
      ["解決", ["gaai2 kyut3"]]
    ];
  }
});

// src/runtime-resources/lexicon/productive-vo.js
var require_productive_vo = __commonJS({
  "src/runtime-resources/lexicon/productive-vo.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      ["食飯", { verb: "食", object: "飯", label: "VP", type: "ProductiveVO" }],
      ["煮飯", { verb: "煮", object: "飯", label: "VP", type: "ProductiveVO" }],
      ["摘芒果", { verb: "摘", object: "芒果", label: "VP", type: "ProductiveVO" }],
      ["買嘢", { verb: "買", object: "嘢", label: "VP", type: "ProductiveVO" }],
      ["食嘢", { verb: "食", object: "嘢", label: "VP", type: "ProductiveVO" }],
      ["飲水", { verb: "飲", object: "水", label: "VP", type: "ProductiveVO" }],
      ["寫字", { verb: "寫", object: "字", label: "VP", type: "ProductiveVO" }],
      ["寫名", { verb: "寫", object: "名", label: "VP", type: "ProductiveVO" }],
      ["睇書", { verb: "睇", object: "書", label: "VP", type: "ProductiveVO" }],
      ["聽歌", { verb: "聽", object: "歌", label: "VP", type: "ProductiveVO" }],
      // Authorized Glossika Week 16 activity phrases. PRODUCTIVE_VO preserves visible verb/object children.
      ["睇戲", { verb: "睇", object: "戲", label: "VP", type: "ProductiveVO" }],
      ["跑步", { verb: "跑", object: "步", label: "VP", type: "ProductiveVO" }],
      ["影相", { verb: "影", object: "相", label: "VP", type: "ProductiveVO" }],
      ["打機", { verb: "打", object: "機", label: "VP", type: "ProductiveVO" }],
      ["煮嘢食", { verb: "煮", object: "嘢食", label: "VP", type: "ProductiveVO" }],
      ["唱K", { verb: "唱", object: "K", label: "VP", type: "ProductiveVO" }],
      ["做運動", { verb: "做", object: "運動", label: "VP", type: "ProductiveVO" }],
      ["踢波", { verb: "踢", object: "波", label: "VP", type: "ProductiveVO" }],
      ["打波", { verb: "打", object: "波", label: "VP", type: "ProductiveVO" }],
      ["彈琴", { verb: "彈", object: "琴", label: "VP", type: "ProductiveVO" }],
      ["釣魚", { verb: "釣", object: "魚", label: "VP", type: "ProductiveVO" }],
      ["唱歌", { verb: "唱", object: "歌", label: "VP", type: "ProductiveVO" }],
      ["睇波", { verb: "睇", object: "波", label: "VP", type: "ProductiveVO" }],
      ["下棋", { verb: "下", object: "棋", label: "VP", type: "ProductiveVO" }],
      ["講嘢", { verb: "講", object: "嘢", label: "VP", type: "ProductiveVO" }],
      ["打電話", { verb: "打", object: "電話", label: "VP", type: "ProductiveVO" }],
      ["打籃球", { verb: "打", object: "籃球", label: "VP", type: "ProductiveVO" }],
      ["聽電話", { verb: "聽", object: "電話", label: "VP", type: "ProductiveVO" }],
      ["返學", { verb: "返", object: "學", label: "VP", type: "ProductiveVO" }],
      ["放學", { verb: "放", object: "學", label: "VP", type: "ProductiveVO" }],
      ["瞓覺", { verb: "瞓", object: "覺", label: "VP", type: "ProductiveVO" }],
      ["洗手", { verb: "洗", object: "手", label: "VP", type: "ProductiveVO" }],
      ["曬太陽", { verb: "曬", object: "太陽", label: "VP", type: "ProductiveVO" }],
      ["打麻雀", { verb: "打", object: "麻雀", label: "VP", type: "ProductiveVO" }],
      ["默書", { verb: "默", object: "書", label: "VP", type: "ProductiveVO" }],
      ["炒股票", { verb: "炒", object: "股票", label: "VP", type: "ProductiveVO" }],
      ["發脾氣", { verb: "發", object: "脾氣", label: "VP", type: "ProductiveVO" }],
      ["食意粉", { verb: "食", object: "意粉", label: "VP", type: "ProductiveVO" }],
      ["Book枱", { verb: "Book", object: "枱", label: "VP", type: "ProductiveVO" }]
    ];
  }
});

// src/runtime-resources/lexicon/verb-object-compounds.js
var require_verb_object_compounds = __commonJS({
  "src/runtime-resources/lexicon/verb-object-compounds.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      ["飲茶", { verb: "飲", object: "茶", label: "VP", type: "ProductiveVO", source_specification: "docs/research/ISSUE-753-AB35-LEXICAL-VO-RUNTIME-CONTRACT-R1.md" }],
      ["游水", { verb: "游", object: "水", label: "VP", type: "ProductiveVO", source_specification: "docs/research/ISSUE-753-AB35-LEXICAL-VO-RUNTIME-CONTRACT-R1.md" }],
      ["沖涼", { verb: "沖", object: "涼", label: "VP", type: "ProductiveVO", source_specification: "docs/research/ISSUE-753-AB35-LEXICAL-VO-RUNTIME-CONTRACT-R1.md" }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r251-500-reviewed.js
var require_cifu_r251_500_reviewed = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r251-500-reviewed.js"(exports2, module2) {
    "use strict";
    var SOURCE = "docs/research/ISSUE-858-CIFU-R251-500-LEXICAL-ADJUDICATION-R1.md";
    var BLOCKED_ATOMIC_SURFACES = Object.freeze(/* @__PURE__ */ new Set([
      "一條",
      "你講",
      "係噉",
      "出來",
      "右行",
      "大樹",
      "尚健",
      "幾好",
      "我講",
      "扮汗",
      "提碑",
      "杏堂",
      "條線",
      "條路線",
      "畫到",
      "行到",
      "講呢",
      "貧大宅"
    ]));
    var CANDIDATE_ONLY_SURFACES = Object.freeze(/* @__PURE__ */ new Set([
      "今次",
      "仲有",
      "入去",
      "出去",
      "唔記得",
      "得到",
      "讀書"
    ]));
    var PROMOTIONS = Object.freeze({
      "啱啱": Object.freeze({ label: "when", pos: "adverb", jyutping: "ngaam1 ngaam1", syntax: "temporal_adverb", gloss: "just now" }),
      "唉": Object.freeze({ label: "particle", pos: "interjection", jyutping: "aai1", syntax: "interjection", gloss: "sigh / response / regret interjection" }),
      "哎": Object.freeze({ label: "particle", pos: "interjection", jyutping: "aai1", syntax: "interjection", gloss: "attention / surprise / disapproval interjection" }),
      "擰": Object.freeze({ label: "doing", pos: "verb", jyutping: "ning6", syntax: "verb", gloss: "twist / wring / turn with force" }),
      "丸": Object.freeze({ label: "what", pos: "noun", jyutping: "jyun2", syntax: "object_np nominal_root", gloss: "pellet / ball / pill" }),
      "隔離": Object.freeze({ label: "doing", pos: "verb", jyutping: "gaak3 lei4", syntax: "verb", gloss: "isolate / separate" }),
      "往": Object.freeze({ label: "func", pos: "function", syntax: "directional_relation motion_coverb", gloss: "towards / in the direction of" }),
      "淨係": Object.freeze({ label: "func", pos: "adverb", syntax: "focus_adverb restrictive_focus", gloss: "only / merely" }),
      "底": Object.freeze({ label: "where", pos: "noun", syntax: "spatial_localizer spatial_nominal", gloss: "bottom / base / underlying part" }),
      "為": Object.freeze({ label: "func", pos: "function", syntax: "coverb relation_function", gloss: "for / because of / as" }),
      "車站": Object.freeze({ label: "where", pos: "noun", syntax: "place_or_goal place_np", gloss: "station / stop" }),
      "差唔多": Object.freeze({ label: "like", pos: "adjective", syntax: "stative_predicate approximation_predicate", gloss: "similar / about the same" }),
      "魚排": Object.freeze({ label: "what", pos: "noun", jyutping: "jyu4 paai4", syntax: "object_np mariculture_raft_np", gloss: "mariculture raft / fish-farm raft" }),
      "唔同": Object.freeze({ label: "like", pos: "adjective", syntax: "stative_predicate difference_property", gloss: "different / not the same" }),
      "會見": Object.freeze({ label: "doing", pos: "verb", jyutping: "wui6 gin3", syntax: "verb formal_meeting_verb", gloss: "meet / interview formally" }),
      "吔": Object.freeze({ label: "particle", pos: "interjection", jyutping: "jaa3", syntax: "interjection_or_particle", gloss: "interjection / pragmatic particle" }),
      "等等": Object.freeze({ label: "func", pos: "function", syntax: "list_continuation_marker", gloss: "etcetera / and so on" }),
      "盛": Object.freeze({ label: "like", pos: "adjective", jyutping: "sing6", syntax: "stative_predicate", gloss: "flourishing / prosperous / grand / abundant" }),
      "燕": Object.freeze({ label: "what", pos: "noun", jyutping: "jin3", syntax: "object_np animal_np", gloss: "swallow (bird)" }),
      "同學": Object.freeze({ label: "who", pos: "noun", syntax: "person_np", gloss: "classmate / fellow student" }),
      "只": Object.freeze({ label: "func", pos: "adverb", jyutping: "zi2", syntax: "restrictive_focus_adverb", gloss: "only / merely" }),
      "可": Object.freeze({ label: "func", pos: "function", syntax: "modal_or_property_function", gloss: "can / may / permissible" }),
      "有時": Object.freeze({ label: "when", pos: "adverb", syntax: "frequency_adverb temporal_adjunct", gloss: "sometimes" }),
      "唧": Object.freeze({ label: "particle", pos: "particle", jyutping: "zek1", syntax: "discourse_particle", gloss: "particle use" }),
      "老": Object.freeze({ label: "like", pos: "adjective", syntax: "stative_or_prefix", gloss: "old / familiar-prefix family" }),
      "依": Object.freeze({ label: "doing", pos: "verb", syntax: "verb relational_verb", gloss: "depend on / according to" }),
      "咦": Object.freeze({ label: "particle", pos: "interjection", jyutping: "ji2", syntax: "interjection", gloss: "surprise interjection" }),
      "扮": Object.freeze({ label: "doing", pos: "verb", jyutping: "baan6", syntax: "verb", gloss: "pretend / dress up" }),
      "擁": Object.freeze({ label: "doing", pos: "verb", syntax: "verb", gloss: "embrace / support / surround" }),
      "本身": Object.freeze({ label: "func", pos: "pronoun", syntax: "reflexive_pronoun emphatic_modifier", gloss: "itself / in itself" }),
      "另": Object.freeze({ label: "how", pos: "adverb", syntax: "separative_modifier_adverb", gloss: "other / another / separately" }),
      "彭": Object.freeze({ label: "who", pos: "proper_noun", jyutping: "paang4", syntax: "surname_element", gloss: "surname Peng" }),
      "像": Object.freeze({ label: "doing", pos: "verb", jyutping: "zoeng6", syntax: "verb resemblance_predicate", gloss: "resemble / appear like" }),
      "仔": Object.freeze({ label: "who", pos: "noun", jyutping: "zai2", syntax: "person_or_young_animal_noun diminutive_suffix", gloss: "boy / child / young animal; diminutive suffix" }),
      "風車": Object.freeze({ label: "what", pos: "noun", syntax: "object_np artifact_np", gloss: "windmill / pinwheel" }),
      "越": Object.freeze({ label: "func", pos: "function", jyutping: "jyut6", syntax: "correlative_degree_marker", gloss: "the more…; degree/correlative function" }),
      "考": Object.freeze({ label: "doing", pos: "verb", syntax: "verb", gloss: "test / examine / take an exam" }),
      "引": Object.freeze({ label: "doing", pos: "verb", syntax: "verb", gloss: "draw / pull / lead / attract" }),
      "前": Object.freeze({ label: "where", pos: "noun", syntax: "spatial_or_temporal_localizer", gloss: "front / before / ago" }),
      "完全": Object.freeze({ label: "how", pos: "adverb", syntax: "degree_adverb manner_adverb", gloss: "completely / totally" }),
      "股": Object.freeze({ label: "what", pos: "noun", syntax: "object_np nominal_or_measure_root", gloss: "share / portion / thigh" }),
      "所": Object.freeze({ label: "func", pos: "function", syntax: "nominalizer_or_classifier_function", gloss: "nominalizing/passive function; institutional classifier" }),
      "所謂": Object.freeze({ label: "func", pos: "function", syntax: "so_called_modifier", gloss: "so-called / alleged" }),
      "首先": Object.freeze({ label: "when", pos: "adverb", syntax: "sequence_adverb discourse_adverb", gloss: "first of all" }),
      "全部": Object.freeze({ label: "func", pos: "quantifier", syntax: "universal_quantifier pronoun_like_quantifier", gloss: "all / the whole" }),
      "服務": Object.freeze({ label: "doing", pos: "verb", syntax: "verb service_verb", gloss: "serve / provide service" }),
      "理": Object.freeze({ label: "doing", pos: "verb", syntax: "verb management_attention_verb", gloss: "manage / handle / pay attention to" }),
      "種": Object.freeze({ label: "what", pos: "noun", syntax: "kind_seed_noun", gloss: "kind / species / seed" }),
      "堂": Object.freeze({ label: "what", pos: "noun", syntax: "room_or_hall_noun", gloss: "hall / room" }),
      "后": Object.freeze({ label: "who", pos: "noun", syntax: "title_noun", gloss: "empress / queen" }),
      "呃": Object.freeze({ label: "doing", pos: "verb", jyutping: "aak1", syntax: "verb deception_verb", gloss: "cheat / deceive" }),
      "就算": Object.freeze({ label: "func", pos: "function", syntax: "concessive_conjunction", gloss: "even if / granted that" }),
      "怨": Object.freeze({ label: "doing", pos: "verb", syntax: "verb psych_verb", gloss: "blame / complain / resent" }),
      "起": Object.freeze({ label: "doing", pos: "verb", syntax: "verb rise_start_verb", gloss: "rise / start / set out" }),
      "打橫": Object.freeze({ label: "how", pos: "adverb", syntax: "manner_adverb orientation_property", gloss: "horizontally / crosswise" }),
      "揀": Object.freeze({ label: "doing", pos: "verb", jyutping: "gaan2", syntax: "verb", gloss: "choose / pick" }),
      "項": Object.freeze({ label: "measure_word", pos: "classifier", jyutping: "hong6", syntax: "general_classifier item_classifier", gloss: "classifier for items / projects / clauses / sums" }),
      "嶺": Object.freeze({ label: "where", pos: "noun", jyutping: "leng5", syntax: "natural_place_np ridge_np", gloss: "ridge / mountain range" }),
      "之": Object.freeze({ label: "func", pos: "function", jyutping: "zi1", syntax: "structural_genitive_nominal_linker", gloss: "formal structural/genitive linker" }),
      "出現": Object.freeze({ label: "doing", pos: "verb", jyutping: "ceot1 jin6", syntax: "verb appearance_verb", gloss: "appear / emerge" }),
      "直情": Object.freeze({ label: "how", pos: "adverb", jyutping: "zik6 cing4", syntax: "discourse_adverb emphatic_adverb", gloss: "simply / outright / actually" }),
      "掂": Object.freeze({ label: "like", pos: "adjective", jyutping: "dim6", syntax: "stative_predicate evaluative_property", gloss: "okay / satisfactory / capable / successful" }),
      "頂": Object.freeze({ label: "doing", pos: "verb", jyutping: "ding2", syntax: "verb support_endure_verb", gloss: "support / withstand / endure / substitute" }),
      "裏邊": Object.freeze({ label: "where", pos: "noun", jyutping: "leoi5 bin6", syntax: "spatial_localizer locality_np", gloss: "inside / interior" }),
      "不": Object.freeze({ label: "func", pos: "function", jyutping: "bat1", syntax: "formal_negator", gloss: "formal/written negator" }),
      "收": Object.freeze({ label: "doing", pos: "verb", syntax: "verb", gloss: "receive / accept / collect" }),
      "愁": Object.freeze({ label: "like", pos: "adjective", syntax: "psych_stative_or_verb", gloss: "worried / troubled" }),
      "忍": Object.freeze({ label: "doing", pos: "verb", syntax: "verb", gloss: "endure / tolerate" }),
      "重": Object.freeze({ label: "how", pos: "adverb", jyutping: "zung6", syntax: "additive_continuative_adverb", gloss: "still / furthermore" }),
      "望": Object.freeze({ label: "doing", pos: "verb", syntax: "verb perception_or_desire_verb", gloss: "look / gaze / hope / expect" }),
      "共": Object.freeze({ label: "func", pos: "bound_morpheme", syntax: "bound_formal_root", gloss: "common / together / total (bound/formal root)" })
    });
    function reviewedAnalysis(surface, key, label, pos, syntax, gloss, jyutping, provenanceKind = "reviewed_lexical_analysis") {
      return Object.freeze({
        id: `lex:${surface}:${key}`,
        label,
        pos,
        jyutping,
        syntax,
        senses: Object.freeze([{ gloss }]),
        provenance: Object.freeze({ kind: provenanceKind, source: SOURCE })
      });
    }
    var EXPLICIT_ANALYSES = Object.freeze({
      "團": Object.freeze([
        reviewedAnalysis("團", "default", "what", "noun", "collective_np object_np", "group / collective", "tyun4"),
        reviewedAnalysis("團", "classifier", "measure_word", "classifier", "classifier_collective_or_mass", "classifier / measure for groups or round masses", "tyun4")
      ]),
      "啱啱": Object.freeze([
        reviewedAnalysis("啱啱", "default", "when", "adverb", "temporal_adverb", "just now", "ngaam1 ngaam1"),
        reviewedAnalysis("啱啱", "aam1_variant", "when", "adverb", "temporal_adverb", "just now; pronunciation variant", "aam1 aam1")
      ]),
      "唉": Object.freeze([
        reviewedAnalysis("唉", "default", "particle", "interjection", "interjection", "sigh / response interjection", "aai1"),
        reviewedAnalysis("唉", "oi1_interjection", "particle", "interjection", "interjection", "sigh / disapproval interjection", "oi1"),
        reviewedAnalysis("唉", "aai6_regret", "particle", "interjection", "interjection", "regret / pity interjection", "aai6")
      ]),
      "哎": Object.freeze([
        reviewedAnalysis("哎", "default", "particle", "interjection", "interjection", "attention / surprise / disapproval interjection", "aai1"),
        reviewedAnalysis("哎", "ai1_variant", "particle", "interjection", "interjection", "rarer interjection reading", "ai1")
      ]),
      "蛇": Object.freeze([
        reviewedAnalysis("蛇", "default", "what", "noun", "object_np animal_np", "snake / serpent", "se4"),
        reviewedAnalysis("蛇", "slack_off_verb", "doing", "verb", "verb work_avoidance_verb", "slack off / shirk at work", "se4")
      ]),
      "擰": Object.freeze([
        reviewedAnalysis("擰", "default", "doing", "verb", "verb", "twist / turn with force; screw", "ning6"),
        reviewedAnalysis("擰", "wring_verb", "doing", "verb", "verb", "wring", "ning4"),
        reviewedAnalysis("擰", "carry_hold_family", "doing", "verb", "verb", "carry / hold; colloquial 拎-type family", "ning1")
      ]),
      "打": Object.freeze([
        reviewedAnalysis("打", "default", "doing", "verb", "verb", "hit / play / call", "daa2"),
        reviewedAnalysis("打", "dozen_classifier", "measure_word", "classifier", "quantity_classifier dozen_measure", "dozen", "daa1")
      ]),
      "丸": Object.freeze([
        reviewedAnalysis("丸", "default", "what", "noun", "object_np nominal_root", "pellet / ball / pill", "jyun2"),
        reviewedAnalysis("丸", "jyun4_literary", "what", "noun", "object_np nominal_root", "pellet / ball / pill; literary reading", "jyun4")
      ]),
      "玩": Object.freeze([
        reviewedAnalysis("玩", "default", "doing", "verb", "verb", "play / have fun", "waan2"),
        reviewedAnalysis("玩", "wun6_bound", "func", "bound_morpheme", "bound_morpheme", "bound reading in forms such as 玩具 / 玩意", "wun6")
      ]),
      "幫": Object.freeze([
        reviewedAnalysis("幫", "default", "doing", "verb", "verb help_benefactive_verb", "help / assist", "bong1"),
        reviewedAnalysis("幫", "benefactive_coverb", "func", "function", "benefactive_coverb", "for / on behalf of", "bong1")
      ]),
      "往": Object.freeze([
        reviewedAnalysis("往", "default", "func", "function", "directional_relation motion_coverb", "towards / in the direction of", "wong5"),
        reviewedAnalysis("往", "motion_verb", "doing", "verb", "verb motion_directional_verb", "go / proceed toward", "wong5")
      ]),
      "份": Object.freeze([
        reviewedAnalysis("份", "default", "measure_word", "classifier", "general_classifier document_task_classifier", "classifier / measure for units or portions", "fan6"),
        reviewedAnalysis("份", "fan2_noun", "what", "noun", "abstract_np", "share / participation / role / entitlement", "fan2")
      ]),
      "為": Object.freeze([
        reviewedAnalysis("為", "default", "func", "function", "coverb", "for / because of / for the sake of", "wai6"),
        reviewedAnalysis("為", "wai4_relation", "func", "function", "relation_function", "as / become / be treated as", "wai4"),
        reviewedAnalysis("為", "wai4_tally_verb", "doing", "verb", "verb formal_tally_verb", "calculate / tally", "wai4")
      ]),
      "差唔多": Object.freeze([
        reviewedAnalysis("差唔多", "default", "like", "adjective", "stative_predicate approximation_predicate", "similar / about the same", "caa1 m4 do1"),
        reviewedAnalysis("差唔多", "approximation_adverb", "how", "adverb", "approximation_adverb", "almost / about / nearly", "caa1 m4 do1")
      ]),
      "開": Object.freeze([
        reviewedAnalysis("開", "default", "doing", "verb", "verb_or_modifier_head", "open / start / turn on", "hoi1"),
        reviewedAnalysis("開", "result_directional_complement", "func", "function", "resultative_or_directional_complement", "productive resultative / directional complement use", "hoi1")
      ]),
      "另外": Object.freeze([
        reviewedAnalysis("另外", "default", "how", "adverb", "additive_adverb discourse_adverb", "besides / in addition / separately", "ling6 ngoi6"),
        reviewedAnalysis("另外", "determiner", "func", "determiner", "determiner_like_modifier", "another / other", "ling6 ngoi6")
      ]),
      "希望": Object.freeze([
        reviewedAnalysis("希望", "default", "doing", "verb", "verb desire_verb", "hope / wish", "hei1 mong6"),
        reviewedAnalysis("希望", "noun", "what", "noun", "abstract_np", "hope / prospect", "hei1 mong6")
      ]),
      "其他": Object.freeze([
        reviewedAnalysis("其他", "default", "func", "determiner", "nominal_modifier determiner_like_modifier", "other", "kei4 taa1"),
        reviewedAnalysis("其他", "pronoun", "what", "pronoun", "pronoun", "the others", "kei4 taa1")
      ]),
      "吔": Object.freeze([
        reviewedAnalysis("吔", "default", "particle", "interjection", "interjection", "interjection", "jaa3"),
        reviewedAnalysis("吔", "sentence_particle", "particle", "particle", "sentence_final_particle", "pragmatic sentence particle", "jaa3")
      ]),
      "明白": Object.freeze([
        reviewedAnalysis("明白", "default", "doing", "verb", "verb cognition_verb", "understand / realize", "ming4 baak6"),
        reviewedAnalysis("明白", "clear_stative", "like", "adjective", "stative_predicate clarity_state", "clear / understood", "ming4 baak6")
      ]),
      "等等": Object.freeze([
        reviewedAnalysis("等等", "default", "func", "function", "list_continuation_marker", "etcetera / and so on", "dang2 dang2"),
        reviewedAnalysis("等等", "wait_verb", "doing", "verb", "verb", "wait / hold on", "dang2 dang2")
      ]),
      "中": Object.freeze([
        reviewedAnalysis("中", "default", "what", "noun", "nominal_or_bound_middle_family", "middle / central / China-related family", "zung1"),
        reviewedAnalysis("中", "zung3_verb", "doing", "verb", "verb", "hit / be hit / win / suffer", "zung3")
      ]),
      "根本": Object.freeze([
        reviewedAnalysis("根本", "default", "how", "adverb", "emphatic_adverb discourse_adverb", "fundamentally / simply / at all", "gan1 bun2"),
        reviewedAnalysis("根本", "noun", "what", "noun", "abstract_np", "root / foundation / basis", "gan1 bun2")
      ]),
      "盛": Object.freeze([
        reviewedAnalysis("盛", "default", "like", "adjective", "stative_predicate", "flourishing / prosperous / grand / abundant", "sing6"),
        reviewedAnalysis("盛", "sing4_verb", "doing", "verb", "verb", "fill / contain / ladle / accommodate", "sing4")
      ]),
      "燕": Object.freeze([
        reviewedAnalysis("燕", "default", "what", "noun", "object_np animal_np", "swallow (bird)", "jin3"),
        reviewedAnalysis("燕", "jin1_proper", "who", "proper_noun", "proper_name_family", "Yan historical / proper-name family", "jin1")
      ]),
      "請": Object.freeze([
        reviewedAnalysis("請", "default", "func", "function", "politeness_imperative_marker request_function", "please / request", "cing2"),
        reviewedAnalysis("請", "ceng2_spoken_verb", "doing", "verb", "verb request_invite_verb", "ask / invite / request", "ceng2")
      ]),
      "仲有": Object.freeze([
        reviewedAnalysis("仲有", "default", "lex", "lexical_item", "lexical_item", "neutral exact-surface coverage; retain productive 仲 + 有 segmentation by default", "zung6 jau5", "neutral_frequency_fallback_preserved"),
        reviewedAnalysis("仲有", "additive_discourse", "func", "function", "additive_discourse_marker", "also / furthermore", "zung6 jau5")
      ]),
      "唧": Object.freeze([
        reviewedAnalysis("唧", "default", "particle", "particle", "discourse_particle", "particle use", "zek1"),
        reviewedAnalysis("唧", "zit1_verb", "doing", "verb", "verb", "squeeze / tickle", "zit1"),
        reviewedAnalysis("唧", "zik1_verb_variant", "doing", "verb", "verb", "squeeze / tickle; reading variant", "zik1")
      ]),
      "老": Object.freeze([
        reviewedAnalysis("老", "default", "like", "adjective", "stative_predicate", "old", "lou5"),
        reviewedAnalysis("老", "familiar_prefix", "func", "bound_morpheme", "familiar_or_ordinal_prefix", "familiar / ordinal prefix", "lou5")
      ]),
      "咦": Object.freeze([
        reviewedAnalysis("咦", "default", "particle", "interjection", "interjection", "surprise interjection", "ji2"),
        reviewedAnalysis("咦", "ji4_variant", "particle", "interjection", "interjection", "surprise interjection; pronunciation variant", "ji4"),
        reviewedAnalysis("咦", "ji6_variant", "particle", "interjection", "interjection", "surprise interjection; pronunciation variant", "ji6")
      ]),
      "感覺": Object.freeze([
        reviewedAnalysis("感覺", "default", "doing", "verb", "verb perception_verb", "feel / perceive", "gam2 gok3"),
        reviewedAnalysis("感覺", "noun", "what", "noun", "abstract_np perception_noun", "feeling / sensation", "gam2 gok3")
      ]),
      "定": Object.freeze([
        reviewedAnalysis("定", "decide_fix_verb", "doing", "verb", "verb decide_fix_schedule_verb", "decide / settle / fix / schedule", "ding6"),
        reviewedAnalysis("定", "alternative_connector", "func", "function", "alternative_question_connector", "or; alternative-question connector", "ding6"),
        reviewedAnalysis("定", "steady_stative", "like", "adjective", "stative_predicate", "steady / calm", "ding6"),
        reviewedAnalysis("定", "advance_adverb", "how", "adverb", "advance_adverb", "in advance / beforehand", "ding6")
      ]),
      "本身": Object.freeze([
        reviewedAnalysis("本身", "default", "func", "pronoun", "reflexive_pronoun", "itself / oneself", "bun2 san1"),
        reviewedAnalysis("本身", "emphatic_modifier", "how", "adverb", "emphatic_modifier", "in itself / per se", "bun2 san1")
      ]),
      "格": Object.freeze([
        reviewedAnalysis("格", "default", "what", "noun", "abstract_or_object_np pattern_np", "square / grid / compartment / pattern", "gaak3"),
        reviewedAnalysis("格", "classifier", "measure_word", "classifier", "classifier_grid_or_compartment", "classifier / measure for grid or compartment units", "gaak3"),
        reviewedAnalysis("格", "formal_verb", "doing", "verb", "formal_or_bound_verb", "formal/bound verbal family", "gaak3")
      ]),
      "不如": Object.freeze([
        reviewedAnalysis("不如", "default", "func", "function", "suggestion_marker", "better to / how about", "bat1 jyu4"),
        reviewedAnalysis("不如", "comparative_predicate", "like", "adjective", "comparative_predicate", "not as good as / inferior to", "bat1 jyu4")
      ]),
      "今次": Object.freeze([
        reviewedAnalysis("今次", "default", "lex", "lexical_item", "lexical_item", "neutral exact-surface coverage; retain productive 今 + 次 segmentation by default", "gam1 ci3", "neutral_frequency_fallback_preserved"),
        reviewedAnalysis("今次", "this_time_noun", "when", "noun", "temporal_np", "this time", "gam1 ci3")
      ]),
      "汗": Object.freeze([
        reviewedAnalysis("汗", "default", "what", "noun", "object_np substance_np", "sweat / perspiration", "hon6"),
        reviewedAnalysis("汗", "hon4_proper", "who", "proper_noun", "proper_title_family", "Khan / proper-title family", "hon4"),
        reviewedAnalysis("汗", "hong6_proper", "who", "proper_noun", "proper_title_family", "Khan / proper-title family", "hong6")
      ]),
      "莊": Object.freeze([
        reviewedAnalysis("莊", "default", "where", "noun", "settlement_or_estate_np", "village / manor / estate family", "zong1"),
        reviewedAnalysis("莊", "organization_noun", "what", "noun", "organization_or_business_noun", "organization / dealer / business family", "zong1"),
        reviewedAnalysis("莊", "surname", "who", "proper_noun", "surname_element", "surname Zhuang/Chong family", "zong1")
      ]),
      "名": Object.freeze([
        reviewedAnalysis("名", "default", "what", "noun", "object_np", "name", "meng2"),
        reviewedAnalysis("名", "ming4_written", "what", "noun", "written_or_bound_nominal classifier_person", "written/bound name family; person-classifier use where licensed", "ming4")
      ]),
      "即刻": Object.freeze([
        reviewedAnalysis("即刻", "default", "when", "adverb", "temporal_adjunct immediate_time_adverb", "immediately / instantly", "zik1 hak1"),
        reviewedAnalysis("即刻", "kak1_variant", "when", "adverb", "temporal_adjunct immediate_time_adverb", "immediately / instantly", "zik1 kak1"),
        reviewedAnalysis("即刻", "haak1_variant", "when", "adverb", "temporal_adjunct immediate_time_adverb", "immediately / instantly", "zik1 haak1"),
        reviewedAnalysis("即刻", "kaak1_variant", "when", "adverb", "temporal_adjunct immediate_time_adverb", "immediately / instantly", "zik1 kaak1")
      ]),
      "勁": Object.freeze([
        reviewedAnalysis("勁", "default", "like", "adjective", "stative_predicate scalar_property", "strong / excellent", "ging6"),
        reviewedAnalysis("勁", "degree_adverb", "how", "adverb", "degree_adverb", "very / strongly", "ging6"),
        reviewedAnalysis("勁", "ging3_noun", "what", "noun", "abstract_np", "vigor / vitality / aura", "ging3")
      ]),
      "將": Object.freeze([
        reviewedAnalysis("將", "default", "func", "function", "disposal_coverb future_or_incipent_function", "disposal / future / incipient function", "zoeng1"),
        reviewedAnalysis("將", "zoeng3_general_noun", "who", "noun", "title_or_role_noun", "general / commander", "zoeng3"),
        reviewedAnalysis("將", "zoeng3_command_verb", "doing", "verb", "verb command_lead_verb", "command / lead", "zoeng3")
      ]),
      "像": Object.freeze([
        reviewedAnalysis("像", "default", "doing", "verb", "verb resemblance_predicate", "resemble / appear like", "zoeng6"),
        reviewedAnalysis("像", "image_noun", "what", "noun", "object_np image_or_statue_noun", "image / portrait / statue", "zoeng6")
      ]),
      "仔": Object.freeze([
        reviewedAnalysis("仔", "default", "who", "noun", "person_or_young_animal_noun", "boy / child / young animal", "zai2"),
        reviewedAnalysis("仔", "diminutive_suffix", "func", "bound_morpheme", "diminutive_nominal_suffix", "diminutive / nominal suffix", "zai2"),
        reviewedAnalysis("仔", "zi2_written_bound", "func", "bound_morpheme", "written_bound_morpheme", "written/bound reading in independently supported forms", "zi2")
      ]),
      "越": Object.freeze([
        reviewedAnalysis("越", "default", "func", "function", "correlative_degree_marker", "the more…; degree/correlative function", "jyut6"),
        reviewedAnalysis("越", "cross_exceed_verb", "doing", "verb", "verb", "cross / exceed / surpass", "jyut6"),
        reviewedAnalysis("越", "proper_bound_family", "what", "proper_noun", "proper_or_bound_nominal", "Yue / Vietnam / historical proper-name family", "jyut6")
      ]),
      "前": Object.freeze([
        reviewedAnalysis("前", "default", "where", "noun", "spatial_or_temporal_localizer", "front / before / ago", "cin4"),
        reviewedAnalysis("前", "localizer_function", "func", "function", "spatial_temporal_localizer_function", "before / ago / in front of", "cin4")
      ]),
      "頭": Object.freeze([
        reviewedAnalysis("頭", "default", "what", "noun", "object_np body_part_np", "head / top / end", "tau4"),
        reviewedAnalysis("頭", "localizer", "where", "noun", "spatial_or_temporal_localizer", "top / end / beginning locality family", "tau4"),
        reviewedAnalysis("頭", "classifier", "measure_word", "classifier", "classifier_family", "classifier use", "tau4")
      ]),
      "股": Object.freeze([
        reviewedAnalysis("股", "default", "what", "noun", "object_np nominal_or_measure_root", "share / portion / thigh", "gu2"),
        reviewedAnalysis("股", "classifier", "measure_word", "classifier", "classifier_current_smell_strand", "classifier for currents / smells / strands / portions", "gu2")
      ]),
      "部": Object.freeze([
        reviewedAnalysis("部", "default", "measure_word", "classifier", "classifier_vehicle_machine", "classifier for machines / films / works", "bou6"),
        reviewedAnalysis("部", "noun", "what", "noun", "part_department_noun", "part / department / division", "bou6")
      ]),
      "信": Object.freeze([
        reviewedAnalysis("信", "default", "doing", "verb", "belief_verb", "believe / trust", "seon3"),
        reviewedAnalysis("信", "letter_noun", "what", "noun", "object_np message_or_evidence_noun", "letter / message / evidence", "seon3")
      ]),
      "所": Object.freeze([
        reviewedAnalysis("所", "default", "func", "function", "nominalizer_or_passive_marker", "nominalizing / relative / passive function", "so2"),
        reviewedAnalysis("所", "institution_classifier", "measure_word", "classifier", "classifier_institution_building", "classifier for institutions / buildings", "so2")
      ]),
      "帶": Object.freeze([
        reviewedAnalysis("帶", "default", "doing", "verb", "verb take_bring_verb", "carry / bring / lead", "daai3"),
        reviewedAnalysis("帶", "daai2_noun", "what", "noun", "object_np band_belt_noun", "band / belt / zone", "daai2")
      ]),
      "全部": Object.freeze([
        reviewedAnalysis("全部", "default", "func", "quantifier", "universal_quantifier pronoun_like_quantifier", "all / the whole", "cyun4 bou6"),
        reviewedAnalysis("全部", "adverb", "how", "adverb", "degree_adverb", "entirely / altogether", "cyun4 bou6")
      ]),
      "服務": Object.freeze([
        reviewedAnalysis("服務", "default", "doing", "verb", "verb service_verb", "serve / provide service", "fuk6 mou6"),
        reviewedAnalysis("服務", "noun", "what", "noun", "abstract_or_event_noun", "service", "fuk6 mou6")
      ]),
      "覺": Object.freeze([
        reviewedAnalysis("覺", "default", "what", "noun", "object_in_vo sleep_noun", "sleep / nap", "gaau3"),
        reviewedAnalysis("覺", "gok3_verb", "doing", "verb", "verb cognition_perception_verb", "feel / think / notice", "gok3")
      ]),
      "本": Object.freeze([
        reviewedAnalysis("本", "default", "measure_word", "classifier", "classifier_book", "classifier for books / files", "bun2"),
        reviewedAnalysis("本", "basis_noun", "what", "noun", "abstract_np", "root / source / basis", "bun2"),
        reviewedAnalysis("本", "current_bound", "func", "bound_morpheme", "determiner_like_bound_morpheme", "this / current", "bun2")
      ]),
      "特別": Object.freeze([
        reviewedAnalysis("特別", "default", "like", "adjective", "modifier_or_stative", "special / unusual", "dak6 bit6"),
        reviewedAnalysis("特別", "adverb", "how", "adverb", "degree_or_focus_adverb", "especially / particularly", "dak6 bit6")
      ]),
      "得到": Object.freeze([
        reviewedAnalysis("得到", "default", "lex", "lexical_item", "lexical_item", "neutral exact-surface coverage; retain productive 得 + 到 resultative segmentation by default", "dak1 dou2", "neutral_frequency_fallback_preserved"),
        reviewedAnalysis("得到", "obtain_verb", "doing", "verb", "verb obtain_receive_verb", "obtain / receive", "dak1 dou3")
      ]),
      "理": Object.freeze([
        reviewedAnalysis("理", "default", "doing", "verb", "verb management_attention_verb", "manage / handle / pay attention to", "lei5"),
        reviewedAnalysis("理", "noun", "what", "noun", "abstract_or_bound_noun", "reason / principle / science", "lei5")
      ]),
      "種": Object.freeze([
        reviewedAnalysis("種", "default", "what", "noun", "kind_seed_noun", "kind / species / seed", "zung2"),
        reviewedAnalysis("種", "classifier", "measure_word", "classifier", "kind_type_classifier", "classifier for kinds / types", "zung2"),
        reviewedAnalysis("種", "plant_verb", "doing", "verb", "verb plant_cultivate_verb", "plant / cultivate", "zung3")
      ]),
      "堂": Object.freeze([
        reviewedAnalysis("堂", "default", "what", "noun", "room_or_hall_noun", "hall / room", "tong4"),
        reviewedAnalysis("堂", "classifier", "measure_word", "classifier", "class_or_set_classifier", "classifier for classes / sets", "tong4"),
        reviewedAnalysis("堂", "kinship_bound", "func", "bound_morpheme", "kinship_clan_bound_modifier", "paternal-clan / kinship bound modifier", "tong4")
      ]),
      "呃": Object.freeze([
        reviewedAnalysis("呃", "default", "doing", "verb", "verb deception_verb", "cheat / deceive", "aak1"),
        reviewedAnalysis("呃", "ngaak1_verb", "doing", "verb", "verb deception_verb", "cheat / deceive", "ngaak1"),
        reviewedAnalysis("呃", "aak3_particle", "particle", "particle", "sentence_final_particle", "sentence particle", "aak3")
      ]),
      "少": Object.freeze([
        reviewedAnalysis("少", "default", "like", "adjective", "stative_or_quantity", "few / little", "siu2"),
        reviewedAnalysis("少", "siu3_young_bound", "func", "bound_morpheme", "young_bound_morpheme", "young; bound reading in compounds", "siu3")
      ]),
      "出去": Object.freeze([
        reviewedAnalysis("出去", "default", "lex", "lexical_item", "lexical_item", "neutral exact-surface coverage; retain productive 出 + 去 segmentation by default", "ceot1 heoi3", "neutral_frequency_fallback_preserved"),
        reviewedAnalysis("出去", "motion_verb", "doing", "verb", "verb outward_motion_verb", "go out / leave", "ceot1 heoi3"),
        reviewedAnalysis("出去", "directional_function", "func", "function", "postverbal_directional_function", "outward directional function", "ceot1 heoi3")
      ]),
      "唔記得": Object.freeze([
        reviewedAnalysis("唔記得", "default", "lex", "lexical_item", "lexical_item", "neutral exact-surface coverage; retain productive negation + 記得 segmentation by default", "m4 gei3 dak1", "neutral_frequency_fallback_preserved"),
        reviewedAnalysis("唔記得", "forget_verb", "doing", "verb", "verb cognition_memory_verb", "forget / not remember", "m4 gei3 dak1")
      ]),
      "起": Object.freeze([
        reviewedAnalysis("起", "default", "doing", "verb", "verb rise_start_verb", "rise / start / set out", "hei2"),
        reviewedAnalysis("起", "inchoative_complement", "func", "function", "inchoative_or_complement_function", "inchoative / complement / start-point function", "hei2"),
        reviewedAnalysis("起", "instance_classifier", "measure_word", "classifier", "instance_case_classifier", "classifier for cases / instances", "hei2")
      ]),
      "項": Object.freeze([
        reviewedAnalysis("項", "default", "measure_word", "classifier", "general_classifier item_classifier", "classifier for items / projects / clauses / sums", "hong6"),
        reviewedAnalysis("項", "noun", "what", "noun", "item_term_noun", "item / term", "hong6")
      ]),
      "蚊": Object.freeze([
        reviewedAnalysis("蚊", "default", "what", "noun", "currency_unit", "dollar / buck", "man1"),
        reviewedAnalysis("蚊", "mosquito_noun", "what", "noun", "object_np animal_np", "mosquito", "man1")
      ]),
      "印": Object.freeze([
        reviewedAnalysis("印", "default", "what", "noun", "object_np mark_or_artifact_np", "stamp / seal / mark", "jan3"),
        reviewedAnalysis("印", "print_verb", "doing", "verb", "verb print_stamp_verb", "print / stamp / impress", "jan3"),
        reviewedAnalysis("印", "india_bound", "func", "bound_morpheme", "proper_name_bound_abbreviation", "India abbreviation in formal compounds", "jan3")
      ]),
      "直": Object.freeze([
        reviewedAnalysis("直", "default", "like", "adjective", "stative_predicate orientation_property", "straight / direct / upright", "zik6"),
        reviewedAnalysis("直", "adverb", "how", "adverb", "manner_or_continuative_adverb", "straight on / directly / continuously", "zik6")
      ]),
      "嶺": Object.freeze([
        reviewedAnalysis("嶺", "default", "where", "noun", "natural_place_np ridge_np", "ridge / mountain range", "leng5"),
        reviewedAnalysis("嶺", "ling5_variant", "where", "noun", "natural_place_np ridge_np", "ridge / mountain range; literary/variant reading", "ling5")
      ]),
      "入去": Object.freeze([
        reviewedAnalysis("入去", "default", "lex", "lexical_item", "lexical_item", "neutral exact-surface coverage; retain productive 入 + 去 segmentation by default", "jap6 heoi3", "neutral_frequency_fallback_preserved"),
        reviewedAnalysis("入去", "motion_verb", "doing", "verb", "verb inward_motion_verb", "enter / go inside", "jap6 heoi3"),
        reviewedAnalysis("入去", "directional_function", "func", "function", "postverbal_directional_function", "inward directional function", "jap6 heoi3")
      ]),
      "掂": Object.freeze([
        reviewedAnalysis("掂", "default", "like", "adjective", "stative_predicate evaluative_property", "okay / satisfactory / capable / successful", "dim6"),
        reviewedAnalysis("掂", "dim3_touch_verb", "doing", "verb", "verb contact_verb", "touch", "dim3"),
        reviewedAnalysis("掂", "dim1_heft_verb", "doing", "verb", "verb weighing_estimation_verb", "heft / weigh / estimate weight", "dim1")
      ]),
      "頂": Object.freeze([
        reviewedAnalysis("頂", "default", "doing", "verb", "verb support_endure_verb", "support / withstand / endure / substitute", "ding2"),
        reviewedAnalysis("頂", "ding2_interjection", "particle", "interjection", "interjection", "exclamation", "ding2"),
        reviewedAnalysis("頂", "deng2_noun", "what", "noun", "top_roof_noun", "top / roof", "deng2"),
        reviewedAnalysis("頂", "deng2_classifier", "measure_word", "classifier", "hat_classifier", "classifier for hats", "deng2")
      ]),
      "裏邊": Object.freeze([
        reviewedAnalysis("裏邊", "default", "where", "noun", "spatial_localizer locality_np", "inside / interior", "leoi5 bin6"),
        reviewedAnalysis("裏邊", "localizer_function", "func", "function", "spatial_localizer_function", "inside / within", "leoi5 bin6")
      ]),
      "讀書": Object.freeze([
        reviewedAnalysis("讀書", "default", "lex", "lexical_item", "lexical_item", "neutral exact-surface coverage; retain productive 讀 + 書 segmentation by default", "duk6 syu1", "neutral_frequency_fallback_preserved"),
        reviewedAnalysis("讀書", "study_verb", "doing", "verb", "verb study_read_verb", "study / read", "duk6 syu1")
      ]),
      "件": Object.freeze([
        reviewedAnalysis("件", "default", "measure_word", "classifier", "classifier_clothing_item", "classifier for events / things / clothing", "gin6"),
        reviewedAnalysis("件", "noun", "what", "noun", "item_component_noun", "item / component", "gin6")
      ]),
      "八": Object.freeze([
        reviewedAnalysis("八", "default", "how", "numeral", "quantity count_value numeral_eight", "eight", "baat3"),
        reviewedAnalysis("八", "gossip_verb", "doing", "verb", "verb slang_gossip_verb", "gossip / be nosy", "baat3")
      ]),
      "重": Object.freeze([
        reviewedAnalysis("重", "default", "how", "adverb", "additive_continuative_adverb", "still / furthermore", "zung6"),
        reviewedAnalysis("重", "zung6_importance", "like", "adjective", "importance_or_seriousness_property", "important / weighty / serious", "zung6"),
        reviewedAnalysis("重", "cung4_repeat", "how", "adverb", "repeat_again_function", "repeat / again", "cung4"),
        reviewedAnalysis("重", "cung5_heavy", "like", "adjective", "stative_predicate weight_property", "heavy", "cung5")
      ]),
      "將來": Object.freeze([
        reviewedAnalysis("將來", "default", "when", "noun", "future_time_np", "the future", "zoeng1 loi4"),
        reviewedAnalysis("將來", "temporal_adverb", "when", "adverb", "temporal_adjunct future_time_adverb", "in the future", "zoeng1 loi4")
      ]),
      "斜": Object.freeze([
        reviewedAnalysis("斜", "default", "like", "adjective", "stative_predicate orientation_property", "slanted / oblique", "ce4"),
        reviewedAnalysis("斜", "ce3_variant", "like", "adjective", "stative_predicate orientation_property", "slanted / oblique; pronunciation variant", "ce3")
      ]),
      "工作": Object.freeze([
        reviewedAnalysis("工作", "default", "what", "noun", "work_noun activity_noun", "work / job / task", "gung1 zok3"),
        reviewedAnalysis("工作", "verb", "doing", "verb", "verb work_verb", "work", "gung1 zok3")
      ])
    });
    function isNeutralFrequencyFallback(entry) {
      return Boolean(
        entry && entry.label === "lex" && entry.pos === "lexical_item" && entry.syntax === "lexical_item" && typeof entry.note === "string" && entry.note.includes("Exact surface retained as neutral lexical coverage")
      );
    }
    function applyReviewedEntries(entries) {
      if (!Array.isArray(entries)) throw new TypeError("reviewed lexical overlay requires an entry array");
      return entries.map(([surface, entry]) => {
        const promotion = PROMOTIONS[surface];
        if (!promotion || !isNeutralFrequencyFallback(entry)) return [surface, entry];
        if (BLOCKED_ATOMIC_SURFACES.has(surface) || CANDIDATE_ONLY_SURFACES.has(surface)) {
          throw new Error(`reviewed promotion unexpectedly targets protected neutral surface ${surface}`);
        }
        const pronunciationStatus = promotion.jyutping ? "reviewed_in_final_adjudication" : "inherited_runtime_candidate_not_independently_promoted";
        const promoted = {
          ...entry,
          label: promotion.label,
          pos: promotion.pos,
          syntax: promotion.syntax,
          senses: [{ gloss: promotion.gloss }],
          note: `Reviewed ranks 251–500 lexical adjudication: ${promotion.gloss}.`,
          provenance: {
            kind: "reviewed_lexical_promotion",
            source: SOURCE,
            pronunciation_status: pronunciationStatus
          }
        };
        if (promotion.jyutping) promoted.jyutping = promotion.jyutping;
        return [surface, promoted];
      });
    }
    module2.exports = Object.freeze({
      SOURCE,
      BLOCKED_ATOMIC_SURFACES,
      CANDIDATE_ONLY_SURFACES,
      PROMOTIONS,
      EXPLICIT_ANALYSES,
      isNeutralFrequencyFallback,
      applyReviewedEntries
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1-250-reviewed.js
var require_cifu_r1_250_reviewed = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1-250-reviewed.js"(exports2, module2) {
    "use strict";
    var SOURCE = "GitHub issue #792 authoritative expert adjudication chain (final audit 5275934145 plus named later corrections)";
    var PROTECTED_NEUTRAL_SURFACES = Object.freeze({
      "一個": "blocked_atomic",
      "係咪": "blocked_atomic",
      "哩": "research_required",
      "落去": "blocked_atomic",
      "穿過": "research_required",
      "兩個": "blocked_atomic",
      "上去": "blocked_atomic",
      "有個": "blocked_atomic",
      "唔到": "blocked_atomic",
      "第一": "blocked_atomic",
      "兜過": "blocked_atomic",
      "冇乜": "blocked_atomic",
      "出嚟": "blocked_atomic",
      "返去": "blocked_atomic"
    });
    var CANDIDATE_ONLY_SURFACES = /* @__PURE__ */ new Set([
      "唔係",
      "喀",
      "真係",
      "咁樣",
      "噉樣",
      "個人",
      "哩個",
      "過去",
      "一樣",
      "唔見",
      "個位",
      "都會",
      "幾多"
    ]);
    var NEUTRAL_READING_OVERRIDES = Object.freeze({
      "喀": "kaa1",
      "哩個": "ni1 go3",
      "個位": "go3 wai2",
      "幾多": "gei2 do1",
      "係咪": "hai6 mai6",
      "唔係": "m4 hai6"
    });
    var PROMOTIONS = Object.freeze({
      "哋": Object.freeze({ label: "func", pos: "function", syntax: "plural_suffix", gloss: "plural suffix for personal pronouns", jyutping: "dei6" }),
      "即": Object.freeze({ label: "func", pos: "function", syntax: "formal_connective discourse_linker", gloss: "formal connective / discourse linker", jyutping: "zik1" }),
      "誒": Object.freeze({ label: "particle", pos: "interjection", syntax: "interjection", gloss: "interjection / attention or reaction call", jyutping: "e6" }),
      "哦": Object.freeze({ label: "particle", pos: "interjection", syntax: "interjection", gloss: "acknowledgement interjection", jyutping: "o4" }),
      "兜": Object.freeze({ label: "doing", pos: "verb", syntax: "verb motion_or_solicit_verb", gloss: "go around / circle / solicit", jyutping: "dau1" }),
      "然": Object.freeze({ label: "func", pos: "bound", syntax: "bound_written_morpheme", gloss: "formal/bound thus/so/-ly morpheme", jyutping: "jin4" }),
      "嘛": Object.freeze({ label: "particle", pos: "particle", syntax: "sentence_final_particle", gloss: "modal/final particle", jyutping: "maa5" }),
      "嗯": Object.freeze({ label: "particle", pos: "interjection", syntax: "backchannel_interjection", gloss: "backchannel / acknowledgement interjection", jyutping: "m6" }),
      "方": Object.freeze({ label: "like", pos: "adjective", syntax: "stative_predicate shape_property", gloss: "square / rectangular", jyutping: "fong1" }),
      "嘩": Object.freeze({ label: "particle", pos: "interjection", syntax: "interjection", gloss: "wow / exclamatory interjection", jyutping: "waa3" }),
      "揾": Object.freeze({ label: "doing", pos: "verb", syntax: "verb find_verb", gloss: "find / search; orthographic variant of 搵", jyutping: "wan2" }),
      "埋": Object.freeze({ label: "doing", pos: "verb", syntax: "verb bury_hide_verb", gloss: "bury / hide", jyutping: "maai4" }),
      "裏": Object.freeze({ label: "where", pos: "noun", syntax: "spatial_localizer locality_np", gloss: "inside / interior localizer", jyutping: "leoi5" }),
      "而": Object.freeze({ label: "func", pos: "conjunction", syntax: "formal_linker conjunction", gloss: "formal conjunction/linker", jyutping: "ji4" }),
      "台": Object.freeze({ label: "what", pos: "noun", syntax: "platform_stage_noun", gloss: "platform / stage", jyutping: "toi4" }),
      "番": Object.freeze({ label: "measure_word", pos: "classifier", syntax: "event_iteration_classifier", gloss: "event/iteration classifier: a time / round", jyutping: "faan1" }),
      "比較": Object.freeze({ label: "doing", pos: "verb", syntax: "verb compare_verb", gloss: "compare", jyutping: "bei2 gaau3" }),
      "死": Object.freeze({ label: "doing", pos: "verb", syntax: "verb die_verb", gloss: "die", jyutping: "sei2" }),
      "右面": Object.freeze({ label: "where", pos: "noun", syntax: "spatial_localizer locality_np", gloss: "right side", jyutping: "jau6 min6" }),
      "左面": Object.freeze({ label: "where", pos: "noun", syntax: "spatial_localizer locality_np", gloss: "left side", jyutping: "zo2 min6" }),
      "成": Object.freeze({ label: "doing", pos: "verb", syntax: "verb success_completion_verb", gloss: "succeed / accomplish / become", jyutping: "sing4" }),
      "戀": Object.freeze({ label: "func", pos: "bound", syntax: "bound_formal_love_root", gloss: "formal/bound love or attachment root", jyutping: "lyun2" })
    });
    var CORRECTIONS = Object.freeze({
      "時": Object.freeze({ label: "when", pos: "bound", syntax: "bound_temporal_root", gloss: "bound/formal temporal root", jyutping: "si4" }),
      "善": Object.freeze({ label: "like", pos: "bound", syntax: "bound_formal_evaluative_root", gloss: "formal/bound good/virtuous/skilled root", jyutping: "sin6" }),
      "港": Object.freeze({ label: "where", pos: "bound", syntax: "bound_place_root", gloss: "bound place root / Hong Kong abbreviation", jyutping: "gong2" }),
      "韻": Object.freeze({ label: "what", pos: "noun", syntax: "linguistic_term_np rhyme_noun", gloss: "rhyme / phonological rime", jyutping: "wan5" }),
      "廟": Object.freeze({ label: "where", pos: "noun", syntax: "place_np temple_noun", gloss: "temple / shrine", jyutping: "miu2" })
    });
    function reviewedAnalysis(surface, key, label, pos, syntax, gloss, jyutping, kind = "reviewed_lexical_analysis") {
      return Object.freeze({
        id: `lex:${surface}:${key}`,
        label,
        pos,
        jyutping: jyutping || "",
        syntax,
        senses: Object.freeze([{ gloss }]),
        provenance: Object.freeze({ kind, source: SOURCE })
      });
    }
    function neutralDefaultAnalysis(surface) {
      return reviewedAnalysis(surface, "default", "lex", "lexical_item", "lexical_item", "neutral exact-surface coverage; preserve productive/component segmentation by default", NEUTRAL_READING_OVERRIDES[surface] || "", "neutral_frequency_fallback_preserved");
    }
    var REVIEWED_ANALYSES = Object.freeze({
      "呢": Object.freeze([
        reviewedAnalysis("呢", "default", "func", "determiner", "demonstrative_determiner", "this; demonstrative before a classifier", "ni1"),
        reviewedAnalysis("呢", "sentence_final_particle", "particle", "particle", "sentence_final_particle thematic_question_particle", "thematic/question particle", "ne1")
      ]),
      "嘅": Object.freeze([
        reviewedAnalysis("嘅", "default", "func", "function", "genitive_modifier_linker", "genitive/attributive linker", "ge3"),
        reviewedAnalysis("嘅", "nominalizer", "func", "function", "nominalizer headless_modifier_marker", "nominalizer / headless modifier marker", "ge3"),
        reviewedAnalysis("嘅", "sentence_final_particle", "particle", "particle", "sentence_final_particle", "sentence-final assertion particle", "ge3")
      ]),
      "就": Object.freeze([
        reviewedAnalysis("就", "default", "how", "adverb", "focus_sequence_adverb", "then / just / exactly / already", "zau6"),
        reviewedAnalysis("就", "conjunction", "func", "conjunction", "clause_linker", "then / in that case / as soon as", "zau6"),
        reviewedAnalysis("就", "accommodate_verb", "doing", "verb", "verb accommodate_yield_verb", "accommodate / yield / suit", "zau6"),
        reviewedAnalysis("就", "relational", "func", "preposition", "relational_preposition", "regarding / in light of", "zau6"),
        reviewedAnalysis("就", "bound", "func", "bound", "bound_morpheme", "bound/formal 就 family", "zau6")
      ]),
      "咁": Object.freeze([
        reviewedAnalysis("咁", "default", "how", "adverb", "manner_demonstrative_adverb", "so / like that / in that way", "gam2"),
        reviewedAnalysis("咁", "degree", "how", "adverb", "degree_adverb", "so / to such a degree", "gam3")
      ]),
      "好": Object.freeze([
        reviewedAnalysis("好", "default", "like", "adjective", "stative_predicate evaluative_property", "good / fine", "hou2"),
        reviewedAnalysis("好", "degree", "how", "adverb", "degree_adverb", "very / quite", "hou2"),
        reviewedAnalysis("好", "like_verb", "doing", "verb", "verb preference_verb", "like / be fond of", "hou3")
      ]),
      "啲": Object.freeze([
        reviewedAnalysis("啲", "default", "how", "quantifier", "quantity_quantifier", "some / a few", "di1"),
        reviewedAnalysis("啲", "degree", "how", "function", "comparative_degree_function", "a little more / comparative degree", "di1")
      ]),
      "喺": Object.freeze([
        reviewedAnalysis("喺", "default", "func", "preposition", "locative_coverb", "at / in / on", "hai2"),
        reviewedAnalysis("喺", "locative_predicate", "where", "verb", "locative_predicate", "be located at", "hai2")
      ]),
      "去": Object.freeze([
        reviewedAnalysis("去", "default", "doing", "verb", "verb motion_verb", "go", "heoi3"),
        reviewedAnalysis("去", "directional", "func", "function", "directional_complement", "away / thither directional", "heoi3")
      ]),
      "到": Object.freeze([
        reviewedAnalysis("到", "default", "doing", "verb", "verb arrival_verb", "arrive / reach", "dou3"),
        reviewedAnalysis("到", "relation", "func", "preposition", "goal_relation", "to / until", "dou3"),
        reviewedAnalysis("到", "resultative", "func", "function", "result_complement", "result/extent complement", "dou2"),
        reviewedAnalysis("到", "approx", "how", "function", "approximation_function", "approximately / up to", "dou2")
      ]),
      "哋": Object.freeze([
        reviewedAnalysis("哋", "default", "func", "function", "plural_suffix", "plural suffix for personal pronouns", "dei6"),
        reviewedAnalysis("哋", "attenuative", "func", "function", "attenuative_suffix", "attenuative/collective suffix", "dei2")
      ]),
      "話": Object.freeze([
        reviewedAnalysis("話", "default", "doing", "verb", "verb speech_verb", "say / tell / opine", "waa6"),
        reviewedAnalysis("話", "noun_waa2", "what", "noun", "speech_words_noun", "speech / words", "waa2"),
        reviewedAnalysis("話", "noun_waa6", "what", "noun", "speech_words_noun", "speech / words", "waa6")
      ]),
      "跟住": Object.freeze([
        reviewedAnalysis("跟住", "default", "func", "function", "sequence_connector discourse_marker", "then / next / following that", "gan1 zyu6"),
        reviewedAnalysis("跟住", "verb", "doing", "verb", "verb follow_continue_verb", "follow / continue following", "gan1 zyu6")
      ]),
      "會": Object.freeze([
        reviewedAnalysis("會", "default", "func", "function", "modal_future_or_capability", "will / can / know how", "wui5"),
        reviewedAnalysis("會", "meeting_noun", "what", "noun", "meeting_association_noun", "meeting / association", "wui6"),
        reviewedAnalysis("會", "assemble_verb", "doing", "verb", "verb assemble_meet_verb", "assemble / meet", "wui6")
      ]),
      "要": Object.freeze([
        reviewedAnalysis("要", "default", "doing", "verb", "verb want_need_verb", "want / need / demand", "jiu3"),
        reviewedAnalysis("要", "modal", "func", "function", "modal_necessity_or_future", "must / need to / will", "jiu3")
      ]),
      "喀": Object.freeze([
        reviewedAnalysis("喀", "haak3_retch", "particle", "interjection", "onomatopoeic_interjection", "retch / vomiting sound", "haak3"),
        reviewedAnalysis("喀", "kaa1_sound", "particle", "interjection", "onomatopoeic_interjection", "click / crack / laugh sound", "kaa1"),
        reviewedAnalysis("喀", "kaa3_transliteration", "func", "bound", "transliteration_onomatopoeic_character", "transliteration / onomatopoeic character reading", "kaa3"),
        reviewedAnalysis("喀", "kak1_bound", "func", "bound", "restricted_historical_bound_reading", "restricted historical/bound use", "kak1")
      ]),
      "一": Object.freeze([
        reviewedAnalysis("一", "default", "how", "numeral", "quantity numeral_one", "one", "jat1"),
        reviewedAnalysis("一", "conjunction", "func", "conjunction", "once_as_soon_as_linker", "once / as soon as", "jat1"),
        reviewedAnalysis("一", "reduplication_affix", "func", "bound", "reduplication_affix", "bound affix/infix in reduplicative patterns", "jat1")
      ]),
      "喎": Object.freeze([
        reviewedAnalysis("喎", "default", "particle", "particle", "sentence_final_particle", "reportative / hearsay particle", "wo3"),
        reviewedAnalysis("喎", "wo5_particle", "particle", "particle", "sentence_final_particle", "marked final-particle family", "wo5"),
        reviewedAnalysis("喎", "wo4_particle", "particle", "particle", "sentence_final_particle", "marked final-particle family", "wo4"),
        reviewedAnalysis("喎", "wo5_failed", "like", "adjective", "stative_predicate", "failed / ruined", "wo5")
      ]),
      "㗎": Object.freeze([
        reviewedAnalysis("㗎", "default", "particle", "particle", "sentence_final_particle", "sentence-final particle", "gaa3"),
        reviewedAnalysis("㗎", "gaa4_particle", "particle", "particle", "sentence_final_particle", "sentence-final particle variant", "gaa4")
      ]),
      "度": Object.freeze([
        reviewedAnalysis("度", "default", "what", "noun", "degree_measure_noun", "degree / measure", "dou6"),
        reviewedAnalysis("度", "locative", "where", "function", "locative_bound_function", "place / locality bound function", "dou6"),
        reviewedAnalysis("度", "measure_verb", "doing", "verb", "verb measure_estimate_verb", "measure / estimate", "dok6"),
        reviewedAnalysis("度", "textile_noun", "what", "noun", "textile_length_noun", "textile-length unit/use", "dou2")
      ]),
      "得": Object.freeze([
        reviewedAnalysis("得", "default", "doing", "verb", "verb obtain_possible_verb", "get / obtain / be possible", "dak1"),
        reviewedAnalysis("得", "modal", "func", "function", "modal_permission_ability", "may / can / be allowed", "dak1"),
        reviewedAnalysis("得", "postverbal", "func", "particle", "postverbal_complement_marker", "postverbal complement / structural marker", "dak1")
      ]),
      "上": Object.freeze([
        reviewedAnalysis("上", "default", "doing", "verb", "verb upward_motion_verb", "go up / get on", "soeng5"),
        reviewedAnalysis("上", "localizer", "where", "noun", "spatial_localizer upper_location", "up / above / upper", "soeng6")
      ]),
      "因為": Object.freeze([
        reviewedAnalysis("因為", "default", "func", "conjunction", "causal_clause_linker", "because", "jan1 wai6"),
        reviewedAnalysis("因為", "coverb", "func", "preposition", "causal_coverb", "because of / due to", "jan1 wai6")
      ]),
      "下": Object.freeze([
        reviewedAnalysis("下", "default", "where", "noun", "spatial_localizer lower_location", "below / under", "haa6"),
        reviewedAnalysis("下", "event_classifier", "measure_word", "classifier", "event_classifier", "a time / brief event classifier", "haa5"),
        reviewedAnalysis("下", "postverbal_suffix", "func", "particle", "postverbal_delimitative_suffix", "delimitative/continuative postverbal suffix", "haa5")
      ]),
      "囉": Object.freeze([
        reviewedAnalysis("囉", "default", "particle", "particle", "sentence_final_particle", "sentence-final particle", "lo1"),
        reviewedAnalysis("囉", "lo4_particle", "particle", "particle", "sentence_final_particle", "sentence-final particle variant", "lo4")
      ]),
      "咩": Object.freeze([
        reviewedAnalysis("咩", "default", "func", "pronoun", "wh_pronoun_or_determiner", "what / which", "me1"),
        reviewedAnalysis("咩", "sentence_final_particle", "particle", "particle", "sentence_final_particle interrogative_surprise", "interrogative / surprise final particle", "me1")
      ]),
      "嚟": Object.freeze([
        reviewedAnalysis("嚟", "default", "doing", "verb", "verb motion_verb", "come / bring", "lai4"),
        reviewedAnalysis("嚟", "lei4_verb", "doing", "verb", "verb motion_verb", "come / bring; pronunciation variant", "lei4"),
        reviewedAnalysis("嚟", "grammaticalized", "func", "function", "copular_emphatic_or_recent_event_function", "grammaticalized copular/emphatic/recent-event function", "lai4")
      ]),
      "哦": Object.freeze([
        reviewedAnalysis("哦", "default", "particle", "interjection", "acknowledgement_interjection", "acknowledgement", "o4"),
        reviewedAnalysis("哦", "realization_particle", "particle", "particle", "realization_particle", "realization / recognition", "o5"),
        reviewedAnalysis("哦", "aha_particle", "particle", "particle", "aha_rule_breaking_particle", "aha / realization / rule-breaking reaction", "o3"),
        reviewedAnalysis("哦", "surprise", "particle", "interjection", "surprise_interjection", "surprise", "o2"),
        reviewedAnalysis("哦", "nag_verb", "doing", "verb", "verb nag_verb", "nag / chant", "ngo4")
      ]),
      "吓": Object.freeze([
        reviewedAnalysis("吓", "default", "particle", "interjection", "interjection", "huh? / what?", "haa2"),
        reviewedAnalysis("吓", "final_particle", "particle", "particle", "sentence_final_particle", "confirmation/final particle", "haa2"),
        reviewedAnalysis("吓", "postverbal_suffix", "func", "particle", "postverbal_delimitative_suffix", "postverbal aspectual/delimitative suffix", "haa5")
      ]),
      "咪": Object.freeze([
        reviewedAnalysis("咪", "prohibitive_marker", "func", "function", "prohibitive_marker", "don't; prohibitive marker", "mai5", "existing_runtime_contextual_override"),
        reviewedAnalysis("咪", "discourse_focus_marker", "func", "function", "discourse_focus_marker", "discourse / focus marker", "mai6", "existing_runtime_contextual_override"),
        reviewedAnalysis("咪", "study_verb", "doing", "verb", "verb study_cram_verb", "study / cram", "mai1")
      ]),
      "同": Object.freeze([
        reviewedAnalysis("同", "default", "like", "adjective", "same_property", "same", "tung4"),
        reviewedAnalysis("同", "relation", "func", "preposition", "co_participant_relation", "with / to / for", "tung4"),
        reviewedAnalysis("同", "conjunction", "func", "conjunction", "coordination_linker", "and / with", "tung4")
      ]),
      "好似": Object.freeze([
        reviewedAnalysis("好似", "default", "doing", "verb", "verb resemblance_predicate", "resemble / be like", "hou2 ci5"),
        reviewedAnalysis("好似", "adverb", "how", "adverb", "epistemic_resemblance_adverb", "apparently / seemingly", "hou2 ci5"),
        reviewedAnalysis("好似", "conjunction", "func", "conjunction", "comparison_linker", "such as / like", "hou2 ci5")
      ]),
      "可能": Object.freeze([
        reviewedAnalysis("可能", "default", "how", "adverb", "epistemic_adverb", "possibly / probably", "ho2 nang4"),
        reviewedAnalysis("可能", "noun", "what", "noun", "possibility_noun", "possibility", "ho2 nang4")
      ]),
      "來": Object.freeze([
        reviewedAnalysis("來", "default", "doing", "verb", "verb motion_verb", "come", "lai4"),
        reviewedAnalysis("來", "lei4_verb", "doing", "verb", "verb motion_verb", "come; pronunciation variant", "lei4"),
        reviewedAnalysis("來", "grammaticalized", "func", "function", "grammaticalized_motion_function", "grammaticalized directional/copular function", "lai4"),
        reviewedAnalysis("來", "loi4_bound", "func", "bound", "written_bound_reading", "written/bound reading", "loi4")
      ]),
      "經過": Object.freeze([
        reviewedAnalysis("經過", "default", "doing", "verb", "verb motion_path_verb", "pass / go through", "ging1 gwo3"),
        reviewedAnalysis("經過", "noun", "what", "noun", "process_course_noun", "process / course", "ging1 gwo3")
      ]),
      "時": Object.freeze([
        reviewedAnalysis("時", "default", "when", "bound", "bound_temporal_root", "bound/formal temporal root", "si4"),
        reviewedAnalysis("時", "conjunction", "func", "conjunction", "temporal_correlative_linker", "when / at times in 時…時… and written linking", "si4"),
        reviewedAnalysis("時", "classifier", "measure_word", "classifier", "written_time_classifier", "written classifier/o'clock use", "si4")
      ]),
      "邊": Object.freeze([
        reviewedAnalysis("邊", "default", "func", "determiner", "wh_determiner", "which / where", "bin1"),
        reviewedAnalysis("邊", "locality_bin1", "where", "noun", "spatial_localizer side_edge", "side / edge", "bin1"),
        reviewedAnalysis("邊", "locality_bin6", "where", "noun", "restricted_positional_localizer", "restricted/contextual positional side reading", "bin6")
      ]),
      "或者": Object.freeze([
        reviewedAnalysis("或者", "default", "func", "conjunction", "alternative_linker", "or", "waak6 ze2"),
        reviewedAnalysis("或者", "adverb", "how", "adverb", "epistemic_adverb", "perhaps / maybe", "waak6 ze2")
      ]),
      "唔好": Object.freeze([
        reviewedAnalysis("唔好", "default", "func", "function", "prohibitive_modal", "don't / should not", "m4 hou2"),
        reviewedAnalysis("唔好", "not_good", "like", "adjective", "negated_stative_predicate", "not good", "m4 hou2")
      ]),
      "俾": Object.freeze([
        reviewedAnalysis("俾", "default", "doing", "verb", "verb give_allow_verb", "give / allow", "bei2"),
        reviewedAnalysis("俾", "passive_agent", "func", "function", "passive_agent_introducer", "passive / agent-introducing function", "bei2")
      ]),
      "少少": Object.freeze([
        reviewedAnalysis("少少", "default", "like", "adjective", "quantificational_property", "a little / a few", "siu2 siu2"),
        reviewedAnalysis("少少", "degree", "how", "adverb", "degree_adverb", "slightly / a little", "siu2 siu2")
      ]),
      "面": Object.freeze([
        reviewedAnalysis("面", "default", "what", "noun", "face_surface_noun", "face / reputation / plane", "min2"),
        reviewedAnalysis("面", "side_location", "where", "noun", "side_location_noun", "side / aspect / location", "min6"),
        reviewedAnalysis("面", "classifier", "measure_word", "classifier", "surface_side_classifier", "classifier for sides/surfaces", "min6")
      ]),
      "幾": Object.freeze([
        reviewedAnalysis("幾", "default", "func", "quantifier", "wh_quantity_quantifier", "how many / several", "gei2"),
        reviewedAnalysis("幾", "degree", "how", "adverb", "degree_adverb", "how / quite", "gei2")
      ]),
      "畀": Object.freeze([
        reviewedAnalysis("畀", "default", "doing", "verb", "verb give_allow_verb", "give / allow", "bei2"),
        reviewedAnalysis("畀", "passive_agent", "func", "function", "passive_agent_introducer", "passive / agent-introducing function", "bei2")
      ]),
      "過": Object.freeze([
        reviewedAnalysis("過", "default", "doing", "verb", "verb cross_pass_verb", "cross / pass / spend", "gwo3"),
        reviewedAnalysis("過", "experiential", "func", "particle", "experiential_aspect_marker", "experiential aspect marker", "gwo3"),
        reviewedAnalysis("過", "degree", "func", "function", "degree_excessive_function", "too / excessive / degree function", "gwo3")
      ]),
      "聽": Object.freeze([
        reviewedAnalysis("聽", "default", "doing", "verb", "verb hear_listen_verb", "hear / listen", "teng1"),
        reviewedAnalysis("聽", "ting3_bound", "func", "bound", "bound_reading", "bound/register-specific reading", "ting3"),
        reviewedAnalysis("聽", "ting3_wait", "doing", "verb", "verb wait_for_inevitable_verb", "wait for an inevitable outcome", "ting3"),
        reviewedAnalysis("聽", "ting1_temporal", "func", "bound", "temporal_prefix", "temporal prefix reading", "ting1")
      ]),
      "返": Object.freeze([
        reviewedAnalysis("返", "default", "doing", "verb", "verb return_affiliated_place_verb", "return / go to one's regular or affiliated place", "faan1"),
        reviewedAnalysis("返", "restitutive", "func", "function", "postverbal_restitutive_function", "back / again / restitutive-repetitive function", "faan1"),
        reviewedAnalysis("返", "faan2_bound", "func", "bound", "written_bound_reading", "written/bound return reading", "faan2")
      ]),
      "嗯": Object.freeze([
        reviewedAnalysis("嗯", "default", "particle", "interjection", "backchannel_interjection", "mm / yes / okay", "m6"),
        reviewedAnalysis("嗯", "m3_backchannel", "particle", "interjection", "backchannel_interjection", "acknowledgement/backchannel variant", "m3"),
        reviewedAnalysis("嗯", "m2_uncertainty", "particle", "interjection", "uncertainty_interjection", "uncertainty / hesitation", "m2")
      ]),
      "對": Object.freeze([
        reviewedAnalysis("對", "default", "func", "preposition", "relational_coverb", "toward / to", "deoi3"),
        reviewedAnalysis("對", "verb", "doing", "verb", "verb face_answer_verb", "face / answer / match", "deoi3"),
        reviewedAnalysis("對", "classifier", "measure_word", "classifier", "pair_classifier", "pair / set classifier", "deoi3"),
        reviewedAnalysis("對", "property", "like", "adjective", "correct_right_property", "correct / right", "deoi3")
      ]),
      "方": Object.freeze([
        reviewedAnalysis("方", "default", "like", "adjective", "shape_property", "square", "fong1"),
        reviewedAnalysis("方", "prescription_noun", "what", "noun", "prescription_method_noun", "prescription / formula / method", "fong1"),
        reviewedAnalysis("方", "side_bound", "func", "bound", "side_party_bound_root", "side / party / direction bound root", "fong1"),
        reviewedAnalysis("方", "formal_adverb", "how", "adverb", "formal_only_then_adverb", "only then", "fong1"),
        reviewedAnalysis("方", "surname", "who", "proper_noun", "surname", "surname Fang", "fong1")
      ]),
      "喂": Object.freeze([
        reviewedAnalysis("喂", "default", "particle", "interjection", "attention_call_interjection", "hello / hey", "wai3"),
        reviewedAnalysis("喂", "feed_verb", "doing", "verb", "verb feed_verb", "feed", "wai3")
      ]),
      "不過": Object.freeze([
        reviewedAnalysis("不過", "default", "func", "conjunction", "adversative_linker", "but / however", "bat1 gwo3"),
        reviewedAnalysis("不過", "restrictive_adverb", "how", "adverb", "restrictive_adverb", "only / merely", "bat1 gwo3")
      ]),
      "樣": Object.freeze([
        reviewedAnalysis("樣", "default", "what", "noun", "appearance_form_noun", "appearance / form", "joeng2"),
        reviewedAnalysis("樣", "classifier", "measure_word", "classifier", "kind_type_classifier", "kind / type classifier", "joeng6")
      ]),
      "嘩": Object.freeze([
        reviewedAnalysis("嘩", "default", "particle", "interjection", "interjection", "wow", "waa3"),
        reviewedAnalysis("嘩", "waa1_bound", "func", "bound", "hubbub_noise_bound_root", "hubbub / noise bound family", "waa1")
      ]),
      "個人": Object.freeze([
        reviewedAnalysis("個人", "noun", "who", "noun", "individual_person_noun", "individual / person", "go3 jan4"),
        reviewedAnalysis("個人", "personal_property", "like", "adjective", "personal_individual_modifier", "personal / individual", "go3 jan4")
      ]),
      "哩個": Object.freeze([
        reviewedAnalysis("哩個", "pronoun", "who", "pronoun", "demonstrative_pronoun", "this / this one; orthographic alias of 呢個", "ni1 go3"),
        reviewedAnalysis("哩個", "determiner", "func", "determiner", "demonstrative_determiner", "this; demonstrative determiner alias of 呢個", "ni1 go3")
      ]),
      "間": Object.freeze([
        reviewedAnalysis("間", "default", "measure_word", "classifier", "classifier_building_shop", "classifier for rooms/buildings", "gaan1"),
        reviewedAnalysis("間", "gaan3_verb", "doing", "verb", "verb divide_partition_verb", "draw/divide/partition", "gaan3"),
        reviewedAnalysis("間", "gaan3_classifier", "measure_word", "classifier", "architectural_bay_classifier", "architectural bay classifier", "gaan3"),
        reviewedAnalysis("間", "gaan3_noun", "what", "noun", "stripe_noun", "stripe", "gaan3")
      ]),
      "啱": Object.freeze([
        reviewedAnalysis("啱", "default", "like", "adjective", "correct_suitable_property", "correct / suitable", "aam1"),
        reviewedAnalysis("啱", "ngaam1_property", "like", "adjective", "correct_suitable_property", "correct / suitable; initial-ng variant", "ngaam1"),
        reviewedAnalysis("啱", "fit_verb", "doing", "verb", "verb fit_suit_verb", "fit / suit", "aam1"),
        reviewedAnalysis("啱", "just_adverb", "when", "adverb", "temporal_exactness_adverb", "just now / exactly", "aam1")
      ]),
      "落": Object.freeze([
        reviewedAnalysis("落", "default", "doing", "verb", "verb downward_motion_verb", "go down / fall", "lok6"),
        reviewedAnalysis("落", "directional", "func", "function", "directional_result_complement", "downward directional/result complement", "lok6")
      ]),
      "埋": Object.freeze([
        reviewedAnalysis("埋", "default", "doing", "verb", "verb bury_hide_verb", "bury / hide", "maai4"),
        reviewedAnalysis("埋", "suffix", "func", "function", "postverbal_inclusion_addition_suffix", "inclusion/addition postverbal suffix", "maai4"),
        reviewedAnalysis("埋", "property", "like", "adjective", "close_intimate_property", "close / intimate", "maai4")
      ]),
      "一定": Object.freeze([
        reviewedAnalysis("一定", "default", "how", "adverb", "modal_epistemic_adverb", "certainly / must", "jat1 ding6"),
        reviewedAnalysis("一定", "property", "like", "adjective", "fixed_given_property", "certain / fixed / given", "jat1 ding6")
      ]),
      "成日": Object.freeze([
        reviewedAnalysis("成日", "default", "when", "adverb", "temporal_frequency_adverb", "all day / always", "seng4 jat6"),
        reviewedAnalysis("成日", "sing4_variant", "when", "adverb", "temporal_frequency_adverb", "all day / always; pronunciation variant", "sing4 jat6")
      ]),
      "點": Object.freeze([
        reviewedAnalysis("點", "default", "func", "pronoun", "wh_manner_pronoun", "how", "dim2"),
        reviewedAnalysis("點", "verb", "doing", "verb", "verb point_count_order_verb", "point / count / order / indicate", "dim2"),
        reviewedAnalysis("點", "noun", "what", "noun", "point_dot_degree_noun", "point / dot / degree", "dim2"),
        reviewedAnalysis("點", "time_classifier", "measure_word", "classifier", "time_oclock_classifier", "o'clock / time classifier", "dim2")
      ]),
      "塔": Object.freeze([
        reviewedAnalysis("塔", "default", "what", "noun", "tower_pagoda_noun", "tower / pagoda", "taap3"),
        reviewedAnalysis("塔", "lock_verb", "doing", "verb", "verb lock_handcuff_verb", "lock / handcuff", "taap3"),
        reviewedAnalysis("塔", "lock_noun", "what", "noun", "lock_noun", "lock", "taap3")
      ]),
      "錢": Object.freeze([
        reviewedAnalysis("錢", "default", "what", "noun", "money_noun", "money / coin", "cin2"),
        reviewedAnalysis("錢", "surname", "who", "proper_noun", "surname", "surname reading", "cin4")
      ]),
      "用": Object.freeze([
        reviewedAnalysis("用", "default", "doing", "verb", "verb use_verb", "use", "jung6"),
        reviewedAnalysis("用", "instrumental", "func", "preposition", "instrumental_coverb", "with / by means of", "jung6")
      ]),
      "過去": Object.freeze([
        reviewedAnalysis("過去", "motion_verb", "doing", "verb", "verb motion_pass_verb", "pass / go over", "gwo3 heoi3"),
        reviewedAnalysis("過去", "past_noun", "when", "noun", "past_time_np", "the past", "gwo3 heoi3"),
        reviewedAnalysis("過去", "past_adverb", "when", "adverb", "past_time_adverb", "formerly / in the past", "gwo3 heoi3")
      ]),
      "叫做": Object.freeze([
        reviewedAnalysis("叫做", "default", "doing", "verb", "verb naming_copular_verb", "be called / be known as", "giu3 zou6"),
        reviewedAnalysis("叫做", "approx_adverb", "how", "adverb", "approximation_evaluation_adverb", "sort of / quite / can be considered", "giu3 zou6")
      ]),
      "右邊": Object.freeze([
        reviewedAnalysis("右邊", "default", "where", "noun", "spatial_localizer locality_np", "right side", "jau6 bin1"),
        reviewedAnalysis("右邊", "bin6_variant", "where", "noun", "spatial_localizer locality_np", "right side; localizer reading variant", "jau6 bin6")
      ]),
      "一樣": Object.freeze([
        reviewedAnalysis("一樣", "property", "like", "adjective", "equative_same_property", "same / alike", "jat1 joeng6"),
        reviewedAnalysis("一樣", "adverb", "how", "adverb", "equative_adverb", "in the same way / likewise", "jat1 joeng6")
      ]),
      "香": Object.freeze([
        reviewedAnalysis("香", "default", "like", "adjective", "fragrance_property", "fragrant / appetizing", "hoeng1"),
        reviewedAnalysis("香", "noun", "what", "noun", "incense_fragrance_noun", "incense / perfume / fragrance", "hoeng1")
      ]),
      "緊": Object.freeze([
        reviewedAnalysis("緊", "default", "like", "adjective", "tight_urgent_property", "tight / urgent", "gan2"),
        reviewedAnalysis("緊", "progressive", "func", "particle", "progressive_durative_marker", "progressive/durative aspect marker", "gan2")
      ]),
      "三": Object.freeze([
        reviewedAnalysis("三", "default", "how", "numeral", "quantity numeral_three", "three", "saam1"),
        reviewedAnalysis("三", "saam3_bound", "func", "bound", "bound_lexicalized_reading", "bound/lexicalized reading only", "saam3")
      ]),
      "張": Object.freeze([
        reviewedAnalysis("張", "default", "measure_word", "classifier", "flat_object_classifier", "classifier for flat objects/furniture", "zoeng1"),
        reviewedAnalysis("張", "surname", "who", "proper_noun", "surname", "surname Cheung/Zhang", "zoeng1")
      ]),
      "由": Object.freeze([
        reviewedAnalysis("由", "default", "func", "preposition", "source_cause_coverb", "from / by / because of", "jau4"),
        reviewedAnalysis("由", "verb_function", "doing", "verb", "verb leave_to_determine_verb", "be up to / leave to / allow to be determined by", "jau4")
      ]),
      "識": Object.freeze([
        reviewedAnalysis("識", "default", "doing", "verb", "verb cognition_acquaintance_verb", "know / recognize / be acquainted with", "sik1"),
        reviewedAnalysis("識", "skill", "func", "function", "skill_ability_function", "know how to / can", "sik1")
      ]),
      "畫": Object.freeze([
        reviewedAnalysis("畫", "default", "doing", "verb", "verb draw_paint_verb", "draw / paint", "waak6"),
        reviewedAnalysis("畫", "noun", "what", "noun", "picture_drawing_noun", "picture / drawing / painting", "waa2")
      ]),
      "左邊": Object.freeze([
        reviewedAnalysis("左邊", "default", "where", "noun", "spatial_localizer locality_np", "left side", "zo2 bin1"),
        reviewedAnalysis("左邊", "bin6_variant", "where", "noun", "spatial_localizer locality_np", "left side; localizer reading variant", "zo2 bin6")
      ]),
      "善": Object.freeze([
        reviewedAnalysis("善", "default", "like", "bound", "bound_formal_evaluative_root", "formal/bound good/virtuous/skilled root", "sin6")
      ]),
      "港": Object.freeze([
        reviewedAnalysis("港", "default", "where", "bound", "bound_place_root", "bound place root", "gong2"),
        reviewedAnalysis("港", "hong_kong_abbrev", "where", "proper_noun", "hong_kong_abbreviation", "Hong Kong abbreviation", "gong2")
      ]),
      "場": Object.freeze([
        reviewedAnalysis("場", "default", "measure_word", "classifier", "event_activity_classifier", "classifier for events/activities", "coeng4"),
        reviewedAnalysis("場", "noun", "where", "noun", "site_venue_field_noun", "site / venue / field", "coeng4")
      ]),
      "台": Object.freeze([
        reviewedAnalysis("台", "default", "what", "noun", "platform_stage_noun", "platform / stage", "toi4"),
        reviewedAnalysis("台", "classifier", "measure_word", "classifier", "machine_performance_classifier", "classifier for machines/equipment/performances", "toi4"),
        reviewedAnalysis("台", "taiwan_bound", "where", "proper_noun", "taiwan_abbreviation", "Taiwan abbreviation", "toi4")
      ]),
      "番": Object.freeze([
        reviewedAnalysis("番", "default", "measure_word", "classifier", "event_iteration_classifier", "a time / round", "faan1"),
        reviewedAnalysis("番", "foreign_bound", "func", "bound", "foreign_non_chinese_bound_root", "foreign / non-Chinese bound morpheme", "faan1")
      ]),
      "唔見": Object.freeze([
        reviewedAnalysis("唔見", "missing_verb", "doing", "verb", "verb missing_lost_verb", "be missing / lost / disappear from view", "m4 gin3")
      ]),
      "正": Object.freeze([
        reviewedAnalysis("正", "default", "like", "adjective", "correct_main_property", "correct / proper / positive / main", "zing3"),
        reviewedAnalysis("正", "zing3_adverb", "how", "adverb", "exactness_adverb", "just / exactly", "zing3"),
        reviewedAnalysis("正", "zeng3_property", "like", "adjective", "excellent_property", "great / excellent", "zeng3"),
        reviewedAnalysis("正", "zing1_bound", "func", "bound", "formal_bound_reading", "formal/bound reading as in 正月", "zing1")
      ]),
      "個位": Object.freeze([
        reviewedAnalysis("個位", "math_noun", "what", "noun", "mathematical_units_place_noun", "units / ones place", "go3 wai2")
      ]),
      "比較": Object.freeze([
        reviewedAnalysis("比較", "default", "doing", "verb", "verb compare_verb", "compare", "bei2 gaau3"),
        reviewedAnalysis("比較", "adverb", "how", "adverb", "comparative_degree_adverb", "comparatively / relatively / rather", "bei2 gaau3"),
        reviewedAnalysis("比較", "noun", "what", "noun", "comparison_noun", "comparison", "bei2 gaau3")
      ]),
      "紙": Object.freeze([
        reviewedAnalysis("紙", "default", "what", "noun", "paper_noun", "paper", "zi2"),
        reviewedAnalysis("紙", "classifier", "measure_word", "classifier", "document_letter_classifier", "formal classifier for documents/letters", "zi2")
      ]),
      "交叉": Object.freeze([
        reviewedAnalysis("交叉", "default", "what", "noun", "intersection_crossing_noun", "intersection / crossing", "gaau1 caa1"),
        reviewedAnalysis("交叉", "verb", "doing", "verb", "verb cross_intersect_verb", "cross / intersect", "gaau1 caa1")
      ]),
      "死": Object.freeze([
        reviewedAnalysis("死", "default", "doing", "verb", "verb die_verb", "die", "sei2"),
        reviewedAnalysis("死", "property", "like", "adjective", "dead_rigid_property", "dead / rigid / inflexible", "sei2"),
        reviewedAnalysis("死", "intensifier", "how", "function", "bound_intensifier", "damned / extremely; intensifying bound function", "sei2")
      ]),
      "年": Object.freeze([
        reviewedAnalysis("年", "default", "when", "noun", "year_noun", "year", "nin4"),
        reviewedAnalysis("年", "classifier", "measure_word", "classifier", "temporal_measure_classifier", "year as temporal measure/classifier", "nin4")
      ]),
      "韻": Object.freeze([
        reviewedAnalysis("韻", "default", "what", "noun", "linguistic_term_np rhyme_noun", "rhyme / phonological rime", "wan5"),
        reviewedAnalysis("韻", "wan6_bound", "func", "bound", "formal_bound_reading", "formal/original bound reading", "wan6")
      ]),
      "驚": Object.freeze([
        reviewedAnalysis("驚", "default", "like", "verb", "cognition_stative_verb", "fear / be afraid / worry", "geng1"),
        reviewedAnalysis("驚", "ging1_bound", "func", "bound", "shock_surprise_bound_root", "shock / surprise formal/bound family", "ging1"),
        reviewedAnalysis("驚", "game_noun", "what", "noun", "slang_game_noun", "game; slang noun", "geng1")
      ]),
      "日": Object.freeze([
        reviewedAnalysis("日", "default", "when", "noun", "day_noun", "day / date", "jat6"),
        reviewedAnalysis("日", "classifier", "measure_word", "classifier", "temporal_measure_classifier", "day as temporal measure/classifier", "jat6"),
        reviewedAnalysis("日", "japan_bound", "where", "bound", "japan_abbreviation", "Japan abbreviation/bound use", "jat6")
      ]),
      "位": Object.freeze([
        reviewedAnalysis("位", "default", "measure_word", "classifier", "polite_person_classifier", "polite classifier for people", "wai2"),
        reviewedAnalysis("位", "noun", "what", "noun", "position_seat_digit_noun", "position / seat / role / digit", "wai2"),
        reviewedAnalysis("位", "wai6_classifier", "measure_word", "classifier", "rank_position_classifier", "rank/position classifier", "wai6")
      ]),
      "廟": Object.freeze([
        reviewedAnalysis("廟", "default", "where", "noun", "place_np temple_noun", "temple / shrine", "miu2"),
        reviewedAnalysis("廟", "miu6_bound", "func", "bound", "nonfinal_bound_reading", "non-final/bound reading", "miu6")
      ]),
      "戀": Object.freeze([
        reviewedAnalysis("戀", "default", "func", "bound", "bound_formal_love_root", "formal/bound love or attachment root", "lyun2"),
        reviewedAnalysis("戀", "lyun5_provenance", "func", "bound", "alternate_reading_provenance", "alternate reading retained as provenance, not a free lexical verb", "lyun5")
      ]),
      "完": Object.freeze([
        reviewedAnalysis("完", "default", "doing", "verb", "verb finish_end_verb", "finish / end", "jyun4"),
        reviewedAnalysis("完", "completive", "func", "function", "postverbal_completive_result", "postverbal completive/result complement", "jyun4")
      ]),
      "都會": Object.freeze([
        reviewedAnalysis("都會", "metropolis_noun", "what", "noun", "metropolis_city_noun", "metropolis / metropolitan area", "dou1 wui6")
      ]),
      "需要": Object.freeze([
        reviewedAnalysis("需要", "default", "doing", "verb", "verb need_require_verb", "need / require", "seoi1 jiu3"),
        reviewedAnalysis("需要", "noun", "what", "noun", "need_requirement_noun", "need / requirement", "seoi1 jiu3")
      ]),
      "成": Object.freeze([
        reviewedAnalysis("成", "default", "doing", "verb", "verb success_completion_verb", "succeed / accomplish / become", "sing4"),
        reviewedAnalysis("成", "seng4_quantifier", "how", "adverb", "whole_amount_quantifier", "whole / all / as much as / nearly", "seng4"),
        reviewedAnalysis("成", "seng4_result_suffix", "func", "function", "completion_result_suffix", "completion/result suffix", "seng4"),
        reviewedAnalysis("成", "tenth_measure", "measure_word", "classifier", "fraction_measure", "one tenth / 10%", "sing4")
      ]),
      "幾多": Object.freeze([
        reviewedAnalysis("幾多", "quantifier", "func", "quantifier", "wh_quantity_quantifier", "how much / how many", "gei2 do1")
      ]),
      "等": Object.freeze([
        reviewedAnalysis("等", "default", "doing", "verb", "verb wait_leave_task_verb", "wait / leave for someone to do", "dang2"),
        reviewedAnalysis("等", "noun", "what", "noun", "rank_class_grade_noun", "rank / class / grade", "dang2"),
        reviewedAnalysis("等", "etc_suffix", "func", "function", "list_continuation_suffix", "etc.; list-continuation suffix", "dang2"),
        reviewedAnalysis("等", "conjunction", "func", "conjunction", "formal_clause_linker", "formal conjunction/linker where independently supported", "dang2")
      ])
    });
    var EXPLICIT_ANALYSES = Object.freeze(Object.fromEntries(
      Object.entries(REVIEWED_ANALYSES).map(([surface, rows]) => [
        surface,
        CANDIDATE_ONLY_SURFACES.has(surface) ? Object.freeze([neutralDefaultAnalysis(surface), ...rows]) : rows
      ])
    ));
    function isNeutralFrequencyFallback(entry) {
      return Boolean(
        entry && entry.label === "lex" && entry.pos === "lexical_item" && entry.syntax === "lexical_item" && typeof entry.note === "string" && entry.note.includes("Exact surface retained as neutral lexical coverage")
      );
    }
    function reviewedNeutralEntry(surface, entry, status) {
      const hasOverride = Object.prototype.hasOwnProperty.call(NEUTRAL_READING_OVERRIDES, surface);
      return {
        ...entry,
        label: "lex",
        pos: "lexical_item",
        syntax: "lexical_item",
        jyutping: hasOverride ? NEUTRAL_READING_OVERRIDES[surface] : entry.jyutping || "",
        senses: [{ gloss: `neutral exact-surface coverage; ${status}` }],
        note: `Reviewed ranks 1–250 adjudication: Exact surface retained as neutral lexical coverage; ${status}.`,
        provenance: { kind: "reviewed_neutral_surface", source: SOURCE, status }
      };
    }
    function reviewedTypedEntry(entry, review, kind) {
      return {
        ...entry,
        label: review.label,
        pos: review.pos,
        syntax: review.syntax,
        jyutping: review.jyutping || "",
        senses: [{ gloss: review.gloss }],
        note: `Reviewed ranks 1–250 lexical adjudication: ${review.gloss}.`,
        provenance: { kind, source: SOURCE, pronunciation_status: review.jyutping ? "reviewed_in_final_adjudication" : "not_promoted" }
      };
    }
    function applyReviewedEntries(entries) {
      if (!Array.isArray(entries)) throw new TypeError("ranks 1–250 reviewed lexical overlay requires an entry array");
      return entries.map(([surface, entry]) => {
        if (Object.prototype.hasOwnProperty.call(PROTECTED_NEUTRAL_SURFACES, surface)) {
          return [surface, reviewedNeutralEntry(surface, entry, PROTECTED_NEUTRAL_SURFACES[surface])];
        }
        if (CANDIDATE_ONLY_SURFACES.has(surface)) {
          return [surface, reviewedNeutralEntry(surface, entry, candidate_only_reason(surface))];
        }
        const correction = CORRECTIONS[surface];
        if (correction) return [surface, reviewedTypedEntry(entry, correction, "reviewed_lexical_correction")];
        const promotion = PROMOTIONS[surface];
        if (!promotion || !isNeutralFrequencyFallback(entry)) return [surface, entry];
        return [surface, reviewedTypedEntry(entry, promotion, "reviewed_lexical_promotion")];
      });
    }
    var CANDIDATE_REASONS = Object.freeze({
      "唔係": "ordinary negative copula remains compositional; preserve reviewed whole-form conjunction candidate",
      "喀": "no single unrestricted default reading; preserve reviewed reading/sense families explicitly",
      "真係": "preserve productive 真 + 係 segmentation while exposing reviewed adverbial expression",
      "咁樣": "preserve compositional segmentation while exposing reviewed manner/demonstrative proform",
      "噉樣": "preserve compositional segmentation while exposing reviewed manner/demonstrative proform",
      "個人": "preserve classifier/noun segmentation while exposing lexical individual/personal analyses",
      "哩個": "preserve phrase segmentation while exposing reviewed 呢個 orthographic-alias analyses",
      "過去": "preserve motion-chain segmentation while exposing reviewed whole-form motion/temporal analyses",
      "一樣": "preserve literal numeral+classifier segmentation while exposing lexical same/alike analyses",
      "唔見": "preserve ordinary negation+見 while exposing lexical missing/lost verb",
      "個位": "preserve component segmentation while exposing lexicalized mathematical units-place noun",
      "都會": "preserve compositional 都 + 會 wui5 while exposing lexical metropolis noun dou1 wui6",
      "幾多": "preserve productive wh-quantity segmentation while exposing reviewed whole-form quantifier"
    });
    function candidate_only_reason(surface) {
      return CANDIDATE_REASONS[surface] || "reviewed candidate-only surface";
    }
    module2.exports = Object.freeze({
      SOURCE,
      PROTECTED_NEUTRAL_SURFACES,
      CANDIDATE_ONLY_SURFACES,
      PROMOTIONS,
      CORRECTIONS,
      EXPLICIT_ANALYSES,
      isNeutralFrequencyFallback,
      applyReviewedEntries
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1-250-candidate-defaults.js
var require_cifu_r1_250_candidate_defaults = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1-250-candidate-defaults.js"(exports2, module2) {
    "use strict";
    var SOURCE = "GitHub issue #792 reviewed ranks 1–250 adjudication; mixed/compositional surfaces retain neutral defaults plus reviewed whole-form candidates";
    var DEFAULT_READINGS = Object.freeze({
      "唔係": "m4 hai6",
      "真係": "zan1 hai6",
      "咁樣": "gam2 joeng6",
      "噉樣": "gam2 joeng6",
      "個人": "go3 jan4",
      "哩個": "ni1 go3",
      "過去": "gwo3 heoi3",
      "一樣": "jat1 joeng6",
      "唔見": "m4 gin3",
      "個位": "go3 wai2",
      "都會": "dou1 wui5",
      "幾多": "gei2 do1"
    });
    function analysis(surface, key, label, pos, jyutping, syntax, gloss) {
      return Object.freeze({
        id: `lex:${surface}:${key}`,
        label,
        pos,
        jyutping,
        syntax,
        senses: Object.freeze([{ gloss }]),
        provenance: Object.freeze({ kind: "reviewed_lexical_analysis", source: SOURCE })
      });
    }
    function neutralDefault(surface, jyutping) {
      return Object.freeze({
        id: `lex:${surface}:default`,
        label: "lex",
        pos: "lexical_item",
        jyutping,
        syntax: "lexical_item",
        senses: Object.freeze([{ gloss: "neutral exact-surface coverage; productive/component segmentation remains the default" }]),
        provenance: Object.freeze({
          kind: "reviewed_candidate_default_pronunciation",
          source: SOURCE,
          scope: "neutral_default_pronunciation_only"
        })
      });
    }
    var CANDIDATE_ANALYSES = Object.freeze({
      "唔係": Object.freeze([
        analysis("唔係", "otherwise_conjunction", "func", "conjunction", "m4 hai6", "otherwise_conjunction", "otherwise / or else")
      ]),
      "真係": Object.freeze([
        analysis("真係", "really_adverb", "how", "adverb", "zan1 hai6", "epistemic_degree_adverb", "really / genuinely / indeed")
      ]),
      "咁樣": Object.freeze([
        analysis("咁樣", "manner_proform", "how", "pronoun", "gam2 joeng6", "manner_demonstrative_proform", "like that / in that way")
      ]),
      "噉樣": Object.freeze([
        analysis("噉樣", "manner_proform", "how", "pronoun", "gam2 joeng6", "manner_demonstrative_proform", "like that / in that way")
      ]),
      "個人": Object.freeze([
        analysis("個人", "noun", "who", "noun", "go3 jan4", "individual_person_noun", "individual / person"),
        analysis("個人", "personal_property", "like", "adjective", "go3 jan4", "personal_individual_modifier", "personal / individual")
      ]),
      "哩個": Object.freeze([
        analysis("哩個", "pronoun", "who", "pronoun", "ni1 go3", "demonstrative_pronoun", "this / this one; orthographic alias of 呢個"),
        analysis("哩個", "determiner", "func", "determiner", "ni1 go3", "demonstrative_determiner", "this; demonstrative determiner alias of 呢個")
      ]),
      "過去": Object.freeze([
        analysis("過去", "motion_verb", "doing", "verb", "gwo3 heoi3", "verb motion_pass_verb", "pass / go over"),
        analysis("過去", "past_noun", "when", "noun", "gwo3 heoi3", "past_time_np", "the past"),
        analysis("過去", "past_adverb", "when", "adverb", "gwo3 heoi3", "past_time_adverb", "formerly / in the past")
      ]),
      "一樣": Object.freeze([
        analysis("一樣", "property", "like", "adjective", "jat1 joeng6", "equative_same_property", "same / alike"),
        analysis("一樣", "adverb", "how", "adverb", "jat1 joeng6", "equative_adverb", "in the same way / likewise")
      ]),
      "唔見": Object.freeze([
        analysis("唔見", "missing_verb", "doing", "verb", "m4 gin3", "verb missing_lost_verb", "be missing / lost / disappear from view")
      ]),
      "個位": Object.freeze([
        analysis("個位", "math_noun", "what", "noun", "go3 wai2", "mathematical_units_place_noun", "units / ones place")
      ]),
      "都會": Object.freeze([
        analysis("都會", "metropolis_noun", "what", "noun", "dou1 wui6", "metropolis_city_noun", "metropolis / metropolitan area")
      ]),
      "幾多": Object.freeze([
        analysis("幾多", "quantifier", "func", "quantifier", "gei2 do1", "wh_quantity_quantifier", "how much / how many")
      ])
    });
    var EXPLICIT_ANALYSES = Object.freeze(Object.fromEntries(
      Object.entries(DEFAULT_READINGS).map(([surface, jyutping]) => [
        surface,
        Object.freeze([neutralDefault(surface, jyutping), ...CANDIDATE_ANALYSES[surface] || []])
      ])
    ));
    function applyCandidateDefaultReadings(entries) {
      if (!Array.isArray(entries)) throw new TypeError("candidate default readings require an entry array");
      return entries.map(([surface, entry]) => {
        const jyutping = DEFAULT_READINGS[surface];
        if (!jyutping) return [surface, entry];
        return [surface, {
          ...entry,
          jyutping,
          provenance: {
            kind: "reviewed_candidate_default_pronunciation",
            source: SOURCE,
            scope: "neutral_default_pronunciation_only",
            prior_provenance: entry.provenance || null
          }
        }];
      });
    }
    module2.exports = Object.freeze({
      SOURCE,
      DEFAULT_READINGS,
      CANDIDATE_ANALYSES,
      EXPLICIT_ANALYSES,
      applyCandidateDefaultReadings
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1-250-runtime-policy.js
var require_cifu_r1_250_runtime_policy = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1-250-runtime-policy.js"(exports2, module2) {
    "use strict";
    var reviewed = require_cifu_r1_250_reviewed();
    var candidateDefaults = require_cifu_r1_250_candidate_defaults();
    var SOURCE = "#792 runtime-default reconciliation after exact-head v0.5.230 validation";
    var PRESERVE_TYPED_DEFAULT_SURFACES = /* @__PURE__ */ new Set(["唔係"]);
    var NEUTRAL_POLYFUNCTIONAL_DEFAULT_SURFACES = /* @__PURE__ */ new Set(["成"]);
    var EFFECTIVE_PROMOTIONS = Object.freeze(Object.fromEntries(
      Object.entries(reviewed.PROMOTIONS).filter(([surface]) => !NEUTRAL_POLYFUNCTIONAL_DEFAULT_SURFACES.has(surface))
    ));
    var EFFECTIVE_CANDIDATE_ONLY_SURFACES = /* @__PURE__ */ new Set([
      ...[...reviewed.CANDIDATE_ONLY_SURFACES].filter((surface) => !PRESERVE_TYPED_DEFAULT_SURFACES.has(surface)),
      ...NEUTRAL_POLYFUNCTIONAL_DEFAULT_SURFACES
    ]);
    function freezeAnalysis(row, overrides = {}) {
      return Object.freeze({
        ...row,
        ...overrides,
        senses: Object.freeze((overrides.senses || row.senses || []).map((sense) => Object.freeze({ ...sense }))),
        provenance: Object.freeze({ ...overrides.provenance || row.provenance || {} })
      });
    }
    function neutralDefault(surface, jyutping = "", note = "") {
      return Object.freeze({
        id: `lex:${surface}:default`,
        label: "lex",
        pos: "lexical_item",
        jyutping,
        syntax: "lexical_item",
        senses: Object.freeze([{ gloss: note || "neutral default retained while reviewed alternatives remain available" }]),
        provenance: Object.freeze({
          kind: "reviewed_runtime_default_policy",
          source: SOURCE,
          status: "neutral_default_preserved"
        })
      });
    }
    var rawSingAnalyses = reviewed.EXPLICIT_ANALYSES["成"] || [];
    var singAlternatives = rawSingAnalyses.map((row) => row.id === "lex:成:default" ? freezeAnalysis(row, {
      id: "lex:成:success_completion_verb",
      provenance: {
        ...row.provenance || {},
        runtime_default_status: "reviewed_alternative_not_global_default"
      }
    }) : row);
    var mHaiOtherwise = candidateDefaults.CANDIDATE_ANALYSES["唔係"] && candidateDefaults.CANDIDATE_ANALYSES["唔係"][0];
    var EXPLICIT_ANALYSIS_OVERRIDES = Object.freeze({
      "唔係": Object.freeze([
        Object.freeze({
          id: "lex:唔係:default",
          label: "func",
          pos: "function",
          jyutping: "m4 hai6",
          syntax: "negated_copula",
          senses: Object.freeze([{ gloss: "not be; ordinary negative-copular default remains compositionally available" }]),
          provenance: Object.freeze({
            kind: "existing_typed_runtime_default_preserved",
            source: "function-words-and-particles.js plus #792 final correction"
          })
        }),
        ...mHaiOtherwise ? [mHaiOtherwise] : []
      ]),
      "成": Object.freeze([
        neutralDefault("成", "seng4", "neutral exact-surface default for productive whole/all composition; preserve 成個 and related component structure while exposing the reviewed sing4/seng4 lexical and functional families"),
        ...singAlternatives
      ])
    });
    function applyRuntimePolicy(entries) {
      if (!Array.isArray(entries)) throw new TypeError("ranks 1–250 runtime policy requires an entry array");
      const baseline = new Map(entries);
      const adjudicated = reviewed.applyReviewedEntries(entries);
      return adjudicated.map(([surface, entry]) => {
        const prior = baseline.get(surface) || entry;
        if (PRESERVE_TYPED_DEFAULT_SURFACES.has(surface) && !reviewed.isNeutralFrequencyFallback(prior)) {
          return [surface, prior];
        }
        if (NEUTRAL_POLYFUNCTIONAL_DEFAULT_SURFACES.has(surface)) {
          return [surface, {
            ...prior,
            label: "lex",
            pos: "lexical_item",
            syntax: "lexical_item",
            jyutping: "seng4",
            note: `${prior.note || "Exact surface retained as neutral lexical coverage."} Reviewed #792 runtime policy: seng4 is retained for the productive whole/all default; the other reviewed sing4/seng4 families remain explicit alternatives rather than replacing this default.`,
            provenance: {
              kind: "reviewed_runtime_default_policy",
              source: SOURCE,
              status: "neutral_polyfunctional_default_preserved",
              pronunciation_status: "reviewed_whole_quantifier_default_seng4",
              prior_provenance: prior.provenance || null
            }
          }];
        }
        return [surface, entry];
      });
    }
    module2.exports = Object.freeze({
      SOURCE,
      PRESERVE_TYPED_DEFAULT_SURFACES,
      NEUTRAL_POLYFUNCTIONAL_DEFAULT_SURFACES,
      EFFECTIVE_PROMOTIONS,
      EFFECTIVE_CANDIDATE_ONLY_SURFACES,
      EXPLICIT_ANALYSIS_OVERRIDES,
      applyRuntimePolicy
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/native-review-corrections.js
var require_native_review_corrections = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/native-review-corrections.js"(exports2, module2) {
    "use strict";
    var { EXPLICIT_ANALYSES: REVIEWED_R1_250_ANALYSES } = require_cifu_r1_250_reviewed();
    var SOURCE = "Guangzhou-native project reviewer correction recorded on issue #849 comments 5289764400 and 5289785425 (2026-08-14)";
    var KAAK_SURFACE = "喀";
    var KAAK_DEFAULT_JYUTPING = "haak1";
    var EXPLICIT_ANALYSES = Object.freeze({
      [KAAK_SURFACE]: Object.freeze(
        REVIEWED_R1_250_ANALYSES[KAAK_SURFACE].map((row, index) => index === 0 ? Object.freeze({
          ...row,
          jyutping: KAAK_DEFAULT_JYUTPING,
          provenance: Object.freeze({
            kind: "native_speaker_pronunciation_correction",
            source: SOURCE,
            scope: "default_runtime_pronunciation_only"
          })
        }) : row)
      )
    });
    function applyNativeReviewCorrections(entries) {
      if (!Array.isArray(entries)) throw new TypeError("native lexical corrections require an entry array");
      return entries.map(([surface, entry]) => {
        if (surface !== KAAK_SURFACE) return [surface, entry];
        return [surface, {
          ...entry,
          jyutping: KAAK_DEFAULT_JYUTPING,
          note: `${entry.note || ""} Native Guangzhou reviewer correction: default pronunciation haak1; attested alternatives retained.`.trim(),
          provenance: {
            kind: "native_speaker_pronunciation_correction",
            source: SOURCE,
            scope: "default_runtime_pronunciation_only",
            prior_provenance: entry.provenance || null
          }
        }];
      });
    }
    module2.exports = Object.freeze({
      SOURCE,
      KAAK_SURFACE,
      KAAK_DEFAULT_JYUTPING,
      EXPLICIT_ANALYSES,
      applyNativeReviewCorrections
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r501-750-reviewed.js
var require_cifu_r501_750_reviewed = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r501-750-reviewed.js"(exports2, module2) {
    "use strict";
    var SOURCE_R1 = "docs/research/ISSUE-866-CIFU-R501-750-LEXICAL-ADJUDICATION-R1.md";
    var SOURCE_R2 = "docs/research/ISSUE-866-CIFU-R501-750-LEXICAL-ADJUDICATION-R2.md";
    var KIND = Object.freeze({
      noun: ["what", "noun", "common_noun"],
      person: ["who", "noun", "person_noun"],
      place: ["where", "noun", "place_noun"],
      verb: ["doing", "verb", "verb"],
      adjective: ["like", "adjective", "stative_predicate"],
      adverb: ["how", "adverb", "adverb"],
      function: ["func", "function", "function"],
      modal: ["func", "modal", "modal"],
      classifier: ["what", "classifier", "classifier"],
      measure: ["what", "measure", "measure_word"],
      determiner: ["what", "determiner", "determiner"],
      pronoun: ["what", "pronoun", "pronoun"],
      bound: ["func", "bound", "bound_morpheme"],
      suffix: ["func", "suffix", "derivational_suffix"],
      formula: ["func", "formula", "discourse_formula"],
      coverb: ["func", "coverb", "coverb"],
      proper: ["what", "proper_noun", "proper_name"],
      localizer: ["where", "localizer", "localizer"],
      temporal: ["when", "noun", "temporal_expression"],
      interjection: ["func", "interjection", "interjection"],
      particle: ["func", "particle", "particle"],
      copula: ["func", "verb", "copula"],
      numeral: ["what", "numeral", "numeral"]
    });
    function lexicalSpec(rank, kind, gloss, jyutping = "", syntax = "") {
      const meta = KIND[kind];
      if (!meta) throw new Error(`unknown lexical kind ${kind}`);
      return Object.freeze({ rank, kind, gloss, jyutping, label: meta[0], pos: meta[1], syntax: syntax || meta[2] });
    }
    function alt(rank, suffix, kind, gloss, jyutping = "", syntax = "") {
      return Object.freeze({ ...lexicalSpec(rank, kind, gloss, jyutping, syntax), suffix });
    }
    var PROMOTIONS = Object.freeze({
      "加": lexicalSpec(507, "verb", "add; plus-related verbal family"),
      "受": lexicalSpec(508, "verb", "receive / accept / suffer"),
      "能夠": lexicalSpec(510, "modal", "ability / possibility modal", "", "ability_modal"),
      "信心": lexicalSpec(515, "noun", "confidence", "", "abstract_noun"),
      "繞": lexicalSpec(520, "verb", "go around / wind / detour", "jiu5", "motion_path_verb"),
      "是": lexicalSpec(525, "copula", "formal/written copula or affirmative verb", "", "formal_copula"),
      "完成": lexicalSpec(532, "verb", "complete / finish", "", "completion_verb"),
      "男仔": lexicalSpec(533, "person", "boy / male person"),
      "任何": lexicalSpec(537, "determiner", "any / whichever", "", "indefinite_determiner"),
      "緊要": lexicalSpec(542, "adjective", "important"),
      "一係": lexicalSpec(558, "function", "or else / either alternative connector", "", "alternative_connector"),
      "提": lexicalSpec(563, "verb", "raise / mention / carry"),
      "養": lexicalSpec(564, "verb", "raise / support / keep"),
      "穿": lexicalSpec(581, "verb", "pierce / pass through / wear"),
      "從": lexicalSpec(584, "coverb", "from / via", "", "source_coverb"),
      "揸": lexicalSpec(589, "verb", "hold / drive"),
      "發覺": lexicalSpec(599, "verb", "discover / notice", "", "cognition_perception_verb"),
      "以": lexicalSpec(604, "coverb", "by / with / according to", "", "formal_instrument_manner_coverb"),
      "未必": lexicalSpec(606, "adverb", "not necessarily", "", "epistemic_adverb"),
      "拍": lexicalSpec(607, "verb", "pat / shoot / photograph"),
      "某": lexicalSpec(619, "determiner", "a certain / some", "", "indefinite_determiner"),
      "射": lexicalSpec(621, "verb", "shoot / emit"),
      "會話": lexicalSpec(622, "noun", "conversation", "wui6 waa2", "conversation_noun"),
      "其中": lexicalSpec(631, "pronoun", "among them / among these", "", "referential_pronoun"),
      "山路": lexicalSpec(637, "place", "mountain road / mountain path", "saan1 lou6"),
      "公共": lexicalSpec(638, "adjective", "public / communal", "", "attributive_modifier"),
      "者": lexicalSpec(643, "suffix", "nominalizer / person-forming suffix", "", "nominalizer_suffix"),
      "賺": lexicalSpec(653, "verb", "earn / profit"),
      "同一": lexicalSpec(655, "determiner", "same / identical", "", "identity_determiner"),
      "接受": lexicalSpec(660, "verb", "accept / receive"),
      "分鐘": lexicalSpec(668, "measure", "minute time-unit measure", "fan1 zung1", "time_measure"),
      "平時": lexicalSpec(671, "temporal", "ordinary times / normally", "", "temporal_expression"),
      "起碼": lexicalSpec(681, "adverb", "at least", "", "minimum_adverb"),
      "反而": lexicalSpec(686, "adverb", "instead / on the contrary", "", "contrastive_adverb"),
      "女人": lexicalSpec(699, "person", "woman"),
      "所有": lexicalSpec(703, "determiner", "all / every member of", "", "universal_determiner"),
      "敢": lexicalSpec(708, "modal", "dare", "", "modal_predicate"),
      "水平線": lexicalSpec(712, "noun", "horizon / horizontal line", "seoi2 ping4 sin3", "geometric_noun"),
      "例如": lexicalSpec(713, "function", "for example", "", "exemplification_marker"),
      "直程": lexicalSpec(716, "adverb", "simply / straight-up", "", "degree_discourse_adverb"),
      "個個": lexicalSpec(717, "pronoun", "every one / each one", "", "distributive_pronoun"),
      "包括": lexicalSpec(732, "verb", "include", "", "inclusion_verb"),
      "始終": lexicalSpec(737, "adverb", "all along / in the end", "", "temporal_stance_adverb"),
      "港人": lexicalSpec(743, "person", "Hong Kong person / people", "gong2 jan4"),
      "公務員": lexicalSpec(748, "person", "civil servant")
    });
    var BLOCKED_ATOMIC_SURFACES = /* @__PURE__ */ new Set([
      "憂",
      "一次",
      "成病",
      "我話",
      "憂店",
      "左上",
      "第二個",
      "點啊",
      "我會",
      "上行",
      "哩樣",
      "右上",
      "落到",
      "繞過",
      "一種",
      "講到",
      "唔夠",
      "我見",
      "貧大",
      "有陣",
      "三個",
      "行過",
      "直落",
      "點呀",
      "直上",
      "畫個",
      "左行",
      "下行",
      "個韻",
      "試過"
    ]);
    var TRANSPARENT_SINGLE_CANDIDATES = /* @__PURE__ */ new Set([
      "細個",
      "打電話",
      "返工",
      "轉彎",
      "最好",
      "打開",
      "一半",
      "食飯",
      "大個"
    ]);
    var MULTI_OR_SPLIT_CANDIDATES = /* @__PURE__ */ new Set([
      "地",
      "領",
      "後尾",
      "當",
      "返來",
      "樂",
      "班",
      "成個",
      "說話",
      "呵",
      "着",
      "包",
      "要求",
      "影響",
      "考慮",
      "攬",
      "水平",
      "教",
      "磅",
      "代表",
      "好過",
      "段",
      "發展",
      "偏",
      "人工",
      "除",
      "剪",
      "袋",
      "慢慢",
      "後",
      "因",
      "計",
      "原本",
      "連",
      "化"
    ]);
    var CANDIDATE_ONLY_SURFACES = /* @__PURE__ */ new Set([...MULTI_OR_SPLIT_CANDIDATES, ...TRANSPARENT_SINGLE_CANDIDATES]);
    var DEFAULT_READING_OVERRIDES = Object.freeze({
      "後尾": "hau6 mei1",
      "返來": "faan1 lei4",
      "樂": "lok6",
      "成個": "seng4 go3",
      "呵": "ho1",
      "攬": "laam2",
      "磅": "bong6",
      "袋": "doi2",
      "慢慢": "maan6 maan2"
    });
    var ALTERNATIVE_SPECS = Object.freeze({
      "地": Object.freeze([
        alt(501, "ground_noun", "noun", "ground / land / place", "dei6"),
        alt(501, "adverb_suffix", "suffix", "written adverb-forming suffix", "dei6", "adverb_forming_suffix"),
        alt(501, "land_bound", "bound", "land / field bound morpheme", "dei2")
      ]),
      "明": Object.freeze([
        alt(502, "understand_verb", "verb", "understand"),
        alt(502, "clear_bright_stative", "adjective", "clear / bright"),
        alt(502, "name_bound", "proper", "proper-name or morphemic family")
      ]),
      "鬼": Object.freeze([
        alt(503, "ghost_noun", "noun", "ghost / person-denoting family"),
        alt(503, "intensifier_function", "adverb", "productive intensifier / negation-related adverbial family"),
        alt(503, "pejorative_bound", "bound", "pejorative adjective/affix family")
      ]),
      "最後": Object.freeze([
        alt(504, "final_stative", "adjective", "final / last"),
        alt(504, "finally_adverb", "adverb", "finally / last in temporal or discourse ordering")
      ]),
      "肯定": Object.freeze([
        alt(509, "certain_stative", "adjective", "certain / definite"),
        alt(509, "confirm_verb", "verb", "confirm / affirm"),
        alt(509, "certainty_adverb", "adverb", "certainly / stance certainty")
      ]),
      "領": Object.freeze([
        alt(513, "collar_noun", "noun", "collar / neck family", "leng5"),
        alt(513, "lead_receive_bound", "bound", "literary lead / receive family", "ling5")
      ]),
      "長": Object.freeze([
        alt(514, "long_stative", "adjective", "long", "coeng4"),
        alt(514, "grow_verb", "verb", "grow", "zoeng2"),
        alt(514, "chief_noun", "noun", "chief / head", "zoeng2")
      ]),
      "後尾": Object.freeze([
        alt(516, "temporal_hau6", "temporal", "later / afterwards", "hau6 mei1"),
        alt(516, "temporal_hau1", "temporal", "later / afterwards reading variant", "hau1 mei1")
      ]),
      "當": Object.freeze([
        alt(519, "temporal_function", "function", "temporal / conditional / adverbial function", "dong1"),
        alt(519, "regard_pawn_verb", "verb", "regard as / pawn", "dong3")
      ]),
      "水": Object.freeze([
        alt(521, "water_money_noun", "noun", "water; money-related noun family"),
        alt(521, "low_quality_stative", "adjective", "low-quality / weak evaluative sense")
      ]),
      "坐": Object.freeze([
        alt(523, "sit_travel_verb", "verb", "sit / travel by / serve a prison sentence", "co5"),
        alt(523, "literary_sit_verb", "verb", "literary/citation sit family", "zo6"),
        alt(523, "placement_verb", "verb", "place / set", "zo6")
      ]),
      "英文": Object.freeze([
        alt(526, "spoken_noun", "noun", "English language, common spoken reading", "jing1 man2"),
        alt(526, "formal_noun", "noun", "English language, formal/reading variant", "jing1 man4")
      ]),
      "真": Object.freeze([
        alt(527, "true_stative", "adjective", "true / real"),
        alt(527, "really_adverb", "adverb", "really / genuinely")
      ]),
      "彎": Object.freeze([
        alt(530, "bend_verb", "verb", "bend"),
        alt(530, "curved_stative", "adjective", "bent / curved")
      ]),
      "今年": Object.freeze([
        alt(531, "this_year_nin4", "temporal", "this year", "gam1 nin4"),
        alt(531, "this_year_nin2", "temporal", "this year, changed-tone reading", "gam1 nin2")
      ]),
      "排": Object.freeze([
        alt(534, "arrange_verb", "verb", "arrange / rank / discharge"),
        alt(534, "row_classifier", "classifier", "classifier/measure for rows"),
        alt(534, "row_platoon_noun", "noun", "row / platoon")
      ]),
      "跟": Object.freeze([
        alt(535, "follow_verb", "verb", "follow"),
        alt(535, "relational_coverb", "coverb", "with / following relational use"),
        alt(535, "conjunctive_function", "function", "conjunctive/linking use")
      ]),
      "返來": Object.freeze([
        alt(538, "motion_lai4", "verb", "come back / return", "faan1 lai4", "motion_verb"),
        alt(538, "motion_lei4", "verb", "come back / return", "faan1 lei4", "motion_verb")
      ]),
      "樂": Object.freeze([
        alt(544, "happy_stative", "adjective", "happy / joyful", "lok6"),
        alt(544, "surname", "proper", "surname", "lok6"),
        alt(544, "music_bound", "bound", "music / musical morpheme", "ngok6"),
        alt(544, "enjoy_bound", "bound", "formal/bound enjoy / appreciate family", "ngaau6")
      ]),
      "形": Object.freeze([
        alt(548, "shape_noun", "noun", "shape / form"),
        alt(548, "form_verb", "verb", "form / appear"),
        alt(548, "form_bound", "bound", "form/shape morpheme")
      ]),
      "直接": Object.freeze([
        alt(549, "direct_stative", "adjective", "direct"),
        alt(549, "directly_adverb", "adverb", "directly")
      ]),
      "班": Object.freeze([
        alt(550, "class_team_noun", "noun", "class / team / group"),
        alt(550, "group_classifier", "classifier", "classifier for groups/classes"),
        alt(550, "group_verb", "verb", "marginal grouping verbal family")
      ]),
      "細個": Object.freeze([alt(553, "young_age_expression", "adjective", "young / in childhood", "", "age_stative_expression")]),
      "經濟": Object.freeze([
        alt(556, "economy_noun", "noun", "economy"),
        alt(556, "economic_stative", "adjective", "economic / economical")
      ]),
      "使": Object.freeze([
        alt(560, "use_cause_verb", "verb", "use / cause / spend"),
        alt(560, "necessity_modal", "modal", "necessity modal", "", "necessity_modal")
      ]),
      "突然": Object.freeze([
        alt(561, "sudden_stative", "adjective", "sudden"),
        alt(561, "suddenly_adverb", "adverb", "suddenly")
      ]),
      "錯": Object.freeze([
        alt(565, "mistake_noun", "noun", "mistake / fault"),
        alt(565, "wrong_stative", "adjective", "wrong / incorrect")
      ]),
      "平": Object.freeze([
        alt(569, "cheap_stative", "adjective", "cheap", "peng4"),
        alt(569, "flat_stative", "adjective", "flat / level / even", "ping4")
      ]),
      "打電話": Object.freeze([alt(570, "telephone_predicate", "verb", "make a telephone call", "", "verb_object_predicate")]),
      "成個": Object.freeze([alt(571, "whole_classifier_phrase", "determiner", "whole / entire", "seng4 go3", "whole_classifier_expression")]),
      "碟": Object.freeze([
        alt(573, "plate_disc_noun", "noun", "plate / disc / album", "dip2"),
        alt(573, "classifier", "classifier", "classifier family", "dip6"),
        alt(573, "bound_reading", "bound", "bound/citation family", "dip6")
      ]),
      "說話": Object.freeze([
        alt(574, "speech_noun", "noun", "speech / words"),
        alt(574, "speak_verb", "verb", "speak / talk")
      ]),
      "自由": Object.freeze([
        alt(578, "freedom_noun", "noun", "freedom"),
        alt(578, "free_stative", "adjective", "free / unrestricted")
      ]),
      "差": Object.freeze([
        alt(583, "difference_stative", "adjective", "difference / shortfall / poor", "caa1"),
        alt(583, "police_dispatch_noun", "noun", "police / dispatch family", "caai1"),
        alt(583, "dispatch_verb", "verb", "dispatch", "caai1")
      ]),
      "舊": Object.freeze([
        alt(585, "old_stative", "adjective", "old / former"),
        alt(585, "old_item_classifier", "classifier", "classifier/measure-like nominalized family")
      ]),
      "呵": Object.freeze([
        alt(586, "breathe_verb", "verb", "breathe / puff", "ho1"),
        alt(586, "interjection", "interjection", "ah / oh / wow", "ho1"),
        alt(586, "confirmation_particle", "particle", "confirmation/question tag", "ho2", "question_tag_particle")
      ]),
      "性": Object.freeze([
        alt(587, "property_noun", "noun", "property / sex / nature"),
        alt(587, "derivational_suffix", "suffix", "derivational -ness/-ity-like morpheme")
      ]),
      "林": Object.freeze([
        alt(588, "forest_noun", "noun", "forest / woods"),
        alt(588, "surname", "proper", "surname")
      ]),
      "似": Object.freeze([
        alt(593, "resemble_verb", "verb", "resemble / seem"),
        alt(593, "similar_stative", "adjective", "similar")
      ]),
      "返工": Object.freeze([alt(595, "go_to_work_predicate", "verb", "go to work", "", "motion_work_predicate")]),
      "着": Object.freeze([
        alt(597, "wear_verb", "verb", "wear / put on", "zoek3"),
        alt(597, "result_suffix", "suffix", "written/result suffix family", "zoek6", "completion_result_suffix"),
        alt(597, "ignite_verb", "verb", "ignite / turn on", "zoek6"),
        alt(597, "correct_stative", "adjective", "correct / right", "zoek6"),
        alt(597, "move_classifier", "classifier", "move / strategy classifier", "zoek6")
      ]),
      "翻版": Object.freeze([
        alt(602, "copy_noun", "noun", "copy / clone / reprint"),
        alt(602, "copy_verb", "verb", "reprint / copy")
      ]),
      "包": Object.freeze([
        alt(605, "wrap_include_verb", "verb", "wrap / include / guarantee"),
        alt(605, "bag_package_noun", "noun", "bag / package"),
        alt(605, "package_classifier", "classifier", "classifier-related use")
      ]),
      "要求": Object.freeze([
        alt(609, "require_verb", "verb", "request / require"),
        alt(609, "requirement_noun", "noun", "request / requirement")
      ]),
      "旅行": Object.freeze([
        alt(610, "travel_verb", "verb", "travel"),
        alt(610, "trip_noun", "noun", "travel / trip")
      ]),
      "影響": Object.freeze([
        alt(611, "affect_verb", "verb", "influence / affect"),
        alt(611, "influence_noun", "noun", "influence / effect")
      ]),
      "關係": Object.freeze([
        alt(614, "relationship_noun", "noun", "relationship"),
        alt(614, "concern_verb", "verb", "concern / have to do with")
      ]),
      "女": Object.freeze([
        alt(616, "daughter_woman_noun", "person", "daughter / girlfriend / young woman", "neoi2"),
        alt(616, "female_modifier", "adjective", "female modifier/distinction word", "neoi5", "attributive_modifier"),
        alt(616, "female_noun", "person", "female / woman", "neoi5")
      ]),
      "考慮": Object.freeze([
        alt(617, "consider_verb", "verb", "consider"),
        alt(617, "consideration_noun", "noun", "consideration")
      ]),
      "男人": Object.freeze([
        alt(618, "man_changed_tone", "person", "man, ordinary lexicalized changed-tone reading", "naam4 jan2"),
        alt(618, "man_base_reading", "person", "man, underlying/base reading relationship", "naam4 jan4")
      ]),
      "轉彎": Object.freeze([alt(625, "turn_verb", "verb", "turn / turn a corner", "zyun3 waan1", "turning_motion_predicate")]),
      "攬": Object.freeze([
        alt(626, "hug_support_verb", "verb", "hug / shoulder / support", "laam2"),
        alt(626, "gather_monopolize_bound", "bound", "gather / monopolize morphemic family", "laam5")
      ]),
      "水平": Object.freeze([
        alt(628, "level_noun", "noun", "level / standard"),
        alt(628, "horizontal_stative", "adjective", "horizontal")
      ]),
      "生活": Object.freeze([
        alt(629, "live_verb", "verb", "live"),
        alt(629, "life_noun", "noun", "life / livelihood")
      ]),
      "早晨": Object.freeze([
        alt(630, "morning_temporal", "temporal", "morning"),
        alt(630, "greeting_formula", "formula", "good morning greeting")
      ]),
      "教": Object.freeze([
        alt(634, "teach_verb", "verb", "teach"),
        alt(634, "religion_noun", "noun", "religion / teaching family"),
        alt(634, "teaching_bound", "bound", "teaching/religion morpheme")
      ]),
      "最好": Object.freeze([alt(635, "best_preference", "adverb", "best / had better", "", "superlative_preference_expression")]),
      "磅": Object.freeze([
        alt(636, "pound_measure", "measure", "pound / weight measure", "bong6"),
        alt(636, "weigh_pay_verb", "verb", "weigh / pay-settle verbal family", "bong6"),
        alt(636, "scales_noun", "noun", "scales / balance device", "bong2")
      ]),
      "代表": Object.freeze([
        alt(640, "represent_verb", "verb", "represent"),
        alt(640, "representative_noun", "person", "representative / delegate")
      ]),
      "好過": Object.freeze([
        alt(641, "better_predicate", "adjective", "better than", "", "comparative_predicate"),
        alt(641, "preference_adverb", "adverb", "preference/discourse use")
      ]),
      "段": Object.freeze([
        alt(644, "segment_noun", "noun", "segment / section"),
        alt(644, "passage_classifier", "classifier", "classifier for passages / stories")
      ]),
      "早": Object.freeze([
        alt(647, "early_stative", "adjective", "early", "zou2"),
        alt(647, "early_adverb", "adverb", "early", "zou2", "temporal_adverb"),
        alt(647, "morning_temporal", "temporal", "morning / early time", "zou2")
      ]),
      "發展": Object.freeze([
        alt(650, "develop_verb", "verb", "develop"),
        alt(650, "development_noun", "noun", "development")
      ]),
      "學": Object.freeze([
        alt(652, "learn_verb", "verb", "learn / study"),
        alt(652, "discipline_bound", "bound", "discipline / school morpheme")
      ]),
      "尾": Object.freeze([
        alt(656, "tail_end_noun", "noun", "tail / end"),
        alt(656, "end_localizer", "localizer", "at the end / later")
      ]),
      "近": Object.freeze([
        alt(658, "near_gan6", "adjective", "near / close", "gan6"),
        alt(658, "near_kan5", "adjective", "near / close reading variant", "kan5")
      ]),
      "最近": Object.freeze([
        alt(663, "recently_adverb", "adverb", "recently"),
        alt(663, "nearest_stative", "adjective", "nearest / most recent")
      ]),
      "打開": Object.freeze([alt(672, "open_verb", "verb", "open / switch on", "", "resultative_open_predicate")]),
      "步": Object.freeze([
        alt(674, "step_noun", "noun", "step / pace", "bou6"),
        alt(674, "step_measure", "measure", "step/pace measure", "bou6"),
        alt(674, "walk_verb", "verb", "walk / stroll / on foot", "bou6")
      ]),
      "狗": Object.freeze([
        alt(676, "dog_noun", "noun", "dog"),
        alt(676, "pejorative_stative", "adjective", "pejorative human-characterization use")
      ]),
      "直行": Object.freeze([
        alt(677, "go_straight_verb", "verb", "go straight", "zik6 haang4", "motion_verb"),
        alt(677, "vertical_column_noun", "noun", "vertical column / line", "zik6 hong4")
      ]),
      "座": Object.freeze([
        alt(680, "seat_location_bound", "bound", "seat/location morpheme", "zo6"),
        alt(680, "building_classifier", "classifier", "classifier for buildings / mountains", "zo6"),
        alt(680, "placement_verb", "verb", "place / set", "zo6"),
        alt(680, "base_stand_noun", "noun", "base / stand / holder", "zo2")
      ]),
      "偏": Object.freeze([
        alt(682, "lean_verb", "verb", "lean / deviate"),
        alt(682, "biased_stative", "adjective", "oblique / biased"),
        alt(682, "contrary_adverb", "adverb", "contrary-to-expectation / stubbornly")
      ]),
      "人工": Object.freeze([
        alt(684, "labor_wages_noun", "noun", "labor / wages / manpower"),
        alt(684, "artificial_stative", "adjective", "artificial / manual")
      ]),
      "分": Object.freeze([
        alt(685, "divide_verb", "verb", "divide / distinguish", "fan1"),
        alt(685, "unit_measure", "measure", "unit / measure", "fan1"),
        alt(685, "share_noun", "noun", "part / share / fraction", "fan6")
      ]),
      "主要": Object.freeze([
        alt(687, "main_stative", "adjective", "main / primary"),
        alt(687, "mainly_adverb", "adverb", "mainly")
      ]),
      "怪": Object.freeze([
        alt(689, "strange_stative", "adjective", "strange"),
        alt(689, "blame_wonder_verb", "verb", "blame / wonder at")
      ]),
      "除": Object.freeze([
        alt(690, "remove_divide_verb", "verb", "remove / divide"),
        alt(690, "except_function", "function", "except / excluding", "", "exclusion_function")
      ]),
      "剪": Object.freeze([
        alt(691, "cut_verb", "verb", "cut with scissors"),
        alt(691, "cutting_tool_noun", "noun", "cutting-tool related noun family")
      ]),
      "袋": Object.freeze([
        alt(693, "bag_noun", "noun", "bag / pocket", "doi2"),
        alt(693, "bag_verb_bound", "verb", "bag / contain verbal family", "doi6"),
        alt(693, "bag_classifier", "classifier", "bag-related classifier family", "doi6")
      ]),
      "閒": Object.freeze([
        alt(695, "idle_stative", "adjective", "idle / free"),
        alt(695, "leisure_noun", "noun", "leisure / free time")
      ]),
      "慢慢": Object.freeze([
        alt(696, "slowly_maan2", "adverb", "slowly", "maan6 maan2"),
        alt(696, "slowly_maan1", "adverb", "slowly, changed-tone variant", "maan6 maan1")
      ]),
      "鐘": Object.freeze([
        alt(697, "clock_bell_noun", "noun", "clock / bell"),
        alt(697, "time_measure", "measure", "time measure / hour-related classifier", "", "time_measure")
      ]),
      "後": Object.freeze([
        alt(705, "back_noun", "noun", "back / rear"),
        alt(705, "behind_localizer", "localizer", "behind"),
        alt(705, "after_function", "function", "after / later", "", "temporal_function")
      ]),
      "家": Object.freeze([
        alt(706, "home_family_noun", "noun", "home / family"),
        alt(706, "institution_classifier", "classifier", "classifier for institutions/businesses"),
        alt(706, "person_suffix", "suffix", "person/profession-forming suffix")
      ]),
      "財政": Object.freeze([
        alt(707, "finance_noun", "noun", "finance / public finances"),
        alt(707, "financial_stative", "adjective", "financial")
      ]),
      "爸爸": Object.freeze([
        alt(714, "father_baa4baa1", "person", "father", "baa4 baa1"),
        alt(714, "father_baa4baa4", "person", "father reading variant", "baa4 baa4"),
        alt(714, "father_written", "person", "written-language reading", "baa1 baa1")
      ]),
      "一半": Object.freeze([alt(722, "half_quantity", "measure", "half / one half", "", "fraction_quantity")]),
      "左右": Object.freeze([
        alt(723, "approximately_adverb", "adverb", "approximately", "zo2 jau2"),
        alt(723, "left_right_noun", "noun", "left and right / sides", "zo2 jau6"),
        alt(723, "influence_verb", "verb", "influence / control", "zo2 jau6")
      ]),
      "食飯": Object.freeze([alt(725, "eat_meal_predicate", "verb", "eat a meal", "", "verb_object_predicate")]),
      "距離": Object.freeze([
        alt(726, "distance_noun", "noun", "distance", "keoi5 lei4"),
        alt(726, "apart_verb", "verb", "be apart / be separated", "keoi5 lei4")
      ]),
      "生": Object.freeze([
        alt(733, "birth_grow_verb", "verb", "give birth / grow", "saang1"),
        alt(733, "raw_alive_stative", "adjective", "raw / alive", "saang1"),
        alt(733, "address_suffix", "suffix", "address/title suffix", "saang1", "address_suffix"),
        alt(733, "written_bound", "bound", "written/bound morpheme family", "sang1")
      ]),
      "因": Object.freeze([
        alt(734, "cause_noun", "noun", "cause / reason"),
        alt(734, "because_function", "function", "because / causal connector", "", "causal_connector")
      ]),
      "考試": Object.freeze([
        alt(735, "exam_noun", "noun", "exam / examination"),
        alt(735, "take_exam_verb", "verb", "take / sit an exam")
      ]),
      "計": Object.freeze([
        alt(739, "calculate_verb", "verb", "calculate / plan", "gai3"),
        alt(739, "plan_idea_noun", "noun", "plan / idea / account-related family", "gai2")
      ]),
      "原本": Object.freeze([
        alt(741, "original_noun_stative", "noun", "original / source text"),
        alt(741, "originally_adverb", "adverb", "originally")
      ]),
      "連": Object.freeze([
        alt(742, "join_verb", "verb", "join / link"),
        alt(742, "even_adverb", "adverb", "even / successively"),
        alt(742, "linking_function", "function", "connector / preposition family", "", "linking_function"),
        alt(742, "proper_family", "proper", "independently supported proper-name family")
      ]),
      "轉": Object.freeze([
        alt(746, "transfer_turn_verb", "verb", "convey / transfer / turn-shift", "zyun2"),
        alt(746, "revolve_turn_verb", "verb", "revolve / turn / repetition family", "zyun3")
      ]),
      "大個": Object.freeze([alt(747, "grown_size_expression", "adjective", "grown / big", "", "age_size_stative_expression")]),
      "分別": Object.freeze([
        alt(749, "difference_noun", "noun", "difference / distinction"),
        alt(749, "separately_adverb", "adverb", "separately / respectively")
      ]),
      "化": Object.freeze([
        alt(750, "transform_verb", "verb", "transform / make into"),
        alt(750, "derivational_suffix", "suffix", "-ize / -ization derivational morpheme")
      ])
    });
    function isNeutralLexicalEntry(entry) {
      return Boolean(entry) && entry.label === "lex" && entry.pos === "lexical_item" && entry.syntax === "lexical_item";
    }
    function applyReviewedEntries(entries) {
      if (!Array.isArray(entries)) throw new TypeError("ranks 501–750 reviewed overlay requires an entry array");
      return entries.map(([surface, entry]) => {
        const promotion = PROMOTIONS[surface];
        if (promotion && isNeutralLexicalEntry(entry)) {
          return [surface, {
            ...entry,
            label: promotion.label,
            pos: promotion.pos,
            syntax: promotion.syntax,
            jyutping: promotion.jyutping || entry.jyutping || "",
            note: promotion.gloss,
            provenance: {
              kind: "reviewed_lexical_promotion",
              source: promotion.rank >= 501 && promotion.rank <= 750 ? SOURCE_R1 : SOURCE_R1,
              rank: promotion.rank,
              pronunciation_status: promotion.jyutping ? "reviewed_in_final_adjudication_or_r2" : "inherited_runtime_candidate_not_independently_promoted",
              prior_provenance: entry.provenance || null
            }
          }];
        }
        if (CANDIDATE_ONLY_SURFACES.has(surface) && isNeutralLexicalEntry(entry) && DEFAULT_READING_OVERRIDES[surface]) {
          return [surface, {
            ...entry,
            jyutping: DEFAULT_READING_OVERRIDES[surface],
            note: `${entry.note || "Exact surface retained as neutral lexical coverage."} Reviewed whole-form analyses remain alternatives; default tokenization stays neutral.`,
            provenance: {
              kind: "reviewed_candidate_default_pronunciation",
              source: SOURCE_R1,
              pronunciation_status: "reviewed_default_reading_without_atomic_pos_promotion",
              prior_provenance: entry.provenance || null
            }
          }];
        }
        return [surface, entry];
      });
    }
    function defaultAnalysis(surface, entry) {
      return Object.freeze({
        id: `lex:${surface}:default`,
        label: entry.label || "neutral",
        pos: entry.pos || "lexical_item",
        jyutping: entry.jyutping || "",
        syntax: entry.syntax || "lexical_candidate",
        senses: Object.freeze([{ gloss: entry.note || "existing runtime default preserved" }]),
        provenance: Object.freeze({
          kind: "existing_runtime_default_preserved",
          source: "v0.5.230 token lexicon before ranks 501–750 alternatives",
          prior_provenance: entry.provenance || null
        })
      });
    }
    function reviewedAlternative(surface, spec, defaultEntry) {
      return Object.freeze({
        id: `lex:${surface}:r${spec.rank}:${spec.suffix}`,
        label: spec.label,
        pos: spec.pos,
        jyutping: spec.jyutping || defaultEntry.jyutping || "",
        syntax: spec.syntax,
        senses: Object.freeze([{ gloss: spec.gloss }]),
        provenance: Object.freeze({
          kind: "reviewed_lexical_analysis",
          source: [544, 636, 677].includes(spec.rank) ? `${SOURCE_R1} + ${SOURCE_R2}` : SOURCE_R1,
          rank: spec.rank,
          pronunciation_status: spec.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted"
        })
      });
    }
    function buildExplicitAnalyses(entries) {
      const defaults = new Map(entries || []);
      const out = /* @__PURE__ */ Object.create(null);
      for (const [surface, specs] of Object.entries(ALTERNATIVE_SPECS)) {
        const entry = defaults.get(surface);
        if (!entry) throw new Error(`ranks 501–750 explicit analyses reference missing runtime surface: ${surface}`);
        const rows = [defaultAnalysis(surface, entry), ...specs.map((spec) => reviewedAlternative(surface, spec, entry))];
        const seen = /* @__PURE__ */ new Set();
        for (const row of rows) {
          if (!row.jyutping) throw new Error(`${row.id}: explicit lexical analysis requires non-empty jyutping`);
          if (seen.has(row.id)) throw new Error(`${row.id}: duplicate ranks 501–750 stable analysis ID`);
          seen.add(row.id);
        }
        out[surface] = Object.freeze(rows);
      }
      return Object.freeze(out);
    }
    module2.exports = Object.freeze({
      SOURCE_R1,
      SOURCE_R2,
      PROMOTIONS,
      BLOCKED_ATOMIC_SURFACES,
      TRANSPARENT_SINGLE_CANDIDATES,
      MULTI_OR_SPLIT_CANDIDATES,
      CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES,
      ALTERNATIVE_SPECS,
      isNeutralLexicalEntry,
      applyReviewedEntries,
      buildExplicitAnalyses
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r501-750-runtime-policy.js
var require_cifu_r501_750_runtime_policy = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r501-750-runtime-policy.js"(exports2, module2) {
    "use strict";
    var reviewed = require_cifu_r501_750_reviewed();
    var SOURCE_R1 = "docs/research/ISSUE-866-CIFU-R501-750-LEXICAL-ADJUDICATION-R1.md";
    var SOURCE_R2 = "docs/research/ISSUE-866-CIFU-R501-750-LEXICAL-ADJUDICATION-R2.md";
    var EXTRA_CANDIDATE_ONLY_SURFACES = /* @__PURE__ */ new Set(["跟"]);
    var EFFECTIVE_CANDIDATE_ONLY_SURFACES = /* @__PURE__ */ new Set([
      ...reviewed.CANDIDATE_ONLY_SURFACES,
      ...EXTRA_CANDIDATE_ONLY_SURFACES
    ]);
    var DEFAULT_READING_OVERRIDES = Object.freeze({
      "着": Object.freeze({ jyutping: "zoek3", source: SOURCE_R1 }),
      "轉彎": Object.freeze({ jyutping: "zyun3 waan1", source: SOURCE_R2 })
    });
    function applyRuntimePolicy(entries) {
      const adjudicated = reviewed.applyReviewedEntries(entries);
      return adjudicated.map(([surface, entry]) => {
        const override = DEFAULT_READING_OVERRIDES[surface];
        if (!override || !reviewed.isNeutralLexicalEntry(entry)) return [surface, entry];
        return [surface, {
          ...entry,
          jyutping: override.jyutping,
          note: `${entry.note || "Exact surface retained as neutral lexical coverage."} Reviewed whole-form alternatives remain explicit while default tokenization stays neutral.`,
          provenance: {
            kind: "reviewed_candidate_default_pronunciation",
            source: override.source,
            pronunciation_status: "reviewed_default_reading_without_atomic_pos_promotion",
            prior_provenance: entry.provenance || null
          }
        }];
      });
    }
    module2.exports = Object.freeze({
      SOURCE_R1,
      SOURCE_R2,
      EXTRA_CANDIDATE_ONLY_SURFACES,
      EFFECTIVE_CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES,
      applyRuntimePolicy
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r751-1000-reviewed.js
var require_cifu_r751_1000_reviewed = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r751-1000-reviewed.js"(exports2, module2) {
    "use strict";
    var SOURCE = "docs/research/ISSUE-872-CIFU-R751-1000-LEXICAL-ADJUDICATION-R1.md";
    var KIND = Object.freeze({
      noun: ["what", "noun", "common_noun"],
      person: ["who", "noun", "person_noun"],
      place: ["where", "noun", "place_noun"],
      verb: ["doing", "verb", "verb"],
      adjective: ["like", "adjective", "stative_predicate"],
      adverb: ["how", "adverb", "adverb"],
      function: ["func", "function", "function"],
      modal: ["func", "modal", "modal"],
      classifier: ["what", "classifier", "classifier"],
      measure: ["what", "measure", "measure_word"],
      determiner: ["what", "determiner", "determiner"],
      pronoun: ["what", "pronoun", "pronoun"],
      bound: ["func", "bound", "bound_morpheme"],
      suffix: ["func", "suffix", "derivational_suffix"],
      formula: ["func", "formula", "discourse_formula"],
      coverb: ["func", "coverb", "coverb"],
      proper: ["what", "proper_noun", "proper_name"],
      localizer: ["where", "localizer", "localizer"],
      temporal: ["when", "noun", "temporal_expression"],
      interjection: ["func", "interjection", "interjection"],
      particle: ["func", "particle", "particle"],
      numeral: ["what", "numeral", "numeral"]
    });
    function lexicalSpec(rank, kind, gloss, jyutping = "", syntax = "") {
      const meta = KIND[kind];
      if (!meta) throw new Error(`unknown lexical kind ${kind}`);
      return Object.freeze({ rank, kind, gloss, jyutping, label: meta[0], pos: meta[1], syntax: syntax || meta[2] });
    }
    function alt(rank, suffix, kind, gloss, jyutping = "", syntax = "") {
      return Object.freeze({ ...lexicalSpec(rank, kind, gloss, jyutping, syntax), suffix });
    }
    var PROMOTIONS = Object.freeze({
      "放棄": lexicalSpec(755, "verb", "give up / abandon"),
      "笨": lexicalSpec(759, "adjective", "stupid / clumsy"),
      "尊重": lexicalSpec(760, "verb", "respect / value"),
      "棚": lexicalSpec(762, "noun", "shed / canopy / shack"),
      "大學": lexicalSpec(767, "place", "university"),
      "害": lexicalSpec(774, "verb", "harm / cause trouble"),
      "當時": lexicalSpec(778, "temporal", "at that time / then"),
      "路線": lexicalSpec(779, "noun", "route / line / itinerary"),
      "餐室": lexicalSpec(781, "place", "dining room"),
      "黐線": lexicalSpec(782, "adjective", "crazy / insane"),
      "表達": lexicalSpec(791, "verb", "express / convey"),
      "甚至": lexicalSpec(792, "adverb", "even / even to the extent that"),
      "秩序": lexicalSpec(795, "noun", "order / sequence / social order"),
      "能力": lexicalSpec(796, "noun", "ability / capability"),
      "廣告": lexicalSpec(798, "noun", "advertisement / commercial"),
      "靠": lexicalSpec(799, "verb", "lean on / rely on / depend on"),
      "永遠": lexicalSpec(804, "adverb", "forever / always"),
      "生意": lexicalSpec(805, "noun", "business / trade"),
      "字典": lexicalSpec(807, "noun", "dictionary"),
      "飛機": lexicalSpec(812, "noun", "airplane"),
      "接近": lexicalSpec(814, "verb", "approach / be near"),
      "短": lexicalSpec(815, "adjective", "short"),
      "機會": lexicalSpec(816, "noun", "opportunity / chance"),
      "制度": lexicalSpec(824, "noun", "system / institution"),
      "面對": lexicalSpec(826, "verb", "face / confront"),
      "案": lexicalSpec(828, "noun", "case / file / incident"),
      "無論": lexicalSpec(830, "function", "regardless / no matter", "", "concessive_subordinator"),
      "圓形": lexicalSpec(831, "noun", "circle / round shape"),
      "鼻": lexicalSpec(834, "noun", "nose"),
      "數碼": lexicalSpec(837, "noun", "digital data / technology / numerals"),
      "誰": lexicalSpec(838, "pronoun", "formal/written who"),
      "大佬": lexicalSpec(841, "person", "boss / big brother / big shot"),
      "女朋友": lexicalSpec(842, "person", "girlfriend / female friend"),
      "小學": lexicalSpec(843, "place", "primary school"),
      "拎": lexicalSpec(851, "verb", "carry / hold / take"),
      "法院": lexicalSpec(852, "place", "court of law"),
      "城市": lexicalSpec(854, "place", "city"),
      "原因": lexicalSpec(857, "noun", "cause / reason"),
      "純": lexicalSpec(859, "adjective", "pure / unmixed"),
      "眼": lexicalSpec(860, "noun", "eye"),
      "發生": lexicalSpec(863, "verb", "happen / occur"),
      "塘": lexicalSpec(865, "place", "pond"),
      "形容": lexicalSpec(872, "verb", "describe"),
      "身": lexicalSpec(873, "noun", "body / person / self"),
      "法官": lexicalSpec(874, "person", "judge"),
      "厘米": lexicalSpec(876, "measure", "centimeter", "", "length_measure"),
      "跌": lexicalSpec(878, "verb", "fall / drop / tumble"),
      "撞": lexicalSpec(882, "verb", "hit / bump / run into"),
      "成為": lexicalSpec(891, "verb", "become"),
      "成績": lexicalSpec(892, "noun", "result / grade / performance record"),
      "明明": lexicalSpec(894, "adverb", "obviously / plainly"),
      "非常": lexicalSpec(897, "adverb", "very / extremely"),
      "送": lexicalSpec(901, "verb", "send / deliver / give / see off"),
      "郵票": lexicalSpec(909, "noun", "postage stamp"),
      "景點": lexicalSpec(923, "place", "scenic spot / tourist attraction"),
      "過程": lexicalSpec(928, "noun", "process / course"),
      "薯": lexicalSpec(932, "noun", "potato / yam"),
      "互聯網": lexicalSpec(935, "noun", "Internet"),
      "公仔": lexicalSpec(936, "noun", "doll / toy"),
      "正常": lexicalSpec(939, "adjective", "normal / regular"),
      "利益": lexicalSpec(944, "noun", "benefit / interest"),
      "法庭": lexicalSpec(948, "place", "court of law"),
      "留意": lexicalSpec(949, "verb", "pay attention / take care"),
      "等如": lexicalSpec(953, "verb", "equal to"),
      "開支": lexicalSpec(956, "noun", "expenditure / expenses"),
      "腳": lexicalSpec(959, "noun", "foot / leg"),
      "旗": lexicalSpec(960, "noun", "flag / banner"),
      "上市": lexicalSpec(964, "verb", "go public / be listed / go on the market"),
      "公約": lexicalSpec(965, "noun", "convention / agreement"),
      "方式": lexicalSpec(967, "noun", "way / method / mode"),
      "日子": lexicalSpec(969, "temporal", "day / date / days of one's life"),
      "奶茶": lexicalSpec(970, "noun", "milk tea"),
      "受害": lexicalSpec(978, "verb", "suffer harm / be victimized"),
      "無聊": lexicalSpec(989, "adjective", "bored / boring / senseless"),
      "頭髮": lexicalSpec(991, "noun", "hair"),
      "類似": lexicalSpec(992, "adjective", "similar / analogous"),
      "鐘頭": lexicalSpec(993, "temporal", "hour"),
      "手指": lexicalSpec(996, "noun", "finger"),
      "在乎": lexicalSpec(1e3, "verb", "care about / mind / depend on")
    });
    var BLOCKED_ATOMIC_SURFACES = /* @__PURE__ */ new Set([
      "我個",
      "我想",
      "幾個",
      "一份",
      "忍依",
      "一年",
      "我知",
      "講下",
      "講得",
      "講過",
      "我要",
      "一間",
      "去邊",
      "知點",
      "包住",
      "問下",
      "一日",
      "右手面",
      "左手",
      "好難",
      "返到",
      "點講",
      "一下",
      "見過",
      "個樣",
      "北行",
      "兩年",
      "幾年",
      "四個"
    ]);
    var RESEARCH_REQUIRED_SURFACES = /* @__PURE__ */ new Set([
      "○",
      "陣時",
      "嬋",
      "喥",
      "經",
      "行行",
      "彭銅",
      "頹餐",
      "沿住",
      "同人"
    ]);
    var TRANSPARENT_SINGLE_CANDIDATES = /* @__PURE__ */ new Set([
      "勇",
      "會堂",
      "之外",
      "堡",
      "右手",
      "外",
      "有錢",
      "上方",
      "好玩",
      "於",
      "嘍",
      "嘿",
      "一陣",
      "最多",
      "開頭",
      "講真",
      "下方",
      "返學",
      "今晚",
      "周圍",
      "國際",
      "之下",
      "教書",
      "透過",
      "幣",
      "體",
      "手邊",
      "起身",
      "掛住",
      "第日",
      "遲啲",
      "左手面",
      "好彩",
      "咸",
      "唔通",
      "停低"
    ]);
    var MULTI_OR_SPLIT_NEUTRAL_CANDIDATES = /* @__PURE__ */ new Set([
      "神",
      "講話",
      "礦",
      "介紹",
      "行為",
      "投資",
      "令",
      "至",
      "美",
      "棵",
      "只不過",
      "型",
      "盆",
      "晚",
      "感受",
      "中文",
      "井",
      "立法",
      "判決",
      "病",
      "搞笑",
      "實",
      "平衡",
      "肥",
      "科",
      "圈",
      "無",
      "絕對",
      "經歷",
      "未來",
      "衰",
      "結果",
      "圓",
      "橢圓",
      "作為",
      "努力",
      "改革",
      "私營化",
      "愛",
      "支持",
      "方便",
      "正話",
      "決定",
      "英",
      "真正",
      "選擇"
    ]);
    var CONSTRUCTIONAL_CANDIDATES = /* @__PURE__ */ new Set(["來講", "令到"]);
    var CANDIDATE_ONLY_SURFACES = /* @__PURE__ */ new Set([
      ...TRANSPARENT_SINGLE_CANDIDATES,
      ...MULTI_OR_SPLIT_NEUTRAL_CANDIDATES,
      ...CONSTRUCTIONAL_CANDIDATES
    ]);
    var DEFAULT_READING_OVERRIDES = Object.freeze({
      "勇": "jung5",
      "外": "ngoi6",
      "嘍": "lo3",
      "嘿": "hei3",
      "礦": "kwong3",
      "行為": "hang4 wai4",
      "棵": "po1",
      "只不過": "zi2 bat1 gwo3",
      "井": "zeng2",
      "圈": "hyun1",
      "衰": "seoi1",
      "愛": "oi3"
    });
    var ALTERNATIVE_SPECS = Object.freeze({
      "比": Object.freeze([
        alt(751, "compare_verb", "verb", "compare / contrast", "bei2"),
        alt(751, "comparative_function", "function", "comparative / ratio marker", "bei2", "comparative_marker")
      ]),
      "車": Object.freeze([
        alt(754, "vehicle_noun", "noun", "vehicle / car", "ce1"),
        alt(754, "vehicle_work_verb", "verb", "vehicle/machine-related verbal family", "ce1"),
        alt(754, "chariot_chess_noun", "noun", "archaic chariot / Chinese-chess rook", "geoi1")
      ]),
      "神": Object.freeze([
        alt(757, "spirit_noun", "noun", "deity / spirit / soul", "san4"),
        alt(757, "figurative_stative", "adjective", "mysterious / extraordinary / figurative property", "san4")
      ]),
      "講話": Object.freeze([
        alt(764, "speak_verb", "verb", "speak / talk / address", "gong2 waa6"),
        alt(764, "speech_event_noun", "noun", "speech / utterance event", "gong2 waa6")
      ]),
      "礦": Object.freeze([
        alt(765, "ore_noun_kwong3", "noun", "mine / mineral / ore", "kwong3"),
        alt(765, "ore_noun_kong3", "noun", "mine / mineral / ore reading variant", "kong3"),
        alt(765, "ore_noun_gwong3", "noun", "older attested mine / mineral reading", "gwong3")
      ]),
      "介紹": Object.freeze([
        alt(769, "introduce_verb", "verb", "introduce", "gaai3 siu6"),
        alt(769, "introduction_noun", "noun", "introduction", "gaai3 siu6")
      ]),
      "行為": Object.freeze([alt(770, "behaviour_noun", "noun", "act / behaviour / conduct", "hang4 wai4")]),
      "投資": Object.freeze([
        alt(772, "invest_verb", "verb", "invest", "tau4 zi1"),
        alt(772, "investment_noun", "noun", "investment", "tau4 zi1")
      ]),
      "架": Object.freeze([
        alt(773, "frame_noun", "noun", "frame / rack / stand", "gaa2"),
        alt(773, "vehicle_classifier", "classifier", "classifier for vehicles / machines", "gaa3"),
        alt(773, "erect_support_verb", "verb", "erect / support", "gaa3")
      ]),
      "魚": Object.freeze([
        alt(776, "fish_noun", "noun", "fish", "jyu2"),
        alt(776, "fish_bound", "bound", "bound/non-final fish morpheme", "jyu4")
      ]),
      "令": Object.freeze([
        alt(785, "cause_order_verb", "verb", "cause / order / command", "ling6"),
        alt(785, "order_honorific_bound", "bound", "order/decree and honorific 令- family", "ling6")
      ]),
      "至": Object.freeze([
        alt(789, "endpoint_function", "coverb", "to / until endpoint relation", "zi3", "endpoint_relation"),
        alt(789, "degree_adverb", "adverb", "most / utmost degree", "zi3", "degree_adverb")
      ]),
      "美": Object.freeze([
        alt(793, "beautiful_stative", "adjective", "beautiful", "mei5"),
        alt(793, "america_bound", "proper", "America / USA bound-name abbreviation", "mei5", "proper_name_bound")
      ]),
      "棵": Object.freeze([alt(797, "plant_classifier", "classifier", "classifier for plants", "po1", "plant_classifier")]),
      "只不過": Object.freeze([
        alt(803, "merely_adverb", "adverb", "merely / only", "zi2 bat1 gwo3"),
        alt(803, "adversative_connector", "function", "but / it is just that", "zi2 bat1 gwo3", "adversative_connector")
      ]),
      "房": Object.freeze([
        alt(808, "room_noun", "noun", "room", "fong2"),
        alt(808, "surname_bound", "proper", "surname / literary-bound family", "fong4", "proper_name_family")
      ]),
      "型": Object.freeze([
        alt(810, "stylish_stative", "adjective", "stylish / cool / handsome", "jing4"),
        alt(810, "type_noun", "noun", "type / model", "jing4")
      ]),
      "盆": Object.freeze([
        alt(811, "basin_noun", "noun", "basin / pot", "pun4"),
        alt(811, "basin_classifier", "classifier", "classifier / measure use", "pun4")
      ]),
      "聲": Object.freeze([
        alt(817, "sound_noun", "noun", "sound / voice / noise", "seng1"),
        alt(817, "sound_classifier", "classifier", "classifier for sounds / utterances", "seng1"),
        alt(817, "literary_sound_bound", "bound", "literary/bound sound family", "sing1")
      ]),
      "出面": Object.freeze([
        alt(820, "outside_localizer", "localizer", "outside", "ceot1 min6", "spatial_localizer"),
        alt(820, "appear_verb", "verb", "appear personally / intervene", "ceot1 min2"),
        alt(820, "overt_stative", "adjective", "overt / obvious", "ceot1 min2")
      ]),
      "來講": Object.freeze([alt(823, "topic_perspective", "function", "in terms of / regarding", "loi4 gong2", "topic_perspective_construction")]),
      "套": Object.freeze([
        alt(827, "set_classifier", "classifier", "classifier for sets / collections", "tou3"),
        alt(827, "cover_noun", "noun", "cover / case / sheath", "tou3"),
        alt(827, "encase_apply_verb", "verb", "encase / apply a set or formula", "tou3")
      ]),
      "晚": Object.freeze([
        alt(829, "evening_temporal", "temporal", "evening / night", "maan5"),
        alt(829, "late_stative", "adjective", "late", "maan5")
      ]),
      "感受": Object.freeze([
        alt(832, "feel_verb", "verb", "feel / experience", "gam2 sau6"),
        alt(832, "feeling_noun", "noun", "feeling / impression / experience", "gam2 sau6")
      ]),
      "中文": Object.freeze([
        alt(844, "chinese_spoken", "noun", "Chinese language, spoken reading", "zung1 man2"),
        alt(844, "chinese_formal", "noun", "Chinese language, formal reading", "zung1 man4")
      ]),
      "井": Object.freeze([
        alt(845, "well_zeng2", "noun", "well", "zeng2"),
        alt(845, "well_zing2", "noun", "well reading variant", "zing2")
      ]),
      "令到": Object.freeze([alt(846, "causative_construction", "function", "cause / make", "ling6 dou3", "causative_construction")]),
      "句": Object.freeze([
        alt(848, "sentence_noun", "noun", "sentence / clause / phrase", "geoi3"),
        alt(848, "utterance_classifier", "classifier", "classifier for utterances / lines", "geoi3")
      ]),
      "立法": Object.freeze([
        alt(849, "legislate_verb", "verb", "legislate", "laap6 faat3"),
        alt(849, "legislation_noun", "noun", "legislation", "laap6 faat3")
      ]),
      "判決": Object.freeze([
        alt(850, "judge_decide_verb", "verb", "judge / decide", "pun3 kyut3"),
        alt(850, "judgment_noun", "noun", "judgment / decision", "pun3 kyut3")
      ]),
      "相": Object.freeze([
        alt(855, "photograph_noun", "noun", "photograph / picture", "soeng2"),
        alt(855, "reciprocal_bound", "bound", "mutual / each-other morpheme", "soeng1")
      ]),
      "飛": Object.freeze([
        alt(856, "fly_dump_verb", "verb", "fly / dump a partner", "fei1"),
        alt(856, "hooligan_noun", "noun", "hoodlum / hooligan", "fei1")
      ]),
      "病": Object.freeze([
        alt(858, "illness_noun", "noun", "illness / disease", "beng6"),
        alt(858, "ill_stative", "adjective", "ill / sick", "beng6"),
        alt(858, "fall_ill_verb", "verb", "fall ill", "beng6")
      ]),
      "搞笑": Object.freeze([
        alt(879, "funny_stative", "adjective", "funny / hilarious", "gaau2 siu3"),
        alt(879, "joke_verb", "verb", "joke / play around / make fun", "gaau2 siu3")
      ]),
      "實": Object.freeze([
        alt(881, "solid_real_stative", "adjective", "solid / firm / real", "sat6"),
        alt(881, "certainly_adverb", "adverb", "certainly / actually", "sat6")
      ]),
      "平衡": Object.freeze([
        alt(889, "balance_noun", "noun", "balance / equilibrium", "ping4 hang4"),
        alt(889, "balance_verb", "verb", "balance", "ping4 hang4"),
        alt(889, "balanced_stative", "adjective", "balanced", "ping4 hang4")
      ]),
      "肥": Object.freeze([
        alt(895, "fat_stative", "adjective", "fat", "fei4"),
        alt(895, "fertilizer_noun", "noun", "fertilizer / manure", "fei4")
      ]),
      "科": Object.freeze([
        alt(899, "field_noun", "noun", "subject / branch / field / section", "fo1"),
        alt(899, "field_classifier", "classifier", "classifier / measure use", "fo1")
      ]),
      "圈": Object.freeze([
        alt(902, "loop_classifier", "classifier", "classifier for loops / laps", "hyun1"),
        alt(902, "circle_noun", "noun", "circle / ring / social circle", "hyun1"),
        alt(902, "circle_verb", "verb", "circle / mark with a circle", "hyun1")
      ]),
      "無": Object.freeze([
        alt(906, "formal_negative", "function", "formal/written negative or bound family", "mou4", "formal_negative"),
        alt(906, "spoken_existential_negator", "function", "spoken existential/possessive negator", "mou5", "existential_negator")
      ]),
      "絕對": Object.freeze([
        alt(907, "absolute_stative", "adjective", "absolute", "zyut6 deoi3"),
        alt(907, "absolutely_adverb", "adverb", "absolutely / certainly", "zyut6 deoi3")
      ]),
      "經歷": Object.freeze([
        alt(910, "experience_noun", "noun", "experience / history", "ging1 lik6"),
        alt(910, "experience_verb", "verb", "experience / go through", "ging1 lik6")
      ]),
      "整": Object.freeze([
        alt(911, "make_fix_verb", "verb", "make / fix / repair / do", "zing2"),
        alt(911, "whole_orderly_bound", "adjective", "whole / complete / orderly property family", "zing2")
      ]),
      "未來": Object.freeze([
        alt(916, "future_temporal", "temporal", "the future", "mei6 loi4"),
        alt(916, "future_modifier", "adjective", "future / upcoming", "mei6 loi4", "relational_modifier")
      ]),
      "衰": Object.freeze([
        alt(920, "bad_weak_stative", "adjective", "bad / annoying / weak", "seoi1"),
        alt(920, "decline_verb", "verb", "decline / wane / fail", "seoi1")
      ]),
      "結果": Object.freeze([
        alt(924, "result_noun", "noun", "result / outcome", "git3 gwo2"),
        alt(924, "result_discourse_adverb", "adverb", "as a result / in the end", "git3 gwo2", "discourse_adverb")
      ]),
      "圓": Object.freeze([
        alt(925, "round_stative", "adjective", "round / circular", "jyun4"),
        alt(925, "circle_unit_noun", "noun", "circle / round unit / yuan", "jyun4")
      ]),
      "橢圓": Object.freeze([
        alt(930, "ellipse_noun", "noun", "ellipse / oval", "to5 jyun4"),
        alt(930, "oval_stative", "adjective", "oval / elliptical", "to5 jyun4")
      ]),
      "小心": Object.freeze([
        alt(934, "careful_stative", "adjective", "careful", "siu2 sam1"),
        alt(934, "take_care_verb", "verb", "take care / be careful", "siu2 sam1")
      ]),
      "作為": Object.freeze([
        alt(943, "serve_as_verb", "verb", "act / serve as"),
        alt(943, "as_coverb", "coverb", "as / in the capacity of", "", "relational_coverb"),
        alt(943, "conduct_noun", "noun", "conduct / deed / accomplishment")
      ]),
      "努力": Object.freeze([
        alt(945, "hardworking_stative", "adjective", "hard-working / effortful", "nou5 lik6"),
        alt(945, "strive_verb", "verb", "strive / work hard", "nou5 lik6"),
        alt(945, "effort_noun", "noun", "effort", "nou5 lik6")
      ]),
      "改革": Object.freeze([
        alt(946, "reform_verb", "verb", "reform", "goi2 gaak3"),
        alt(946, "reform_noun", "noun", "reform", "goi2 gaak3")
      ]),
      "私營化": Object.freeze([
        alt(947, "privatize_verb", "verb", "privatize"),
        alt(947, "privatization_noun", "noun", "privatization")
      ]),
      "亂": Object.freeze([
        alt(957, "chaotic_stative", "adjective", "chaotic / disordered", "lyun6"),
        alt(957, "randomly_adverb", "adverb", "randomly / disorderly", "lyun6")
      ]),
      "愛": Object.freeze([
        alt(958, "love_verb", "verb", "love / be fond of", "oi3"),
        alt(958, "love_noun", "noun", "love / affection", "oi3")
      ]),
      "支持": Object.freeze([
        alt(966, "support_verb", "verb", "support / back", "zi1 ci4"),
        alt(966, "support_noun", "noun", "support / backing", "zi1 ci4")
      ]),
      "方便": Object.freeze([
        alt(968, "convenient_stative", "adjective", "convenient / suitable", "fong1 bin6"),
        alt(968, "facilitate_verb", "verb", "facilitate / help out", "fong1 bin6"),
        alt(968, "convenience_noun", "noun", "convenience", "fong1 bin6")
      ]),
      "正話": Object.freeze([
        alt(973, "just_now_zing3", "temporal", "just now", "zing3 waa6"),
        alt(973, "just_now_zeng3", "temporal", "just now reading variant", "zeng3 waa6")
      ]),
      "決定": Object.freeze([
        alt(976, "decide_verb", "verb", "decide / determine", "kyut3 ding6"),
        alt(976, "decision_noun", "noun", "decision", "kyut3 ding6")
      ]),
      "門": Object.freeze([
        alt(981, "door_noun", "noun", "door / gate / category", "mun4"),
        alt(981, "subject_classifier", "classifier", "classifier for lessons / subjects / large guns", "mun4")
      ]),
      "英": Object.freeze([
        alt(984, "england_bound", "proper", "England / English bound-name root", "jing1", "proper_name_bound"),
        alt(984, "heroic_bound", "adjective", "heroic / outstanding property root", "jing1", "bound_property")
      ]),
      "真正": Object.freeze([
        alt(986, "genuine_stative", "adjective", "real / genuine", "zan1 zing3"),
        alt(986, "truly_adverb", "adverb", "truly / genuinely", "zan1 zing3")
      ]),
      "選擇": Object.freeze([
        alt(990, "choose_verb", "verb", "choose / select", "syun2 zaak6"),
        alt(990, "choice_noun", "noun", "choice / option", "syun2 zaak6")
      ]),
      "勇": Object.freeze([alt(756, "reviewed_whole_form", "adjective", "brave / courageous", "jung5", "stative_or_bound_property")]),
      "會堂": Object.freeze([alt(763, "reviewed_whole_form", "place", "hall / auditorium", "", "place_noun")]),
      "之外": Object.freeze([alt(768, "reviewed_whole_form", "localizer", "outside / beyond / besides", "", "relational_localizer")]),
      "堡": Object.freeze([alt(777, "reviewed_whole_form", "noun", "fort / castle / stronghold", "", "bound_or_count_noun")]),
      "右手": Object.freeze([alt(786, "reviewed_whole_form", "noun", "right hand", "", "body_part_noun")]),
      "外": Object.freeze([alt(787, "reviewed_whole_form", "localizer", "outside / external", "ngoi6", "spatial_localizer")]),
      "有錢": Object.freeze([alt(788, "reviewed_whole_form", "adjective", "rich / wealthy", "", "stative_expression")]),
      "上方": Object.freeze([alt(802, "reviewed_whole_form", "localizer", "above / area above", "", "spatial_localizer")]),
      "好玩": Object.freeze([alt(806, "reviewed_whole_form", "adjective", "fun / enjoyable", "", "stative_expression")]),
      "於": Object.freeze([alt(809, "reviewed_whole_form", "coverb", "in / at / to / from / by / than", "", "formal_relational_coverb")]),
      "嘍": Object.freeze([alt(833, "reviewed_whole_form", "particle", "final particle", "lo3", "final_particle")]),
      "嘿": Object.freeze([alt(835, "reviewed_whole_form", "interjection", "hey / interjection", "hei3", "interjection")]),
      "一陣": Object.freeze([alt(839, "reviewed_whole_form", "temporal", "a while / for a moment", "", "temporal_expression")]),
      "最多": Object.freeze([alt(861, "reviewed_whole_form", "adverb", "at most / the most", "", "quantity_degree_adverb")]),
      "開頭": Object.freeze([alt(864, "reviewed_whole_form", "temporal", "beginning / at first", "", "temporal_expression")]),
      "講真": Object.freeze([alt(868, "reviewed_whole_form", "formula", "frankly / honestly speaking", "", "discourse_formula")]),
      "下方": Object.freeze([alt(869, "reviewed_whole_form", "localizer", "below / underneath", "", "spatial_localizer")]),
      "返學": Object.freeze([alt(875, "reviewed_whole_form", "verb", "go to / attend school", "", "lexicalized_motion_activity")]),
      "今晚": Object.freeze([alt(886, "reviewed_whole_form", "temporal", "tonight", "gam1 maan5", "temporal_expression")]),
      "周圍": Object.freeze([alt(893, "reviewed_whole_form", "localizer", "surroundings / all around", "", "spatial_localizer")]),
      "國際": Object.freeze([alt(903, "reviewed_whole_form", "adjective", "international", "", "relational_modifier")]),
      "之下": Object.freeze([alt(915, "reviewed_whole_form", "localizer", "under / beneath", "", "relational_localizer")]),
      "教書": Object.freeze([alt(921, "reviewed_whole_form", "verb", "teach as an occupation", "", "lexicalized_activity")]),
      "透過": Object.freeze([alt(922, "reviewed_whole_form", "coverb", "through / via", "", "relational_coverb")]),
      "幣": Object.freeze([alt(929, "reviewed_whole_form", "bound", "currency / coin", "bai6", "bound_or_noun")]),
      "體": Object.freeze([alt(933, "reviewed_whole_form", "bound", "body / form / style / system", "tai2", "bound_or_noun")]),
      "手邊": Object.freeze([alt(937, "reviewed_whole_form", "localizer", "at hand / by one's hand", "", "spatial_localizer")]),
      "起身": Object.freeze([alt(950, "reviewed_whole_form", "verb", "get up / rise / leave", "", "lexicalized_motion_verb")]),
      "掛住": Object.freeze([alt(951, "reviewed_whole_form", "verb", "miss / keep thinking about", "gwaa3 zyu6", "idiomatic_cognition_verb")]),
      "第日": Object.freeze([alt(952, "reviewed_whole_form", "temporal", "another day / later / some other day", "dai6 jat6", "temporal_expression")]),
      "遲啲": Object.freeze([alt(962, "reviewed_whole_form", "temporal", "later", "ci4 di1", "temporal_adverbial")]),
      "左手面": Object.freeze([alt(971, "reviewed_whole_form", "localizer", "left-hand side", "zo2 sau2 min6", "spatial_localizer")]),
      "好彩": Object.freeze([alt(974, "reviewed_whole_form", "adjective", "lucky / fortunate", "hou2 coi2", "stative_expression")]),
      "咸": Object.freeze([alt(983, "reviewed_whole_form", "function", "all / every", "haam4", "formal_quantificational_function")]),
      "唔通": Object.freeze([alt(985, "reviewed_whole_form", "adverb", "could it be...?", "m4 tung1", "rhetorical_epistemic_adverb")]),
      "停低": Object.freeze([alt(987, "reviewed_whole_form", "verb", "stop / halt", "ting4 dai1", "lexicalized_resultative_verb")])
    });
    function isNeutralLexicalEntry(entry) {
      return Boolean(entry) && entry.label === "lex" && entry.pos === "lexical_item" && entry.syntax === "lexical_item";
    }
    function applyReviewedEntries(entries) {
      if (!Array.isArray(entries)) throw new TypeError("ranks 751–1000 reviewed overlay requires an entry array");
      return entries.map(([surface, entry]) => {
        const promotion = PROMOTIONS[surface];
        if (promotion && isNeutralLexicalEntry(entry)) {
          return [surface, {
            ...entry,
            label: promotion.label,
            pos: promotion.pos,
            syntax: promotion.syntax,
            jyutping: promotion.jyutping || entry.jyutping || "",
            note: promotion.gloss,
            provenance: {
              kind: "reviewed_lexical_promotion",
              source: SOURCE,
              rank: promotion.rank,
              pronunciation_status: promotion.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted",
              prior_provenance: entry.provenance || null
            }
          }];
        }
        if (CANDIDATE_ONLY_SURFACES.has(surface) && isNeutralLexicalEntry(entry) && DEFAULT_READING_OVERRIDES[surface]) {
          return [surface, {
            ...entry,
            jyutping: DEFAULT_READING_OVERRIDES[surface],
            note: `${entry.note || "Exact surface retained as neutral lexical coverage."} Reviewed whole-form analyses remain alternatives; default tokenization stays neutral.`,
            provenance: {
              kind: "reviewed_candidate_default_pronunciation",
              source: SOURCE,
              pronunciation_status: "reviewed_default_reading_without_atomic_pos_promotion",
              prior_provenance: entry.provenance || null
            }
          }];
        }
        return [surface, entry];
      });
    }
    function defaultAnalysis(surface, entry) {
      return Object.freeze({
        id: `lex:${surface}:default`,
        label: entry.label || "neutral",
        pos: entry.pos || "lexical_item",
        jyutping: entry.jyutping || "",
        syntax: entry.syntax || "lexical_candidate",
        senses: Object.freeze([{ gloss: entry.note || "existing runtime default preserved" }]),
        provenance: Object.freeze({
          kind: "existing_runtime_default_preserved",
          source: "v0.5.231 token lexicon before ranks 751–1000 alternatives",
          prior_provenance: entry.provenance || null
        })
      });
    }
    function reviewedAlternative(surface, spec, defaultEntry) {
      return Object.freeze({
        id: `lex:${surface}:r${spec.rank}:${spec.suffix}`,
        label: spec.label,
        pos: spec.pos,
        jyutping: spec.jyutping || defaultEntry.jyutping || "",
        syntax: spec.syntax,
        senses: Object.freeze([{ gloss: spec.gloss }]),
        provenance: Object.freeze({
          kind: "reviewed_lexical_analysis",
          source: SOURCE,
          rank: spec.rank,
          pronunciation_status: spec.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted"
        })
      });
    }
    function buildExplicitAnalyses(entries) {
      const defaults = new Map(entries || []);
      const out = /* @__PURE__ */ Object.create(null);
      for (const [surface, specs] of Object.entries(ALTERNATIVE_SPECS)) {
        const entry = defaults.get(surface);
        if (!entry) throw new Error(`ranks 751–1000 explicit analyses reference missing runtime surface: ${surface}`);
        const rows = [defaultAnalysis(surface, entry), ...specs.map((spec) => reviewedAlternative(surface, spec, entry))];
        const seen = /* @__PURE__ */ new Set();
        for (const row of rows) {
          if (!row.jyutping) throw new Error(`${row.id}: explicit lexical analysis requires non-empty jyutping`);
          if (seen.has(row.id)) throw new Error(`${row.id}: duplicate ranks 751–1000 stable analysis ID`);
          seen.add(row.id);
        }
        out[surface] = Object.freeze(rows);
      }
      return Object.freeze(out);
    }
    module2.exports = Object.freeze({
      SOURCE,
      PROMOTIONS,
      BLOCKED_ATOMIC_SURFACES,
      RESEARCH_REQUIRED_SURFACES,
      TRANSPARENT_SINGLE_CANDIDATES,
      MULTI_OR_SPLIT_NEUTRAL_CANDIDATES,
      CONSTRUCTIONAL_CANDIDATES,
      CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES,
      ALTERNATIVE_SPECS,
      isNeutralLexicalEntry,
      applyReviewedEntries,
      buildExplicitAnalyses
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r751-1000-runtime-policy.js
var require_cifu_r751_1000_runtime_policy = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r751-1000-runtime-policy.js"(exports2, module2) {
    "use strict";
    var reviewed = require_cifu_r751_1000_reviewed();
    var TYPED_DEFAULT_OVERRIDES = Object.freeze({
      "魚": Object.freeze({
        label: "what",
        pos: "noun",
        syntax: "object_np fish_noun",
        jyutping: "jyu2",
        note: "fish; free-noun reading jyu2. Bound/non-final jyu4 remains a separate reviewed analysis.",
        rank: 776
      }),
      "飛": Object.freeze({
        label: "doing",
        pos: "verb",
        syntax: "verb motion_verb lexical_verb",
        jyutping: "fei1",
        note: "fly; colloquial dump-a-partner sense belongs to the same reviewed verb family. The stale ticket-noun default is superseded.",
        rank: 856
      }),
      "小心": Object.freeze({
        label: "like",
        pos: "adjective",
        syntax: "stative_predicate careful_property",
        jyutping: "siu2 sam1",
        note: "careful; the reviewed verb/imperative take-care use remains a separate analysis.",
        rank: 934
      }),
      "全": Object.freeze({
        label: "what",
        pos: "determiner",
        syntax: "quantifier determiner universal_quantifier",
        jyutping: "cyun4",
        note: "all / whole / entire; universal quantifier/determiner.",
        rank: 941
      })
    });
    function applyTypedDefaultOverrides(entries) {
      return entries.map(([surface, entry]) => {
        const correction = TYPED_DEFAULT_OVERRIDES[surface];
        if (!correction) return [surface, entry];
        return [surface, {
          ...entry,
          label: correction.label,
          pos: correction.pos,
          syntax: correction.syntax,
          jyutping: correction.jyutping,
          note: correction.note,
          provenance: {
            kind: "reviewed_typed_default_correction",
            source: reviewed.SOURCE,
            rank: correction.rank,
            prior_provenance: entry.provenance || null
          }
        }];
      });
    }
    function applyRuntimePolicy(entries) {
      return applyTypedDefaultOverrides(reviewed.applyReviewedEntries(entries));
    }
    module2.exports = Object.freeze({
      SOURCE: reviewed.SOURCE,
      CANDIDATE_ONLY_SURFACES: reviewed.CANDIDATE_ONLY_SURFACES,
      BLOCKED_ATOMIC_SURFACES: reviewed.BLOCKED_ATOMIC_SURFACES,
      RESEARCH_REQUIRED_SURFACES: reviewed.RESEARCH_REQUIRED_SURFACES,
      DEFAULT_READING_OVERRIDES: reviewed.DEFAULT_READING_OVERRIDES,
      TYPED_DEFAULT_OVERRIDES,
      applyRuntimePolicy
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1001-1250-reviewed.js
var require_cifu_r1001_1250_reviewed = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1001-1250-reviewed.js"(exports2, module2) {
    "use strict";
    var SOURCE = "docs/research/ISSUE-878-CIFU-R1001-1250-LEXICAL-ADJUDICATION-R1.md";
    var KIND = Object.freeze({
      noun: ["what", "noun", "common_noun"],
      person: ["who", "noun", "person_noun"],
      place: ["where", "noun", "place_noun"],
      verb: ["doing", "verb", "verb"],
      adjective: ["like", "adjective", "stative_predicate"],
      adverb: ["how", "adverb", "adverb"],
      function: ["func", "function", "function"],
      classifier: ["what", "classifier", "classifier"],
      measure: ["what", "measure", "measure_word"],
      determiner: ["what", "determiner", "determiner"],
      pronoun: ["what", "pronoun", "pronoun"],
      bound: ["func", "bound", "bound_morpheme"],
      suffix: ["func", "suffix", "derivational_suffix"],
      formula: ["func", "formula", "discourse_formula"],
      coverb: ["func", "coverb", "coverb"],
      proper: ["what", "proper_noun", "proper_name"],
      localizer: ["where", "localizer", "localizer"],
      temporal: ["when", "noun", "temporal_expression"],
      interjection: ["func", "interjection", "interjection"],
      particle: ["func", "particle", "particle"],
      numeral: ["what", "numeral", "numeral"]
    });
    function lexicalSpec(rank, kind, gloss, jyutping = "", syntax = "") {
      const meta = KIND[kind];
      if (!meta) throw new Error(`unknown lexical kind ${kind}`);
      return Object.freeze({ rank, kind, gloss, jyutping, label: meta[0], pos: meta[1], syntax: syntax || meta[2] });
    }
    function alt(rank, suffix, kind, gloss, jyutping = "", syntax = "") {
      return Object.freeze({ ...lexicalSpec(rank, kind, gloss, jyutping, syntax), suffix });
    }
    var PROMOTION_ROWS = Object.freeze([
      [1003, "改", "verb", "change / correct"],
      [1004, "其", "pronoun", "formal referential pronoun / determiner"],
      [1005, "拖", "verb", "drag / pull"],
      [1006, "後來", "temporal", "later / afterwards"],
      [1011, "婆", "person", "grandmother / matron kinship noun"],
      [1012, "得意", "adjective", "cute / complacent"],
      [1015, "曾經", "adverb", "once / ever"],
      [1016, "答", "verb", "answer / reply"],
      [1017, "搬", "verb", "move / transport"],
      [1020, "態度", "noun", "attitude"],
      [1021, "慘", "adjective", "miserable / serious"],
      [1024, "緊張", "adjective", "tense / nervous"],
      [1025, "價值", "noun", "value / worth"],
      [1035, "八月", "temporal", "August"],
      [1037, "大部份", "determiner", "the greater part / majority", "", "quantity_expression"],
      [1039, "尤其", "adverb", "especially / particularly"],
      [1043, "而且", "function", "moreover / in addition", "", "additive_connector"],
      [1044, "抑或", "function", "or / alternatively", "", "alternative_connector"],
      [1045, "到時", "temporal", "at that time"],
      [1051, "凍", "adjective", "cold"],
      [1052, "馬", "noun", "horse"],
      [1057, "暑假", "noun", "summer vacation"],
      [1058, "發現", "verb", "discover / find"],
      [1059, "睇法", "noun", "viewpoint / opinion"],
      [1060, "程度", "noun", "degree / extent"],
      [1063, "嘥", "verb", "waste"],
      [1064, "澳洲", "proper", "Australia"],
      [1067, "聽日", "temporal", "tomorrow"],
      [1068, "聽講", "verb", "hear that / be told", "", "evidential_verb"],
      [1070, "力", "noun", "strength / force"],
      [1071, "冇事", "adjective", "be all right / nothing is wrong", "", "stative_expression"],
      [1072, "功課", "noun", "homework / assignment"],
      [1075, "本來", "adverb", "originally / at first"],
      [1076, "生日", "noun", "birthday"],
      [1079, "年紀", "noun", "age"],
      [1080, "百", "numeral", "hundred"],
      [1081, "男", "adjective", "male", "", "relational_modifier"],
      [1082, "事實", "noun", "fact"],
      [1085, "於是", "function", "thereupon / as a result", "", "result_connector"],
      [1099, "舊年", "temporal", "last year"],
      [1101, "醫生", "person", "doctor"],
      [1103, "大約", "adverb", "approximately / about", "", "approximation_adverb"],
      [1104, "分開", "verb", "separate / part"],
      [1110, "形狀", "noun", "shape / form"],
      [1116, "底下", "localizer", "below / underneath"],
      [1121, "捱", "verb", "endure / suffer"],
      [1122, "終於", "adverb", "finally / eventually"],
      [1125, "幾耐", "pronoun", "how long", "", "wh_duration_expression"],
      [1129, "資產", "noun", "assets / property"],
      [1132, "維持", "verb", "maintain / preserve"],
      [1134, "機構", "noun", "institution / organization"],
      [1135, "離開", "verb", "leave / depart"],
      [1138, "七月", "temporal", "July"],
      [1140, "仍然", "adverb", "still / yet"],
      [1143, "以後", "temporal", "after / later"],
      [1149, "地產", "noun", "real estate"],
      [1150, "年代", "temporal", "era / decade"],
      [1153, "身邊", "localizer", "at one's side"],
      [1154, "例子", "noun", "example"],
      [1155, "咖啡", "noun", "coffee"],
      [1156, "怕", "verb", "fear / be afraid"],
      [1159, "盈利", "noun", "profit / gain"],
      [1160, "值得", "adjective", "be worth / deserve", "", "worth_predicate"],
      [1169, "結婚", "verb", "marry / get married"],
      [1177, "對唔住", "formula", "sorry / apology"],
      [1180, "賠", "verb", "compensate / indemnify"],
      [1181, "擔心", "verb", "worry / be anxious"],
      [1182, "整個", "determiner", "whole / entire", "", "whole_determiner"],
      [1188, "只要", "function", "so long as / provided that", "", "conditional_subordinator"],
      [1194, "地鐵", "noun", "metro / subway"],
      [1195, "好話", "noun", "praise / good words; related discourse sense"],
      [1197, "形式", "noun", "form / shape"],
      [1199, "到底", "adverb", "after all / in the end", "", "discourse_adverb"],
      [1201, "的", "particle", "written possessive / nominalizing particle", "dik1", "written_possessive_nominalizer"],
      [1205, "媽媽", "person", "mother / mama"],
      [1210, "電視機", "noun", "television set"],
      [1212, "歌", "noun", "song"],
      [1213, "銀行", "place", "bank"],
      [1214, "價錢", "noun", "price"],
      [1215, "噏", "verb", "babble / mutter"],
      [1217, "變成", "verb", "become / turn into"],
      [1225, "市民", "person", "city resident"],
      [1228, "有人", "pronoun", "someone / there is someone", "", "referential_existential_expression"],
      [1232, "性格", "noun", "personality / temperament"],
      [1233, "放假", "verb", "take / have a holiday"],
      [1234, "直頭", "adverb", "simply / straight-up"],
      [1237, "是但", "adverb", "whatever / casually", "", "indefinite_choice_adverb"],
      [1239, "英國", "proper", "United Kingdom"],
      [1245, "旁邊", "localizer", "beside / at the side"],
      [1246, "留", "verb", "remain / keep / retain"],
      [1247, "記住", "verb", "remember"],
      [1248, "追", "verb", "chase / pursue"],
      [1250, "得罪", "verb", "offend"]
    ]);
    var PROMOTIONS = Object.freeze(Object.fromEntries(
      PROMOTION_ROWS.map(([rank, surface, kind, gloss, jyutping = "", syntax = ""]) => [surface, lexicalSpec(rank, kind, gloss, jyutping, syntax)])
    ));
    var SOURCE_ONLY_SURFACES = /* @__PURE__ */ new Set([
      "如",
      "添",
      "欣",
      "帳",
      "陶",
      "尚",
      "直到",
      "空白",
      "要點",
      "談",
      "聯",
      "長方形",
      "航",
      "預算案",
      "墓",
      "一方面",
      "乎",
      "北面",
      "仲要",
      "健",
      "煙海",
      "圖形",
      "闆",
      "叢",
      "民",
      "例",
      "頹",
      "三角形",
      "分手",
      "南方",
      "要好",
      "原則"
    ]);
    var INDEPENDENT_ZERO_HIT_SURFACES = /* @__PURE__ */ new Set(["兆", "整個", "好話"]);
    var BLOCKED_ATOMIC_SURFACES = /* @__PURE__ */ new Set([
      "有條",
      "條路",
      "講呀",
      "點呢",
      "一段",
      "一張",
      "下個",
      "皮池",
      "南行",
      "重係",
      "講啦",
      "點算",
      "左下角",
      "左畫",
      "白紙",
      "兩間",
      "冤墓",
      "畫條",
      "講返",
      "九點",
      "打個",
      "行去",
      "我想問",
      "兩個韻",
      "受山",
      "東行",
      "兜返",
      "三年",
      "手面",
      "同個",
      "即話",
      "我同",
      "唔肯",
      "做咩",
      "講乜",
      "右下角",
      "正下方",
      "先算",
      "你點",
      "張紙",
      "畫畫",
      "想講",
      "一個韻",
      "右下",
      "右手邊",
      "返上",
      "俾人",
      "埋個",
      "做過"
    ]);
    var MULTI_SPECS = Object.freeze({
      "研究": Object.freeze([alt(1007, "research_noun", "noun", "research / study"), alt(1007, "research_verb", "verb", "study / research")]),
      "限制": Object.freeze([alt(1009, "restriction_noun", "noun", "restriction / limit"), alt(1009, "restrict_verb", "verb", "restrict / limit")]),
      "值": Object.freeze([alt(1010, "value_noun", "noun", "value"), alt(1010, "worth_predicate", "adjective", "be worth / have value", "", "worth_predicate")]),
      "經驗": Object.freeze([alt(1019, "experience_noun", "noun", "experience"), alt(1019, "experience_verb", "verb", "experience")]),
      "盡量": Object.freeze([alt(1022, "as_much_as_possible", "adverb", "as much as possible"), alt(1022, "full_extent_verb_family", "verb", "to the full / exhaust an amount", "", "lexical_verb_family")]),
      "影": Object.freeze([alt(1027, "image_noun", "noun", "image / shadow / photograph"), alt(1027, "photograph_verb", "verb", "photograph / film")]),
      "餐": Object.freeze([alt(1029, "meal_noun", "noun", "meal"), alt(1029, "meal_classifier", "classifier", "classifier for meals")]),
      "一般": Object.freeze([alt(1033, "ordinary_stative", "adjective", "ordinary / general"), alt(1033, "generally_adverb", "adverb", "generally / in general")]),
      "女性": Object.freeze([alt(1038, "female_noun", "noun", "female / woman"), alt(1038, "female_modifier", "adjective", "female", "", "relational_modifier")]),
      "外圍": Object.freeze([alt(1040, "periphery_noun", "noun", "periphery / surrounding area"), alt(1040, "surrounding_localizer", "localizer", "around / surrounding")]),
      "刑事": Object.freeze([alt(1042, "criminal_matter_noun", "noun", "criminal matter"), alt(1042, "penal_modifier", "adjective", "criminal / penal", "", "relational_modifier")]),
      "常": Object.freeze([alt(1054, "common_stative", "adjective", "common / constant"), alt(1054, "often_adverb", "adverb", "often / frequently")]),
      "麻煩": Object.freeze([alt(1056, "troublesome_stative", "adjective", "troublesome / inconvenient"), alt(1056, "bother_verb", "verb", "bother / trouble")]),
      "塊": Object.freeze([alt(1061, "piece_noun", "noun", "piece / lump"), alt(1061, "piece_classifier", "classifier", "classifier for pieces")]),
      "尖": Object.freeze([alt(1078, "point_noun", "noun", "point / tip"), alt(1078, "sharp_stative", "adjective", "sharp / pointed")]),
      "版": Object.freeze([alt(1086, "edition_noun", "noun", "edition / page / version"), alt(1086, "edition_classifier", "classifier", "classifier for editions / pages")]),
      "指": Object.freeze([alt(1089, "finger_noun", "noun", "finger"), alt(1089, "point_verb", "verb", "point / indicate / refer")]),
      "負責": Object.freeze([alt(1091, "take_responsibility_verb", "verb", "take responsibility / be in charge"), alt(1091, "responsible_stative", "adjective", "responsible")]),
      "電子": Object.freeze([alt(1094, "electron_noun", "noun", "electron"), alt(1094, "electronic_modifier", "adjective", "electronic", "", "relational_modifier")]),
      "標": Object.freeze([alt(1095, "mark_bid_noun", "noun", "mark / sign / bid"), alt(1095, "mark_bid_verb", "verb", "mark / bid")]),
      "升": Object.freeze([alt(1105, "rise_verb", "verb", "rise / raise"), alt(1105, "litre_measure", "measure", "litre", "", "volume_measure")]),
      "白色": Object.freeze([alt(1108, "white_noun", "noun", "white color"), alt(1108, "white_stative", "adjective", "white")]),
      "改變": Object.freeze([alt(1112, "change_noun", "noun", "change"), alt(1112, "change_verb", "verb", "change / alter")]),
      "私人": Object.freeze([alt(1113, "private_person_noun", "person", "private citizen"), alt(1113, "private_modifier", "adjective", "private", "", "relational_modifier")]),
      "單": Object.freeze([alt(1124, "form_bill_noun", "noun", "form / bill / list"), alt(1124, "sheet_classifier", "classifier", "classifier for forms / bills"), alt(1124, "single_stative", "adjective", "single / sole")]),
      "準備": Object.freeze([alt(1127, "preparation_noun", "noun", "preparation"), alt(1127, "prepare_verb", "verb", "prepare / get ready")]),
      "抵": Object.freeze([alt(1157, "worth_cheap_stative", "adjective", "worthwhile / cheap"), alt(1157, "resist_support_verb", "verb", "resist / support / offset")]),
      "夠": Object.freeze([alt(1165, "reach_enough_verb", "verb", "reach / be enough"), alt(1165, "sufficient_stative", "adjective", "sufficient / enough"), alt(1165, "enough_adverb", "adverb", "enough / sufficiently")]),
      "控制": Object.freeze([alt(1166, "control_noun", "noun", "control"), alt(1166, "control_verb", "verb", "control / contain")]),
      "報": Object.freeze([alt(1168, "report_newspaper_noun", "noun", "report / newspaper / recompense"), alt(1168, "report_verb", "verb", "report / announce / recompense")]),
      "順": Object.freeze([alt(1170, "smooth_favourable_stative", "adjective", "smooth / favourable"), alt(1170, "follow_obey_verb", "verb", "follow / obey")]),
      "聖經": Object.freeze([alt(1172, "bible_proper", "proper", "the Bible"), alt(1172, "classics_noun", "noun", "scripture / classics")]),
      "補習": Object.freeze([alt(1173, "extra_lessons_noun", "noun", "extra lessons / tutoring"), alt(1173, "tutor_study_verb", "verb", "take or give extra lessons")]),
      "解釋": Object.freeze([alt(1174, "explanation_noun", "noun", "explanation"), alt(1174, "explain_verb", "verb", "explain / interpret")]),
      "實在": Object.freeze([alt(1176, "real_concrete_stative", "adjective", "real / concrete"), alt(1176, "really_adverb", "adverb", "really / honestly")]),
      "精神": Object.freeze([alt(1178, "spirit_mind_noun", "noun", "spirit / mind"), alt(1178, "energetic_mental_stative", "adjective", "energetic / mental")]),
      "暫時": Object.freeze([alt(1179, "temporary_stative", "adjective", "temporary"), alt(1179, "for_now_adverb", "adverb", "for now / temporarily")]),
      "競爭": Object.freeze([alt(1186, "competition_noun", "noun", "competition"), alt(1186, "compete_verb", "verb", "compete")]),
      "公": Object.freeze([alt(1187, "public_fair_stative", "adjective", "public / fair"), alt(1187, "title_male_noun", "noun", "title / male-animal / kinship nominal family")]),
      "失業": Object.freeze([alt(1190, "unemployment_noun", "noun", "unemployment"), alt(1190, "unemployed_predicate", "adjective", "be unemployed", "", "stative_predicate")]),
      "保障": Object.freeze([alt(1202, "safeguard_noun", "noun", "safeguard / guarantee"), alt(1202, "ensure_verb", "verb", "ensure / safeguard")]),
      "溝通": Object.freeze([alt(1207, "communication_noun", "noun", "communication"), alt(1207, "communicate_verb", "verb", "communicate")]),
      "道": Object.freeze([alt(1208, "way_principle_noun", "noun", "way / principle / path"), alt(1208, "utterance_classifier", "classifier", "classifier / measure use"), alt(1208, "say_literary_verb", "verb", "say / speak, literary")]),
      "預備": Object.freeze([alt(1211, "preparation_noun", "noun", "preparation"), alt(1211, "prepare_verb", "verb", "prepare / make ready"), alt(1211, "preparatory_modifier", "adjective", "preparatory", "", "relational_modifier")]),
      "反應": Object.freeze([alt(1221, "reaction_noun", "noun", "reaction / response"), alt(1221, "react_verb", "verb", "react / respond")]),
      "支": Object.freeze([alt(1222, "branch_support_noun", "noun", "branch / support"), alt(1222, "rod_classifier", "classifier", "classifier for rods / songs / units"), alt(1222, "support_verb", "verb", "support / sustain")]),
      "回歸": Object.freeze([alt(1226, "return_noun", "noun", "return / reversion"), alt(1226, "return_verb", "verb", "return / revert")]),
      "成功": Object.freeze([alt(1227, "success_noun", "noun", "success"), alt(1227, "successful_stative", "adjective", "successful"), alt(1227, "succeed_verb", "verb", "succeed")]),
      "自然": Object.freeze([alt(1229, "nature_noun", "noun", "nature"), alt(1229, "natural_stative", "adjective", "natural"), alt(1229, "naturally_adverb", "adverb", "naturally")]),
      "男性": Object.freeze([alt(1231, "male_noun", "noun", "male"), alt(1231, "male_modifier", "adjective", "male", "", "relational_modifier")]),
      "相當": Object.freeze([alt(1238, "equivalent_stative", "adjective", "equivalent / appropriate"), alt(1238, "fairly_adverb", "adverb", "fairly / considerably")])
    });
    var READING_SPECS = Object.freeze({
      "衫": Object.freeze([alt(1008, "clothing_saam1", "noun", "clothing / shirt", "saam1")]),
      "會考": Object.freeze([alt(1018, "exam_wui6haau2", "noun", "public examination", "wui6 haau2")]),
      "網絡": Object.freeze([alt(1023, "network_lok6", "noun", "network", "mong5 lok6"), alt(1023, "network_lok3", "noun", "network reading variant", "mong5 lok3")]),
      "噉樣樣": Object.freeze([alt(1026, "demonstrative_discourse", "pronoun", "like this / in this way", "gam2 joeng2 joeng2", "demonstrative_discourse_expression")]),
      "橋": Object.freeze([alt(1028, "bridge_kiu4", "noun", "bridge", "kiu4"), alt(1028, "idea_kiu2", "noun", "idea / plan", "kiu2")]),
      "拉": Object.freeze([alt(1046, "pull_laai1", "verb", "pull / drag", "laai1")]),
      "咯": Object.freeze([alt(1049, "particle_lok3", "particle", "final particle", "lok3", "final_particle"), alt(1049, "particle_lo3", "particle", "final particle reading variant", "lo3", "final_particle")]),
      "試": Object.freeze([alt(1062, "try_si3", "verb", "try / test", "si3"), alt(1062, "test_si5", "noun", "test / examination", "si5")]),
      "入邊": Object.freeze([alt(1069, "inside_bin6", "localizer", "inside", "jap6 bin6"), alt(1069, "inside_bin1", "localizer", "inside reading variant", "jap6 bin1")]),
      "覆": Object.freeze([alt(1100, "reply_fuk1", "verb", "reply / cover / overturn family", "fuk1")]),
      "廿": Object.freeze([alt(1106, "twenty_jaa6", "numeral", "twenty", "jaa6")]),
      "處理": Object.freeze([alt(1123, "handle_cyu5lei5", "verb", "handle / process / deal with", "cyu5 lei5")]),
      "新聞": Object.freeze([alt(1126, "news_man4", "noun", "news", "san1 man4"), alt(1126, "news_man2", "noun", "news reading variant", "san1 man2")]),
      "解": Object.freeze([alt(1128, "solve_gaai2", "verb", "solve / explain / untie", "gaai2"), alt(1128, "separate_haai6_family", "bound", "separate haai6 lexical reading family", "haai6", "separate_reading_family")]),
      "舖": Object.freeze([alt(1133, "shop_pou3", "noun", "shop / store", "pou3"), alt(1133, "shop_changed_tone_pou2", "noun", "shop / store changed-tone use", "pou2")]),
      "聽眾": Object.freeze([alt(1136, "audience_ting3", "noun", "audience / listeners", "ting3 zung3")]),
      "兆": Object.freeze([alt(1146, "omen_noun_siu6", "noun", "omen / sign / large-number family", "siu6"), alt(1146, "portend_verb_siu6", "verb", "portend / indicate", "siu6"), alt(1146, "large_number_siu6", "numeral", "large-number value", "siu6")]),
      "地下": Object.freeze([alt(1148, "underground_dei6haa6", "adjective", "underground / subterranean", "dei6 haa6", "relational_modifier"), alt(1148, "ground_dei6haa2", "noun", "ground / floor", "dei6 haa2")]),
      "爭": Object.freeze([alt(1158, "strive_zaang1", "verb", "strive / compete / argue", "zaang1"), alt(1158, "bound_zang1", "bound", "bound compound reading", "zang1")]),
      "哩度": Object.freeze([alt(1161, "here_ni1", "pronoun", "here", "ni1 dou6", "locative_pronoun"), alt(1161, "here_nei1", "pronoun", "here reading variant", "nei1 dou6", "locative_pronoun")]),
      "被": Object.freeze([alt(1167, "passive_bei6", "coverb", "passive marker / by", "bei6", "passive_coverb"), alt(1167, "quilt_pei5", "noun", "quilt / blanket", "pei5")]),
      "抹": Object.freeze([alt(1200, "wipe_maat3", "verb", "wipe", "maat3"), alt(1200, "erase_mut3", "verb", "wipe / erase reading family", "mut3")]),
      "零": Object.freeze([alt(1209, "zero_ling4", "numeral", "zero", "ling4"), alt(1209, "remainder_leng4", "suffix", "approximate-remainder suffix", "leng4", "approximate_remainder_suffix"), alt(1209, "remainder_leng2", "suffix", "approximate-remainder suffix reading", "leng2", "approximate_remainder_suffix"), alt(1209, "remainder_leng1", "suffix", "approximate-remainder suffix reading", "leng1", "approximate_remainder_suffix")]),
      "估": Object.freeze([alt(1230, "estimate_gu2", "verb", "estimate / guess", "gu2")]),
      "拿": Object.freeze([alt(1244, "take_naa4", "verb", "take / hold / grasp, written/formal", "naa4"), alt(1244, "discourse_laa4", "interjection", "discourse utterance / particle in the 嗱 family", "laa4", "discourse_utterance")])
    });
    var ALTERNATIVE_SPECS = Object.freeze({ ...MULTI_SPECS, ...READING_SPECS });
    var CANDIDATE_ONLY_SURFACES = new Set(Object.keys(ALTERNATIVE_SPECS));
    var DEFAULT_READING_OVERRIDES = Object.freeze({
      "會考": "wui6 haau2",
      "噉樣樣": "gam2 joeng2 joeng2",
      "咯": "lok3",
      "處理": "cyu5 lei5",
      "聽眾": "ting3 zung3",
      "爭": "zaang1",
      "哩度": "ni1 dou6"
    });
    function isNeutralLexicalEntry(entry) {
      return Boolean(entry) && entry.label === "lex" && entry.pos === "lexical_item" && entry.syntax === "lexical_item";
    }
    function applyReviewedEntries(entries) {
      if (!Array.isArray(entries)) throw new TypeError("ranks 1001–1250 reviewed overlay requires an entry array");
      return entries.map(([surface, entry]) => {
        const promotion = PROMOTIONS[surface];
        if (promotion && isNeutralLexicalEntry(entry)) {
          return [surface, {
            ...entry,
            label: promotion.label,
            pos: promotion.pos,
            syntax: promotion.syntax,
            jyutping: promotion.jyutping || entry.jyutping || "",
            note: promotion.gloss,
            provenance: {
              kind: "reviewed_lexical_promotion",
              source: SOURCE,
              rank: promotion.rank,
              pronunciation_status: promotion.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted",
              prior_provenance: entry.provenance || null
            }
          }];
        }
        if (CANDIDATE_ONLY_SURFACES.has(surface) && isNeutralLexicalEntry(entry) && DEFAULT_READING_OVERRIDES[surface]) {
          return [surface, {
            ...entry,
            jyutping: DEFAULT_READING_OVERRIDES[surface],
            note: `${entry.note || "Exact surface retained as neutral lexical coverage."} Reviewed analyses remain alternatives; default tokenization stays neutral.`,
            provenance: {
              kind: "reviewed_candidate_default_pronunciation",
              source: SOURCE,
              pronunciation_status: "reviewed_default_reading_without_atomic_pos_promotion",
              prior_provenance: entry.provenance || null
            }
          }];
        }
        return [surface, entry];
      });
    }
    function defaultAnalysis(surface, entry) {
      return Object.freeze({
        id: `lex:${surface}:default`,
        label: entry.label || "neutral",
        pos: entry.pos || "lexical_item",
        jyutping: entry.jyutping || "",
        syntax: entry.syntax || "lexical_candidate",
        senses: Object.freeze([{ gloss: entry.note || "existing runtime default preserved" }]),
        provenance: Object.freeze({
          kind: "existing_runtime_default_preserved",
          source: "v0.5.232 token lexicon before ranks 1001–1250 alternatives",
          prior_provenance: entry.provenance || null
        })
      });
    }
    function reviewedAlternative(surface, spec, defaultEntry) {
      return Object.freeze({
        id: `lex:${surface}:r${spec.rank}:${spec.suffix}`,
        label: spec.label,
        pos: spec.pos,
        jyutping: spec.jyutping || defaultEntry.jyutping || "",
        syntax: spec.syntax,
        senses: Object.freeze([{ gloss: spec.gloss }]),
        provenance: Object.freeze({
          kind: "reviewed_lexical_analysis",
          source: SOURCE,
          rank: spec.rank,
          pronunciation_status: spec.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted"
        })
      });
    }
    function buildExplicitAnalyses(entries) {
      const defaults = new Map(entries || []);
      const out = /* @__PURE__ */ Object.create(null);
      for (const [surface, specs] of Object.entries(ALTERNATIVE_SPECS)) {
        const entry = defaults.get(surface);
        if (!entry) throw new Error(`ranks 1001–1250 explicit analyses reference missing runtime surface: ${surface}`);
        const rows = [defaultAnalysis(surface, entry), ...specs.map((spec) => reviewedAlternative(surface, spec, entry))];
        const seen = /* @__PURE__ */ new Set();
        for (const row of rows) {
          if (!row.jyutping) throw new Error(`${row.id}: explicit lexical analysis requires non-empty jyutping`);
          if (seen.has(row.id)) throw new Error(`${row.id}: duplicate ranks 1001–1250 stable analysis ID`);
          seen.add(row.id);
        }
        out[surface] = Object.freeze(rows);
      }
      return Object.freeze(out);
    }
    module2.exports = Object.freeze({
      SOURCE,
      PROMOTIONS,
      SOURCE_ONLY_SURFACES,
      INDEPENDENT_ZERO_HIT_SURFACES,
      BLOCKED_ATOMIC_SURFACES,
      MULTI_SPECS,
      READING_SPECS,
      ALTERNATIVE_SPECS,
      CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES,
      isNeutralLexicalEntry,
      applyReviewedEntries,
      buildExplicitAnalyses
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1001-1250-runtime-policy.js
var require_cifu_r1001_1250_runtime_policy = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1001-1250-runtime-policy.js"(exports2, module2) {
    "use strict";
    var reviewed = require_cifu_r1001_1250_reviewed();
    function applyRuntimePolicy(entries) {
      return reviewed.applyReviewedEntries(entries);
    }
    module2.exports = Object.freeze({
      SOURCE: reviewed.SOURCE,
      PROMOTIONS: reviewed.PROMOTIONS,
      SOURCE_ONLY_SURFACES: reviewed.SOURCE_ONLY_SURFACES,
      INDEPENDENT_ZERO_HIT_SURFACES: reviewed.INDEPENDENT_ZERO_HIT_SURFACES,
      BLOCKED_ATOMIC_SURFACES: reviewed.BLOCKED_ATOMIC_SURFACES,
      CANDIDATE_ONLY_SURFACES: reviewed.CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES: reviewed.DEFAULT_READING_OVERRIDES,
      applyRuntimePolicy
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1251-1500-reviewed.js
var require_cifu_r1251_1500_reviewed = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1251-1500-reviewed.js"(exports2, module2) {
    "use strict";
    var SOURCE = "docs/research/ISSUE-884-CIFU-R1251-1500-LEXICAL-ADJUDICATION-R3.md";
    var KIND = Object.freeze({
      noun: ["what", "noun", "common_noun"],
      person: ["who", "noun", "person_noun"],
      place: ["where", "noun", "place_noun"],
      verb: ["doing", "verb", "verb"],
      adjective: ["like", "adjective", "stative_predicate"],
      adverb: ["how", "adverb", "adverb"],
      function: ["func", "function", "function"],
      classifier: ["what", "classifier", "classifier"],
      measure: ["what", "measure", "measure_word"],
      determiner: ["what", "determiner", "determiner"],
      pronoun: ["what", "pronoun", "pronoun"],
      bound: ["func", "bound", "bound_morpheme"],
      suffix: ["func", "suffix", "derivational_suffix"],
      formula: ["func", "formula", "discourse_formula"],
      coverb: ["func", "coverb", "coverb"],
      proper: ["what", "proper_noun", "proper_name"],
      localizer: ["where", "localizer", "localizer"],
      temporal: ["when", "noun", "temporal_expression"]
    });
    function lexicalSpec(rank, kind, gloss, jyutping = "", syntax = "") {
      const meta = KIND[kind];
      if (!meta) throw new Error(`unknown lexical kind ${kind}`);
      return Object.freeze({
        rank,
        kind,
        gloss,
        jyutping,
        label: meta[0],
        pos: meta[1],
        syntax: syntax || meta[2]
      });
    }
    function alt(rank, row) {
      const [suffix, kind, gloss, jyutping = "", syntax = ""] = row;
      return Object.freeze({ ...lexicalSpec(rank, kind, gloss, jyutping, syntax), suffix });
    }
    var PROMOTION_ROWS = Object.freeze([
      [1251, "畢業", "verb", "graduate / finish a course"],
      [1254, "提供", "verb", "provide"],
      [1256, "感情", "noun", "emotion / relationship / affection"],
      [1258, "解決", "verb", "solve / resolve"],
      [1260, "熄", "verb", "switch off / extinguish"],
      [1261, "廠", "noun", "factory / works"],
      [1262, "熟", "adjective", "ripe / cooked / familiar"],
      [1263, "適合", "adjective", "suitable / fit"],
      [1264, "靚女", "person", "pretty woman / girl"],
      [1267, "戲", "noun", "play / show / drama"],
      [1268, "擦膠", "noun", "eraser", "caat3 gaau1"],
      [1269, "禮物", "noun", "gift"],
      [1270, "雞", "noun", "chicken"],
      [1272, "鐘意", "verb", "like / be fond of", "", "cognition_verb"],
      [1273, "權", "noun", "right / authority / power; bound-root family"],
      [1278, "太過", "adverb", "too / excessively", "", "degree_adverb"],
      [1283, "必須", "function", "must / have to", "", "modal_necessity"],
      [1286, "民主黨", "proper", "Democratic Party / political-party name"],
      [1293, "有機", "adjective", "organic"],
      [1294, "告", "verb", "tell / inform / sue"],
      [1297, "言論", "noun", "speech / opinion / comment / discourse"],
      [1298, "受到", "verb", "receive / be subjected to"],
      [1299, "咁上下", "adjective", "roughly that level / about so much / so-so", "", "degree_stative_expression"],
      [1300, "波浪", "noun", "wave / waves"],
      [1302, "阿哥", "person", "older brother"],
      [1304, "後面", "localizer", "behind / back / later"],
      [1310, "高度", "noun", "height / altitude / degree"],
      [1311, "基本法", "proper", "Basic Law; generic basic-law reading remains contextual"],
      [1313, "理論", "noun", "theory"],
      [1315, "喊", "verb", "cry / yell / shout / call out"],
      [1317, "尊嚴", "noun", "dignity / honor"],
      [1321, "資料", "noun", "data / information / materials"],
      [1322, "審", "verb", "examine / investigate / try in court"],
      [1323, "撳", "verb", "press / push down / restrain / extort", "gam6"],
      [1324, "複雜", "adjective", "complicated / complex"],
      [1329, "鬧", "verb", "scold / quarrel / make a disturbance"],
      [1330, "瞭解", "verb", "understand / find out", "", "cognition_verb"],
      [1332, "權利", "noun", "right / privilege"],
      [1334, "一點", "measure", "a little / a bit", "", "quantity_expression"],
      [1335, "人士", "person", "person / figure"],
      [1337, "上邊", "localizer", "above / on top / upper side"],
      [1339, "已", "adverb", "already", "", "temporal_adverb"],
      [1340, "不嬲", "adverb", "always / all along", "", "habitual_temporal_adverb"],
      [1341, "公平", "adjective", "fair / impartial"],
      [1342, "冇所謂", "adjective", "does not matter / do not care", "", "stative_expression"],
      [1345, "合格", "adjective", "be qualified / pass", "", "result_stative_predicate"],
      [1348, "肉", "noun", "meat / flesh / pulp"],
      [1349, "自", "bound", "formal reflexive / source bound morpheme"],
      [1350, "似乎", "function", "epistemic / seeming predicate-adverbial family: apparently / seem", "", "epistemic_seeming_expression"],
      [1352, "吸引", "verb", "attract / be attractive"],
      [1354, "垂直", "adjective", "vertical / perpendicular"],
      [1355, "奇怪", "adjective", "strange / odd"],
      [1356, "空間", "noun", "space"],
      [1357, "阿婆", "person", "old woman / grandmother"],
      [1359, "咬", "verb", "bite"],
      [1360, "城堡", "noun", "castle"],
      [1361, "政黨", "noun", "political party", "", "organization_noun"],
      [1363, "效", "bound", "effect / efficacy / imitative bound family", "haau6"],
      [1364, "酒店", "place", "hotel / hotel-business noun"],
      [1367, "基本", "adjective", "basic / fundamental", "", "relational_modifier"],
      [1369, "殺", "verb", "kill"],
      [1370, "毫子", "measure", "ten cents", "hou4 zi2", "currency_measure"],
      [1371, "深", "adjective", "deep / profound"],
      [1372, "雀仔", "noun", "small bird / birdie"],
      [1375, "朝頭早", "temporal", "morning / early in the morning"],
      [1376, "無端端", "adverb", "for no reason / out of nowhere", "", "discourse_adverb"],
      [1379, "搞錯", "verb", "make a mistake / get wrong", "", "resultative_verb"],
      [1380, "資訊", "noun", "information"],
      [1381, "對方", "person", "the other side / counterpart"],
      [1382, "語言", "noun", "language"],
      [1383, "整體", "noun", "whole / entity / overall body"],
      [1389, "證明", "verb", "prove / verify / testify"],
      [1391, "攪", "verb", "stir / disturb / mix", "gaau2"],
      [1392, "一次過", "adverb", "all at once / in one go"],
      [1398, "冇用", "adjective", "useless / of no use", "", "stative_expression"],
      [1401, "左手邊", "localizer", "left-hand side", "zo2 sau2 bin1"],
      [1403, "地點", "place", "location / site / venue"],
      [1407, "好處", "noun", "benefit / advantage"],
      [1408, "存在", "verb", "exist / be present"],
      [1409, "有趣", "adjective", "interesting / amusing"],
      [1410, "老實", "adjective", "honest / sincere"],
      [1413, "冷氣", "noun", "air conditioning / cool air"],
      [1414, "沖", "verb", "rinse / flush / infuse / rush against"],
      [1415, "角色", "noun", "role / character"],
      [1416, "車主", "person", "vehicle / car owner"],
      [1418, "依家", "temporal", "now; orthographic variant of 而家", "ji1 gaa1"],
      [1421, "明顯", "adjective", "obvious / clear"],
      [1422, "法例", "noun", "law / statute / regulation"],
      [1423, "波", "noun", "ball / wave / typhoon nominal family"],
      [1429, "重新", "adverb", "again / anew"],
      [1430, "音樂", "noun", "music"],
      [1431, "音響", "noun", "audio / sound system / acoustics"],
      [1437, "除非", "function", "unless / only if", "", "conditional_subordinator"],
      [1438, "情", "noun", "feeling / emotion / situation"],
      [1439, "排隊", "verb", "line up / queue"],
      [1440, "票", "noun", "ticket / note / vote"],
      [1441, "終審", "noun", "final adjudication / final appeal", "", "legal_nominal"],
      [1442, "竟然", "adverb", "unexpectedly / to one's surprise", "", "evaluative_adverb"],
      [1444, "提出", "verb", "raise / propose / put forward"],
      [1445, "答案", "noun", "answer / solution"],
      [1449, "椰", "noun", "coconut / coconut palm", "je4"],
      [1452, "隔", "verb", "separate / be apart / lie between", "gaak3", "relational_predicate"],
      [1453, "電影", "noun", "movie / film", "din6 jing2"],
      [1455, "摺", "verb", "fold / bend", "zip3"],
      [1457, "福利", "noun", "welfare / benefits / well-being", "fuk1 lei6"],
      [1459, "劍", "noun", "sword", "gim3"],
      [1464, "諗法", "noun", "way of thinking / opinion / idea", "nam2 faat3"],
      [1465, "質素", "noun", "quality / standard", "zat1 sou3"],
      [1466, "選", "verb", "choose / select / elect", "syun2"],
      [1467, "錄", "verb", "record / copy", "luk6"],
      [1470, "醫療", "noun", "medical treatment / healthcare", "ji1 liu4"],
      [1471, "贊成", "verb", "approve / support / endorse", "zaan3 sing4"],
      [1472, "曬", "verb", "sun / dry / sunbathe; loan share-files sense remains same POS", "saai3"],
      [1476, "大隻", "adjective", "big-built / large-bodied / strong"],
      [1477, "女皇", "person", "queen / empress"],
      [1479, "引起", "verb", "cause / give rise to / arouse"],
      [1480, "心理", "noun", "psychology / mental state; nominal-bound modifier family"],
      [1481, "文化", "noun", "culture / civilization; nominal-bound modifier family"],
      [1485, "各", "determiner", "each / every", "", "distributive_determiner"],
      [1488, "有關", "verb", "be related to / concern", "jau5 gwaan1", "relational_verb"],
      [1489, "行業", "noun", "industry / trade", "hong4 jip6"],
      [1490, "初初", "adverb", "at first / initially", "", "temporal_adverb"],
      [1494, "角度", "noun", "angle / point of view"],
      [1495, "事實上", "adverb", "in fact / actually", "", "discourse_adverb"],
      [1498, "拍拖", "verb", "date / be in a romantic relationship"],
      [1500, "沾污", "verb", "stain / soil / defile", "zim1 wu1"]
    ]);
    var PROMOTIONS = Object.freeze(Object.fromEntries(
      PROMOTION_ROWS.map(([rank, surface, kind, gloss, jyutping = "", syntax = ""]) => [
        surface,
        lexicalSpec(rank, kind, gloss, jyutping, syntax)
      ])
    ));
    var SOURCE_ONLY_SURFACES = /* @__PURE__ */ new Set(["平排", "打直", "打斜"]);
    var INDEPENDENT_ZERO_HIT_SURFACES = /* @__PURE__ */ new Set([
      "象徵",
      "解決",
      "擦膠",
      "公眾",
      "毋",
      "必須",
      "民主黨",
      "收到",
      "言論",
      "波浪",
      "基本法",
      "瞭解",
      "權利",
      "失敗",
      "垂直",
      "城堡",
      "政黨",
      "冤",
      "效",
      "環保",
      "黨",
      "攪",
      "左手邊",
      "角色",
      "車主",
      "坦白",
      "法例",
      "票",
      "終審",
      "著",
      "賀",
      "椰",
      "劍",
      "引起",
      "回應",
      "果",
      "沾污"
    ]);
    var BLOCKED_ATOMIC_SURFACES = /* @__PURE__ */ new Set([
      "幾大",
      "試下",
      "鑑林",
      "三點",
      "四十五度",
      "左下",
      "再落",
      "好大",
      "好慘",
      "我估",
      "我畫",
      "知有",
      "個害",
      "將個",
      "圍住",
      "落落",
      "邊行",
      "一晚",
      "上角",
      "成張",
      "即個",
      "信佳",
      "問我",
      "幾喇",
      "越來",
      "講個",
      "轉個",
      "一百蚊",
      "乜鬼",
      "右下方",
      "印椰樹",
      "多少",
      "多次",
      "多個",
      "個意",
      "時話",
      "就畫",
      "落返",
      "過個",
      "講番",
      "點會",
      "一本",
      "一座",
      "下角",
      "五點",
      "右畫",
      "再畫",
      "好貴",
      "每個"
    ]);
    var MULTI_ROWS = Object.freeze([
      [1255, "象徵", ["symbol_noun", "noun", "symbol / emblem"], ["symbolize_verb", "verb", "symbolize / represent"]],
      [1271, "懷疑", ["doubt_verb", "verb", "doubt / suspect"], ["suspicion_noun", "noun", "doubt / suspicion"]],
      [1277, "公眾", ["public_noun", "noun", "the public"], ["public_modifier", "adjective", "public modifier / distinguishing-word use", "", "relational_modifier"]],
      [1279, "毋", ["formal_negative", "function", "formal / written negative", "mou4", "negative_function"], ["surname", "proper", "surname / proper-name homograph", "mou4", "proper_name"]],
      [1287, "任", ["appoint_allow_verb", "verb", "appoint / take office / allow"], ["surname", "proper", "surname / proper-name homograph", "", "proper_name"]],
      [1289, "多數", ["majority_noun", "noun", "majority / most"], ["mostly_adverb", "adverb", "usually / mostly"]],
      [1303, "後生", ["young_stative", "adjective", "young"], ["young_person", "person", "young person / young people"]],
      [1305, "計劃", ["plan_noun", "noun", "plan / project"], ["plan_verb", "verb", "plan"]],
      [1308, "租", ["rent_verb", "verb", "rent / lease"], ["rent_noun", "noun", "rent / rental payment"]],
      [1318, "發", ["send_emit_verb", "verb", "send / issue / develop / emit"], ["round_classifier", "classifier", "classifier / measure for shots or rounds"]],
      [1319, "照", ["shine_photo_verb", "verb", "shine / photograph / reflect"], ["according_coverb", "coverb", "according to", "", "relation_coverb"], ["photo_noun", "noun", "photo"]],
      [1325, "論", ["discuss_verb", "verb", "discuss / evaluate"], ["theory_noun", "noun", "view / theory / discourse"], ["regarding_coverb", "coverb", "regarding / in terms of", "", "relation_coverb"]],
      [1326, "輪", ["wheel_round_noun", "noun", "wheel / round"], ["round_classifier", "classifier", "classifier for rounds / turns / round objects"], ["rotate_verb", "verb", "rotate / take turns"]],
      [1327, "遮", ["cover_verb", "verb", "cover / screen / conceal"], ["umbrella_noun", "noun", "umbrella"]],
      [1328, "靚仔", ["handsome_guy_noun", "person", "handsome guy / boy", "leng3 zai2"], ["handsome_stative", "adjective", "handsome / good / fine", "leng3 zai2"]],
      [1343, "失敗", ["fail_verb", "verb", "fail / be defeated"], ["failure_noun", "noun", "failure / defeat"]],
      [1344, "申請", ["apply_verb", "verb", "apply / request"], ["application_noun", "noun", "application / request"]],
      [1346, "安排", ["arrange_verb", "verb", "arrange / plan"], ["arrangement_noun", "noun", "arrangement / plan"]],
      [1362, "冤", ["grievance_noun", "noun", "injustice / grievance"], ["wronged_stative", "adjective", "wronged / unjust"]],
      [1368, "教育", ["educate_verb", "verb", "educate / teach"], ["education_noun", "noun", "education"]],
      [1373, "創作", ["create_verb", "verb", "create / produce / write"], ["creation_noun", "noun", "creative work / creation"]],
      [1378, "黃", ["yellow_stative", "adjective", "yellow"], ["surname", "proper", "surname / proper-name homograph"]],
      [1385, "環保", ["eco_friendly_stative", "adjective", "eco-friendly / environmentally friendly"], ["environmental_protection_noun", "noun", "environmental protection"]],
      [1386, "總", ["total_stative", "adjective", "total / overall"], ["total_gather_verb", "verb", "gather / total"], ["always_overall_adverb", "adverb", "always / overall / in every case"]],
      [1390, "黨", ["party_noun", "noun", "party / gang / faction"], ["take_sides_verb", "verb", "be partial / take sides"]],
      [1412, "何", ["surname", "proper", "surname / proper-name family"], ["formal_interrogative", "pronoun", "formal interrogative what / how / why / which", "", "formal_interrogative"]],
      [1419, "坦白", ["frank_stative", "adjective", "frank / honest"], ["confess_verb", "verb", "confess / state frankly"]],
      [1420, "姓", ["surname_noun", "noun", "surname"], ["be_surnamed_verb", "verb", "have the surname / be surnamed"]],
      [1424, "花", ["flower_noun", "noun", "flower"], ["spend_verb", "verb", "spend / use up"], ["variegated_stative", "adjective", "multicoloured / scratched / dirty"], ["productive_bound", "bound", "productive bound / suffix-like family", "", "bound_morpheme"]],
      [1425, "金", ["gold_noun", "noun", "gold / money"], ["golden_bound", "bound", "gold / golden bound-attributive family", "", "bound_morpheme"]],
      [1426, "約", ["appointment_noun", "noun", "appointment / agreement"], ["arrange_restrict_verb", "verb", "arrange / restrict"], ["approximately_adverb", "adverb", "about / approximately", "", "approximation_adverb"]],
      [1434, "恐怖", ["frightening_stative", "adjective", "frightening / terrible"], ["terror_noun", "noun", "terror / horror"]],
      [1447, "賀", ["congratulate_verb", "verb", "congratulate / celebrate", "ho6"], ["surname", "proper", "surname / proper-name family", "ho6"]],
      [1456, "盡", ["exhaust_verb", "verb", "use up / exhaust"], ["utmost_adverb", "adverb", "to the utmost / as much as possible", "", "degree_adverb"], ["limit_noun", "noun", "end / limit"], ["limit_localizer", "localizer", "end / limit localizer family"]],
      [1458, "與", ["and_with_function", "function", "formal and / with", "jyu5", "coordination_relation"], ["give_participate_verb", "verb", "verb family independently attested", "jyu5"], ["with_coverb", "coverb", "formal relational / prepositional with", "jyu5", "relation_coverb"]],
      [1460, "層", ["layer_classifier", "classifier", "classifier for floors / layers", "cang4"], ["layer_noun", "noun", "layer / storey", "cang4"]],
      [1482, "毛", ["hair_noun", "noun", "hair / fur"], ["money_measure", "measure", "monetary measure / classifier", "", "currency_measure"]],
      [1486, "回應", ["response_noun", "noun", "response"], ["respond_verb", "verb", "respond / reply"]],
      [1491, "困難", ["difficulty_noun", "noun", "difficulty / problem"], ["difficult_stative", "adjective", "difficult"]],
      [1493, "沙", ["sand_noun", "noun", "sand / granules", "saa1"], ["hoarse_stative", "adjective", "hoarse / husky of voice", "saa1"]],
      [1496, "佬", ["man_noun", "person", "man / male"], ["person_suffix", "suffix", "productive colloquial person suffix", "", "person_suffix"]],
      [1497, "或", ["or_function", "function", "formal or", "waak6", "alternative_connector"], ["possibly_adverb", "adverb", "possibly", "waak6"]],
      [1499, "果", ["fruit_result_noun", "noun", "fruit / result", "gwo2"], ["surely_adverb", "adverb", "surely / really", "gwo2"], ["stuff_succeed_verb", "verb", "stuff / succeed lexical family", "gwo2"], ["surname", "proper", "surname / proper-name family", "gwo2"]]
    ]);
    var READING_ROWS = Object.freeze([
      [1252, "剩", ["remain_sing6", "verb", "remain / be left, standard-literary reading", "sing6"], ["remain_zing6", "verb", "remain / be left, spoken-variant reading", "zing6"]],
      [1257, "號", ["designation_hou6", "noun", "number / designation / date / title family", "hou6"], ["howl_hou4", "verb", "howl / cry family", "hou4"]],
      [1265, "儲", ["store_cou5", "verb", "store / save, colloquial reading", "cou5"], ["store_cyu5", "verb", "store / save, literary reading", "cyu5"]],
      [1266, "應", ["should_jing1", "function", "should / ought to", "jing1", "modal_necessity"], ["respond_jing3", "verb", "answer / respond family", "jing3"]],
      [1276, "下邊", ["below_bin1", "localizer", "below / under; exact graph 下邊", "haa6 bin1"]],
      [1292, "收到", ["receive_result", "verb", "receive / result family", "sau1 dou2"], ["received_understood_formula", "formula", "received / understood discourse response", "sau1 dou2", "discourse_formula"]],
      [1307, "唔好意思", ["politeness_si1", "formula", "sorry / excuse me; embarrassed / shy family", "m4 hou2 ji3 si1", "politeness_expression"], ["politeness_si3", "formula", "sorry / excuse me; embarrassed / shy reading variant", "m4 hou2 ji3 si3", "politeness_expression"]],
      [1309, "純粹", ["purely_seoi5", "adverb", "purely / merely", "seon4 seoi5", "focus_adverb"], ["purely_seoi6", "adverb", "purely / merely reading variant", "seon4 seoi6", "focus_adverb"]],
      [1314, "處", ["place_cyu3", "noun", "place / office / department", "cyu3"], ["locality_syu3", "localizer", "colloquial locality family", "syu3"], ["locality_syu2", "localizer", "colloquial locality reading variant", "syu2"], ["formal_verb_cyu2", "verb", "formal verbal family", "cyu2"], ["formal_bound_cyu5", "bound", "formal bound family", "cyu5", "bound_morpheme"]],
      [1338, "下下", ["every_time_haa5haa5", "temporal", "every time / each instance", "haa5 haa5", "distributive_temporal_expression"]],
      [1353, "更", ["more_gang3", "adverb", "more / even more", "gang3", "degree_adverb"], ["change_gang1", "verb", "change-family reading", "gang1"]],
      [1365, "區", ["district_keoi1", "noun", "area / district", "keoi1"], ["surname_au1", "proper", "surname / proper-name family", "au1"]],
      [1384, "醒", ["wake_seng2", "verb", "wake / regain consciousness", "seng2"], ["treat_sing2", "verb", "give / treat family", "sing2"], ["smart_sing2", "adjective", "smart / alert", "sing2"]],
      [1395, "上年", ["last_year", "temporal", "last year", "soeng6 nin2"]],
      [1396, "不斷", ["continuously", "adverb", "continuously / constantly", "bat1 dyun6"]],
      [1397, "之類", ["and_the_like_leoi6", "suffix", "and the like / and so on", "zi1 leoi6", "listing_expression"], ["and_the_like_leoi2", "suffix", "and the like / and so on reading variant", "zi1 leoi2", "listing_expression"]],
      [1399, "片", ["film_pin2", "noun", "film / video / scan / diaper family", "pin2"], ["slice_classifier_pin3", "classifier", "classifier for slices / tracts / broad scenes", "pin3"]],
      [1411, "艾爾頓", ["proper_name", "proper", "proper-name transliteration", "aai6 ji5 deon6"]],
      [1417, "亞視", ["asia_television", "proper", "ATV / Asia Television", "aa3 si6"]],
      [1427, "訂", ["order_deng6", "verb", "order / book / subscribe / reserve", "deng6"], ["formal_set_ding3", "bound", "formal / bound conclude / set / draw up family", "ding3", "bound_morpheme"]],
      [1428, "迫", ["force_bik1", "verb", "force / press", "bik1"], ["crowded_bik1", "adjective", "crowded / pressing", "bik1"], ["execute_baak1", "verb", "specialized slang execution family", "baak1"]],
      [1433, "家姐", ["elder_sister", "person", "elder sister", "gaa1 ze1"]],
      [1435, "捉", ["catch", "verb", "catch / grab", "zuk1"]],
      [1446, "著", ["writing_zyu3", "bound", "writing / works / notability literary-bound family", "zyu3", "bound_morpheme"], ["wear_zoek3", "verb", "wear / put on family", "zoek3"], ["lexical_zoek6", "verb", "independently attested zoek6 lexical family", "zoek6", "separate_reading_family"]],
      [1448, "黑", ["black_haak1", "adjective", "black / dark / unlucky color family", "haak1"], ["black_hak1", "adjective", "black / dark / unlucky pronunciation variant", "hak1"]],
      [1454, "寧願", ["prefer_jyun2", "verb", "would rather / prefer predicate", "ning4 jyun2", "preference_predicate"], ["rather_jyun6", "adverb", "would rather modal-adverb function", "ning4 jyun6", "preference_adverb"]],
      [1461, "數", ["count_sou2", "verb", "count / enumerate / criticize by listing", "sou2"], ["number_sou3", "noun", "number / figure / amount", "sou3"]],
      [1462, "樓", ["building_lau2", "noun", "building / flat / floor", "lau2"], ["surname_bound_lau4", "proper", "surname / proper / bound-compound family", "lau4"]],
      [1463, "膠袋", ["plastic_bag", "noun", "plastic bag", "gaau1 doi2"]]
    ]);
    function specMap(rows) {
      return Object.freeze(Object.fromEntries(
        rows.map(([rank, surface, ...specs]) => [surface, Object.freeze(specs.map((row) => alt(rank, row)))])
      ));
    }
    var MULTI_SPECS = specMap(MULTI_ROWS);
    var READING_SPECS = specMap(READING_ROWS);
    var ALTERNATIVE_SPECS = Object.freeze({ ...MULTI_SPECS, ...READING_SPECS });
    var CANDIDATE_ONLY_SURFACES = new Set(Object.keys(ALTERNATIVE_SPECS));
    var DEFAULT_READING_OVERRIDES = Object.freeze({
      "下邊": "haa6 bin1",
      "收到": "sau1 dou2",
      "下下": "haa5 haa5",
      "上年": "soeng6 nin2",
      "不斷": "bat1 dyun6",
      "艾爾頓": "aai6 ji5 deon6",
      "亞視": "aa3 si6",
      "家姐": "gaa1 ze1",
      "捉": "zuk1",
      "膠袋": "gaau1 doi2"
    });
    function isNeutralLexicalEntry(entry) {
      return Boolean(entry) && entry.label === "lex" && entry.pos === "lexical_item" && entry.syntax === "lexical_item";
    }
    function applyReviewedEntries(entries) {
      if (!Array.isArray(entries)) throw new TypeError("ranks 1251–1500 reviewed overlay requires an entry array");
      return entries.map(([surface, entry]) => {
        const promotion = PROMOTIONS[surface];
        let next = entry;
        if (promotion && isNeutralLexicalEntry(next)) {
          next = {
            ...next,
            label: promotion.label,
            pos: promotion.pos,
            syntax: promotion.syntax,
            jyutping: promotion.jyutping || next.jyutping || "",
            note: promotion.gloss,
            provenance: {
              kind: "reviewed_lexical_promotion",
              source: SOURCE,
              rank: promotion.rank,
              pronunciation_status: promotion.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted",
              prior_provenance: next.provenance || null
            }
          };
        }
        const reviewedReading = DEFAULT_READING_OVERRIDES[surface];
        if (reviewedReading && next && next.jyutping !== reviewedReading) {
          next = {
            ...next,
            jyutping: reviewedReading,
            provenance: {
              kind: isNeutralLexicalEntry(next) ? "reviewed_candidate_default_pronunciation" : "reviewed_default_pronunciation_correction",
              source: SOURCE,
              pronunciation_status: "reviewed_explicit_reading",
              prior_provenance: next.provenance || null
            }
          };
        }
        return [surface, next];
      });
    }
    function defaultAnalysis(surface, entry) {
      return Object.freeze({
        id: `lex:${surface}:default`,
        label: entry.label || "neutral",
        pos: entry.pos || "lexical_item",
        jyutping: entry.jyutping || "",
        syntax: entry.syntax || "lexical_candidate",
        senses: Object.freeze([{ gloss: entry.note || "existing runtime default preserved" }]),
        provenance: Object.freeze({
          kind: "existing_runtime_default_preserved",
          source: "v0.5.233 token lexicon before ranks 1251–1500 alternatives",
          prior_provenance: entry.provenance || null
        })
      });
    }
    function reviewedAlternative(surface, spec, defaultEntry) {
      return Object.freeze({
        id: `lex:${surface}:r${spec.rank}:${spec.suffix}`,
        label: spec.label,
        pos: spec.pos,
        jyutping: spec.jyutping || defaultEntry.jyutping || "",
        syntax: spec.syntax,
        senses: Object.freeze([{ gloss: spec.gloss }]),
        provenance: Object.freeze({
          kind: "reviewed_lexical_analysis",
          source: SOURCE,
          rank: spec.rank,
          pronunciation_status: spec.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted"
        })
      });
    }
    function buildExplicitAnalyses(entries) {
      const defaults = new Map(entries || []);
      const out = /* @__PURE__ */ Object.create(null);
      for (const [surface, specs] of Object.entries(ALTERNATIVE_SPECS)) {
        const entry = defaults.get(surface);
        if (!entry) throw new Error(`ranks 1251–1500 explicit analyses reference missing runtime surface: ${surface}`);
        const rows = [
          defaultAnalysis(surface, entry),
          ...specs.map((spec) => reviewedAlternative(surface, spec, entry))
        ];
        const seen = /* @__PURE__ */ new Set();
        for (const row of rows) {
          if (!row.jyutping) throw new Error(`${row.id}: explicit lexical analysis requires non-empty jyutping`);
          if (seen.has(row.id)) throw new Error(`${row.id}: duplicate ranks 1251–1500 stable analysis ID`);
          seen.add(row.id);
        }
        out[surface] = Object.freeze(rows);
      }
      return Object.freeze(out);
    }
    module2.exports = Object.freeze({
      SOURCE,
      PROMOTIONS,
      SOURCE_ONLY_SURFACES,
      INDEPENDENT_ZERO_HIT_SURFACES,
      BLOCKED_ATOMIC_SURFACES,
      MULTI_SPECS,
      READING_SPECS,
      ALTERNATIVE_SPECS,
      CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES,
      isNeutralLexicalEntry,
      applyReviewedEntries,
      buildExplicitAnalyses
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1251-1500-runtime-policy.js
var require_cifu_r1251_1500_runtime_policy = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1251-1500-runtime-policy.js"(exports2, module2) {
    "use strict";
    var reviewed = require_cifu_r1251_1500_reviewed();
    var MANDARIN_ONLY_SURFACES = /* @__PURE__ */ new Set(["多少"]);
    function applyRuntimePolicy(entries) {
      return reviewed.applyReviewedEntries(entries).filter(([surface]) => !MANDARIN_ONLY_SURFACES.has(surface));
    }
    module2.exports = Object.freeze({
      SOURCE: reviewed.SOURCE,
      PROMOTIONS: reviewed.PROMOTIONS,
      SOURCE_ONLY_SURFACES: reviewed.SOURCE_ONLY_SURFACES,
      INDEPENDENT_ZERO_HIT_SURFACES: reviewed.INDEPENDENT_ZERO_HIT_SURFACES,
      BLOCKED_ATOMIC_SURFACES: reviewed.BLOCKED_ATOMIC_SURFACES,
      CANDIDATE_ONLY_SURFACES: reviewed.CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES: reviewed.DEFAULT_READING_OVERRIDES,
      MANDARIN_ONLY_SURFACES,
      applyRuntimePolicy
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1501-1750-reviewed.js
var require_cifu_r1501_1750_reviewed = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1501-1750-reviewed.js"(exports2, module2) {
    "use strict";
    var SOURCE = "docs/research/ISSUE-892-CIFU-R1501-1750-LEXICAL-ADJUDICATION.md";
    var KIND = Object.freeze({
      noun: ["what", "noun", "common_noun"],
      person: ["who", "noun", "person_noun"],
      place: ["where", "noun", "place_noun"],
      verb: ["doing", "verb", "verb"],
      adjective: ["like", "adjective", "stative_predicate"],
      adverb: ["how", "adverb", "adverb"],
      function: ["func", "function", "function"],
      classifier: ["what", "classifier", "classifier"],
      measure: ["what", "measure", "measure_word"],
      bound: ["func", "bound", "bound_morpheme"],
      coverb: ["func", "coverb", "coverb"],
      proper: ["what", "proper_noun", "proper_name"],
      localizer: ["where", "localizer", "localizer"],
      temporal: ["when", "noun", "temporal_expression"]
    });
    function lexicalSpec(rank, kind, gloss, jyutping = "", syntax = "") {
      const meta = KIND[kind];
      if (!meta) throw new Error(`unknown lexical kind ${kind}`);
      return Object.freeze({ rank, kind, gloss, jyutping, label: meta[0], pos: meta[1], syntax: syntax || meta[2] });
    }
    function alt(rank, suffix, kind, gloss, jyutping = "", syntax = "") {
      return Object.freeze({ ...lexicalSpec(rank, kind, gloss, jyutping, syntax), suffix });
    }
    var PROMOTION_ROWS = Object.freeze([
      [1501, "削", "verb", "cut / pare / sharpen"],
      [1502, "屋企人", "person", "family member"],
      [1504, "查", "verb", "check / investigate / search"],
      [1508, "剛才", "temporal", "just now"],
      [1509, "容許", "verb", "allow / permit"],
      [1510, "效率", "noun", "efficiency"],
      [1512, "特登", "adverb", "deliberately / on purpose"],
      [1514, "唪", "verb", "recite / chant loudly", "fung2"],
      [1515, "從來", "adverb", "ever / always / from the beginning"],
      [1518, "貨", "noun", "goods / merchandise"],
      [1519, "郭富城", "proper", "Aaron Kwok", "gwok3 fu3 sing4"],
      [1522, "牌子", "noun", "brand / sign / label"],
      [1523, "等於", "verb", "equal / be equivalent to"],
      [1526, "媽咪", "person", "mum / mummy"],
      [1527, "搞掂", "verb", "finish / settle / fix successfully"],
      [1528, "經常", "adverb", "often / regularly"],
      [1529, "聖誕", "temporal", "Christmas"],
      [1537, "醫院", "place", "hospital"],
      [1538, "離婚", "verb", "divorce / be divorced"],
      [1541, "辭典", "noun", "dictionary"],
      [1542, "邊緣", "noun", "edge / periphery / margin"],
      [1544, "讓", "verb", "let / allow / yield"],
      [1550, "中心", "noun", "centre / core"],
      [1551, "中醫", "noun", "Traditional Chinese Medicine / practitioner"],
      [1552, "之中", "localizer", "inside / among / within"],
      [1553, "互相", "adverb", "mutually / each other", "", "reciprocal_function"],
      [1555, "太太", "person", "wife / Mrs. / madam"],
      [1556, "日後", "temporal", "later / in the future"],
      [1557, "主角", "person", "leading role / protagonist"],
      [1558, "主題", "noun", "theme / topic"],
      [1559, "出聲", "verb", "make a sound / speak up"],
      [1560, "台灣", "proper", "Taiwan"],
      [1564, "目的", "noun", "purpose / aim / goal"],
      [1567, "好笑", "adjective", "funny / laughable"],
      [1569, "扣", "verb", "fasten / deduct / detain / arrest"],
      [1570, "收埋", "verb", "put away / hide / conceal"],
      [1571, "自信", "noun", "self-confidence", "zi6 seon3"],
      [1572, "作", "verb", "make / do / compose / write / pretend"],
      [1577, "身體", "noun", "body / health"],
      [1579, "咁滯", "adverb", "almost / nearly / roughly"],
      [1582, "金融", "noun", "finance / banking"],
      [1584, "政治", "noun", "politics"],
      [1586, "為止", "function", "up to / until", "", "temporal_boundary_function"],
      [1587, "界", "noun", "boundary / domain / sphere"],
      [1588, "閂", "verb", "shut / close / switch off", "saan1"],
      [1589, "面前", "localizer", "in front of / in the presence of"],
      [1592, "唔錯", "adjective", "not bad / pretty good"],
      [1593, "庭", "noun", "court / courtyard"],
      [1594, "秘書", "person", "secretary"],
      [1597, "茶", "noun", "tea"],
      [1598, "骨折", "verb", "suffer / have a fracture", "gwat1 zit3", "medical_event_predicate"],
      [1600, "接", "verb", "receive / connect / take over / answer"],
      [1601, "痕", "noun", "mark / scar / trace"],
      [1603, "速度", "noun", "speed / rate / velocity"],
      [1604, "部門", "noun", "department / division"],
      [1609, "幾點", "temporal", "what time", "gei2 dim2", "interrogative_time_expression"],
      [1612, "琴日", "temporal", "yesterday"],
      [1614, "稅", "noun", "tax / duty"],
      [1615, "黑色", "noun", "black / black color", "", "color_expression"],
      [1616, "傷害", "verb", "injure / harm"],
      [1617, "傾偈", "verb", "chat / talk"],
      [1621, "照顧", "verb", "take care of / look after"],
      [1626, "網", "noun", "net / network"],
      [1628, "增加", "verb", "increase / add"],
      [1629, "數字", "noun", "digit / number / figure"],
      [1632, "燈", "noun", "lamp / light"],
      [1633, "牆", "noun", "wall"],
      [1635, "嚮", "coverb", "at / toward / in the direction of", "", "directional_relation_coverb"],
      [1637, "一刻", "temporal", "an instant / the moment"],
      [1641, "十分", "adverb", "very / extremely", "", "degree_adverb"],
      [1642, "中學", "place", "secondary school"],
      [1643, "之內", "localizer", "within / inside"],
      [1644, "今", "temporal", "now / current / this"],
      [1645, "內容", "noun", "content / details"],
      [1646, "水草", "noun", "aquatic plants"],
      [1647, "牙", "noun", "tooth"],
      [1651, "合理", "adjective", "reasonable / rational"],
      [1653, "好食", "adjective", "tasty / good to eat"],
      [1655, "好聽", "adjective", "pleasant to hear / sound good"],
      [1656, "成熟", "adjective", "mature / ripe"],
      [1657, "早餐", "noun", "breakfast"],
      [1660, "西面", "localizer", "west side"],
      [1661, "佔", "verb", "occupy / account for / constitute"],
      [1662, "判", "verb", "judge / sentence / decide"],
      [1663, "即將", "function", "about to", "", "prospective_function"],
      [1664, "形象", "noun", "image / figure"],
      [1666, "私家", "adjective", "private / privately owned", "", "relational_modifier"],
      [1671, "居民", "person", "resident / inhabitant"],
      [1672, "直角", "noun", "right angle"],
      [1674, "股份", "noun", "share / stock"],
      [1675, "門口", "localizer", "doorway / entrance"],
      [1676, "既然", "function", "since / as, this being the case", "", "causal_subordinator"],
      [1677, "珍惜", "verb", "treasure / value / cherish"],
      [1678, "相處", "verb", "get along / interact"],
      [1680, "音", "noun", "sound / tone / syllable / reading"],
      [1682, "員", "person", "member / person nominal"],
      [1684, "時效", "noun", "timeliness / validity over time"],
      [1685, "校長", "person", "headmaster / principal"],
      [1686, "核突", "adjective", "ugly / disgusting"],
      [1687, "案件", "noun", "legal case"],
      [1689, "財務", "noun", "financial affairs / finance"],
      [1691, "乾淨", "adjective", "clean / neat"],
      [1692, "唱", "verb", "sing"],
      [1693, "商業", "noun", "commerce / business"],
      [1695, "國內", "localizer", "within the country / domestic sphere"],
      [1699, "提到", "verb", "mention / refer to"],
      [1700, "插", "verb", "insert / stick / pierce / interpose"],
      [1701, "琴晚", "temporal", "last night", "kam4 maan5"],
      [1702, "痛", "adjective", "painful / hurt / ache", "tung3"],
      [1703, "睇住", "verb", "watch / keep an eye on / watch out", "tai2 zyu6"],
      [1704, "窗", "noun", "window", "coeng1"],
      [1708, "溜", "verb", "slip away / escape / skate", "lau6"],
      [1709, "溫暖", "adjective", "warm", "wan1 nyun5"],
      [1710, "罪", "noun", "crime / guilt / sin / fault", "zeoi6"],
      [1712, "詳細", "adjective", "detailed", "coeng4 sai3"],
      [1714, "遇到", "verb", "encounter / come across", "jyu6 dou2"],
      [1715, "電芯", "noun", "battery cell", "din6 sam1"],
      [1716, "嘉賓", "person", "honoured guest", "gaa1 ban1"],
      [1717, "對面", "localizer", "opposite / across from", "deoi3 min6"],
      [1718, "製造", "verb", "manufacture / make", "zai3 zou6"],
      [1720, "黎明", "temporal", "dawn / daybreak", "lai4 ming4"],
      [1721, "橫線", "noun", "horizontal line", "waang4 sin3"],
      [1722, "澳門", "proper", "Macau", "ou3 mun2"],
      [1723, "燒", "verb", "burn / cook / roast", "siu1"],
      [1725, "龜", "noun", "turtle / tortoise", "gwai1"],
      [1726, "尷尬", "adjective", "awkward / embarrassed"],
      [1727, "闊", "adjective", "wide / broad / lavish"],
      [1732, "廳", "place", "hall / reception room / office"],
      [1734, "一號", "noun", "Hong Kong No. 1 typhoon signal", "jat1 hou6", "typhoon_signal_noun"],
      [1735, "九月", "temporal", "September"],
      [1736, "人話", "noun", "human / intelligible / sensible speech", "jan4 waa6", "speech_expression"],
      [1737, "千祈", "adverb", "by all means / whatever you do / definitely not", "", "emphatic_adverb"],
      [1738, "工人", "person", "worker"],
      [1739, "工業", "noun", "industry"],
      [1743, "巴士", "noun", "bus"],
      [1744, "心胸", "noun", "breadth of mind / disposition"],
      [1745, "比例", "noun", "proportion / ratio / scale"],
      [1746, "牛肉", "noun", "beef"],
      [1747, "冬天", "temporal", "winter"]
    ]);
    var PROMOTIONS = Object.freeze(Object.fromEntries(PROMOTION_ROWS.map(([rank, surface, kind, gloss, jyutping = "", syntax = ""]) => [surface, lexicalSpec(rank, kind, gloss, jyutping, syntax)])));
    var RESEARCH_REQUIRED_SURFACES = /* @__PURE__ */ new Set(["罷", "羅", "司", "個萍", "單仲佳", "感", "會會", "碧"]);
    var BLOCKED_ATOMIC_SURFACES = /* @__PURE__ */ new Set([
      "要講",
      "幾咁",
      "養魚",
      "講講",
      "一件",
      "一部份",
      "一棵",
      "十年",
      "今個",
      "先得",
      "好少",
      "好煩",
      "你畫",
      "即要",
      "我識",
      "兩條",
      "返轉頭",
      "食到",
      "就會",
      "幾靚",
      "畫一個",
      "講多次",
      "一套",
      "一部",
      "十二點",
      "去過",
      "多過",
      "好悶",
      "西行",
      "兩日",
      "兩樣",
      "呢種",
      "個頭",
      "唔多",
      "高過",
      "問吓",
      "最右",
      "最底",
      "貼住",
      "開燈",
      "邊有",
      "聽過",
      "一句",
      "五個",
      "半年"
    ]);
    var ALTERNATIVE_SPECS = Object.freeze({
      "架構": Object.freeze([alt(1503, "framework_noun", "noun", "framework / structure"), alt(1503, "construct_verb", "verb", "construct / structure")]),
      "重複": Object.freeze([alt(1506, "repeat_verb", "verb", "repeat / do again", "cung4 fuk1"), alt(1506, "again_adverb", "adverb", "again / repeatedly", "cung4 fuk1")]),
      "根據": Object.freeze([alt(1511, "according_coverb", "coverb", "according to / on the basis of", "", "relation_coverb"), alt(1511, "basis_noun", "noun", "basis / evidence"), alt(1511, "base_on_verb", "verb", "base on")]),
      "教授": Object.freeze([alt(1516, "teach_verb", "verb", "teach / instruct"), alt(1516, "professor_noun", "person", "professor")]),
      "焗": Object.freeze([alt(1517, "bake_force_verb", "verb", "bake / heat / force / restrict", "guk6"), alt(1517, "stuffy_stative", "adjective", "stuffy / hot of air, room, or weather", "guk6")]),
      "費事": Object.freeze([alt(1524, "avoidance_function", "function", "not bother / so as to avoid / not worth the trouble", "fai3 si6", "avoidance_function"), alt(1524, "troublesome_stative", "adjective", "troublesome / time-consuming written-register use", "fai3 si6")]),
      "預": Object.freeze([alt(1530, "anticipate_reserve_verb", "verb", "anticipate / reserve / prepare / include in advance"), alt(1530, "advance_bound", "bound", "pre- / in-advance bound family")]),
      "認識": Object.freeze([alt(1531, "know_verb", "verb", "know / recognize"), alt(1531, "knowledge_noun", "noun", "knowledge / awareness")]),
      "錄音": Object.freeze([alt(1535, "record_sound_verb", "verb", "record sound"), alt(1535, "recording_noun", "noun", "sound recording")]),
      "類": Object.freeze([alt(1543, "kind_noun", "noun", "kind / type"), alt(1543, "category_classifier", "classifier", "classifier / category function")]),
      "叉": Object.freeze([alt(1549, "fork_noun", "noun", "fork / prong"), alt(1549, "fork_pierce_verb", "verb", "fork / pierce / cross"), alt(1549, "food_bound", "bound", "bound food-name family")]),
      "白": Object.freeze([alt(1563, "white_stative", "adjective", "white / clear / plain"), alt(1563, "in_vain_free_adverb", "adverb", "in vain / for free"), alt(1563, "surname", "proper", "surname Baak")]),
      "封": Object.freeze([alt(1583, "seal_bestow_verb", "verb", "seal / bestow"), alt(1583, "sealed_item_classifier", "classifier", "classifier for letters / sealed items")]),
      "站": Object.freeze([alt(1595, "station_noun", "noun", "station / site / branch"), alt(1595, "stand_halt_verb", "verb", "stand / halt")]),
      "能": Object.freeze([alt(1596, "ability_function", "function", "formal can / be able to", "", "modal_ability"), alt(1596, "ability_energy_bound", "bound", "ability / energy bound nominal family")]),
      "參考": Object.freeze([alt(1599, "consult_verb", "verb", "consult / refer to"), alt(1599, "reference_noun", "noun", "reference / basis for comparison")]),
      "期": Object.freeze([alt(1611, "period_noun", "noun", "period / term"), alt(1611, "period_classifier", "classifier", "classifier / measure for issues, courses, or periods")]),
      "損失": Object.freeze([alt(1619, "loss_noun", "noun", "loss"), alt(1619, "lose_verb", "verb", "lose / suffer a loss")]),
      "運": Object.freeze([alt(1622, "transport_use_verb", "verb", "move / transport / use"), alt(1622, "luck_noun", "noun", "luck / fortune / fate")]),
      "嘗試": Object.freeze([alt(1623, "try_verb", "verb", "try / attempt"), alt(1623, "attempt_noun", "noun", "attempt")]),
      "監管": Object.freeze([alt(1624, "supervise_verb", "verb", "supervise / regulate"), alt(1624, "supervision_noun", "noun", "supervision / regulation")]),
      "認真": Object.freeze([alt(1627, "serious_stative", "adjective", "serious / earnest"), alt(1627, "seriously_adverb", "adverb", "seriously / earnestly")]),
      "熱": Object.freeze([alt(1630, "hot_stative", "adjective", "hot"), alt(1630, "heat_noun", "noun", "heat")]),
      "關": Object.freeze([alt(1636, "close_concern_verb", "verb", "close / turn off / concern"), alt(1636, "pass_barrier_noun", "noun", "pass / barrier"), alt(1636, "surname", "proper", "surname Kwan")]),
      "正面": Object.freeze([alt(1649, "front_noun", "noun", "front / front side"), alt(1649, "positive_front_stative", "adjective", "positive / direct / front-facing")]),
      "生產": Object.freeze([alt(1650, "produce_verb", "verb", "produce / manufacture / give birth"), alt(1650, "production_noun", "noun", "production / childbirth")]),
      "曲線": Object.freeze([alt(1658, "curve_noun", "noun", "curve / curved line"), alt(1658, "indirect_stative", "adjective", "indirect / roundabout / figuratively curved")]),
      "投訴": Object.freeze([alt(1665, "complain_verb", "verb", "complain / file a complaint"), alt(1665, "complaint_noun", "noun", "complaint")]),
      "制": Object.freeze([alt(1669, "agree_accept_verb", "verb", "agree / accept / be willing"), alt(1669, "system_control_bound", "bound", "system / control / regulate formal-bound family")]),
      "空": Object.freeze([alt(1673, "empty_stative", "adjective", "empty / vacant"), alt(1673, "air_space_noun", "noun", "air / sky / space"), alt(1673, "empty_verb", "verb", "empty / make vacant")]),
      "相對": Object.freeze([alt(1679, "relative_stative", "adjective", "relative / opposite"), alt(1679, "relatively_adverb", "adverb", "relatively"), alt(1679, "face_oppose_verb", "verb", "face / oppose where syntax supports it")]),
      "破壞": Object.freeze([alt(1688, "damage_verb", "verb", "damage / destroy"), alt(1688, "destruction_noun", "noun", "destruction / damage")]),
      "報告": Object.freeze([alt(1698, "report_noun", "noun", "report"), alt(1698, "report_verb", "verb", "report / inform / present")]),
      "結": Object.freeze([alt(1705, "tie_form_verb", "verb", "tie / bind / form / settle", "git3"), alt(1705, "knot_bond_noun", "noun", "knot / bond", "git3")]),
      "補充": Object.freeze([alt(1711, "supplement_verb", "verb", "replenish / supplement / add", "bou2 cung1"), alt(1711, "supplement_noun", "noun", "supplement / additional comment", "bou2 cung1")]),
      "誇張": Object.freeze([alt(1713, "exaggerate_verb", "verb", "exaggerate", "kwaa1 zoeng1"), alt(1713, "exaggerated_stative", "adjective", "exaggerated / overstated", "kwaa1 zoeng1")]),
      "增長": Object.freeze([alt(1719, "grow_increase_verb", "verb", "grow / increase", "zang1 zoeng2"), alt(1719, "growth_noun", "noun", "growth / increase", "zang1 zoeng2")]),
      "響": Object.freeze([alt(1729, "sound_verb", "verb", "sound / make a sound", "hoeng2", "sound_verb"), alt(1729, "locative_coverb", "coverb", "at / in / on", "hoeng2", "locative_relation_coverb"), alt(1729, "locative_existential_verb", "verb", "be at / in", "hoeng2", "locative_existential_verb")]),
      "爛": Object.freeze([alt(1730, "rotten_stative", "adjective", "rotten / broken / messy"), alt(1730, "utterly_adverb", "adverb", "utterly / thoroughly as degree intensifier", "", "degree_adverb")]),
      "及": Object.freeze([alt(1741, "and_function", "function", "formal and / as well as", "kap6", "coordination_relation"), alt(1741, "reach_involve_bound", "bound", "formal reach / involve morpheme family", "kap6")]),
      "反對": Object.freeze([alt(1742, "oppose_verb", "verb", "oppose"), alt(1742, "opposition_noun", "noun", "opposition")]),
      "出版": Object.freeze([alt(1748, "publish_verb", "verb", "publish"), alt(1748, "publication_noun", "noun", "publication / publishing process")]),
      "倒": Object.freeze([alt(1507, "fall_dou2", "verb", "fall / topple", "dou2"), alt(1507, "reverse_dou3", "verb", "reverse / invert / turn backwards", "dou3")]),
      "假": Object.freeze([alt(1513, "false_gaa2", "adjective", "false / fake", "gaa2"), alt(1513, "leave_gaa3", "noun", "leave / holiday", "gaa3")]),
      "牌": Object.freeze([alt(1521, "nominal_paai4", "noun", "brand / sign / card nominal family", "paai4"), alt(1521, "nominal_paai2", "noun", "brand / sign / card nominal reading variant", "paai2")]),
      "貼": Object.freeze([alt(1525, "stick_tip3", "verb", "stick / paste / keep close / subsidize", "tip3"), alt(1525, "tip_prediction_tip1", "verb", "loan-derived tip / prediction family", "tip1")]),
      "舖頭": Object.freeze([alt(1533, "shop", "place", "shop / store", "pou3 tau2", "place_noun")]),
      "壞": Object.freeze([alt(1539, "bad_stative", "adjective", "bad / spoiled / broken", "waai6"), alt(1539, "become_bad_verb", "verb", "break / go bad / become spoiled", "waai6", "change_of_state_verb")]),
      "犯": Object.freeze([alt(1562, "offend_faan6", "verb", "offend / violate / commit an offence", "faan6"), alt(1562, "offender_faan2", "person", "offender / criminal nominal family", "faan2")]),
      "夾": Object.freeze([alt(1575, "wedge_mix_gaap3", "verb", "sandwich / wedge / mix / cooperate", "gaap3"), alt(1575, "clip_gaap2", "noun", "clip / folder", "gaap2")]),
      "拗": Object.freeze([alt(1580, "bend_aau2", "verb", "bend / break", "aau2"), alt(1580, "argue_aau3", "verb", "argue / act obstinately", "aau3")]),
      "柱": Object.freeze([alt(1585, "pillar_cyu5", "noun", "pillar / column", "cyu5")]),
      "細路仔": Object.freeze([alt(1602, "child", "person", "child / kid", "sai3 lou6 zai2")]),
      "雀": Object.freeze([alt(1605, "bird_zoek3", "noun", "bird, standard reading", "zoek3"), alt(1605, "bird_zoek2", "noun", "bird, independently supported variant", "zoek2")]),
      "掣": Object.freeze([alt(1610, "switch_zai3", "noun", "button / switch / brake", "zai3")]),
      "鋪": Object.freeze([alt(1631, "spread_pou1", "verb", "spread / lay / pave", "pou1"), alt(1631, "shop_pou3", "noun", "shop / store", "pou3"), alt(1631, "classifier_pou3", "classifier", "classifier family", "pou3")]),
      "興": Object.freeze([alt(1724, "flourish_hing1", "verb", "flourish / become popular", "hing1"), alt(1724, "interest_hing3", "noun", "interest / mood root", "hing3")]),
      "卡": Object.freeze([alt(1750, "card_kaat1", "noun", "card", "kaat1"), alt(1750, "classifier_kaa1", "classifier", "classifier for train compartments / related units", "kaa1"), alt(1750, "carat_kaa1", "measure", "carat / measure family", "kaa1")])
    });
    var MULTI_SURFACES = /* @__PURE__ */ new Set(["架構", "重複", "根據", "教授", "焗", "費事", "預", "認識", "錄音", "類", "叉", "白", "封", "站", "能", "參考", "期", "損失", "運", "嘗試", "監管", "認真", "熱", "關", "正面", "生產", "曲線", "投訴", "制", "空", "相對", "破壞", "報告", "結", "補充", "誇張", "增長", "響", "爛", "及", "反對", "出版"]);
    var READING_SPLIT_SURFACES = /* @__PURE__ */ new Set(["倒", "假", "牌", "貼", "舖頭", "壞", "犯", "夾", "拗", "柱", "細路仔", "雀", "掣", "鋪", "興", "卡"]);
    var CANDIDATE_ONLY_SURFACES = new Set(Object.keys(ALTERNATIVE_SPECS));
    var DEFAULT_READING_OVERRIDES = Object.freeze({
      "重複": "cung4 fuk1",
      "唪": "fung2",
      "郭富城": "gwok3 fu3 sing4",
      "焗": "guk6",
      "費事": "fai3 si6",
      "貼": "tip3",
      "舖頭": "pou3 tau2",
      "壞": "waai6",
      "犯": "faan6",
      "自信": "zi6 seon3",
      "夾": "gaap3",
      "柱": "cyu5",
      "閂": "saan1",
      "骨折": "gwat1 zit3",
      "細路仔": "sai3 lou6 zai2",
      "雀": "zoek3",
      "幾點": "gei2 dim2",
      "掣": "zai3",
      "琴晚": "kam4 maan5",
      "痛": "tung3",
      "睇住": "tai2 zyu6",
      "窗": "coeng1",
      "溜": "lau6",
      "溫暖": "wan1 nyun5",
      "罪": "zeoi6",
      "補充": "bou2 cung1",
      "詳細": "coeng4 sai3",
      "誇張": "kwaa1 zoeng1",
      "遇到": "jyu6 dou2",
      "電芯": "din6 sam1",
      "嘉賓": "gaa1 ban1",
      "對面": "deoi3 min6",
      "製造": "zai3 zou6",
      "增長": "zang1 zoeng2",
      "黎明": "lai4 ming4",
      "橫線": "waang4 sin3",
      "澳門": "ou3 mun2",
      "燒": "siu1",
      "龜": "gwai1",
      "一號": "jat1 hou6",
      "人話": "jan4 waa6",
      "及": "kap6",
      "響": "hoeng2"
    });
    function isNeutralLexicalEntry(entry) {
      return Boolean(entry) && entry.label === "lex" && entry.pos === "lexical_item" && entry.syntax === "lexical_item";
    }
    function applyReviewedEntries(entries) {
      if (!Array.isArray(entries)) throw new TypeError("ranks 1501–1750 reviewed overlay requires an entry array");
      return entries.map(([surface, entry]) => {
        const promotion = PROMOTIONS[surface];
        let next = entry;
        if (promotion && isNeutralLexicalEntry(next)) {
          next = {
            ...next,
            label: promotion.label,
            pos: promotion.pos,
            syntax: promotion.syntax,
            jyutping: promotion.jyutping || next.jyutping || "",
            note: promotion.gloss,
            provenance: {
              kind: "reviewed_lexical_promotion",
              source: SOURCE,
              rank: promotion.rank,
              pronunciation_status: promotion.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted",
              prior_provenance: next.provenance || null
            }
          };
        }
        const reviewedReading = DEFAULT_READING_OVERRIDES[surface];
        if (reviewedReading && next && next.jyutping !== reviewedReading) {
          next = {
            ...next,
            jyutping: reviewedReading,
            provenance: {
              kind: isNeutralLexicalEntry(next) ? "reviewed_candidate_default_pronunciation" : "reviewed_default_pronunciation_correction",
              source: SOURCE,
              pronunciation_status: "reviewed_explicit_reading",
              prior_provenance: next.provenance || null
            }
          };
        }
        return [surface, next];
      });
    }
    function defaultAnalysis(surface, entry) {
      return Object.freeze({
        id: `lex:${surface}:default`,
        label: entry.label || "neutral",
        pos: entry.pos || "lexical_item",
        jyutping: entry.jyutping || "",
        syntax: entry.syntax || "lexical_candidate",
        senses: Object.freeze([{ gloss: entry.note || "existing runtime default preserved" }]),
        provenance: Object.freeze({ kind: "existing_runtime_default_preserved", source: "v0.5.234 token lexicon before ranks 1501–1750 alternatives", prior_provenance: entry.provenance || null })
      });
    }
    function reviewedAlternative(surface, spec, defaultEntry) {
      return Object.freeze({
        id: `lex:${surface}:r${spec.rank}:${spec.suffix}`,
        label: spec.label,
        pos: spec.pos,
        jyutping: spec.jyutping || defaultEntry.jyutping || "",
        syntax: spec.syntax,
        senses: Object.freeze([{ gloss: spec.gloss }]),
        provenance: Object.freeze({ kind: "reviewed_lexical_analysis", source: SOURCE, rank: spec.rank, pronunciation_status: spec.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted" })
      });
    }
    function buildExplicitAnalyses(entries) {
      const defaults = new Map(entries || []);
      const out = /* @__PURE__ */ Object.create(null);
      for (const [surface, specs] of Object.entries(ALTERNATIVE_SPECS)) {
        const entry = defaults.get(surface);
        if (!entry) throw new Error(`ranks 1501–1750 explicit analyses reference missing runtime surface: ${surface}`);
        const rows = [defaultAnalysis(surface, entry), ...specs.map((spec) => reviewedAlternative(surface, spec, entry))];
        const seen = /* @__PURE__ */ new Set();
        for (const row of rows) {
          if (!row.jyutping) throw new Error(`${row.id}: explicit lexical analysis requires non-empty jyutping`);
          if (seen.has(row.id)) throw new Error(`${row.id}: duplicate ranks 1501–1750 stable analysis ID`);
          seen.add(row.id);
        }
        out[surface] = Object.freeze(rows);
      }
      return Object.freeze(out);
    }
    module2.exports = Object.freeze({
      SOURCE,
      PROMOTIONS,
      RESEARCH_REQUIRED_SURFACES,
      BLOCKED_ATOMIC_SURFACES,
      MULTI_SURFACES,
      READING_SPLIT_SURFACES,
      ALTERNATIVE_SPECS,
      CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES,
      isNeutralLexicalEntry,
      applyReviewedEntries,
      buildExplicitAnalyses
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1501-1750-runtime-policy.js
var require_cifu_r1501_1750_runtime_policy = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1501-1750-runtime-policy.js"(exports2, module2) {
    "use strict";
    var reviewed = require_cifu_r1501_1750_reviewed();
    var MANDARIN_ONLY_SURFACES = /* @__PURE__ */ new Set();
    function applyRuntimePolicy(entries) {
      return reviewed.applyReviewedEntries(entries).filter(([surface]) => !MANDARIN_ONLY_SURFACES.has(surface));
    }
    module2.exports = Object.freeze({
      SOURCE: reviewed.SOURCE,
      PROMOTIONS: reviewed.PROMOTIONS,
      RESEARCH_REQUIRED_SURFACES: reviewed.RESEARCH_REQUIRED_SURFACES,
      BLOCKED_ATOMIC_SURFACES: reviewed.BLOCKED_ATOMIC_SURFACES,
      CANDIDATE_ONLY_SURFACES: reviewed.CANDIDATE_ONLY_SURFACES,
      DEFAULT_READING_OVERRIDES: reviewed.DEFAULT_READING_OVERRIDES,
      MANDARIN_ONLY_SURFACES,
      applyRuntimePolicy
    });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1751-2000-reviewed.js
var require_cifu_r1751_2000_reviewed = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1751-2000-reviewed.js"(exports2, module2) {
    "use strict";
    var SOURCE = "docs/research/CIFU-R1751-2000-LEXICAL-ADJUDICATION.md";
    var KIND = Object.freeze({
      noun: ["what", "noun", "common_noun"],
      person: ["who", "noun", "person_noun"],
      place: ["where", "noun", "place_noun"],
      verb: ["doing", "verb", "verb"],
      adjective: ["like", "adjective", "stative_predicate"],
      adverb: ["how", "adverb", "adverb"],
      function: ["func", "function", "function"],
      classifier: ["what", "classifier", "classifier"],
      measure: ["what", "measure", "measure_word"],
      bound: ["func", "bound", "bound_morpheme"],
      coverb: ["func", "coverb", "coverb"],
      proper: ["what", "proper_noun", "proper_name"],
      localizer: ["where", "localizer", "localizer"],
      temporal: ["when", "noun", "temporal_expression"],
      interjection: ["func", "interjection", "interjection"]
    });
    function lexicalSpec(rank, kind, gloss, jyutping = "", syntax = "") {
      const meta = KIND[kind];
      if (!meta) throw new Error("unknown lexical kind " + kind);
      return Object.freeze({ rank, kind, gloss, jyutping, label: meta[0], pos: meta[1], syntax: syntax || meta[2] });
    }
    var PROMOTION_ROWS = Object.freeze([
      [
        1751,
        "司長",
        "person",
        "person/title noun: director, department/bureau head",
        "si1 zoeng2"
      ],
      [
        1753,
        "本地",
        "localizer",
        "locality/common noun “the local area; locality”",
        "bun2 dei6"
      ],
      [
        1755,
        "正版",
        "noun",
        "noun “authorized/genuine edition or copy”",
        "zing3 baan2"
      ],
      [
        1756,
        "甩",
        "verb",
        "verb: come off / detach / lose / get rid of / escape",
        "lat1"
      ],
      [
        1759,
        "在",
        "coverb",
        "formal/written locative relation/existential material; exclude Mandarin progressive `在 + VP`",
        "zoi6"
      ],
      [
        1765,
        "忙",
        "adjective",
        "stative/predicate “busy”",
        "mong4"
      ],
      [
        1766,
        "成年",
        "verb",
        "verb/predicate “come of age; be legally adult”",
        "sing4 nin4"
      ],
      [
        1767,
        "老細",
        "person",
        "person/role noun “boss; proprietor”",
        "lou5 sai3"
      ],
      [
        1768,
        "自從",
        "function",
        "temporal relational/connective “since; ever since”",
        "zi6 cung4"
      ],
      [
        1769,
        "初頭",
        "temporal",
        "temporal expression “at first; initially”",
        "co1 tau4"
      ],
      [
        1772,
        "足夠",
        "adjective",
        "stative/predicate “enough; sufficient”",
        "zuk1 gau3"
      ],
      [
        1774,
        "事業",
        "noun",
        "noun “career; undertaking; cause/enterprise”",
        "si6 jip6"
      ],
      [
        1778,
        "忽然",
        "adverb",
        "adverb “suddenly”",
        "fat1 jin4"
      ],
      [
        1781,
        "東面",
        "localizer",
        "spatial/locality noun “east side”",
        "dung1 min6"
      ],
      [
        1782,
        "欣賞",
        "verb",
        "verb “appreciate; admire; enjoy”",
        "jan1 soeng2"
      ],
      [
        1783,
        "股價",
        "noun",
        "finance noun “share/stock price”",
        "gu2 gaa3"
      ],
      [
        1785,
        "長遠",
        "adjective",
        "stative/property “long-term; far-reaching”",
        "coeng4 jyun5"
      ],
      [
        1788,
        "重心",
        "noun",
        "noun “centre of gravity; central focus”",
        "zung6 sam1"
      ],
      [
        1789,
        "哲學",
        "noun",
        "domain noun “philosophy”",
        "zit3 hok6"
      ],
      [
        1790,
        "家人",
        "person",
        "person/kin collective noun “family member(s)”",
        "gaa1 jan4"
      ],
      [
        1791,
        "晏晝",
        "temporal",
        "temporal expression “afternoon”",
        "aan3 zau3"
      ],
      [
        1793,
        "窄",
        "adjective",
        "stative “narrow”",
        "zaak3"
      ],
      [
        1794,
        "粉",
        "noun",
        "noun “powder; flour/starch food/noodles; cosmetic powder”",
        "fan2"
      ],
      [
        1795,
        "做法",
        "noun",
        "noun “method; way of doing; practice”",
        "zou6 faat3"
      ],
      [
        1797,
        "唯一",
        "adjective",
        "exclusive property/modifier “only; sole”",
        "wai4 jat1"
      ],
      [
        1798,
        "唱片",
        "noun",
        "noun “record; music album”",
        "coeng3 pin2"
      ],
      [
        1799,
        "婚姻",
        "noun",
        "noun “marriage; matrimony”",
        "fan1 jan1"
      ],
      [
        1804,
        "現象",
        "noun",
        "noun “phenomenon”",
        "jin6 zoeng6"
      ],
      [
        1808,
        "粗口",
        "noun",
        "noun “swear words; obscene language”",
        "cou1 hau2"
      ],
      [
        1809,
        "細妹",
        "person",
        "kin noun “younger sister”",
        "sai3 mui2"
      ],
      [
        1811,
        "規則",
        "noun",
        "noun “rule; regulation”",
        "kwai1 zak1"
      ],
      [
        1814,
        "創意",
        "noun",
        "noun “creativity; creative idea”",
        "cong3 ji3"
      ],
      [
        1818,
        "換",
        "verb",
        "verb “change; exchange; replace”",
        "wun6"
      ],
      [
        1819,
        "普通話",
        "noun",
        "language-name noun “Putonghua; Mandarin”",
        "pou2 tung1 waa2"
      ],
      [
        1820,
        "普遍",
        "adjective",
        "stative “general; widespread; common”",
        "pou2 pin3"
      ],
      [
        1821,
        "評價",
        "verb",
        "verb “evaluate; assess”; no noun promoted from Cifu gloss alone",
        "ping4 gaa3"
      ],
      [
        1822,
        "意義",
        "noun",
        "noun “meaning; significance”",
        "ji3 ji6"
      ],
      [
        1823,
        "愛情",
        "noun",
        "noun “romantic love; love relationship”",
        "oi3 cing4"
      ],
      [
        1824,
        "業",
        "noun",
        "noun/bound nominal “business; occupation; profession; field”",
        "jip6"
      ],
      [
        1825,
        "業主",
        "person",
        "person/role noun “owner; proprietor”",
        "jip6 zyu2"
      ],
      [
        1828,
        "罪行",
        "noun",
        "legal noun “crime; offence”",
        "zeoi6 hang4"
      ],
      [
        1830,
        "遊戲",
        "noun",
        "noun “game; play”",
        "jau4 hei3"
      ],
      [
        1831,
        "電訊",
        "noun",
        "domain noun “telecommunications”",
        "din6 seon3"
      ],
      [
        1832,
        "鼓勵",
        "verb",
        "verb “encourage”",
        "gu2 lai6"
      ],
      [
        1833,
        "嘔",
        "verb",
        "verb “vomit; retch”",
        "au2"
      ],
      [
        1834,
        "實際上",
        "adverb",
        "lexical adverbial “in fact; in reality; in practice”",
        "sat6 zai3 soeng6"
      ],
      [
        1836,
        "樓下",
        "localizer",
        "locality noun “downstairs; area below”",
        "lau4 haa6"
      ],
      [
        1837,
        "瘦",
        "adjective",
        "stative “thin; lean”",
        "sau3"
      ],
      [
        1838,
        "賭",
        "verb",
        "verb “gamble; bet”",
        "dou2"
      ],
      [
        1839,
        "踩",
        "verb",
        "verb “step/tread/stamp/pedal”",
        "caai2"
      ],
      [
        1840,
        "燕梳",
        "noun",
        "Cantonese loan noun “insurance/insurance policy”",
        "jin3 so1"
      ],
      [
        1841,
        "遲",
        "adjective",
        "stative “late; delayed; slow”",
        "ci4"
      ],
      [
        1843,
        "戲院",
        "place",
        "place noun “cinema; theatre”",
        "hei3 jyun2"
      ],
      [
        1844,
        "擦紙膠",
        "noun",
        "noun “eraser; rubber”; independently lexicalized Cantonese compound",
        "caat3 zi2 gaau1"
      ],
      [
        1846,
        "聯絡",
        "verb",
        "verb “contact; get in touch with”; no noun promoted without direct lexical evidence",
        "lyun4 lok3"
      ],
      [
        1847,
        "講法",
        "noun",
        "noun “way of saying; formulation; account/claim”",
        "gong2 faat3"
      ],
      [
        1850,
        "禮拜三",
        "temporal",
        "temporal expression “Wednesday”",
        "lai5 baai3 saam1"
      ],
      [
        1851,
        "禮拜六",
        "temporal",
        "temporal expression “Saturday”",
        "lai5 baai3 luk6"
      ],
      [
        1854,
        "轉頭",
        "temporal",
        "lexical Cantonese temporal adverb “soon; shortly afterwards”; literal turn-head structure remains productive",
        "zyun3 tau4"
      ],
      [
        1855,
        "額",
        "noun",
        "noun/bound nominal “forehead; amount/quota/limit”",
        "ngaak6"
      ],
      [
        1856,
        "鯉",
        "noun",
        "noun “carp”",
        "lei5"
      ],
      [
        1857,
        "穩定",
        "adjective",
        "stative/property “stable; steady”",
        "wan2 ding6"
      ],
      [
        1859,
        "嚴重",
        "adjective",
        "stative/property “serious; severe”",
        "jim4 zung6"
      ],
      [
        1860,
        "屬於",
        "verb",
        "relational verb “belong to; be classified as”",
        "suk6 jyu1"
      ],
      [
        1863,
        "顯示",
        "verb",
        "verb “show; display; demonstrate”; noun not promoted from corpus VN tagging alone",
        "hin2 si6"
      ],
      [
        1864,
        "一模一樣",
        "adjective",
        "lexicalized stative/idiomatic expression “exactly the same; identical”",
        "jat1 mou4 jat1 joeng6"
      ],
      [
        1867,
        "人生",
        "noun",
        "abstract noun “human life; a person's life”",
        "jan4 sang1"
      ],
      [
        1869,
        "上堂",
        "verb",
        "lexical verb “attend class; give a lesson”; productive internal structure remains available",
        "soeng5 tong4"
      ],
      [
        1872,
        "中國人",
        "person",
        "person/demonym noun “Chinese person/people”",
        "zung1 gwok3 jan4"
      ],
      [
        1873,
        "五月",
        "temporal",
        "temporal noun/month name “May”",
        "ng5 jyut6"
      ],
      [
        1874,
        "內心",
        "noun",
        "abstract noun “inner feelings/thoughts; inner self”",
        "noi6 sam1"
      ],
      [
        1875,
        "反映",
        "verb",
        "verb “reflect; reveal; report/represent”",
        "faan2 jing2"
      ],
      [
        1876,
        "天堂",
        "place",
        "place/abstract noun “heaven; paradise”",
        "tin1 tong4"
      ],
      [
        1877,
        "太陽",
        "noun",
        "celestial noun “the sun”",
        "taai3 joeng4"
      ],
      [
        1878,
        "心態",
        "noun",
        "abstract noun “mindset; mentality; attitude”",
        "sam1 taai3"
      ],
      [
        1882,
        "仙人掌",
        "noun",
        "plant noun “cactus”",
        "sin1 jan4 zoeng2"
      ],
      [
        1883,
        "以往",
        "temporal",
        "temporal expression/adverb “in the past; previously”",
        "ji5 wong5"
      ],
      [
        1885,
        "出席",
        "verb",
        "verb “attend; be present”",
        "ceot1 zik6"
      ],
      [
        1886,
        "出邊",
        "localizer",
        "spatial/locality noun “outside”",
        "ceot1 bin6"
      ],
      [
        1888,
        "可憐",
        "adjective",
        "stative/property “pitiful; pitiable; miserable”",
        "ho2 lin4"
      ],
      [
        1889,
        "失",
        "verb",
        "verb/bound verbal family “lose; miss; fail; lapse”",
        "sat1"
      ],
      [
        1890,
        "民主",
        "noun",
        "abstract/political noun “democracy”",
        "man4 zyu2"
      ],
      [
        1891,
        "民建聯",
        "proper",
        "proper organization abbreviation: Democratic Alliance for the Betterment and Progress of Hong Kong",
        "man4 gin3 lyun4"
      ],
      [
        1892,
        "生物",
        "noun",
        "count/domain noun “living organism; biological entity”",
        "sang1 mat6"
      ],
      [
        1894,
        "全名",
        "noun",
        "noun “full name”",
        "cyun4 meng2"
      ],
      [
        1896,
        "合法",
        "adjective",
        "stative/property “legal; lawful”",
        "hap6 faat3"
      ],
      [
        1897,
        "吋",
        "measure",
        "measure/classifier “inch”",
        "cyun3"
      ],
      [
        1898,
        "同時",
        "temporal",
        "temporal/additive adverbial “simultaneously; at the same time; additionally”",
        "tung4 si4"
      ],
      [
        1900,
        "地球",
        "proper",
        "celestial/common proper noun “Earth; the planet Earth”",
        "dei6 kau4"
      ],
      [
        1901,
        "好人",
        "person",
        "person noun “good/decent person”",
        "hou2 jan4"
      ],
      [
        1902,
        "好味",
        "adjective",
        "lexicalized stative “tasty; delicious”",
        "hou2 mei6"
      ],
      [
        1905,
        "年青",
        "adjective",
        "stative/property “young; youthful”",
        "nin4 cing1"
      ],
      [
        1906,
        "年齡",
        "noun",
        "abstract/count noun “age”",
        "nin4 ling4"
      ],
      [
        1907,
        "式",
        "noun",
        "noun/bound nominal “form; type; style; pattern”",
        "sik1"
      ],
      [
        1908,
        "攰",
        "adjective",
        "stative/predicate “tired”",
        "gui6"
      ],
      [
        1912,
        "老公",
        "person",
        "kin noun “husband”",
        "lou5 gung1"
      ],
      [
        1915,
        "行路",
        "verb",
        "lexical verb expression “walk; travel on foot”",
        "haang4 lou6"
      ],
      [
        1919,
        "別人",
        "noun",
        "formal person/pronominal noun “other person/people; others”",
        "bit6 jan4"
      ],
      [
        1920,
        "完整",
        "adjective",
        "stative/property “complete; intact”",
        "jyun4 zing2"
      ],
      [
        1922,
        "使錢",
        "verb",
        "lexical verb expression “spend money”",
        "sai2 cin2"
      ],
      [
        1923,
        "依然",
        "adverb",
        "adverb “still; as before”",
        "ji1 jin4"
      ],
      [
        1926,
        "呢邊",
        "localizer",
        "deictic/locality expression “this side; here”",
        "ni1 bin1"
      ],
      [
        1931,
        "板",
        "noun",
        "noun/bound nominal “board; plank; plate; slab; panel”",
        "baan2"
      ],
      [
        1933,
        "侮辱",
        "verb",
        "verb “insult; humiliate”",
        "mou5 juk6"
      ],
      [
        1934,
        "保護",
        "verb",
        "verb “protect; safeguard”",
        "bou2 wu6"
      ],
      [
        1935,
        "哈哈",
        "interjection",
        "interjection/onomatopoeic laughter expression",
        "haa1 haa1"
      ],
      [
        1938,
        "星",
        "noun",
        "noun “star; celestial body”",
        "sing1"
      ],
      [
        1939,
        "洲",
        "noun",
        "geographic noun/bound nominal “continent; island/land mass”",
        "zau1"
      ],
      [
        1942,
        "耶穌",
        "proper",
        "proper person/religious name “Jesus”",
        "je4 sou1"
      ],
      [
        1943,
        "背後",
        "localizer",
        "spatial/locality expression “behind; at the back”",
        "bui3 hau6"
      ],
      [
        1945,
        "飛碟",
        "noun",
        "noun “flying saucer; frisbee”",
        "fei1 dip2"
      ],
      [
        1946,
        "個案",
        "noun",
        "count/abstract noun “case; individual case”",
        "go3 on3"
      ],
      [
        1949,
        "唔知幾",
        "adverb",
        "lexical Cantonese degree adverbial “very; extremely”",
        "m4 zi1 gei2"
      ],
      [
        1950,
        "孭",
        "verb",
        "verb “carry on the back/shoulder”",
        "me1"
      ],
      [
        1952,
        "特色",
        "noun",
        "noun “distinctive feature/characteristic”",
        "dak6 sik1"
      ],
      [
        1953,
        "特殊",
        "adjective",
        "stative/property “special; unusual; particular”",
        "dak6 syu4"
      ],
      [
        1954,
        "狹窄",
        "adjective",
        "stative/property “narrow; cramped”",
        "haap6 zaak3"
      ],
      [
        1955,
        "退",
        "verb",
        "verb “retreat; withdraw; recede; decline”",
        "teoi3"
      ],
      [
        1956,
        "針對",
        "verb",
        "relational/transitive verb “target; direct at; address specifically”",
        "zam1 deoi3"
      ],
      [
        1960,
        "唯有",
        "adverb",
        "adverb/function “can only; have no choice but”",
        "wai4 jau5"
      ],
      [
        1962,
        "得滯",
        "function",
        "Cantonese excessive-degree function/suffix “too; excessively” following a predicate",
        "dak1 zai6"
      ],
      [
        1963,
        "梳打",
        "noun",
        "Cantonese loan noun “soda”",
        "so1 daa2"
      ],
      [
        1965,
        "清晰",
        "adjective",
        "stative/property “clear; distinct”",
        "cing1 sik1"
      ],
      [
        1967,
        "船",
        "noun",
        "count noun “boat; ship; vessel”",
        "syun4"
      ],
      [
        1968,
        "規定",
        "verb",
        "formal verb “stipulate; prescribe; set/fix”",
        "kwai1 ding6"
      ],
      [
        1969,
        "貨櫃",
        "noun",
        "count noun “freight/shipping container”",
        "fo3 gwai6"
      ],
      [
        1970,
        "責任",
        "noun",
        "abstract/count noun “responsibility; duty; liability”",
        "zaak3 jam6"
      ],
      [
        1971,
        "連埋",
        "function",
        "lexical/constructional inclusion expression “together with; including; add/include as well”",
        "lin4 maai4"
      ],
      [
        1973,
        "單位",
        "noun",
        "count/abstract noun “unit; work unit; apartment/unit of measure”",
        "daan1 wai2"
      ],
      [
        1975,
        "期間",
        "temporal",
        "temporal noun “period; during the period”",
        "kei4 gaan1"
      ],
      [
        1976,
        "無謂",
        "adjective",
        "stative/property “pointless; needless; not worthwhile”",
        "mou4 wai6"
      ],
      [
        1980,
        "開會",
        "verb",
        "lexical verb expression “hold/attend a meeting”",
        "hoi1 wui2"
      ],
      [
        1982,
        "傳媒",
        "noun",
        "collective/domain noun “media; mass media”",
        "cyun4 mui4"
      ],
      [
        1987,
        "運動",
        "verb",
        "noun/event “exercise; sport; movement/campaign”; no free verb promoted without direct Cantonese verbal syntax",
        "wan6 dung6"
      ],
      [
        1988,
        "圖標",
        "noun",
        "count/domain noun “icon; graphical symbol”",
        "tou4 biu1"
      ],
      [
        1990,
        "監察",
        "verb",
        "formal verb “monitor; supervise; inspect”",
        "gaam1 caat3"
      ],
      [
        1992,
        "網球",
        "noun",
        "sport noun “tennis”",
        "mong5 kau4"
      ],
      [
        1994,
        "價",
        "noun",
        "noun/bound nominal “price; value; cost”",
        "gaa3"
      ],
      [
        1995,
        "嘴",
        "noun",
        "noun “mouth; beak; spout”; **exclude Cifu's Mandarin classifier `張` analysis**",
        "zeoi2"
      ],
      [
        1997,
        "數學",
        "noun",
        "domain noun “mathematics”",
        "sou3 hok6"
      ],
      [
        1998,
        "擁有",
        "verb",
        "verb “possess; own; have”",
        "jung2 jau5"
      ],
      [
        1999,
        "輸",
        "verb",
        "verb “lose; transport/transmit” family, with ordinary free Cantonese default “lose”",
        "syu1"
      ],
      [
        2e3,
        "隨時",
        "adverb",
        "adverb “at any time; possibly/liable to at any time”",
        "ceoi4 si4"
      ]
    ]);
    var PROMOTIONS = Object.freeze(Object.fromEntries(PROMOTION_ROWS.map(([rank, surface, kind, gloss, jyutping = "", syntax = ""]) => [surface, lexicalSpec(rank, kind, gloss, jyutping, syntax)])));
    var RESEARCH_REQUIRED_SURFACES = /* @__PURE__ */ new Set(["山個", "猷"]);
    var BLOCKED_ATOMIC_SURFACES = /* @__PURE__ */ new Set(["正上方", "再行", "好多人", "好快", "好近", "好遠", "我問", "兩點", "第二個韻", "就講", "幾日", "當你", "當我", "幫到", "講開", "轉右", "邊間", "聽到", "九十度", "十一點", "下畫", "加返", "由個", "好靚", "有位", "考到", "行返", "你明", "你識", "我明", "兩位", "定下", "返番", "要畫", "哩件", "兜個", "會有"]);
    var MULTI_SURFACES = /* @__PURE__ */ new Set(["必要", "光", "妄想", "局", "邪", "咀", "定義", "杯", "表現", "保險", "活動", "健康", "接觸", "清", "笪", "終", "通", "通知", "創新", "較", "慳", "營", "翻譯", "黐", "一邊", "止", "火星", "安全", "曲", "味", "指示", "故", "原", "旅遊", "偈", "副", "專業", "現實", "陰", "悶", "發達", "煩", "實際", "端", "齊"]);
    var READING_SPLIT_SURFACES = /* @__PURE__ */ new Set(["拃", "校", "梗", "率", "第時", "黏", "文", "凹", "合", "同樣", "有份", "判斷", "兔仔", "和", "姊妹", "界定", "紅籌", "淨", "硬係", "量", "傳", "傾計", "撈"]);
    var RAW_ALTERNATIVE_SPECS = Object.freeze({
      "必要": [
        {
          "rank": 1752,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "stative/property “necessary” + abstract noun “necessity/need”",
          "jyutping": "bit1 jiu3"
        },
        {
          "rank": 1752,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "stative/property “necessary” + abstract noun “necessity/need”",
          "jyutping": "bit1 jiu3"
        },
        {
          "rank": 1752,
          "suffix": "reviewed_noun_3",
          "kind": "noun",
          "gloss": "stative/property “necessary” + abstract noun “necessity/need”",
          "jyutping": "bit1 jiu3"
        }
      ],
      "光": [
        {
          "rank": 1757,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "stative “bright” + noun “light” + independently lexical adverbial/bare family",
          "jyutping": "gwong1"
        },
        {
          "rank": 1757,
          "suffix": "reviewed_adverb_2",
          "kind": "adverb",
          "gloss": "stative “bright” + noun “light” + independently lexical adverbial/bare family",
          "jyutping": "gwong1"
        },
        {
          "rank": 1757,
          "suffix": "reviewed_verb_3",
          "kind": "verb",
          "gloss": "stative “bright” + noun “light” + independently lexical adverbial/bare family",
          "jyutping": "gwong1"
        },
        {
          "rank": 1757,
          "suffix": "reviewed_noun_4",
          "kind": "noun",
          "gloss": "stative “bright” + noun “light” + independently lexical adverbial/bare family",
          "jyutping": "gwong1"
        }
      ],
      "妄想": [
        {
          "rank": 1764,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "verb “fantasize unrealistically” + noun “delusion/unrealistic idea”",
          "jyutping": "mong5 soeng2"
        },
        {
          "rank": 1764,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "verb “fantasize unrealistically” + noun “delusion/unrealistic idea”",
          "jyutping": "mong5 soeng2"
        }
      ],
      "局": [
        {
          "rank": 1770,
          "suffix": "reviewed_classifier_1",
          "kind": "classifier",
          "gloss": "noun “bureau/office; situation/round” + classifier for games/matches/rounds",
          "jyutping": "guk6"
        },
        {
          "rank": 1770,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "noun “bureau/office; situation/round” + classifier for games/matches/rounds",
          "jyutping": "guk6"
        }
      ],
      "邪": [
        {
          "rank": 1773,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "stative “evil/heretical/strange” + noun/bound “evil influence/misfortune”",
          "jyutping": "ce4"
        },
        {
          "rank": 1773,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "stative “evil/heretical/strange” + noun/bound “evil influence/misfortune”",
          "jyutping": "ce4"
        },
        {
          "rank": 1773,
          "suffix": "reviewed_bound_3",
          "kind": "bound",
          "gloss": "stative “evil/heretical/strange” + noun/bound “evil influence/misfortune”",
          "jyutping": "ce4"
        }
      ],
      "咀": [
        {
          "rank": 1776,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "noun spelling variant of `嘴` + slang verb “kiss”",
          "jyutping": "zeoi2"
        },
        {
          "rank": 1776,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "noun spelling variant of `嘴` + slang verb “kiss”",
          "jyutping": "zeoi2"
        }
      ],
      "定義": [
        {
          "rank": 1777,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "noun “definition” + verb “define”",
          "jyutping": "ding6 ji6"
        },
        {
          "rank": 1777,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "noun “definition” + verb “define”",
          "jyutping": "ding6 ji6"
        }
      ],
      "拃": [
        {
          "rank": 1779,
          "suffix": "reviewed_zaa6_classifier_1",
          "kind": "classifier",
          "gloss": "`zaa6` classifier for handfuls/groups + obstruction verb; `zaa3` handspan measure",
          "jyutping": "zaa6"
        },
        {
          "rank": 1779,
          "suffix": "reviewed_zaa6_verb_2",
          "kind": "verb",
          "gloss": "`zaa6` classifier for handfuls/groups + obstruction verb; `zaa3` handspan measure",
          "jyutping": "zaa6"
        },
        {
          "rank": 1779,
          "suffix": "reviewed_zaa3_measure_1",
          "kind": "measure",
          "gloss": "`zaa6` classifier for handfuls/groups + obstruction verb; `zaa3` handspan measure",
          "jyutping": "zaa3"
        }
      ],
      "杯": [
        {
          "rank": 1780,
          "suffix": "reviewed_classifier_1",
          "kind": "classifier",
          "gloss": "noun “cup/glass” + classifier/measure for cupfuls/drinks",
          "jyutping": "bui1"
        },
        {
          "rank": 1780,
          "suffix": "reviewed_measure_2",
          "kind": "measure",
          "gloss": "noun “cup/glass” + classifier/measure for cupfuls/drinks",
          "jyutping": "bui1"
        },
        {
          "rank": 1780,
          "suffix": "reviewed_noun_3",
          "kind": "noun",
          "gloss": "noun “cup/glass” + classifier/measure for cupfuls/drinks",
          "jyutping": "bui1"
        }
      ],
      "表現": [
        {
          "rank": 1784,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "verb “show/manifest” + noun “performance/manifestation”",
          "jyutping": "biu2 jin6"
        },
        {
          "rank": 1784,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "verb “show/manifest” + noun “performance/manifestation”",
          "jyutping": "biu2 jin6"
        }
      ],
      "保險": [
        {
          "rank": 1786,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "noun “insurance” + stative “safe/cautious”",
          "jyutping": "bou2 him2"
        },
        {
          "rank": 1786,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "noun “insurance” + stative “safe/cautious”",
          "jyutping": "bou2 him2"
        }
      ],
      "活動": [
        {
          "rank": 1787,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "noun “activity” + verb “move/operate/be active”",
          "jyutping": "wut6 dung6"
        },
        {
          "rank": 1787,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "noun “activity” + verb “move/operate/be active”",
          "jyutping": "wut6 dung6"
        }
      ],
      "校": [
        {
          "rank": 1792,
          "suffix": "reviewed_haau6_noun_1",
          "kind": "noun",
          "gloss": "`haau6` school family vs `gaau3` proofread/check and field-officer family; exclude Mandarin classifier gloss",
          "jyutping": "haau6"
        },
        {
          "rank": 1792,
          "suffix": "reviewed_gaau3_verb_1",
          "kind": "verb",
          "gloss": "`haau6` school family vs `gaau3` proofread/check and field-officer family; exclude Mandarin classifier gloss",
          "jyutping": "gaau3"
        },
        {
          "rank": 1792,
          "suffix": "reviewed_gaau3_bound_2",
          "kind": "bound",
          "gloss": "`haau6` school family vs `gaau3` proofread/check and field-officer family; exclude Mandarin classifier gloss",
          "jyutping": "gaau3"
        }
      ],
      "健康": [
        {
          "rank": 1796,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "noun “health” + stative “healthy”",
          "jyutping": "gin6 hong1"
        },
        {
          "rank": 1796,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "noun “health” + stative “healthy”",
          "jyutping": "gin6 hong1"
        }
      ],
      "接觸": [
        {
          "rank": 1800,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "verb “contact/touch” + noun/event “contact”",
          "jyutping": "zip3 zuk1"
        },
        {
          "rank": 1800,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "verb “contact/touch” + noun/event “contact”",
          "jyutping": "zip3 zuk1"
        }
      ],
      "梗": [
        {
          "rank": 1801,
          "suffix": "reviewed_gang2_adjective_1",
          "kind": "adjective",
          "gloss": "`gang2` fixed/rigid property and certainty adverb vs `kwaang2` stem/stalk noun; reject Cifu `gang2 = stem` pairing",
          "jyutping": "gang2"
        },
        {
          "rank": 1801,
          "suffix": "reviewed_gang2_adverb_2",
          "kind": "adverb",
          "gloss": "`gang2` fixed/rigid property and certainty adverb vs `kwaang2` stem/stalk noun; reject Cifu `gang2 = stem` pairing",
          "jyutping": "gang2"
        },
        {
          "rank": 1801,
          "suffix": "reviewed_kwaang2_noun_1",
          "kind": "noun",
          "gloss": "`gang2` fixed/rigid property and certainty adverb vs `kwaang2` stem/stalk noun; reject Cifu `gang2 = stem` pairing",
          "jyutping": "kwaang2"
        }
      ],
      "清": [
        {
          "rank": 1802,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "stative “clear/clean/pure” + verb “clear/clean/set straight”",
          "jyutping": "cing1"
        },
        {
          "rank": 1802,
          "suffix": "reviewed_verb_2",
          "kind": "verb",
          "gloss": "stative “clear/clean/pure” + verb “clear/clean/set straight”",
          "jyutping": "cing1"
        }
      ],
      "率": [
        {
          "rank": 1803,
          "suffix": "reviewed_seot1_verb_1",
          "kind": "verb",
          "gloss": "`seot1` lead/command/frank/general family vs `leot6` rate/ratio noun/measure root",
          "jyutping": "seot1"
        },
        {
          "rank": 1803,
          "suffix": "reviewed_seot1_adjective_2",
          "kind": "adjective",
          "gloss": "`seot1` lead/command/frank/general family vs `leot6` rate/ratio noun/measure root",
          "jyutping": "seot1"
        },
        {
          "rank": 1803,
          "suffix": "reviewed_seot1_adverb_3",
          "kind": "adverb",
          "gloss": "`seot1` lead/command/frank/general family vs `leot6` rate/ratio noun/measure root",
          "jyutping": "seot1"
        },
        {
          "rank": 1803,
          "suffix": "reviewed_leot6_noun_1",
          "kind": "noun",
          "gloss": "`seot1` lead/command/frank/general family vs `leot6` rate/ratio noun/measure root",
          "jyutping": "leot6"
        },
        {
          "rank": 1803,
          "suffix": "reviewed_leot6_measure_2",
          "kind": "measure",
          "gloss": "`seot1` lead/command/frank/general family vs `leot6` rate/ratio noun/measure root",
          "jyutping": "leot6"
        }
      ],
      "笪": [
        {
          "rank": 1805,
          "suffix": "reviewed_classifier_1",
          "kind": "classifier",
          "gloss": "Cantonese classifier for a patch/plot/area + noun “rough bamboo mat”",
          "jyutping": "daat3"
        },
        {
          "rank": 1805,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "Cantonese classifier for a patch/plot/area + noun “rough bamboo mat”",
          "jyutping": "daat3"
        }
      ],
      "第時": [
        {
          "rank": 1807,
          "suffix": "reviewed_dai6_si4_temporal_1",
          "kind": "temporal",
          "gloss": "temporal noun “future; later; next time” with recorded reading variation",
          "jyutping": "dai6 si4"
        },
        {
          "rank": 1807,
          "suffix": "reviewed_dai6_si2_temporal_1",
          "kind": "temporal",
          "gloss": "temporal noun “future; later; next time” with recorded reading variation",
          "jyutping": "dai6 si2"
        }
      ],
      "終": [
        {
          "rank": 1810,
          "suffix": "reviewed_adverb_1",
          "kind": "adverb",
          "gloss": "formal noun/bound “end” + verb “end/finish” + adverbial “finally”",
          "jyutping": "zung1"
        },
        {
          "rank": 1810,
          "suffix": "reviewed_verb_2",
          "kind": "verb",
          "gloss": "formal noun/bound “end” + verb “end/finish” + adverbial “finally”",
          "jyutping": "zung1"
        },
        {
          "rank": 1810,
          "suffix": "reviewed_noun_3",
          "kind": "noun",
          "gloss": "formal noun/bound “end” + verb “end/finish” + adverbial “finally”",
          "jyutping": "zung1"
        },
        {
          "rank": 1810,
          "suffix": "reviewed_bound_4",
          "kind": "bound",
          "gloss": "formal noun/bound “end” + verb “end/finish” + adverbial “finally”",
          "jyutping": "zung1"
        }
      ],
      "通": [
        {
          "rank": 1812,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "verb “pass/connect/communicate/understand” + property/bound “through/general” + communication measure family",
          "jyutping": "tung1"
        },
        {
          "rank": 1812,
          "suffix": "reviewed_measure_2",
          "kind": "measure",
          "gloss": "verb “pass/connect/communicate/understand” + property/bound “through/general” + communication measure family",
          "jyutping": "tung1"
        },
        {
          "rank": 1812,
          "suffix": "reviewed_adjective_3",
          "kind": "adjective",
          "gloss": "verb “pass/connect/communicate/understand” + property/bound “through/general” + communication measure family",
          "jyutping": "tung1"
        },
        {
          "rank": 1812,
          "suffix": "reviewed_verb_4",
          "kind": "verb",
          "gloss": "verb “pass/connect/communicate/understand” + property/bound “through/general” + communication measure family",
          "jyutping": "tung1"
        },
        {
          "rank": 1812,
          "suffix": "reviewed_bound_5",
          "kind": "bound",
          "gloss": "verb “pass/connect/communicate/understand” + property/bound “through/general” + communication measure family",
          "jyutping": "tung1"
        }
      ],
      "通知": [
        {
          "rank": 1813,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "verb “notify/inform” + noun “notification/notice”",
          "jyutping": "tung1 zi1"
        },
        {
          "rank": 1813,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "verb “notify/inform” + noun “notification/notice”",
          "jyutping": "tung1 zi1"
        }
      ],
      "創新": [
        {
          "rank": 1815,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "stative “innovative/pioneering” + verb “innovate”",
          "jyutping": "cong3 san1"
        },
        {
          "rank": 1815,
          "suffix": "reviewed_verb_2",
          "kind": "verb",
          "gloss": "stative “innovative/pioneering” + verb “innovate”",
          "jyutping": "cong3 san1"
        }
      ],
      "較": [
        {
          "rank": 1829,
          "suffix": "reviewed_function_1",
          "kind": "function",
          "gloss": "comparative degree/relation function + formal compare/contest verb family",
          "jyutping": "gaau3"
        },
        {
          "rank": 1829,
          "suffix": "reviewed_verb_2",
          "kind": "verb",
          "gloss": "comparative degree/relation function + formal compare/contest verb family",
          "jyutping": "gaau3"
        }
      ],
      "慳": [
        {
          "rank": 1835,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "verb “save/economize” + stative “thrifty/frugal”",
          "jyutping": "haan1"
        },
        {
          "rank": 1835,
          "suffix": "reviewed_verb_2",
          "kind": "verb",
          "gloss": "verb “save/economize” + stative “thrifty/frugal”",
          "jyutping": "haan1"
        }
      ],
      "營": [
        {
          "rank": 1845,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "noun/bound “camp/barracks/battalion” + formal verb “operate/manage”",
          "jyutping": "jing4"
        },
        {
          "rank": 1845,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "noun/bound “camp/barracks/battalion” + formal verb “operate/manage”",
          "jyutping": "jing4"
        },
        {
          "rank": 1845,
          "suffix": "reviewed_bound_3",
          "kind": "bound",
          "gloss": "noun/bound “camp/barracks/battalion” + formal verb “operate/manage”",
          "jyutping": "jing4"
        }
      ],
      "黏": [
        {
          "rank": 1849,
          "suffix": "reviewed_nim4_verb_1",
          "kind": "verb",
          "gloss": "`nim4` verb “stick/adhere” vs `nim1` stative “sticky/glutinous”; packet `zim1` unpromoted",
          "jyutping": "nim4"
        },
        {
          "rank": 1849,
          "suffix": "reviewed_nim1_adjective_1",
          "kind": "adjective",
          "gloss": "`nim4` verb “stick/adhere” vs `nim1` stative “sticky/glutinous”; packet `zim1` unpromoted",
          "jyutping": "nim1"
        }
      ],
      "翻譯": [
        {
          "rank": 1852,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "noun “translation; translator/interpreter” + verb “translate/interpret”",
          "jyutping": "faan1 jik6"
        },
        {
          "rank": 1852,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "noun “translation; translator/interpreter” + verb “translate/interpret”",
          "jyutping": "faan1 jik6"
        }
      ],
      "黐": [
        {
          "rank": 1862,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "verb “stick/adhere/cling” + stative “sticky”",
          "jyutping": "ci1"
        },
        {
          "rank": 1862,
          "suffix": "reviewed_verb_2",
          "kind": "verb",
          "gloss": "verb “stick/adhere/cling” + stative “sticky”",
          "jyutping": "ci1"
        }
      ],
      "一邊": [
        {
          "rank": 1865,
          "suffix": "reviewed_localizer_1",
          "kind": "localizer",
          "gloss": "locality noun “one side” + constructional/adverbial member of paired simultaneous `一邊…一邊…` pattern",
          "jyutping": "jat1 bin1"
        },
        {
          "rank": 1865,
          "suffix": "reviewed_adverb_2",
          "kind": "adverb",
          "gloss": "locality noun “one side” + constructional/adverbial member of paired simultaneous `一邊…一邊…` pattern",
          "jyutping": "jat1 bin1"
        },
        {
          "rank": 1865,
          "suffix": "reviewed_verb_3",
          "kind": "verb",
          "gloss": "locality noun “one side” + constructional/adverbial member of paired simultaneous `一邊…一邊…` pattern",
          "jyutping": "jat1 bin1"
        },
        {
          "rank": 1865,
          "suffix": "reviewed_noun_4",
          "kind": "noun",
          "gloss": "locality noun “one side” + constructional/adverbial member of paired simultaneous `一邊…一邊…` pattern",
          "jyutping": "jat1 bin1"
        }
      ],
      "文": [
        {
          "rank": 1879,
          "suffix": "reviewed_man4_noun_1",
          "kind": "noun",
          "gloss": "`man4` noun/property family “writing, language, literature, culture” + Cantonese changed-tone money/classifier family + `man6` cover/paint-over verb",
          "jyutping": "man4"
        },
        {
          "rank": 1879,
          "suffix": "reviewed_man4_adjective_2",
          "kind": "adjective",
          "gloss": "`man4` noun/property family “writing, language, literature, culture” + Cantonese changed-tone money/classifier family + `man6` cover/paint-over verb",
          "jyutping": "man4"
        },
        {
          "rank": 1879,
          "suffix": "reviewed_man1_classifier_1",
          "kind": "classifier",
          "gloss": "`man4` noun/property family “writing, language, literature, culture” + Cantonese changed-tone money/classifier family + `man6` cover/paint-over verb",
          "jyutping": "man1"
        },
        {
          "rank": 1879,
          "suffix": "reviewed_man1_measure_2",
          "kind": "measure",
          "gloss": "`man4` noun/property family “writing, language, literature, culture” + Cantonese changed-tone money/classifier family + `man6` cover/paint-over verb",
          "jyutping": "man1"
        },
        {
          "rank": 1879,
          "suffix": "reviewed_man6_verb_1",
          "kind": "verb",
          "gloss": "`man4` noun/property family “writing, language, literature, culture” + Cantonese changed-tone money/classifier family + `man6` cover/paint-over verb",
          "jyutping": "man6"
        }
      ],
      "止": [
        {
          "rank": 1880,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "formal verb “stop/halt/prohibit” + property/adverbial “still/calm/only” family; “until” belongs to larger forms such as `為止`",
          "jyutping": "zi2"
        },
        {
          "rank": 1880,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "formal verb “stop/halt/prohibit” + property/adverbial “still/calm/only” family; “until” belongs to larger forms such as `為止`",
          "jyutping": "zi2"
        },
        {
          "rank": 1880,
          "suffix": "reviewed_adverb_3",
          "kind": "adverb",
          "gloss": "formal verb “stop/halt/prohibit” + property/adverbial “still/calm/only” family; “until” belongs to larger forms such as `為止`",
          "jyutping": "zi2"
        },
        {
          "rank": 1880,
          "suffix": "reviewed_verb_4",
          "kind": "verb",
          "gloss": "formal verb “stop/halt/prohibit” + property/adverbial “still/calm/only” family; “until” belongs to larger forms such as `為止`",
          "jyutping": "zi2"
        }
      ],
      "火星": [
        {
          "rank": 1881,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "proper/celestial noun “Mars” + common noun “spark”",
          "jyutping": "fo2 sing1"
        },
        {
          "rank": 1881,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "proper/celestial noun “Mars” + common noun “spark”",
          "jyutping": "fo2 sing1"
        }
      ],
      "凹": [
        {
          "rank": 1884,
          "suffix": "reviewed_nap1_adjective_1",
          "kind": "adjective",
          "gloss": "ordinary stative “concave/sunken” plus restricted alternate lexical/placename readings; packet `lap1` not adopted as general default",
          "jyutping": "nap1"
        },
        {
          "rank": 1884,
          "suffix": "reviewed_aau3_adjective_1",
          "kind": "adjective",
          "gloss": "ordinary stative “concave/sunken” plus restricted alternate lexical/placename readings; packet `lap1` not adopted as general default",
          "jyutping": "aau3"
        },
        {
          "rank": 1884,
          "suffix": "reviewed_waa1_adjective_1",
          "kind": "adjective",
          "gloss": "ordinary stative “concave/sunken” plus restricted alternate lexical/placename readings; packet `lap1` not adopted as general default",
          "jyutping": "waa1"
        }
      ],
      "合": [
        {
          "rank": 1895,
          "suffix": "reviewed_hap6_verb_1",
          "kind": "verb",
          "gloss": "`hap6` combine/fit/whole family; `ho4` musical-note noun; `gap3` grain measure",
          "jyutping": "hap6"
        },
        {
          "rank": 1895,
          "suffix": "reviewed_hap6_adjective_2",
          "kind": "adjective",
          "gloss": "`hap6` combine/fit/whole family; `ho4` musical-note noun; `gap3` grain measure",
          "jyutping": "hap6"
        },
        {
          "rank": 1895,
          "suffix": "reviewed_ho4_noun_1",
          "kind": "noun",
          "gloss": "`hap6` combine/fit/whole family; `ho4` musical-note noun; `gap3` grain measure",
          "jyutping": "ho4"
        },
        {
          "rank": 1895,
          "suffix": "reviewed_gap3_measure_1",
          "kind": "measure",
          "gloss": "`hap6` combine/fit/whole family; `ho4` musical-note noun; `gap3` grain measure",
          "jyutping": "gap3"
        }
      ],
      "同樣": [
        {
          "rank": 1899,
          "suffix": "reviewed_tung4_joeng6_adjective_1",
          "kind": "adjective",
          "gloss": "same/similar/equal property-adverbial family with independently recorded changed-tone “same type” reading",
          "jyutping": "tung4 joeng6"
        },
        {
          "rank": 1899,
          "suffix": "reviewed_tung4_joeng6_adverb_2",
          "kind": "adverb",
          "gloss": "same/similar/equal property-adverbial family with independently recorded changed-tone “same type” reading",
          "jyutping": "tung4 joeng6"
        },
        {
          "rank": 1899,
          "suffix": "reviewed_tung4_joeng2_noun_1",
          "kind": "noun",
          "gloss": "same/similar/equal property-adverbial family with independently recorded changed-tone “same type” reading",
          "jyutping": "tung4 joeng2"
        }
      ],
      "安全": [
        {
          "rank": 1904,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "abstract noun “safety/security” + stative “safe/secure”",
          "jyutping": "on1 cyun4"
        },
        {
          "rank": 1904,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "abstract noun “safety/security” + stative “safe/secure”",
          "jyutping": "on1 cyun4"
        }
      ],
      "曲": [
        {
          "rank": 1909,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "noun “song/tune” + stative/property “bent/crooked” family",
          "jyutping": "kuk1"
        },
        {
          "rank": 1909,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "noun “song/tune” + stative/property “bent/crooked” family",
          "jyutping": "kuk1"
        },
        {
          "rank": 1909,
          "suffix": "reviewed_noun_3",
          "kind": "noun",
          "gloss": "noun “song/tune” + stative/property “bent/crooked” family",
          "jyutping": "kuk1"
        }
      ],
      "有份": [
        {
          "rank": 1910,
          "suffix": "reviewed_jau5_fan2_verb_1",
          "kind": "verb",
          "gloss": "lexical expression “have a share/part; participate/be involved” with Cantonese changed tone",
          "jyutping": "jau5 fan2"
        }
      ],
      "判斷": [
        {
          "rank": 1918,
          "suffix": "reviewed_pun3_dyun3_verb_1",
          "kind": "verb",
          "gloss": "verb “judge/determine” and nominal judgment family with corrected second-syllable reading",
          "jyutping": "pun3 dyun3"
        },
        {
          "rank": 1918,
          "suffix": "reviewed_pun3_dyun6_noun_1",
          "kind": "noun",
          "gloss": "verb “judge/determine” and nominal judgment family with corrected second-syllable reading",
          "jyutping": "pun3 dyun6"
        }
      ],
      "兔仔": [
        {
          "rank": 1924,
          "suffix": "reviewed_tou3_zai2_noun_1",
          "kind": "noun",
          "gloss": "animal noun “rabbit/bunny” with corrected suffix reading",
          "jyutping": "tou3 zai2"
        }
      ],
      "味": [
        {
          "rank": 1927,
          "suffix": "reviewed_classifier_1",
          "kind": "classifier",
          "gloss": "noun “taste/smell/flavour” + verb “taste/savour” + classifier/measure family",
          "jyutping": "mei6"
        },
        {
          "rank": 1927,
          "suffix": "reviewed_measure_2",
          "kind": "measure",
          "gloss": "noun “taste/smell/flavour” + verb “taste/savour” + classifier/measure family",
          "jyutping": "mei6"
        },
        {
          "rank": 1927,
          "suffix": "reviewed_verb_3",
          "kind": "verb",
          "gloss": "noun “taste/smell/flavour” + verb “taste/savour” + classifier/measure family",
          "jyutping": "mei6"
        },
        {
          "rank": 1927,
          "suffix": "reviewed_noun_4",
          "kind": "noun",
          "gloss": "noun “taste/smell/flavour” + verb “taste/savour” + classifier/measure family",
          "jyutping": "mei6"
        }
      ],
      "和": [
        {
          "rank": 1928,
          "suffix": "reviewed_wo4_function_1",
          "kind": "function",
          "gloss": "formal conjunction/noun/property family plus distinct reading-sensitive respond-in-song and mahjong/mixing families",
          "jyutping": "wo4"
        },
        {
          "rank": 1928,
          "suffix": "reviewed_wo4_noun_2",
          "kind": "noun",
          "gloss": "formal conjunction/noun/property family plus distinct reading-sensitive respond-in-song and mahjong/mixing families",
          "jyutping": "wo4"
        },
        {
          "rank": 1928,
          "suffix": "reviewed_wo4_adjective_3",
          "kind": "adjective",
          "gloss": "formal conjunction/noun/property family plus distinct reading-sensitive respond-in-song and mahjong/mixing families",
          "jyutping": "wo4"
        },
        {
          "rank": 1928,
          "suffix": "reviewed_wo6_verb_1",
          "kind": "verb",
          "gloss": "formal conjunction/noun/property family plus distinct reading-sensitive respond-in-song and mahjong/mixing families",
          "jyutping": "wo6"
        },
        {
          "rank": 1928,
          "suffix": "reviewed_wu2_verb_1",
          "kind": "verb",
          "gloss": "formal conjunction/noun/property family plus distinct reading-sensitive respond-in-song and mahjong/mixing families",
          "jyutping": "wu2"
        }
      ],
      "姊妹": [
        {
          "rank": 1929,
          "suffix": "reviewed_zi2_mui6_person_1",
          "kind": "person",
          "gloss": "kin/social noun “sisters; close female friends” with ordinary and changed-tone second-syllable readings",
          "jyutping": "zi2 mui6"
        },
        {
          "rank": 1929,
          "suffix": "reviewed_zi2_mui2_person_1",
          "kind": "person",
          "gloss": "kin/social noun “sisters; close female friends” with ordinary and changed-tone second-syllable readings",
          "jyutping": "zi2 mui2"
        }
      ],
      "指示": [
        {
          "rank": 1936,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "verb “indicate/instruct” + noun “instruction/directive”",
          "jyutping": "zi2 si6"
        },
        {
          "rank": 1936,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "verb “indicate/instruct” + noun “instruction/directive”",
          "jyutping": "zi2 si6"
        }
      ],
      "故": [
        {
          "rank": 1937,
          "suffix": "reviewed_adverb_1",
          "kind": "adverb",
          "gloss": "formal noun “reason/cause/event” + modifier “former/deceased/old” + connective/adverbial/verb families where independently licensed",
          "jyutping": "gu3"
        },
        {
          "rank": 1937,
          "suffix": "reviewed_function_2",
          "kind": "function",
          "gloss": "formal noun “reason/cause/event” + modifier “former/deceased/old” + connective/adverbial/verb families where independently licensed",
          "jyutping": "gu3"
        },
        {
          "rank": 1937,
          "suffix": "reviewed_verb_3",
          "kind": "verb",
          "gloss": "formal noun “reason/cause/event” + modifier “former/deceased/old” + connective/adverbial/verb families where independently licensed",
          "jyutping": "gu3"
        },
        {
          "rank": 1937,
          "suffix": "reviewed_noun_4",
          "kind": "noun",
          "gloss": "formal noun “reason/cause/event” + modifier “former/deceased/old” + connective/adverbial/verb families where independently licensed",
          "jyutping": "gu3"
        },
        {
          "rank": 1937,
          "suffix": "reviewed_bound_5",
          "kind": "bound",
          "gloss": "formal noun “reason/cause/event” + modifier “former/deceased/old” + connective/adverbial/verb families where independently licensed",
          "jyutping": "gu3"
        }
      ],
      "界定": [
        {
          "rank": 1940,
          "suffix": "reviewed_gaai3_deng6_verb_1",
          "kind": "verb",
          "gloss": "verb “define; delimit” with independently corrected reading",
          "jyutping": "gaai3 deng6"
        }
      ],
      "紅籌": [
        {
          "rank": 1941,
          "suffix": "reviewed_hung4_cau2_noun_1",
          "kind": "noun",
          "gloss": "finance noun “red chip/red-chip stock” with Cantonese changed-tone standalone reading",
          "jyutping": "hung4 cau2"
        },
        {
          "rank": 1941,
          "suffix": "reviewed_hung4_cau4_noun_1",
          "kind": "noun",
          "gloss": "finance noun “red chip/red-chip stock” with Cantonese changed-tone standalone reading",
          "jyutping": "hung4 cau4"
        }
      ],
      "原": [
        {
          "rank": 1947,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "formal modifier/property “original/former/raw” + noun “source/origin” + verb/bound family",
          "jyutping": "jyun4"
        },
        {
          "rank": 1947,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "formal modifier/property “original/former/raw” + noun “source/origin” + verb/bound family",
          "jyutping": "jyun4"
        },
        {
          "rank": 1947,
          "suffix": "reviewed_verb_3",
          "kind": "verb",
          "gloss": "formal modifier/property “original/former/raw” + noun “source/origin” + verb/bound family",
          "jyutping": "jyun4"
        },
        {
          "rank": 1947,
          "suffix": "reviewed_noun_4",
          "kind": "noun",
          "gloss": "formal modifier/property “original/former/raw” + noun “source/origin” + verb/bound family",
          "jyutping": "jyun4"
        },
        {
          "rank": 1947,
          "suffix": "reviewed_bound_5",
          "kind": "bound",
          "gloss": "formal modifier/property “original/former/raw” + noun “source/origin” + verb/bound family",
          "jyutping": "jyun4"
        }
      ],
      "旅遊": [
        {
          "rank": 1951,
          "suffix": "reviewed_verb_1",
          "kind": "verb",
          "gloss": "verb “travel/tour” + noun/event “travel/tourism”",
          "jyutping": "leoi5 jau4"
        },
        {
          "rank": 1951,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "verb “travel/tour” + noun/event “travel/tourism”",
          "jyutping": "leoi5 jau4"
        }
      ],
      "偈": [
        {
          "rank": 1957,
          "suffix": "reviewed_noun_1",
          "kind": "noun",
          "gloss": "Cantonese `gai2` noun/bound lexical families, including conventional conversational and engine/condition material; formal Buddhist-gāthā material remains category/reading-sensitive and is not inferred from Cifu alone",
          "jyutping": "gai2"
        },
        {
          "rank": 1957,
          "suffix": "reviewed_bound_2",
          "kind": "bound",
          "gloss": "Cantonese `gai2` noun/bound lexical families, including conventional conversational and engine/condition material; formal Buddhist-gāthā material remains category/reading-sensitive and is not inferred from Cifu alone",
          "jyutping": "gai2"
        }
      ],
      "副": [
        {
          "rank": 1959,
          "suffix": "reviewed_classifier_1",
          "kind": "classifier",
          "gloss": "modifier/bound “secondary/deputy/vice-” + noun “deputy/assistant” + classifier for pairs/sets",
          "jyutping": "fu3"
        },
        {
          "rank": 1959,
          "suffix": "reviewed_noun_2",
          "kind": "noun",
          "gloss": "modifier/bound “secondary/deputy/vice-” + noun “deputy/assistant” + classifier for pairs/sets",
          "jyutping": "fu3"
        },
        {
          "rank": 1959,
          "suffix": "reviewed_bound_3",
          "kind": "bound",
          "gloss": "modifier/bound “secondary/deputy/vice-” + noun “deputy/assistant” + classifier for pairs/sets",
          "jyutping": "fu3"
        }
      ],
      "專業": [
        {
          "rank": 1961,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "stative/property “professional” + abstract noun “professional expertise/profession”; Mainland university-major sense does not drive ordinary Cantonese typing",
          "jyutping": "zyun1 jip6"
        },
        {
          "rank": 1961,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "stative/property “professional” + abstract noun “professional expertise/profession”; Mainland university-major sense does not drive ordinary Cantonese typing",
          "jyutping": "zyun1 jip6"
        },
        {
          "rank": 1961,
          "suffix": "reviewed_noun_3",
          "kind": "noun",
          "gloss": "stative/property “professional” + abstract noun “professional expertise/profession”; Mainland university-major sense does not drive ordinary Cantonese typing",
          "jyutping": "zyun1 jip6"
        }
      ],
      "淨": [
        {
          "rank": 1964,
          "suffix": "reviewed_zing6_adjective_1",
          "kind": "adjective",
          "gloss": "formal/literary `zing6` and colloquial `zeng6` property/adverb/verb families “clean/pure/net; only; cleanse”",
          "jyutping": "zing6"
        },
        {
          "rank": 1964,
          "suffix": "reviewed_zing6_adverb_2",
          "kind": "adverb",
          "gloss": "formal/literary `zing6` and colloquial `zeng6` property/adverb/verb families “clean/pure/net; only; cleanse”",
          "jyutping": "zing6"
        },
        {
          "rank": 1964,
          "suffix": "reviewed_zing6_verb_3",
          "kind": "verb",
          "gloss": "formal/literary `zing6` and colloquial `zeng6` property/adverb/verb families “clean/pure/net; only; cleanse”",
          "jyutping": "zing6"
        },
        {
          "rank": 1964,
          "suffix": "reviewed_zeng6_adjective_1",
          "kind": "adjective",
          "gloss": "formal/literary `zing6` and colloquial `zeng6` property/adverb/verb families “clean/pure/net; only; cleanse”",
          "jyutping": "zeng6"
        },
        {
          "rank": 1964,
          "suffix": "reviewed_zeng6_verb_2",
          "kind": "verb",
          "gloss": "formal/literary `zing6` and colloquial `zeng6` property/adverb/verb families “clean/pure/net; only; cleanse”",
          "jyutping": "zeng6"
        }
      ],
      "現實": [
        {
          "rank": 1966,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "abstract noun “reality” + stative/property “real; realistic; actual”",
          "jyutping": "jin6 sat6"
        },
        {
          "rank": 1966,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "abstract noun “reality” + stative/property “real; realistic; actual”",
          "jyutping": "jin6 sat6"
        },
        {
          "rank": 1966,
          "suffix": "reviewed_noun_3",
          "kind": "noun",
          "gloss": "abstract noun “reality” + stative/property “real; realistic; actual”",
          "jyutping": "jin6 sat6"
        }
      ],
      "陰": [
        {
          "rank": 1972,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "noun/bound “yin/shade/negative side” + stative/property “overcast/dark/hidden” + verb “trick/set up” family",
          "jyutping": "jam1"
        },
        {
          "rank": 1972,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "noun/bound “yin/shade/negative side” + stative/property “overcast/dark/hidden” + verb “trick/set up” family",
          "jyutping": "jam1"
        },
        {
          "rank": 1972,
          "suffix": "reviewed_verb_3",
          "kind": "verb",
          "gloss": "noun/bound “yin/shade/negative side” + stative/property “overcast/dark/hidden” + verb “trick/set up” family",
          "jyutping": "jam1"
        },
        {
          "rank": 1972,
          "suffix": "reviewed_noun_4",
          "kind": "noun",
          "gloss": "noun/bound “yin/shade/negative side” + stative/property “overcast/dark/hidden” + verb “trick/set up” family",
          "jyutping": "jam1"
        },
        {
          "rank": 1972,
          "suffix": "reviewed_bound_5",
          "kind": "bound",
          "gloss": "noun/bound “yin/shade/negative side” + stative/property “overcast/dark/hidden” + verb “trick/set up” family",
          "jyutping": "jam1"
        }
      ],
      "悶": [
        {
          "rank": 1974,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "stative “bored/stuffy/depressed” + verb “cover/smother/keep shut” family",
          "jyutping": "mun6"
        },
        {
          "rank": 1974,
          "suffix": "reviewed_verb_2",
          "kind": "verb",
          "gloss": "stative “bored/stuffy/depressed” + verb “cover/smother/keep shut” family",
          "jyutping": "mun6"
        }
      ],
      "發達": [
        {
          "rank": 1977,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "verb “develop/prosper/get rich” + stative/property “developed/prosperous”",
          "jyutping": "faat3 daat6"
        },
        {
          "rank": 1977,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "verb “develop/prosper/get rich” + stative/property “developed/prosperous”",
          "jyutping": "faat3 daat6"
        },
        {
          "rank": 1977,
          "suffix": "reviewed_verb_3",
          "kind": "verb",
          "gloss": "verb “develop/prosper/get rich” + stative/property “developed/prosperous”",
          "jyutping": "faat3 daat6"
        }
      ],
      "硬係": [
        {
          "rank": 1978,
          "suffix": "reviewed_ngaang2_hai6_adverb_1",
          "kind": "adverb",
          "gloss": "Cantonese emphatic adverb “definitely; always/insistently” with independently attested changed-tone pronunciation",
          "jyutping": "ngaang2 hai6"
        },
        {
          "rank": 1978,
          "suffix": "reviewed_ngaang6_hai6_adverb_1",
          "kind": "adverb",
          "gloss": "Cantonese emphatic adverb “definitely; always/insistently” with independently attested changed-tone pronunciation",
          "jyutping": "ngaang6 hai6"
        }
      ],
      "量": [
        {
          "rank": 1979,
          "suffix": "reviewed_loeng4_verb_1",
          "kind": "verb",
          "gloss": "`loeng4` verb “measure/consider” vs `loeng6` noun “amount/capacity” and estimate/quantity family",
          "jyutping": "loeng4"
        },
        {
          "rank": 1979,
          "suffix": "reviewed_loeng6_noun_1",
          "kind": "noun",
          "gloss": "`loeng4` verb “measure/consider” vs `loeng6` noun “amount/capacity” and estimate/quantity family",
          "jyutping": "loeng6"
        }
      ],
      "傳": [
        {
          "rank": 1981,
          "suffix": "reviewed_cyun4_verb_1",
          "kind": "verb",
          "gloss": "`cyun4` verb “transmit/spread/pass on” vs `zyun6` noun “biography/account/commentary” family",
          "jyutping": "cyun4"
        },
        {
          "rank": 1981,
          "suffix": "reviewed_zyun6_noun_1",
          "kind": "noun",
          "gloss": "`cyun4` verb “transmit/spread/pass on” vs `zyun6` noun “biography/account/commentary” family",
          "jyutping": "zyun6"
        }
      ],
      "傾計": [
        {
          "rank": 1983,
          "suffix": "reviewed_king1_gai2_verb_1",
          "kind": "verb",
          "gloss": "lexical verb “chat; talk” with corrected final changed-tone reading",
          "jyutping": "king1 gai2"
        }
      ],
      "煩": [
        {
          "rank": 1985,
          "suffix": "reviewed_adjective_1",
          "kind": "adjective",
          "gloss": "stative “annoying/troublesome/fed up” + verb “bother/annoy/trouble”",
          "jyutping": "faan4"
        },
        {
          "rank": 1985,
          "suffix": "reviewed_verb_2",
          "kind": "verb",
          "gloss": "stative “annoying/troublesome/fed up” + verb “bother/annoy/trouble”",
          "jyutping": "faan4"
        }
      ],
      "實際": [
        {
          "rank": 1989,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "abstract noun “reality/practice” + stative/property “actual; practical; realistic”",
          "jyutping": "sat6 zai3"
        },
        {
          "rank": 1989,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "abstract noun “reality/practice” + stative/property “actual; practical; realistic”",
          "jyutping": "sat6 zai3"
        },
        {
          "rank": 1989,
          "suffix": "reviewed_noun_3",
          "kind": "noun",
          "gloss": "abstract noun “reality/practice” + stative/property “actual; practical; realistic”",
          "jyutping": "sat6 zai3"
        }
      ],
      "端": [
        {
          "rank": 1991,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "noun/bound “end/extremity/item/side” + formal verb “hold/carry level” + property/bound “upright/regular” family",
          "jyutping": "dyun1"
        },
        {
          "rank": 1991,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "noun/bound “end/extremity/item/side” + formal verb “hold/carry level” + property/bound “upright/regular” family",
          "jyutping": "dyun1"
        },
        {
          "rank": 1991,
          "suffix": "reviewed_verb_3",
          "kind": "verb",
          "gloss": "noun/bound “end/extremity/item/side” + formal verb “hold/carry level” + property/bound “upright/regular” family",
          "jyutping": "dyun1"
        },
        {
          "rank": 1991,
          "suffix": "reviewed_noun_4",
          "kind": "noun",
          "gloss": "noun/bound “end/extremity/item/side” + formal verb “hold/carry level” + property/bound “upright/regular” family",
          "jyutping": "dyun1"
        },
        {
          "rank": 1991,
          "suffix": "reviewed_bound_5",
          "kind": "bound",
          "gloss": "noun/bound “end/extremity/item/side” + formal verb “hold/carry level” + property/bound “upright/regular” family",
          "jyutping": "dyun1"
        }
      ],
      "齊": [
        {
          "rank": 1993,
          "suffix": "reviewed_proper_1",
          "kind": "proper",
          "gloss": "stative/property “complete/even/aligned” + adverb “together/all at once” + proper-name family",
          "jyutping": "cai4"
        },
        {
          "rank": 1993,
          "suffix": "reviewed_adjective_2",
          "kind": "adjective",
          "gloss": "stative/property “complete/even/aligned” + adverb “together/all at once” + proper-name family",
          "jyutping": "cai4"
        },
        {
          "rank": 1993,
          "suffix": "reviewed_adverb_3",
          "kind": "adverb",
          "gloss": "stative/property “complete/even/aligned” + adverb “together/all at once” + proper-name family",
          "jyutping": "cai4"
        },
        {
          "rank": 1993,
          "suffix": "reviewed_verb_4",
          "kind": "verb",
          "gloss": "stative/property “complete/even/aligned” + adverb “together/all at once” + proper-name family",
          "jyutping": "cai4"
        }
      ],
      "撈": [
        {
          "rank": 1996,
          "suffix": "reviewed_lou1_verb_1",
          "kind": "verb",
          "gloss": "colloquial `lou1` “mix; earn/make a living, often opportunistically” vs `laau4` “scoop/dredge/fish out”; reject Cifu `lou4`",
          "jyutping": "lou1"
        },
        {
          "rank": 1996,
          "suffix": "reviewed_laau4_verb_1",
          "kind": "verb",
          "gloss": "colloquial `lou1` “mix; earn/make a living, often opportunistically” vs `laau4` “scoop/dredge/fish out”; reject Cifu `lou4`",
          "jyutping": "laau4"
        }
      ]
    });
    var ALTERNATIVE_SPECS = Object.freeze(Object.fromEntries(Object.entries(RAW_ALTERNATIVE_SPECS).map(([surface, specs]) => [surface, Object.freeze(specs.map((spec) => Object.freeze({ ...lexicalSpec(spec.rank, spec.kind, spec.gloss, spec.jyutping, spec.syntax || ""), suffix: spec.suffix })))])));
    var CANDIDATE_ONLY_SURFACES = new Set(Object.keys(ALTERNATIVE_SPECS));
    var DEFAULT_READING_OVERRIDES = Object.freeze({
      "司長": "si1 zoeng2",
      "必要": "bit1 jiu3",
      "本地": "bun2 dei6",
      "正版": "zing3 baan2",
      "甩": "lat1",
      "光": "gwong1",
      "在": "zoi6",
      "妄想": "mong5 soeng2",
      "忙": "mong4",
      "成年": "sing4 nin4",
      "老細": "lou5 sai3",
      "自從": "zi6 cung4",
      "初頭": "co1 tau4",
      "局": "guk6",
      "足夠": "zuk1 gau3",
      "邪": "ce4",
      "事業": "si6 jip6",
      "咀": "zeoi2",
      "定義": "ding6 ji6",
      "忽然": "fat1 jin4",
      "拃": "zaa6",
      "杯": "bui1",
      "東面": "dung1 min6",
      "欣賞": "jan1 soeng2",
      "股價": "gu2 gaa3",
      "表現": "biu2 jin6",
      "長遠": "coeng4 jyun5",
      "保險": "bou2 him2",
      "活動": "wut6 dung6",
      "重心": "zung6 sam1",
      "哲學": "zit3 hok6",
      "家人": "gaa1 jan4",
      "晏晝": "aan3 zau3",
      "校": "haau6",
      "窄": "zaak3",
      "粉": "fan2",
      "做法": "zou6 faat3",
      "健康": "gin6 hong1",
      "唯一": "wai4 jat1",
      "唱片": "coeng3 pin2",
      "婚姻": "fan1 jan1",
      "接觸": "zip3 zuk1",
      "梗": "gang2",
      "清": "cing1",
      "率": "seot1",
      "現象": "jin6 zoeng6",
      "笪": "daat3",
      "第時": "dai6 si4",
      "粗口": "cou1 hau2",
      "細妹": "sai3 mui2",
      "終": "zung1",
      "規則": "kwai1 zak1",
      "通": "tung1",
      "通知": "tung1 zi1",
      "創意": "cong3 ji3",
      "創新": "cong3 san1",
      "換": "wun6",
      "普通話": "pou2 tung1 waa2",
      "普遍": "pou2 pin3",
      "評價": "ping4 gaa3",
      "意義": "ji3 ji6",
      "愛情": "oi3 cing4",
      "業": "jip6",
      "業主": "jip6 zyu2",
      "罪行": "zeoi6 hang4",
      "較": "gaau3",
      "遊戲": "jau4 hei3",
      "電訊": "din6 seon3",
      "鼓勵": "gu2 lai6",
      "嘔": "au2",
      "實際上": "sat6 zai3 soeng6",
      "慳": "haan1",
      "樓下": "lau4 haa6",
      "瘦": "sau3",
      "賭": "dou2",
      "踩": "caai2",
      "燕梳": "jin3 so1",
      "遲": "ci4",
      "戲院": "hei3 jyun2",
      "擦紙膠": "caat3 zi2 gaau1",
      "營": "jing4",
      "聯絡": "lyun4 lok3",
      "講法": "gong2 faat3",
      "黏": "nim4",
      "禮拜三": "lai5 baai3 saam1",
      "禮拜六": "lai5 baai3 luk6",
      "翻譯": "faan1 jik6",
      "轉頭": "zyun3 tau4",
      "額": "ngaak6",
      "鯉": "lei5",
      "穩定": "wan2 ding6",
      "嚴重": "jim4 zung6",
      "屬於": "suk6 jyu1",
      "黐": "ci1",
      "顯示": "hin2 si6",
      "一模一樣": "jat1 mou4 jat1 joeng6",
      "一邊": "jat1 bin1",
      "人生": "jan4 sang1",
      "上堂": "soeng5 tong4",
      "中國人": "zung1 gwok3 jan4",
      "五月": "ng5 jyut6",
      "內心": "noi6 sam1",
      "反映": "faan2 jing2",
      "天堂": "tin1 tong4",
      "太陽": "taai3 joeng4",
      "心態": "sam1 taai3",
      "文": "man4",
      "止": "zi2",
      "火星": "fo2 sing1",
      "仙人掌": "sin1 jan4 zoeng2",
      "以往": "ji5 wong5",
      "凹": "nap1",
      "出席": "ceot1 zik6",
      "出邊": "ceot1 bin6",
      "可憐": "ho2 lin4",
      "失": "sat1",
      "民主": "man4 zyu2",
      "民建聯": "man4 gin3 lyun4",
      "生物": "sang1 mat6",
      "全名": "cyun4 meng2",
      "合": "hap6",
      "合法": "hap6 faat3",
      "吋": "cyun3",
      "同時": "tung4 si4",
      "同樣": "tung4 joeng6",
      "地球": "dei6 kau4",
      "好人": "hou2 jan4",
      "好味": "hou2 mei6",
      "安全": "on1 cyun4",
      "年青": "nin4 cing1",
      "年齡": "nin4 ling4",
      "式": "sik1",
      "攰": "gui6",
      "曲": "kuk1",
      "有份": "jau5 fan2",
      "老公": "lou5 gung1",
      "行路": "haang4 lou6",
      "判斷": "pun3 dyun3",
      "別人": "bit6 jan4",
      "完整": "jyun4 zing2",
      "使錢": "sai2 cin2",
      "依然": "ji1 jin4",
      "兔仔": "tou3 zai2",
      "呢邊": "ni1 bin1",
      "味": "mei6",
      "和": "wo4",
      "姊妹": "zi2 mui6",
      "板": "baan2",
      "侮辱": "mou5 juk6",
      "保護": "bou2 wu6",
      "哈哈": "haa1 haa1",
      "指示": "zi2 si6",
      "故": "gu3",
      "星": "sing1",
      "洲": "zau1",
      "界定": "gaai3 deng6",
      "紅籌": "hung4 cau2",
      "耶穌": "je4 sou1",
      "背後": "bui3 hau6",
      "飛碟": "fei1 dip2",
      "個案": "go3 on3",
      "原": "jyun4",
      "唔知幾": "m4 zi1 gei2",
      "孭": "me1",
      "旅遊": "leoi5 jau4",
      "特色": "dak6 sik1",
      "特殊": "dak6 syu4",
      "狹窄": "haap6 zaak3",
      "退": "teoi3",
      "針對": "zam1 deoi3",
      "偈": "gai2",
      "副": "fu3",
      "唯有": "wai4 jau5",
      "專業": "zyun1 jip6",
      "得滯": "dak1 zai6",
      "梳打": "so1 daa2",
      "淨": "zing6",
      "清晰": "cing1 sik1",
      "現實": "jin6 sat6",
      "船": "syun4",
      "規定": "kwai1 ding6",
      "貨櫃": "fo3 gwai6",
      "責任": "zaak3 jam6",
      "連埋": "lin4 maai4",
      "陰": "jam1",
      "單位": "daan1 wai2",
      "悶": "mun6",
      "期間": "kei4 gaan1",
      "無謂": "mou4 wai6",
      "發達": "faat3 daat6",
      "硬係": "ngaang2 hai6",
      "量": "loeng4",
      "開會": "hoi1 wui2",
      "傳": "cyun4",
      "傳媒": "cyun4 mui4",
      "傾計": "king1 gai2",
      "煩": "faan4",
      "運動": "wan6 dung6",
      "圖標": "tou4 biu1",
      "實際": "sat6 zai3",
      "監察": "gaam1 caat3",
      "端": "dyun1",
      "網球": "mong5 kau4",
      "齊": "cai4",
      "價": "gaa3",
      "嘴": "zeoi2",
      "撈": "lou1",
      "數學": "sou3 hok6",
      "擁有": "jung2 jau5",
      "輸": "syu1",
      "隨時": "ceoi4 si4"
    });
    function isNeutralLexicalEntry(entry) {
      return Boolean(entry) && entry.label === "lex" && entry.pos === "lexical_item" && entry.syntax === "lexical_item";
    }
    function applyReviewedEntries(entries) {
      if (!Array.isArray(entries)) throw new TypeError("ranks 1751–2000 reviewed overlay requires an entry array");
      return entries.map(([surface, entry]) => {
        const promotion = PROMOTIONS[surface];
        let next = entry;
        if (promotion && isNeutralLexicalEntry(next)) next = { ...next, label: promotion.label, pos: promotion.pos, syntax: promotion.syntax, note: "Reviewed Cifu ranks 1751–2000 lexical selection: " + promotion.gloss, provenance: { kind: "reviewed_lexical_promotion", source: SOURCE, rank: promotion.rank, pronunciation_status: promotion.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted", prior_provenance: next.provenance || null } };
        const reviewedReading = DEFAULT_READING_OVERRIDES[surface];
        if (reviewedReading && next && next.jyutping !== reviewedReading) next = { ...next, jyutping: reviewedReading, provenance: { kind: isNeutralLexicalEntry(next) ? "reviewed_candidate_default_pronunciation" : "reviewed_default_pronunciation_correction", source: SOURCE, pronunciation_status: "reviewed_explicit_reading", prior_provenance: next.provenance || null } };
        return [surface, next];
      });
    }
    function defaultAnalysis(surface, entry) {
      return Object.freeze({ id: "lex:" + surface + ":default", label: entry.label || "neutral", pos: entry.pos || "lexical_item", jyutping: entry.jyutping || "", syntax: entry.syntax || "lexical_candidate", senses: Object.freeze([{ gloss: entry.note || "existing runtime default preserved" }]), provenance: Object.freeze({ kind: "existing_runtime_default_preserved", source: "v0.5.236 token lexicon before ranks 1751–2000 alternatives", prior_provenance: entry.provenance || null }) });
    }
    function reviewedAlternative(surface, spec, defaultEntry) {
      return Object.freeze({ id: "lex:" + surface + ":r" + spec.rank + ":" + spec.suffix, label: spec.label, pos: spec.pos, jyutping: spec.jyutping || defaultEntry.jyutping || "", syntax: spec.syntax, senses: Object.freeze([{ gloss: spec.gloss }]), provenance: Object.freeze({ kind: "reviewed_lexical_analysis", source: SOURCE, rank: spec.rank, pronunciation_status: spec.jyutping ? "reviewed_explicit_reading" : "inherited_runtime_candidate_not_independently_promoted" }) });
    }
    function buildExplicitAnalyses(entries) {
      const defaults = new Map(entries || []);
      const out = /* @__PURE__ */ Object.create(null);
      for (const [surface, specs] of Object.entries(ALTERNATIVE_SPECS)) {
        const entry = defaults.get(surface);
        if (!entry) throw new Error("ranks 1751–2000 explicit analyses reference missing runtime surface: " + surface);
        const rows = [defaultAnalysis(surface, entry), ...specs.map((spec) => reviewedAlternative(surface, spec, entry))];
        const seen = /* @__PURE__ */ new Set();
        for (const row of rows) {
          if (!row.jyutping) throw new Error(row.id + ": explicit lexical analysis requires non-empty jyutping");
          if (seen.has(row.id)) throw new Error(row.id + ": duplicate ranks 1751–2000 stable analysis ID");
          seen.add(row.id);
        }
        out[surface] = Object.freeze(rows);
      }
      return Object.freeze(out);
    }
    module2.exports = Object.freeze({ SOURCE, PROMOTIONS, RESEARCH_REQUIRED_SURFACES, BLOCKED_ATOMIC_SURFACES, MULTI_SURFACES, READING_SPLIT_SURFACES, ALTERNATIVE_SPECS, CANDIDATE_ONLY_SURFACES, DEFAULT_READING_OVERRIDES, isNeutralLexicalEntry, applyReviewedEntries, buildExplicitAnalyses });
  }
});

// src/runtime-resources/lexicon/token-lexicon/cifu-r1751-2000-runtime-policy.js
var require_cifu_r1751_2000_runtime_policy = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/cifu-r1751-2000-runtime-policy.js"(exports2, module2) {
    "use strict";
    var reviewed = require_cifu_r1751_2000_reviewed();
    var MANDARIN_ONLY_SURFACES = /* @__PURE__ */ new Set();
    function applyRuntimePolicy(entries) {
      return reviewed.applyReviewedEntries(entries).filter(([surface]) => !MANDARIN_ONLY_SURFACES.has(surface));
    }
    module2.exports = Object.freeze({ SOURCE: reviewed.SOURCE, PROMOTIONS: reviewed.PROMOTIONS, RESEARCH_REQUIRED_SURFACES: reviewed.RESEARCH_REQUIRED_SURFACES, BLOCKED_ATOMIC_SURFACES: reviewed.BLOCKED_ATOMIC_SURFACES, CANDIDATE_ONLY_SURFACES: reviewed.CANDIDATE_ONLY_SURFACES, DEFAULT_READING_OVERRIDES: reviewed.DEFAULT_READING_OVERRIDES, MANDARIN_ONLY_SURFACES, applyRuntimePolicy });
  }
});

// src/runtime-resources/lexicon/token-lexicon/vernacular-component-coverage.js
var require_vernacular_component_coverage = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/vernacular-component-coverage.js"(exports2, module2) {
    "use strict";
    var SOURCE = "Most Common Cantonese Words (Frequency List)";
    var SOURCE_URL = "https://docs.google.com/spreadsheets/d/1ArxEFo46PTrDyDDhWyu3wB0epxqTyd8WBaprnwTEPm4";
    var READINGS = Object.freeze({
      "世": "sai3",
      "並": "bing6",
      "主": "zyu2",
      "也": "jaa5",
      "仁": "jan4",
      "介": "gaai3",
      "付": "fu6",
      "伙": "fo2",
      "佑": "jau6",
      "倉": "cong1",
      "們": "mun4",
      "傷": "seung1",
      "兄": "hing1",
      "充": "cung1",
      "兒": "ji4",
      "內": "noi6",
      "准": "zeon2",
      "勸": "hyun3",
      "危": "ngai4",
      "友": "jau5",
      "反": "faan2",
      "叔": "suk1",
      "吩": "fan1",
      "吹": "ceoi1",
      "周": "zau1",
      "呼": "fu1",
      "咐": "fu3",
      "哈": "haa1",
      "唏": "hei1",
      "商": "soeng1",
      "啖": "daam6",
      "嗱": "naa4",
      "噚": "cam4",
      "器": "hei3",
      "回": "wui4",
      "執": "zap1",
      "基": "gei1",
      "堅": "gin1",
      "堯": "jiu4",
      "天": "tin1",
      "奇": "kei4",
      "奈": "noi6",
      "奮": "fan5",
      "妹": "mui6",
      "姑": "gu1",
      "娘": "noeng4",
      "婚": "fan1",
      "嫁": "ga3",
      "嫂": "sou2",
      "子": "zi2",
      "孝": "haau3",
      "客": "haak3",
      "容": "jung4",
      "密": "mat6",
      "察": "caat3",
      "寶": "bou2",
      "專": "zyun1",
      "尋": "cam4",
      "尬": "gaai3",
      "展": "zin2",
      "師": "si1",
      "幸": "hang6",
      "床": "cong4",
      "廈": "haa6",
      "廣": "gwong2",
      "弟": "dai6",
      "彩": "coi2",
      "律": "leot6",
      "德": "dak1",
      "志": "zi3",
      "忘": "mong4",
      "忠": "zung1",
      "急": "gap1",
      "息": "sik1",
      "愈": "jyu6",
      "慣": "gwaan3",
      "憎": "zang1",
      "戚": "cik1",
      "承": "sing4",
      "投": "tau4",
      "抖": "dau2",
      "抽": "cau1",
      "拒": "keoi5",
      "招": "ziu1",
      "持": "ci4",
      "推": "teoi1",
      "擇": "zaak6",
      "救": "gau3",
      "敗": "baai6",
      "敬": "ging3",
      "斬": "zaam2",
      "施": "si1",
      "既": "ge3",
      "李": "lei5",
      "栽": "zoi1",
      "桂": "gwai3",
      "梯": "tai1",
      "極": "gik6",
      "榮": "wing4",
      "欠": "him3",
      "款": "fun2",
      "歷": "lik6",
      "母": "mou5",
      "毒": "duk6",
      "氣": "hei3",
      "求": "kau4",
      "注": "zyu3",
      "派": "paai3",
      "流": "lau4",
      "淚": "leoi6",
      "湊": "cau3",
      "滿": "mun5",
      "潘": "pun1",
      "潮": "ciu4",
      "濕": "sap1",
      "火": "fo2",
      "爆": "baau3",
      "父": "fu6",
      "爸": "ba1",
      "爺": "je4",
      "猶": "jau4",
      "獨": "duk6",
      "珍": "zan1",
      "甘": "gam1",
      "畢": "bat1",
      "癲": "din1",
      "登": "dang1",
      "目": "muk6",
      "示": "si6",
      "福": "fuk1",
      "禮": "lai5",
      "秀": "sau3",
      "秒": "miu5",
      "秘": "bei3",
      "窮": "kung4",
      "章": "zoeng1",
      "篇": "pin1",
      "簡": "gaan2",
      "系": "hai6",
      "絕": "zyut6",
      "統": "tung2",
      "維": "wai4",
      "緒": "seoi5",
      "繩": "sing4",
      "耳": "ji5",
      "聞": "man4",
      "職": "zik1",
      "肚": "tou5",
      "肯": "hang2",
      "膊": "bok3",
      "膽": "daam2",
      "臉": "lim5",
      "般": "bun1",
      "良": "loeng4",
      "芝": "zi1",
      "藝": "ngai6",
      "虎": "fu2",
      "術": "seot6",
      "衝": "cung1",
      "表": "biu2",
      "裙": "kwan4",
      "裝": "zong1",
      "褲": "fu3",
      "襯": "can3",
      "言": "jin4",
      "討": "tou2",
      "記": "gei3",
      "詞": "ci4",
      "認": "jing6",
      "語": "jyu5",
      "誤": "ng6",
      "諒": "loeng6",
      "謝": "ze6",
      "警": "ging2",
      "議": "ji5",
      "豫": "jyu6",
      "貌": "maau6",
      "財": "coi4",
      "資": "zi1",
      "賊": "caak6",
      "賴": "laai6",
      "賽": "coi3",
      "趕": "gon2",
      "足": "zuk1",
      "距": "keoi5",
      "辦": "baan6",
      "辭": "ci4",
      "進": "zeon3",
      "逼": "bik1",
      "酒": "zau2",
      "野": "je5",
      "阻": "zo2",
      "際": "zai3",
      "隨": "ceoi4",
      "險": "him2",
      "離": "lei4",
      "雲": "wan4",
      "霜": "soeng1",
      "露": "lou6",
      "非": "fei1",
      "鞋": "haai4",
      "題": "tai4",
      "駛": "sai2",
      "鬆": "sung1",
      "黎": "lai4",
      "龍": "lung4",
      "圍": "wai4",
      "嗌": "aai3",
      "嚇": "haak3",
      "姨": "ji4",
      "安": "on1",
      "廚": "cyu4",
      "掛": "gwaa3",
      "玉": "juk6",
      "現": "jin6",
      "盒": "hap6",
      "紹": "siu6",
      "親": "can1",
      "觀": "gun1",
      "贏": "jeng4",
      "輕": "hing1",
      "造": "zou6",
      "馮": "fung4",
      "士": "si6",
      "契": "kai3",
      "強": "koeng4",
      "抱": "pou5",
      "搶": "coeng2",
      "散": "saan3",
      "枝": "zi1",
      "歉": "him3",
      "串": "cyun3",
      "仆": "puk1",
      "便": "bin6",
      "冚": "kam2",
      "利": "lei6",
      "參": "caam1",
      "吧": "baa1",
      "呆": "ngoi4",
      "命": "ming6",
      "哼": "hang1",
      "娜": "naa4",
      "惡": "ok3",
      "撚": "nan2",
      "斷": "dyun6",
      "朝": "ziu1",
      "王": "wong4",
      "硬": "ngaang6",
      "立": "laap6",
      "悉": "sik1",
      "惜": "sik1",
      "慮": "leoi6",
      "絡": "lok3",
      "蝕": "sit6",
      "觸": "zuk1",
      "訊": "seon3"
    });
    function applyVernacularComponentCoverage(entries) {
      const seen = /* @__PURE__ */ new Set();
      const out = entries.map(([surface, entry]) => {
        seen.add(surface);
        const reading = READINGS[surface];
        if (!reading || String(entry && entry.jyutping || "").trim()) return [surface, entry];
        return [surface, {
          ...entry,
          jyutping: reading,
          note: [entry && entry.note, `Pronunciation filled from ${SOURCE}; bounded vernacular parser audit exposed a missing component reading.`].filter(Boolean).join(" ")
        }];
      });
      for (const [surface, jyutping] of Object.entries(READINGS)) {
        if (seen.has(surface)) continue;
        out.push([surface, {
          label: "lex",
          pos: "lexical_item",
          syntax: "lexical_item",
          jyutping,
          note: `Neutral component pronunciation coverage from ${SOURCE}; no grammar/category promotion is implied.`,
          provenance: { kind: "external_vernacular_component_reading", source: SOURCE, url: SOURCE_URL }
        }]);
      }
      return out;
    }
    module2.exports = Object.freeze({ SOURCE, SOURCE_URL, READINGS, applyVernacularComponentCoverage });
  }
});

// src/runtime-resources/lexicon/token-lexicon/people-and-address.js
var require_people_and_address = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/people-and-address.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Pronouns / people
      ["我", { label: "who", jyutping: "ngo5", syntax: "subject_or_topic", note: "I / me" }],
      ["你", { label: "who", jyutping: "nei5", syntax: "subject_or_topic", note: "you" }],
      ["佢", { label: "who", jyutping: "keoi5", syntax: "subject_or_topic", note: "he / she / they singular" }],
      ["我哋", { label: "who", jyutping: "ngo5 dei6", syntax: "subject_or_topic", note: "we / us" }],
      ["你哋", { label: "who", jyutping: "nei5 dei6", syntax: "subject_or_topic", note: "you plural" }],
      ["佢哋", { label: "who", jyutping: "keoi5 dei6", syntax: "subject_or_topic", note: "they" }],
      ["媽媽", { label: "who", jyutping: "maa4 maa1", syntax: "person_np", note: "mother" }],
      ["爸爸", { label: "who", jyutping: "baa1 baa1", syntax: "person_np", note: "father" }],
      ["老師", { label: "who", jyutping: "lou5 si1", syntax: "person_np", classifier_classes: ["person"], note: "teacher" }],
      ["經理", { label: "who", jyutping: "ging1 lei5", syntax: "person_np title_np", note: "manager; reviewed CP021B heavy-theme boundary participant." }],
      ["朋友", { label: "who", jyutping: "pang4 jau5", syntax: "person_np", note: "friend" }],
      ["小朋友", { label: "who", jyutping: "siu2 pang4 jau5", syntax: "person_np", note: "child" }],
      ["細路", { label: "who", jyutping: "sai3 lou6", syntax: "person_np", note: "child / kid" }],
      ["老闆", { label: "who", jyutping: "lou5 baan2", syntax: "person_np", note: "boss" }],
      ["醫生", { label: "who", jyutping: "ji1 saang1", syntax: "person_np", classifier_classes: ["person"], note: "doctor" }],
      ["人", { label: "who", jyutping: "jan4", syntax: "person_np", note: "person" }],
      ["人哋", { label: "who", jyutping: "jan4 dei6", syntax: "person_np plural_or_indefinite_pronoun", note: "people / other people / they, depending on context" }],
      ["大家", { label: "who", jyutping: "daai6 gaa1", syntax: "person_np collective_pronoun", note: "everyone / everybody" }],
      ["狗", { label: "who", jyutping: "gau2", syntax: "animal_agent_np person_like_agent_np", classifier_classes: ["animal"], note: "dog; agent-like participant in affectedness examples." }],
      ["貓", { label: "who", jyutping: "maau1", syntax: "animal_np head_noun object_np", classifier_classes: ["animal"], note: "cat." }],
      ["志明", { label: "who", jyutping: "zi3 ming4", syntax: "proper_name", note: "proper name" }],
      ["美玲", { label: "who", jyutping: "mei5 ling4", syntax: "proper_name", note: "proper name" }],
      ["黃志強", { label: "who", jyutping: "wong4 zi3 koeng4", syntax: "proper_name", note: "proper name from lesson" }],
      ["張美玲", { label: "who", jyutping: "zoeng1 mei5 ling4", syntax: "proper_name", note: "proper name from lesson" }],
      ["阿強", { label: "who", jyutping: "aa3 koeng4", syntax: "proper_name_or_nickname", note: "nickname/proper name; lesson context may be confusing because speaker is also 志強." }],
      ["阿媽", { label: "who", jyutping: "aa3 maa1", syntax: "kinship_person_np", note: "mum / mother; reviewed CP021B kinship participant." }],
      ["阿明", { label: "who", jyutping: "aa3 ming4", syntax: "proper_name_or_nickname", note: "Ah Ming; reviewed CP021B name participant." }],
      ["張三", { label: "who", jyutping: "zoeng1 saam1", syntax: "proper_name", note: "Cheung Saam; name in a reviewed CP021B source example." }],
      // Address-form Jyutping seeds. These do not create new parser constructions; they
      // only provide pronunciation data for fallback address-form children and protected
      // kinship/title controls surfaced by the Jyutping diagnostics audit.
      ["阿", { label: "who", jyutping: "aa3", syntax: "address_prefix", note: "Familiar Cantonese name/address prefix 阿." }],
      ["霞", { label: "who", jyutping: "haa4", syntax: "name_element", note: "Name element used in native-speech address-form diagnostics." }],
      ["姐", { label: "who", jyutping: "ze2", syntax: "address_suffix_or_kinship", note: "Address suffix / elder-sister title element." }],
      ["明", { label: "who", jyutping: "ming4", syntax: "name_element", note: "Name element used in address-form diagnostics." }],
      ["哥", { label: "who", jyutping: "go1", syntax: "address_suffix_or_kinship", note: "Address suffix / elder-brother title element." }],
      ["陳", { label: "who", jyutping: "can4", syntax: "surname_element", note: "Surname element used in address-form diagnostics." }],
      ["生", { label: "who", jyutping: "saang1", syntax: "address_suffix", note: "Abbreviated address suffix from 先生 in names such as 陳生." }],
      ["先生", { label: "who", jyutping: "sin1 saang1", syntax: "person_title", note: "Mr. / teacher / gentleman; protected from fallback named-address overgeneration when standalone." }],
      ["家姐", { label: "who", jyutping: "gaa1 ze2", syntax: "kinship_term", note: "elder sister; protected from fallback named-address overgeneration." }],
      ["小姐", { label: "who", jyutping: "siu2 ze2", syntax: "person_title", note: "Miss / young lady; protected from fallback named-address overgeneration." }],
      ["姐姐", { label: "who", jyutping: "ze4 ze2", syntax: "kinship_term", note: "older sister; protected from fallback named-address overgeneration." }],
      ["姐夫", { label: "who", jyutping: "ze2 fu1", syntax: "kinship_term", note: "older sister's husband; protected from fallback named-address overgeneration." }],
      ["姐妹", { label: "who", jyutping: "ze2 mui6", syntax: "kinship_collective", note: "sisters; protected from fallback named-address overgeneration." }],
      ["大姐", { label: "who", jyutping: "daai6 ze2", syntax: "kinship_or_address_title", note: "eldest sister / senior address title; protected from fallback named-address overgeneration." }],
      ["阿姐", { label: "who", jyutping: "aa3 ze2", syntax: "kinship_or_address_title", note: "address/kinship title; protected from fallback named-address overgeneration." }],
      ["學生", { label: "who", jyutping: "hok6 saang1", syntax: "person_np", note: "student; protected from fallback named-address overgeneration." }],
      ["其他", { label: "func", pos: "determiner", jyutping: "kei4 taa1", syntax: "nominal_modifier determiner_like_modifier modifier", note: "other; determiner-like nominal modifier before a head noun" }],
      ["同事", { label: "who", jyutping: "tung4 si6", syntax: "person_np", note: "coworker" }],
      ["其他同事", { label: "who", jyutping: "kei4 taa1 tung4 si6", syntax: "person_np", note: "other coworkers; rendered transparently as a modified noun phrase when possible." }],
      ["公司", { label: "what", jyutping: "gung1 si1", syntax: "organization_np head_noun object_np", classifier_classes: ["general_count"], note: "company / organization" }],
      // v0.5.227: Cifu spoken top-2000 R5 lexical coverage.
      ["女仔", { "label": "who", "jyutping": "neoi5 zai2", "syntax": "person_np female_person_np", "note": "girl / young woman; Cifu spoken top-2000 R5 lexical audit." }],
      ["男朋友", { "label": "who", "jyutping": "naam4 pang4 jau5", "syntax": "person_np relationship_person_np", "note": "boyfriend / male friend; Cifu spoken top-2000 R5 lexical audit." }],
      ["男人", { "label": "who", "jyutping": "naam4 jan2", "syntax": "person_np male_person_np", "note": "man / male; Cifu spoken top-2000 R5 lexical audit." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/referents-and-boundaries.js
var require_referents_and_boundaries = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/referents-and-boundaries.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Demonstratives / referents
      ["呢個", { label: "what", jyutping: "ni1 go3", syntax: "demonstrative_np_or_topic", note: "this one" }],
      ["嗰個", { label: "what", jyutping: "go2 go3", syntax: "demonstrative_np_or_topic", note: "that one" }],
      ["嗰", { label: "func", pos: "determiner", jyutping: "go2", syntax: "demonstrative_determiner", note: "that; specifier/determiner, not a head noun" }],
      ["邊", { label: "func", pos: "determiner", jyutping: "bin1", syntax: "wh_determiner", note: "which; wh-determiner, not a head noun" }],
      ["間", { label: "measure_word", jyutping: "gaan1", syntax: "classifier_building_shop", note: "measure word / classifier for rooms, shops, restaurants, buildings" }],
      ["張", { label: "measure_word", jyutping: "zoeng1", syntax: "classifier_flat_surface_table", note: "measure word / classifier for flat objects and tables" }],
      ["杯", { label: "measure_word", jyutping: "bui1", syntax: "classifier_container_cup", note: "measure word / cup classifier for drinks" }],
      ["套", { label: "measure_word", jyutping: "tou3", syntax: "classifier_set_media", note: "measure word / classifier for films, shows, sets, and series" }],
      ["首", { label: "measure_word", jyutping: "sau2", syntax: "classifier_song_poem", note: "measure word / classifier for songs and poems" }],
      ["件", { label: "measure_word", jyutping: "gin6", syntax: "classifier_clothing_item", note: "measure word / classifier for clothing/items" }],
      ["隻", { label: "measure_word", jyutping: "zek3", syntax: "classifier_animal_body_part_one_of_pair", note: "measure word / classifier for animals, body parts, and one of a pair" }],
      ["幅", { label: "measure_word", jyutping: "fuk1", syntax: "classifier_picture_flat_work", note: "measure word / classifier for pictures and flat works" }],
      ["呢間", { label: "what", jyutping: "ni1 gaan1", syntax: "demonstrative_classifier_np", note: "this one/building/shop; long restaurant NPs render transparently when possible." }],
      ["邊間", { label: "what", jyutping: "bin1 gaan1", syntax: "wh_classifier_np", note: "which one/shop/building; 邊間呀 renders transparently when possible." }],
      ["嗰間", { label: "what", jyutping: "go2 gaan1", syntax: "demonstrative_classifier_np", note: "that one/shop/building; long restaurant NPs render transparently when possible." }],
      ["呢間餐廳", { label: "what", jyutping: "ni1 gaan1 caan1 teng1", syntax: "topic_or_object_np", note: "this restaurant" }],
      ["嗰間餐廳", { label: "what", jyutping: "go2 gaan1 caan1 teng1", syntax: "topic_or_object_np", note: "that restaurant" }],
      ["嗰間新開嘅意大利餐廳", { label: "what", jyutping: "go2 gaan1 san1 hoi1 ge3 ji3 daai6 lei6 caan1 teng1", syntax: "modified_np", note: "that newly opened Italian restaurant" }],
      ["呢啲", { label: "what", jyutping: "ni1 di1", syntax: "demonstrative_np_or_topic", note: "these" }],
      ["嗰啲", { label: "what", jyutping: "go2 di1", syntax: "demonstrative_np_or_topic", note: "those" }],
      ["呢度", { label: "where", jyutping: "ni1 dou6", syntax: "place", note: "here" }],
      ["嗰度", { label: "where", jyutping: "go2 dou6", syntax: "place", note: "there" }],
      ["邊個", { label: "who", jyutping: "bin1 go3", syntax: "wh_person", note: "who" }],
      ["邊度", { label: "where", jyutping: "bin1 dou6", syntax: "wh_place", note: "where" }],
      ["咩", { label: "what", jyutping: "me1", syntax: "wh_thing direct_nominal_wh_determiner question_particle wh_or_particle", note: "what / what-kind-of / question particle; construction context decides whether it directly modifies a noun, fills a wh-object/complement, or functions as a sentence-final surprise particle." }],
      ["乜嘢", { label: "what", jyutping: "mat1 je5", syntax: "wh_thing", note: "what" }],
      ["幾時", { label: "when", jyutping: "gei2 si4", syntax: "wh_time", note: "when" }],
      ["點解", { label: "why", jyutping: "dim2 gaai2", syntax: "wh_reason", note: "why" }],
      ["點", { label: "how", jyutping: "dim2", syntax: "wh_manner", note: "how" }],
      ["點樣", { label: "how", jyutping: "dim2 joeng2", syntax: "evaluation_wh_predicate", note: "how / what kind; asks for evaluation" }],
      ["幾錢", { label: "how", jyutping: "gei2 cin2", syntax: "wh_price scalar_value money_amount", note: "how much money; asks about price/value on a scale" }],
      ["邊間呀", { label: "what", jyutping: "bin1 gaan1 aa3", syntax: "wh_fragment", note: "which one?" }],
      // v0.5.153: Extend broad MotionGoalVP to transparent perfective motion-to-destination predicates
      // such as 去咗香港. Reuse the existing public label; perfective aspect remains an internal
      // relation, and wh-place expressions retain LocativeWhQuestion precedence.
      // v0.5.152: Broad perfective locative-wh question support is implemented in the
      // generated construction templates below; lexical entries remain unchanged.
      // v0.5.151: W17 lexical-boundary blockers. These multi-character lexical items
      // prevent valid words from being split into independently productive grammar material.
      ["正題", { label: "what", jyutping: "zing3 tai4", syntax: "topic_np object_np discourse_topic", note: "main topic / main point; lexical boundary blocks the false 正 stative parse in 講返正題." }],
      ["最近", { label: "when", jyutping: "zeoi3 gan6", syntax: "temporal_adjunct time_expression", note: "recently / lately; lexical boundary blocks the false 最 + 近 modifier parse." }],
      // v0.5.150-r1: W17 lexicon import after learner-role/internal-slot review.
      // Learner roles are sentence-facing display defaults; syntax strings carry lexical/internal affordances.
      // Negative stative/preference phrases and large numerals remain registered but tokenize compositionally.
      ["唔開心", { label: "like", jyutping: "m4 hoi1 sam1", syntax: "negative_stative_phrase compositional_lexicon_entry", note: "unhappy; registered from W17 but forced to tokenize as 唔 + 開心 so the negator remains visible." }],
      ["攰", { label: "like", jyutping: "gui6", syntax: "stative_predicate emotion_state_stative", note: "tired; W17 lexicon expansion." }],
      ["嬲", { label: "like", jyutping: "nau1", syntax: "stative_predicate emotion_state_stative", note: "angry; W17 lexicon expansion." }],
      ["驚", { label: "like", jyutping: "geng1", syntax: "stative_predicate emotion_state_stative", note: "scared/startled/surprised; W17 lexicon expansion." }],
      ["擔心", { label: "doing", jyutping: "daam1 sam1", syntax: "mental_state_verb cognition_predicate", note: "worry / be worried; learner-visible verb role remains doing while cognition is internal metadata." }],
      ["悶", { label: "like", jyutping: "mun6", syntax: "stative_predicate emotion_state_stative", note: "bored; W17 lexicon expansion." }],
      ["緊張", { label: "like", jyutping: "gan2 zoeng1", syntax: "stative_predicate emotion_state_stative", note: "nervous/tense; W17 lexicon expansion." }],
      ["壓力", { label: "what", jyutping: "aat3 lik6", syntax: "abstract_noun object_np topic_np", note: "pressure/stress; W17 lexicon expansion." }],
      ["辛苦", { label: "like", jyutping: "san1 fu2", syntax: "stative_predicate emotion_state_stative", note: "tough/tiring/laborious; W17 lexicon expansion." }],
      ["感動", { label: "like", jyutping: "gam2 dung6", syntax: "stative_predicate emotion_state_stative", note: "emotionally moved/touched; W17 lexicon expansion." }],
      ["激動", { label: "like", jyutping: "gik1 dung6", syntax: "stative_predicate emotion_state_stative", note: "emotionally excited/agitated; W17 lexicon expansion." }],
      ["失望", { label: "like", jyutping: "sat1 mong6", syntax: "stative_predicate emotion_state_stative", note: "disappointed; W17 lexicon expansion." }],
      ["放鬆", { label: "doing", jyutping: "fong3 sung1", syntax: "verb action_verb", note: "relax; W17 lexicon expansion." }],
      ["傷心", { label: "like", jyutping: "soeng1 sam1", syntax: "stative_predicate emotion_state_stative", note: "sad/heartbroken; W17 lexicon expansion." }],
      ["唔鍾意", { label: "like", jyutping: "m4 zung1 ji3", syntax: "negative_preference_phrase compositional_lexicon_entry", note: "dislike/not like; registered from W17 but forced to tokenize as 唔 + 鍾意 so negation and preference remain transparent." }],
      ["興奮", { label: "like", jyutping: "hing1 fan5", syntax: "stative_predicate emotion_state_stative", note: "excited; W17 lexicon expansion." }],
      ["孤獨", { label: "like", jyutping: "gu1 duk6", syntax: "stative_predicate emotion_state_stative", note: "lonely; W17 lexicon expansion." }],
      ["滿足", { label: "like", jyutping: "mun5 zuk1", syntax: "stative_predicate emotion_state_stative", note: "satisfied; W17 lexicon expansion." }],
      ["驕傲", { label: "like", jyutping: "giu1 ngou6", syntax: "stative_predicate emotion_state_stative", note: "proud/arrogant depending on context; W17 lexicon expansion." }],
      ["嫉妒", { label: "doing", jyutping: "zat6 dou3", syntax: "mental_state_verb cognition_predicate", note: "be jealous/envious; learner-visible verb role doing, with mental-state class internal." }],
      ["焦慮", { label: "like", jyutping: "ziu1 leoi6", syntax: "stative_predicate emotion_state_stative", note: "anxious; W17 lexicon expansion." }],
      ["感激", { label: "doing", jyutping: "gam2 gik1", syntax: "mental_state_verb cognition_predicate", note: "feel grateful/appreciative; learner-visible verb role doing." }],
      ["慚愧", { label: "like", jyutping: "caam4 kwai5", syntax: "stative_predicate emotion_state_stative", note: "ashamed; W17 lexicon expansion." }],
      ["害怕", { label: "doing", jyutping: "hoi6 paa3", syntax: "mental_state_verb cognition_predicate", note: "fear/be afraid of; learner-visible verb role doing." }],
      ["感恩", { label: "doing", jyutping: "gam2 jan1", syntax: "mental_state_verb cognition_predicate", note: "feel thankful/grateful; learner-visible verb role doing." }],
      ["好奇", { label: "like", jyutping: "hou2 kei4", syntax: "stative_predicate emotion_state_stative", note: "curious; W17 source reading retained for review." }],
      ["樂觀", { label: "like", jyutping: "lok6 gun1", syntax: "stative_predicate emotion_state_stative", note: "optimistic; W17 lexicon expansion." }],
      // Authorized Glossika Week 16 hobbies/free-time lexical slice. These entries add lexical
      // and pronunciation coverage only; they license no new construction or productivity claim.
      ["游", { label: "doing", jyutping: "jau4", syntax: "verb swimming_verb action_verb", note: "swim; component of 游水." }],
      ["跑", { label: "doing", jyutping: "paau2", syntax: "verb running_verb action_verb", note: "run; component of 跑步." }],
      ["步", { label: "what", jyutping: "bou6", syntax: "step_noun activity_object", note: "step / running activity element in 跑步." }],
      ["影", { label: "doing", jyutping: "jing2", syntax: "verb photographing_verb transitive_affordance", note: "photograph / take a photo." }],
      ["相", { label: "what", jyutping: "soeng2", syntax: "photograph_noun object_np", note: "photograph / photo." }],
      ["唱", { label: "doing", jyutping: "coeng3", syntax: "verb singing_verb transitive_affordance", note: "sing." }],
      ["K", { label: "what", jyutping: "kei1", syntax: "code_switched_karaoke_object latin_abbreviation", note: "pronounced letter name in 唱K / karaoke." }],
      ["街", { label: "where", jyutping: "gaai1", syntax: "place_np street_noun", note: "street; part of the conventional activity 行街." }],
      ["行街", { label: "doing", jyutping: "haang4 gaai1", syntax: "verb activity_predicate shopping_browsing_activity", note: "go shopping / browse shops." }],
      ["沙灘", { label: "where", jyutping: "saa1 taan1", syntax: "place_np destination_np beach_place", note: "beach; preserves 去 + 沙灘 as transparent motion plus destination." }],
      ["踢", { label: "doing", jyutping: "tek3", syntax: "verb kicking_verb transitive_affordance", note: "kick / play a kicking sport." }],
      ["波", { label: "what", jyutping: "bo1", syntax: "ball_or_sport_noun object_np", note: "ball / ball game depending on context." }],
      ["彈", { label: "doing", jyutping: "taan4", syntax: "verb instrument_playing_verb transitive_affordance", note: "play a musical instrument." }],
      ["琴", { label: "what", jyutping: "kam4", syntax: "musical_instrument_noun object_np", note: "piano or stringed keyboard instrument depending on context." }],
      ["釣", { label: "doing", jyutping: "diu3", syntax: "verb fishing_verb transitive_affordance", note: "fish / angle for." }],
      ["旅行", { label: "doing", jyutping: "leoi5 hang4", syntax: "verb activity_predicate travel_activity", note: "travel / take a trip." }],
      ["瑜伽", { label: "what", jyutping: "jyu4 gaa1", syntax: "activity_noun exercise_activity", note: "yoga; nominal activity entry, not silently converted into a verb." }],
      ["露營", { label: "doing", jyutping: "lou6 jing4", syntax: "verb activity_predicate camping_activity", note: "camp / go camping." }],
      ["棋", { label: "what", jyutping: "kei2", syntax: "board_game_noun object_np", note: "chess / board-game piece or game depending on context." }],
      ["行公園", { label: "doing", jyutping: "haang4 gung1 jyun2", syntax: "verb activity_predicate park_walking_activity", note: "walk in the park; kept as the source-listed conventional activity rather than a literal object relation." }],
      // Glossika Week 16 million expressions are registered but remain compositionally visible.
      ["一百萬", { label: "how", jyutping: "jat1 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "1 million; authorized Glossika Week 16 number lexicon." }],
      ["二百萬", { label: "how", jyutping: "ji6 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "2 million; authorized Glossika Week 16 number lexicon." }],
      ["三百萬", { label: "how", jyutping: "saam1 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "3 million; authorized Glossika Week 16 number lexicon." }],
      ["四百萬", { label: "how", jyutping: "sei3 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "4 million; authorized Glossika Week 16 number lexicon." }],
      ["五百萬", { label: "how", jyutping: "ng5 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "5 million; authorized Glossika Week 16 number lexicon." }],
      ["六百萬", { label: "how", jyutping: "luk6 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "6 million; authorized Glossika Week 16 number lexicon." }],
      ["七百萬", { label: "how", jyutping: "cat1 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "7 million; authorized Glossika Week 16 number lexicon." }],
      ["八百萬", { label: "how", jyutping: "baat3 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "8 million; authorized Glossika Week 16 number lexicon." }],
      ["九百萬", { label: "how", jyutping: "gau2 baak3 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "9 million; authorized Glossika Week 16 number lexicon." }],
      // W17 large-number entries are registered as lexical data but forced to remain compositionally visible.
      ["一千萬", { label: "how", jyutping: "jat1 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "10 million; W17 number lexicon." }],
      ["二千萬", { label: "how", jyutping: "ji6 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "20 million; W17 number lexicon." }],
      ["三千萬", { label: "how", jyutping: "saam1 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "30 million; W17 number lexicon." }],
      ["四千萬", { label: "how", jyutping: "sei3 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "40 million; W17 number lexicon." }],
      ["五千萬", { label: "how", jyutping: "ng5 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "50 million; W17 number lexicon." }],
      ["六千萬", { label: "how", jyutping: "luk6 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "60 million; W17 number lexicon." }],
      ["七千萬", { label: "how", jyutping: "cat1 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "70 million; W17 number lexicon." }],
      ["八千萬", { label: "how", jyutping: "baat3 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "80 million; W17 number lexicon." }],
      ["九千萬", { label: "how", jyutping: "gau2 cin1 maan6", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "90 million; W17 number lexicon." }],
      ["一億", { label: "how", jyutping: "jat1 jik1", syntax: "quantity count_value large_numeral compositional_lexicon_entry", note: "100 million; W17 number lexicon." }],
      ["千", { label: "how", jyutping: "cin1", syntax: "quantity numeric_unit thousand_unit", note: "thousand; numeric unit added for transparent W17 large-number tokenization." }],
      ["萬", { label: "how", jyutping: "maan6", syntax: "quantity numeric_unit ten_thousand_unit", note: "ten thousand; numeric unit added for transparent W17 large-number tokenization." }],
      ["億", { label: "how", jyutping: "jik1", syntax: "quantity numeric_unit hundred_million_unit", note: "hundred million; numeric unit added for transparent W17 large-number tokenization." }],
      // First bounded functional-corpus lexical slice.
      ["離題", { label: "doing", jyutping: "lei4 tai4", syntax: "verb topic_management_verb", note: "go off topic; W17 COR-002 lexical coverage." }],
      ["關於", { label: "func", jyutping: "gwaan1 jyu1", syntax: "relational_topic_coverb discourse_topic_marker", note: "regarding/about; W17 COR-003 lexical coverage." }],
      ["對於", { label: "func", jyutping: "deoi3 jyu1", syntax: "relational_topic_coverb discourse_topic_marker", note: "regarding/concerning; formal explicit topic-frame linker distinct from bare 對." }],
      ["OT", { label: "doing", jyutping: "ou1 ti1", syntax: "borrowed_verb overtime_verb code_switched", note: "work overtime; W17 COR-016 Hong Kong code-switching." }],
      ["夜", { label: "like", jyutping: "je6", syntax: "stative_predicate late_time_property", note: "late in degree-predicate uses such as 好夜; broader time-noun readings remain context-dependent." }],
      // W17 coverb/preposition additions not already present in the authoritative core lexicon.
      ["對", { label: "func", jyutping: "deoi3", syntax: "relational_coverb", note: "toward/regarding; construction determines the following participant/domain relation." }],
      ["除咗", { label: "func", jyutping: "ceoi4 zo2", syntax: "complex_preposition exclusion_marker", note: "except/apart from; W17 coverb/preposition expansion." }],
      // W17 phonics additions. Ambiguous noun/verb items keep one conservative lexical default; sentence constructions may override it.
      ["蘇", { label: "who", jyutping: "sou1", syntax: "surname_proper_name verb_ambiguous", note: "Su as a surname; verb sense 'revive' remains context-dependent." }],
      ["祖", { label: "who", jyutping: "zou2", syntax: "kinship_ancestor_noun person_np", note: "ancestor; W17 phonics expansion." }],
      ["全", { label: "like", jyutping: "cyun4", syntax: "stative_predicate completeness_property", note: "whole/complete; W17 phonics expansion." }],
      ["存", { label: "doing", jyutping: "cyun4", syntax: "verb existence_storage_verb", note: "store/exist; W17 phonics expansion." }],
      ["如", { label: "func", jyutping: "jyu4", syntax: "verb_conjunction comparison_or_condition_marker", note: "if/like/as; multifunctional and context-dependent." }],
      ["油", { label: "what", jyutping: "jau4", syntax: "object_np substance_noun", note: "oil; W17 phonics expansion." }],
      ["血", { label: "what", jyutping: "hyut3", syntax: "object_np substance_noun", note: "blood; W17 phonics expansion." }],
      ["闊", { label: "like", jyutping: "fut3", syntax: "stative_predicate width_property", note: "wide; W17 reading corrected to fut3." }],
      ["聯", { label: "doing", jyutping: "lyun4", syntax: "verb connection_verb", note: "connect/unite; W17 phonics expansion." }],
      ["論", { label: "what", jyutping: "leon6", syntax: "noun_verb discussion_or_theory", note: "theory/discussion; verb sense 'discuss' remains context-dependent." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/things.js
var require_things = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/things.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Things / nouns
      ["飯", { label: "what", jyutping: "faan6", syntax: "object_np", classifier_classes: ["food_bowl"], note: "rice / meal" }],
      ["嘢", { label: "what", jyutping: "je5", syntax: "object_np", note: "thing / stuff" }],
      ["水", { label: "what", jyutping: "seoi2", syntax: "object_np liquid drink_object water_noun", classifier_classes: ["liquid_measure"], note: "water; liquid/drink object, not generic food." }],
      ["茶", { label: "what", jyutping: "caa4", syntax: "object_np liquid drink_object beverage_noun", classifier_classes: ["liquid_measure"], note: "tea; drink/beverage object, not generic food." }],
      ["咖啡", { label: "what", jyutping: "gaa3 fe1", syntax: "object_np liquid drink_object beverage_noun", note: "coffee; drink/beverage object." }],
      ["奶", { label: "what", jyutping: "naai5", syntax: "object_np liquid drink_object beverage_noun", note: "milk; drink/liquid object." }],
      ["麵", { label: "what", jyutping: "min6", syntax: "object_np", note: "noodles" }],
      ["菜", { label: "what", jyutping: "coi3", syntax: "object_np", note: "vegetables" }],
      ["肉", { label: "what", jyutping: "juk6", syntax: "object_np", note: "meat" }],
      ["刀", { label: "what", jyutping: "dou1", syntax: "object_np instrument_np", note: "knife; overt resource/instrument noun." }],
      ["煲", { label: "what", jyutping: "bou1", syntax: "object_np instrument_np container_np", note: "pot / cooker; overt resource noun." }],
      ["魚", { label: "what", jyutping: "jyu4", syntax: "object_np", note: "fish" }],
      ["雞蛋", { label: "what", jyutping: "gai1 daan2", syntax: "object_np", note: "egg" }],
      ["蘋果", { label: "what", jyutping: "ping4 gwo2", syntax: "object_np food", semantic: ["food"], note: "apple" }],
      ["芒果", { label: "what", jyutping: "mong1 gwo2", syntax: "food_np object_np", note: "mango; native-speech mango-pickup diagnostic object." }],
      ["書", { label: "what", jyutping: "syu1", syntax: "object_np", classifier_classes: ["book"], note: "book" }],
      ["紙", { label: "what", jyutping: "zi2", syntax: "object_np flat_object_np", classifier_classes: ["flat_object"], note: "paper / sheet of paper; reviewed CP021B theme noun." }],
      ["筆", { label: "what", jyutping: "bat1", syntax: "object_np instrument_np", classifier_classes: ["long_rigid"], note: "pen; can be an instrument in 用 + NP + action frames." }],
      ["筷子", { label: "what", jyutping: "faai3 zi2", syntax: "object_np instrument_np", note: "chopsticks; can be an instrument in 用 + NP + action frames." }],
      ["書包", { label: "what", jyutping: "syu1 baau1", syntax: "object_np", note: "schoolbag" }],
      ["功課", { label: "what", jyutping: "gung1 fo3", syntax: "object_np", note: "homework" }],
      ["歌", { label: "what", jyutping: "go1", syntax: "object_np", note: "song" }],
      ["電話", { label: "what", jyutping: "din6 waa2", syntax: "object_np", classifier_classes: ["machine_device"], note: "telephone" }],
      ["故事", { label: "what", jyutping: "gu3 si6", syntax: "object_np story_np information_content", semantic: ["information_content", "readable_content"], note: "story; added as lexicon/Jyutping cleanup for UI example diagnostics." }],
      ["報告", { label: "what", jyutping: "bou3 gou3", syntax: "object_np document_np", note: "report; reviewed CP021B heavy-theme boundary noun." }],
      ["籃球", { label: "what", jyutping: "laam4 kau4", syntax: "object_np sport_object", note: "basketball; overt object in a preserved CP020 permissive example." }],
      ["戲", { label: "what", jyutping: "hei3", syntax: "object_np media_np", note: "film / show / play" }],
      ["衫", { label: "what", jyutping: "saam1", syntax: "object_np clothing_np", classifier_classes: ["clothing"], note: "clothing / shirt" }],
      ["湯", { label: "what", jyutping: "tong1", syntax: "food_np soup_np", note: "soup" }],
      ["手", { label: "what", jyutping: "sau2", syntax: "body_part_np", note: "hand" }],
      ["覺", { label: "what", jyutping: "gaau3", syntax: "object_in_vo", note: "sleep object in 瞓覺" }],
      ["學", { label: "what", jyutping: "hok6", syntax: "object_in_vo", note: "schooling object in 返學/放學" }],
      ["涼", { label: "what", jyutping: "loeng4", syntax: "object_in_vo", note: "bath object in 沖涼" }],
      ["太陽", { label: "what", jyutping: "taai3 joeng4", syntax: "object_np", note: "sun" }],
      ["麻雀", { label: "what", jyutping: "maa4 zoek3", syntax: "object_np", note: "mahjong / sparrow" }],
      ["股票", { label: "what", jyutping: "gu2 piu3", syntax: "object_np", note: "stocks" }],
      ["脾氣", { label: "what", jyutping: "pei4 hei3", syntax: "object_np", note: "temper" }],
      ["聲", { label: "what", jyutping: "seng1", syntax: "object_np", note: "sound / voice" }],
      ["名", { label: "what", jyutping: "meng2", syntax: "object_np", note: "name" }],
      ["名字", { label: "what", jyutping: "meng2 zi6", syntax: "object_np", note: "name" }],
      ["錢", { label: "what", jyutping: "cin2", syntax: "object_np", note: "money" }],
      ["問題", { label: "what", jyutping: "man6 tai4", syntax: "object_np", classifier_classes: ["general_count"], note: "problem" }],
      ["意粉", { label: "what", jyutping: "ji3 fan2", syntax: "food_np", note: "pasta" }],
      ["薄餅", { label: "what", jyutping: "bok6 beng2", syntax: "food_np", note: "pizza" }],
      ["食物", { label: "what", jyutping: "sik6 mat6", syntax: "food_np", note: "food" }],
      ["菜式", { label: "what", jyutping: "coi3 sik1", syntax: "food_np", note: "dish / cuisine item" }],
      ["特別菜式", { label: "what", jyutping: "dak6 bit6 coi3 sik1", syntax: "food_np", note: "special dishes" }],
      ["枱", { label: "what", jyutping: "toi2", syntax: "object_np location_base", note: "table; nominal location base inside spatial-localizer phrases such as 枱上面" }],
      ["牆", { label: "what", jyutping: "coeng4", syntax: "object_np location_base surface_location_noun", note: "wall; nominal location base inside spatial-localizer phrases such as 牆上面" }],
      ["上面", { label: "where", jyutping: "soeng6 min6", syntax: "place_or_goal spatial_localizer upper_surface_localizer", note: "above / on top / on the surface; combines with a visible nominal location base" }],
      ["消息", { label: "what", jyutping: "siu1 sik1", syntax: "object_np", note: "news / update" }],
      ["主意", { label: "what", jyutping: "zyu2 ji3", syntax: "object_np", note: "idea" }],
      ["好主意", { label: "func", jyutping: "hou2 zyu2 ji3", syntax: "formula_evaluation", note: "good idea" }],
      ["興趣", { label: "what", jyutping: "hing3 ceoi3", syntax: "object_np", note: "interest" }],
      ["意大利", { label: "what", jyutping: "ji3 daai6 lei6", syntax: "modifier_or_place_name", note: "Italy / Italian" }],
      ["意大利餐廳", { label: "what", jyutping: "ji3 daai6 lei6 caan1 teng1", syntax: "restaurant_np", note: "Italian restaurant; place entity, but learner-visible role defaults to what/topic unless used as a location." }],
      ["環境", { label: "what", jyutping: "waan4 ging2", syntax: "object_np", note: "environment / ambience" }],
      ["價", { label: "what", jyutping: "gaa3", syntax: "price_np", note: "price" }],
      ["價錢", { label: "what", jyutping: "gaa3 cin2", syntax: "price_np", note: "price" }],
      ["價位", { label: "what", jyutping: "gaa3 wai2", syntax: "price_range_np", note: "price range / price point" }],
      ["中", { label: "like", jyutping: "zung1", syntax: "modifier_middle", note: "middle / mid-" }],
      ["中等", { label: "like", jyutping: "zung1 dang2", syntax: "stative_predicate", note: "medium / moderate" }],
      ["價錢中等", { label: "like", jyutping: "gaa3 cin2 zung1 dang2", syntax: "compositional_price_evaluation", note: "Compositional source phrase; tokenizer keeps 價錢 + 中等 visible." }],
      ["中等價錢", { label: "what", jyutping: "zung1 dang2 gaa3 cin2", syntax: "compositional_price_np", note: "Compositional source phrase; tokenizer keeps 中等 + 價錢 visible." }],
      ["中價位", { label: "what", jyutping: "zung1 gaa3 wai2", syntax: "compositional_price_range", note: "Compositional price range; tokenizer keeps 中 + 價位 visible." }],
      ["中價", { label: "what", jyutping: "zung1 gaa3", syntax: "compositional_price_category_source_form", note: "Glossika source form; native-speaker alternatives are documented in the lesson note, not preferred in runtime parsing." }],
      ["錢", { label: "what", jyutping: "cin2", syntax: "object_np", note: "money" }],
      ["蚊", { label: "what", jyutping: "man1", syntax: "currency_unit", note: "dollar / Hong Kong dollar unit" }],
      ["時間", { label: "when", jyutping: "si4 gaan3", syntax: "time_np", note: "time" }],
      // v0.5.227: source-audited high-frequency lexical coverage.
      ["線", { "label": "what", "jyutping": "sin3", "syntax": "object_np", "note": "line / string / wire; Cifu spoken top-2000 R1 lexical audit." }],
      ["樹", { "label": "what", "jyutping": "syu6", "syntax": "object_np", "note": "tree; Cifu spoken top-2000 R1 lexical audit." }],
      ["政府", { "label": "what", "jyutping": "zing3 fu2", "syntax": "organization_np object_np", "note": "government; Cifu spoken top-2000 R1 lexical audit." }],
      ["塔", { "label": "what", "jyutping": "taap3", "syntax": "object_np", "note": "tower / pagoda; Cifu spoken top-2000 R1 lexical audit." }],
      ["馬戲", { "label": "what", "jyutping": "maa5 hei3", "syntax": "object_np", "note": "circus; Cifu spoken top-2000 R1 lexical audit." }],
      ["角", { "label": "what", "jyutping": "gok3", "syntax": "object_np", "note": "angle / corner / horn; Cifu spoken top-2000 R1 lexical audit." }],
      ["韻", { "label": "what", "jyutping": "wan6", "syntax": "object_np linguistic_term_np", "note": "rhyme / rime; Cifu spoken top-2000 R1 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R2 source-audited lexical coverage.
      ["蛇", { "label": "what", "jyutping": "se4", "syntax": "object_np animal_np", "note": "snake / serpent; Cifu spoken top-2000 R2 lexical audit." }],
      ["葡萄", { "label": "what", "jyutping": "pou4 tou4", "syntax": "object_np food_np", "note": "grape; Cifu spoken top-2000 R2 lexical audit." }],
      ["腰", { "label": "what", "jyutping": "jiu1", "syntax": "object_np body_part_np", "note": "waist / lower back; Cifu spoken top-2000 R2 lexical audit." }],
      ["科技", { "label": "what", "jyutping": "fo1 gei6", "syntax": "abstract_np domain_np", "note": "science and technology; Cifu spoken top-2000 R2 lexical audit." }],
      ["社會", { "label": "what", "jyutping": "se5 wui2", "syntax": "abstract_np collective_np", "note": "society; Cifu spoken top-2000 R2 lexical audit." }],
      ["方面", { "label": "what", "jyutping": "fong1 min6", "syntax": "abstract_np aspect_np", "note": "aspect / respect / field / side; Cifu spoken top-2000 R2 lexical audit." }],
      ["煙", { "label": "what", "jyutping": "jin1", "syntax": "object_np substance_np", "note": "smoke / tobacco / cigarette; Cifu spoken top-2000 R2 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R3 noun/place lexical coverage.
      ["萍", { "label": "what", "jyutping": "ping4", "syntax": "object_np plant_np", "note": "duckweed; Cifu spoken top-2000 R3 lexical audit." }],
      ["金字塔", { "label": "what", "jyutping": "gam1 zi6 taap3", "syntax": "object_np landmark_np", "note": "pyramid; Cifu spoken top-2000 R3 lexical audit." }],
      ["農", { "label": "what", "jyutping": "nung4", "syntax": "abstract_np domain_np", "note": "agriculture; Cifu spoken top-2000 R3 lexical audit." }],
      ["情況", { "label": "what", "jyutping": "cing4 fong3", "syntax": "abstract_np situation_np", "note": "circumstances / situation; Cifu spoken top-2000 R3 lexical audit." }],
      ["國旗", { "label": "what", "jyutping": "gwok3 kei4", "syntax": "object_np artifact_np", "note": "national flag; Cifu spoken top-2000 R3 lexical audit." }],
      ["燈塔", { "label": "what", "jyutping": "dang1 taap3", "syntax": "object_np landmark_np", "note": "lighthouse; Cifu spoken top-2000 R3 lexical audit." }],
      ["纜車", { "label": "what", "jyutping": "laam6 ce1", "syntax": "object_np vehicle_np", "note": "cable car; Cifu spoken top-2000 R3 lexical audit." }],
      ["效果", { "label": "what", "jyutping": "haau6 gwo2", "syntax": "abstract_np result_np", "note": "result / effect; Cifu spoken top-2000 R3 lexical audit." }],
      ["文字", { "label": "what", "jyutping": "man4 zi6", "syntax": "object_np linguistic_term_np", "note": "writing / script / characters; Cifu spoken top-2000 R3 lexical audit." }],
      ["圖", { "label": "what", "jyutping": "tou4", "syntax": "object_np visual_object_np", "note": "diagram / picture / drawing; Cifu spoken top-2000 R3 lexical audit." }],
      ["銅", { "label": "what", "jyutping": "tung4", "syntax": "object_np material_np", "note": "copper; Cifu spoken top-2000 R3 lexical audit." }],
      ["地標", { "label": "what", "jyutping": "dei6 biu1", "syntax": "object_np landmark_np", "note": "landmark; Cifu spoken top-2000 R3 lexical audit." }],
      ["皮", { "label": "what", "jyutping": "pei4", "syntax": "object_np material_or_body_np", "note": "skin / leather; Cifu spoken top-2000 R3 lexical audit." }],
      ["鬼", { "label": "what", "jyutping": "gwai2", "syntax": "object_np animate_np", "note": "ghost; Cifu spoken top-2000 R3 lexical audit." }],
      ["電腦", { "label": "what", "jyutping": "din6 nou5", "syntax": "object_np device_np", "note": "computer; Cifu spoken top-2000 R3 lexical audit." }],
      ["英文", { "label": "what", "jyutping": "jing1 man4", "syntax": "object_np language_np", "note": "English language; Cifu spoken top-2000 R3 lexical audit." }],
      ["經濟", { "label": "what", "jyutping": "ging1 zai3", "syntax": "abstract_np domain_np", "note": "economy / economics; Cifu spoken top-2000 R3 lexical audit." }],
      ["理由", { "label": "what", "jyutping": "lei5 jau4", "syntax": "abstract_np reason_np", "note": "reason / grounds / justification; Cifu spoken top-2000 R3 lexical audit." }],
      ["法律", { "label": "what", "jyutping": "faat3 leot6", "syntax": "abstract_np domain_np", "note": "law; Cifu spoken top-2000 R3 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R4 nominal lexical coverage.
      ["虛線", { "label": "what", "jyutping": "heoi1 sin3", "syntax": "object_np visual_line_np", "note": "dotted line; Cifu spoken top-2000 R4 lexical audit." }],
      ["自由", { "label": "what", "jyutping": "zi6 jau4", "syntax": "abstract_np concept_np", "note": "freedom / liberty; Cifu spoken top-2000 R4 lexical audit." }],
      ["直線", { "label": "what", "jyutping": "zik6 sin3", "syntax": "object_np visual_line_np", "note": "straight line; Cifu spoken top-2000 R4 lexical audit." }],
      ["性", { "label": "what", "jyutping": "sing3", "syntax": "abstract_np property_np", "note": "nature / property / sex; Cifu spoken top-2000 R4 lexical audit." }],
      ["關係", { "label": "what", "jyutping": "gwaan1 hai6", "syntax": "abstract_np relation_np", "note": "relation / relationship; Cifu spoken top-2000 R4 lexical audit." }],
      ["生活", { "label": "what", "jyutping": "sang1 wut6", "syntax": "abstract_np life_np", "note": "life / livelihood; Cifu spoken top-2000 R4 lexical audit." }],
      ["法", { "label": "what", "jyutping": "faat3", "syntax": "abstract_np method_or_law_np", "note": "law / method / way; Cifu spoken top-2000 R4 lexical audit." }],
      ["方法", { "label": "what", "jyutping": "fong1 faat3", "syntax": "abstract_np method_np", "note": "method / way / means; Cifu spoken top-2000 R4 lexical audit." }],
      ["圖案", { "label": "what", "jyutping": "tou4 on3", "syntax": "object_np visual_object_np", "note": "design / pattern; Cifu spoken top-2000 R4 lexical audit." }],
      ["尾", { "label": "what", "jyutping": "mei5", "syntax": "object_np body_part_np", "note": "tail / end; Cifu spoken top-2000 R4 lexical audit." }],
      ["意見", { "label": "what", "jyutping": "ji3 gin3", "syntax": "abstract_np opinion_np", "note": "opinion / suggestion; Cifu spoken top-2000 R4 lexical audit." }],
      ["圖片", { "label": "what", "jyutping": "tou4 pin3", "syntax": "object_np visual_object_np", "note": "image / picture / photograph; Cifu spoken top-2000 R4 lexical audit." }],
      ["辦法", { "label": "what", "jyutping": "baan6 faat3", "syntax": "abstract_np method_np", "note": "means / method / way; Cifu spoken top-2000 R4 lexical audit." }],
      ["財政", { "label": "what", "jyutping": "coi4 zing3", "syntax": "abstract_np domain_np", "note": "public finance / finances; Cifu spoken top-2000 R4 lexical audit." }],
      ["碑", { "label": "what", "jyutping": "bei1", "syntax": "object_np monument_np", "note": "monument / stone tablet; Cifu spoken top-2000 R4 lexical audit." }],
      ["距離", { "label": "what", "jyutping": "keoi5 lei4", "syntax": "abstract_np distance_np", "note": "distance; Cifu spoken top-2000 R4 lexical audit." }],
      ["概念", { "label": "what", "jyutping": "koi3 nim6", "syntax": "abstract_np concept_np", "note": "concept / idea; Cifu spoken top-2000 R4 lexical audit." }],
      ["人權", { "label": "what", "jyutping": "jan4 kyun4", "syntax": "abstract_np rights_np", "note": "human rights; Cifu spoken top-2000 R4 lexical audit." }],
      ["考試", { "label": "what", "jyutping": "haau2 si3", "syntax": "event_np assessment_np", "note": "exam; Cifu spoken top-2000 R4 lexical audit." }],
      ["杏", { "label": "what", "jyutping": "hang6", "syntax": "object_np food_or_plant_np", "note": "apricot; Cifu spoken top-2000 R4 lexical audit." }],
      ["格", { "label": "what", "jyutping": "gaak3", "syntax": "abstract_or_object_np pattern_np", "note": "frame / style / pattern; Cifu spoken top-2000 R4 lexical audit." }],
      ["團", { "label": "what", "jyutping": "tyun4", "syntax": "collective_np object_np", "note": "group / society / regiment; Cifu spoken top-2000 R4 lexical audit." }],
      ["頭", { "label": "what", "jyutping": "tau4", "syntax": "object_np body_part_np", "note": "head; Cifu spoken top-2000 R4 lexical audit." }],
      ["意", { "label": "what", "jyutping": "ji3", "syntax": "abstract_np idea_or_intention_np", "note": "idea / meaning / intention; Cifu spoken top-2000 R4 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R5 lexical coverage.
      ["汗", { "label": "what", "jyutping": "hon6", "syntax": "object_np substance_np", "note": "sweat / perspiration; Cifu spoken top-2000 R5 lexical audit." }],
      ["印", { "label": "what", "jyutping": "jan3", "syntax": "object_np mark_or_artifact_np", "note": "stamp / seal / mark / print; Cifu spoken top-2000 R5 lexical audit." }],
      ["形", { "label": "what", "jyutping": "jing4", "syntax": "abstract_or_object_np shape_np", "note": "form / shape; Cifu spoken top-2000 R5 lexical audit." }],
      ["翻版", { "label": "what", "jyutping": "faan1 baan2", "syntax": "object_np copy_np", "note": "reprint / duplicate / clone; Cifu spoken top-2000 R5 lexical audit." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/places-and-times.js
var require_places_and_times = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/places-and-times.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Places / times
      ["學校", { label: "where", jyutping: "hok6 haau6", syntax: "place_or_goal", note: "school" }],
      ["屋企", { label: "where", jyutping: "uk1 kei2", syntax: "place_or_goal", note: "home" }],
      ["出面", { label: "where", jyutping: "ceot1 min6", syntax: "place_or_goal spatial_localizer ambient_location_frame", note: "outside / the area outside; can frame an ambient environmental property without acting as an expletive subject" }],
      ["公園", { label: "where", jyutping: "gung1 jyun2", syntax: "place_or_goal", note: "park; common changed-tone reading, with jyun4 retained as an accepted variant in the review expectations." }],
      ["商場", { label: "where", jyutping: "soeng1 coeng4", syntax: "place_or_goal", note: "mall" }],
      ["餐廳", { label: "what", jyutping: "caan1 teng1", syntax: "restaurant_np", classifier_classes: ["building_shop"], note: "restaurant; place entity, but learner-visible role defaults to what/topic unless used as a location." }],
      ["廁所", { label: "where", jyutping: "ci3 so2", syntax: "place_or_goal", note: "bathroom" }],
      ["香港", { label: "where", jyutping: "hoeng1 gong2", syntax: "place_or_goal", note: "Hong Kong" }],
      ["樓下", { label: "where", jyutping: "lau4 haa6", syntax: "place_or_goal spatial_localizer", note: "downstairs / lower floor; overt location or motion goal." }],
      ["中環", { label: "where", jyutping: "zung1 waan4", syntax: "place_or_goal", note: "Central, Hong Kong" }],
      ["音樂", { label: "what", jyutping: "jam1 ngok6", syntax: "noun_np_modifier_topic", note: "music; can modify 嘢 in 音樂嘅嘢" }],
      ["下", { label: "when", jyutping: "haa6", syntax: "temporal_modifier_next", note: "next / below depending context" }],
      ["上", { label: "when", jyutping: "soeng5", syntax: "temporal_modifier_previous movement_direction_up", note: "previous/upper; also a directional component in native motion compounds such as 上嚟 / 返上嚟" }],
      ["落", { label: "doing", jyutping: "lok6", syntax: "movement_direction_down movement_verb", note: "go down / come down; directional component in native motion compounds such as 落嚟" }],
      ["個", { label: "measure_word", jyutping: "go3", syntax: "general_classifier", note: "general measure word / classifier" }],
      ["份", { label: "measure_word", jyutping: "fan6", syntax: "general_classifier document_task_classifier", note: "classifier for portions, copies, jobs, and assignments" }],
      ["戴", { label: "doing", jyutping: "daai3", syntax: "verb transitive_affordance wearing_action", note: "wear / put on" }],
      ["眼鏡", { label: "what", jyutping: "ngaan5 geng2", syntax: "object_np wearable_object", note: "glasses / spectacles" }],
      ["糖", { label: "what", jyutping: "tong4", syntax: "object_np edible_object", note: "sugar / candy" }],
      ["入", { label: "doing", jyutping: "jap6", syntax: "movement_direction_in movement_direction", note: "enter / inward" }],
      ["出", { label: "doing", jyutping: "ceot1", syntax: "movement_direction_out movement_direction", note: "exit / outward" }],
      ["琴日", { label: "when", jyutping: "kam4 jat6", syntax: "temporal_adjunct", note: "yesterday" }],
      ["日本", { label: "where", jyutping: "jat6 bun2", syntax: "place_or_goal country_location", note: "Japan" }],
      ["向", { label: "func", jyutping: "hoeng3", syntax: "orientation_coverb path_coverb", note: "toward / facing" }],
      ["晏", { label: "what", jyutping: "aan3", syntax: "meal_object lunch_event", note: "lunch / daytime meal" }],
      ["街市", { label: "where", jyutping: "gaai1 si5", syntax: "place_or_goal market_location", note: "market" }],
      ["餸", { label: "what", jyutping: "sung3", syntax: "object_np food_object", note: "dish / food to go with rice" }],
      ["門口", { label: "where", jyutping: "mun4 hau2", syntax: "place_or_goal entrance_location", note: "doorway / entrance" }],
      ["房", { label: "where", jyutping: "fong2", syntax: "place_or_goal interior_location", note: "room" }],
      ["寄", { label: "doing", jyutping: "gei3", syntax: "verb transitive_affordance transfer_action", note: "send / mail" }],
      ["星期五", { label: "when", jyutping: "sing1 kei4 ng5", syntax: "weekday_np", note: "Friday" }],
      ["今日", { label: "when", jyutping: "gam1 jat6", syntax: "temporal_adjunct", note: "today" }],
      ["聽日", { label: "when", jyutping: "ting1 jat6", syntax: "temporal_adjunct", note: "tomorrow" }],
      ["尋日", { label: "when", jyutping: "cam4 jat6", syntax: "temporal_adjunct", note: "yesterday" }],
      ["而家", { label: "when", jyutping: "ji4 gaa1", syntax: "temporal_adjunct", note: "now" }],
      ["陣間", { label: "when", jyutping: "zan6 gaan1", syntax: "temporal_adjunct", note: "later" }],
      ["朝早", { label: "when", jyutping: "ziu1 zou2", syntax: "temporal_adjunct", note: "morning" }],
      ["夜晚", { label: "when", jyutping: "je6 maan5", syntax: "temporal_adjunct", note: "night" }],
      ["上個禮拜", { label: "when", jyutping: "soeng6 go3 lai5 baai3", syntax: "temporal_adjunct", note: "last week; retained only as excluded lexical candidate so transparent time-expression tokenization can expose 上 + 個 + 禮拜." }],
      ["禮拜", { label: "when", jyutping: "lai5 baai3", syntax: "time_head temporal", note: "week; time head for transparent expressions such as 上個禮拜." }],
      ["次", { label: "when", jyutping: "ci3", syntax: "time_head temporal", note: "time/occasion; time head for transparent expressions such as 上次 / 下次." }],
      ["上次", { label: "when", jyutping: "soeng6 ci3", syntax: "temporal_adjunct", note: "last time; retained only as excluded lexical candidate so transparent time-expression tokenization can expose 上 + 次." }],
      ["下個星期五", { label: "when", jyutping: "haa6 go3 sing1 kei4 ng5", syntax: "temporal_adjunct", note: "next Friday" }],
      ["下次", { label: "when", jyutping: "haa6 ci3", syntax: "temporal_adjunct", note: "next time" }],
      ["到時", { label: "when", jyutping: "dou3 si4", syntax: "temporal_adjunct", note: "when the time comes" }],
      ["成日", { label: "when", jyutping: "seng4 jat6", syntax: "frequency_adjunct", note: "always / all day" }],
      ["一", { label: "like", jyutping: "jat1", syntax: "quantity count_value numeral_one", note: "one; quantity descriptor before classifier or time head" }],
      ["兩", { label: "like", jyutping: "loeng5", syntax: "quantity count_value numeral_two", note: "two; quantity descriptor before classifier or time head" }],
      ["二", { label: "how", jyutping: "ji6", syntax: "quantity count_value numeral_two_formal", note: "two; formal numeral in quantity expressions" }],
      ["三", { label: "like", jyutping: "saam1", syntax: "quantity count_value numeral_three", note: "three; quantity descriptor before classifier or time head" }],
      ["四", { label: "how", jyutping: "sei3", syntax: "quantity count_value numeral_four", note: "four; quantity descriptor" }],
      ["五", { label: "how", jyutping: "ng5", syntax: "quantity count_value numeral_five", note: "five; quantity descriptor" }],
      ["六", { label: "how", jyutping: "luk6", syntax: "quantity count_value numeral_six", note: "six; quantity descriptor" }],
      ["七", { label: "how", jyutping: "cat1", syntax: "quantity count_value numeral_seven", note: "seven; quantity descriptor" }],
      ["八", { label: "how", jyutping: "baat3", syntax: "quantity count_value numeral_eight", note: "eight; quantity descriptor" }],
      ["九", { label: "how", jyutping: "gau2", syntax: "quantity count_value numeral_nine", note: "nine; quantity descriptor" }],
      ["十", { label: "how", jyutping: "sap6", syntax: "quantity count_value numeral_ten", note: "ten; quantity descriptor" }],
      ["百", { label: "how", jyutping: "baak3", syntax: "quantity count_value numeral_hundred", note: "hundred; quantity scale component" }],
      ["千", { label: "how", jyutping: "cin1", syntax: "quantity count_value numeral_thousand", note: "thousand; quantity scale component" }],
      ["歲", { label: "measure_word", jyutping: "seoi3", syntax: "age_unit nominal_measure_unit", note: "years old; age-measure unit in restricted nominal predication" }],
      ["呎", { label: "measure_word", jyutping: "cek3", syntax: "measure_unit area_measure_unit length_measure_unit nominal_measure_unit", note: "foot / square-foot measure unit; construction context selects area or length" }],
      ["尺", { label: "measure_word", jyutping: "cek3", syntax: "measure_unit length_measure_unit nominal_measure_unit", note: "foot-length measure unit" }],
      ["長", { label: "like", jyutping: "coeng4", syntax: "stative_predicate scalar_dimension_predicate length_dimension_predicate", note: "long; dimensional property predicate" }],
      ["一個人", { label: "who", jyutping: "jat1 go3 jan4", syntax: "quantity_person_np", note: "one person / per person" }],
      ["兩三百蚊", { label: "like", jyutping: "loeng5 saam1 baak3 man1", syntax: "quantity approximate_money_amount price_np", note: "two or three hundred dollars; approximate price/value descriptor" }],
      // v0.5.227: source-audited high-frequency lexical coverage.
      ["中間", { "label": "where", "jyutping": "zung1 gaan1", "syntax": "place_or_goal spatial_localizer", "note": "middle / between; Cifu spoken top-2000 R1 lexical audit." }],
      ["下面", { "label": "where", "jyutping": "haa6 min6", "syntax": "place_or_goal spatial_localizer", "note": "below / underneath; Cifu spoken top-2000 R1 lexical audit." }],
      ["沙漠", { "label": "where", "jyutping": "saa1 mok6", "syntax": "place_or_goal", "note": "desert; Cifu spoken top-2000 R1 lexical audit." }],
      ["終點", { "label": "where", "jyutping": "zung1 dim2", "syntax": "place_or_goal", "note": "endpoint / destination / terminus; Cifu spoken top-2000 R1 lexical audit." }],
      ["山", { "label": "where", "jyutping": "saan1", "syntax": "place_or_goal", "note": "mountain / hill; Cifu spoken top-2000 R1 lexical audit." }],
      ["園", { "label": "where", "jyutping": "jyun4", "syntax": "place_or_goal", "note": "garden; Cifu spoken top-2000 R1 lexical audit." }],
      ["池", { "label": "where", "jyutping": "ci4", "syntax": "place_or_goal", "note": "pond / pool; Cifu spoken top-2000 R1 lexical audit." }],
      ["茅屋", { "label": "where", "jyutping": "maau4 uk1", "syntax": "place_or_goal", "note": "cottage / hut; Cifu spoken top-2000 R1 lexical audit." }],
      ["頭先", { "label": "when", "jyutping": "tau4 sin1", "syntax": "temporal_adjunct", "note": "just now / a moment ago; Cifu spoken top-2000 R1 lexical audit." }],
      ["右邊", { "label": "where", "jyutping": "jau6 bin1", "syntax": "place_or_goal spatial_localizer", "note": "right side; Cifu spoken top-2000 R1 lexical audit." }],
      ["一路", { "label": "when", "jyutping": "jat1 lou6", "syntax": "temporal_adjunct", "note": "continually / all along; Cifu spoken top-2000 R1 lexical audit." }],
      ["一直", { "label": "when", "jyutping": "jat1 zik6", "syntax": "temporal_adjunct", "note": "continuously / all along; Cifu spoken top-2000 R1 lexical audit." }],
      ["左邊", { "label": "where", "jyutping": "zo2 bin1", "syntax": "place_or_goal spatial_localizer", "note": "left side; Cifu spoken top-2000 R1 lexical audit." }],
      ["港", { "label": "where", "jyutping": "gong2", "syntax": "place_or_goal", "note": "port / harbor; Hong Kong abbreviation in context; Cifu spoken top-2000 R1 lexical audit." }],
      ["湖", { "label": "where", "jyutping": "wu4", "syntax": "place_or_goal", "note": "lake; Cifu spoken top-2000 R1 lexical audit." }],
      ["洞", { "label": "where", "jyutping": "dung6", "syntax": "place_or_goal", "note": "cave / hole; Cifu spoken top-2000 R1 lexical audit." }],
      ["點鐘", { "label": "when", "jyutping": "dim2 zung1", "syntax": "time_np clock_time_unit", "note": "o'clock / clock-hour expression; Cifu spoken top-2000 R1 lexical audit." }],
      ["廟", { "label": "where", "jyutping": "miu6", "syntax": "place_or_goal place_np", "note": "temple; Cifu spoken top-2000 R1 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R2 source-audited lexical coverage.
      ["以前", { "label": "when", "jyutping": "ji5 cin4", "syntax": "temporal_adjunct", "note": "before / formerly / ago; Cifu spoken top-2000 R2 lexical audit." }],
      ["之間", { "label": "where", "jyutping": "zi1 gaan1", "syntax": "relational_localizer spatial_nominal", "note": "between / among; Cifu spoken top-2000 R2 lexical audit." }],
      ["方向", { "label": "where", "jyutping": "fong1 hoeng3", "syntax": "direction_np spatial_nominal", "note": "direction / orientation; Cifu spoken top-2000 R2 lexical audit." }],
      ["泳池", { "label": "where", "jyutping": "wing6 ci4", "syntax": "place_or_goal place_np", "note": "swimming pool; Cifu spoken top-2000 R2 lexical audit." }],
      ["位置", { "label": "where", "jyutping": "wai6 zi3", "syntax": "location_np place_or_goal", "note": "position / place / location; Cifu spoken top-2000 R2 lexical audit." }],
      ["路", { "label": "where", "jyutping": "lou6", "syntax": "path_np place_or_goal", "note": "road / path / way; Cifu spoken top-2000 R2 lexical audit." }],
      ["機場", { "label": "where", "jyutping": "gei1 coeng4", "syntax": "place_or_goal place_np", "note": "airport / airfield; Cifu spoken top-2000 R2 lexical audit." }],
      ["迷宮", { "label": "where", "jyutping": "mai4 gung1", "syntax": "place_or_goal place_np", "note": "maze / labyrinth; Cifu spoken top-2000 R2 lexical audit." }],
      ["起點", { "label": "where", "jyutping": "hei2 dim2", "syntax": "source_or_startpoint place_or_goal", "note": "starting point; Cifu spoken top-2000 R2 lexical audit." }],
      ["南", { "label": "where", "jyutping": "naam4", "syntax": "direction_np cardinal_direction", "note": "south; Cifu spoken top-2000 R2 lexical audit." }],
      ["工廠", { "label": "where", "jyutping": "gung1 cong2", "syntax": "place_or_goal place_np", "note": "factory; Cifu spoken top-2000 R2 lexical audit." }],
      ["城", { "label": "where", "jyutping": "sing4", "syntax": "place_or_goal place_np", "note": "city / town / city walls; Cifu spoken top-2000 R2 lexical audit." }],
      ["球場", { "label": "where", "jyutping": "kau4 coeng4", "syntax": "place_or_goal place_np", "note": "sports ground / court / field; Cifu spoken top-2000 R2 lexical audit." }],
      ["宅", { "label": "where", "jyutping": "zaak6", "syntax": "place_or_goal residence_np", "note": "residence; Cifu spoken top-2000 R2 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R3 noun/place lexical coverage.
      ["北方", { "label": "where", "jyutping": "bak1 fong1", "syntax": "direction_np cardinal_region", "note": "north / northern region; Cifu spoken top-2000 R3 lexical audit." }],
      ["東", { "label": "where", "jyutping": "dung1", "syntax": "direction_np cardinal_direction", "note": "east; Cifu spoken top-2000 R3 lexical audit." }],
      ["碼頭", { "label": "where", "jyutping": "maa5 tau4", "syntax": "place_or_goal place_np", "note": "dock / pier / wharf; Cifu spoken top-2000 R3 lexical audit." }],
      ["礦場", { "label": "where", "jyutping": "kwong3 coeng4", "syntax": "place_or_goal place_np", "note": "mine / pit; Cifu spoken top-2000 R3 lexical audit." }],
      ["農莊", { "label": "where", "jyutping": "nung4 zong1", "syntax": "place_or_goal place_np", "note": "farm / ranch; Cifu spoken top-2000 R3 lexical audit." }],
      ["瀑布", { "label": "where", "jyutping": "buk6 bou3", "syntax": "place_or_goal natural_place_np", "note": "waterfall; Cifu spoken top-2000 R3 lexical audit." }],
      ["墳場", { "label": "where", "jyutping": "fan4 coeng4", "syntax": "place_or_goal place_np", "note": "cemetery / graveyard; Cifu spoken top-2000 R3 lexical audit." }],
      ["樹林", { "label": "where", "jyutping": "syu6 lam4", "syntax": "place_or_goal natural_place_np", "note": "woods / grove / forest; Cifu spoken top-2000 R3 lexical audit." }],
      ["禮堂", { "label": "where", "jyutping": "lai5 tong4", "syntax": "place_or_goal place_np", "note": "assembly hall / auditorium; Cifu spoken top-2000 R3 lexical audit." }],
      ["地盤", { "label": "where", "jyutping": "dei6 pun4", "syntax": "place_or_goal place_np", "note": "construction site / territory; Cifu spoken top-2000 R3 lexical audit." }],
      ["市場", { "label": "where", "jyutping": "si5 coeng4", "syntax": "place_or_goal place_np", "note": "market / marketplace; Cifu spoken top-2000 R3 lexical audit." }],
      ["國", { "label": "where", "jyutping": "gwok3", "syntax": "place_or_goal geopolitical_place_np", "note": "country / nation / state; Cifu spoken top-2000 R3 lexical audit." }],
      ["西", { "label": "where", "jyutping": "sai1", "syntax": "direction_np cardinal_direction", "note": "west; Cifu spoken top-2000 R3 lexical audit." }],
      ["水塘", { "label": "where", "jyutping": "seoi2 tong4", "syntax": "place_or_goal natural_place_np", "note": "reservoir / pool; Cifu spoken top-2000 R3 lexical audit." }],
      ["國家", { "label": "where", "jyutping": "gwok3 gaa1", "syntax": "place_or_goal geopolitical_place_np", "note": "country / nation / state; Cifu spoken top-2000 R3 lexical audit." }],
      ["世界", { "label": "where", "jyutping": "sai3 gaai3", "syntax": "place_or_goal world_np", "note": "world; Cifu spoken top-2000 R3 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R4 nominal lexical coverage.
      ["灣", { "label": "where", "jyutping": "waan1", "syntax": "place_or_goal natural_place_np", "note": "bay / gulf; Cifu spoken top-2000 R4 lexical audit." }],
      ["室", { "label": "where", "jyutping": "sat1", "syntax": "place_or_goal room_np", "note": "room; Cifu spoken top-2000 R4 lexical audit." }],
      ["樂園", { "label": "where", "jyutping": "lok6 jyun4", "syntax": "place_or_goal place_np", "note": "paradise / pleasure park; Cifu spoken top-2000 R4 lexical audit." }],
      ["林", { "label": "where", "jyutping": "lam4", "syntax": "place_or_goal natural_place_np", "note": "woods / forest; Cifu spoken top-2000 R4 lexical audit." }],
      ["東方", { "label": "where", "jyutping": "dung1 fong1", "syntax": "direction_np cardinal_region", "note": "the East / eastern region; Cifu spoken top-2000 R4 lexical audit." }],
      ["貨倉", { "label": "where", "jyutping": "fo3 cong1", "syntax": "place_or_goal place_np", "note": "warehouse; Cifu spoken top-2000 R4 lexical audit." }],
      ["店", { "label": "where", "jyutping": "dim3", "syntax": "place_or_goal place_np", "note": "shop / store / inn; Cifu spoken top-2000 R4 lexical audit." }],
      ["北", { "label": "where", "jyutping": "bak1", "syntax": "direction_np cardinal_direction", "note": "north; Cifu spoken top-2000 R4 lexical audit." }],
      ["外國", { "label": "where", "jyutping": "ngoi6 gwok3", "syntax": "place_or_goal geopolitical_place_np", "note": "foreign country; Cifu spoken top-2000 R4 lexical audit." }],
      ["莊", { "label": "where", "jyutping": "zong1", "syntax": "place_or_goal settlement_or_estate_np", "note": "farmstead / village / manor; Cifu spoken top-2000 R4 lexical audit." }],
      ["將來", { "label": "when", "jyutping": "zoeng1 loi4", "syntax": "temporal_adjunct future_time_np", "note": "the future; Cifu spoken top-2000 R4 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R5 lexical coverage.
      ["海", { "label": "where", "jyutping": "hoi2", "syntax": "place_or_goal natural_place_np", "note": "ocean / sea; Cifu spoken top-2000 R5 lexical audit." }],
      ["叢林", { "label": "where", "jyutping": "cung4 lam4", "syntax": "place_or_goal natural_place_np", "note": "jungle / thicket / forest; Cifu spoken top-2000 R5 lexical audit." }],
      ["大陸", { "label": "where", "jyutping": "daai6 luk6", "syntax": "place_or_goal geopolitical_place_np", "note": "mainland / continent; Cifu spoken top-2000 R5 lexical audit." }],
      ["灘", { "label": "where", "jyutping": "taan1", "syntax": "place_or_goal natural_place_np", "note": "beach / shoal; Cifu spoken top-2000 R5 lexical audit." }],
      ["西方", { "label": "where", "jyutping": "sai1 fong1", "syntax": "direction_np cardinal_region", "note": "the West / western region; Cifu spoken top-2000 R5 lexical audit." }],
      ["之前", { "label": "when", "jyutping": "zi1 cin4", "syntax": "temporal_adjunct", "note": "before / prior to; Cifu spoken top-2000 R5 lexical audit." }],
      ["即刻", { "label": "when", "jyutping": "zik1 hak1", "syntax": "temporal_adjunct immediate_time_adverb", "note": "immediately / instantly; Cifu spoken top-2000 R5 lexical audit." }],
      ["最後", { "label": "when", "jyutping": "zeoi3 hau6", "syntax": "temporal_adjunct sequence_time_adverb", "note": "finally / last; Cifu spoken top-2000 R5 lexical audit." }],
      ["今年", { "label": "when", "jyutping": "gam1 nin4", "syntax": "temporal_adjunct time_np", "note": "this year; Cifu spoken top-2000 R5 lexical audit." }],
      ["早", { "label": "when", "jyutping": "zou2", "syntax": "temporal_adjunct time_adverb", "note": "early / morning; Cifu spoken top-2000 R5 lexical audit." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/verbs.js
var require_verbs = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/verbs.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Verbs
      ["食", { label: "doing", jyutping: "sik6", syntax: "verb transitive_affordance chain_select_edible", note: "eat" }],
      ["切", { label: "doing", jyutping: "cit3", syntax: "verb action_verb transitive_affordance", note: "cut; added for the sourced 用嚟 function example and ordinary lexical coverage." }],
      ["畫", { label: "doing", jyutping: "waak6", syntax: "verb action_verb transitive_affordance", note: "draw / paint; verb reading corrected during the authorized Glossika Week 16 lexical audit." }],
      ["畫畫", { label: "doing", jyutping: "waak6 waa2", syntax: "verb action_verb", note: "draw pictures / do drawing; verb + picture reading." }],
      ["飲", { label: "doing", jyutping: "jam2", syntax: "verb transitive_affordance chain_select_drinkable", note: "drink" }],
      ["睇", { label: "doing", jyutping: "tai2", syntax: "verb transitive_affordance chain_select_perception", note: "look/read/watch" }],
      ["聽", { label: "doing", jyutping: "teng1", syntax: "verb", note: "listen/hear" }],
      ["講", { label: "doing", jyutping: "gong2", syntax: "verb transitive_affordance chain_select_discourse_content", note: "speak" }],
      ["問", { label: "doing", jyutping: "man6", syntax: "verb", note: "ask" }],
      ["答", { label: "doing", jyutping: "daap3", syntax: "verb", note: "answer" }],
      ["去", { label: "doing", jyutping: "heoi3", syntax: "movement_verb", note: "go" }],
      ["返", { label: "doing", jyutping: "faan1", syntax: "movement_verb", note: "return" }],
      ["嚟", { label: "doing", jyutping: "lai4", syntax: "movement_verb", note: "come" }],
      ["坐", { label: "doing", jyutping: "co5", syntax: "verb", note: "sit" }],
      ["企", { label: "doing", jyutping: "kei5", syntax: "verb", note: "stand" }],
      ["瞓", { label: "doing", jyutping: "fan3", syntax: "verb", note: "sleep" }],
      ["玩", { label: "doing", jyutping: "waan2", syntax: "verb", note: "play" }],
      ["買", { label: "doing", jyutping: "maai5", syntax: "verb", note: "buy" }],
      ["賣", { label: "doing", jyutping: "maai6", syntax: "verb", note: "sell" }],
      ["煮", { label: "doing", jyutping: "zyu2", syntax: "verb", note: "cook; lexicon/Jyutping cleanup for 煮飯 examples." }],
      ["解決", { label: "doing", jyutping: "gaai2 kyut3", syntax: "verb", note: "solve / resolve; lexicon/Jyutping cleanup for UI example diagnostics." }],
      ["攞", { label: "doing", jyutping: "lo2", syntax: "verb", note: "take" }],
      ["走", { label: "func", jyutping: "zau2", syntax: "verb_complement directional_result_complement", note: "away; result/directional complement after action verbs such as 攞走." }],
      ["鬧", { label: "doing", jyutping: "naau6", syntax: "verb transitive_social_action_verb", note: "scold / tell off" }],
      ["幫", { label: "doing", jyutping: "bong1", syntax: "verb help_benefactive_verb", note: "help / help someone do something." }],
      ["摘", { label: "doing", jyutping: "zaak6", syntax: "verb harvest_pick_verb", note: "pick / pluck, as in 摘芒果." }],
      ["畀", { label: "doing", jyutping: "bei2", syntax: "ditransitive_verb", note: "give" }],
      ["俾", { label: "doing", jyutping: "bei2", syntax: "ditransitive_verb orthographic_variant_bei", note: "give; common spelling variant of 畀" }],
      ["做", { label: "doing", jyutping: "zou6", syntax: "verb", note: "do" }],
      ["使", { label: "func", jyutping: "sai2", syntax: "modal_necessity", note: "need to / be necessary; positive arm of 使唔使 and the base predicate related to 唔使." }],
      ["洗", { label: "doing", jyutping: "sai2", syntax: "verb", note: "wash" }],
      ["沖", { label: "doing", jyutping: "cung1", syntax: "verb", note: "rinse / bathe" }],
      ["打", { label: "doing", jyutping: "daa2", syntax: "verb", note: "hit / play / call" }],
      ["放", { label: "doing", jyutping: "fong3", syntax: "verb", note: "release / finish school" }],
      ["曬", { label: "doing", jyutping: "saai3", syntax: "verb", note: "sun / expose to sun" }],
      ["默", { label: "doing", jyutping: "mak6", syntax: "verb", note: "dictate from memory" }],
      ["炒", { label: "doing", jyutping: "caau2", syntax: "verb", note: "fry / speculate" }],
      ["發", { label: "doing", jyutping: "faat3", syntax: "verb", note: "issue / break out" }],
      ["陪", { label: "doing", jyutping: "pui4", syntax: "verb", note: "accompany" }],
      ["叫", { label: "doing", jyutping: "giu3", syntax: "naming_or_calling_verb", note: "call / be named" }],
      ["覺得", { label: "doing", jyutping: "gok3 dak1", syntax: "cognition_opinion_predicate", note: "think / feel" }],
      ["以為", { label: "doing", jyutping: "ji5 wai4", syntax: "cognition_opinion_predicate belief_predicate mistaken_belief_predicate", note: "think / suppose, often with a mistaken or counterfactual expectation." }],
      ["認為", { label: "doing", jyutping: "jing6 wai4", syntax: "cognition_opinion_predicate belief_predicate", note: "consider / hold the view that" }],
      ["相信", { label: "doing", jyutping: "soeng1 seon3", syntax: "cognition_opinion_predicate belief_predicate", note: "believe / trust" }],
      ["懷疑", { label: "doing", jyutping: "waai4 ji4", syntax: "cognition_opinion_predicate belief_predicate", note: "doubt / suspect" }],
      ["真", { label: "like", jyutping: "zan1", syntax: "stative_predicate truth_stative", note: "true / real" }],
      ["諗", { label: "doing", jyutping: "nam2", syntax: "cognition_verb", note: "think / consider; visible role is doing, internal class is cognition" }],
      ["諗住", { label: "doing", jyutping: "nam2 zyu6", syntax: "intention_frame", note: "intend / plan to" }],
      ["知", { label: "doing", jyutping: "zi1", syntax: "cognition_verb", note: "know" }],
      ["唔知", { label: "doing", jyutping: "m4 zi1", syntax: "negative_cognition_predicate", note: "don't know" }],
      ["點知", { label: "how", jyutping: "dim2 zi1", syntax: "wh_cognition_question", note: "how do you know?" }],
      ["話", { label: "doing", jyutping: "waa6", syntax: "speech_reporting_verb", note: "say / report" }],
      ["試", { label: "doing", jyutping: "si3", syntax: "verb", note: "try" }],
      ["慶祝", { label: "doing", jyutping: "hing3 zuk1", syntax: "verb", note: "celebrate" }],
      ["約", { label: "doing", jyutping: "joek3", syntax: "verb", note: "arrange / invite" }],
      ["約埋", { label: "doing", jyutping: "joek3 maai4", syntax: "verb_particle_include_together", note: "arrange/include together" }],
      ["分", { label: "doing", jyutping: "fan1", syntax: "verb", note: "divide / split" }],
      ["平均分", { label: "doing", jyutping: "ping4 gwan1 fan1", syntax: "compound_verb", note: "split evenly" }],
      ["鍾意", { label: "like", jyutping: "zung1 ji3", syntax: "preference_predicate", note: "like / prefer" }],
      ["信", { label: "doing", jyutping: "seon3", syntax: "belief_verb", note: "believe / trust" }],
      ["一次", { label: "how", jyutping: "jat1 ci3", syntax: "frequency_quantity", note: "once" }],
      ["等", { label: "doing", jyutping: "dang2", syntax: "verb", note: "wait" }],
      ["記得", { label: "doing", jyutping: "gei3 dak1", syntax: "cognition_reminder_predicate", note: "remember" }],
      ["放心", { label: "func", jyutping: "fong3 sam1", syntax: "formula_discourse_unit", note: "don't worry" }],
      ["Book", { label: "doing", jyutping: "book1", syntax: "borrowed_verb transitive_affordance chain_select_bookable_resource", note: "book / reserve" }],
      ["廣東話", { label: "what", jyutping: "gwong2 dung1 waa2", syntax: "language_np object_np discourse_content", note: "Cantonese language." }],
      // v0.5.29: active-corpus lexicon and learner-role cleanup.
      // These are lexical/category entries only. Larger reusable constructions
      // such as 用嚟 purpose frames, subject-preserving modal clauses, 係時候
      // time-to-action frames, and change/result templates remain planned in the
      // append-only Peppa expected-behavior note instead of being patched here.
      ["故仔", { label: "what", jyutping: "gu3 zai2", syntax: "story_noun object_np", note: "colloquial story noun; prevents misleading 故/仔 unknown split in active corpus." }],
      ["花", { label: "what", jyutping: "faa1", syntax: "flower_noun object_np", note: "flower" }],
      ["第二個", { label: "what", jyutping: "dai6 ji6 go3", syntax: "object_np headless_choice_np ordinal_or_alternative_np", note: "another / other one in the checked choice context; ordinal versus alternative reading otherwise depends on context." }],
      ["第二", { label: "func", jyutping: "dai6 ji6", syntax: "ordinal_modifier sequence_modifier", note: "second; ordinal/sequence modifier before a classifier, not a how-many question word." }],
      ["本", { label: "measure_word", jyutping: "bun2", syntax: "classifier_book", note: "measure word / classifier for books." }],
      ["本書", { label: "what", jyutping: "bun2 syu1", syntax: "classifier_object_np affected_theme_np", note: "Reference entry for the transparent classifier-object phrase 本 + 書; tokenizer must preserve the classifier and noun as separate learner-visible tokens." }],
      ["架", { label: "measure_word", jyutping: "gaa3", syntax: "classifier_vehicle", note: "measure word / classifier for vehicles." }],
      ["部", { label: "measure_word", jyutping: "bou6", syntax: "classifier_vehicle_machine", note: "measure word / classifier for vehicles and machines." }],
      ["支", { label: "measure_word", jyutping: "zi1", syntax: "classifier_long_rigid pen_classifier", note: "classifier for long rigid objects such as pens." }],
      ["機", { label: "what", jyutping: "gei1", syntax: "machine_noun object_np head_noun", note: "machine or device; can be the visible noun head after 部." }],
      ["樣", { label: "measure_word", jyutping: "joeng6", syntax: "classifier_kind_item", note: "measure word / classifier for a kind/item, as in 有樣驚喜." }],
      ["塊", { label: "measure_word", jyutping: "faai3", syntax: "classifier_flat_piece", note: "measure word / classifier for flat pieces/slices." }],
      ["座", { label: "measure_word", jyutping: "zo6", syntax: "classifier_large_structure", note: "measure word / classifier for mountains, buildings, and large structures." }],
      ["條", { label: "measure_word", jyutping: "tiu4", syntax: "classifier_long_thin", note: "measure word / classifier for long thin things and rivers." }],
      ["圖書館", { label: "where", jyutping: "tou4 syu1 gun2", syntax: "place_np library_place", note: "library/place noun; prevents 書 from surfacing as an object inside 圖書館." }],
      ["度", { label: "func", jyutping: "dou6", syntax: "locative_postposition", note: "locative postposition after 喺 + place." }],
      ["借", { label: "doing", jyutping: "ze3", syntax: "verb borrow_verb", note: "borrow/lend; transitive verb in the library corpus lines." }],
      ["交", { label: "doing", jyutping: "gaau1", syntax: "verb hand_over_verb", note: "hand over / deliver; reviewed CP021B predicate profile." }],
      ["織", { label: "doing", jyutping: "zik1", syntax: "verb creation_verb", note: "knit / weave; reviewed CP021B predicate profile." }],
      ["冷", { label: "like", jyutping: "laang5", syntax: "nominal_modifier clothing_modifier", note: "woollen / sweater-forming modifier in 冷衫." }],
      ["厚", { label: "like", jyutping: "hau5", syntax: "stative_predicate nominal_modifier degreeable", note: "thick; modifier in a reviewed CP021B heavy-theme boundary." }],
      ["整理", { label: "doing", jyutping: "zing2 lei5", syntax: "verb organization_action", note: "organize / put in order; visible relative-clause predicate in a CP021B boundary." }],
      ["書展", { label: "where", jyutping: "syu1 zin2", syntax: "place_np event_location", note: "book fair; visible relative-clause location in a reviewed CP021B example." }],
      ["讚", { label: "doing", jyutping: "zaan3", syntax: "verb transitive_social_action_verb", note: "praise; predicate in a preserved CP020 passive example." }],
      ["之後", { label: "when", jyutping: "zi1 hau6", syntax: "temporal_connector", note: "after / afterwards." }],
      ["還", { label: "doing", jyutping: "waan4", syntax: "verb return_borrowed_item", note: "return a borrowed item." }],
      ["準時", { label: "when", jyutping: "zeon2 si4", syntax: "temporal_manner_adverb", note: "on time / punctually." }],
      ["叫做", { label: "doing", jyutping: "giu3 zou6", syntax: "naming_verb", note: "be called / named." }],
      ["Peppa", { label: "who", jyutping: "pe1 paa1", syntax: "proper_name_ascii", note: "ASCII proper name in active corpus; Jyutping-style placeholder avoids letter-level neutral noise." }],
      ["George", { label: "who", jyutping: "zo3 zi6", syntax: "proper_name_ascii", note: "ASCII proper name in active corpus; prevents letter-level neutral noise." }],
      ["Richard", { label: "who", jyutping: "lei5 caat3", syntax: "proper_name_ascii", note: "ASCII proper name in active corpus; Cantonese-name reading placeholder prevents letter-level neutral noise." }],
      ["細佬", { label: "who", jyutping: "sai3 lou2", syntax: "kinship_person_np", note: "younger brother." }],
      ["時候", { label: "when", jyutping: "si4 hau6", syntax: "time_np", note: "time / occasion; keeps 係時候 lines from splitting 時/候 before a future time-to-action template." }],
      ["早唞", { label: "func", jyutping: "zou2 tau2", syntax: "formula_discourse_unit goodnight_formula", note: "good night / rest early formula." }],
      ["入邊", { label: "where", jyutping: "jap6 bin1", syntax: "place_np interior_location", note: "inside / interior." }],
      ["入面", { label: "where", jyutping: "jap6 min6", syntax: "place_np interior_location", note: "inside / interior." }],
      ["保持", { label: "doing", jyutping: "bou2 ci4", syntax: "verb maintain_verb", note: "maintain / keep." }],
      ["安靜", { label: "like", jyutping: "on1 zing6", syntax: "stative_predicate degreeable quiet_property", note: "quiet; stative complement/predicate." }],
      ["綠色", { label: "like", jyutping: "luk6 sik1", syntax: "color_modifier stative_predicate degreeable", note: "green; color modifier/stative." }],
      ["紅色", { label: "like", jyutping: "hung4 sik1", syntax: "color_modifier stative_predicate degreeable", note: "red; color modifier/stative." }],
      ["掣", { label: "what", jyutping: "zai3", syntax: "object_np button_switch_noun", note: "button / switch." }],
      ["用", { label: "doing", jyutping: "jung6", syntax: "verb use_verb instrument_coverb", note: "use; as a preverbal coverb it can mark an instrument before an action predicate." }],
      ["寫", { label: "doing", jyutping: "se2", syntax: "verb write_verb", note: "write." }],
      ["字", { label: "what", jyutping: "zi6", syntax: "object_np writing_content_np", note: "characters / writing." }],
      ["泥氹", { label: "what", jyutping: "nai4 tam5", syntax: "object_np place_np puddle_noun", note: "mud puddle; object/place-capable noun." }],
      ["跳", { label: "doing", jyutping: "tiu3", syntax: "verb jump_verb", note: "jump." }],
      ["水喉", { label: "what", jyutping: "seoi2 hau4", syntax: "object_np tap_noun", note: "water tap / faucet." }],
      ["露營車", { label: "what", jyutping: "lou6 jing4 ce1", syntax: "vehicle_np object_np topic_np", note: "camper van / caravan." }],
      ["帶", { label: "doing", jyutping: "daai3", syntax: "verb take_bring_verb", note: "take / bring." }],
      ["度假", { label: "doing", jyutping: "dou6 gaa3", syntax: "verb vacation_verb", note: "go on holiday / vacation." }],
      ["請", { label: "func", jyutping: "cing2", syntax: "politeness_imperative_marker", note: "please; polite imperative marker." }],
      ["沿住", { label: "func", jyutping: "jyun4 zyu6", syntax: "path_coverb", note: "along / following a path." }],
      ["公路", { label: "where", jyutping: "gung1 lou6", syntax: "place_np path_np road_noun", note: "road / highway." }],
      ["直行", { label: "doing", jyutping: "zik6 haang4", syntax: "verb motion_verb go_straight", note: "go straight." }],
      ["係咪", { label: "func", jyutping: "hai6 mai6", syntax: "yes_no_question_marker", note: "is it / whether; yes-no question marker." }],
      ["蕩失路", { label: "doing", jyutping: "dong6 sat1 lou6", syntax: "verb get_lost_idiom", note: "get lost." }],
      ["爹哋", { label: "who", jyutping: "de1 di6", syntax: "kinship_person_np", note: "daddy / father." }],
      ["驚喜", { label: "what", jyutping: "ging1 hei2", syntax: "object_np surprise_noun abstract_np", note: "surprise." }],
      ["最", { label: "how", jyutping: "zeoi3", syntax: "degree_superlative", note: "most; superlative degree adverb." }],
      ["叻", { label: "like", jyutping: "lek1", syntax: "stative_predicate degreeable skillful_property", note: "clever / skillful / good at." }],
      ["地圖", { label: "what", jyutping: "dei6 tou4", syntax: "object_np map_noun", note: "map." }],
      ["已經", { label: "when", jyutping: "ji5 ging1", syntax: "temporal_adverb perfective_temporal", note: "already." }],
      ["到達", { label: "doing", jyutping: "dou3 daat6", syntax: "verb arrive_reach_verb", note: "arrive / reach." }],
      ["目的地", { label: "where", jyutping: "muk6 dik1 dei6", syntax: "place_np goal_place destination_noun", note: "destination." }],
      ["野餐", { label: "doing", jyutping: "je5 caan1", syntax: "verb picnic_verb", note: "have a picnic." }],
      ["豬先生", { label: "who", jyutping: "zyu1 sin1 saang1", syntax: "personified_name_title_np", note: "Mr Pig; personified character name/title." }],
      ["喺度", { label: "func", jyutping: "hai2 dou6", syntax: "progressive_or_locative_marker", note: "at here / progressive marker depending context." }],
      ["整", { label: "doing", jyutping: "zing2", syntax: "verb make_fix_verb", note: "make / fix / repair." }],
      ["蔬菜湯", { label: "what", jyutping: "so1 coi3 tong1", syntax: "food_np soup_noun object_np", note: "vegetable soup." }],
      ["午餐", { label: "what", jyutping: "ng5 caan1", syntax: "meal_np food_np object_np", note: "lunch." }],
      ["蔬果皮", { label: "what", jyutping: "so1 gwo2 pei4", syntax: "object_np compostable_food_peels", note: "fruit/vegetable peels." }],
      ["堆肥", { label: "what", jyutping: "deoi1 fei4", syntax: "object_np compost_noun", note: "compost." }],
      ["營養", { label: "what", jyutping: "jing4 joeng5", syntax: "abstract_np nutrition_noun", note: "nutrition; in 有營養 = nutritious." }],
      ["泥土", { label: "what", jyutping: "nai4 tou2", syntax: "object_np soil_noun", note: "soil." }],
      ["香蕉皮", { label: "what", jyutping: "hoeng1 ziu1 pei4", syntax: "object_np banana_peel_noun", note: "banana peel." }],
      ["幫助", { label: "doing", jyutping: "bong1 zo6", syntax: "verb help_verb", note: "help." }],
      ["植物", { label: "what", jyutping: "zik6 mat6", syntax: "object_np plant_noun", note: "plant." }],
      ["生長", { label: "doing", jyutping: "sang1 zoeng2", syntax: "verb grow_verb", note: "grow." }],
      ["蟲蟲", { label: "what", jyutping: "cung4 cung2", syntax: "animal_np object_np bug_noun", note: "bug/wormy creature; animals remain learner role what unless personification is later promoted." }],
      ["將", { label: "func", jyutping: "zoeng1", syntax: "disposal_coverb", note: "disposal/handling coverb 將 + object + predicate." }],
      ["搵", { label: "doing", jyutping: "wan2", syntax: "verb find_verb", note: "find / look for." }],
      ["轆", { label: "doing", jyutping: "luk1", syntax: "verb swipe_card_verb", note: "swipe, as in 轆咭; source-linked bounded action use." }],
      ["咭", { label: "what", jyutping: "kaat1", syntax: "object_np card_noun", note: "card; Hong Kong written variant in 轆咭." }],
      ["蘋果芯", { label: "what", jyutping: "ping4 gwo2 sam1", syntax: "object_np apple_core_noun", note: "apple core." }],
      ["掉落", { label: "doing", jyutping: "diu6 lok6", syntax: "verb drop_into_down", note: "drop / put down / fall down; contextually drop into a bin." }],
      ["堆肥箱", { label: "what", jyutping: "deoi1 fei4 soeng1", syntax: "container_np object_np compost_bin_noun", note: "compost bin." }],
      ["蚯蚓", { label: "what", jyutping: "jau1 jan5", syntax: "animal_np object_np earthworm_noun", note: "earthworm." }],
      ["睡房", { label: "where", jyutping: "seoi6 fong2", syntax: "place_np room_place bedroom_noun", note: "bedroom." }],
      ["變成", { label: "doing", jyutping: "bin3 sing4", syntax: "verb change_into_verb", note: "become / turn into." }],
      ["變", { label: "doing", jyutping: "bin3", syntax: "verb change_verb", note: "change / become; future change-result templates should keep 變+咗 transparent." }],
      ["恐龍天地", { label: "what", jyutping: "hung2 lung4 tin1 dei6", syntax: "compound_np dinosaur_world object_np", note: "dinosaur world; story-specific compound noun." }],
      ["恐龍", { label: "what", jyutping: "hung2 lung4", syntax: "animal_toy_np object_np dinosaur_noun", note: "dinosaur." }],
      ["火車", { label: "what", jyutping: "fo2 ce1", syntax: "vehicle_toy_np object_np train_noun", note: "train." }],
      ["冧", { label: "doing", jyutping: "lam3", syntax: "verb result_collapse_verb", note: "collapse / knock down; appears in 整冧咗." }],
      ["屋", { label: "what", jyutping: "uk1", syntax: "object_np house_noun", classifier_classes: ["building_shop"], note: "house." }],
      ["嘈", { label: "like", jyutping: "cou4", syntax: "stative_predicate degreeable noisy_property", note: "noisy." }],
      ["靜", { label: "like", jyutping: "zing6", syntax: "stative_predicate degreeable quiet_property", note: "quiet; property predicate in location-framed clauses such as 屋企好靜." }],
      ["擺", { label: "doing", jyutping: "baai2", syntax: "verb action_verb positioning_verb", note: "put / place; placement predicate in locative-inversion structures." }],
      ["玩具籃", { label: "what", jyutping: "wun6 geoi6 laam4", syntax: "container_np object_np toy_basket_noun", note: "toy basket." }],
      ["火山", { label: "what", jyutping: "fo2 saan1", syntax: "place_object_np volcano_noun", note: "volcano." }],
      ["積木", { label: "what", jyutping: "zik1 muk6", syntax: "object_np blocks_noun", note: "building blocks." }],
      ["河", { label: "where", jyutping: "ho4", syntax: "place_np river_noun", note: "river; place-like object in the toy-room transformation line." }],
      ["醫院", { label: "where", jyutping: "ji1 jyun2", syntax: "place_np hospital_noun", note: "hospital/place noun." }],
      ["一定", { label: "how", jyutping: "jat1 ding6", syntax: "certainty_adverb", note: "definitely / certainly; premodal certainty adverb." }],
      ["上床", { label: "doing", jyutping: "soeng5 cong4", syntax: "verb get_into_bed", note: "go/get into bed." }],
      ["休息", { label: "doing", jyutping: "jau1 sik1", syntax: "verb rest_verb", note: "rest." }],
      ["出邊", { label: "where", jyutping: "ceot1 bin1", syntax: "place_np outside_location", note: "outside." }],
      ["停", { label: "doing", jyutping: "ting4", syntax: "verb stop_verb", note: "stop." }],
      ["雨", { label: "what", jyutping: "jyu5", syntax: "weather_np rain_noun weather_phenomenon environmental_phenomenon object_np", note: "rain; a visible weather-phenomenon component in 落雨." }],
      ["風", { label: "what", jyutping: "fung1", syntax: "weather_np wind_noun weather_phenomenon environmental_phenomenon", note: "wind; a visible weather-phenomenon component in 打風." }],
      ["天光", { label: "doing", jyutping: "tin1 gwong1", syntax: "environmental_transition_predicate daylight_transition_predicate impersonal_predicate", note: "daybreak / become light; a genuinely subjectless environmental transition predicate." }],
      ["花園", { label: "where", jyutping: "faa1 jyun2", syntax: "place_np garden_noun", note: "garden." }],
      ["通常", { label: "when", jyutping: "tung1 soeng4", syntax: "habitual_frequency_adverb", note: "usually." }],
      // v0.5.227: source-audited high-frequency lexical coverage.
      ["知道", { "label": "doing", "jyutping": "zi1 dou3", "syntax": "cognition_verb", "note": "know / be aware of; Cifu spoken top-2000 R1 lexical audit." }],
      ["繼續", { "label": "doing", "jyutping": "gai3 zuk6", "syntax": "verb", "note": "continue / proceed; Cifu spoken top-2000 R1 lexical audit." }],
      ["恨", { "label": "doing", "jyutping": "han6", "syntax": "verb attitude_verb", "note": "hate / regret / long for; Cifu spoken top-2000 R1 lexical audit." }],
      ["交叉", { "label": "doing", "jyutping": "gaau1 caa1", "syntax": "verb", "note": "cross / intersect; Cifu spoken top-2000 R1 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R2 source-audited lexical coverage.
      ["經過", { "label": "doing", "jyutping": "ging1 gwo3", "syntax": "verb motion_path_verb", "note": "pass through / go through / undergo; Cifu spoken top-2000 R2 lexical audit." }],
      ["需要", { "label": "doing", "jyutping": "seoi1 jiu3", "syntax": "verb necessity_predicate", "note": "need / require; Cifu spoken top-2000 R2 lexical audit." }],
      ["搞", { "label": "doing", "jyutping": "gaau2", "syntax": "verb", "note": "do / make / arrange / handle; Cifu spoken top-2000 R2 lexical audit." }],
      ["希望", { "label": "doing", "jyutping": "hei1 mong6", "syntax": "verb desire_verb", "note": "hope / wish for; Cifu spoken top-2000 R2 lexical audit." }],
      ["滑雪", { "label": "doing", "jyutping": "waat6 syut3", "syntax": "verb activity_verb", "note": "ski / skiing; Cifu spoken top-2000 R2 lexical audit." }],
      ["明白", { "label": "doing", "jyutping": "ming4 baak6", "syntax": "verb cognition_verb", "note": "understand / realize; Cifu spoken top-2000 R2 lexical audit." }],
      ["感覺", { "label": "doing", "jyutping": "gam2 gok3", "syntax": "verb perception_verb", "note": "feel / sense; Cifu spoken top-2000 R2 lexical audit." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/statives.js
var require_statives = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/statives.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Statives / property predicates
      ["開心", { label: "like", jyutping: "hoi1 sam1", syntax: "stative_predicate", note: "happy" }],
      ["熟", { label: "like", jyutping: "suk6", syntax: "stative_predicate result_state_property", note: "ripe / cooked / familiar; in native mango-picking speech 熟咗 means became ripe / is ripe now." }],
      ["甜", { label: "like", jyutping: "tim4", syntax: "stative_predicate", note: "sweet; lexicon/Jyutping cleanup for UI example diagnostics." }],
      ["笑", { label: "doing", jyutping: "siu3", syntax: "verb", note: "laugh / smile; kept as a plain verb unless lexicalized in 好笑." }],
      // Lexicalized sensory/evaluation stative predicates.
      // These are treated as whole stative predicates when they occur by themselves.
      // Intensified forms such as 好好食 remain compositional: 好 + 好食.
      ["好食", { label: "like", jyutping: "hou2 sik6", syntax: "lexicalized_stative_predicate sensory_evaluation food_evaluation degreeable", note: "lexicalized stative — tasty / delicious / good to eat; antonym 難食" }],
      ["好飲", { label: "like", jyutping: "hou2 jam2", syntax: "lexicalized_stative_predicate sensory_evaluation drink_evaluation degreeable", note: "lexicalized stative — tasty / good to drink; antonym 難飲" }],
      ["好睇", { label: "like", jyutping: "hou2 tai2", syntax: "lexicalized_stative_predicate sensory_evaluation visual_evaluation degreeable", note: "lexicalized stative — good-looking / enjoyable to watch; antonym 難睇" }],
      ["好聽", { label: "like", jyutping: "hou2 teng1", syntax: "lexicalized_stative_predicate sensory_evaluation sound_evaluation degreeable", note: "lexicalized stative — pleasant-sounding / pleasant to hear; antonym 難聽" }],
      ["好味", { label: "like", jyutping: "hou2 mei6", syntax: "lexicalized_stative_predicate sensory_evaluation taste_evaluation degreeable", note: "lexicalized stative — delicious / tasty" }],
      ["好笑", { label: "like", jyutping: "hou2 siu3", syntax: "lexicalized_stative_predicate evaluation amusing_evaluation degreeable", note: "lexicalized stative — funny / amusing; added for UI example diagnostics." }],
      // Negative lexicalized sensory/evaluation stative predicates.
      // These are conventional bad-quality counterparts, not prohibitives.
      ["難食", { label: "like", jyutping: "naan4 sik6", syntax: "negative_lexicalized_stative_predicate lexicalized_stative_predicate stative_predicate sensory_evaluation food_evaluation degreeable", note: "negative lexicalized stative — bad-tasting / unpalatable; antonym 好食" }],
      ["難飲", { label: "like", jyutping: "naan4 jam2", syntax: "negative_lexicalized_stative_predicate lexicalized_stative_predicate stative_predicate sensory_evaluation drink_evaluation degreeable", note: "negative lexicalized stative — bad to drink / unpleasant-tasting; antonym 好飲" }],
      ["難睇", { label: "like", jyutping: "naan4 tai2", syntax: "negative_lexicalized_stative_predicate lexicalized_stative_predicate stative_predicate sensory_evaluation visual_evaluation degreeable", note: "negative lexicalized stative — ugly / unpleasant to watch; antonym 好睇" }],
      ["難聽", { label: "like", jyutping: "naan4 teng1", syntax: "negative_lexicalized_stative_predicate lexicalized_stative_predicate stative_predicate sensory_evaluation sound_evaluation degreeable", note: "negative lexicalized stative — unpleasant-sounding / harsh; antonym 好聽" }],
      ["好好食", { label: "like", jyutping: "hou2 hou2 sik6", syntax: "degree_stative_predicate", note: "very tasty; kept compositional as 好 + 好食" }],
      ["好貴", { label: "like", jyutping: "hou2 gwai3", syntax: "degree_stative_predicate", note: "expensive" }],
      ["好甜", { label: "like", jyutping: "hou2 tim4", syntax: "degree_stative_predicate", note: "sweet; lexicon/Jyutping cleanup for UI example diagnostics." }],
      ["好靚", { label: "like", jyutping: "hou2 leng3", syntax: "degree_stative_predicate", note: "nice / pretty" }],
      ["好正", { label: "like", jyutping: "hou2 zeng3", syntax: "degree_stative_predicate", note: "great / excellent" }],
      ["抵食", { label: "like", jyutping: "dai2 sik6", syntax: "stative_predicate", note: "good value to eat / worth the price" }],
      ["出名", { label: "like", jyutping: "ceot1 meng2", syntax: "stative_predicate", note: "famous" }],
      ["好出名", { label: "like", jyutping: "hou2 ceot1 meng2", syntax: "degree_stative_predicate", note: "very famous" }],
      ["適合", { label: "like", jyutping: "sik1 hap6", syntax: "stative_predicate", note: "suitable" }],
      ["好適合", { label: "like", jyutping: "hou2 sik1 hap6", syntax: "degree_stative_predicate", note: "very suitable" }],
      ["靚", { label: "like", jyutping: "leng3", syntax: "stative_predicate", note: "pretty" }],
      ["大", { label: "like", jyutping: "daai6", syntax: "stative_predicate", note: "big" }],
      ["細", { label: "like", jyutping: "sai3", syntax: "stative_predicate", note: "small" }],
      ["多", { label: "like", jyutping: "do1", syntax: "stative_or_quantity", note: "many/much" }],
      ["少", { label: "like", jyutping: "siu2", syntax: "stative_or_quantity", note: "few/little" }],
      ["貴", { label: "like", jyutping: "gwai3", syntax: "stative_predicate", note: "expensive" }],
      ["平", { label: "like", jyutping: "peng4", syntax: "stative_predicate", note: "cheap" }],
      ["快", { label: "like", jyutping: "faai3", syntax: "stative_predicate", note: "fast" }],
      ["慢", { label: "like", jyutping: "maan6", syntax: "stative_predicate", note: "slow" }],
      ["大聲", { label: "how", jyutping: "daai6 seng1", syntax: "manner_degree_word degree_manner_head", note: "loudly / loud-voice manner word; degreeable before 啲." }],
      ["細聲", { label: "how", jyutping: "sai3 seng1", syntax: "manner_degree_word degree_manner_head", note: "quietly / soft-voice manner word; degreeable before 啲." }],
      ["小心", { label: "how", jyutping: "siu2 sam1", syntax: "manner_degree_word degree_manner_head", note: "carefully / be careful; degreeable before 啲." }],
      ["忙", { label: "like", jyutping: "mong4", syntax: "stative_predicate", note: "busy" }],
      ["遠", { label: "like", jyutping: "jyun5", syntax: "stative_predicate distance_stative scalar_dimension_predicate", note: "far" }],
      ["近", { label: "like", jyutping: "kan5", syntax: "stative_predicate distance_stative scalar_dimension_predicate", note: "near / close" }],
      ["高", { label: "like", jyutping: "gou1", syntax: "stative_predicate height_stative scalar_dimension_predicate", note: "high / tall" }],
      ["矮", { label: "like", jyutping: "ai2", syntax: "stative_predicate height_stative scalar_dimension_predicate", note: "short / low in height; gradable property predicate and antonym of 高." }],
      ["低", { label: "like", jyutping: "dai1", syntax: "stative_predicate directional_result_complement", note: "low / lower; result complement in 放低" }],
      ["耐", { label: "like", jyutping: "noi6", syntax: "stative_predicate duration_stative scalar_dimension_predicate", note: "long in time / duration" }],
      ["熱", { label: "like", jyutping: "jit6", syntax: "stative_predicate ambient_property_predicate temperature_property", note: "hot; may describe a participant or an ambient environment depending on overt framing and context" }],
      ["凍", { label: "like", jyutping: "dung3", syntax: "stative_predicate ambient_property_predicate temperature_property", note: "cold; may describe a participant or an ambient environment depending on overt framing and context" }],
      ["難", { label: "like", jyutping: "naan4", syntax: "stative_predicate", note: "hard" }],
      ["易", { label: "like", jyutping: "ji6", syntax: "stative_predicate", note: "easy" }],
      ["正", { label: "like", jyutping: "zeng3", syntax: "stative_predicate", note: "great / excellent" }],
      ["抵", { label: "like", jyutping: "dai2", syntax: "stative_predicate", note: "worth it" }],
      ["新", { label: "like", jyutping: "san1", syntax: "stative_or_modifier", note: "new" }],
      ["開", { label: "doing", jyutping: "hoi1", syntax: "verb_or_modifier_head", note: "open" }],
      ["新開", { label: "like", jyutping: "san1 hoi1", syntax: "noun_modifier", note: "newly opened; long modified NPs render transparently when possible." }],
      ["特別", { label: "like", jyutping: "dak6 bit6", syntax: "modifier_or_stative", note: "special" }],
      ["啱", { label: "like", jyutping: "ngaam1", syntax: "stative_predicate", note: "right / suitable" }],
      ["算", { label: "func", jyutping: "syun3", syntax: "evaluation_verb_or_marker", note: "count as / be considered" }],
      ["唔算", { label: "func", jyutping: "m4 syun3", syntax: "negative_evaluation_marker", note: "doesn't count as / not considered; tokenizer keeps 唔 + 算 visible in transparent price evaluations." }],
      // v0.5.227: source-audited high-frequency lexical coverage.
      ["香", { "label": "like", "jyutping": "hoeng1", "syntax": "stative_predicate sensory_property", "note": "fragrant / aromatic; Cifu spoken top-2000 R1 lexical audit." }],
      ["善", { "label": "like", "jyutping": "sin6", "syntax": "stative_predicate evaluative_property", "note": "good / benevolent; Cifu spoken top-2000 R1 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R6 stative lexical coverage.
      ["傻", { "label": "like", "jyutping": "so4", "syntax": "stative_predicate evaluative_property", "note": "foolish / silly; Cifu spoken top-2000 R6 lexical audit." }],
      ["勁", { "label": "like", "jyutping": "ging6", "syntax": "stative_predicate scalar_property", "note": "strong / powerful / excellent; Cifu spoken top-2000 R6 lexical audit." }],
      ["暖", { "label": "like", "jyutping": "nyun5", "syntax": "stative_predicate temperature_property", "note": "warm; Cifu spoken top-2000 R6 lexical audit." }],
      ["濫", { "label": "like", "jyutping": "laam6", "syntax": "stative_predicate evaluative_property", "note": "excessive / indiscriminate; Cifu spoken top-2000 R6 lexical audit." }],
      ["小", { "label": "like", "jyutping": "siu2", "syntax": "stative_predicate size_stative scalar_dimension_predicate", "note": "small / tiny; Cifu spoken top-2000 R6 lexical audit." }],
      ["斜", { "label": "like", "jyutping": "ce4", "syntax": "stative_predicate orientation_property", "note": "slanting / oblique; Cifu spoken top-2000 R6 lexical audit." }],
      ["瑞", { "label": "like", "jyutping": "seoi6", "syntax": "stative_predicate evaluative_property", "note": "auspicious / lucky; Cifu spoken top-2000 R6 lexical audit." }],
      ["重要", { "label": "like", "jyutping": "zung6 jiu3", "syntax": "stative_predicate importance_property", "note": "important / significant; Cifu spoken top-2000 R6 lexical audit." }],
      ["貧", { "label": "like", "jyutping": "pan4", "syntax": "stative_predicate resource_property", "note": "poor / deficient; Cifu spoken top-2000 R6 lexical audit." }],
      ["彎", { "label": "like", "jyutping": "waan1", "syntax": "stative_predicate shape_property", "note": "bent / curved; Cifu spoken top-2000 R6 lexical audit." }],
      ["錯", { "label": "like", "jyutping": "co3", "syntax": "stative_predicate evaluative_property", "note": "wrong / mistaken; Cifu spoken top-2000 R6 lexical audit." }],
      ["舊", { "label": "like", "jyutping": "gau6", "syntax": "stative_predicate age_state_stative", "note": "old / former / worn; Cifu spoken top-2000 R6 lexical audit." }],
      ["似", { "label": "like", "jyutping": "ci5", "syntax": "stative_predicate similarity_predicate", "note": "similar / resemble / seem; Cifu spoken top-2000 R6 lexical audit." }],
      ["簡單", { "label": "like", "jyutping": "gaan2 daan1", "syntax": "stative_predicate complexity_property", "note": "simple / uncomplicated; Cifu spoken top-2000 R6 lexical audit." }],
      ["主要", { "label": "like", "jyutping": "zyu2 jiu3", "syntax": "stative_predicate importance_property modifier", "note": "main / principal / primary; Cifu spoken top-2000 R6 lexical audit." }],
      ["怪", { "label": "like", "jyutping": "gwaai3", "syntax": "stative_predicate evaluative_property", "note": "strange / odd / uncanny; Cifu spoken top-2000 R6 lexical audit." }],
      ["幼", { "label": "like", "jyutping": "jau3", "syntax": "stative_predicate age_state_stative", "note": "young; Cifu spoken top-2000 R6 lexical audit." }],
      ["普通", { "label": "like", "jyutping": "pou2 tung1", "syntax": "stative_predicate evaluative_property", "note": "common / ordinary / average; Cifu spoken top-2000 R6 lexical audit." }],
      ["直", { "label": "like", "jyutping": "zik6", "syntax": "stative_predicate orientation_property", "note": "straight / upright; Cifu spoken top-2000 R6 lexical audit." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/function-words-and-particles.js
var require_function_words_and_particles = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/function-words-and-particles.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Function words / particles / stance
      ["唔", { label: "func", jyutping: "m4", syntax: "negator", note: "not" }],
      ["冇", { label: "func", jyutping: "mou5", syntax: "negated_existential", note: "not have / no" }],
      ["有", { label: "func", jyutping: "jau5", syntax: "existential_or_possessive", note: "have / exist" }],
      ["有冇", { label: "func", jyutping: "jau5 mou5", syntax: "existential_question", note: "have or not?" }],
      ["有冇興趣", { label: "func", jyutping: "jau5 mou5 hing3 ceoi3", syntax: "interest_question_frame", note: "are you interested in..." }],
      ["係", { label: "func", jyutping: "hai6", syntax: "copula", note: "be / be the case" }],
      ["唔係", { label: "func", jyutping: "m4 hai6", syntax: "negated_copula", note: "not be" }],
      ["想", { label: "func", jyutping: "soeng2", syntax: "modal_desiderative", note: "want to" }],
      ["要", { label: "func", jyutping: "jiu3", syntax: "modal_need_or_want", note: "need / want" }],
      ["可以", { label: "func", jyutping: "ho2 ji5", syntax: "modal_permission_or_ability", note: "can / may" }],
      ["得", { label: "func", jyutping: "dak1", syntax: "acceptability_predicate", note: "okay / can / works" }],
      ["可唔可以", { label: "func", jyutping: "ho2 m4 ho2 ji5", syntax: "modal_a_not_a", note: "can or not?" }],
      ["會", { label: "func", jyutping: "wui5", syntax: "modal_future_or_habitual", note: "will / would / know how in some contexts" }],
      ["識", { label: "func", jyutping: "sik1", syntax: "modal_know_how", note: "know how to" }],
      ["完", { label: "func", jyutping: "jyun4", syntax: "completive_result", note: "finish / completive" }],
      ["到", { label: "func", jyutping: "dou2", syntax: "result_or_attainment", note: "achieve / can reach result" }],
      ["咗", { label: "func", jyutping: "zo2", syntax: "perfective_aspect", note: "completed action marker" }],
      ["緊", { label: "func", jyutping: "gan2", syntax: "progressive_aspect", note: "in progress" }],
      ["過", { label: "func", jyutping: "gwo3", syntax: "experiential_aspect", note: "have done before" }],
      ["晒", { label: "func", jyutping: "saai3", syntax: "completive_totality_aspect", note: "finish all / completely" }],
      ["未", { label: "func", jyutping: "mei6", syntax: "aspectual_question", note: "not yet / whether completed" }],
      ["先", { label: "how", jyutping: "sin1", syntax: "sequence_or_focus", note: "first / only then" }],
      ["等陣", { label: "when", jyutping: "dang2 zan6", syntax: "time_np", note: "later / in a moment" }],
      ["再", { label: "how", jyutping: "zoi3", syntax: "sequence", note: "then / again" }],
      ["都", { label: "how", jyutping: "dou1", syntax: "focus_adverb", note: "also / even" }],
      ["都算", { label: "func", jyutping: "dou1 syun3", syntax: "evaluation_marker", note: "can be counted as / fairly" }],
      ["又", { label: "how", jyutping: "jau6", syntax: "focus_adverb", note: "again / also" }],
      ["就", { label: "func", jyutping: "zau6", syntax: "focus_or_sequence", note: "then / exactly" }],
      ["就係", { label: "func", jyutping: "zau6 hai6", syntax: "focus_copula", note: "it is exactly / precisely" }],
      ["如果", { label: "func", jyutping: "jyu4 gwo2", syntax: "connector_condition", note: "if" }],
      ["因為", { label: "func", jyutping: "jan1 wai6", syntax: "connector_reason", note: "because" }],
      ["所以", { label: "func", jyutping: "so2 ji5", syntax: "connector_result", note: "so" }],
      ["雖然", { label: "func", jyutping: "seoi1 jin4", syntax: "connector_concession", note: "although / even though" }],
      ["但係", { label: "func", jyutping: "daan6 hai6", syntax: "connector_contrast", note: "but" }],
      ["不過", { label: "func", jyutping: "bat1 gwo3", syntax: "connector_contrast", note: "however / but" }],
      ["同埋", { label: "func", jyutping: "tung4 maai4", syntax: "connector_additive", note: "and also" }],
      ["咁", { label: "func", jyutping: "gam2", syntax: "discourse_marker", note: "then / so / like that" }],
      ["噉", { label: "func", jyutping: "gam2", syntax: "discourse_marker demonstrative_manner", note: "orthographic variant of 咁: then / so / like that" }],
      ["與其", { label: "func", jyutping: "jyu5 kei4", syntax: "ordered_preference_opener", note: "rather than; opens the disfavored member of 與其...不如..." }],
      ["不如", { label: "func", jyutping: "bat1 jyu4", syntax: "suggestion_marker", note: "how about / why don't we" }],
      ["應該", { label: "func", jyutping: "jing1 goi1", syntax: "stance_probability_or_should", note: "should / probably" }],
      ["大概", { label: "how", jyutping: "daai6 koi3", syntax: "approximation_adverb", note: "approximately" }],
      ["左右", { label: "how", jyutping: "zo2 jau6", syntax: "approximation_marker", note: "around / approximately" }],
      ["啲", { label: "how", jyutping: "di1", syntax: "quantity_degree_or_plural", note: "some / more / comparative degree" }],
      ["平啲", { label: "like", jyutping: "peng4 di1", syntax: "comparative_stative", note: "cheaper" }],
      ["多啲", { label: "how", jyutping: "do1 di1", syntax: "comparative_quantity", note: "more" }],
      ["多啲人", { label: "who", jyutping: "do1 di1 jan4", syntax: "quantity_person_np", note: "more people" }],
      ["同", { label: "func", jyutping: "tung4", syntax: "connector_or_coverb comitative_coverb interpersonal_coverb", note: "and / with / to; as a coverb it can mark a co-participant or addressee depending on the predicate" }],
      ["同心協力", { label: "doing", jyutping: "tung4 sam1 hip3 lik6", syntax: "verb action_verb cooperative_action", note: "work together with one heart; predicate in the sourced PRQ2-014 necessary-condition example." }],
      ["喺", { label: "func", pos: "function", jyutping: "hai2", syntax: "locative_coverb", note: "locative coverb/marker: at / in / on; not a place noun" }],
      ["由", { label: "where", jyutping: "jau4", syntax: "source_coverb", note: "from" }],
      ["然後", { label: "func", jyutping: "jin4 hau6", syntax: "connector_sequence discourse_marker", note: "then / afterwards" }],
      ["見到", { label: "doing", jyutping: "gin3 dou2", syntax: "verb action_verb transitive_verb perception_verb immediate_temporal_trigger_predicate", note: "see / catch sight of" }],
      ["笑", { label: "doing", jyutping: "siu3", syntax: "verb action_verb intransitive_verb", note: "laugh / smile" }],
      ["同意", { label: "doing", jyutping: "tung4 ji3", syntax: "verb action_verb stance_agreement_predicate intransitive_use", note: "agree / consent" }],
      ["行", { label: "doing", jyutping: "haang4", syntax: "verb action_verb walking_verb", note: "walk / go on foot" }],
      ["每", { label: "func", jyutping: "mui5", syntax: "distributive_quantifier determiner", note: "every / each" }],
      ["年", { label: "when", jyutping: "nin4", syntax: "time_head temporal year_unit", note: "year / years" }],
      ["月", { label: "when", jyutping: "jyut6", syntax: "time_head temporal month_unit", note: "month / months" }],
      ["日", { label: "when", jyutping: "jat6", syntax: "time_head temporal day_noun day_unit", note: "day / days" }],
      ["唔好", { label: "func", jyutping: "m4 hou2", syntax: "prohibitive_marker_or_discourse_unit", note: "don't / not good, context dependent" }],
      ["唔使", { label: "func", jyutping: "m4 sai2", syntax: "modal_need_not", note: "do not need to" }],
      ["一齊", { label: "how", jyutping: "jat1 cai4", syntax: "comitative_adverb", note: "together" }],
      ["好似", { label: "func", jyutping: "hou2 ci5", syntax: "stance_seeming", note: "seems like" }],
      ["可能", { label: "func", jyutping: "ho2 nang4", syntax: "stance_possibility", note: "maybe" }],
      ["我覺得", { label: "doing", jyutping: "ngo5 gok3 dak1", syntax: "stance_cognition_frame", note: "I think/feel; compositional phrase should remain learner-visible when possible." }],
      ["我知", { label: "doing", jyutping: "ngo5 zi1", syntax: "cognition_frame", note: "I know; compositional phrase should remain learner-visible when possible." }],
      ["我唔知", { label: "doing", jyutping: "ngo5 m4 zi1", syntax: "negative_cognition_frame", note: "I don't know; compositional phrase should remain learner-visible when possible." }],
      // v0.5.227: source-audited high-frequency lexical coverage.
      ["其實", { "label": "how", "jyutping": "kei4 sat6", "syntax": "discourse_adverb stance_adverb", "note": "actually / in fact; Cifu spoken top-2000 R1 lexical audit." }],
      ["譬如", { "label": "func", "jyutping": "pei3 jyu4", "syntax": "example_introducer discourse_marker", "note": "for example / for instance / such as; Cifu spoken top-2000 R1 lexical audit." }],
      ["但", { "label": "func", "jyutping": "daan6", "syntax": "connector_contrast", "note": "but / however; Cifu spoken top-2000 R1 lexical audit." }],
      ["亦", { "label": "how", "jyutping": "jik6", "syntax": "additive_adverb focus_adverb", "note": "also / too; Cifu spoken top-2000 R1 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R2 source-audited lexical coverage.
      ["即係", { "label": "func", "jyutping": "zik1 hai6", "syntax": "discourse_marker reformulation_marker", "note": "exactly / that is / i.e.; Cifu spoken top-2000 R2 lexical audit." }],
      ["或者", { "label": "func", "jyutping": "waak6 ze2", "syntax": "alternative_connector epistemic_possibility_marker", "note": "or / maybe / perhaps; Cifu spoken top-2000 R2 lexical audit." }],
      ["梗係", { "label": "func", "jyutping": "gang2 hai6", "syntax": "discourse_marker certainty_marker", "note": "of course; Cifu spoken top-2000 R2 lexical audit." }],
      ["冇錯", { "label": "func", "jyutping": "mou5 co3", "syntax": "acknowledgement_formula discourse_marker", "note": "that is correct / exactly; Cifu spoken top-2000 R2 lexical audit." }],
      ["另外", { "label": "how", "jyutping": "ling6 ngoi6", "syntax": "additive_adverb discourse_adverb", "note": "additionally / besides / separately; Cifu spoken top-2000 R2 lexical audit." }],
      ["根本", { "label": "how", "jyutping": "gan1 bun2", "syntax": "emphatic_adverb discourse_adverb", "note": "fundamentally / simply / at all; Cifu spoken top-2000 R2 lexical audit." }],
      ["枉", { "label": "how", "jyutping": "wong2", "syntax": "manner_adverb evaluative_adverb", "note": "in vain / wrongly; Cifu spoken top-2000 R2 lexical audit." }],
      // v0.5.227: Cifu spoken top-2000 R5 lexical coverage.
      ["當然", { "label": "how", "jyutping": "dong1 jin4", "syntax": "stance_adverb discourse_marker", "note": "of course / certainly; Cifu spoken top-2000 R5 lexical audit." }],
      ["究竟", { "label": "how", "jyutping": "gau3 ging2", "syntax": "question_stance_adverb discourse_adverb", "note": "after all / actually; Cifu spoken top-2000 R5 lexical audit." }],
      ["總之", { "label": "func", "jyutping": "zung2 zi1", "syntax": "discourse_summary_marker", "note": "in short / in brief; Cifu spoken top-2000 R5 lexical audit." }],
      ["重有", { "label": "how", "jyutping": "zung6 jau5", "syntax": "additive_adverb discourse_adverb", "note": "furthermore / also; Cifu spoken top-2000 R5 lexical audit." }],
      ["更加", { "label": "how", "jyutping": "gang3 gaa1", "syntax": "degree_adverb comparative_intensifier", "note": "even more; Cifu spoken top-2000 R5 lexical audit." }],
      ["基本上", { "label": "how", "jyutping": "gei1 bun2 soeng6", "syntax": "stance_adverb discourse_adverb", "note": "basically / on the whole; Cifu spoken top-2000 R5 lexical audit." }],
      ["突然", { "label": "how", "jyutping": "dat6 jin4", "syntax": "manner_adverb temporal_adverb", "note": "suddenly; Cifu spoken top-2000 R5 lexical audit." }],
      ["直接", { "label": "how", "jyutping": "zik6 zip3", "syntax": "manner_adverb", "note": "directly / straightforwardly; Cifu spoken top-2000 R5 lexical audit." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/degree-and-formulas.js
var require_degree_and_formulas = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/degree-and-formulas.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // Degree words / particles / formulas
      ["好", { label: "how", jyutping: "hou2", syntax: "degree_marker", note: "very / good depending context" }],
      ["太", { label: "how", jyutping: "taai3", syntax: "degree_marker", note: "too" }],
      ["幾", { label: "how", jyutping: "gei2", syntax: "degree_marker", note: "quite / how many" }],
      ["有啲", { label: "how", jyutping: "jau5 di1", syntax: "degree_marker", note: "a bit" }],
      ["少少", { label: "how", jyutping: "siu2 siu2", syntax: "quantity_degree", note: "a little" }],
      ["好多", { label: "how", jyutping: "hou2 do1", syntax: "quantity_degree", note: "many / much" }],
      ["好多人", { label: "who", jyutping: "hou2 do1 jan4", syntax: "quantity_person_np", note: "many people" }],
      ["呀", { label: "particle", jyutping: "aa3", syntax: "sentence_final_particle", note: "softening / question particle" }],
      ["吖", { label: "particle", jyutping: "aa1", syntax: "sentence_final_particle", note: "soft question / response particle" }],
      ["㗎", { label: "particle", jyutping: "gaa3", syntax: "explanatory_sentence_final_particle", note: "explanatory/assertive particle; not only a question particle" }],
      ["啊", { label: "particle", jyutping: "aa3", syntax: "sentence_final_particle", note: "softening / question particle" }],
      ["喇", { label: "particle", jyutping: "laa3", syntax: "sentence_final_particle change_state_current_relevance_particle", note: "changed situation / current relevance / transition" }],
      ["啦", { label: "particle", jyutping: "laa1", syntax: "sentence_final_particle directive_closure_particle", note: "directive / suggestion / invitation / interpersonal closure" }],
      ["嘞", { label: "particle", jyutping: "laak3", syntax: "sentence_final_particle changed_state_particle", note: "changed state / finality particle; common readings include laak3, lak3, and laa3" }],
      ["呢", { label: "func", pos: "determiner", jyutping: "ni1", syntax: "demonstrative_determiner", note: "this; specifier/determiner before a classifier. Sentence-final 呢 is not handled in this slice." }],
      ["喎", { label: "particle", jyutping: "wo3", syntax: "sentence_final_particle evidential_noteworthiness_particle", note: "reported / noteworthy / reminder stance; exact subtype depends on tone and context" }],
      ["啩", { label: "particle", jyutping: "gwaa3", syntax: "sentence_final_particle epistemic_uncertainty_particle", note: "probably / perhaps; sentence-final particle marking uncertainty or probability over proposition-like content" }],
      ["囉", { label: "particle", jyutping: "lo1", syntax: "sentence_final_particle agreement_particle", note: "agreement / obviousness particle" }],
      ["嘅", { label: "particle", jyutping: "ge3", syntax: "sentence_final_particle_or_nominalizer", note: "assertion / nominalizer" }],
      ["嘅話", { label: "func", jyutping: "ge3 waa6", syntax: "conditional_marker protected_formula", note: "if / in the case that", review: "protected_formula" }],
      ["啫", { label: "particle", jyutping: "ze1", syntax: "sentence_final_particle restrictive_focus_particle minimizing_particle", note: "only / just" }],
      ["咋", { label: "particle", jyutping: "zaa3", syntax: "sentence_final_particle restrictive_focus_particle exhaustive_limit_particle", note: "only / that's all" }],
      ["嗎", { label: "particle", jyutping: "maa3", syntax: "question_particle", note: "yes/no question particle" }],
      ["係咩", { label: "func", jyutping: "hai6 me1", syntax: "formula_question", note: "really? is that so?" }],
      ["好呀", { label: "func", jyutping: "hou2 aa3", syntax: "formula_response", note: "okay" }],
      ["咁啱呀", { label: "func", jyutping: "gam2 ngaam1 aa3", syntax: "formula_response", note: "that works out / what a coincidence" }],
      ["咁就啱啦", { label: "func", jyutping: "gam2 zau6 ngaam1 laa1", syntax: "formula_response", note: "then that works / that's right then" }],
      ["放心", { label: "like", jyutping: "fong3 sam1", syntax: "reassurance_stative", note: "at ease / reassured; don't worry" }],
      ["放心啦", { label: "func", jyutping: "fong3 sam1 laa1", syntax: "formula_discourse_unit", note: "don't worry" }],
      ["到時見啦", { label: "func", jyutping: "dou3 si4 gin3 laa1", syntax: "formula_discourse_unit", note: "see you then" }],
      ["OK", { label: "func", jyutping: "ok1", syntax: "formula_response", note: "OK" }],
      ["噢", { label: "particle", jyutping: "o5", syntax: "interjection", note: "oh" }],
      ["哈哈", { label: "particle", jyutping: "haa1 haa1", syntax: "interjection", note: "haha" }],
      ["唔該", { label: "func", jyutping: "m4 goi1", syntax: "formula_discourse_unit", note: "please / thanks" }],
      ["唔該晒", { label: "func", jyutping: "m4 goi1 saai3", syntax: "formula_discourse_unit", note: "thanks very much" }],
      ["多謝", { label: "func", jyutping: "do1 ze6", syntax: "formula_discourse_unit", note: "thank you" }],
      ["唔好意思", { label: "func", jyutping: "m4 hou2 ji3 si1", syntax: "formula_discourse_unit", note: "excuse me / sorry" }],
      ["早晨", { label: "func", jyutping: "zou2 san4", syntax: "greeting_formula", note: "good morning" }],
      ["你好", { label: "func", jyutping: "nei5 hou2", syntax: "greeting_formula", note: "hello" }],
      ["見", { label: "doing", jyutping: "gin3", syntax: "verb", note: "see / meet" }],
      ["拜拜", { label: "func", jyutping: "baai1 baai3", syntax: "leave_taking_formula", note: "bye-bye" }],
      ["對唔住", { label: "func", jyutping: "deoi3 m4 zyu6", syntax: "apology_formula", note: "sorry" }],
      ["冇問題", { label: "func", jyutping: "mou5 man6 tai4", syntax: "formula_discourse_unit", note: "no problem" }],
      ["得喇", { label: "func", jyutping: "dak1 laa3", syntax: "formula_discourse_unit", note: "okay / that's enough" }],
      ["唔緊要", { label: "func", jyutping: "m4 gan2 jiu3", syntax: "formula_discourse_unit", note: "it doesn't matter" }],
      ["喂", { label: "particle", jyutping: "wai3", syntax: "attention_getter", note: "hey / hello" }],
      ["吓", { label: "particle", jyutping: "haa2", syntax: "interjection", note: "huh?" }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/study-suite.js
var require_study_suite = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/study-suite.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      // CP021B-LX1: bounded lexical and Jyutping coverage imported from the
      // rendered study-suite audit. These entries add lexical recognition only;
      // they do not create or promote grammar constructions.
      ["閒", { label: "like", jyutping: "haan4", syntax: "stative_predicate availability_state", note: "free / available; retained as the lexical stem in 得閒 and 得唔得閒." }],
      ["得閒", { label: "like", jyutping: "dak1 haan4", syntax: "lexicalized_stative_predicate availability_state", note: "free / available / having spare time." }],
      ["車", { label: "what", jyutping: "ce1", syntax: "vehicle_noun object_np head_noun", classifier_classes: ["vehicle"], note: "vehicle / car." }],
      ["心", { label: "what", jyutping: "sam1", syntax: "body_part_or_abstract_noun head_noun", note: "heart / mind; also the second syllable of 點心." }],
      ["點心", { label: "what", jyutping: "dim2 sam1", syntax: "food_noun object_np head_noun", note: "dim sum." }],
      ["住", { label: "doing", jyutping: "zyu6", syntax: "verb residence_verb_or_durative_marker_homograph", note: "residence verb by lexical default; durative/state marker in constructional or utterance-final contexts." }],
      ["仲", { label: "how", jyutping: "zung6", syntax: "additive_continuative_adverb", note: "still / also / in addition." }],
      ["位", { label: "measure_word", jyutping: "wai2", syntax: "classifier_person polite_person_classifier", note: "polite classifier for people." }],
      ["定係", { label: "func", jyutping: "ding6 hai6", syntax: "alternative_question_connector", note: "or; introduces the second alternative." }],
      ["定", { label: "func", jyutping: "ding6", syntax: "alternative_connector_or_decide_verb_homograph", note: "alternative connector by default; decide/fix before overt aspect in the bounded suite data." }],
      ["時", { label: "when", jyutping: "si4", syntax: "time_noun time_head", note: "time." }],
      ["地鐵", { label: "what", jyutping: "dei6 tit3", syntax: "transport_noun object_np head_noun", note: "MTR / metro." }],
      ["站", { label: "where", jyutping: "zaam6", syntax: "place_noun station_head", note: "station / stop." }],
      ["地鐵站", { label: "where", jyutping: "dei6 tit3 zaam6", syntax: "place_np transport_station", note: "MTR / metro station." }],
      ["句", { label: "measure_word", jyutping: "geoi3", syntax: "classifier_utterance sentence_classifier", note: "classifier for sentences and utterances." }],
      ["痛", { label: "like", jyutping: "tung3", syntax: "stative_predicate pain_state", note: "painful / hurt." }],
      ["星期", { label: "when", jyutping: "sing1 kei4", syntax: "time_noun week_expression", note: "week; combines with day numbers." }],
      ["晝", { label: "when", jyutping: "zau3", syntax: "time_head daypart_morpheme", note: "daytime; bound in 晏晝 / 下晝." }],
      ["下晝", { label: "when", jyutping: "haa6 zau3", syntax: "time_np daypart", note: "afternoon." }],
      ["晏晝", { label: "when", jyutping: "aan3 zau3", syntax: "time_np daypart", note: "noon / afternoon." }],
      ["呢排", { label: "when", jyutping: "ni1 paai4", syntax: "time_np recent_period", note: "recently / these days." }],
      ["排", { label: "when", jyutping: "paai4", syntax: "time_period_head", note: "period / stretch, including 呢排." }],
      ["面", { label: "where", jyutping: "min6", syntax: "location_surface_or_side_noun head_noun", note: "surface / side / face; location use depends on the compound." }],
      ["前面", { label: "where", jyutping: "cin4 min6", syntax: "place_np relative_location", note: "in front / ahead." }],
      ["後面", { label: "where", jyutping: "hau6 min6", syntax: "place_np relative_location", note: "behind / at the back." }],
      ["舒服", { label: "like", jyutping: "syu1 fuk6", syntax: "stative_predicate physical_condition", note: "comfortable / feeling well." }],
      ["工", { label: "what", jyutping: "gung1", syntax: "work_noun object_np head_noun", note: "work / job." }],
      ["口", { label: "what", jyutping: "hau2", syntax: "noun opening_or_mouth head_noun", note: "mouth / opening." }],
      ["入口", { label: "where", jyutping: "jap6 hau2", syntax: "place_np entrance", note: "entrance." }],
      ["出口", { label: "where", jyutping: "ceot1 hau2", syntax: "place_np exit", note: "exit." }],
      ["路口", { label: "where", jyutping: "lou6 hau2", syntax: "place_np road_junction", note: "road junction / intersection." }],
      ["辣", { label: "like", jyutping: "laat6", syntax: "stative_predicate taste_property", note: "spicy / hot." }],
      ["高興", { label: "like", jyutping: "gou1 hing3", syntax: "stative_predicate emotion_state", note: "happy / pleased." }],
      ["認識", { label: "doing", jyutping: "jing6 sik1", syntax: "verb cognition_social_acquaintance transitive_verb", note: "know / become acquainted with." }],
      ["訂", { label: "doing", jyutping: "ding6", syntax: "verb booking_ordering_verb transitive_verb", note: "book / reserve / order." }],
      ["讀", { label: "doing", jyutping: "duk6", syntax: "verb reading_pronunciation_verb transitive_verb", note: "read / pronounce." }],
      ["市", { label: "what", jyutping: "si5", syntax: "place_or_market_noun head_noun", note: "city / market; exact sense depends on compound." }],
      ["市中心", { label: "where", jyutping: "si5 zung1 sam1", syntax: "place_np city_center", note: "city centre." }],
      ["事", { label: "what", jyutping: "si6", syntax: "event_or_matter_noun head_noun", note: "matter / affair / thing." }],
      ["比", { label: "func", jyutping: "bei2", syntax: "comparison_marker coverb", note: "comparison marker 'than'; distinct from 畀 by orthography." }],
      ["搭", { label: "doing", jyutping: "daap3", syntax: "verb transport_use_verb transitive_verb", note: "take / ride (transport)." }],
      ["巴士", { label: "what", jyutping: "baa1 si2", syntax: "transport_noun object_np", note: "bus." }],
      ["號", { label: "func", jyutping: "hou6", syntax: "number_or_date_suffix", note: "number; date or ordinal-like suffix depending on context." }],
      ["救護車", { label: "what", jyutping: "gau3 wu6 ce1", syntax: "vehicle_noun emergency_vehicle object_np", note: "ambulance." }],
      ["埋單", { label: "doing", jyutping: "maai4 daan1", syntax: "verb restaurant_payment_formula", note: "ask for / settle the bill." }],
      ["電視", { label: "what", jyutping: "din6 si6", syntax: "media_noun object_np head_noun", note: "television / TV." }],
      ["普通話", { label: "what", jyutping: "pou2 tung1 waa2", syntax: "language_noun object_np", note: "Mandarin / Putonghua." }],
      ["女", { label: "who", jyutping: "neoi5", syntax: "person_noun female_person", note: "woman / daughter depending on context." }],
      ["馬路", { label: "where", jyutping: "maa5 lou6", syntax: "place_noun road", note: "road / street." }],
      ["飛", { label: "what", jyutping: "fei1", syntax: "ticket_noun colloquial_object_np", note: "ticket (colloquial)." }],
      ["後日", { label: "when", jyutping: "hau6 jat6", syntax: "time_np day_expression", note: "the day after tomorrow." }],
      ["原來", { label: "func", jyutping: "jyun4 loi4", syntax: "discourse_marker realization_marker", note: "as it turns out / originally." }],
      ["暈", { label: "like", jyutping: "wan4", syntax: "stative_or_change_state_predicate physical_condition", note: "faint / dizzy / unconscious." }],
      ["郁", { label: "doing", jyutping: "juk1", syntax: "verb movement_verb", note: "move." }],
      ["一共", { label: "how", jyutping: "jat1 gung6", syntax: "totality_adverb", note: "in total / altogether." }],
      ["開始", { label: "doing", jyutping: "hoi1 ci2", syntax: "verb inception_verb", note: "begin / start." }],
      ["散紙", { label: "what", jyutping: "saan2 zi2", syntax: "money_noun object_np", note: "loose change." }],
      ["碗", { label: "measure_word", jyutping: "wun2", syntax: "classifier_bowl container_classifier", note: "classifier for bowls / bowlfuls." }],
      ["報名", { label: "doing", jyutping: "bou3 meng2", syntax: "verb registration_verb", note: "register / sign up." }],
      ["半", { label: "how", jyutping: "bun3", syntax: "fraction_quantifier", note: "half." }],
      ["新聞", { label: "what", jyutping: "san1 man4", syntax: "information_noun news_noun", note: "news." }],
      ["報紙", { label: "what", jyutping: "bou3 zi2", syntax: "media_noun newspaper_noun", note: "newspaper." }],
      ["容易", { label: "like", jyutping: "jung4 ji6", syntax: "stative_predicate difficulty_scale", note: "easy." }],
      ["意思", { label: "what", jyutping: "ji3 si1", syntax: "abstract_noun meaning_or_intention", note: "meaning / intention." }],
      ["講法", { label: "what", jyutping: "gong2 faat3", syntax: "nominalized_expression wording_noun", note: "way of saying / wording." }],
      ["廣州", { label: "where", jyutping: "gwong2 zau1", syntax: "proper_place city_name", note: "Guangzhou." }],
      ["練", { label: "doing", jyutping: "lin6", syntax: "verb practice_verb transitive_verb", note: "practise / train." }],
      ["詩", { label: "what", jyutping: "si1", syntax: "text_noun pronunciation_drill_item", note: "poem / poetry." }],
      ["史", { label: "what", jyutping: "si2", syntax: "history_noun pronunciation_drill_item", note: "history." }],
      ["星", { label: "what", jyutping: "sing1", syntax: "celestial_noun pronunciation_drill_item", note: "star." }],
      ["巴", { label: "neutral", jyutping: "baa1", syntax: "pronunciation_drill_item lexical_character", note: "pronunciation-drill character 巴." }],
      ["怕", { label: "doing", jyutping: "paa3", syntax: "verb emotion_predicate fear_verb", note: "fear / be afraid." }],
      ["他", { label: "who", jyutping: "taa1", syntax: "written_register_pronoun pronunciation_drill_item", note: "written-register third-person pronoun; spoken Cantonese normally uses 佢." }],
      ["家", { label: "what", jyutping: "gaa1", syntax: "home_family_noun pronunciation_drill_item", note: "home / family." }],
      ["卡", { label: "what", jyutping: "kaa1", syntax: "card_noun pronunciation_drill_item", note: "card." }],
      ["渣", { label: "neutral", jyutping: "zaa1", syntax: "pronunciation_drill_item lexical_character", note: "pronunciation-drill character 渣." }],
      ["叉", { label: "what", jyutping: "caa1", syntax: "utensil_or_fork_noun pronunciation_drill_item", note: "fork / cross." }],
      ["牙", { label: "what", jyutping: "ngaa4", syntax: "body_part_noun pronunciation_drill_item", note: "tooth." }],
      ["門", { label: "what", jyutping: "mun4", syntax: "door_noun head_noun", note: "door / gate." }],
      ["清楚", { label: "like", jyutping: "cing1 co2", syntax: "result_or_stative_predicate clarity_state", note: "clear / clearly." }],
      ["質素", { label: "what", jyutping: "zat1 sou3", syntax: "quality_noun abstract_noun", note: "quality." }],
      ["轉", { label: "doing", jyutping: "zyun3", syntax: "verb direction_change_verb", note: "turn; lexical reading here is zyun3 for physical direction." }],
      ["左", { label: "where", jyutping: "zo2", syntax: "direction_noun_or_modifier", note: "left." }],
      ["右", { label: "where", jyutping: "jau6", syntax: "direction_noun_or_modifier", note: "right." }],
      ["毫", { label: "measure_word", jyutping: "hou4", syntax: "money_subunit dime_unit", note: "ten-cent monetary unit / hou." }],
      ["證件", { label: "what", jyutping: "zing3 gin6", syntax: "document_noun object_np", note: "identity or official document." }],
      ["噃", { label: "particle", jyutping: "bo3", syntax: "sentence_final_particle reminder_realization_particle", note: "sentence-final reminder / realization particle." }],
      ["塞車", { label: "doing", jyutping: "sak1 ce1", syntax: "traffic_event_predicate", note: "traffic jam / be stuck in traffic." }],
      ["救命", { label: "func", jyutping: "gau3 meng6", syntax: "emergency_formula", note: "help! / save me." }],
      ["交通意外", { label: "what", jyutping: "gaau1 tung1 ji3 ngoi6", syntax: "event_noun accident_event", note: "traffic accident." }],
      ["呼吸", { label: "doing", jyutping: "fu1 kap1", syntax: "verb_or_noun breathing_predicate", note: "breathe / breathing." }],
      ["火警", { label: "what", jyutping: "fo2 ging2", syntax: "emergency_event fire_alarm", note: "fire / fire alarm." }],
      ["壺", { label: "measure_word", jyutping: "wu4", syntax: "classifier_pot container_classifier", note: "classifier for pots / a pot." }],
      ["飽", { label: "like", jyutping: "baau2", syntax: "result_or_stative_predicate satiation_state", note: "full after eating." }],
      ["頭痛", { label: "like", jyutping: "tau4 tung3", syntax: "stative_predicate symptom", note: "headache / have a headache." }],
      ["發燒", { label: "doing", jyutping: "faat3 siu1", syntax: "health_state_predicate", note: "have a fever." }],
      ["嗰陣", { label: "when", jyutping: "go2 zan6", syntax: "time_np demonstrative_time", note: "at that time / then." }],
      ["陣", { label: "when", jyutping: "zan6", syntax: "time_or_event_classifier time_head", note: "period / spell / time." }],
      ["藥", { label: "what", jyutping: "joek6", syntax: "medicine_noun object_np", note: "medicine / drug." }],
      ["敏感", { label: "like", jyutping: "man5 gam2", syntax: "stative_predicate sensitivity_or_allergy", note: "sensitive / allergic." }],
      ["困難", { label: "like", jyutping: "kwan3 naan4", syntax: "stative_or_nominal difficulty_state", note: "difficult / difficulty." }],
      ["改", { label: "doing", jyutping: "goi2", syntax: "verb change_verb transitive_verb", note: "change / alter." }],
      ["碼", { label: "what", jyutping: "maa5", syntax: "size_or_code_noun head_noun", note: "size / code / number." }],
      ["色", { label: "what", jyutping: "sik1", syntax: "color_noun head_noun", note: "colour." }],
      ["行山", { label: "doing", jyutping: "haang4 saan1", syntax: "verb activity_predicate", note: "hike / go hiking." }],
      ["焗", { label: "like", jyutping: "guk6", syntax: "stative_predicate weather_property", note: "stuffy / muggy / oppressively hot." }],
      ["第", { label: "func", jyutping: "dai6", syntax: "ordinal_prefix", note: "ordinal prefix." }],
      ["歡迎", { label: "doing", jyutping: "fun1 jing4", syntax: "verb greeting_welcome_verb", note: "welcome." }],
      ["初次", { label: "when", jyutping: "co1 ci3", syntax: "temporal_adverb first_occurrence", note: "for the first time." }],
      ["酒店", { label: "where", jyutping: "zau2 dim3", syntax: "place_np hotel", note: "hotel." }],
      ["早餐", { label: "what", jyutping: "zou2 caan1", syntax: "meal_noun breakfast", note: "breakfast." }],
      ["換", { label: "doing", jyutping: "wun6", syntax: "verb exchange_change_verb transitive_verb", note: "exchange / change." }],
      ["港幣", { label: "what", jyutping: "gong2 bai6", syntax: "currency_noun money_np", note: "Hong Kong dollars / HK currency." }],
      ["匯率", { label: "what", jyutping: "wui6 leot6", syntax: "financial_noun exchange_rate", note: "exchange rate." }],
      ["美國", { label: "where", jyutping: "mei5 gwok3", syntax: "proper_place country_name", note: "United States." }],
      ["中國", { label: "where", jyutping: "zung1 gwok3", syntax: "proper_place country_name", note: "China." }],
      ["工作", { label: "what", jyutping: "gung1 zok3", syntax: "work_noun activity_noun", note: "work / job." }],
      ["附近", { label: "where", jyutping: "fu6 gan6", syntax: "relative_place_expression", note: "nearby / vicinity." }],
      ["銀行", { label: "where", jyutping: "ngan4 hong4", syntax: "place_or_institution_noun bank", note: "bank." }],
      ["先至", { label: "func", jyutping: "sin1 zi3", syntax: "sequence_focus_marker", note: "only then / not until." }],
      ["地方", { label: "where", jyutping: "dei6 fong1", syntax: "place_noun location_np", note: "place." }],
      ["戶口", { label: "what", jyutping: "wu6 hau2", syntax: "account_noun bank_account", note: "account." }],
      ["文件", { label: "what", jyutping: "man4 gin2", syntax: "document_noun object_np", note: "document / paperwork." }],
      ["簽", { label: "doing", jyutping: "cim1", syntax: "verb signing_verb transitive_verb", note: "sign." }],
      ["行程", { label: "what", jyutping: "hang4 cing4", syntax: "schedule_itinerary_noun", note: "itinerary / schedule." }],
      ["乾淨", { label: "like", jyutping: "gon1 zeng6", syntax: "stative_predicate cleanliness_property", note: "clean." }],
      ["垃圾", { label: "what", jyutping: "laap6 saap3", syntax: "waste_noun object_np", note: "rubbish / garbage." }],
      ["亂", { label: "how", jyutping: "lyun6", syntax: "manner_adverb disorderly_manner", note: "randomly / carelessly / in disorder." }],
      ["掉", { label: "doing", jyutping: "diu6", syntax: "verb disposal_verb", note: "throw away; standard-written item in the suite." }],
      ["學費", { label: "what", jyutping: "hok6 fai3", syntax: "money_noun tuition_fee", note: "tuition / school fees." }],
      ["運動", { label: "doing", jyutping: "wan6 dung6", syntax: "activity_noun_or_verb exercise_activity", note: "exercise / sport." }],
      ["鐘", { label: "when", jyutping: "zung1", syntax: "time_unit hour_unit", note: "hour / clock." }],
      ["提醒", { label: "doing", jyutping: "tai4 sing2", syntax: "verb reminder_verb transitive_verb", note: "remind." }],
      ["搵工", { label: "doing", jyutping: "wan2 gung1", syntax: "verb job_search_activity", note: "look for work." }],
      ["轉工", { label: "doing", jyutping: "zyun2 gung1", syntax: "verb job_change_activity", note: "change jobs." }],
      ["估", { label: "doing", jyutping: "gu2", syntax: "verb cognition_guess_verb", note: "guess / suppose." }],
      ["單", { label: "measure_word", jyutping: "daan1", syntax: "classifier_case_order_or_news_item", note: "classifier for cases, orders, or news items." }],
      ["乜", { label: "what", jyutping: "mat1", syntax: "wh_thing colloquial_wh", note: "what." }],
      ["犀利", { label: "like", jyutping: "sai1 lei6", syntax: "stative_predicate degree_evaluation", note: "formidable / intense / impressive." }],
      ["轉車", { label: "doing", jyutping: "zyun2 ce1", syntax: "verb transport_transfer_activity", note: "change vehicles / transfer." }],
      ["海洋公園", { label: "where", jyutping: "hoi2 joeng4 gung1 jyun2", syntax: "proper_place attraction_name", note: "Ocean Park." }],
      ["門票", { label: "what", jyutping: "mun4 piu3", syntax: "ticket_noun admission_ticket", note: "admission ticket." }],
      ["記住", { label: "doing", jyutping: "gei3 zyu6", syntax: "verb cognition_memory_imperative", note: "remember." }],
      ["黃大仙", { label: "where", jyutping: "wong4 daai6 sin1", syntax: "proper_place district_or_temple_name", note: "Wong Tai Sin." }],
      ["節目", { label: "what", jyutping: "zit3 muk6", syntax: "media_or_event_noun program", note: "programme / show." }],
      ["事關", { label: "func", jyutping: "si6 gwaan1", syntax: "cause_connector discourse_connector", note: "because / the reason is." }],
      ["碟", { label: "measure_word", jyutping: "dip6", syntax: "classifier_dish plate_classifier", note: "classifier for dishes / plates." }],
      ["假", { label: "like", jyutping: "gaa2", syntax: "stative_predicate authenticity_property", note: "fake / false." }],
      ["女人街", { label: "where", jyutping: "neoi5 jan2 gaai1", syntax: "proper_place street_name", note: "Ladies' Market / Tung Choi Street." }],
      ["公屋", { label: "where", jyutping: "gung1 uk1", syntax: "housing_place public_housing", note: "public housing." }],
      ["區", { label: "where", jyutping: "keoi1", syntax: "area_noun place_head", note: "district / area." }],
      ["租金", { label: "what", jyutping: "zou1 gam1", syntax: "money_noun rent", note: "rent." }],
      ["申請", { label: "doing", jyutping: "san1 cing2", syntax: "verb application_verb", note: "apply / application." }],
      ["生活費", { label: "what", jyutping: "sang1 wut6 fai3", syntax: "money_noun living_expenses", note: "living expenses." }],
      ["愈來愈", { label: "how", jyutping: "jyu6 loi4 jyu6", syntax: "degree_change_marker", note: "more and more." }],
      ["慳", { label: "doing", jyutping: "haan1", syntax: "verb economize_save_verb", note: "save / economize." }],
      ["搬", { label: "doing", jyutping: "bun1", syntax: "verb relocation_verb", note: "move / relocate." }],
      ["初初", { label: "when", jyutping: "co1 co1", syntax: "temporal_adverb initial_period", note: "at first / initially." }],
      ["習慣", { label: "like", jyutping: "zaap6 gwaan3", syntax: "stative_or_verb habituation_predicate", note: "used to / accustomed / habit." }],
      ["解釋", { label: "doing", jyutping: "gaai2 sik1", syntax: "verb explanation_verb transitive_verb", note: "explain." }],
      ["自然", { label: "like", jyutping: "zi6 jin4", syntax: "stative_predicate naturalness_property", note: "natural." }],
      ["發音", { label: "what", jyutping: "faat3 jam1", syntax: "pronunciation_noun_or_verb", note: "pronunciation / pronounce." }],
      ["聲調", { label: "what", jyutping: "sing1 diu6", syntax: "linguistic_noun tone", note: "tone." }],
      ["日常", { label: "how", jyutping: "jat6 soeng4", syntax: "nominal_modifier everyday_register", note: "everyday / daily." }],
      ["對話", { label: "what", jyutping: "deoi3 waa6", syntax: "speech_noun dialogue", note: "dialogue / conversation." }],
      ["肯定", { label: "like", jyutping: "hang2 ding6", syntax: "stance_predicate certainty", note: "certain / sure / affirm." }],
      ["差", { label: "doing", jyutping: "caa1", syntax: "verb subtraction_or_shortfall_marker", note: "lack / be short of; used in clock expressions." }],
      ["遲", { label: "like", jyutping: "ci4", syntax: "stative_predicate lateness_property", note: "late." }],
      ["肚餓", { label: "like", jyutping: "tou5 ngo6", syntax: "stative_predicate hunger_state", note: "hungry." }],
      ["口渴", { label: "like", jyutping: "hau2 hot3", syntax: "stative_predicate thirst_state", note: "thirsty." }],
      ["銀包", { label: "what", jyutping: "ngan4 baau1", syntax: "object_np wallet_noun", note: "wallet / purse." }],
      ["分別", { label: "what", jyutping: "fan1 bit6", syntax: "abstract_noun difference", note: "difference." }],
      ["Chris", { label: "who", syntax: "latin_proper_name person_np", note: "Latin-script personal name; no Jyutping applicable." }],
      ["Crawford", { label: "who", syntax: "latin_proper_name surname_np", note: "Latin-script surname; no Jyutping applicable." }],
      ["雲吞麵", { label: "what", jyutping: "wan4 tan1 min6", syntax: "food_noun object_np", note: "wonton noodles." }],
      ["咪", { label: "func", syntax: "polysemous_mai_marker", note: "咪 is context-sensitive: prohibitive mai5 before a predicate, but mai6 in several discourse/focus uses." }],
      ["客氣", { label: "like", jyutping: "haak3 hei3", syntax: "stative_or_behavior_predicate politeness_property degree_manner_head", note: "polite / ceremonious; degreeable before 啲 in a scalar adjustment phrase." }],
      ["姓", { label: "doing", jyutping: "sing3", syntax: "verb surname_relation_verb", note: "have the surname / be surnamed." }],
      ["跟住", { label: "func", jyutping: "gan1 zyu6", syntax: "sequence_connector discourse_marker", note: "then / next / following that." }],
      ["麻麻地", { label: "like", jyutping: "maa4 maa2 dei2", syntax: "lexicalized_stative_predicate moderate_evaluation", note: "so-so / mediocre." }],
      ["找", { label: "doing", jyutping: "zaau2", syntax: "verb change_money_verb written_or_regional_form", note: "give change / make change in 找錢; distinct from 搵 'look for'." }],
      ["旺角", { label: "where", jyutping: "wong6 gok3", syntax: "proper_place district_name", note: "Mong Kok." }],
      ["報警", { label: "doing", jyutping: "bou3 ging2", syntax: "verb emergency_report_verb", note: "call / report to the police." }],
      ["受傷", { label: "like", jyutping: "sau6 soeng1", syntax: "result_state_predicate injury_state", note: "injured / sustain an injury." }],
      ["地址", { label: "where", jyutping: "dei6 zi2", syntax: "location_reference_noun address", note: "address." }],
      ["自己", { label: "who", jyutping: "zi6 gei2", syntax: "reflexive_pronoun person_np", note: "self / oneself." }],
      ["籠", { label: "measure_word", jyutping: "lung4", syntax: "classifier_bamboo_steamer container_classifier", note: "classifier for bamboo steamers / baskets." }],
      ["蝦餃", { label: "what", jyutping: "haa1 gaau2", syntax: "food_noun dim_sum_item", note: "shrimp dumpling / har gow." }],
      ["斤", { label: "measure_word", jyutping: "gan1", syntax: "weight_unit classifier_weight", note: "catty / jin, a weight measure." }],
      ["新鮮", { label: "like", jyutping: "san1 sin1", syntax: "stative_predicate freshness_property", note: "fresh." }],
      ["傾", { label: "doing", jyutping: "king1", syntax: "verb conversation_verb", note: "chat / talk." }],
      ["把", { label: "measure_word", jyutping: "baa2", syntax: "classifier_handled_object", note: "classifier for handled objects such as umbrellas." }],
      ["遮", { label: "what", jyutping: "ze1", syntax: "object_np umbrella_noun", note: "umbrella." }],
      ["塞", { label: "doing", jyutping: "sak1", syntax: "verb blockage_or_traffic_jam_predicate", note: "block / be congested." }],
      ["吳", { label: "who", jyutping: "ng4", syntax: "surname_element proper_name_part", note: "surname Ng / Wu." }],
      ["飯前", { label: "when", jyutping: "faan6 cin4", syntax: "time_np meal_relative_time", note: "before a meal." }],
      ["飯後", { label: "when", jyutping: "faan6 hau6", syntax: "time_np meal_relative_time", note: "after a meal." }],
      ["藍色", { label: "like", jyutping: "laam4 sik1", syntax: "color_modifier stative_predicate degreeable", note: "blue / blue-colored." }],
      ["……", { label: "neutral", syntax: "ellipsis_punctuation", note: "ellipsis punctuation; no Jyutping applicable." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/lexical-coverage-additions.js
var require_lexical_coverage_additions = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/lexical-coverage-additions.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      ["刻", {
        label: "when",
        jyutping: "hak1",
        syntax: "time_head temporal nominal_measure_unit",
        note: "moment / quarter-hour time unit; independently attested by 粵典 (words.hk) and 粵音資料集叢, added because the reviewed lexicalized surface 一刻 exposed a missing component lexeme."
      }],
      ["遇", {
        label: "doing",
        jyutping: "jyu6",
        syntax: "verb transitive_affordance encounter_verb",
        note: "meet / encounter; independently attested Cantonese verbal root, added because the reviewed lexicalized surface 遇到 exposed a missing component lexeme."
      }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/frequency-gap-fill-r7.js
var require_frequency_gap_fill_r7 = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/frequency-gap-fill-r7.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      ["哋", { label: "lex", pos: "lexical_item", jyutping: "dei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 25, frequency 2116; prior audit status missing. plural suffix. Source readings: dei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["即", { label: "lex", pos: "lexical_item", jyutping: "zik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 27, frequency 1930; prior audit status missing. namely/that is/i.e./prompt/at once/at present/even if/prompted (by the occasion)/to approach/to come into contact/to assume (office)/to draw near. Source readings: zik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一個", { label: "lex", pos: "lexical_item", jyutping: "jat1 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 38, frequency 1246; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["喀", { label: "lex", pos: "lexical_item", jyutping: "kak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 39, frequency 1219; prior audit status missing. (onomat.). Source readings: kak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["真係", { label: "lex", pos: "lexical_item", jyutping: "zan1 hai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 56, frequency 872; prior audit status handled_structurally. really. Source readings: zan1hai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["誒", { label: "lex", pos: "lexical_item", jyutping: "aai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 58, frequency 817; prior audit status missing. hey. Source readings: aai2|ei6|oi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["哦", { label: "lex", pos: "lexical_item", jyutping: "ngo4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 69, frequency 710; prior audit status missing. oh (叹词, interjection)/to chant. Source readings: ngo4|o4|o6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["咁樣", { label: "lex", pos: "lexical_item", jyutping: "gam2 joeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 72, frequency 703; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: gam2joeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["噉樣", { label: "lex", pos: "lexical_item", jyutping: "gam2 joeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 73, frequency 681; prior audit status handled_structurally. this way. Source readings: gam2joeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兜", { label: "lex", pos: "lexical_item", jyutping: "dau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 78, frequency 647; prior audit status missing. container for rice, pet food or coins/to move in a circle/to canvas or solicit/to go for a (leisurely) stroll, drive, jaunt. Source readings: dau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["來", { label: "lex", pos: "lexical_item", jyutping: "loi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 90, frequency 491; prior audit status missing. to come/to arrive/to come round/ever since/next. Source readings: loi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["然", { label: "lex", pos: "lexical_item", jyutping: "jin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 91, frequency 483; prior audit status missing. correct/right/so/thus/like this/-ly. Source readings: jin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘛", { label: "lex", pos: "lexical_item", jyutping: "maa4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 109, frequency 402; prior audit status missing. (a modal particle). Source readings: maa4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嗯", { label: "lex", pos: "lexical_item", jyutping: "ng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 116, frequency 370; prior audit status missing. (a groaning sound)/(nonverbal grunt as interjection)/OK, yeah/what?/interjection indicating approval, appreciation or agreement. Source readings: ng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["哩", { label: "lex", pos: "lexical_item", jyutping: "lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 120, frequency 342; prior audit status missing. (modal final particle sim. to 呢 or 啦)/(onomat.)/see 哩哩羅羅|哩哩罗罗 lililuoluo endless mumbling noise/see 哩哩啦啦 lililala, scattered or intermittent/mile/old form of modern 英里. Source readings: lei5|le1|lei1|li1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["落去", { label: "lex", pos: "lexical_item", jyutping: "lok6 heoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 121, frequency 329; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: lok6heoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["方", { label: "lex", pos: "lexical_item", jyutping: "fong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 125, frequency 322; prior audit status missing. square/quadrilateral/power (such as cube 立方)/classifier for square things/upright/honest/fair and square/surname Fang/direction/party (to a dispute)/one side/place/method/prescription/just/then/only then. Source readings: fong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘩", { label: "lex", pos: "lexical_item", jyutping: "waa1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 136, frequency 298; prior audit status missing. cat-calling sound/clamor/noise/crashing sound. Source readings: waa1|waa4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["穿過", { label: "lex", pos: "lexical_item", jyutping: "cyun1 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 137, frequency 297; prior audit status missing. to pass through. Source readings: cyun1gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩個", { label: "lex", pos: "lexical_item", jyutping: "loeng5 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 139, frequency 291; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *loeng5go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個人", { label: "lex", pos: "lexical_item", jyutping: "go3 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 140, frequency 284; prior audit status handled_structurally. individual/personal/oneself. Source readings: go3jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["上去", { label: "lex", pos: "lexical_item", jyutping: "soeng5 heoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 144, frequency 270; prior audit status handled_structurally. to go up. Source readings: soeng5heoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["揾", { label: "lex", pos: "lexical_item", jyutping: "", syntax: "lexical_item", note: "Cifu SpokenAdult rank 149, frequency 255; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *?. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["哩個", { label: "lex", pos: "lexical_item", jyutping: "lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 151, frequency 254; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lei5|le1|lei1|li1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有個", { label: "lex", pos: "lexical_item", jyutping: "jau5 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 156, frequency 245; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau5go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔到", { label: "lex", pos: "lexical_item", jyutping: "m4 dou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 157, frequency 244; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *m4dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["埋", { label: "lex", pos: "lexical_item", jyutping: "maai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 162, frequency 234; prior audit status missing. bury/to blame. Source readings: maai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["裏", { label: "lex", pos: "lexical_item", jyutping: "leoi5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 174, frequency 201; prior audit status missing. inside/internal/interior/lining/village/basic unit of city administration. Source readings: leoi5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["過去", { label: "lex", pos: "lexical_item", jyutping: "gwo3 heoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 185, frequency 185; prior audit status handled_structurally. (in the) past/former/previous/to go over/to pass by. Source readings: gwo3heoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["而", { label: "lex", pos: "lexical_item", jyutping: "ji4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 190, frequency 179; prior audit status missing. and/as well as/and so/but (not)/yet (not)/(indicates causal relation)/(indicates change of state)/(indicates contrast). Source readings: ji4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一樣", { label: "lex", pos: "lexical_item", jyutping: "jat1 joeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 192, frequency 177; prior audit status handled_structurally. same/like/equal to/the same as/just like. Source readings: jat1joeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["第一", { label: "lex", pos: "lexical_item", jyutping: "dai6 jat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 200, frequency 170; prior audit status handled_structurally. first/number one. Source readings: dai6jat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["場", { label: "lex", pos: "lexical_item", jyutping: "coeng4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 211, frequency 162; prior audit status missing. large place used for a specific purpose/stage/scene (of a play)/classifier for sporting or recreational activities/classifier for number of exams/classifier for events and happenings: spell, episode, bout. Source readings: coeng4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["台", { label: "lex", pos: "lexical_item", jyutping: "toi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 212, frequency 161; prior audit status missing. (classical) you (in letters)/platform/Taiwan (abbr.)/a surname. Source readings: toi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["番", { label: "lex", pos: "lexical_item", jyutping: "faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 214, frequency 159; prior audit status missing. foreign/ethnic groups from outside China/(when used after a verb) times or fold/classifier for the number of iterations of an action or deed etc. Source readings: faan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔見", { label: "lex", pos: "lexical_item", jyutping: "m4 gin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 217, frequency 157; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *m4gin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兜過", { label: "lex", pos: "lexical_item", jyutping: "dau1 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 219, frequency 157; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dau1gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個位", { label: "lex", pos: "lexical_item", jyutping: "go3 wai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 221, frequency 156; prior audit status handled_structurally. the units place (or column) in the decimal system. Source readings: go3wai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["比較", { label: "lex", pos: "lexical_item", jyutping: "bei2 gaau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 222, frequency 155; prior audit status missing. compare/contrast/fairly/comparatively/relatively/quite/rather. Source readings: bei2gaau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["冇乜", { label: "lex", pos: "lexical_item", jyutping: "mou5 mat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 223, frequency 154; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *mou5mat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["死", { label: "lex", pos: "lexical_item", jyutping: "sei2", syntax: "lexical_item", note: 'Cifu SpokenAdult rank 229, frequency 153; prior audit status missing. to die/impassable/uncrossable/inflexible/rigid/extremely/used to stress the meaning of a word or phrase/used to emphasise negative characteristics, "shitty", "damned", "useless". Source readings: sei2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.' }],
      ["出嚟", { label: "lex", pos: "lexical_item", jyutping: "ceot1 lai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 233, frequency 151; prior audit status handled_structurally. to come out. Source readings: ceot1lai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["返去", { label: "lex", pos: "lexical_item", jyutping: "faan1 heoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 234, frequency 150; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: faan1heoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["戀", { label: "lex", pos: "lexical_item", jyutping: "lyun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 239, frequency 149; prior audit status missing. to feel attached to/long for/love. Source readings: lyun2|lyun5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["都會", { label: "lex", pos: "lexical_item", jyutping: "dou1 wui5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 241, frequency 147; prior audit status handled_structurally. society/community/city/metropolis. Source readings: dou1wui5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右面", { label: "lex", pos: "lexical_item", jyutping: "jau6 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 242, frequency 146; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: jau6min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左面", { label: "lex", pos: "lexical_item", jyutping: "zo2 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 245, frequency 145; prior audit status handled_structurally. left side. Source readings: zo2min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成", { label: "lex", pos: "lexical_item", jyutping: "sing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 247, frequency 144; prior audit status missing. to succeed/to finish/to complete/to accomplish/to become/to turn into/one tenth|whole/complete/to be all right/OK!. Source readings: sing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾多", { label: "lex", pos: "lexical_item", jyutping: "gei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 249, frequency 144; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gei2|gei1do1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["啱啱", { label: "lex", pos: "lexical_item", jyutping: "ngaam1 ngaam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 256, frequency 141; prior audit status handled_structurally. just now. Source readings: ngaam1ngaam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唉", { label: "lex", pos: "lexical_item", jyutping: "aai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 257, frequency 140; prior audit status missing. alas/oh dear/interjection or grunt of agreement or recognition (e.g. yes, it's me!)/to sigh. Source readings: aai1|oi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["哎", { label: "lex", pos: "lexical_item", jyutping: "aai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 259, frequency 138; prior audit status missing. hey!/interjection used to attract attention or to express surprise or disapprobation. Source readings: aai1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["擰", { label: "lex", pos: "lexical_item", jyutping: "ning4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 263, frequency 133; prior audit status missing. mistake/to twist/stubborn/to pinch/wring. Source readings: ning4|ning6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["丸", { label: "lex", pos: "lexical_item", jyutping: "jyun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 265, frequency 131; prior audit status missing. pill. Source readings: jyun2|jyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["隔離", { label: "lex", pos: "lexical_item", jyutping: "gaak3 lei4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 268, frequency 129; prior audit status missing. to separate/to isolate. Source readings: gaak3lei4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["往", { label: "lex", pos: "lexical_item", jyutping: "wong5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 274, frequency 126; prior audit status missing. to go (in a direction)/to/towards/(of a train) bound for/past/previous. Source readings: wong5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["淨係", { label: "lex", pos: "lexical_item", jyutping: "zing6 hai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 275, frequency 125; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zing6hai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["底", { label: "lex", pos: "lexical_item", jyutping: "dai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 278, frequency 123; prior audit status missing. background/bottom/base/plan, strategy or secret/the end of a period of time/towards the end of (last month). Source readings: dai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["為", { label: "lex", pos: "lexical_item", jyutping: "wai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 281, frequency 121; prior audit status missing. as (in the capacity of)/to take sth as/to act as/to serve as/to behave as/to become/to be/to do/because of/for/to. Source readings: wai6|wai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["車站", { label: "lex", pos: "lexical_item", jyutping: "ce1 zaam6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 283, frequency 120; prior audit status handled_structurally. rail station/bus stop. Source readings: ce1zaam6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["係噉", { label: "lex", pos: "lexical_item", jyutping: "hai6 gam2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 285, frequency 119; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hai6gam2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["差唔多", { label: "lex", pos: "lexical_item", jyutping: "caa1 m4 do1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 286, frequency 119; prior audit status handled_structurally. similar. Source readings: caa1m4do1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["魚排", { label: "lex", pos: "lexical_item", jyutping: "jyu4 paai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 288, frequency 118; prior audit status handled_structurally. fish steak. Source readings: jyu4paai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔同", { label: "lex", pos: "lexical_item", jyutping: "m4 tung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 296, frequency 114; prior audit status handled_structurally. different. Source readings: m4tung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["條線", { label: "lex", pos: "lexical_item", jyutping: "tiu4 sin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 297, frequency 114; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tiu4sin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["會見", { label: "lex", pos: "lexical_item", jyutping: "wui6 gin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 298, frequency 114; prior audit status handled_structurally. to meet with (sb who is paying a visit). Source readings: wui6gin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["吔", { label: "lex", pos: "lexical_item", jyutping: "jaa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 307, frequency 108; prior audit status missing. particle. Source readings: jaa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["等等", { label: "lex", pos: "lexical_item", jyutping: "dang2 dang2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 311, frequency 106; prior audit status handled_structurally. etcetera/and so on ../wait a minute!/hold on!. Source readings: dang2dang2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["盛", { label: "lex", pos: "lexical_item", jyutping: "sing6", syntax: "lexical_item", note: 'Cifu SpokenAdult rank 318, frequency 104; prior audit status missing. flourishing/vigorous/magnificent/extensively/to hold/contain/to ladle/pick up with a utensil/used to indicate a further series of events not specified, "and so on", "etcetera"/a surname. Source readings: sing6|sing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.' }],
      ["燕", { label: "lex", pos: "lexical_item", jyutping: "jin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 319, frequency 104; prior audit status missing. Yan, a vassal state of Zhou in modern Hebei and Liaoning/north Hebei/the four Yan kingdoms of the Sixteen Kingdoms, namely: Former Yan 前燕 (337-370), Later Yan 後燕|后燕 (384-409), Southern Yan 南燕 (398-410), Northern Yan 北燕 (. Source readings: jin3|jin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["同學", { label: "lex", pos: "lexical_item", jyutping: "tung4 hok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 322, frequency 102; prior audit status handled_structurally. (fellow) classmate. Source readings: tung4hok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["只", { label: "lex", pos: "lexical_item", jyutping: "zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 328, frequency 100; prior audit status missing. only/merely/just/but. Source readings: zi2|zek3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["可", { label: "lex", pos: "lexical_item", jyutping: "ho2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 329, frequency 100; prior audit status missing. can/may/able to/to approve/to permit/certain(ly)/to suit/(particle used for emphasis). Source readings: ho2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有時", { label: "lex", pos: "lexical_item", jyutping: "jau5 si4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 330, frequency 100; prior audit status handled_structurally. sometimes/now and then. Source readings: jau5si4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["仲有", { label: "lex", pos: "lexical_item", jyutping: "zung6 jau5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 332, frequency 99; prior audit status handled_structurally. furthermore. Source readings: zung6jau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唧", { label: "lex", pos: "lexical_item", jyutping: "zik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 333, frequency 99; prior audit status missing. 1. to squeeze/2. to tickle. Source readings: zik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["大樹", { label: "lex", pos: "lexical_item", jyutping: "daai6 syu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 336, frequency 98; prior audit status handled_structurally. tree. Source readings: daai6syu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["老", { label: "lex", pos: "lexical_item", jyutping: "lou5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 337, frequency 98; prior audit status missing. prefix used before the surname of a person or a numeral indicating the order of birth of the children in a family or to indicate affection or familiarity/old (of people)/venerable (person)/experienced/of long standing/al. Source readings: lou5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["依", { label: "lex", pos: "lexical_item", jyutping: "ji1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 338, frequency 98; prior audit status missing. according to/depend on/near to. Source readings: ji1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["咦", { label: "lex", pos: "lexical_item", jyutping: "ji4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 339, frequency 98; prior audit status missing. expression of surprise. Source readings: ji4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["扮", { label: "lex", pos: "lexical_item", jyutping: "baan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 343, frequency 95; prior audit status missing. to disguise oneself/to dress up/adorn. Source readings: baan6|baan3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["擁", { label: "lex", pos: "lexical_item", jyutping: "jung2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 346, frequency 95; prior audit status missing. to hold/to embrace/to wrap around/to gather around (sb)/to throng/to swarm/to support. Source readings: jung2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["本身", { label: "lex", pos: "lexical_item", jyutping: "bun2 san1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 347, frequency 93; prior audit status missing. itself/in itself/per se. Source readings: bun2san1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["另", { label: "lex", pos: "lexical_item", jyutping: "ling6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 349, frequency 92; prior audit status missing. other/another/separate/separately. Source readings: ling6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["扮汗", { label: "lex", pos: "lexical_item", jyutping: "baan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 350, frequency 92; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *baan6|baan3hon6|hon4|hong6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["今次", { label: "lex", pos: "lexical_item", jyutping: "gam1 ci3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 353, frequency 91; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gam1ci3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾好", { label: "lex", pos: "lexical_item", jyutping: "gei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 360, frequency 89; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gei2|gei1hou2|hou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["彭", { label: "lex", pos: "lexical_item", jyutping: "pang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 361, frequency 89; prior audit status missing. surname Peng. Source readings: pang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["貧大宅", { label: "lex", pos: "lexical_item", jyutping: "pan4 daai6 zaak2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 364, frequency 88; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *pan4daai6zaak2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["像", { label: "lex", pos: "lexical_item", jyutping: "zoeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 369, frequency 87; prior audit status missing. (look) like/similar (to)/appearance/to appear/to seem/image/portrait/resemble/seem. Source readings: zoeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["仔", { label: "lex", pos: "lexical_item", jyutping: "zai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 372, frequency 85; prior audit status missing. a son/a suffix used to describe something small/a boy/a kid/a boyfriend/(of domestic animals or fowls) young. Source readings: zai2|zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["風車", { label: "lex", pos: "lexical_item", jyutping: "fung1 ce1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 376, frequency 84; prior audit status handled_structurally. pinwheel/windmill. Source readings: fung1ce1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["越", { label: "lex", pos: "lexical_item", jyutping: "jyut6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 377, frequency 84; prior audit status missing. generic word for peoples or states of south China or south Asia at different historical periods/abbr. for Vietnam 越南/to exceed/to climb over/to surpass/the more... the more. Source readings: jyut6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["考", { label: "lex", pos: "lexical_item", jyutping: "haau2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 379, frequency 83; prior audit status missing. to check/to verify/to test/to examine/to take an exam. Source readings: haau2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["引", { label: "lex", pos: "lexical_item", jyutping: "jan5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 386, frequency 82; prior audit status missing. to draw (a bow)/to pull/to stretch sth/to extend/to lengthen/to involve in/to attract/to lead/to guide/to divert (water)/unit of distance equal to 10 丈[zhang1], now one-thirtieth km or 33.33 meters. Source readings: jan5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["前", { label: "lex", pos: "lexical_item", jyutping: "cin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 389, frequency 82; prior audit status missing. front/forward/ahead/ago/before/first/former/formerly/future/BC (e.g. 前293年). Source readings: cin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["完全", { label: "lex", pos: "lexical_item", jyutping: "jyun4 cyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 394, frequency 80; prior audit status handled_structurally. complete/whole/totally/entirely. Source readings: jyun4cyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["股", { label: "lex", pos: "lexical_item", jyutping: "gu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 402, frequency 78; prior audit status missing. share/portion/section/part/thigh/(classifier for smells, electric currents, spirals etc)/whiff. Source readings: gu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["出來", { label: "lex", pos: "lexical_item", jyutping: "ceot1 loi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 404, frequency 77; prior audit status missing. to come out/to emerge. Source readings: ceot1loi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["你講", { label: "lex", pos: "lexical_item", jyutping: "nei5 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 406, frequency 76; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *nei5gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["所", { label: "lex", pos: "lexical_item", jyutping: "so2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 410, frequency 75; prior audit status missing. actually/place/classifier for houses, small buildings, institutions etc/that which/particle introducing a relative clause or passive. Source readings: so2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["所謂", { label: "lex", pos: "lexical_item", jyutping: "so2 wai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 411, frequency 75; prior audit status missing. so-called. Source readings: so2wai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["首先", { label: "lex", pos: "lexical_item", jyutping: "sau2 sin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 412, frequency 75; prior audit status handled_structurally. first (of all)/in the first place. Source readings: sau2sin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["畫到", { label: "lex", pos: "lexical_item", jyutping: "waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 415, frequency 75; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *waak6|waa2dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講呢", { label: "lex", pos: "lexical_item", jyutping: "gong2 ne1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 417, frequency 75; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2ne1|ni1|nei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["全部", { label: "lex", pos: "lexical_item", jyutping: "cyun4 bou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 419, frequency 74; prior audit status handled_structurally. whole/entire/complete. Source readings: cyun4bou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["服務", { label: "lex", pos: "lexical_item", jyutping: "fuk6 mou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 423, frequency 73; prior audit status missing. to serve/service. Source readings: fuk6mou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["得到", { label: "lex", pos: "lexical_item", jyutping: "dak1 dou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 428, frequency 72; prior audit status handled_structurally. to get/to obtain/to receive. Source readings: dak1dou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["理", { label: "lex", pos: "lexical_item", jyutping: "lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 430, frequency 72; prior audit status missing. texture/grain (of wood)/inner essence/intrinsic order/reason/logic/truth/science/natural science (esp. physics)/to manage/to pay attention to/to run (affairs)/to handle/to put in order/to tidy up. Source readings: lei5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["種", { label: "lex", pos: "lexical_item", jyutping: "zung2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 431, frequency 72; prior audit status missing. abbr. for 物種|物种, genus/race/seed/breed/species/strain/kind/type/has guts (i.e. courage)/nerve/classifier for types: kind, sort/classifier for languages/to plant/to grow/to cultivate. Source readings: zung2|zung3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["堂", { label: "lex", pos: "lexical_item", jyutping: "tong4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 432, frequency 71; prior audit status missing. (main) hall/large room for a specific purpose/relationship between cousins etc on the paternal side of a family/of the same clan/classifier for sets (or suites) of furniture, classes etc. Source readings: tong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["后", { label: "lex", pos: "lexical_item", jyutping: "hau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 433, frequency 70; prior audit status missing. empress/queen/surname Hou. Source readings: hau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["呃", { label: "lex", pos: "lexical_item", jyutping: "ngaak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 434, frequency 70; prior audit status missing. to cheat/to trick. Source readings: ngaak1|aak1|ak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["就算", { label: "lex", pos: "lexical_item", jyutping: "zau6 syun3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 435, frequency 70; prior audit status handled_structurally. granted that/even if. Source readings: zau6syun3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["出去", { label: "lex", pos: "lexical_item", jyutping: "ceot1 heoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 439, frequency 69; prior audit status handled_structurally. to go out. Source readings: ceot1heoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["怨", { label: "lex", pos: "lexical_item", jyutping: "jyun3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 440, frequency 69; prior audit status missing. to blame/to complain. Source readings: jyun3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔記得", { label: "lex", pos: "lexical_item", jyutping: "m4 gei3 dak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 441, frequency 69; prior audit status handled_structurally. to forget. Source readings: m4gei3dak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["起", { label: "lex", pos: "lexical_item", jyutping: "hei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 443, frequency 69; prior audit status missing. to rise/to raise/to get up/to set out/to start/to appear/to launch/to initiate (action)/to draft/to establish/to get (from a depot or counter)/verb suffix, to start/(before place or time) starting from/classifier for occ. Source readings: hei2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["打橫", { label: "lex", pos: "lexical_item", jyutping: "daa2 waang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 446, frequency 68; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: daa2waang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["行到", { label: "lex", pos: "lexical_item", jyutping: "hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 450, frequency 67; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hang4|haang4|hang6|hong4|hong2dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["杏堂", { label: "lex", pos: "lexical_item", jyutping: "hang6 tong4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 451, frequency 67; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hang6tong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["揀", { label: "lex", pos: "lexical_item", jyutping: "gaan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 453, frequency 67; prior audit status missing. to choose/to pick/to sort out/to pick up. Source readings: gaan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["項", { label: "lex", pos: "lexical_item", jyutping: "hong6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 454, frequency 67; prior audit status missing. back of neck/item/thing/term (in a mathematical formula)/sum (of money)/classifier for principles, items, clauses, tasks, research projects etc/surname Xiang. Source readings: hong6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我講", { label: "lex", pos: "lexical_item", jyutping: "ngo5 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 457, frequency 66; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["尚健", { label: "lex", pos: "lexical_item", jyutping: "soeng6 gin6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 458, frequency 66; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *soeng6gin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嶺", { label: "lex", pos: "lexical_item", jyutping: "leng5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 462, frequency 65; prior audit status missing. mountain range. Source readings: leng5|ling5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["入去", { label: "lex", pos: "lexical_item", jyutping: "jap6 heoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 463, frequency 64; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: jap6heoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["之", { label: "lex", pos: "lexical_item", jyutping: "zi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 464, frequency 64; prior audit status missing. (possessive particle, literary equivalent of 的)/him/her/it. Source readings: zi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["出現", { label: "lex", pos: "lexical_item", jyutping: "ceot1 jin6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 466, frequency 64; prior audit status missing. to appear/to arise/to emerge. Source readings: ceot1jin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["直情", { label: "lex", pos: "lexical_item", jyutping: "zik6 cing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 467, frequency 64; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zik6cing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["掂", { label: "lex", pos: "lexical_item", jyutping: "dim1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 471, frequency 63; prior audit status missing. to weigh in the hand/to estimate|satisfactory/okay. Source readings: dim1|dim6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["條路線", { label: "lex", pos: "lexical_item", jyutping: "tiu4 lou6 sin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 472, frequency 63; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tiu4lou6sin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["頂", { label: "lex", pos: "lexical_item", jyutping: "ding2", syntax: "lexical_item", note: 'Cifu SpokenAdult rank 473, frequency 63; prior audit status missing. apex/top/roof/to carry on the head/to push to the top/to go against/most/to replace/to substitute/to "bump" a forum thread to raise its profile (internet slang)/to bear, to endure, to put up with/an exclamation expressin. Source readings: ding2|deng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.' }],
      ["裏邊", { label: "lex", pos: "lexical_item", jyutping: "leoi5 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 474, frequency 63; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *leoi5bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右行", { label: "lex", pos: "lexical_item", jyutping: "jau6 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 479, frequency 62; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau6hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["讀書", { label: "lex", pos: "lexical_item", jyutping: "duk6 syu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 480, frequency 62; prior audit status handled_structurally. study/read. Source readings: duk6syu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["不", { label: "lex", pos: "lexical_item", jyutping: "bat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 481, frequency 61; prior audit status missing. not/no. Source readings: bat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["收", { label: "lex", pos: "lexical_item", jyutping: "sau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 485, frequency 61; prior audit status missing. to receive/to accept/to collect/in care of (used on address line after name). Source readings: sau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["愁", { label: "lex", pos: "lexical_item", jyutping: "sau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 489, frequency 61; prior audit status missing. worry about. Source readings: sau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一條", { label: "lex", pos: "lexical_item", jyutping: "jat1 tiu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 490, frequency 60; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1tiu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["忍", { label: "lex", pos: "lexical_item", jyutping: "jan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 492, frequency 60; prior audit status missing. to endure/to tolerate. Source readings: jan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["重", { label: "lex", pos: "lexical_item", jyutping: "zung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 493, frequency 60; prior audit status missing. to attach importance to/important/weighty/furthermore/further/more/still/to lay stress on/discreet|repeat/again/double|heavy (in weight)/deep/heavy/serious/weight. Source readings: zung6|cung4|cung5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["望", { label: "lex", pos: "lexical_item", jyutping: "mong6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 496, frequency 60; prior audit status missing. to look at/to view/to hope/to expect/to visit/to gaze (into the distance)/to look towards/towards/full moon. Source readings: mong6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["提碑", { label: "lex", pos: "lexical_item", jyutping: "tai4 bei1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 497, frequency 60; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tai4bei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["共", { label: "lex", pos: "lexical_item", jyutping: "gung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 500, frequency 59; prior audit status missing. common/general/to share/together/total/altogether/abbr. for 共產黨|共产党[gong4 chan3 dang3], Communist party. Source readings: gung6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["地", { label: "lex", pos: "lexical_item", jyutping: "dei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 501, frequency 59; prior audit status missing. earth/ground/field/place/land/CL:片[pin](Mandarin)/CL:塊[faai]/CL:笪[daat]/-ly/structural particle: used before a verb or adjective, linking it to preceding modifying adverbial adjunct. Source readings: dei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["憂", { label: "lex", pos: "lexical_item", jyutping: "jau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 505, frequency 59; prior audit status missing. to worry/to concern oneself with/worried/anxiety/sorrow/a parent's funeral/inconvenienced by being orphaned. Source readings: jau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["加", { label: "lex", pos: "lexical_item", jyutping: "gaa1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 507, frequency 58; prior audit status missing. to add/plus/abbr. for Canada 加拿大. Source readings: gaa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["受", { label: "lex", pos: "lexical_item", jyutping: "sau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 508, frequency 58; prior audit status missing. to receive/to accept/to suffer/subjected to/to bear/to stand/pleasant/(passive marker). Source readings: sau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["能夠", { label: "lex", pos: "lexical_item", jyutping: "nang4 gau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 510, frequency 58; prior audit status missing. be capable of/can/is able. Source readings: nang4gau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["領", { label: "lex", pos: "lexical_item", jyutping: "ling5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 513, frequency 58; prior audit status missing. neck/collar/to lead/to receive. Source readings: ling5|leng5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["信心", { label: "lex", pos: "lexical_item", jyutping: "seon3 sam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 515, frequency 57; prior audit status handled_structurally. confidence/faith (in sb or sth). Source readings: seon3sam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["後尾", { label: "lex", pos: "lexical_item", jyutping: "hau6 mei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 516, frequency 57; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hau6mei5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["當", { label: "lex", pos: "lexical_item", jyutping: "dong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 519, frequency 57; prior audit status missing. when/during/just at (a time or place)/on the spot/ought/should/equal/same/manage|to regard as/suitable/adequate/fitting/proper/to pawn/obstruct. Source readings: dong1|dong3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["繞", { label: "lex", pos: "lexical_item", jyutping: "jiu5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 520, frequency 57; prior audit status missing. to wind/to coil (thread)/to rotate around/to spiral/to move around/to go round (an obstacle)/to by-pass/to make a detour/to confuse/to perplex. Source readings: jiu5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成病", { label: "lex", pos: "lexical_item", jyutping: "sing4 beng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 522, frequency 56; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sing4beng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我話", { label: "lex", pos: "lexical_item", jyutping: "ngo5 waa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 524, frequency 56; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5waa6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["是", { label: "lex", pos: "lexical_item", jyutping: "si6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 525, frequency 56; prior audit status missing. is/are/am/yes/to be. Source readings: si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["完成", { label: "lex", pos: "lexical_item", jyutping: "jyun4 sing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 532, frequency 55; prior audit status missing. complete/accomplish/perfect tense (grammar). Source readings: jyun4sing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["男仔", { label: "lex", pos: "lexical_item", jyutping: "naam4 zai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 533, frequency 55; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *naam4zai2|zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["跟", { label: "lex", pos: "lexical_item", jyutping: "gan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 535, frequency 55; prior audit status missing. to follow closely/to go with/to marry sb (of woman)/with/towards/as (compared to)/from (different from)/and (in addition to)/heel(Mandarin). Source readings: gan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["任何", { label: "lex", pos: "lexical_item", jyutping: "jam6 ho4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 537, frequency 54; prior audit status missing. any/whatever/whichever/whatsoever. Source readings: jam6ho4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["返來", { label: "lex", pos: "lexical_item", jyutping: "faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 538, frequency 54; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *faan1|faan2loi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["緊要", { label: "lex", pos: "lexical_item", jyutping: "gan2 jiu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 542, frequency 54; prior audit status handled_structurally. important. Source readings: gan2jiu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["憂店", { label: "lex", pos: "lexical_item", jyutping: "jau1 dim3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 543, frequency 54; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau1dim3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["樂", { label: "lex", pos: "lexical_item", jyutping: "lok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 544, frequency 54; prior audit status missing. happy/laugh/cheerful/surname Le/surname Yue/music. Source readings: lok6|ngok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左上", { label: "lex", pos: "lexical_item", jyutping: "zo2 soeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 546, frequency 53; prior audit status handled_structurally. upper left. Source readings: zo2soeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["班", { label: "lex", pos: "lexical_item", jyutping: "baan1", syntax: "lexical_item", note: `Cifu SpokenAdult rank 550, frequency 53; prior audit status missing. team/class/squad/work shift/classifier for groups/ranking/to gather together/to gather support ("back-up")/to deploy one's gang member/a surname. Source readings: baan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.` }],
      ["細個", { label: "lex", pos: "lexical_item", jyutping: "sai3 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 553, frequency 53; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sai3go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["點啊", { label: "lex", pos: "lexical_item", jyutping: "dim2 aa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 557, frequency 53; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dim2aa3|aa1|aa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一係", { label: "lex", pos: "lexical_item", jyutping: "jat1 hai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 558, frequency 52; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: jat1hai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我會", { label: "lex", pos: "lexical_item", jyutping: "ngo5 wui5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 559, frequency 52; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5wui5|wui6|wui2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["提", { label: "lex", pos: "lexical_item", jyutping: "tai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 563, frequency 52; prior audit status missing. carry (suspended)/to carry (hanging down from the hand)/to lift/to put forward/to mention/to raise (an issue)/upwards character stroke/lifting brush stroke (in painting)/scoop for measuring liquid. Source readings: tai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["養", { label: "lex", pos: "lexical_item", jyutping: "joeng5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 564, frequency 52; prior audit status missing. to raise (animals)/to bring up (children)/to keep (pets)/to support/to give birth. Source readings: joeng5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["打電話", { label: "lex", pos: "lexical_item", jyutping: "daa2 din6 waa2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 570, frequency 51; prior audit status handled_structurally. to make a telephone call. Source readings: daa2din6waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成個", { label: "lex", pos: "lexical_item", jyutping: "sing4 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 571, frequency 51; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sing4go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["說話", { label: "lex", pos: "lexical_item", jyutping: "syut3 waa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 574, frequency 51; prior audit status missing. to speak/to say/to talk/to gossip/to tell stories/talk/word. Source readings: syut3waa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["上行", { label: "lex", pos: "lexical_item", jyutping: "soeng6 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 576, frequency 50; prior audit status handled_structurally. up train (i.e. towards the capital). Source readings: soeng6hang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["穿", { label: "lex", pos: "lexical_item", jyutping: "cyun1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 581, frequency 50; prior audit status missing. to bore through/to pierce/to perforate/to penetrate/to pass through/to dress/to wear/to put on/to thread/damaged with a hole or holes (e.g. pots, socks). Source readings: cyun1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["哩樣", { label: "lex", pos: "lexical_item", jyutping: "lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 582, frequency 50; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lei5|le1|lei1|li1joeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["從", { label: "lex", pos: "lexical_item", jyutping: "cung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 584, frequency 50; prior audit status missing. from/via/passing through/through (a gap)/past/ever (followed by negative, meaning never)/(formerly pr. zong4 and related to 縱|纵) to follow/to comply with/to obey/to join/to engage in/adopting some mode of action or attit. Source readings: cung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["呵", { label: "lex", pos: "lexical_item", jyutping: "ho1", syntax: "lexical_item", note: 'Cifu SpokenAdult rank 586, frequency 49; prior audit status missing. expel breath/my goodness/variantof 啊/phonetic "a". Source readings: ho1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.' }],
      ["揸", { label: "lex", pos: "lexical_item", jyutping: "zaa1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 589, frequency 49; prior audit status missing. to hold/to drive/to pilot/to make a decision. Source readings: zaa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右上", { label: "lex", pos: "lexical_item", jyutping: "jau6 soeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 592, frequency 48; prior audit status handled_structurally. upper right. Source readings: jau6soeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["返工", { label: "lex", pos: "lexical_item", jyutping: "faan1 gung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 595, frequency 48; prior audit status handled_structurally. go to work. Source readings: faan1gung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["着", { label: "lex", pos: "lexical_item", jyutping: "", syntax: "lexical_item", note: "Cifu SpokenAdult rank 597, frequency 48; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *?. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["發覺", { label: "lex", pos: "lexical_item", jyutping: "faat3 gok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 599, frequency 48; prior audit status handled_structurally. to find/to detect/to discover. Source readings: faat3gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["落到", { label: "lex", pos: "lexical_item", jyutping: "lok6 dou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 600, frequency 48; prior audit status handled_structurally. befallen. Source readings: lok6dou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["繞過", { label: "lex", pos: "lexical_item", jyutping: "jiu5 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 601, frequency 48; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jiu5gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一種", { label: "lex", pos: "lexical_item", jyutping: "jat1 zung2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 603, frequency 47; prior audit status missing. one kind of/one type of. Source readings: jat1zung2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["以", { label: "lex", pos: "lexical_item", jyutping: "ji5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 604, frequency 47; prior audit status missing. abbr. for Israel 以色列[yíh sīk liht]/to use/according to/so as to/in order to/by/with/because. Source readings: ji5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["包", { label: "lex", pos: "lexical_item", jyutping: "baau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 605, frequency 47; prior audit status missing. to cover/to wrap/to promise/to guarantee/to include/to keep a mistress or second wife/package/wrapper/container/bag/bundle/packet. Source readings: baau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["未必", { label: "lex", pos: "lexical_item", jyutping: "mei6 bit1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 606, frequency 47; prior audit status missing. not necessarily/maybe not. Source readings: mei6bit1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["拍", { label: "lex", pos: "lexical_item", jyutping: "paak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 607, frequency 47; prior audit status missing. to pat/to clap/to slap/to swat/to take (a photo)/to shoot (a film)/racket (sports)/beat (music). Source readings: paak3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["要求", { label: "lex", pos: "lexical_item", jyutping: "jiu1 kau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 609, frequency 47; prior audit status missing. to request/to require/to stake a claim/to ask/to demand. Source readings: jiu1kau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["影響", { label: "lex", pos: "lexical_item", jyutping: "jing2 hoeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 611, frequency 47; prior audit status missing. an influence/an effect/to influence/to affect (usually adversely)/to disturb. Source readings: jing2hoeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講到", { label: "lex", pos: "lexical_item", jyutping: "gong2 dou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 613, frequency 47; prior audit status handled_structurally. to talk about sth. Source readings: gong2dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["考慮", { label: "lex", pos: "lexical_item", jyutping: "haau2 leoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 617, frequency 46; prior audit status missing. to think over/to consider/consideration. Source readings: haau2leoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["某", { label: "lex", pos: "lexical_item", jyutping: "mau5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 619, frequency 46; prior audit status missing. some/a certain/sb or sth indefinite/such-and-such. Source readings: mau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔夠", { label: "lex", pos: "lexical_item", jyutping: "m4 gau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 620, frequency 46; prior audit status missing. not enough. Source readings: m4gau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["射", { label: "lex", pos: "lexical_item", jyutping: "se6", syntax: "lexical_item", note: 'Cifu SpokenAdult rank 621, frequency 46; prior audit status missing. to shoot/to look after, to protect, to watch over/to ejaculate, to "come"/radio- (chemistry). Source readings: se6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.' }],
      ["會話", { label: "lex", pos: "lexical_item", jyutping: "wui6 waa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 622, frequency 46; prior audit status handled_structurally. conversation. Source readings: wui6waa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["轉彎", { label: "lex", pos: "lexical_item", jyutping: "zyun3 waan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 625, frequency 46; prior audit status handled_structurally. turn (around). Source readings: zyun3waan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["攬", { label: "lex", pos: "lexical_item", jyutping: "laam5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 626, frequency 46; prior audit status missing. to embrace/to monopolize/to seize. Source readings: laam5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["水平", { label: "lex", pos: "lexical_item", jyutping: "seoi2 ping4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 628, frequency 45; prior audit status handled_structurally. level (of achievement etc)/standard/horizontal. Source readings: seoi2ping4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["其中", { label: "lex", pos: "lexical_item", jyutping: "kei4 zung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 631, frequency 45; prior audit status missing. among/in/included among these. Source readings: kei4zung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["教", { label: "lex", pos: "lexical_item", jyutping: "gaau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 634, frequency 45; prior audit status missing. religion/teaching/to teach/to make (Mandarin)/to cause (Mandarin)/to tell (Mandarin). Source readings: gaau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["最好", { label: "lex", pos: "lexical_item", jyutping: "zeoi3 hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 635, frequency 45; prior audit status handled_structurally. best/(you) had better (do what we suggest). Source readings: zeoi3hou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["磅", { label: "lex", pos: "lexical_item", jyutping: "bong6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 636, frequency 45; prior audit status missing. to weigh/to give/to accompany/to escort/to protect/pound (unit of weight, about 454 grams)|scale/platform balance. Source readings: bong6|bong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["山路", { label: "lex", pos: "lexical_item", jyutping: "saan1 lou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 637, frequency 44; prior audit status handled_structurally. mountain road. Source readings: saan1lou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["公共", { label: "lex", pos: "lexical_item", jyutping: "gung1 gung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 638, frequency 44; prior audit status missing. public/common (use). Source readings: gung1gung6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["代表", { label: "lex", pos: "lexical_item", jyutping: "doi6 biu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 640, frequency 44; prior audit status missing. representative/delegate/to represent/to stand for. Source readings: doi6biu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好過", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 641, frequency 44; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["者", { label: "lex", pos: "lexical_item", jyutping: "ze2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 643, frequency 44; prior audit status missing. -ist, -er (person)/person (who does sth). Source readings: ze2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["段", { label: "lex", pos: "lexical_item", jyutping: "dyun6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 644, frequency 44; prior audit status missing. surname Duan/paragraph/section/segment/stage (of a process)/classifier for stories. Source readings: dyun6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["發展", { label: "lex", pos: "lexical_item", jyutping: "faat3 zin2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 650, frequency 43; prior audit status missing. development/growth/to develop/to grow/to expand. Source readings: faat3zin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["賺", { label: "lex", pos: "lexical_item", jyutping: "zaan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 653, frequency 43; prior audit status missing. to earn/to make a profit. Source readings: zaan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["同一", { label: "lex", pos: "lexical_item", jyutping: "tung4 jat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 655, frequency 42; prior audit status handled_structurally. identical/the same. Source readings: tung4jat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我見", { label: "lex", pos: "lexical_item", jyutping: "ngo5 gin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 657, frequency 42; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5gin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["接受", { label: "lex", pos: "lexical_item", jyutping: "zip3 sau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 660, frequency 42; prior audit status missing. to accept/to receive. Source readings: zip3sau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["貧大", { label: "lex", pos: "lexical_item", jyutping: "pan4 daai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 662, frequency 42; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *pan4daai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["分鐘", { label: "lex", pos: "lexical_item", jyutping: "fan1 zung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 668, frequency 41; prior audit status handled_structurally. minute. Source readings: fan1zung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["平時", { label: "lex", pos: "lexical_item", jyutping: "ping4 si4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 671, frequency 41; prior audit status handled_structurally. in normal times/in peacetime. Source readings: ping4si4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["打開", { label: "lex", pos: "lexical_item", jyutping: "daa2 hoi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 672, frequency 41; prior audit status handled_structurally. to open/to show (a ticket)/to turn on/to switch on. Source readings: daa2hoi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["起碼", { label: "lex", pos: "lexical_item", jyutping: "hei2 maa5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 681, frequency 41; prior audit status missing. at the minimum/at the very least. Source readings: hei2maa5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["偏", { label: "lex", pos: "lexical_item", jyutping: "pin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 682, frequency 41; prior audit status missing. to lean/to slant/oblique/prejudiced/to deviate from average/to stray from the intended line/stubbornly/contrary to expectations/left-hand side of a split Chinese character, often the key or radical. Source readings: pin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["人工", { label: "lex", pos: "lexical_item", jyutping: "jan4 gung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 684, frequency 40; prior audit status handled_structurally. artificial/manpower/manual work. Source readings: jan4gung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["反而", { label: "lex", pos: "lexical_item", jyutping: "faan2 ji4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 686, frequency 40; prior audit status missing. instead/on the contrary/contrary (to expectations). Source readings: faan2ji4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有陣", { label: "lex", pos: "lexical_item", jyutping: "jau5 zan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 688, frequency 40; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau5zan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["除", { label: "lex", pos: "lexical_item", jyutping: "ceoi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 690, frequency 40; prior audit status missing. to get rid of/to remove/to exclude/to eliminate/to wipe out/to divide/except/not including. Source readings: ceoi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["剪", { label: "lex", pos: "lexical_item", jyutping: "zin2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 691, frequency 40; prior audit status missing. scissors/shears/clippers/CL:把[bá]/to cut with scissors/to trim. Source readings: zin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["袋", { label: "lex", pos: "lexical_item", jyutping: "doi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 693, frequency 40; prior audit status missing. to bag|a pouch/bag/sack/pocket/a condom/scrotum. Source readings: doi6|doi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["慢慢", { label: "lex", pos: "lexical_item", jyutping: "maan6 maan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 696, frequency 40; prior audit status handled_structurally. slowly. Source readings: maan6maan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["三個", { label: "lex", pos: "lexical_item", jyutping: "saam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 698, frequency 39; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *saam1|saam3go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["女人", { label: "lex", pos: "lexical_item", jyutping: "neoi5 jan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 699, frequency 39; prior audit status handled_structurally. woman. Source readings: neoi5jan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["行過", { label: "lex", pos: "lexical_item", jyutping: "hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 702, frequency 39; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hang4|haang4|hang6|hong4|hong2gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["所有", { label: "lex", pos: "lexical_item", jyutping: "so2 jau5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 703, frequency 39; prior audit status missing. all/to have/to possess/to own. Source readings: so2jau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["直落", { label: "lex", pos: "lexical_item", jyutping: "zik6 lok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 704, frequency 39; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zik6lok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["後", { label: "lex", pos: "lexical_item", jyutping: "hau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 705, frequency 39; prior audit status missing. back/behind/rear/afterwards/after/later. Source readings: hau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["敢", { label: "lex", pos: "lexical_item", jyutping: "gam2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 708, frequency 39; prior audit status missing. dare. Source readings: gam2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["點呀", { label: "lex", pos: "lexical_item", jyutping: "dim2 aa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 710, frequency 39; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dim2aa3|aa1|aa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["水平線", { label: "lex", pos: "lexical_item", jyutping: "seoi2 ping4 sin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 712, frequency 38; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: seoi2ping4sin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["例如", { label: "lex", pos: "lexical_item", jyutping: "lai6 jyu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 713, frequency 38; prior audit status missing. for example/for instance/such as. Source readings: lai6jyu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["直上", { label: "lex", pos: "lexical_item", jyutping: "zik6 soeng5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 715, frequency 38; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zik6soeng5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["直程", { label: "lex", pos: "lexical_item", jyutping: "zik6 cing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 716, frequency 38; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zik6cing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個個", { label: "lex", pos: "lexical_item", jyutping: "go3 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 717, frequency 38; prior audit status handled_structurally. each one individually/each and every/same as 各個|各个. Source readings: go3go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["畫個", { label: "lex", pos: "lexical_item", jyutping: "waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 720, frequency 38; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *waak6|waa2go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一半", { label: "lex", pos: "lexical_item", jyutping: "jat1 bun3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 722, frequency 37; prior audit status handled_structurally. half. Source readings: jat1bun3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左行", { label: "lex", pos: "lexical_item", jyutping: "zo2 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 724, frequency 37; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zo2hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["食飯", { label: "lex", pos: "lexical_item", jyutping: "sik6 faan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 725, frequency 37; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sik6faan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["下行", { label: "lex", pos: "lexical_item", jyutping: "haa6 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 730, frequency 36; prior audit status handled_structurally. down train (i.e. away from the capital). Source readings: haa6hang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["包括", { label: "lex", pos: "lexical_item", jyutping: "baau1 kut3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 732, frequency 36; prior audit status missing. to comprise/to include/to involve/to incorporate/to consist of. Source readings: baau1kut3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["因", { label: "lex", pos: "lexical_item", jyutping: "jan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 734, frequency 36; prior audit status missing. cause/reason/because. Source readings: jan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["始終", { label: "lex", pos: "lexical_item", jyutping: "ci2 zung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 737, frequency 36; prior audit status missing. from beginning to end/all along. Source readings: ci2zung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["計", { label: "lex", pos: "lexical_item", jyutping: "gai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 739, frequency 36; prior audit status missing. to calculate/to compute/to count/to regard as important/to plan/ruse/meter/gauge. Source readings: gai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個韻", { label: "lex", pos: "lexical_item", jyutping: "go3 wan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 740, frequency 36; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *go3wan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["原本", { label: "lex", pos: "lexical_item", jyutping: "jyun4 bun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 741, frequency 36; prior audit status missing. originally/original/abbr. for Euclid's elements 幾何原本|几何原本. Source readings: jyun4bun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["連", { label: "lex", pos: "lexical_item", jyutping: "lin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 742, frequency 36; prior audit status missing. surname Lian/even/as/join/to link/successively. Source readings: lin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["港人", { label: "lex", pos: "lexical_item", jyutping: "gong2 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 743, frequency 36; prior audit status handled_structurally. Hong Kong person or people. Source readings: gong2jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["試過", { label: "lex", pos: "lexical_item", jyutping: "si3 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 744, frequency 36; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *si3gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["大個", { label: "lex", pos: "lexical_item", jyutping: "daai6 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 747, frequency 35; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *daai6go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["公務員", { label: "lex", pos: "lexical_item", jyutping: "gung1 mou6 jyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 748, frequency 35; prior audit status missing. functionary/office-bearer. Source readings: gung1mou6jyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["化", { label: "lex", pos: "lexical_item", jyutping: "faa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 750, frequency 35; prior audit status missing. to make into/to change into/-ization/to ... -ize/to transform/to be liberal, tolerant/abbr. for chemistry 化學. Source readings: faa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我個", { label: "lex", pos: "lexical_item", jyutping: "ngo5 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 752, frequency 35; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我想", { label: "lex", pos: "lexical_item", jyutping: "ngo5 soeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 753, frequency 35; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5soeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["放棄", { label: "lex", pos: "lexical_item", jyutping: "fong3 hei3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 755, frequency 35; prior audit status missing. to renounce/to abandon/to give up. Source readings: fong3hei3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["勇", { label: "lex", pos: "lexical_item", jyutping: "jung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 756, frequency 35; prior audit status missing. brave. Source readings: jung6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["神", { label: "lex", pos: "lexical_item", jyutping: "san4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 757, frequency 35; prior audit status missing. God/unusual/mysterious/soul/spirit/divine essence/lively/spiritual being/an idol/a role model/a hero. Source readings: san4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["笨", { label: "lex", pos: "lexical_item", jyutping: "ban6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 759, frequency 35; prior audit status missing. stupid/foolish/silly/slow-witted/clumsy. Source readings: ban6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["尊重", { label: "lex", pos: "lexical_item", jyutping: "zyun1 zung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 760, frequency 35; prior audit status missing. esteem/respect/to honor/to value sth. Source readings: zyun1zung6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾個", { label: "lex", pos: "lexical_item", jyutping: "gei2 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 761, frequency 35; prior audit status handled_structurally. a few/several/how many. Source readings: gei2go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["棚", { label: "lex", pos: "lexical_item", jyutping: "paang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 762, frequency 35; prior audit status missing. shed/canopy/shack. Source readings: paang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["會堂", { label: "lex", pos: "lexical_item", jyutping: "wui6 tong4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 763, frequency 35; prior audit status missing. meeting hall/assembly hall. Source readings: wui6tong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講話", { label: "lex", pos: "lexical_item", jyutping: "gong2 waa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 764, frequency 35; prior audit status handled_structurally. a speech/to speak/to talk/to address. Source readings: gong2waa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["礦", { label: "lex", pos: "lexical_item", jyutping: "kwong3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 765, frequency 35; prior audit status missing. ore/mine. Source readings: kwong3|gwong3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一份", { label: "lex", pos: "lexical_item", jyutping: "jat1 fan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 766, frequency 34; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1fan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["大學", { label: "lex", pos: "lexical_item", jyutping: "daai6 hok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 767, frequency 34; prior audit status handled_structurally. university. Source readings: daai6hok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["之外", { label: "lex", pos: "lexical_item", jyutping: "zi1 ngoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 768, frequency 34; prior audit status missing. outside/excluding. Source readings: zi1ngoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["介紹", { label: "lex", pos: "lexical_item", jyutping: "gaai3 siu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 769, frequency 34; prior audit status missing. to present/introduction/to introduce. Source readings: gaai3siu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["行為", { label: "lex", pos: "lexical_item", jyutping: "hang6 wai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 770, frequency 34; prior audit status missing. action/conduct/behavior/activity. Source readings: hang6wai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["忍依", { label: "lex", pos: "lexical_item", jyutping: "jan2 ji1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 771, frequency 34; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jan2ji1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["投資", { label: "lex", pos: "lexical_item", jyutping: "tau4 zi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 772, frequency 34; prior audit status missing. investment/to invest. Source readings: tau4zi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["害", { label: "lex", pos: "lexical_item", jyutping: "hoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 774, frequency 34; prior audit status missing. to do harm to/to cause trouble to/harm/evil/calamity. Source readings: hoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["堡", { label: "lex", pos: "lexical_item", jyutping: "bou2", syntax: "lexical_item", note: 'Cifu SpokenAdult rank 777, frequency 34; prior audit status missing. an earthwork/castle/position of defense/stronghold/used in place names, often as phonetic bao for "burg" or "bad"/variant of 铺|铺/used in place names. Source readings: bou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.' }],
      ["當時", { label: "lex", pos: "lexical_item", jyutping: "dong1 si4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 778, frequency 34; prior audit status missing. then/at that time/while. Source readings: dong1si4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["路線", { label: "lex", pos: "lexical_item", jyutping: "lou6 sin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 779, frequency 34; prior audit status handled_structurally. itinerary/route/political line (e.g. right revisionist road). Source readings: lou6sin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["餐室", { label: "lex", pos: "lexical_item", jyutping: "caan1 sat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 781, frequency 34; prior audit status missing. dining room. Source readings: caan1sat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["黐線", { label: "lex", pos: "lexical_item", jyutping: "ci1 sin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 782, frequency 34; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ci1sin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一年", { label: "lex", pos: "lexical_item", jyutping: "jat1 nin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 783, frequency 33; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1nin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["令", { label: "lex", pos: "lexical_item", jyutping: "ling6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 785, frequency 33; prior audit status missing. make or cause to be/order/command/decree/honorable. Source readings: ling6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右手", { label: "lex", pos: "lexical_item", jyutping: "jau6 sau2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 786, frequency 33; prior audit status handled_structurally. right-hand. Source readings: jau6sau2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["外", { label: "lex", pos: "lexical_item", jyutping: "ngoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 787, frequency 33; prior audit status missing. outside/in addition/foreign/external. Source readings: ngoi6|oi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有錢", { label: "lex", pos: "lexical_item", jyutping: "jau5 cin2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 788, frequency 33; prior audit status handled_structurally. to be rich/rich. Source readings: jau5cin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["至", { label: "lex", pos: "lexical_item", jyutping: "zi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 789, frequency 33; prior audit status missing. arrive/most/to/until. Source readings: zi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["表達", { label: "lex", pos: "lexical_item", jyutping: "biu2 daat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 791, frequency 33; prior audit status missing. to voice (an opinion)/to express/to convey. Source readings: biu2daat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["甚至", { label: "lex", pos: "lexical_item", jyutping: "sam6 zi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 792, frequency 33; prior audit status missing. even/so much so that. Source readings: sam6zi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["美", { label: "lex", pos: "lexical_item", jyutping: "mei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 793, frequency 33; prior audit status missing. beautiful/abbr. for USA 美國[méih gwok]. Source readings: mei5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["秩序", { label: "lex", pos: "lexical_item", jyutping: "dit6 zeoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 795, frequency 33; prior audit status missing. order (orderly)/order (sequence)/social order/the state (of society). Source readings: dit6zeoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["能力", { label: "lex", pos: "lexical_item", jyutping: "nang4 lik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 796, frequency 33; prior audit status missing. capability/capable/able/ability. Source readings: nang4lik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["棵", { label: "lex", pos: "lexical_item", jyutping: "po1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 797, frequency 33; prior audit status missing. classifier for trees, cabbages, plants etc. Source readings: po1|fo2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["廣告", { label: "lex", pos: "lexical_item", jyutping: "gwong2 gou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 798, frequency 33; prior audit status missing. to advertise/a commercial/advertisement/CL:項|项[xiang4]. Source readings: gwong2gou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["靠", { label: "lex", pos: "lexical_item", jyutping: "kaau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 799, frequency 33; prior audit status missing. to lean against or on/to stand by the side of/to come near to/to depend or rely on/to trust/to fuck (vulgar) (Mandarin). Source readings: kaau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講下", { label: "lex", pos: "lexical_item", jyutping: "gong2 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 800, frequency 33; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["○", { label: "lex", pos: "lexical_item", jyutping: "", syntax: "lexical_item", note: "Cifu SpokenAdult rank 801, frequency 32; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *?. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["上方", { label: "lex", pos: "lexical_item", jyutping: "soeng6 fong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 802, frequency 32; prior audit status missing. place above. Source readings: soeng6fong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["只不過", { label: "lex", pos: "lexical_item", jyutping: "zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 803, frequency 32; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zi2|zek3bat1gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["永遠", { label: "lex", pos: "lexical_item", jyutping: "wing5 jyun5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 804, frequency 32; prior audit status missing. forever/eternal. Source readings: wing5jyun5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["生意", { label: "lex", pos: "lexical_item", jyutping: "saang1 ji3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 805, frequency 32; prior audit status handled_structurally. business. Source readings: saang1ji3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好玩", { label: "lex", pos: "lexical_item", jyutping: "hou2 waan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 806, frequency 32; prior audit status handled_structurally. fun (to do). Source readings: hou2waan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["字典", { label: "lex", pos: "lexical_item", jyutping: "zi6 din2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 807, frequency 32; prior audit status missing. dictionary/character dictionary/CL:本[ben3]. Source readings: zi6din2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["於", { label: "lex", pos: "lexical_item", jyutping: "jyu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 809, frequency 32; prior audit status missing. in/at/to/from/by/than/out of. Source readings: jyu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["型", { label: "lex", pos: "lexical_item", jyutping: "jing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 810, frequency 32; prior audit status missing. model/good-looking/cool/handsome/type (e.g. blood type). Source readings: jing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["盆", { label: "lex", pos: "lexical_item", jyutping: "pun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 811, frequency 32; prior audit status missing. basin/flower pot/unit of volume equal to 12 斗 and 8 升, approx 128 liters. Source readings: pun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["飛機", { label: "lex", pos: "lexical_item", jyutping: "fei1 gei1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 812, frequency 32; prior audit status handled_structurally. airplane/CL: 架[jia4]. Source readings: fei1gei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["陣時", { label: "lex", pos: "lexical_item", jyutping: "zan6 si4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 813, frequency 32; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zan6si4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["接近", { label: "lex", pos: "lexical_item", jyutping: "zip3 gan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 814, frequency 32; prior audit status missing. near/close to. Source readings: zip3gan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["短", { label: "lex", pos: "lexical_item", jyutping: "dyun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 815, frequency 32; prior audit status missing. short or brief/to lack/weak point/fault. Source readings: dyun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["機會", { label: "lex", pos: "lexical_item", jyutping: "gei1 wui6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 816, frequency 32; prior audit status handled_structurally. opportunity/chance/occasion. Source readings: gei1wui6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講得", { label: "lex", pos: "lexical_item", jyutping: "gong2 dak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 818, frequency 32; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2dak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講過", { label: "lex", pos: "lexical_item", jyutping: "gong2 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 819, frequency 32; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我要", { label: "lex", pos: "lexical_item", jyutping: "ngo5 jiu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 822, frequency 31; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5jiu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["來講", { label: "lex", pos: "lexical_item", jyutping: "loi4 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 823, frequency 31; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *loi4gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["制度", { label: "lex", pos: "lexical_item", jyutping: "zai3 dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 824, frequency 31; prior audit status missing. (political or administrative) system or institution. Source readings: zai3dou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["面對", { label: "lex", pos: "lexical_item", jyutping: "min6 deoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 826, frequency 31; prior audit status handled_structurally. to confront/to face. Source readings: min6deoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["案", { label: "lex", pos: "lexical_item", jyutping: "on3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 828, frequency 31; prior audit status missing. (legal) case/incident/record/file/table. Source readings: on3|ngon3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["晚", { label: "lex", pos: "lexical_item", jyutping: "maan5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 829, frequency 31; prior audit status missing. evening/night/late. Source readings: maan5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["無論", { label: "lex", pos: "lexical_item", jyutping: "mou4 leon6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 830, frequency 31; prior audit status missing. no matter what or how/regardless of whether.... Source readings: mou4leon6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["圓形", { label: "lex", pos: "lexical_item", jyutping: "jyun4 jing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 831, frequency 31; prior audit status missing. round/circular. Source readings: jyun4jing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["感受", { label: "lex", pos: "lexical_item", jyutping: "gam2 sau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 832, frequency 31; prior audit status missing. to sense/perception/to feel (through the senses)/a feeling/an impression/an experience. Source readings: gam2sau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘍", { label: "lex", pos: "lexical_item", jyutping: "lau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 833, frequency 31; prior audit status missing. (final particle equivalent to 了)/subordinates in gang of bandits. Source readings: lau1|lau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["鼻", { label: "lex", pos: "lexical_item", jyutping: "bei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 834, frequency 31; prior audit status missing. nose. Source readings: bei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘿", { label: "lex", pos: "lexical_item", jyutping: "hei1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 835, frequency 31; prior audit status missing. hey. Source readings: hei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嬋", { label: "lex", pos: "lexical_item", jyutping: "sim4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 836, frequency 31; prior audit status missing. beautiful/graceful. Source readings: sim4|sin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["數碼", { label: "lex", pos: "lexical_item", jyutping: "sou3 maa5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 837, frequency 31; prior audit status missing. number/numerals/figures/digital/amount/numerical code. Source readings: sou3maa5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["誰", { label: "lex", pos: "lexical_item", jyutping: "seoi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 838, frequency 31; prior audit status missing. variant pronunciation of shei2, who/surname Shui/who/also pronounced shui2. Source readings: seoi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一陣", { label: "lex", pos: "lexical_item", jyutping: "jat1 zan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 839, frequency 30; prior audit status handled_structurally. a burst/a fit/a peal. Source readings: jat1zan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一間", { label: "lex", pos: "lexical_item", jyutping: "jat1 gaan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 840, frequency 30; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1gaan1|gaan3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["大佬", { label: "lex", pos: "lexical_item", jyutping: "daai6 lou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 841, frequency 30; prior audit status missing. big shot (leading some field or group)/godfather/an exclamation used to express impatience with someone (male or female). Source readings: daai6lou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["女朋友", { label: "lex", pos: "lexical_item", jyutping: "neoi5 pang4 jau5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 842, frequency 30; prior audit status handled_structurally. female friend/girlfriend. Source readings: neoi5pang4jau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["小學", { label: "lex", pos: "lexical_item", jyutping: "siu2 hok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 843, frequency 30; prior audit status handled_structurally. primary school. Source readings: siu2hok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["中文", { label: "lex", pos: "lexical_item", jyutping: "zung1 man4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 844, frequency 30; prior audit status missing. Chinese/Chinese written language/Chinese writing. Source readings: zung1man4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["井", { label: "lex", pos: "lexical_item", jyutping: "zeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 845, frequency 30; prior audit status missing. warn/well/surname Jing. Source readings: zeng2|zing2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["令到", { label: "lex", pos: "lexical_item", jyutping: "ling6 dou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 846, frequency 30; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ling6dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["去邊", { label: "lex", pos: "lexical_item", jyutping: "heoi3 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 847, frequency 30; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *heoi3bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["立法", { label: "lex", pos: "lexical_item", jyutping: "lap6 faat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 849, frequency 30; prior audit status missing. legislation. Source readings: lap6faat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["判決", { label: "lex", pos: "lexical_item", jyutping: "pun3 kyut3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 850, frequency 30; prior audit status missing. judgment (by a court of law). Source readings: pun3kyut3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["拎", { label: "lex", pos: "lexical_item", jyutping: "ling1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 851, frequency 30; prior audit status missing. to lift/to hold up/to carry (on bent fingers). Source readings: ling1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["法院", { label: "lex", pos: "lexical_item", jyutping: "faat3 jyun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 852, frequency 30; prior audit status missing. court of law/court. Source readings: faat3jyun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["知點", { label: "lex", pos: "lexical_item", jyutping: "zi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 853, frequency 30; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zi1|zi3dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["城市", { label: "lex", pos: "lexical_item", jyutping: "sing4 si5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 854, frequency 30; prior audit status handled_structurally. city/town/CL:座[zuo4]. Source readings: sing4si5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["原因", { label: "lex", pos: "lexical_item", jyutping: "jyun4 jan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 857, frequency 30; prior audit status missing. cause/origin/root cause/reason. Source readings: jyun4jan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["病", { label: "lex", pos: "lexical_item", jyutping: "beng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 858, frequency 30; prior audit status missing. illness/CL:場[chèuhng]/disease/to fall ill/defect. Source readings: beng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["純", { label: "lex", pos: "lexical_item", jyutping: "seon4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 859, frequency 30; prior audit status missing. pure/simple/unmixed/genuine. Source readings: seon4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["眼", { label: "lex", pos: "lexical_item", jyutping: "ngaan5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 860, frequency 30; prior audit status missing. eye/CL:隻[jek],對[deui]. Source readings: ngaan5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["最多", { label: "lex", pos: "lexical_item", jyutping: "zeoi3 do1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 861, frequency 30; prior audit status handled_structurally. at most/maximum/largest (number of sth)/the most. Source readings: zeoi3do1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["喥", { label: "lex", pos: "lexical_item", jyutping: "dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 862, frequency 30; prior audit status missing. (Cantonese) a place. Source readings: dou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["發生", { label: "lex", pos: "lexical_item", jyutping: "faat3 sang1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 863, frequency 30; prior audit status handled_structurally. to happen/to occur/to take place/to break out. Source readings: faat3sang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["開頭", { label: "lex", pos: "lexical_item", jyutping: "hoi1 tau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 864, frequency 30; prior audit status handled_structurally. beginning. Source readings: hoi1tau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["塘", { label: "lex", pos: "lexical_item", jyutping: "tong4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 865, frequency 30; prior audit status missing. pond. Source readings: tong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["經", { label: "lex", pos: "lexical_item", jyutping: "ging1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 867, frequency 30; prior audit status missing. classics/sacred book/scripture/to pass through/to undergo/warp/longitude/abbr. for economics 經濟|经济/surname Jing. Source readings: ging1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講真", { label: "lex", pos: "lexical_item", jyutping: "gong2 zan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 868, frequency 30; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2zan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["下方", { label: "lex", pos: "lexical_item", jyutping: "haa6 fong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 869, frequency 29; prior audit status missing. under; the underside; below; underneath/world of mortals; (of Gods) to descend to the world of mortals. Source readings: haa6fong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["包住", { label: "lex", pos: "lexical_item", jyutping: "baau1 zyu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 870, frequency 29; prior audit status missing. encase. Source readings: baau1zyu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["行行", { label: "lex", pos: "lexical_item", jyutping: "hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 871, frequency 29; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hang4|haang4|hang6|hong4|hong2hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["形容", { label: "lex", pos: "lexical_item", jyutping: "jing4 jung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 872, frequency 29; prior audit status missing. to describe/appearance/look. Source readings: jing4jung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["身", { label: "lex", pos: "lexical_item", jyutping: "san1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 873, frequency 29; prior audit status missing. body/life/oneself/personally/one's morality and conduct/the main part of a structure or body. Source readings: san1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["法官", { label: "lex", pos: "lexical_item", jyutping: "faat3 gun1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 874, frequency 29; prior audit status missing. a judge/judge/to judge. Source readings: faat3gun1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["返學", { label: "lex", pos: "lexical_item", jyutping: "faan1 hok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 875, frequency 29; prior audit status handled_structurally. go to school. Source readings: faan1hok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["厘米", { label: "lex", pos: "lexical_item", jyutping: "lei4 mai5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 876, frequency 29; prior audit status missing. centimeter. Source readings: lei4mai5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["問下", { label: "lex", pos: "lexical_item", jyutping: "man6 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 877, frequency 29; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *man6haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["跌", { label: "lex", pos: "lexical_item", jyutping: "dit3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 878, frequency 29; prior audit status missing. to drop/to fall/to tumble. Source readings: dit3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["搞笑", { label: "lex", pos: "lexical_item", jyutping: "gaau2 siu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 879, frequency 29; prior audit status handled_structurally. funny/hilarious/to play around/to joke. Source readings: gaau2siu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["實", { label: "lex", pos: "lexical_item", jyutping: "sat6", syntax: "lexical_item", note: 'Cifu SpokenAdult rank 881, frequency 29; prior audit status missing. real/true/honest/really/solid/fruit/seed/used as a euphemism for the obscene 柒 [chaht] "prick". Source readings: sat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.' }],
      ["撞", { label: "lex", pos: "lexical_item", jyutping: "zong6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 882, frequency 29; prior audit status missing. to hit/to strike/to meet by accident/to run into/to bump against/to bump into. Source readings: zong6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一日", { label: "lex", pos: "lexical_item", jyutping: "jat1 jat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 883, frequency 28; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1jat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["今晚", { label: "lex", pos: "lexical_item", jyutping: "gam1 maan5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 886, frequency 28; prior audit status missing. tonight. Source readings: gam1maan5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右手面", { label: "lex", pos: "lexical_item", jyutping: "jau6 sau2 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 887, frequency 28; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: jau6sau2min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左手", { label: "lex", pos: "lexical_item", jyutping: "zo2 sau2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 888, frequency 28; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zo2sau2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["平衡", { label: "lex", pos: "lexical_item", jyutping: "ping4 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 889, frequency 28; prior audit status missing. balance/equilibrium. Source readings: ping4hang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好難", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 890, frequency 28; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3naan6|naan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成為", { label: "lex", pos: "lexical_item", jyutping: "sing4 wai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 891, frequency 28; prior audit status missing. to become/to turn into. Source readings: sing4wai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成績", { label: "lex", pos: "lexical_item", jyutping: "sing4 zik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 892, frequency 28; prior audit status missing. achievement/performance records/grades. Source readings: sing4zik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["周圍", { label: "lex", pos: "lexical_item", jyutping: "zau1 wai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 893, frequency 28; prior audit status missing. surroundings/environment/to encompass. Source readings: zau1wai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["明明", { label: "lex", pos: "lexical_item", jyutping: "ming4 ming4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 894, frequency 28; prior audit status handled_structurally. obviously/plainly/undoubtedly/definitely. Source readings: ming4ming4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["肥", { label: "lex", pos: "lexical_item", jyutping: "fei4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 895, frequency 28; prior audit status missing. fat/fertile/loose-fitting or large/to fertilize/to become rich by illegal means/fertilizer/manure. Source readings: fei4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["返到", { label: "lex", pos: "lexical_item", jyutping: "faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 896, frequency 28; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *faan1|faan2dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["非常", { label: "lex", pos: "lexical_item", jyutping: "fei1 soeng4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 897, frequency 28; prior audit status missing. unusual/extraordinary/extreme/very/exceptional. Source readings: fei1soeng4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["科", { label: "lex", pos: "lexical_item", jyutping: "fo1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 899, frequency 28; prior audit status missing. branch of study/administrative section/division/field/branch/stage directions/family (taxonomy)/rules/laws/to mete out (punishment)/to levy (taxes etc)/to fine sb. Source readings: fo1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["送", { label: "lex", pos: "lexical_item", jyutping: "sung3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 901, frequency 28; prior audit status missing. to deliver/to carry/to give (as a present)/to present (with)/to see off/to send. Source readings: sung3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["圈", { label: "lex", pos: "lexical_item", jyutping: "hyun1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 902, frequency 28; prior audit status missing. circle/ring/loop/classifier for loops, orbits, laps of race etc/CL:個[go]/to surround/to circle|pen (pig)/a fold/to confine/to lock up/to pen in. Source readings: hyun1|gyun6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["國際", { label: "lex", pos: "lexical_item", jyutping: "gwok3 zai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 903, frequency 28; prior audit status missing. international. Source readings: gwok3zai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["彭銅", { label: "lex", pos: "lexical_item", jyutping: "pang4 tung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 905, frequency 28; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *pang4tung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["無", { label: "lex", pos: "lexical_item", jyutping: "mou4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 906, frequency 28; prior audit status missing. -less/not to have/no/none/not/to lack/un-. Source readings: mou4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["絕對", { label: "lex", pos: "lexical_item", jyutping: "zyut6 deoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 907, frequency 28; prior audit status missing. absolute/unconditional. Source readings: zyut6deoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["郵票", { label: "lex", pos: "lexical_item", jyutping: "jau4 piu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 909, frequency 28; prior audit status missing. (postage) stamp. Source readings: jau4piu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["經歷", { label: "lex", pos: "lexical_item", jyutping: "ging1 lik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 910, frequency 28; prior audit status missing. experience/go through. Source readings: ging1lik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["點講", { label: "lex", pos: "lexical_item", jyutping: "dim2 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 912, frequency 28; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dim2gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一下", { label: "lex", pos: "lexical_item", jyutping: "jat1 haa5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 914, frequency 27; prior audit status handled_structurally. (used after a verb) give it a go/to do (sth for a bit to give it a try). Source readings: jat1haa5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["之下", { label: "lex", pos: "lexical_item", jyutping: "zi1 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 915, frequency 27; prior audit status missing. under/beneath/less than. Source readings: zi1haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["未來", { label: "lex", pos: "lexical_item", jyutping: "mei6 loi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 916, frequency 27; prior audit status missing. future/tomorrow/approaching/coming/pending. Source readings: mei6loi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["見過", { label: "lex", pos: "lexical_item", jyutping: "gin3 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 918, frequency 27; prior audit status handled_structurally. to have seen. Source readings: gin3gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個樣", { label: "lex", pos: "lexical_item", jyutping: "go3 joeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 919, frequency 27; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *go3joeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["衰", { label: "lex", pos: "lexical_item", jyutping: "seoi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 920, frequency 27; prior audit status missing. to decline/to wane/to become weak or feeble/bad, irritating, useless, annoying/to be arrested or convicted of an offence/losing face. Source readings: seoi1|ceoi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["教書", { label: "lex", pos: "lexical_item", jyutping: "gaau3 syu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 921, frequency 27; prior audit status missing. to teach (in a school). Source readings: gaau3syu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["透過", { label: "lex", pos: "lexical_item", jyutping: "tau3 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 922, frequency 27; prior audit status missing. through/via. Source readings: tau3gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["景點", { label: "lex", pos: "lexical_item", jyutping: "ging2 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 923, frequency 27; prior audit status missing. scenery/scenic spot/place of interest (tourism). Source readings: ging2dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["結果", { label: "lex", pos: "lexical_item", jyutping: "git3 gwo2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 924, frequency 27; prior audit status missing. to bear fruit. Source readings: git3gwo2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["圓", { label: "lex", pos: "lexical_item", jyutping: "jyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 925, frequency 27; prior audit status missing. circle/round/circular/spherical/(of the moon) full/unit of Chinese currency (Yuan)/tactful/to justify. Source readings: jyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["過程", { label: "lex", pos: "lexical_item", jyutping: "gwo3 cing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 928, frequency 27; prior audit status missing. course of events/process. Source readings: gwo3cing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幣", { label: "lex", pos: "lexical_item", jyutping: "bai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 929, frequency 27; prior audit status missing. money/coins/currency. Source readings: bai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["橢圓", { label: "lex", pos: "lexical_item", jyutping: "to5 jyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 930, frequency 27; prior audit status missing. oval/ellipse/elliptic. Source readings: to5jyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["薯", { label: "lex", pos: "lexical_item", jyutping: "syu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 932, frequency 27; prior audit status missing. potato/yam. Source readings: syu4|syu5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["體", { label: "lex", pos: "lexical_item", jyutping: "tai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 933, frequency 27; prior audit status missing. body/form/style/system. Source readings: tai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["互聯網", { label: "lex", pos: "lexical_item", jyutping: "wu6 lyun4 mong5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 935, frequency 26; prior audit status missing. the Internet. Source readings: wu6lyun4mong5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["公仔", { label: "lex", pos: "lexical_item", jyutping: "gung1 zai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 936, frequency 26; prior audit status missing. doll/cuddly toy. Source readings: gung1zai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["手邊", { label: "lex", pos: "lexical_item", jyutping: "sau2 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 937, frequency 26; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sau2bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["北行", { label: "lex", pos: "lexical_item", jyutping: "bak1 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 938, frequency 26; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *bak1hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["正常", { label: "lex", pos: "lexical_item", jyutping: "zing3 soeng4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 939, frequency 26; prior audit status missing. regular/normal/ordinary. Source readings: zing3soeng4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["作為", { label: "lex", pos: "lexical_item", jyutping: "zok3 wai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 943, frequency 26; prior audit status missing. one's conduct/deed/activity/accomplishment/achievement/to act as/as (in the capacity of)/qua/to view as/to look upon (sth as)/to take sth to be. Source readings: zok3wai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["利益", { label: "lex", pos: "lexical_item", jyutping: "lei6 jik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 944, frequency 26; prior audit status missing. benefit/(in sb's) interest. Source readings: lei6jik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["努力", { label: "lex", pos: "lexical_item", jyutping: "nou5 lik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 945, frequency 26; prior audit status missing. great effort/to strive/to try hard. Source readings: nou5lik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["改革", { label: "lex", pos: "lexical_item", jyutping: "goi2 gaak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 946, frequency 26; prior audit status missing. to reform. Source readings: goi2gaak3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["私營化", { label: "lex", pos: "lexical_item", jyutping: "si1 jing4 faa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 947, frequency 26; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *si1jing4faa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["法庭", { label: "lex", pos: "lexical_item", jyutping: "faat3 ting4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 948, frequency 26; prior audit status missing. court (of law). Source readings: faat3ting4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["留意", { label: "lex", pos: "lexical_item", jyutping: "lau4 ji3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 949, frequency 26; prior audit status missing. to take care/mindful. Source readings: lau4ji3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["起身", { label: "lex", pos: "lexical_item", jyutping: "hei2 san1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 950, frequency 26; prior audit status missing. to get up/to leave/to set forth. Source readings: hei2san1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["掛住", { label: "lex", pos: "lexical_item", jyutping: "gwaa3 zyu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 951, frequency 26; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gwaa3zyu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["第日", { label: "lex", pos: "lexical_item", jyutping: "dai6 jat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 952, frequency 26; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dai6jat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["等如", { label: "lex", pos: "lexical_item", jyutping: "dang2 jyu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 953, frequency 26; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dang2jyu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["開支", { label: "lex", pos: "lexical_item", jyutping: "hoi1 zi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 956, frequency 26; prior audit status handled_structurally. expenditures/pay/expenses. Source readings: hoi1zi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["愛", { label: "lex", pos: "lexical_item", jyutping: "oi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 958, frequency 26; prior audit status missing. to love/affection/to be fond of/to like. Source readings: oi3|ngoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["腳", { label: "lex", pos: "lexical_item", jyutping: "goek3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 959, frequency 26; prior audit status missing. foot/leg/base/CL:雙[sēung](Mandarin)/CL:對[deui]/CL:隻[jek]/kick/role/a player in mahjong/a player in a drinking game. Source readings: goek3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["旗", { label: "lex", pos: "lexical_item", jyutping: "kei4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 960, frequency 26; prior audit status missing. banner/flag/a hire, a trip with a customer (taxi driver jargon)/(in Qing times) refers to Manchurian ruling class, from 八旗[baat kèih] eight banners/administrative subdivision in inner Mongolia equivalent to 縣 county/CL:支. Source readings: kei4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["遲啲", { label: "lex", pos: "lexical_item", jyutping: "ci4 di1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 962, frequency 26; prior audit status handled_structurally. later. Source readings: ci4di1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["頹餐", { label: "lex", pos: "lexical_item", jyutping: "teoi4 caan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 963, frequency 26; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *teoi4caan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["上市", { label: "lex", pos: "lexical_item", jyutping: "soeng5 si5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 964, frequency 25; prior audit status handled_structurally. on the market/to float (a company on the stock market). Source readings: soeng5si5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["公約", { label: "lex", pos: "lexical_item", jyutping: "gung1 joek3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 965, frequency 25; prior audit status missing. convention (i.e. international agreement). Source readings: gung1joek3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["支持", { label: "lex", pos: "lexical_item", jyutping: "zi1 ci4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 966, frequency 25; prior audit status missing. to be in favor of/to support/to back/support/backing/to stand by. Source readings: zi1ci4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["方式", { label: "lex", pos: "lexical_item", jyutping: "fong1 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 967, frequency 25; prior audit status missing. way (of life)/pattern/style/mode/manner. Source readings: fong1sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["方便", { label: "lex", pos: "lexical_item", jyutping: "fong1 bin6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 968, frequency 25; prior audit status missing. convenient/to help out/to make things easy for people/convenience/suitable/having money to spare/(euphemism) to go to the toilet. Source readings: fong1bin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["日子", { label: "lex", pos: "lexical_item", jyutping: "jat6 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 969, frequency 25; prior audit status missing. day/a (calendar) date/days of one's life. Source readings: jat6zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["奶茶", { label: "lex", pos: "lexical_item", jyutping: "naai5 caa4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 970, frequency 25; prior audit status handled_structurally. milk tea. Source readings: naai5caa4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左手面", { label: "lex", pos: "lexical_item", jyutping: "zo2 sau2 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 971, frequency 25; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zo2sau2min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["正話", { label: "lex", pos: "lexical_item", jyutping: "zing3 waa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 973, frequency 25; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zing3waa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好彩", { label: "lex", pos: "lexical_item", jyutping: "hou2 coi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 974, frequency 25; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: hou2coi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["決定", { label: "lex", pos: "lexical_item", jyutping: "kyut3 ding6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 976, frequency 25; prior audit status missing. to decide (to do something)/to determine/to resolve. Source readings: kyut3ding6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩年", { label: "lex", pos: "lexical_item", jyutping: "loeng5 nin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 977, frequency 25; prior audit status handled_structurally. two years. Source readings: loeng5nin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["受害", { label: "lex", pos: "lexical_item", jyutping: "sau6 hoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 978, frequency 25; prior audit status missing. to suffer damage, injury etc/damaged/injured/killed/robbed. Source readings: sau6hoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["咸", { label: "lex", pos: "lexical_item", jyutping: "haam4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 983, frequency 25; prior audit status missing. all/everyone/each/widespread/harmonious/a surname. Source readings: haam4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["英", { label: "lex", pos: "lexical_item", jyutping: "jing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 984, frequency 25; prior audit status missing. handsome/admirable/smart/great/heroic/braveEnglish/surname Ying. Source readings: jing1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔通", { label: "lex", pos: "lexical_item", jyutping: "m4 tung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 985, frequency 25; prior audit status missing. could it be. Source readings: m4tung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["真正", { label: "lex", pos: "lexical_item", jyutping: "zan1 zing3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 986, frequency 25; prior audit status handled_structurally. genuine/real/true/genuinely. Source readings: zan1zing3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["停低", { label: "lex", pos: "lexical_item", jyutping: "ting4 dai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 987, frequency 25; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: ting4dai1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾年", { label: "lex", pos: "lexical_item", jyutping: "gei2 nin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 988, frequency 25; prior audit status handled_structurally. a few years/several years. Source readings: gei2nin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["無聊", { label: "lex", pos: "lexical_item", jyutping: "mou4 liu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 989, frequency 25; prior audit status missing. bored/boring/senseless. Source readings: mou4liu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["選擇", { label: "lex", pos: "lexical_item", jyutping: "syun2 zaak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 990, frequency 25; prior audit status missing. to select/to pick/choice/option/alternative. Source readings: syun2zaak6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["頭髮", { label: "lex", pos: "lexical_item", jyutping: "tau4 faat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 991, frequency 25; prior audit status missing. hair (on the head). Source readings: tau4faat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["類似", { label: "lex", pos: "lexical_item", jyutping: "leoi6 ci5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 992, frequency 25; prior audit status missing. similar/analogous. Source readings: leoi6ci5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["鐘頭", { label: "lex", pos: "lexical_item", jyutping: "zung1 tau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 993, frequency 25; prior audit status handled_structurally. hour. Source readings: zung1tau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["手指", { label: "lex", pos: "lexical_item", jyutping: "sau2 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 996, frequency 24; prior audit status missing. finger. Source readings: sau2zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["四個", { label: "lex", pos: "lexical_item", jyutping: "sei3 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 997, frequency 24; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sei3go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["同人", { label: "lex", pos: "lexical_item", jyutping: "tung4 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 999, frequency 24; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tung4jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["在乎", { label: "lex", pos: "lexical_item", jyutping: "zoi6 fu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1000, frequency 24; prior audit status missing. determined by/to care about/to mind. Source readings: zoi6fu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有條", { label: "lex", pos: "lexical_item", jyutping: "jau5 tiu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1002, frequency 24; prior audit status handled_structurally. streakiness. Source readings: jau5tiu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["其", { label: "lex", pos: "lexical_item", jyutping: "kei4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1004, frequency 24; prior audit status missing. his/her/its/theirs/that/such/it (refers to sth preceding it). Source readings: kei4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["拖", { label: "lex", pos: "lexical_item", jyutping: "to1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1005, frequency 24; prior audit status missing. to drag/to pull/to trail/to delay/to drag on/to lead. Source readings: to1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["後來", { label: "lex", pos: "lexical_item", jyutping: "hau6 loi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1006, frequency 24; prior audit status missing. afterwards/later. Source readings: hau6loi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["研究", { label: "lex", pos: "lexical_item", jyutping: "jin4 gau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1007, frequency 24; prior audit status missing. research/a study/CL:項|项[xiang4]. Source readings: jin4gau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["限制", { label: "lex", pos: "lexical_item", jyutping: "haan6 zai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1009, frequency 24; prior audit status missing. (impose) restrictions/to limit/to bound/to restrict. Source readings: haan6zai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["值", { label: "lex", pos: "lexical_item", jyutping: "zik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1010, frequency 24; prior audit status missing. value/(to be) worth/to happen. Source readings: zik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["婆", { label: "lex", pos: "lexical_item", jyutping: "po4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1011, frequency 24; prior audit status missing. grandmother/matron/mother-in-law. Source readings: po4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["得意", { label: "lex", pos: "lexical_item", jyutping: "dak1 ji3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1012, frequency 24; prior audit status handled_structurally. cute/strange/proud of oneself/pleased with oneself/complacent. Source readings: dak1ji3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["條路", { label: "lex", pos: "lexical_item", jyutping: "tiu4 lou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1013, frequency 24; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tiu4lou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["添", { label: "lex", pos: "lexical_item", jyutping: "tim1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1014, frequency 24; prior audit status missing. to add/to increase/to replenish. Source readings: tim1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["曾經", { label: "lex", pos: "lexical_item", jyutping: "cang4 ging1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1015, frequency 24; prior audit status missing. once/already/former/previously/ever/(past tense marker used before verb or clause). Source readings: cang4ging1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["會考", { label: "lex", pos: "lexical_item", jyutping: "wui5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1018, frequency 24; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *wui5|wui6|wui2haau2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["經驗", { label: "lex", pos: "lexical_item", jyutping: "ging1 jim6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1019, frequency 24; prior audit status missing. to experience/experience. Source readings: ging1jim6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["態度", { label: "lex", pos: "lexical_item", jyutping: "taai3 dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1020, frequency 24; prior audit status missing. manner/bearing/attitude/approach. Source readings: taai3dou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["慘", { label: "lex", pos: "lexical_item", jyutping: "caam2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1021, frequency 24; prior audit status missing. miserable/wretched/cruel/inhuman/seriously/badly/tragic. Source readings: caam2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["盡量", { label: "lex", pos: "lexical_item", jyutping: "zeon6 loeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1022, frequency 24; prior audit status missing. as much as possible/to the greatest extent. Source readings: zeon6loeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["網絡", { label: "lex", pos: "lexical_item", jyutping: "mong5 lok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1023, frequency 24; prior audit status missing. network (computer, telecom etc). Source readings: mong5lok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["價值", { label: "lex", pos: "lexical_item", jyutping: "gaa3 zik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1025, frequency 24; prior audit status missing. value/worth/fig. values (ethical, cultural etc). Source readings: gaa3zik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["噉樣樣", { label: "lex", pos: "lexical_item", jyutping: "gam2 joeng2 joeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1026, frequency 24; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gam2joeng2joeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["橋", { label: "lex", pos: "lexical_item", jyutping: "kiu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1028, frequency 24; prior audit status missing. bridge. Source readings: kiu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["餐", { label: "lex", pos: "lexical_item", jyutping: "caan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1029, frequency 24; prior audit status missing. a meal. Source readings: caan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講呀", { label: "lex", pos: "lexical_item", jyutping: "gong2 aa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1030, frequency 24; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2aa3|aa1|aa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["點呢", { label: "lex", pos: "lexical_item", jyutping: "dim2 ne1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1031, frequency 24; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dim2ne1|ni1|nei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一段", { label: "lex", pos: "lexical_item", jyutping: "jat1 dyun6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1032, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1dyun6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一般", { label: "lex", pos: "lexical_item", jyutping: "jat1 bun1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1033, frequency 23; prior audit status missing. same/ordinary/common/general/generally/in general. Source readings: jat1bun1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一張", { label: "lex", pos: "lexical_item", jyutping: "jat1 zoeng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1034, frequency 23; prior audit status handled_structurally. sheet. Source readings: jat1zoeng1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["八月", { label: "lex", pos: "lexical_item", jyutping: "baat3 jyut6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1035, frequency 23; prior audit status handled_structurally. August/eighth month (of the lunar year). Source readings: baat3jyut6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["下個", { label: "lex", pos: "lexical_item", jyutping: "haa6 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1036, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *haa6go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["大部份", { label: "lex", pos: "lexical_item", jyutping: "daai6 bou6 fan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1037, frequency 23; prior audit status handled_structurally. in large part/the greater part/the majority. Source readings: daai6bou6fan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["女性", { label: "lex", pos: "lexical_item", jyutping: "neoi5 sing3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1038, frequency 23; prior audit status handled_structurally. the female sex/a woman. Source readings: neoi5sing3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["尤其", { label: "lex", pos: "lexical_item", jyutping: "jau4 kei4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1039, frequency 23; prior audit status missing. especially/particularly. Source readings: jau4kei4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["外圍", { label: "lex", pos: "lexical_item", jyutping: "ngoi6 wai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1040, frequency 23; prior audit status missing. (outside the boundary)/surrounding/illegal betting. Source readings: ngoi6wai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["皮池", { label: "lex", pos: "lexical_item", jyutping: "pei4 ci4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1041, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *pei4ci4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["刑事", { label: "lex", pos: "lexical_item", jyutping: "jing4 si6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1042, frequency 23; prior audit status missing. criminal/penal. Source readings: jing4si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["而且", { label: "lex", pos: "lexical_item", jyutping: "ji4 ce2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1043, frequency 23; prior audit status missing. not only ... but also/as well as/moreover/in addition/furthermore. Source readings: ji4ce2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["抑或", { label: "lex", pos: "lexical_item", jyutping: "jik1 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1044, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jik1waak6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["拉", { label: "lex", pos: "lexical_item", jyutping: "laai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1046, frequency 23; prior audit status missing. to pull/to play (string instruments)/to drag/to draw. Source readings: laai1|laa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["欣", { label: "lex", pos: "lexical_item", jyutping: "jan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1047, frequency 23; prior audit status missing. happy. Source readings: jan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["南行", { label: "lex", pos: "lexical_item", jyutping: "naam4 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1048, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *naam4hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["咯", { label: "lex", pos: "lexical_item", jyutping: "gaak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1049, frequency 23; prior audit status missing. (final particle similar to 了, indicating that sth is obvious)/(phonetic)/cough (blood)/to argue/debate. Source readings: gaak3|lo1|lok3|gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["重係", { label: "lex", pos: "lexical_item", jyutping: "zung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1050, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zung6|cung4|cung5hai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["馬", { label: "lex", pos: "lexical_item", jyutping: "maa5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1052, frequency 23; prior audit status missing. horse/CL:隻[jek],匹[pāt]/a junior employee/a subordinate. Source readings: maa5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["帳", { label: "lex", pos: "lexical_item", jyutping: "zoeng3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1053, frequency 23; prior audit status missing. account/mosquito net/tent/curtain/debt/credit. Source readings: zoeng3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["常", { label: "lex", pos: "lexical_item", jyutping: "soeng4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1054, frequency 23; prior audit status missing. always/ever/often/frequently/common/general/constant/surname Chang. Source readings: soeng4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["陶", { label: "lex", pos: "lexical_item", jyutping: "tou4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1055, frequency 23; prior audit status missing. pottery/pleased/surname Tao. Source readings: tou4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["麻煩", { label: "lex", pos: "lexical_item", jyutping: "maa4 faan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1056, frequency 23; prior audit status missing. inconvenient/troublesome/annoying/to trouble or bother sb/to put sb to trouble. Source readings: maa4faan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["暑假", { label: "lex", pos: "lexical_item", jyutping: "syu2 gaa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1057, frequency 23; prior audit status missing. summer vacation. Source readings: syu2gaa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["發現", { label: "lex", pos: "lexical_item", jyutping: "faat3 jin6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1058, frequency 23; prior audit status missing. to find/to discover. Source readings: faat3jin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["睇法", { label: "lex", pos: "lexical_item", jyutping: "tai2 faat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1059, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tai2faat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["程度", { label: "lex", pos: "lexical_item", jyutping: "cing4 dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1060, frequency 23; prior audit status missing. degree (level or extent)/level. Source readings: cing4dou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘥", { label: "lex", pos: "lexical_item", jyutping: "saai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1063, frequency 23; prior audit status missing. waste. Source readings: saai1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["澳洲", { label: "lex", pos: "lexical_item", jyutping: "ou3 zau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1064, frequency 23; prior audit status missing. Australia (continent)/abbr. for 澳大利亞|澳大利亚. Source readings: ou3zau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講啦", { label: "lex", pos: "lexical_item", jyutping: "gong2 laa1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1065, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2laa1|laa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["點算", { label: "lex", pos: "lexical_item", jyutping: "dim2 syun3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1066, frequency 23; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dim2syun3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["聽講", { label: "lex", pos: "lexical_item", jyutping: "teng1 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1068, frequency 23; prior audit status handled_structurally. to attend a lecture/to listen to a talk. Source readings: teng1gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["力", { label: "lex", pos: "lexical_item", jyutping: "lik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1070, frequency 22; prior audit status missing. power/force/strength/ability/strenuously. Source readings: lik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["冇事", { label: "lex", pos: "lexical_item", jyutping: "mou5 si6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1071, frequency 22; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *mou5si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左下角", { label: "lex", pos: "lexical_item", jyutping: "zo2 haa6 gok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1073, frequency 22; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zo2haa6gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左畫", { label: "lex", pos: "lexical_item", jyutping: "zo2 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1074, frequency 22; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zo2waak6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["本來", { label: "lex", pos: "lexical_item", jyutping: "bun2 loi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1075, frequency 22; prior audit status missing. original/originally/at first/it goes without saying/of course. Source readings: bun2loi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["生日", { label: "lex", pos: "lexical_item", jyutping: "saang1 jat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1076, frequency 22; prior audit status handled_structurally. birthday. Source readings: saang1jat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["白紙", { label: "lex", pos: "lexical_item", jyutping: "baak6 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1077, frequency 22; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *baak6zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["尖", { label: "lex", pos: "lexical_item", jyutping: "zim1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1078, frequency 22; prior audit status missing. point (of needle)/sharp/shrewd/pointed. Source readings: zim1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["年紀", { label: "lex", pos: "lexical_item", jyutping: "nin4 gei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1079, frequency 22; prior audit status missing. age. Source readings: nin4gei2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["男", { label: "lex", pos: "lexical_item", jyutping: "naam4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1081, frequency 22; prior audit status missing. male/Baron, lowest of five orders of nobility 五等爵位[ńgh dáng jeuk waih]. Source readings: naam4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["事實", { label: "lex", pos: "lexical_item", jyutping: "si6 sat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1082, frequency 22; prior audit status missing. a fact/the fact that. Source readings: si6sat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩間", { label: "lex", pos: "lexical_item", jyutping: "loeng5 gaan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1083, frequency 22; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *loeng5gaan1|gaan3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["尚", { label: "lex", pos: "lexical_item", jyutping: "soeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1084, frequency 22; prior audit status missing. still/yet/to value/to esteem/surname Shang. Source readings: soeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["於是", { label: "lex", pos: "lexical_item", jyutping: "jyu1 si6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1085, frequency 22; prior audit status missing. thereupon/as a result/consequently/thus/hence. Source readings: jyu1si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["版", { label: "lex", pos: "lexical_item", jyutping: "baan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1086, frequency 22; prior audit status missing. a register/a block of printing/an edition/version/page. Source readings: baan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["直到", { label: "lex", pos: "lexical_item", jyutping: "zik6 dou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1087, frequency 22; prior audit status handled_structurally. until. Source readings: zik6dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["空白", { label: "lex", pos: "lexical_item", jyutping: "hung1 baak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1088, frequency 22; prior audit status missing. blank space. Source readings: hung1baak6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["指", { label: "lex", pos: "lexical_item", jyutping: "zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1089, frequency 22; prior audit status missing. finger/to point/to direct/to indicate/to refer to/to rely on/to count on/(hair) stands stiffly on end. Source readings: zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["要點", { label: "lex", pos: "lexical_item", jyutping: "jiu3 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1090, frequency 22; prior audit status handled_structurally. main point/essential. Source readings: jiu3dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["負責", { label: "lex", pos: "lexical_item", jyutping: "fu6 zaak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1091, frequency 22; prior audit status missing. to be in charge of/to take responsibility for/to be to blame/conscientious. Source readings: fu6zaak3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["冤墓", { label: "lex", pos: "lexical_item", jyutping: "jyun1 mou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1092, frequency 22; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jyun1mou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["畫條", { label: "lex", pos: "lexical_item", jyutping: "waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1093, frequency 22; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *waak6|waa2tiu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["電子", { label: "lex", pos: "lexical_item", jyutping: "din6 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1094, frequency 22; prior audit status missing. electronic/electron. Source readings: din6zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["標", { label: "lex", pos: "lexical_item", jyutping: "biu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1095, frequency 22; prior audit status missing. the topmost branches of a tree/surface/sign/to mark/(outward) sign/indication/prize/award/bid. Source readings: biu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["談", { label: "lex", pos: "lexical_item", jyutping: "taam4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1096, frequency 22; prior audit status missing. to speak/to talk/to converse/to chat/to discuss/surname Tan. Source readings: taam4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講返", { label: "lex", pos: "lexical_item", jyutping: "gong2 faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1098, frequency 22; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2faan1|faan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["舊年", { label: "lex", pos: "lexical_item", jyutping: "gau6 nin2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1099, frequency 22; prior audit status handled_structurally. last year/the Chinese New Year (i.e. the new year in the old calendar). Source readings: gau6nin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["覆", { label: "lex", pos: "lexical_item", jyutping: "fuk1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1100, frequency 22; prior audit status missing. to cover/to overflow/to reply to a letter/to overturn/to capsize. Source readings: fuk1|fau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["九點", { label: "lex", pos: "lexical_item", jyutping: "gau2 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1102, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gau2dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["大約", { label: "lex", pos: "lexical_item", jyutping: "daai6 joek3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1103, frequency 21; prior audit status handled_structurally. approximately/about. Source readings: daai6joek3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["分開", { label: "lex", pos: "lexical_item", jyutping: "fan1 hoi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1104, frequency 21; prior audit status handled_structurally. to separate/to part. Source readings: fan1hoi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["升", { label: "lex", pos: "lexical_item", jyutping: "sing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1105, frequency 21; prior audit status missing. to raise/to hoist/to promote/metric liter (also written 公升)/measure for dry grain equal to one-tenth dou 斗. Source readings: sing1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["廿", { label: "lex", pos: "lexical_item", jyutping: "jaa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1106, frequency 21; prior audit status missing. twenty (20), in a limited number of set expressions/also written using banker's character 念. Source readings: jaa6|nim6|je6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["打個", { label: "lex", pos: "lexical_item", jyutping: "daa2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1107, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *daa2|daa1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["白色", { label: "lex", pos: "lexical_item", jyutping: "baak6 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1108, frequency 21; prior audit status missing. white/fig. reactionary. Source readings: baak6sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["行去", { label: "lex", pos: "lexical_item", jyutping: "hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1109, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hang4|haang4|hang6|hong4|hong2heoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["形狀", { label: "lex", pos: "lexical_item", jyutping: "jing4 zong6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1110, frequency 21; prior audit status missing. form/shape. Source readings: jing4zong6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我想問", { label: "lex", pos: "lexical_item", jyutping: "ngo5 soeng2 man6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1111, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5soeng2man6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["改變", { label: "lex", pos: "lexical_item", jyutping: "goi2 bin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1112, frequency 21; prior audit status handled_structurally. to change/to alter/to transform. Source readings: goi2bin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["私人", { label: "lex", pos: "lexical_item", jyutping: "si1 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1113, frequency 21; prior audit status missing. private (citizen)/private. Source readings: si1jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩個韻", { label: "lex", pos: "lexical_item", jyutping: "loeng5 go3 wan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1114, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *loeng5go3wan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["受山", { label: "lex", pos: "lexical_item", jyutping: "sau6 saan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1115, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sau6saan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["底下", { label: "lex", pos: "lexical_item", jyutping: "dai2 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1116, frequency 21; prior audit status missing. the location below sth/afterwards. Source readings: dai2haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["東行", { label: "lex", pos: "lexical_item", jyutping: "dung1 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1117, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dung1hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["長方形", { label: "lex", pos: "lexical_item", jyutping: "coeng4 fong1 jing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1118, frequency 21; prior audit status missing. rectangle. Source readings: coeng4fong1jing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["航", { label: "lex", pos: "lexical_item", jyutping: "hong4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1119, frequency 21; prior audit status missing. boat/ship/vessel/craft/to navigate/to sail. Source readings: hong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兜返", { label: "lex", pos: "lexical_item", jyutping: "dau1 faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1120, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dau1faan1|faan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["捱", { label: "lex", pos: "lexical_item", jyutping: "ngaai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1121, frequency 21; prior audit status missing. to suffer/to endure/to pull through (hard times)/to delay/to stall/to play for time/to dawdle. Source readings: ngaai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["終於", { label: "lex", pos: "lexical_item", jyutping: "zung1 jyu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1122, frequency 21; prior audit status missing. at last/in the end/finally/eventually. Source readings: zung1jyu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["處理", { label: "lex", pos: "lexical_item", jyutping: "cyu2 lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1123, frequency 21; prior audit status missing. to handle/to treat/to deal with/to process. Source readings: cyu2lei5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾耐", { label: "lex", pos: "lexical_item", jyutping: "gei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1125, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gei2|gei1noi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["準備", { label: "lex", pos: "lexical_item", jyutping: "zeon2 bei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1127, frequency 21; prior audit status missing. preparation/prepare. Source readings: zeon2bei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["解", { label: "lex", pos: "lexical_item", jyutping: "gaai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1128, frequency 21; prior audit status missing. surname Xie/to divide/to break up/to split/to separate/to dissolve/to solve/to melt/to remove/to untie/to loosen/to open/to emancipate/to explain/to understand/to know/a solution/a dissection/transport under guard. Source readings: gaai2|haai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["資產", { label: "lex", pos: "lexical_item", jyutping: "zi1 caan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1129, frequency 21; prior audit status missing. property/assets. Source readings: zi1caan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["預算案", { label: "lex", pos: "lexical_item", jyutping: "jyu6 syun3 on3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1130, frequency 21; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jyu6syun3on3|ngon3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["墓", { label: "lex", pos: "lexical_item", jyutping: "mou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1131, frequency 21; prior audit status missing. tomb. Source readings: mou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["維持", { label: "lex", pos: "lexical_item", jyutping: "wai4 ci4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1132, frequency 21; prior audit status missing. to keep/to maintain/to preserve. Source readings: wai4ci4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["舖", { label: "lex", pos: "lexical_item", jyutping: "pou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1133, frequency 21; prior audit status missing. a store. Source readings: pou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["機構", { label: "lex", pos: "lexical_item", jyutping: "gei1 kau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1134, frequency 21; prior audit status missing. mechanism/structure/organization/agency/institution. Source readings: gei1kau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["離開", { label: "lex", pos: "lexical_item", jyutping: "lei4 hoi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1135, frequency 21; prior audit status missing. to depart/to leave. Source readings: lei4hoi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["聽眾", { label: "lex", pos: "lexical_item", jyutping: "ting1 zung3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1136, frequency 21; prior audit status missing. audience/listeners. Source readings: ting1zung3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一方面", { label: "lex", pos: "lexical_item", jyutping: "jat1 fong1 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1137, frequency 20; prior audit status handled_structurally. one aspect is. Source readings: jat1fong1min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["七月", { label: "lex", pos: "lexical_item", jyutping: "cat1 jyut6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1138, frequency 20; prior audit status handled_structurally. July/seventh month. Source readings: cat1jyut6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["三年", { label: "lex", pos: "lexical_item", jyutping: "saam1 nin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1139, frequency 20; prior audit status handled_structurally. three years. Source readings: saam1nin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["仍然", { label: "lex", pos: "lexical_item", jyutping: "jing4 jin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1140, frequency 20; prior audit status missing. still/yet. Source readings: jing4jin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["手面", { label: "lex", pos: "lexical_item", jyutping: "sau2 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1141, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sau2min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["乎", { label: "lex", pos: "lexical_item", jyutping: "fu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1142, frequency 20; prior audit status missing. (final particle in classical Chinese, expressing question or doubt, similar to 嗎|吗). Source readings: fu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["以後", { label: "lex", pos: "lexical_item", jyutping: "ji5 hau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1143, frequency 20; prior audit status missing. after/later/afterwards/following/later on/in the future. Source readings: ji5hau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["北面", { label: "lex", pos: "lexical_item", jyutping: "bak1 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1144, frequency 20; prior audit status handled_structurally. northern side/north. Source readings: bak1min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["仲要", { label: "lex", pos: "lexical_item", jyutping: "zung6 jiu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1145, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zung6jiu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兆", { label: "lex", pos: "lexical_item", jyutping: "siu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1146, frequency 20; prior audit status missing. omen/million/million million, trillion. Source readings: siu6|ziu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["同個", { label: "lex", pos: "lexical_item", jyutping: "tung4 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1147, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tung4go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["地下", { label: "lex", pos: "lexical_item", jyutping: "dei6 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1148, frequency 20; prior audit status missing. underground/subterranean/covert|floor/ground. Source readings: dei6haa6|dei5haa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["地產", { label: "lex", pos: "lexical_item", jyutping: "dei6 caan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1149, frequency 20; prior audit status missing. estate. Source readings: dei6caan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["年代", { label: "lex", pos: "lexical_item", jyutping: "nin4 doi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1150, frequency 20; prior audit status missing. a decade of a century (e.g. the Sixties)/age/era/period. Source readings: nin4doi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["即話", { label: "lex", pos: "lexical_item", jyutping: "zik1 waa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1151, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zik1waa6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我同", { label: "lex", pos: "lexical_item", jyutping: "ngo5 tung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1152, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5tung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["身邊", { label: "lex", pos: "lexical_item", jyutping: "san1 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1153, frequency 20; prior audit status missing. at one's side. Source readings: san1bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["例子", { label: "lex", pos: "lexical_item", jyutping: "lai6 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1154, frequency 20; prior audit status missing. case/(for) instance/example. Source readings: lai6zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["爭", { label: "lex", pos: "lexical_item", jyutping: "zang1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1158, frequency 20; prior audit status missing. to strive for/to vie for/to argue or debate/deficient or lacking/how or what (literary). Source readings: zang1|zaang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["盈利", { label: "lex", pos: "lexical_item", jyutping: "jing4 lei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1159, frequency 20; prior audit status missing. profit/gain. Source readings: jing4lei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["值得", { label: "lex", pos: "lexical_item", jyutping: "zik6 dak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1160, frequency 20; prior audit status missing. to be worth/to deserve. Source readings: zik6dak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["哩度", { label: "lex", pos: "lexical_item", jyutping: "lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1161, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lei5|le1|lei1|li1dou6|dok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔肯", { label: "lex", pos: "lexical_item", jyutping: "m4 hang2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1162, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *m4hang2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["做咩", { label: "lex", pos: "lexical_item", jyutping: "zou6 me1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1163, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zou6me1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["健", { label: "lex", pos: "lexical_item", jyutping: "gin6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1164, frequency 20; prior audit status missing. healthy/to invigorate/to strengthen/to be good at/to be strong in. Source readings: gin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["夠", { label: "lex", pos: "lexical_item", jyutping: "gau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1165, frequency 20; prior audit status missing. to reach/to be enough. Source readings: gau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["控制", { label: "lex", pos: "lexical_item", jyutping: "hung3 zai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1166, frequency 20; prior audit status missing. control/to exercise control over/to contain. Source readings: hung3zai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["被", { label: "lex", pos: "lexical_item", jyutping: "bei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1167, frequency 20; prior audit status missing. by (indicates passive-voice sentences or clauses)/to cover/to wear. Source readings: bei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["報", { label: "lex", pos: "lexical_item", jyutping: "bou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1168, frequency 20; prior audit status missing. to announce/to inform/report/newspaper/recompense/revenge. Source readings: bou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["結婚", { label: "lex", pos: "lexical_item", jyutping: "git3 fan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1169, frequency 20; prior audit status missing. to marry/to get married. Source readings: git3fan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["順", { label: "lex", pos: "lexical_item", jyutping: "seon6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1170, frequency 20; prior audit status missing. to obey/to follow/to arrange/to make reasonable/along/favorable. Source readings: seon6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["煙海", { label: "lex", pos: "lexical_item", jyutping: "jin1 hoi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1171, frequency 20; prior audit status handled_structurally. the vast ocean. Source readings: jin1hoi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["聖經", { label: "lex", pos: "lexical_item", jyutping: "sing3 ging1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1172, frequency 20; prior audit status missing. Holy Bible/the Confucian classics. Source readings: sing3ging1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["補習", { label: "lex", pos: "lexical_item", jyutping: "bou2 zaap6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1173, frequency 20; prior audit status missing. to take extra lessons/to cram (for a test). Source readings: bou2zaap6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["圖形", { label: "lex", pos: "lexical_item", jyutping: "tou4 jing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1175, frequency 20; prior audit status handled_structurally. picture/figure/diagram/graph/depiction/graphical. Source readings: tou4jing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["實在", { label: "lex", pos: "lexical_item", jyutping: "sat6 zoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1176, frequency 20; prior audit status missing. in reality/honestly/really/verily/concrete. Source readings: sat6zoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["精神", { label: "lex", pos: "lexical_item", jyutping: "zing1 san4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1178, frequency 20; prior audit status missing. spirit/mind/consciousness/thought/mental/psychological/essence/gist. Source readings: zing1san4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["暫時", { label: "lex", pos: "lexical_item", jyutping: "zaam6 si4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1179, frequency 20; prior audit status missing. temporary/provisional. Source readings: zaam6si4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["賠", { label: "lex", pos: "lexical_item", jyutping: "pui4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1180, frequency 20; prior audit status missing. to compensate for loss/to indemnify/to suffer a financial loss. Source readings: pui4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["整個", { label: "lex", pos: "lexical_item", jyutping: "zing2 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1182, frequency 20; prior audit status handled_structurally. whole/entire/total. Source readings: zing2go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講乜", { label: "lex", pos: "lexical_item", jyutping: "gong2 mat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1183, frequency 20; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2mat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["闆", { label: "lex", pos: "lexical_item", jyutping: "baan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1184, frequency 20; prior audit status missing. see 老闆|老板, boss. Source readings: baan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["叢", { label: "lex", pos: "lexical_item", jyutping: "cung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1185, frequency 20; prior audit status missing. cluster/collection/collection of books/thicket. Source readings: cung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["競爭", { label: "lex", pos: "lexical_item", jyutping: "ging6 zang1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1186, frequency 20; prior audit status missing. to compete/competition. Source readings: ging6zang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["公", { label: "lex", pos: "lexical_item", jyutping: "gung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1187, frequency 19; prior audit status missing. public/collectively owned/common/international (e.g. high seas, metric system, calendar)/make public/fair/just/Duke, highest of five orders of nobility 五等爵位[ńgh dáng jeuk waih]/honorable (gentlemen)/father-in-law/male (a. Source readings: gung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["只要", { label: "lex", pos: "lexical_item", jyutping: "zi2 jiu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1188, frequency 19; prior audit status missing. if only/so long as. Source readings: zi2jiu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右下角", { label: "lex", pos: "lexical_item", jyutping: "jau6 haa6 gok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1189, frequency 19; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau6haa6gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["失業", { label: "lex", pos: "lexical_item", jyutping: "sat1 jip6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1190, frequency 19; prior audit status missing. unemployment. Source readings: sat1jip6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["正下方", { label: "lex", pos: "lexical_item", jyutping: "zing3 haa6 fong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1191, frequency 19; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zing3haa6fong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["民", { label: "lex", pos: "lexical_item", jyutping: "man4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1192, frequency 19; prior audit status missing. the people/nationality/citizen. Source readings: man4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["先算", { label: "lex", pos: "lexical_item", jyutping: "sin1 syun3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1193, frequency 19; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sin1syun3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好話", { label: "lex", pos: "lexical_item", jyutping: "hou2 waa2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1195, frequency 19; prior audit status handled_structurally. words of praise/to speak well of. Source readings: hou2waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["你點", { label: "lex", pos: "lexical_item", jyutping: "nei5 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1196, frequency 19; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *nei5dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["形式", { label: "lex", pos: "lexical_item", jyutping: "jing4 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1197, frequency 19; prior audit status missing. form/shape/situation/circumstance. Source readings: jing4sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["例", { label: "lex", pos: "lexical_item", jyutping: "lai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1198, frequency 19; prior audit status missing. example/precedent/rule/case/instance. Source readings: lai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["到底", { label: "lex", pos: "lexical_item", jyutping: "dou3 dai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1199, frequency 19; prior audit status missing. finally/in the end/when all is said and done. Source readings: dou3dai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["抹", { label: "lex", pos: "lexical_item", jyutping: "maat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1200, frequency 19; prior audit status missing. to wipe/to forget/to wipe out a memory/classifier for wisps of cloud, light-beams etc (Mandarin). Source readings: maat3|mut3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["的", { label: "lex", pos: "lexical_item", jyutping: "dik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1201, frequency 19; prior audit status missing. of/~'s (possessive particle)/(used after an attribute)/(used to form a nominal expression)/(used at the end of a declarative sentence for emphasis)/to fetch. Source readings: dik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["保障", { label: "lex", pos: "lexical_item", jyutping: "bou2 zoeng3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1202, frequency 19; prior audit status missing. to ensure/to guarantee/to safeguard. Source readings: bou2zoeng3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["張紙", { label: "lex", pos: "lexical_item", jyutping: "zoeng1 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1203, frequency 19; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zoeng1zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["想講", { label: "lex", pos: "lexical_item", jyutping: "soeng2 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1206, frequency 19; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *soeng2gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["溝通", { label: "lex", pos: "lexical_item", jyutping: "kau1 tung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1207, frequency 19; prior audit status missing. communicate. Source readings: kau1tung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["道", { label: "lex", pos: "lexical_item", jyutping: "dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1208, frequency 19; prior audit status missing. direction/way/road/path/principle/truth/morality/reason/skill/method/Dao (of Daoism)/to say/to speak/to talk. Source readings: dou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["零", { label: "lex", pos: "lexical_item", jyutping: "ling4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1209, frequency 19; prior audit status missing. remnant/zero. Source readings: ling4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["電視機", { label: "lex", pos: "lexical_item", jyutping: "din6 si6 gei1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1210, frequency 19; prior audit status handled_structurally. television set/CL: 臺|台[tai2]. Source readings: din6si6gei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["預備", { label: "lex", pos: "lexical_item", jyutping: "jyu6 bei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1211, frequency 19; prior audit status missing. to prepare/to make ready/preparation/preparatory. Source readings: jyu6bei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["噏", { label: "lex", pos: "lexical_item", jyutping: "ap1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1215, frequency 19; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: ap1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["頹", { label: "lex", pos: "lexical_item", jyutping: "teoi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1216, frequency 19; prior audit status missing. to crumble/to collapse/to decline/to decay/decadent/dejected/dispirited/balding. Source readings: teoi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一個韻", { label: "lex", pos: "lexical_item", jyutping: "jat1 go3 wan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1218, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1go3wan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["三角形", { label: "lex", pos: "lexical_item", jyutping: "saam1 gok3 jing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1219, frequency 18; prior audit status handled_structurally. triangle. Source readings: saam1gok3jing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["分手", { label: "lex", pos: "lexical_item", jyutping: "fan1 sau2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1220, frequency 18; prior audit status handled_structurally. to split up/to break up. Source readings: fan1sau2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["反應", { label: "lex", pos: "lexical_item", jyutping: "faan2 jing3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1221, frequency 18; prior audit status missing. to react/to respond/reaction/response/reply/chemical reaction. Source readings: faan2jing3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右下", { label: "lex", pos: "lexical_item", jyutping: "jau6 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1223, frequency 18; prior audit status handled_structurally. lower right. Source readings: jau6haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右手邊", { label: "lex", pos: "lexical_item", jyutping: "jau6 sau2 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1224, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: jau6sau2bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["市民", { label: "lex", pos: "lexical_item", jyutping: "si5 man4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1225, frequency 18; prior audit status missing. city resident. Source readings: si5man4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["回歸", { label: "lex", pos: "lexical_item", jyutping: "wui4 gwai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1226, frequency 18; prior audit status missing. to return (to previous condition)/Hong Kong's return to Chinese sovereignty/to regress. Source readings: wui4gwai1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成功", { label: "lex", pos: "lexical_item", jyutping: "sing4 gung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1227, frequency 18; prior audit status missing. success/to succeed. Source readings: sing4gung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有人", { label: "lex", pos: "lexical_item", jyutping: "jau5 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1228, frequency 18; prior audit status handled_structurally. someone/people/anyone/there is someone there/occupied (as in restroom). Source readings: jau5jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["男性", { label: "lex", pos: "lexical_item", jyutping: "naam4 sing3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1231, frequency 18; prior audit status missing. a male. Source readings: naam4sing3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["性格", { label: "lex", pos: "lexical_item", jyutping: "sing3 gaak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1232, frequency 18; prior audit status handled_structurally. nature/disposition/temperament/character. Source readings: sing3gaak3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["放假", { label: "lex", pos: "lexical_item", jyutping: "fong3 gaa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1233, frequency 18; prior audit status handled_structurally. to have a holiday or vacation. Source readings: fong3gaa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["直頭", { label: "lex", pos: "lexical_item", jyutping: "zik6 tau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1234, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zik6tau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["返上", { label: "lex", pos: "lexical_item", jyutping: "faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1235, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *faan1|faan2soeng6|soeng5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["南方", { label: "lex", pos: "lexical_item", jyutping: "naam4 fong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1236, frequency 18; prior audit status missing. south/the southern part of the country/the South. Source readings: naam4fong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["是但", { label: "lex", pos: "lexical_item", jyutping: "si6 daan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1237, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: si6daan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["相當", { label: "lex", pos: "lexical_item", jyutping: "soeng1 dong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1238, frequency 18; prior audit status missing. equivalent to/appropriate/considerably/to a certain extent/fairly. Source readings: soeng1dong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["英國", { label: "lex", pos: "lexical_item", jyutping: "jing1 gwok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1239, frequency 18; prior audit status missing. United Kingdom 聯合王國|联合王国[Lian2 he2 wang2 guo2]/United Kingdom of Great Britain and Northern Ireland/abbr. for England 英格蘭|英格兰[Ying1 ge2 lan2]. Source readings: jing1gwok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["要好", { label: "lex", pos: "lexical_item", jyutping: "jiu3 hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1240, frequency 18; prior audit status handled_structurally. be on good terms/be close friends. Source readings: jiu3hou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["俾人", { label: "lex", pos: "lexical_item", jyutping: "bei2 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1241, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *bei2jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["原則", { label: "lex", pos: "lexical_item", jyutping: "jyun4 zak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1242, frequency 18; prior audit status missing. principle/doctrine. Source readings: jyun4zak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["埋個", { label: "lex", pos: "lexical_item", jyutping: "maai4 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1243, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *maai4go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["拿", { label: "lex", pos: "lexical_item", jyutping: "naa4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1244, frequency 18; prior audit status missing. to hold/to seize/to catch/to apprehend/to take. Source readings: naa4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["旁邊", { label: "lex", pos: "lexical_item", jyutping: "pong4 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1245, frequency 18; prior audit status missing. lateral/side/to the side/beside. Source readings: pong4bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["留", { label: "lex", pos: "lexical_item", jyutping: "lau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1246, frequency 18; prior audit status missing. leave (message)/to retain/to stay/to remain/to keep/to preserve. Source readings: lau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["追", { label: "lex", pos: "lexical_item", jyutping: "zeoi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1248, frequency 18; prior audit status missing. to chase after/to seek/to do one's utmost to seek or procure sth/to recall. Source readings: zeoi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["做過", { label: "lex", pos: "lexical_item", jyutping: "zou6 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1249, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zou6gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["得罪", { label: "lex", pos: "lexical_item", jyutping: "dak1 zeoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1250, frequency 18; prior audit status missing. to offend sb/a faux pas. Source readings: dak1zeoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["畢業", { label: "lex", pos: "lexical_item", jyutping: "bat1 jip6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1251, frequency 18; prior audit status missing. graduation/to graduate/to finish school. Source readings: bat1jip6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["剩", { label: "lex", pos: "lexical_item", jyutping: "zing6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1252, frequency 18; prior audit status missing. to remain/to be left/to have as remainder. Source readings: zing6|sing6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾大", { label: "lex", pos: "lexical_item", jyutping: "gei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1253, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gei2|gei1daai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["提供", { label: "lex", pos: "lexical_item", jyutping: "tai4 gung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1254, frequency 18; prior audit status missing. to supply/to provide/to furnish. Source readings: tai4gung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["象徵", { label: "lex", pos: "lexical_item", jyutping: "zoeng6 zing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1255, frequency 18; prior audit status missing. emblem/symbol/token/badge/to symbolize/to signify/to stand for. Source readings: zoeng6zing1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["感情", { label: "lex", pos: "lexical_item", jyutping: "gam2 cing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1256, frequency 18; prior audit status missing. feeling/emotion/sensation/likes and dislikes/deep affection for sb or sth/relationship (i.e. love affair). Source readings: gam2cing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["試下", { label: "lex", pos: "lexical_item", jyutping: "si3 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1259, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *si3haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["熄", { label: "lex", pos: "lexical_item", jyutping: "sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1260, frequency 18; prior audit status missing. to extinguish/to put out (fire)/to quench/to stop burning/to go out (of fire, lamp etc)/to come to an end/to wither away/to die out. Source readings: sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["廠", { label: "lex", pos: "lexical_item", jyutping: "cong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1261, frequency 18; prior audit status missing. factory/hospital. Source readings: cong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["靚女", { label: "lex", pos: "lexical_item", jyutping: "leng3 neoi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1264, frequency 18; prior audit status handled_structurally. pretty girl. Source readings: leng3neoi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["儲", { label: "lex", pos: "lexical_item", jyutping: "cyu5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1265, frequency 18; prior audit status missing. savings/to save/to deposit/to store/surname Chu/Taiwan pr. chu2. Source readings: cyu5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["應", { label: "lex", pos: "lexical_item", jyutping: "jing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1266, frequency 18; prior audit status missing. ought/surname Ying/to answer/to respond. Source readings: jing1|jing3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["擦膠", { label: "lex", pos: "lexical_item", jyutping: "caat3 gaau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1268, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: caat3gaau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["禮物", { label: "lex", pos: "lexical_item", jyutping: "lai5 mat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1269, frequency 18; prior audit status missing. gift/present. Source readings: lai5mat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["雞", { label: "lex", pos: "lexical_item", jyutping: "gai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1270, frequency 18; prior audit status missing. fowl/chicken/CL:隻[jek]/a prostitute/ten thousand dollars/the trigger of a gun/a whistle. Source readings: gai1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["鐘意", { label: "lex", pos: "lexical_item", jyutping: "zung1 ji3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1272, frequency 18; prior audit status handled_structurally. like. Source readings: zung1ji3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["權", { label: "lex", pos: "lexical_item", jyutping: "kyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1273, frequency 18; prior audit status missing. authority/power/right. Source readings: kyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["鑑林", { label: "lex", pos: "lexical_item", jyutping: "gaam3 lam4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1274, frequency 18; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gaam3lam4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["三點", { label: "lex", pos: "lexical_item", jyutping: "saam1 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1275, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: saam1dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["下邊", { label: "lex", pos: "lexical_item", jyutping: "haa6 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1276, frequency 17; prior audit status handled_structurally. under/the underside/below. Source readings: haa6bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["公眾", { label: "lex", pos: "lexical_item", jyutping: "gung1 zung3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1277, frequency 17; prior audit status missing. public. Source readings: gung1zung3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["太過", { label: "lex", pos: "lexical_item", jyutping: "taai3 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1278, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *taai3gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["毋", { label: "lex", pos: "lexical_item", jyutping: "mou4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1279, frequency 17; prior audit status missing. no/not. Source readings: mou4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["四十五度", { label: "lex", pos: "lexical_item", jyutping: "sei3 sap6 m5 dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1280, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sei3sap6m5dou6|dok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左下", { label: "lex", pos: "lexical_item", jyutping: "zo2 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1281, frequency 17; prior audit status handled_structurally. lower left. Source readings: zo2haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["平排", { label: "lex", pos: "lexical_item", jyutping: "ping4 paai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1282, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: ping4paai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["必須", { label: "lex", pos: "lexical_item", jyutping: "bit1 seoi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1283, frequency 17; prior audit status missing. to have to/must. Source readings: bit1seoi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["打直", { label: "lex", pos: "lexical_item", jyutping: "daa2 zik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1284, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: daa2zik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["打斜", { label: "lex", pos: "lexical_item", jyutping: "daa2 ce3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1285, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: daa2ce3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["民主黨", { label: "lex", pos: "lexical_item", jyutping: "man4 zyu2 dong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1286, frequency 17; prior audit status missing. Democratic Party. Source readings: man4zyu2dong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["任", { label: "lex", pos: "lexical_item", jyutping: "jam6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1287, frequency 17; prior audit status missing. to assign/to appoint/to take up a post/office/responsibility/to let/to allow/to give free rein to/no matter (how, what etc)/surname Ren. Source readings: jam6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["再落", { label: "lex", pos: "lexical_item", jyutping: "zoi3 lok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1288, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zoi3lok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["多數", { label: "lex", pos: "lexical_item", jyutping: "do1 sou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1289, frequency 17; prior audit status missing. majority/most. Source readings: do1sou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好大", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1290, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3daai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好慘", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1291, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3caam2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["收到", { label: "lex", pos: "lexical_item", jyutping: "sau1 dou3", syntax: "lexical_item", note: `Cifu SpokenAdult rank 1292, frequency 17; prior audit status missing. to receive|used to say that one has understood, "message received!"; "understood!"; "I get what you're saying!"/to receive (colloquial). Source readings: sau1dou3|sau1dou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate.` }],
      ["有機", { label: "lex", pos: "lexical_item", jyutping: "jau5 gei1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1293, frequency 17; prior audit status handled_structurally. organic. Source readings: jau5gei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["告", { label: "lex", pos: "lexical_item", jyutping: "gou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1294, frequency 17; prior audit status missing. to sue/to tell/to inform/to say. Source readings: gou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我估", { label: "lex", pos: "lexical_item", jyutping: "ngo5 gu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1295, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5gu2|gu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我畫", { label: "lex", pos: "lexical_item", jyutping: "ngo5 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1296, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5waak6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["言論", { label: "lex", pos: "lexical_item", jyutping: "jin4 leon6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1297, frequency 17; prior audit status missing. expression of (political) opinion. Source readings: jin4leon6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["受到", { label: "lex", pos: "lexical_item", jyutping: "sau6 dou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1298, frequency 17; prior audit status missing. to receive/to suffer/obtained/given. Source readings: sau6dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["咁上下", { label: "lex", pos: "lexical_item", jyutping: "gam3 soeng6 haa2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1299, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: gam3soeng6haa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["波浪", { label: "lex", pos: "lexical_item", jyutping: "bo1 long6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1300, frequency 17; prior audit status missing. wave. Source readings: bo1long6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["知有", { label: "lex", pos: "lexical_item", jyutping: "zi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1301, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zi1|zi3jau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["阿哥", { label: "lex", pos: "lexical_item", jyutping: "aa3 go1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1302, frequency 17; prior audit status handled_structurally. (familiar) elder brother. Source readings: aa3go1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["後生", { label: "lex", pos: "lexical_item", jyutping: "hau6 saang1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1303, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hau6saang1|sang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["計劃", { label: "lex", pos: "lexical_item", jyutping: "gai3 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1305, frequency 17; prior audit status missing. plan/project/program/to plan/to map out. Source readings: gai3waak6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個害", { label: "lex", pos: "lexical_item", jyutping: "go3 hoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1306, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *go3hoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["租", { label: "lex", pos: "lexical_item", jyutping: "zou1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1308, frequency 17; prior audit status missing. rent/taxes. Source readings: zou1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["純粹", { label: "lex", pos: "lexical_item", jyutping: "seon4 seoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1309, frequency 17; prior audit status missing. purely. Source readings: seon4seoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["高度", { label: "lex", pos: "lexical_item", jyutping: "gou1 dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1310, frequency 17; prior audit status handled_structurally. height/altitude/elevation/high degree/highly. Source readings: gou1dou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["基本法", { label: "lex", pos: "lexical_item", jyutping: "gei1 bun2 faat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1311, frequency 17; prior audit status missing. Basic Law. Source readings: gei1bun2faat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["將個", { label: "lex", pos: "lexical_item", jyutping: "zoeng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1312, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zoeng1|zoeng3go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["理論", { label: "lex", pos: "lexical_item", jyutping: "lei5 leon6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1313, frequency 17; prior audit status missing. theory. Source readings: lei5leon6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["處", { label: "lex", pos: "lexical_item", jyutping: "cyu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1314, frequency 17; prior audit status missing. place/location/spot/point/office/department/bureau/respect/one's virginity/classifier for locations or items of damage: spot, point/to reside/to live/to dwell/to be in/to be situated at/to stay/to get along with/to be in. Source readings: cyu2|cyu3|syu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["喊", { label: "lex", pos: "lexical_item", jyutping: "haam3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1315, frequency 17; prior audit status missing. to cry/to yell/to shout/to call out for (a person). Source readings: haam3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["圍住", { label: "lex", pos: "lexical_item", jyutping: "wai4 zyu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1316, frequency 17; prior audit status missing. to surround/to gird. Source readings: wai4zyu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["尊嚴", { label: "lex", pos: "lexical_item", jyutping: "zyun1 jim4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1317, frequency 17; prior audit status missing. dignity/sanctity/honor. Source readings: zyun1jim4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["照", { label: "lex", pos: "lexical_item", jyutping: "ziu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1319, frequency 17; prior audit status missing. according to/in accordance with/to shine/to illuminate/to reflect/to take (a photo)/photo/as requested/as before/to protect/to support/to back-up. Source readings: ziu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["落落", { label: "lex", pos: "lexical_item", jyutping: "lok6 lok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1320, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lok6lok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["資料", { label: "lex", pos: "lexical_item", jyutping: "zi1 liu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1321, frequency 17; prior audit status missing. material/resources/data/information. Source readings: zi1liu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["審", { label: "lex", pos: "lexical_item", jyutping: "sam2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1322, frequency 17; prior audit status missing. to examine/to investigate/carefully/to try (in court). Source readings: sam2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["撳", { label: "lex", pos: "lexical_item", jyutping: "gam6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1323, frequency 17; prior audit status missing. 1. to press down/2. to oppress, to keep someone down/3. to extort money out of someone/4. to settle an arguement, to dampen down a quarrel. Source readings: gam6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["複雜", { label: "lex", pos: "lexical_item", jyutping: "fuk1 zaap6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1324, frequency 17; prior audit status missing. complicated/complex. Source readings: fuk1zaap6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["輪", { label: "lex", pos: "lexical_item", jyutping: "leon4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1326, frequency 17; prior audit status missing. wheel/disk/ring/steamship/to take turns/to rotate/by turn/classifier for big round objects: disk, or recurring events: round, turn. Source readings: leon4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["靚仔", { label: "lex", pos: "lexical_item", jyutping: "leng3 zai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1328, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *leng3zai2|zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["瞭解", { label: "lex", pos: "lexical_item", jyutping: "liu5 gaai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1330, frequency 17; prior audit status missing. to understand/to realize/to find out. Source readings: liu5gaai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["邊行", { label: "lex", pos: "lexical_item", jyutping: "bin1 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1331, frequency 17; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *bin1hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["權利", { label: "lex", pos: "lexical_item", jyutping: "kyun4 lei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1332, frequency 17; prior audit status missing. power/right/privilege. Source readings: kyun4lei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一晚", { label: "lex", pos: "lexical_item", jyutping: "jat1 maan5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1333, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1maan5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一點", { label: "lex", pos: "lexical_item", jyutping: "jat1 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1334, frequency 16; prior audit status handled_structurally. a bit/a little/one dot/one point. Source readings: jat1dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["人士", { label: "lex", pos: "lexical_item", jyutping: "jan4 si6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1335, frequency 16; prior audit status missing. person/figure/public figure. Source readings: jan4si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["上角", { label: "lex", pos: "lexical_item", jyutping: "soeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1336, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *soeng6|soeng5gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["上邊", { label: "lex", pos: "lexical_item", jyutping: "soeng6 bin6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1337, frequency 16; prior audit status handled_structurally. the top/above/overhead/upwards/the top margin/above-mentioned/those higher up/mainland China/the Beijing authorities. Source readings: soeng6bin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["下下", { label: "lex", pos: "lexical_item", jyutping: "haa6 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1338, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *haa6haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["已", { label: "lex", pos: "lexical_item", jyutping: "ji5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1339, frequency 16; prior audit status missing. already/to stop/then/afterwards. Source readings: ji5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["不嬲", { label: "lex", pos: "lexical_item", jyutping: "bat1 nau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1340, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *bat1nau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["公平", { label: "lex", pos: "lexical_item", jyutping: "gung1 ping4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1341, frequency 16; prior audit status missing. fair/impartial. Source readings: gung1ping4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["冇所謂", { label: "lex", pos: "lexical_item", jyutping: "mou5 so2 wai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1342, frequency 16; prior audit status missing. don't care. Source readings: mou5so2wai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["失敗", { label: "lex", pos: "lexical_item", jyutping: "sat1 baai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1343, frequency 16; prior audit status missing. to be defeated/to fail/to lose. Source readings: sat1baai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["合格", { label: "lex", pos: "lexical_item", jyutping: "hap6 gaak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1345, frequency 16; prior audit status missing. qualified/eligible (voter). Source readings: hap6gaak3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["安排", { label: "lex", pos: "lexical_item", jyutping: "on1 paai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1346, frequency 16; prior audit status missing. to arrange/to plan/to set up. Source readings: on1paai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成張", { label: "lex", pos: "lexical_item", jyutping: "sing4 zoeng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1347, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sing4zoeng1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["自", { label: "lex", pos: "lexical_item", jyutping: "zi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1349, frequency 16; prior audit status missing. from/self/oneself/since. Source readings: zi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["似乎", { label: "lex", pos: "lexical_item", jyutping: "ci5 fu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1350, frequency 16; prior audit status missing. apparently/to seem/to appear/as if/seemingly. Source readings: ci5fu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["即個", { label: "lex", pos: "lexical_item", jyutping: "zik1 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1351, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zik1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["吸引", { label: "lex", pos: "lexical_item", jyutping: "kap1 jan5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1352, frequency 16; prior audit status missing. to attract (interest, investment etc). Source readings: kap1jan5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["更", { label: "lex", pos: "lexical_item", jyutping: "gang3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1353, frequency 16; prior audit status missing. more/even more/further/still/still more/to change. Source readings: gang3|gang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["垂直", { label: "lex", pos: "lexical_item", jyutping: "seoi4 zik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1354, frequency 16; prior audit status missing. perpendicular/vertical. Source readings: seoi4zik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["奇怪", { label: "lex", pos: "lexical_item", jyutping: "kei4 gwaai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1355, frequency 16; prior audit status missing. strange/odd. Source readings: kei4gwaai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["空間", { label: "lex", pos: "lexical_item", jyutping: "hung1 gaan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1356, frequency 16; prior audit status missing. space. Source readings: hung1gaan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["阿婆", { label: "lex", pos: "lexical_item", jyutping: "aa3 po4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1357, frequency 16; prior audit status missing. an old woman/maternal grandmother. Source readings: aa3po4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["信佳", { label: "lex", pos: "lexical_item", jyutping: "seon3 gaai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1358, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *seon3gaai1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["咬", { label: "lex", pos: "lexical_item", jyutping: "ngaau5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1359, frequency 16; prior audit status missing. to bite/to nip. Source readings: ngaau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["城堡", { label: "lex", pos: "lexical_item", jyutping: "sing4 bou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1360, frequency 16; prior audit status missing. castle/rook (chess piece). Source readings: sing4bou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["政黨", { label: "lex", pos: "lexical_item", jyutping: "zing3 dong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1361, frequency 16; prior audit status missing. political party. Source readings: zing3dong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["冤", { label: "lex", pos: "lexical_item", jyutping: "jyun1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1362, frequency 16; prior audit status missing. injustice/grievance/wrong. Source readings: jyun1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["效", { label: "lex", pos: "lexical_item", jyutping: "haau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1363, frequency 16; prior audit status missing. effect/efficacy/imitate. Source readings: haau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["問我", { label: "lex", pos: "lexical_item", jyutping: "man6 ngo5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1366, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *man6ngo5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["基本", { label: "lex", pos: "lexical_item", jyutping: "gei1 bun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1367, frequency 16; prior audit status missing. basic/fundamental/main/elementary. Source readings: gei1bun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["教育", { label: "lex", pos: "lexical_item", jyutping: "gaau3 juk6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1368, frequency 16; prior audit status missing. to educate/to teach/education. Source readings: gaau3juk6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["殺", { label: "lex", pos: "lexical_item", jyutping: "saat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1369, frequency 16; prior audit status missing. to kill/to murder/to fight. Source readings: saat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["毫子", { label: "lex", pos: "lexical_item", jyutping: "hou4 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1370, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou4zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["深", { label: "lex", pos: "lexical_item", jyutping: "sam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1371, frequency 16; prior audit status missing. deep/profound. Source readings: sam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["雀仔", { label: "lex", pos: "lexical_item", jyutping: "zoek3 zai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1372, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zoek3zai2|zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["創作", { label: "lex", pos: "lexical_item", jyutping: "cong3 zok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1373, frequency 16; prior audit status missing. to create/to produce/to write/creative work/creation. Source readings: cong3zok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾喇", { label: "lex", pos: "lexical_item", jyutping: "gei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1374, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gei2|gei1laa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["朝頭早", { label: "lex", pos: "lexical_item", jyutping: "ziu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1375, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ziu1|ciu4tau4zou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["無端端", { label: "lex", pos: "lexical_item", jyutping: "mou4 dyun1 dyun1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1376, frequency 16; prior audit status missing. for no reason. Source readings: mou4dyun1dyun1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["越來", { label: "lex", pos: "lexical_item", jyutping: "jyut6 loi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1377, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jyut6loi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["黃", { label: "lex", pos: "lexical_item", jyutping: "wong4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1378, frequency 16; prior audit status missing. yellow/sulfur/surname Wong. Source readings: wong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["搞錯", { label: "lex", pos: "lexical_item", jyutping: "gaau2 co3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1379, frequency 16; prior audit status handled_structurally. mistake/to make a mistake/to blunder/mistaken. Source readings: gaau2co3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["資訊", { label: "lex", pos: "lexical_item", jyutping: "zi1 seon3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1380, frequency 16; prior audit status missing. information. Source readings: zi1seon3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["對方", { label: "lex", pos: "lexical_item", jyutping: "deoi3 fong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1381, frequency 16; prior audit status missing. counterpart/other person involved/opposite side/other side/receiving party. Source readings: deoi3fong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["語言", { label: "lex", pos: "lexical_item", jyutping: "jyu5 jin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1382, frequency 16; prior audit status missing. (spoken) language. Source readings: jyu5jin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["整體", { label: "lex", pos: "lexical_item", jyutping: "zing2 tai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1383, frequency 16; prior audit status missing. whole entity/entire body/synthesis/as a whole (situation, construction, team etc)/global/macrocosm/integral/holistic/whole. Source readings: zing2tai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["醒", { label: "lex", pos: "lexical_item", jyutping: "sing2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1384, frequency 16; prior audit status missing. to wake up/to awaken/to be awake/to give, to present, to let (someone) have (guidance, money, a blow). Source readings: sing2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["環保", { label: "lex", pos: "lexical_item", jyutping: "waan4 bou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1385, frequency 16; prior audit status missing. environmental protection. Source readings: waan4bou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["總", { label: "lex", pos: "lexical_item", jyutping: "zung2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1386, frequency 16; prior audit status missing. always/to assemble/gather/total/overall/head/chief/general/in every case. Source readings: zung2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講個", { label: "lex", pos: "lexical_item", jyutping: "gong2 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1387, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["轉個", { label: "lex", pos: "lexical_item", jyutping: "zyun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1388, frequency 16; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zyun2|zyun3go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["證明", { label: "lex", pos: "lexical_item", jyutping: "zing3 ming4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1389, frequency 16; prior audit status missing. proof/testimony/to prove/to confirm the truth of. Source readings: zing3ming4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["黨", { label: "lex", pos: "lexical_item", jyutping: "dong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1390, frequency 16; prior audit status missing. party/association/club/society/surname Dang. Source readings: dong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["攪", { label: "lex", pos: "lexical_item", jyutping: "gaau2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1391, frequency 16; prior audit status missing. to disturb/to annoy/to mix/to stir. Source readings: gaau2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一次過", { label: "lex", pos: "lexical_item", jyutping: "jat1 ci3 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1392, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1ci3gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一百蚊", { label: "lex", pos: "lexical_item", jyutping: "jat1 baak3 man1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1393, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1baak3man1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["乜鬼", { label: "lex", pos: "lexical_item", jyutping: "mat1 gwai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1394, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *mat1gwai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["上年", { label: "lex", pos: "lexical_item", jyutping: "soeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1395, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *soeng6|soeng5nin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["不斷", { label: "lex", pos: "lexical_item", jyutping: "bat1 tyun5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1396, frequency 15; prior audit status missing. unceasing/uninterrupted/continuous/constant. Source readings: bat1tyun5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["之類", { label: "lex", pos: "lexical_item", jyutping: "zi1 leoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1397, frequency 15; prior audit status missing. and so on/and such. Source readings: zi1leoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["冇用", { label: "lex", pos: "lexical_item", jyutping: "mou5 jung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1398, frequency 15; prior audit status handled_structurally. useless. Source readings: mou5jung6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["片", { label: "lex", pos: "lexical_item", jyutping: "pin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1399, frequency 15; prior audit status missing. thin piece/flake/a slice/film/TV play/to slice/to carve thin/partial/incomplete/one-sided/classifier for slices, tablets, tract of land, area of water/classifier for CDs, movies, DVDs etc (Mandarin)/used with numeral 一[y. Source readings: pin3|pin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右下方", { label: "lex", pos: "lexical_item", jyutping: "jau6 haa6 fong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1400, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: jau6haa6fong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["左手邊", { label: "lex", pos: "lexical_item", jyutping: "zo2 sau2 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1401, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zo2sau2bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["印椰樹", { label: "lex", pos: "lexical_item", jyutping: "jan3 je4 syu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1402, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jan3je4syu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["地點", { label: "lex", pos: "lexical_item", jyutping: "dei6 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1403, frequency 15; prior audit status missing. place/site/location/venue. Source readings: dei6dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["多少", { label: "lex", pos: "lexical_item", jyutping: "do1 siu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1404, frequency 15; prior audit status handled_structurally. number/amount/somewhat. Source readings: do1siu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["多次", { label: "lex", pos: "lexical_item", jyutping: "do1 ci3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1405, frequency 15; prior audit status handled_structurally. many times/repeatedly. Source readings: do1ci3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["多個", { label: "lex", pos: "lexical_item", jyutping: "do1 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1406, frequency 15; prior audit status handled_structurally. many/multiple/multi- (faceted, ethnic etc). Source readings: do1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好處", { label: "lex", pos: "lexical_item", jyutping: "hou2 cyu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1407, frequency 15; prior audit status missing. benefit; advantage/gain; profit/also pr. hao3 chu4. Source readings: hou2cyu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["存在", { label: "lex", pos: "lexical_item", jyutping: "cyun4 zoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1408, frequency 15; prior audit status missing. to exist/to be. Source readings: cyun4zoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有趣", { label: "lex", pos: "lexical_item", jyutping: "jau5 ceoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1409, frequency 15; prior audit status missing. interesting/fascinating/amusing. Source readings: jau5ceoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["老實", { label: "lex", pos: "lexical_item", jyutping: "lou5 sat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1410, frequency 15; prior audit status missing. honest/sincere/open and guileless/naive. Source readings: lou5sat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["艾爾頓", { label: "lex", pos: "lexical_item", jyutping: "ngaai6 ji5 deon6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1411, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngaai6ji5deon6|duk6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["何", { label: "lex", pos: "lexical_item", jyutping: "ho4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1412, frequency 15; prior audit status missing. surname Ho/what/how/why/which/carry. Source readings: ho4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["冷氣", { label: "lex", pos: "lexical_item", jyutping: "laang5 hei3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1413, frequency 15; prior audit status missing. air conditioning (used in Taiwan). Source readings: laang5hei3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["角色", { label: "lex", pos: "lexical_item", jyutping: "gok3 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1415, frequency 15; prior audit status handled_structurally. persona/character in a novel. Source readings: gok3sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["車主", { label: "lex", pos: "lexical_item", jyutping: "ce1 zyu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1416, frequency 15; prior audit status missing. car owner. Source readings: ce1zyu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["亞視", { label: "lex", pos: "lexical_item", jyutping: "aa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1417, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *aa3|ngaa3si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["依家", { label: "lex", pos: "lexical_item", jyutping: "ji1 gaa1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1418, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: ji1gaa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["坦白", { label: "lex", pos: "lexical_item", jyutping: "taan2 baak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1419, frequency 15; prior audit status missing. honest/forthcoming/to confess. Source readings: taan2baak6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["明顯", { label: "lex", pos: "lexical_item", jyutping: "ming4 hin2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1421, frequency 15; prior audit status missing. clear/distinct/obvious. Source readings: ming4hin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["法例", { label: "lex", pos: "lexical_item", jyutping: "faat3 lai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1422, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *faat3lai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["金", { label: "lex", pos: "lexical_item", jyutping: "gam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1425, frequency 15; prior audit status missing. gold/money/golden/a surname/Jurchen Jin dynasty (1115-1234)/feces. Source readings: gam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["迫", { label: "lex", pos: "lexical_item", jyutping: "baak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1428, frequency 15; prior audit status missing. to force/to compel/pressing/urgent. Source readings: baak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["重新", { label: "lex", pos: "lexical_item", jyutping: "cung4 san1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1429, frequency 15; prior audit status missing. again/once more/re-. Source readings: cung4san1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["音響", { label: "lex", pos: "lexical_item", jyutping: "jam1 hoeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1431, frequency 15; prior audit status missing. speakers or speaker (electronic)/acoustics/sound field (in a room or theater). Source readings: jam1hoeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個意", { label: "lex", pos: "lexical_item", jyutping: "go3 ji3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1432, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *go3ji3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["恐怖", { label: "lex", pos: "lexical_item", jyutping: "hung2 bou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1434, frequency 15; prior audit status missing. terrible/frightful/frightening/terror/terrorist. Source readings: hung2bou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["捉", { label: "lex", pos: "lexical_item", jyutping: "zuk1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1435, frequency 15; prior audit status missing. to clutch/to grab/to capture. Source readings: zuk1|zuk3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["時話", { label: "lex", pos: "lexical_item", jyutping: "si4 waa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1436, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *si4waa6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["除非", { label: "lex", pos: "lexical_item", jyutping: "ceoi4 fei1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1437, frequency 15; prior audit status missing. only if (..., or otherwise, ...)/only when/only in the case that/unless. Source readings: ceoi4fei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["情", { label: "lex", pos: "lexical_item", jyutping: "cing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1438, frequency 15; prior audit status missing. feeling/emotion/passion/situation. Source readings: cing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["排隊", { label: "lex", pos: "lexical_item", jyutping: "paai4 deoi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1439, frequency 15; prior audit status missing. to line up. Source readings: paai4deoi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["票", { label: "lex", pos: "lexical_item", jyutping: "piu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1440, frequency 15; prior audit status missing. bank note/ticket/a vote. Source readings: piu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["終審", { label: "lex", pos: "lexical_item", jyutping: "zung1 sam2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1441, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zung1sam2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["竟然", { label: "lex", pos: "lexical_item", jyutping: "ging2 jin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1442, frequency 15; prior audit status missing. unexpectedly/to one's surprise/in spite of everything/in that crazy way/actually/to go as far as to. Source readings: ging2jin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["就畫", { label: "lex", pos: "lexical_item", jyutping: "zau6 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1443, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zau6waak6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["提出", { label: "lex", pos: "lexical_item", jyutping: "tai4 ceot1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1444, frequency 15; prior audit status missing. to raise (an issue)/to propose/to put forward/to suggest/to post (on a website)/to withdraw (cash). Source readings: tai4ceot1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["答案", { label: "lex", pos: "lexical_item", jyutping: "daap3 on3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1445, frequency 15; prior audit status missing. answer/solution. Source readings: daap3on3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["著", { label: "lex", pos: "lexical_item", jyutping: "zyu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1446, frequency 15; prior audit status missing. to make known/to show/to prove/to write/book/outstanding. Source readings: zyu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["賀", { label: "lex", pos: "lexical_item", jyutping: "ho6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1447, frequency 15; prior audit status missing. congratulate/surname He. Source readings: ho6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["黑", { label: "lex", pos: "lexical_item", jyutping: "haak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1448, frequency 15; prior audit status missing. black/dark/unlucky, unfortunate, cursed/(loanword) to hack/abbr. for Heilongjiang 黑龍江 province in northeast China. Source readings: haak1|hak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["椰", { label: "lex", pos: "lexical_item", jyutping: "je4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1449, frequency 15; prior audit status missing. coconut palm/Taiwan pr. ye2. Source readings: je4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["落返", { label: "lex", pos: "lexical_item", jyutping: "lok6 faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1450, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lok6faan1|faan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["過個", { label: "lex", pos: "lexical_item", jyutping: "gwo3 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1451, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gwo3go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["隔", { label: "lex", pos: "lexical_item", jyutping: "gaak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1452, frequency 15; prior audit status missing. to separate/to partition/to stand or lie between/at a distance from/after or at an interval of. Source readings: gaak3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["電影", { label: "lex", pos: "lexical_item", jyutping: "din6 jing2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1453, frequency 15; prior audit status missing. movie/film. Source readings: din6jing2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["寧願", { label: "lex", pos: "lexical_item", jyutping: "ning4 jyun6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1454, frequency 15; prior audit status missing. would rather/better. Source readings: ning4jyun6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["摺", { label: "lex", pos: "lexical_item", jyutping: "zip3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1455, frequency 15; prior audit status missing. to fold (a document)/to turn/to bend. Source readings: zip3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["盡", { label: "lex", pos: "lexical_item", jyutping: "zeon6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1456, frequency 15; prior audit status missing. to use up/to exhaust/to end/to finish/to the utmost/exhausted/finished/to the limit (of sth). Source readings: zeon6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["福利", { label: "lex", pos: "lexical_item", jyutping: "fuk1 lei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1457, frequency 15; prior audit status missing. welfare/well-being. Source readings: fuk1lei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["與", { label: "lex", pos: "lexical_item", jyutping: "jyu5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1458, frequency 15; prior audit status missing. (same as 歟|欤, final particle expression doubt or surprise, similar to 吗 or 呢)/and/to give/together with/take part in. Source readings: jyu5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["劍", { label: "lex", pos: "lexical_item", jyutping: "gim3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1459, frequency 15; prior audit status missing. (double-edged) sword. Source readings: gim3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["層", { label: "lex", pos: "lexical_item", jyutping: "cang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1460, frequency 15; prior audit status missing. layer/stratum/laminated/floor (of a building)/storey/classifier for layers/repeated/sheaf (math.). Source readings: cang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["數", { label: "lex", pos: "lexical_item", jyutping: "sou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1461, frequency 15; prior audit status missing. frequently/repeatedly/number/figure/several/to count/to enumerate/to criticize (i.e. enumerate shortcomings). Source readings: sou3|sou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["樓", { label: "lex", pos: "lexical_item", jyutping: "lau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1462, frequency 15; prior audit status missing. house with more than 1 story/storied building/floor/CL:層[chàhng],座[joh],棟[duhng]. Source readings: lau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["膠袋", { label: "lex", pos: "lexical_item", jyutping: "gaau1 doi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1463, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gaau1doi6|doi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["諗法", { label: "lex", pos: "lexical_item", jyutping: "nam2 faat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1464, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *nam2faat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["選", { label: "lex", pos: "lexical_item", jyutping: "syun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1466, frequency 15; prior audit status missing. to choose/to pick/to select/to elect. Source readings: syun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["錄", { label: "lex", pos: "lexical_item", jyutping: "luk6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1467, frequency 15; prior audit status missing. diary/record/to hit/to copy/surname Lu. Source readings: luk6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講番", { label: "lex", pos: "lexical_item", jyutping: "gong2 faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1468, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2faan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["點會", { label: "lex", pos: "lexical_item", jyutping: "dim2 wui5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1469, frequency 15; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dim2wui5|wui6|wui2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["醫療", { label: "lex", pos: "lexical_item", jyutping: "ji1 liu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1470, frequency 15; prior audit status missing. medical treatment. Source readings: ji1liu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["贊成", { label: "lex", pos: "lexical_item", jyutping: "zaan3 sing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1471, frequency 15; prior audit status missing. approve/endorse. Source readings: zaan3sing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一本", { label: "lex", pos: "lexical_item", jyutping: "jat1 bun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1473, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1bun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一座", { label: "lex", pos: "lexical_item", jyutping: "jat1 zo6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1474, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1zo6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["下角", { label: "lex", pos: "lexical_item", jyutping: "haa6 gok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1475, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *haa6gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["大隻", { label: "lex", pos: "lexical_item", jyutping: "daai6 zek3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1476, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *daai6zek3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["女皇", { label: "lex", pos: "lexical_item", jyutping: "neoi5 wong4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1477, frequency 14; prior audit status missing. empress. Source readings: neoi5wong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["五點", { label: "lex", pos: "lexical_item", jyutping: "ng5 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1478, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ng5dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["引起", { label: "lex", pos: "lexical_item", jyutping: "jan5 hei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1479, frequency 14; prior audit status missing. to give rise to/to lead to/to cause/to arouse. Source readings: jan5hei2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["心理", { label: "lex", pos: "lexical_item", jyutping: "sam1 lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1480, frequency 14; prior audit status missing. mental/psychological. Source readings: sam1lei5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["文化", { label: "lex", pos: "lexical_item", jyutping: "man4 faa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1481, frequency 14; prior audit status missing. culture/civilization/cultural. Source readings: man4faa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["毛", { label: "lex", pos: "lexical_item", jyutping: "mou4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1482, frequency 14; prior audit status missing. hair/feather/down/wool/mildew/used in constructions in which a statement is rejected or ridiculed/classifier for Chinese fractional monetary unit ( = 角[gok] , = 1/10 yuan or 10 fen 分[fān])/a surname. Source readings: mou4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["右畫", { label: "lex", pos: "lexical_item", jyutping: "jau6 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1483, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau6waak6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["再畫", { label: "lex", pos: "lexical_item", jyutping: "zoi3 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1484, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zoi3waak6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["各", { label: "lex", pos: "lexical_item", jyutping: "gok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1485, frequency 14; prior audit status missing. each/every. Source readings: gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["回應", { label: "lex", pos: "lexical_item", jyutping: "wui4 jing3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1486, frequency 14; prior audit status missing. response/respond. Source readings: wui4jing3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有關", { label: "lex", pos: "lexical_item", jyutping: "jau5 gwaan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1488, frequency 14; prior audit status missing. to have sth to do with/to relate to/related to/to concern/concerning. Source readings: jau5gwaan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["行業", { label: "lex", pos: "lexical_item", jyutping: "hong4 jip6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1489, frequency 14; prior audit status missing. industry/business. Source readings: hong4jip6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["每個", { label: "lex", pos: "lexical_item", jyutping: "mui5 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1492, frequency 14; prior audit status handled_structurally. each. Source readings: mui5go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["沙", { label: "lex", pos: "lexical_item", jyutping: "saa1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1493, frequency 14; prior audit status missing. granule/hoarse/raspy/sand/powder/CL:粒[lāp]. Source readings: saa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["角度", { label: "lex", pos: "lexical_item", jyutping: "gok3 dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1494, frequency 14; prior audit status handled_structurally. angle/point of view. Source readings: gok3dou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["事實上", { label: "lex", pos: "lexical_item", jyutping: "si6 sat6 soeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1495, frequency 14; prior audit status missing. in fact/in reality/actually/as a matter of fact/de facto/ipso facto. Source readings: si6sat6soeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["佬", { label: "lex", pos: "lexical_item", jyutping: "lou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1496, frequency 14; prior audit status missing. male/man (Cantonese). Source readings: lou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["或", { label: "lex", pos: "lexical_item", jyutping: "waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1497, frequency 14; prior audit status missing. maybe/perhaps/might/possibly/or. Source readings: waak6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["拍拖", { label: "lex", pos: "lexical_item", jyutping: "paak3 to1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1498, frequency 14; prior audit status missing. to have a romantic relationship with someone/2. to go out with someone, to go on a date, to date. Source readings: paak3to1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["果", { label: "lex", pos: "lexical_item", jyutping: "gwo2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1499, frequency 14; prior audit status missing. fruit/result. Source readings: gwo2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["沾污", { label: "lex", pos: "lexical_item", jyutping: "zim1 wu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1500, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zim1wu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["削", { label: "lex", pos: "lexical_item", jyutping: "soek3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1501, frequency 14; prior audit status missing. to reduce/to pare (away)/to cut (down)/to scrape. Source readings: soek3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["屋企人", { label: "lex", pos: "lexical_item", jyutping: "uk1 kei2 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1502, frequency 14; prior audit status handled_structurally. family. Source readings: uk1kei2jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["架構", { label: "lex", pos: "lexical_item", jyutping: "gaa3 kau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1503, frequency 14; prior audit status missing. to construct/to build/infrastructure/architecture/framework. Source readings: gaa3kau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["查", { label: "lex", pos: "lexical_item", jyutping: "caa4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1504, frequency 14; prior audit status missing. to research/to check/to investigate/to examine/to refer to/to search/surname Cha. Source readings: caa4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["要講", { label: "lex", pos: "lexical_item", jyutping: "jiu3 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1505, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jiu3gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["重複", { label: "lex", pos: "lexical_item", jyutping: "cung4 fuk1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1506, frequency 14; prior audit status missing. to repeat/to duplicate. Source readings: cung4fuk1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["倒", { label: "lex", pos: "lexical_item", jyutping: "dou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1507, frequency 14; prior audit status missing. to fall over/to topple/to overthrow/(business) fail, collapse/to change/to shift/to exchange/to pour out/to dump (garbage)|to turn upside down/to invert/to reverse/upside down/inverse/inverted/to move backwards/to turn b. Source readings: dou2|dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["剛才", { label: "lex", pos: "lexical_item", jyutping: "gong1 coi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1508, frequency 14; prior audit status missing. just. Source readings: gong1coi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["容許", { label: "lex", pos: "lexical_item", jyutping: "jung4 heoi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1509, frequency 14; prior audit status missing. permit/allow. Source readings: jung4heoi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["效率", { label: "lex", pos: "lexical_item", jyutping: "haau6 leot2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1510, frequency 14; prior audit status missing. efficiency. Source readings: haau6leot2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["根據", { label: "lex", pos: "lexical_item", jyutping: "gan1 geoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1511, frequency 14; prior audit status missing. according to/based on/basis/foundation. Source readings: gan1geoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["特登", { label: "lex", pos: "lexical_item", jyutping: "dak6 dang1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1512, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dak6dang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唪", { label: "lex", pos: "lexical_item", jyutping: "fung2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1514, frequency 14; prior audit status missing. recite/chant. Source readings: fung2|baa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["從來", { label: "lex", pos: "lexical_item", jyutping: "cung4 loi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1515, frequency 14; prior audit status missing. always/at all times/never (if used in negative sentence). Source readings: cung4loi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["教授", { label: "lex", pos: "lexical_item", jyutping: "gaau3 sau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1516, frequency 14; prior audit status missing. professor/to instruct/to lecture on. Source readings: gaau3sau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["貨", { label: "lex", pos: "lexical_item", jyutping: "fo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1518, frequency 14; prior audit status missing. goods/money/commodity. Source readings: fo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["郭富城", { label: "lex", pos: "lexical_item", jyutping: "gwok3 fu3 sing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1519, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gwok3fu3sing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾咁", { label: "lex", pos: "lexical_item", jyutping: "gei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1520, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gei2|gei1gam3|gam2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["牌", { label: "lex", pos: "lexical_item", jyutping: "paai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1521, frequency 14; prior audit status missing. mahjong tile/playing card/game pieces/signboard/plate/tablet/medal. Source readings: paai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["牌子", { label: "lex", pos: "lexical_item", jyutping: "paai4 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1522, frequency 14; prior audit status missing. sign/trademark. Source readings: paai4zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["等於", { label: "lex", pos: "lexical_item", jyutping: "dang2 jyu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1523, frequency 14; prior audit status missing. to equal/to be tantamount to. Source readings: dang2jyu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["費事", { label: "lex", pos: "lexical_item", jyutping: "fai3 si6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1524, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *fai3si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["貼", { label: "lex", pos: "lexical_item", jyutping: "tip1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1525, frequency 14; prior audit status missing. to stick/to paste/to keep close to/to fit snugly/to subsidize/to guess/allowance (e.g. money for food or housing)/sticker. Source readings: tip1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["媽咪", { label: "lex", pos: "lexical_item", jyutping: "maa1 mi4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1526, frequency 14; prior audit status missing. Mummy (mother). Source readings: maa1mi4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["搞掂", { label: "lex", pos: "lexical_item", jyutping: "gaau2 dim6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1527, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: gaau2dim6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["經常", { label: "lex", pos: "lexical_item", jyutping: "ging1 soeng4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1528, frequency 14; prior audit status missing. day to day/everyday/daily/frequently/constantly/regularly/often. Source readings: ging1soeng4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["聖誕", { label: "lex", pos: "lexical_item", jyutping: "sing3 daan3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1529, frequency 14; prior audit status missing. Christmas/birthday of reigning Emperor/Confucius' birthday. Source readings: sing3daan3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["預", { label: "lex", pos: "lexical_item", jyutping: "jyu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1530, frequency 14; prior audit status missing. to advance/in advance/beforehand/to prepare. Source readings: jyu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["罷", { label: "lex", pos: "lexical_item", jyutping: "baa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1532, frequency 14; prior audit status missing. to stop/to cease/to dismiss/to suspend/to quit/to finish/final particle, same as 吧 (Mandarin). Source readings: baa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["舖頭", { label: "lex", pos: "lexical_item", jyutping: "pou3 tau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1533, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *pou3tau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["養魚", { label: "lex", pos: "lexical_item", jyutping: "joeng5 jyu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1534, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *joeng5jyu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["錄音", { label: "lex", pos: "lexical_item", jyutping: "luk6 jam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1535, frequency 14; prior audit status missing. (sound) recording. Source readings: luk6jam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講講", { label: "lex", pos: "lexical_item", jyutping: "gong2 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1536, frequency 14; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["離婚", { label: "lex", pos: "lexical_item", jyutping: "lei4 fan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1538, frequency 14; prior audit status missing. to divorce/divorced from (one's spouse). Source readings: lei4fan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["壞", { label: "lex", pos: "lexical_item", jyutping: "waai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1539, frequency 14; prior audit status missing. bad/spoiled/broken/to break down. Source readings: waai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["羅", { label: "lex", pos: "lexical_item", jyutping: "lo4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1540, frequency 14; prior audit status missing. gauze/to collect/to gather/to catch/to shift/surname Luo. Source readings: lo4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["辭典", { label: "lex", pos: "lexical_item", jyutping: "ci4 din2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1541, frequency 14; prior audit status missing. dictionary (of Chinese compound words)/variant of 詞典|词典. Source readings: ci4din2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["邊緣", { label: "lex", pos: "lexical_item", jyutping: "bin1 jyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1542, frequency 14; prior audit status missing. edge/fringe/verge/brink/periphery/marginal/borderline. Source readings: bin1jyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["類", { label: "lex", pos: "lexical_item", jyutping: "leoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1543, frequency 14; prior audit status missing. kind/type/class/category/similar/like/to resemble. Source readings: leoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["讓", { label: "lex", pos: "lexical_item", jyutping: "joeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1544, frequency 14; prior audit status missing. to yield/to permit/to let sb do sth/to have sb do sth. Source readings: joeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一件", { label: "lex", pos: "lexical_item", jyutping: "jat1 gin6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1545, frequency 13; prior audit status handled_structurally. piece. Source readings: jat1gin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一部份", { label: "lex", pos: "lexical_item", jyutping: "jat1 bou6 fan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1546, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1bou6fan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一棵", { label: "lex", pos: "lexical_item", jyutping: "jat1 po1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1547, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1po1|fo2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["十年", { label: "lex", pos: "lexical_item", jyutping: "sap6 nin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1548, frequency 13; prior audit status handled_structurally. ten years. Source readings: sap6nin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["中心", { label: "lex", pos: "lexical_item", jyutping: "zung1 sam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1550, frequency 13; prior audit status handled_structurally. center/heart/core. Source readings: zung1sam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["中醫", { label: "lex", pos: "lexical_item", jyutping: "zung1 ji1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1551, frequency 13; prior audit status missing. traditional Chinese medical science. Source readings: zung1ji1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["之中", { label: "lex", pos: "lexical_item", jyutping: "zi1 zung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1552, frequency 13; prior audit status missing. inside. Source readings: zi1zung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["互相", { label: "lex", pos: "lexical_item", jyutping: "wu6 soeng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1553, frequency 13; prior audit status missing. each other/mutually/mutual. Source readings: wu6soeng1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["今個", { label: "lex", pos: "lexical_item", jyutping: "gam1 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1554, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gam1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["太太", { label: "lex", pos: "lexical_item", jyutping: "taai3 taai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1555, frequency 13; prior audit status handled_structurally. married woman/Mrs./Madam/wife. Source readings: taai3taai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["日後", { label: "lex", pos: "lexical_item", jyutping: "jat6 hau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1556, frequency 13; prior audit status missing. sometime/someday (in the future). Source readings: jat6hau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["主角", { label: "lex", pos: "lexical_item", jyutping: "zyu2 gok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1557, frequency 13; prior audit status missing. leading role/lead. Source readings: zyu2gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["主題", { label: "lex", pos: "lexical_item", jyutping: "zyu2 tai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1558, frequency 13; prior audit status missing. theme/subject. Source readings: zyu2tai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["出聲", { label: "lex", pos: "lexical_item", jyutping: "ceot1 seng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1559, frequency 13; prior audit status handled_structurally. to utter/to give voice. Source readings: ceot1seng1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["台灣", { label: "lex", pos: "lexical_item", jyutping: "toi4 waan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1560, frequency 13; prior audit status missing. Taiwan. Source readings: toi4waan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["司", { label: "lex", pos: "lexical_item", jyutping: "si1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1561, frequency 13; prior audit status missing. company/control/surname Si. Source readings: si1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["犯", { label: "lex", pos: "lexical_item", jyutping: "faan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1562, frequency 13; prior audit status missing. to violate/to offend/to assault/criminal/crime/to make a mistake/recurrence (of mistake or sth bad). Source readings: faan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["白", { label: "lex", pos: "lexical_item", jyutping: "baak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1563, frequency 13; prior audit status missing. white/clear/pure/bright/clean/plain/in vain/for nothing/free of charge/vernacular/the spoken part in an opera/to state/to explain/a surname. Source readings: baak6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["目的", { label: "lex", pos: "lexical_item", jyutping: "muk6 dik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1564, frequency 13; prior audit status missing. purpose/aim/goal/target/objective. Source readings: muk6dik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["先得", { label: "lex", pos: "lexical_item", jyutping: "sin1 dak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1565, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sin1dak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好少", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1566, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3siu2|siu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好煩", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1568, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3faan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["扣", { label: "lex", pos: "lexical_item", jyutping: "kau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1569, frequency 13; prior audit status missing. to fasten/to button/button/buckle/knot/to arrest/to confiscate/to deduct (money)/discount/to knock/to smash or spike (a ball)/to cover (with a bowl etc)/fig. to tag a label on sb. Source readings: kau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["收埋", { label: "lex", pos: "lexical_item", jyutping: "sau1 maai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1570, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sau1maai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["自信", { label: "lex", pos: "lexical_item", jyutping: "zi6 seon3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1571, frequency 13; prior audit status missing. confidence/self-confidence. Source readings: zi6seon3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["作", { label: "lex", pos: "lexical_item", jyutping: "zok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1572, frequency 13; prior audit status missing. to compose/to write/to do/to pretend/to regard as/to feel/writings or works. Source readings: zok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["你畫", { label: "lex", pos: "lexical_item", jyutping: "nei5 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1573, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *nei5waak6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["即要", { label: "lex", pos: "lexical_item", jyutping: "zik1 jiu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1574, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zik1jiu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["夾", { label: "lex", pos: "lexical_item", jyutping: "gaap3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1575, frequency 13; prior audit status missing. to place in between/to sandwich/to carry sth under armpit/wedged between/between/to intersperse/to mix/to mingle|clip/folder. Source readings: gaap3|gaap2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我識", { label: "lex", pos: "lexical_item", jyutping: "ngo5 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1576, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["身體", { label: "lex", pos: "lexical_item", jyutping: "san1 tai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1577, frequency 13; prior audit status missing. (human) body/health. Source readings: san1tai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩條", { label: "lex", pos: "lexical_item", jyutping: "loeng5 tiu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1578, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *loeng5tiu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["咁滯", { label: "lex", pos: "lexical_item", jyutping: "gam3 zai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1579, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: gam3zai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["拗", { label: "lex", pos: "lexical_item", jyutping: "aau2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1580, frequency 13; prior audit status missing. to bend in two so as to break|stubborn/obstinate/to argue. Source readings: aau2|aau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["返轉頭", { label: "lex", pos: "lexical_item", jyutping: "faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1581, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *faan1|faan2zyun3tau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["金融", { label: "lex", pos: "lexical_item", jyutping: "gam1 jung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1582, frequency 13; prior audit status missing. banking/finance/financial. Source readings: gam1jung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["封", { label: "lex", pos: "lexical_item", jyutping: "fung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1583, frequency 13; prior audit status missing. to confer/to grant/to bestow a title/to seal/classifier for sealed objects, esp. letters. Source readings: fung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["政治", { label: "lex", pos: "lexical_item", jyutping: "zing3 zi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1584, frequency 13; prior audit status missing. politics/political. Source readings: zing3zi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["柱", { label: "lex", pos: "lexical_item", jyutping: "cyu5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1585, frequency 13; prior audit status missing. pillar. Source readings: cyu5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["為止", { label: "lex", pos: "lexical_item", jyutping: "wai4 zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1586, frequency 13; prior audit status missing. until. Source readings: wai4zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["界", { label: "lex", pos: "lexical_item", jyutping: "gaai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1587, frequency 13; prior audit status missing. boundary/scope/extent/circles/group/kingdom (taxonomy). Source readings: gaai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["閂", { label: "lex", pos: "lexical_item", jyutping: "saan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1588, frequency 13; prior audit status missing. bolt/latch/to bolt/to latch. Source readings: saan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["面前", { label: "lex", pos: "lexical_item", jyutping: "min6 cin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1589, frequency 13; prior audit status missing. in front of/facing/presence. Source readings: min6cin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["食到", { label: "lex", pos: "lexical_item", jyutping: "sik6 dou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1590, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sik6dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個萍", { label: "lex", pos: "lexical_item", jyutping: "go3 ping4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1591, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *go3ping4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔錯", { label: "lex", pos: "lexical_item", jyutping: "m4 co3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1592, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *m4co3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["庭", { label: "lex", pos: "lexical_item", jyutping: "ting4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1593, frequency 13; prior audit status missing. court/courtyard. Source readings: ting4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["秘書", { label: "lex", pos: "lexical_item", jyutping: "bei3 syu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1594, frequency 13; prior audit status missing. secretary. Source readings: bei3syu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["能", { label: "lex", pos: "lexical_item", jyutping: "nang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1596, frequency 13; prior audit status missing. can/may/capable/energy/able/surname Neng. Source readings: nang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["骨折", { label: "lex", pos: "lexical_item", jyutping: "gwat1 zit3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1598, frequency 13; prior audit status missing. broken bones/fracture. Source readings: gwat1zit3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["參考", { label: "lex", pos: "lexical_item", jyutping: "caam1 haau2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1599, frequency 13; prior audit status missing. consultation/reference/to consult/to refer. Source readings: caam1haau2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["接", { label: "lex", pos: "lexical_item", jyutping: "zip3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1600, frequency 13; prior audit status missing. to receive/to answer (the phone)/to meet or welcome sb/to connect/to catch/to join/to extend/to take one's turn on duty/take over for sb. Source readings: zip3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["痕", { label: "lex", pos: "lexical_item", jyutping: "han4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1601, frequency 13; prior audit status missing. scar/traces. Source readings: han4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["細路仔", { label: "lex", pos: "lexical_item", jyutping: "sai3 lou6 zai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1602, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sai3lou6zai2|zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["速度", { label: "lex", pos: "lexical_item", jyutping: "cuk1 dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1603, frequency 13; prior audit status missing. speed/rate/velocity. Source readings: cuk1dou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["部門", { label: "lex", pos: "lexical_item", jyutping: "bou6 mun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1604, frequency 13; prior audit status handled_structurally. department/branch/section/division. Source readings: bou6mun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["雀", { label: "lex", pos: "lexical_item", jyutping: "zoek3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1605, frequency 13; prior audit status missing. small bird/sparrow/a freckle/lentigo. Source readings: zoek3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["單仲佳", { label: "lex", pos: "lexical_item", jyutping: "daan1 zung6 gaai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1606, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *daan1zung6gaai1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["就會", { label: "lex", pos: "lexical_item", jyutping: "zau6 wui5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1607, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zau6wui5|wui6|wui2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾靚", { label: "lex", pos: "lexical_item", jyutping: "gei2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1608, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gei2|gei1leng3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾點", { label: "lex", pos: "lexical_item", jyutping: "gei2 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1609, frequency 13; prior audit status handled_structurally. what time?/when?. Source readings: gei2dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["期", { label: "lex", pos: "lexical_item", jyutping: "kei4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1611, frequency 13; prior audit status missing. a period of time/phase/stage/(used for issue of a periodical, courses of study)/time/term/period/to hope. Source readings: kei4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["畫一個", { label: "lex", pos: "lexical_item", jyutping: "waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1613, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *waak6|waa2jat1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["稅", { label: "lex", pos: "lexical_item", jyutping: "seoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1614, frequency 13; prior audit status missing. taxes/duties. Source readings: seoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["黑色", { label: "lex", pos: "lexical_item", jyutping: "haak1 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1615, frequency 13; prior audit status missing. black. Source readings: haak1sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["傷害", { label: "lex", pos: "lexical_item", jyutping: "soeng1 hoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1616, frequency 13; prior audit status missing. injure/harm. Source readings: soeng1hoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["傾偈", { label: "lex", pos: "lexical_item", jyutping: "king1 gai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1617, frequency 13; prior audit status missing. to chat. Source readings: king1gai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["感", { label: "lex", pos: "lexical_item", jyutping: "gam2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1618, frequency 13; prior audit status missing. to feel/to move/to touch/to affect. Source readings: gam2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["損失", { label: "lex", pos: "lexical_item", jyutping: "syun2 sat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1619, frequency 13; prior audit status missing. loss (e.g. financial)/to lose. Source readings: syun2sat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["會會", { label: "lex", pos: "lexical_item", jyutping: "wui5 wui5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1620, frequency 13; prior audit status handled_structurally. to meet. Source readings: wui5wui5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["照顧", { label: "lex", pos: "lexical_item", jyutping: "ziu3 gu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1621, frequency 13; prior audit status missing. to take care of/to show consideration/to attend to/to look after. Source readings: ziu3gu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["運", { label: "lex", pos: "lexical_item", jyutping: "wan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1622, frequency 13; prior audit status missing. to move/to transport/to use/to apply/fortune/luck/fate. Source readings: wan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘗試", { label: "lex", pos: "lexical_item", jyutping: "soeng4 si3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1623, frequency 13; prior audit status missing. to try/to attempt. Source readings: soeng4si3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["監管", { label: "lex", pos: "lexical_item", jyutping: "gaam1 gun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1624, frequency 13; prior audit status missing. to oversee/to take charge of/to supervise/to administer/supervisory/supervision. Source readings: gaam1gun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["碧", { label: "lex", pos: "lexical_item", jyutping: "bik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1625, frequency 13; prior audit status missing. green jade/bluish green/blue/jade. Source readings: bik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["網", { label: "lex", pos: "lexical_item", jyutping: "mong5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1626, frequency 13; prior audit status missing. net/network. Source readings: mong5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["認真", { label: "lex", pos: "lexical_item", jyutping: "jing6 zan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1627, frequency 13; prior audit status missing. earnest/serious/to take seriously/to take to heart. Source readings: jing6zan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["增加", { label: "lex", pos: "lexical_item", jyutping: "zang1 gaa1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1628, frequency 13; prior audit status missing. to raise/to increase. Source readings: zang1gaa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["數字", { label: "lex", pos: "lexical_item", jyutping: "sou3 zi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1629, frequency 13; prior audit status missing. numeral/digit/number/figure/amount/digital (electronics etc). Source readings: sou3zi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["鋪", { label: "lex", pos: "lexical_item", jyutping: "pou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1631, frequency 13; prior audit status missing. to spread/to display/to set up/plank bed/place to sleep/shop/store. Source readings: pou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["燈", { label: "lex", pos: "lexical_item", jyutping: "dang1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1632, frequency 13; prior audit status missing. lamp/light/lantern/CL:盞[jáan]. Source readings: dang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講多次", { label: "lex", pos: "lexical_item", jyutping: "gong2 do1 ci3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1634, frequency 13; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2do1ci3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嚮", { label: "lex", pos: "lexical_item", jyutping: "hoeng3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1635, frequency 13; prior audit status missing. variant of 向, direction/orientation/to face/to turn toward/to/towards/shortly before/formerly/to side with/to be partial to/all along (previously)/surname Xiang. Source readings: hoeng3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["關", { label: "lex", pos: "lexical_item", jyutping: "gwaan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1636, frequency 13; prior audit status missing. mountain pass/to close/to shut/to turn off/to concern/to involve/surname Kwan. Source readings: gwaan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一刻", { label: "lex", pos: "lexical_item", jyutping: "jat1 hak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1637, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1hak1|haak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一套", { label: "lex", pos: "lexical_item", jyutping: "jat1 tou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1638, frequency 12; prior audit status handled_structurally. suit/a set. Source readings: jat1tou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一部", { label: "lex", pos: "lexical_item", jyutping: "jat1 bou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1639, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jat1bou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["十二點", { label: "lex", pos: "lexical_item", jyutping: "sap6 ji6 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1640, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: sap6ji6dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["十分", { label: "lex", pos: "lexical_item", jyutping: "sap6 fan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1641, frequency 12; prior audit status handled_structurally. to divide into ten equal parts/very/hundred percent/completely/extremely/utterly/absolutely. Source readings: sap6fan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["中學", { label: "lex", pos: "lexical_item", jyutping: "zung1 hok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1642, frequency 12; prior audit status handled_structurally. middle school. Source readings: zung1hok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["之內", { label: "lex", pos: "lexical_item", jyutping: "zi1 noi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1643, frequency 12; prior audit status missing. inside. Source readings: zi1noi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["今", { label: "lex", pos: "lexical_item", jyutping: "gam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1644, frequency 12; prior audit status missing. today/modern/present/current/this/now. Source readings: gam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["內容", { label: "lex", pos: "lexical_item", jyutping: "noi6 jung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1645, frequency 12; prior audit status missing. content/substance/details. Source readings: noi6jung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["水草", { label: "lex", pos: "lexical_item", jyutping: "seoi2 cou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1646, frequency 12; prior audit status missing. water plants/habitat with water source and grass. Source readings: seoi2cou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["去過", { label: "lex", pos: "lexical_item", jyutping: "heoi3 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1648, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *heoi3gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["正面", { label: "lex", pos: "lexical_item", jyutping: "zing3 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1649, frequency 12; prior audit status handled_structurally. front/positive/direct/open. Source readings: zing3min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["生產", { label: "lex", pos: "lexical_item", jyutping: "sang1 caan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1650, frequency 12; prior audit status missing. childbirth/parturition/to produce/manufacture. Source readings: sang1caan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["合理", { label: "lex", pos: "lexical_item", jyutping: "hap6 lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1651, frequency 12; prior audit status missing. rational/reasonable/fair. Source readings: hap6lei5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["多過", { label: "lex", pos: "lexical_item", jyutping: "do1 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1652, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *do1gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好悶", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1654, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3mun6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成熟", { label: "lex", pos: "lexical_item", jyutping: "sing4 suk6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1656, frequency 12; prior audit status missing. mature/ripe/Taiwan pr. cheng2 shou2. Source readings: sing4suk6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["曲線", { label: "lex", pos: "lexical_item", jyutping: "kuk1 sin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1658, frequency 12; prior audit status missing. curve/curved line/indirect/in a roundabout way. Source readings: kuk1sin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["西行", { label: "lex", pos: "lexical_item", jyutping: "sai1 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1659, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sai1hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["西面", { label: "lex", pos: "lexical_item", jyutping: "sai1 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1660, frequency 12; prior audit status handled_structurally. west side/west. Source readings: sai1min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["佔", { label: "lex", pos: "lexical_item", jyutping: "zim3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1661, frequency 12; prior audit status missing. to take possession of/to occupy/to constitute/to make up/to account for/占 sometimes used as traditional character. Source readings: zim3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["判", { label: "lex", pos: "lexical_item", jyutping: "pun3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1662, frequency 12; prior audit status missing. to judge/to sentence/to discriminate/to discern. Source readings: pun3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["即將", { label: "lex", pos: "lexical_item", jyutping: "zik1 zoeng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1663, frequency 12; prior audit status missing. on the eve of/to be about to/to be on the verge of. Source readings: zik1zoeng1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["形象", { label: "lex", pos: "lexical_item", jyutping: "jing4 zoeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1664, frequency 12; prior audit status missing. image/form/figure. Source readings: jing4zoeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["投訴", { label: "lex", pos: "lexical_item", jyutping: "tau4 sou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1665, frequency 12; prior audit status missing. complaint/to file a complaint/to sue. Source readings: tau4sou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["私家", { label: "lex", pos: "lexical_item", jyutping: "si1 gaa1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1666, frequency 12; prior audit status missing. private/privately owned or managed. Source readings: si1gaa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩日", { label: "lex", pos: "lexical_item", jyutping: "loeng5 jat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1667, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *loeng5jat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩樣", { label: "lex", pos: "lexical_item", jyutping: "loeng5 joeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1668, frequency 12; prior audit status handled_structurally. two kinds/difference. Source readings: loeng5joeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["制", { label: "lex", pos: "lexical_item", jyutping: "zai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1669, frequency 12; prior audit status missing. system/to control/to regulate/to agree (to a deal). Source readings: zai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["呢種", { label: "lex", pos: "lexical_item", jyutping: "ne1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1670, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ne1|ni1|nei1zung2|zung3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["居民", { label: "lex", pos: "lexical_item", jyutping: "geoi1 man4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1671, frequency 12; prior audit status missing. resident/inhabitant. Source readings: geoi1man4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["直角", { label: "lex", pos: "lexical_item", jyutping: "zik6 gok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1672, frequency 12; prior audit status handled_structurally. a right angle. Source readings: zik6gok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["空", { label: "lex", pos: "lexical_item", jyutping: "hung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1673, frequency 12; prior audit status missing. empty/air/sky/in vain/to empty/vacant/unoccupied/space/leisure/free time. Source readings: hung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["股份", { label: "lex", pos: "lexical_item", jyutping: "gu2 fan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1674, frequency 12; prior audit status missing. a share (in a company)/stock. Source readings: gu2fan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["既然", { label: "lex", pos: "lexical_item", jyutping: "gei3 jin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1676, frequency 12; prior audit status missing. since/as/this being the case. Source readings: gei3jin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["珍惜", { label: "lex", pos: "lexical_item", jyutping: "zan1 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1677, frequency 12; prior audit status missing. treasure/value/cherish. Source readings: zan1sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["相處", { label: "lex", pos: "lexical_item", jyutping: "soeng1 cyu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1678, frequency 12; prior audit status missing. get along with each other. Source readings: soeng1cyu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["相對", { label: "lex", pos: "lexical_item", jyutping: "soeng1 deoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1679, frequency 12; prior audit status handled_structurally. relatively/opposite/to resist/to oppose/relative/vis-a-vis. Source readings: soeng1deoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["音", { label: "lex", pos: "lexical_item", jyutping: "jam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1680, frequency 12; prior audit status missing. sound/noise/note (of musical scale)/tone/news/syllable/reading (phonetic value of a character). Source readings: jam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個頭", { label: "lex", pos: "lexical_item", jyutping: "go3 tau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1681, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *go3tau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["員", { label: "lex", pos: "lexical_item", jyutping: "jyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1682, frequency 12; prior audit status missing. person/employee/member. Source readings: jyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔多", { label: "lex", pos: "lexical_item", jyutping: "m4 do1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1683, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *m4do1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["時效", { label: "lex", pos: "lexical_item", jyutping: "si4 haau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1684, frequency 12; prior audit status missing. timeliness. Source readings: si4haau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["校長", { label: "lex", pos: "lexical_item", jyutping: "haau6 zoeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1685, frequency 12; prior audit status missing. (college, university) president/headmaster. Source readings: haau6zoeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["核突", { label: "lex", pos: "lexical_item", jyutping: "wat6 dat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1686, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: wat6dat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["案件", { label: "lex", pos: "lexical_item", jyutping: "on3 gin2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1687, frequency 12; prior audit status missing. law case/legal case/judicial case. Source readings: on3gin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["破壞", { label: "lex", pos: "lexical_item", jyutping: "po3 waai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1688, frequency 12; prior audit status missing. destruction/damage/to wreck/to break/to destroy. Source readings: po3waai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["財務", { label: "lex", pos: "lexical_item", jyutping: "coi4 mou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1689, frequency 12; prior audit status missing. financial affairs. Source readings: coi4mou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["高過", { label: "lex", pos: "lexical_item", jyutping: "gou1 gwo3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1690, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gou1gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["商業", { label: "lex", pos: "lexical_item", jyutping: "soeng1 jip6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1693, frequency 12; prior audit status missing. business/trade/commerce. Source readings: soeng1jip6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["問吓", { label: "lex", pos: "lexical_item", jyutping: "man6 haa5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1694, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *man6haa5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["國內", { label: "lex", pos: "lexical_item", jyutping: "gwok3 noi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1695, frequency 12; prior audit status missing. domestic/internal (to a country)/civil. Source readings: gwok3noi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["最右", { label: "lex", pos: "lexical_item", jyutping: "zeoi3 jau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1696, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zeoi3jau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["最底", { label: "lex", pos: "lexical_item", jyutping: "zeoi3 dai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1697, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zeoi3dai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["提到", { label: "lex", pos: "lexical_item", jyutping: "tai4 dou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1699, frequency 12; prior audit status missing. to mention/to raise (a subject)/to refer to. Source readings: tai4dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["插", { label: "lex", pos: "lexical_item", jyutping: "caap3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1700, frequency 12; prior audit status missing. to insert/stick in/pierce/to take part in/to interfere/to interpose/to scold. Source readings: caap3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["琴晚", { label: "lex", pos: "lexical_item", jyutping: "kam4 maan5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1701, frequency 12; prior audit status missing. yesterday night. Source readings: kam4maan5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["睇住", { label: "lex", pos: "lexical_item", jyutping: "tai2 zyu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1703, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tai2zyu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["窗", { label: "lex", pos: "lexical_item", jyutping: "coeng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1704, frequency 12; prior audit status missing. shutter/window. Source readings: coeng1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["結", { label: "lex", pos: "lexical_item", jyutping: "git3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1705, frequency 12; prior audit status missing. knot/sturdy/bond/to tie/to bind/to check out (of a hotel)/to bear fruit/to produce/firm/solid. Source readings: git3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["貼住", { label: "lex", pos: "lexical_item", jyutping: "tip1 zyu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1706, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tip1zyu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["開燈", { label: "lex", pos: "lexical_item", jyutping: "hoi1 dang1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1707, frequency 12; prior audit status missing. to turn on a light. Source readings: hoi1dang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["溜", { label: "lex", pos: "lexical_item", jyutping: "lau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1708, frequency 12; prior audit status missing. to slip away/to escape in stealth/to skate. Source readings: lau6|lau1|liu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["溫暖", { label: "lex", pos: "lexical_item", jyutping: "wan1 nyun5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1709, frequency 12; prior audit status missing. warm. Source readings: wan1nyun5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["罪", { label: "lex", pos: "lexical_item", jyutping: "zeoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1710, frequency 12; prior audit status missing. guilt/crime/fault/blame/sin. Source readings: zeoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["補充", { label: "lex", pos: "lexical_item", jyutping: "bou2 cung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1711, frequency 12; prior audit status missing. to replenish/to supplement/to complement/additional/supplementary. Source readings: bou2cung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["詳細", { label: "lex", pos: "lexical_item", jyutping: "coeng4 sai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1712, frequency 12; prior audit status missing. detailed/in detail/minute. Source readings: coeng4sai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["誇張", { label: "lex", pos: "lexical_item", jyutping: "kwaa1 zoeng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1713, frequency 12; prior audit status missing. to exaggerate/vaunted/overstated/exaggerated. Source readings: kwaa1zoeng1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["遇到", { label: "lex", pos: "lexical_item", jyutping: "jyu6 dou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1714, frequency 12; prior audit status missing. to meet/to run into/to come across. Source readings: jyu6dou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["電芯", { label: "lex", pos: "lexical_item", jyutping: "din6 sam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1715, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *din6sam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘉賓", { label: "lex", pos: "lexical_item", jyutping: "gaa1 ban1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1716, frequency 12; prior audit status missing. esteemed guest/honored guest. Source readings: gaa1ban1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["對面", { label: "lex", pos: "lexical_item", jyutping: "deoi3 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1717, frequency 12; prior audit status handled_structurally. opposite. Source readings: deoi3min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["製造", { label: "lex", pos: "lexical_item", jyutping: "zai3 zou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1718, frequency 12; prior audit status missing. to manufacture/to make. Source readings: zai3zou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["增長", { label: "lex", pos: "lexical_item", jyutping: "zang1 zoeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1719, frequency 12; prior audit status missing. to grow/to increase. Source readings: zang1zoeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["黎明", { label: "lex", pos: "lexical_item", jyutping: "lai4 ming4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1720, frequency 12; prior audit status missing. dawn/daybreak. Source readings: lai4ming4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["橫線", { label: "lex", pos: "lexical_item", jyutping: "waang4 sin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1721, frequency 12; prior audit status missing. horizontal line/horizontal coordinate line. Source readings: waang4sin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["澳門", { label: "lex", pos: "lexical_item", jyutping: "ou3 mun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1722, frequency 12; prior audit status missing. Macao/Macau. Source readings: ou3mun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["燒", { label: "lex", pos: "lexical_item", jyutping: "siu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1723, frequency 12; prior audit status missing. to burn/to cook/to stew/to bake/to roast/fever. Source readings: siu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["興", { label: "lex", pos: "lexical_item", jyutping: "hing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1724, frequency 12; prior audit status missing. flourish/it is the fashion to/to become popular/interest. Source readings: hing1|hing3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["龜", { label: "lex", pos: "lexical_item", jyutping: "gwai1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1725, frequency 12; prior audit status missing. tortoise/turtle/CL:隻[jek]/the police/cowardly. Source readings: gwai1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["尷尬", { label: "lex", pos: "lexical_item", jyutping: "gaam3 gaai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1726, frequency 12; prior audit status missing. awkward/embarrassed. Source readings: gaam3gaai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["邊有", { label: "lex", pos: "lexical_item", jyutping: "bin1 jau5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1728, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *bin1jau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["響", { label: "lex", pos: "lexical_item", jyutping: "hoeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1729, frequency 12; prior audit status missing. echo/sound/noise/to make a sound/to sound/to ring/loud/classifier for noises (Mandarin). Source readings: hoeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["爛", { label: "lex", pos: "lexical_item", jyutping: "laan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1730, frequency 12; prior audit status missing. soft/mushy/well-cooked and soft/to rot/to decompose/rotten/worn out/chaotic/messy/utterly/thoroughly. Source readings: laan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["聽過", { label: "lex", pos: "lexical_item", jyutping: "teng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1731, frequency 12; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *teng1|ting1|ting3gwo3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["廳", { label: "lex", pos: "lexical_item", jyutping: "teng1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1732, frequency 12; prior audit status missing. (reception) hall/office. Source readings: teng1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一句", { label: "lex", pos: "lexical_item", jyutping: "jat1 geoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1733, frequency 11; prior audit status handled_structurally. a line of verse/a sentence. Source readings: jat1geoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一號", { label: "lex", pos: "lexical_item", jyutping: "jat1 hou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1734, frequency 11; prior audit status handled_structurally. first day of the month. Source readings: jat1hou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["九月", { label: "lex", pos: "lexical_item", jyutping: "gau2 jyut6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1735, frequency 11; prior audit status handled_structurally. ninth month/September. Source readings: gau2jyut6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["人話", { label: "lex", pos: "lexical_item", jyutping: "jan4 waa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1736, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jan4waa6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["千祈", { label: "lex", pos: "lexical_item", jyutping: "cin1 kei4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1737, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *cin1kei4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["工人", { label: "lex", pos: "lexical_item", jyutping: "gung1 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1738, frequency 11; prior audit status handled_structurally. worker. Source readings: gung1jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["工業", { label: "lex", pos: "lexical_item", jyutping: "gung1 jip6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1739, frequency 11; prior audit status missing. industry. Source readings: gung1jip6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["五個", { label: "lex", pos: "lexical_item", jyutping: "ng5 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1740, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ng5go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["及", { label: "lex", pos: "lexical_item", jyutping: "kap6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1741, frequency 11; prior audit status missing. and/to reach/up to/in time for. Source readings: kap6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["反對", { label: "lex", pos: "lexical_item", jyutping: "faan2 deoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1742, frequency 11; prior audit status missing. to fight against/to oppose/to be opposed to/opposition. Source readings: faan2deoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["心胸", { label: "lex", pos: "lexical_item", jyutping: "sam1 hung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1744, frequency 11; prior audit status missing. breadth of mind. Source readings: sam1hung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["比例", { label: "lex", pos: "lexical_item", jyutping: "bei2 lai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1745, frequency 11; prior audit status missing. proportion/scale. Source readings: bei2lai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["牛肉", { label: "lex", pos: "lexical_item", jyutping: "ngau4 juk6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1746, frequency 11; prior audit status missing. beef. Source readings: ngau4juk6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["冬天", { label: "lex", pos: "lexical_item", jyutping: "dung1 tin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1747, frequency 11; prior audit status missing. winter. Source readings: dung1tin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["出版", { label: "lex", pos: "lexical_item", jyutping: "ceot1 baan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1748, frequency 11; prior audit status missing. to publish/to come off the press/to put out. Source readings: ceot1baan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["半年", { label: "lex", pos: "lexical_item", jyutping: "bun3 nin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1749, frequency 11; prior audit status handled_structurally. half a year. Source readings: bun3nin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["司長", { label: "lex", pos: "lexical_item", jyutping: "si1 zoeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1751, frequency 11; prior audit status missing. bureau chief. Source readings: si1zoeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["必要", { label: "lex", pos: "lexical_item", jyutping: "bit1 jiu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1752, frequency 11; prior audit status missing. necessary/essential/indispensable/required. Source readings: bit1jiu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["本地", { label: "lex", pos: "lexical_item", jyutping: "bun2 dei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1753, frequency 11; prior audit status missing. local/this locality. Source readings: bun2dei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["正上方", { label: "lex", pos: "lexical_item", jyutping: "zing3 soeng6 fong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1754, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: zing3soeng6fong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["正版", { label: "lex", pos: "lexical_item", jyutping: "zing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1755, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zing1|zing3|zengbaan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["甩", { label: "lex", pos: "lexical_item", jyutping: "lat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1756, frequency 11; prior audit status missing. to lose/to get rid of/to know the mahjong tiles by touch, without needing to look and see if the tile is wanted. Source readings: lat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["光", { label: "lex", pos: "lexical_item", jyutping: "gwong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1757, frequency 11; prior audit status missing. light/ray/CL:道[douh]/bright/only/merely/to use up. Source readings: gwong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["再行", { label: "lex", pos: "lexical_item", jyutping: "zoi3 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1758, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zoi3hang4|haang4|hang6|hong4|hong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["在", { label: "lex", pos: "lexical_item", jyutping: "zoi6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1759, frequency 11; prior audit status missing. (located) at/(to be) in/to exist/in the middle of doing sth/(indicating an action in progress). Source readings: zoi6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好快", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1761, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3faai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好近", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1762, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3gan6|kan5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好遠", { label: "lex", pos: "lexical_item", jyutping: "hou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1763, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hou2|hou3jyun5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["妄想", { label: "lex", pos: "lexical_item", jyutping: "mong5 soeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1764, frequency 11; prior audit status missing. to attempt vainly/a vain attempt. Source readings: mong5soeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["成年", { label: "lex", pos: "lexical_item", jyutping: "sing4 nin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1766, frequency 11; prior audit status missing. adult. Source readings: sing4nin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["老細", { label: "lex", pos: "lexical_item", jyutping: "lou5 sai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1767, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lou5sai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["自從", { label: "lex", pos: "lexical_item", jyutping: "zi6 cung4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1768, frequency 11; prior audit status missing. since (a time)/ever since. Source readings: zi6cung4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["初頭", { label: "lex", pos: "lexical_item", jyutping: "co1 tau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1769, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *co1tau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["局", { label: "lex", pos: "lexical_item", jyutping: "guk6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1770, frequency 11; prior audit status missing. office/situation/classifier for games: match, set, round etc. Source readings: guk6|guk2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我問", { label: "lex", pos: "lexical_item", jyutping: "ngo5 man6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1771, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5man6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["足夠", { label: "lex", pos: "lexical_item", jyutping: "zuk1 gau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1772, frequency 11; prior audit status missing. enough/sufficient. Source readings: zuk1gau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["邪", { label: "lex", pos: "lexical_item", jyutping: "ce4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1773, frequency 11; prior audit status missing. demonic/iniquitous/nefarious/evil/unhealthy influences that cause disease (Chinese medicine). Source readings: ce4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["事業", { label: "lex", pos: "lexical_item", jyutping: "si6 jip6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1774, frequency 11; prior audit status missing. undertaking/project/activity/(charitable, political or revolutionary) cause/publicly funded institution, enterprise or foundation/career/occupation. Source readings: si6jip6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩點", { label: "lex", pos: "lexical_item", jyutping: "loeng5 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1775, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: loeng5dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["咀", { label: "lex", pos: "lexical_item", jyutping: "zeoi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1776, frequency 11; prior audit status missing. to kiss. Source readings: zeoi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["定義", { label: "lex", pos: "lexical_item", jyutping: "ding6 ji6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1777, frequency 11; prior audit status missing. definition. Source readings: ding6ji6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["忽然", { label: "lex", pos: "lexical_item", jyutping: "fat1 jin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1778, frequency 11; prior audit status missing. suddenly/all of a sudden. Source readings: fat1jin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["拃", { label: "lex", pos: "lexical_item", jyutping: "", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1779, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *?. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["東面", { label: "lex", pos: "lexical_item", jyutping: "dung1 min6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1781, frequency 11; prior audit status handled_structurally. east side (of sth). Source readings: dung1min6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["欣賞", { label: "lex", pos: "lexical_item", jyutping: "jan1 soeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1782, frequency 11; prior audit status missing. to appreciate/to enjoy/to admire. Source readings: jan1soeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["股價", { label: "lex", pos: "lexical_item", jyutping: "gu2 gaa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1783, frequency 11; prior audit status missing. stock price/share price. Source readings: gu2gaa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["表現", { label: "lex", pos: "lexical_item", jyutping: "biu2 jin6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1784, frequency 11; prior audit status missing. to show/to show off/to display/to manifest/expression/manifestation/show/display/performance (at work etc). Source readings: biu2jin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["長遠", { label: "lex", pos: "lexical_item", jyutping: "coeng4 jyun5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1785, frequency 11; prior audit status handled_structurally. long-term/long-range. Source readings: coeng4jyun5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["保險", { label: "lex", pos: "lexical_item", jyutping: "bou2 him2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1786, frequency 11; prior audit status missing. insurance/to insure/safe/secure/be sure/be bound to. Source readings: bou2him2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["活動", { label: "lex", pos: "lexical_item", jyutping: "wut6 dung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1787, frequency 11; prior audit status missing. to exercise/to move about/to operate/activity/loose/shakey/active/movable/maneuver/to use connections. Source readings: wut6dung6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["重心", { label: "lex", pos: "lexical_item", jyutping: "zung6 sam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1788, frequency 11; prior audit status missing. center of gravity/central core/main part. Source readings: zung6sam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["哲學", { label: "lex", pos: "lexical_item", jyutping: "zit3 hok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1789, frequency 11; prior audit status missing. philosophy. Source readings: zit3hok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["家人", { label: "lex", pos: "lexical_item", jyutping: "gaa1 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1790, frequency 11; prior audit status handled_structurally. household/(one's) family. Source readings: gaa1jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["校", { label: "lex", pos: "lexical_item", jyutping: "haau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1792, frequency 11; prior audit status missing. school/CL:所[só](Mandarin)/CL:間[gāan]|to proofread/to check/to compare/military officer. Source readings: haau6|gaau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["窄", { label: "lex", pos: "lexical_item", jyutping: "zaak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1793, frequency 11; prior audit status missing. narrow. Source readings: zaak3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["粉", { label: "lex", pos: "lexical_item", jyutping: "fan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1794, frequency 11; prior audit status missing. powder/cosmetic face powder/food prepared from starch/noodles or pasta made from any kind of flour/whitewash/white/pink/heroin. Source readings: fan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["做法", { label: "lex", pos: "lexical_item", jyutping: "zou6 faat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1795, frequency 11; prior audit status handled_structurally. way of handling sth/method for making/work method/practice. Source readings: zou6faat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["健康", { label: "lex", pos: "lexical_item", jyutping: "gin6 hong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1796, frequency 11; prior audit status missing. health/healthy. Source readings: gin6hong1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唯一", { label: "lex", pos: "lexical_item", jyutping: "wai4 jat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1797, frequency 11; prior audit status missing. only/sole. Source readings: wai4jat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唱片", { label: "lex", pos: "lexical_item", jyutping: "coeng3 pin2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1798, frequency 11; prior audit status missing. gramophone record/LP. Source readings: coeng3pin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["婚姻", { label: "lex", pos: "lexical_item", jyutping: "fan1 jan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1799, frequency 11; prior audit status missing. matrimony/wedding/marriage. Source readings: fan1jan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["接觸", { label: "lex", pos: "lexical_item", jyutping: "zip3 zuk1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1800, frequency 11; prior audit status missing. to touch/to contact/access/in touch with. Source readings: zip3zuk1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["梗", { label: "lex", pos: "lexical_item", jyutping: "gang2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1801, frequency 11; prior audit status missing. stem. Source readings: gang2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["清", { label: "lex", pos: "lexical_item", jyutping: "cing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1802, frequency 11; prior audit status missing. clear/distinct/quiet/just and honest/pure/to settle or clear up/to clean up or purge/Qing or Ch'ing dynasty of Imperial China (1644-1911). Source readings: cing1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["率", { label: "lex", pos: "lexical_item", jyutping: "leot6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1803, frequency 11; prior audit status missing. rate/frequency/to lead/to command/rash/hasty/frank/straightforward/generally/usually. Source readings: leot6|seot1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["現象", { label: "lex", pos: "lexical_item", jyutping: "jin6 zoeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1804, frequency 11; prior audit status missing. appearance/phenomenon. Source readings: jin6zoeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["笪", { label: "lex", pos: "lexical_item", jyutping: "daat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1805, frequency 11; prior audit status missing. surname Da/rough bamboo mat. Source readings: daat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["第二個韻", { label: "lex", pos: "lexical_item", jyutping: "dai6 ji6 go3 wan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1806, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dai6ji6go3wan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["第時", { label: "lex", pos: "lexical_item", jyutping: "dai6 si4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1807, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dai6si4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["粗口", { label: "lex", pos: "lexical_item", jyutping: "cou1 hau2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1808, frequency 11; prior audit status missing. swear words/obscene language. Source readings: cou1hau2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["細妹", { label: "lex", pos: "lexical_item", jyutping: "sai3 mui6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1809, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *sai3mui6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["終", { label: "lex", pos: "lexical_item", jyutping: "zung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1810, frequency 11; prior audit status missing. end/finish. Source readings: zung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["規則", { label: "lex", pos: "lexical_item", jyutping: "kwai1 zak1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1811, frequency 11; prior audit status missing. rule/regulation/rules and regulations. Source readings: kwai1zak1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["通", { label: "lex", pos: "lexical_item", jyutping: "tung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1812, frequency 11; prior audit status missing. to go through/to know well/to connect/to communicate/to clear/open. Source readings: tung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["通知", { label: "lex", pos: "lexical_item", jyutping: "tung1 zi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1813, frequency 11; prior audit status missing. to notify/to inform/notice/notification. Source readings: tung1zi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["創意", { label: "lex", pos: "lexical_item", jyutping: "cong3 ji3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1814, frequency 11; prior audit status missing. creative/creativity. Source readings: cong3ji3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["創新", { label: "lex", pos: "lexical_item", jyutping: "cong3 san1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1815, frequency 11; prior audit status missing. innovation/to bring forth new ideas/to blaze new trails. Source readings: cong3san1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["就講", { label: "lex", pos: "lexical_item", jyutping: "zau6 gong2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1816, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zau6gong2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幾日", { label: "lex", pos: "lexical_item", jyutping: "gei2 jat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1817, frequency 11; prior audit status handled_structurally. several days. Source readings: gei2jat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["普遍", { label: "lex", pos: "lexical_item", jyutping: "pou2 pin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1820, frequency 11; prior audit status missing. universal/general/widespread/common. Source readings: pou2pin3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["評價", { label: "lex", pos: "lexical_item", jyutping: "ping4 gaa3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1821, frequency 11; prior audit status missing. to evaluate/to assess. Source readings: ping4gaa3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["意義", { label: "lex", pos: "lexical_item", jyutping: "ji3 ji6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1822, frequency 11; prior audit status missing. meaning/significance. Source readings: ji3ji6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["愛情", { label: "lex", pos: "lexical_item", jyutping: "oi3 cing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1823, frequency 11; prior audit status missing. romance/love (between man and woman). Source readings: oi3cing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["業", { label: "lex", pos: "lexical_item", jyutping: "jip6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1824, frequency 11; prior audit status missing. business/occupation/study. Source readings: jip6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["業主", { label: "lex", pos: "lexical_item", jyutping: "jip6 zyu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1825, frequency 11; prior audit status missing. owner/proprietor. Source readings: jip6zyu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["當你", { label: "lex", pos: "lexical_item", jyutping: "dong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1826, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dong1|dong3nei5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["當我", { label: "lex", pos: "lexical_item", jyutping: "dong1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1827, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dong1|dong3ngo5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["罪行", { label: "lex", pos: "lexical_item", jyutping: "zeoi6 hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1828, frequency 11; prior audit status missing. crime/offense. Source readings: zeoi6hang4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["較", { label: "lex", pos: "lexical_item", jyutping: "gaau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1829, frequency 11; prior audit status missing. comparatively/(preposition comparing difference in degree)/to contrast/to compare/rather/fairly/clearly (different)/markedly/to haggle over/to quibble. Source readings: gaau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["遊戲", { label: "lex", pos: "lexical_item", jyutping: "jau4 hei3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1830, frequency 11; prior audit status missing. game/play. Source readings: jau4hei3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["電訊", { label: "lex", pos: "lexical_item", jyutping: "din6 seon3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1831, frequency 11; prior audit status missing. telecommunications/telecom. Source readings: din6seon3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["鼓勵", { label: "lex", pos: "lexical_item", jyutping: "gu2 lai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1832, frequency 11; prior audit status missing. to encourage. Source readings: gu2lai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘔", { label: "lex", pos: "lexical_item", jyutping: "au2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1833, frequency 11; prior audit status missing. vomit. Source readings: au2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["實際上", { label: "lex", pos: "lexical_item", jyutping: "sat6 zai3 soeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1834, frequency 11; prior audit status missing. in fact/in reality/as a matter of fact/in practice. Source readings: sat6zai3soeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["瘦", { label: "lex", pos: "lexical_item", jyutping: "sau3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1837, frequency 11; prior audit status missing. thin/lean. Source readings: sau3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["賭", { label: "lex", pos: "lexical_item", jyutping: "dou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1838, frequency 11; prior audit status missing. to bet/to gamble. Source readings: dou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["踩", { label: "lex", pos: "lexical_item", jyutping: "caai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1839, frequency 11; prior audit status missing. to step on/to tread/to stamp/to press a pedal/to pedal (a bike)/to enter, to cross into (territory). Source readings: caai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["燕梳", { label: "lex", pos: "lexical_item", jyutping: "jin3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1840, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jin3|jin1so1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["幫到", { label: "lex", pos: "lexical_item", jyutping: "bong1 dou3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1842, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *bong1dou3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["戲院", { label: "lex", pos: "lexical_item", jyutping: "hei3 jyun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1843, frequency 11; prior audit status missing. theater. Source readings: hei3jyun2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["擦紙膠", { label: "lex", pos: "lexical_item", jyutping: "caat3 zi2 gaau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1844, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: caat3zi2gaau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["營", { label: "lex", pos: "lexical_item", jyutping: "jing4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1845, frequency 11; prior audit status missing. army/to deal in/to trade/to operate/to run/camp/nourishment/to manage. Source readings: jing4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["聯絡", { label: "lex", pos: "lexical_item", jyutping: "lyun4 lok3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1846, frequency 11; prior audit status missing. communication/to get in touch with/to contact/connection (math.). Source readings: lyun4lok3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["講開", { label: "lex", pos: "lexical_item", jyutping: "gong2 hoi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1848, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gong2hoi1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["黏", { label: "lex", pos: "lexical_item", jyutping: "nim4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1849, frequency 11; prior audit status missing. to stick/sticky/glutinous/glue. Source readings: nim4|nim1|zim1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["禮拜三", { label: "lex", pos: "lexical_item", jyutping: "lai5 baai3 saam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1850, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lai5baai3saam1|saam3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["禮拜六", { label: "lex", pos: "lexical_item", jyutping: "lai5 baai3 luk6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1851, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lai5baai3luk6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["翻譯", { label: "lex", pos: "lexical_item", jyutping: "faan1 jik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1852, frequency 11; prior audit status missing. to translate/to interpret/translator/interpreter/translation/interpretation. Source readings: faan1jik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["轉右", { label: "lex", pos: "lexical_item", jyutping: "zyun2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1853, frequency 11; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *zyun2|zyun3jau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["轉頭", { label: "lex", pos: "lexical_item", jyutping: "zyun3 tau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1854, frequency 11; prior audit status handled_structurally. to turn one's head/to change direction/U-turn/volte face/to repent/nutation (plants turning to face the sun). Source readings: zyun3tau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["額", { label: "lex", pos: "lexical_item", jyutping: "ngaak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1855, frequency 11; prior audit status missing. volume/amount/quantity/forehead/quota. Source readings: ngaak6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["鯉", { label: "lex", pos: "lexical_item", jyutping: "lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1856, frequency 11; prior audit status missing. carp. Source readings: lei5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["穩定", { label: "lex", pos: "lexical_item", jyutping: "wan2 ding6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1857, frequency 11; prior audit status missing. steady/stable/stability/to stabilize/to pacify. Source readings: wan2ding6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嚴重", { label: "lex", pos: "lexical_item", jyutping: "jim4 zung6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1859, frequency 11; prior audit status missing. grave/serious/severe/critical. Source readings: jim4zung6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["屬於", { label: "lex", pos: "lexical_item", jyutping: "suk6 jyu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1860, frequency 11; prior audit status missing. classified as/to belong to/to be part of. Source readings: suk6jyu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["聽到", { label: "lex", pos: "lexical_item", jyutping: "teng1 dou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1861, frequency 11; prior audit status handled_structurally. to hear. Source readings: teng1dou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["黐", { label: "lex", pos: "lexical_item", jyutping: "ci1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1862, frequency 11; prior audit status missing. sticky/crazy/to stick to/to go crazy. Source readings: ci1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["顯示", { label: "lex", pos: "lexical_item", jyutping: "hin2 si6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1863, frequency 11; prior audit status missing. to show/to illustrate/to display/to demonstrate. Source readings: hin2si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一模一樣", { label: "lex", pos: "lexical_item", jyutping: "jat1 mou4 jat1 joeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1864, frequency 10; prior audit status missing. exactly the same (idiom)/carbon copy. Source readings: jat1mou4jat1joeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["一邊", { label: "lex", pos: "lexical_item", jyutping: "jat1 bin1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1865, frequency 10; prior audit status handled_structurally. one side/either side/on the one hand/on the other hand/doing while. Source readings: jat1bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["九十度", { label: "lex", pos: "lexical_item", jyutping: "gau2 sap6 dou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1866, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gau2sap6dou6|dok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["人生", { label: "lex", pos: "lexical_item", jyutping: "jan4 sang1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1867, frequency 10; prior audit status handled_structurally. life. Source readings: jan4sang1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["十一點", { label: "lex", pos: "lexical_item", jyutping: "sap6 jat1 dim2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1868, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: sap6jat1dim2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["上堂", { label: "lex", pos: "lexical_item", jyutping: "soeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1869, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *soeng6|soeng5tong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["下畫", { label: "lex", pos: "lexical_item", jyutping: "haa6 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1870, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *haa6waak6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["山個", { label: "lex", pos: "lexical_item", jyutping: "saan1 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1871, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *saan1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["中國人", { label: "lex", pos: "lexical_item", jyutping: "zung1 gwok3 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1872, frequency 10; prior audit status handled_structurally. Chinese person/Chinese people. Source readings: zung1gwok3jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["五月", { label: "lex", pos: "lexical_item", jyutping: "ng5 jyut6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1873, frequency 10; prior audit status handled_structurally. May/fifth month. Source readings: ng5jyut6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["內心", { label: "lex", pos: "lexical_item", jyutping: "noi6 sam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1874, frequency 10; prior audit status missing. heart/innermost being. Source readings: noi6sam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["反映", { label: "lex", pos: "lexical_item", jyutping: "faan2 jing2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1875, frequency 10; prior audit status missing. to mirror/to reflect/mirror image/reflection/fig. to report/to make known/to render/used erroneously for 反應|反应, response or reaction. Source readings: faan2jing2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["天堂", { label: "lex", pos: "lexical_item", jyutping: "tin1 tong4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1876, frequency 10; prior audit status missing. paradise/heaven. Source readings: tin1tong4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["心態", { label: "lex", pos: "lexical_item", jyutping: "sam1 taai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1878, frequency 10; prior audit status missing. attitude (of the heart)/state of one's psyche/way of thinking/mentality. Source readings: sam1taai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["文", { label: "lex", pos: "lexical_item", jyutping: "man4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1879, frequency 10; prior audit status missing. language/culture/writing/formal/literary/gentle/surname Wen. Source readings: man4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["止", { label: "lex", pos: "lexical_item", jyutping: "zi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1880, frequency 10; prior audit status missing. to stop/to prohibit/untill. Source readings: zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["火星", { label: "lex", pos: "lexical_item", jyutping: "fo2 sing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1881, frequency 10; prior audit status missing. Mars (planet). Source readings: fo2sing1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["仙人掌", { label: "lex", pos: "lexical_item", jyutping: "sin1 jan4 zoeng2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1882, frequency 10; prior audit status missing. cactus. Source readings: sin1jan4zoeng2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["以往", { label: "lex", pos: "lexical_item", jyutping: "ji5 wong5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1883, frequency 10; prior audit status missing. in the past/formerly. Source readings: ji5wong5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["凹", { label: "lex", pos: "lexical_item", jyutping: "nap1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1884, frequency 10; prior audit status missing. a depression/indentation/concave/hollow. Source readings: nap1|aau3|waa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["出席", { label: "lex", pos: "lexical_item", jyutping: "ceot1 zik6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1885, frequency 10; prior audit status missing. to attend/to participate/present. Source readings: ceot1zik6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["加返", { label: "lex", pos: "lexical_item", jyutping: "gaa1 faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1887, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *gaa1faan1|faan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["可憐", { label: "lex", pos: "lexical_item", jyutping: "ho2 lin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1888, frequency 10; prior audit status missing. pitiful/pathetic. Source readings: ho2lin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["失", { label: "lex", pos: "lexical_item", jyutping: "sat1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1889, frequency 10; prior audit status missing. to lose/to miss/to fail. Source readings: sat1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["民主", { label: "lex", pos: "lexical_item", jyutping: "man4 zyu2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1890, frequency 10; prior audit status missing. democracy. Source readings: man4zyu2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["民建聯", { label: "lex", pos: "lexical_item", jyutping: "man4 gin3 lyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1891, frequency 10; prior audit status missing. abbr. for 民主建港協進聯盟|民主建港协进联盟, Democratic alliance for the betterment of Hong Kong (DAB), Hong Kong pro-Beijing party. Source readings: man4gin3lyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["生物", { label: "lex", pos: "lexical_item", jyutping: "sang1 mat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1892, frequency 10; prior audit status missing. organism/living creature/life form/biological. Source readings: sang1mat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["由個", { label: "lex", pos: "lexical_item", jyutping: "jau4 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1893, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau4go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["全名", { label: "lex", pos: "lexical_item", jyutping: "cyun4 ming4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1894, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *cyun4ming4|meng4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["合", { label: "lex", pos: "lexical_item", jyutping: "hap6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1895, frequency 10; prior audit status missing. 100 ml/one-tenth of a peck/measure for dry grain equal to one-tenth of sheng 升 or liter, or one-hundredth dou 斗/Chinese musical note/fit/to join. Source readings: hap6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["合法", { label: "lex", pos: "lexical_item", jyutping: "hap6 faat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1896, frequency 10; prior audit status missing. lawful/legitimate/legal. Source readings: hap6faat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["吋", { label: "lex", pos: "lexical_item", jyutping: "cyun3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1897, frequency 10; prior audit status missing. inch (English). Source readings: cyun3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["同時", { label: "lex", pos: "lexical_item", jyutping: "tung4 si4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1898, frequency 10; prior audit status handled_structurally. at the same time/simultaneously. Source readings: tung4si4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["同樣", { label: "lex", pos: "lexical_item", jyutping: "tung4 joeng6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1899, frequency 10; prior audit status handled_structurally. same/equal/equivalent. Source readings: tung4joeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["地球", { label: "lex", pos: "lexical_item", jyutping: "dei6 kau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1900, frequency 10; prior audit status missing. the Earth/planet. Source readings: dei6kau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["好人", { label: "lex", pos: "lexical_item", jyutping: "hou2 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1901, frequency 10; prior audit status handled_structurally. good person. Source readings: hou2jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["安全", { label: "lex", pos: "lexical_item", jyutping: "on1 cyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1904, frequency 10; prior audit status missing. safe/secure/safety/security. Source readings: on1cyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["年青", { label: "lex", pos: "lexical_item", jyutping: "nin4 cing1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1905, frequency 10; prior audit status missing. youthful. Source readings: nin4cing1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["年齡", { label: "lex", pos: "lexical_item", jyutping: "nin4 ling4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1906, frequency 10; prior audit status missing. (a person's) age. Source readings: nin4ling4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["式", { label: "lex", pos: "lexical_item", jyutping: "sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1907, frequency 10; prior audit status missing. type/form/pattern/style. Source readings: sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["曲", { label: "lex", pos: "lexical_item", jyutping: "kuk1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1909, frequency 10; prior audit status missing. bent/crooked/wrong/tune/song. Source readings: kuk1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有份", { label: "lex", pos: "lexical_item", jyutping: "jau5 fan6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1910, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau5fan6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["有位", { label: "lex", pos: "lexical_item", jyutping: "jau5 wai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1911, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jau5wai6|wai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["老公", { label: "lex", pos: "lexical_item", jyutping: "lou5 gung1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1912, frequency 10; prior audit status missing. husband. Source readings: lou5gung1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["考到", { label: "lex", pos: "lexical_item", jyutping: "haau2 dou2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1913, frequency 10; prior audit status missing. to test. Source readings: haau2dou2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["行返", { label: "lex", pos: "lexical_item", jyutping: "hang4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1914, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *hang4|haang4|hang6|hong4|hong2faan1|faan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["行路", { label: "lex", pos: "lexical_item", jyutping: "haang4 lou6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1915, frequency 10; prior audit status handled_structurally. to travel/transport. Source readings: haang4lou6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["你明", { label: "lex", pos: "lexical_item", jyutping: "nei5 ming4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1916, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *nei5ming4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["你識", { label: "lex", pos: "lexical_item", jyutping: "nei5 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1917, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *nei5sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["判斷", { label: "lex", pos: "lexical_item", jyutping: "pun3 tyun5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1918, frequency 10; prior audit status missing. to decide/to determine. Source readings: pun3tyun5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["別人", { label: "lex", pos: "lexical_item", jyutping: "bit6 jan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1919, frequency 10; prior audit status missing. other people/others/other person. Source readings: bit6jan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["完整", { label: "lex", pos: "lexical_item", jyutping: "jyun4 zing2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1920, frequency 10; prior audit status handled_structurally. complete/intact. Source readings: jyun4zing2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["我明", { label: "lex", pos: "lexical_item", jyutping: "ngo5 ming4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1921, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngo5ming4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["使錢", { label: "lex", pos: "lexical_item", jyutping: "sai2 cin2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1922, frequency 10; prior audit status handled_structurally. to spend money. Source readings: sai2cin2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["依然", { label: "lex", pos: "lexical_item", jyutping: "ji1 jin4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1923, frequency 10; prior audit status missing. still/as before. Source readings: ji1jin4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兔仔", { label: "lex", pos: "lexical_item", jyutping: "tou3 zai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1924, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *tou3zai2|zi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兩位", { label: "lex", pos: "lexical_item", jyutping: "loeng5 wai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1925, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *loeng5wai6|wai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["呢邊", { label: "lex", pos: "lexical_item", jyutping: "ne1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1926, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ne1|ni1|nei1bin1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["味", { label: "lex", pos: "lexical_item", jyutping: "mei6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1927, frequency 10; prior audit status missing. taste/smell/a sexual service, a type of sex or sexual stimulation offered by a prostitute. Source readings: mei6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["和", { label: "lex", pos: "lexical_item", jyutping: "wo4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1928, frequency 10; prior audit status missing. and/together with/with/sum/union/peace/harmony/surname Wo/Japanese related/cap (a poem)/to respond in singing/soft/warm/to complete a set in mahjong or playing cards/to mix together/to blend. Source readings: wo4|wo6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["姊妹", { label: "lex", pos: "lexical_item", jyutping: "zi2 mui6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1929, frequency 10; prior audit status missing. (older and younger) sisters|1. female friends/2. women who are close friends and show loyalty to each other. Source readings: zi2mui6|zi2mui2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["定下", { label: "lex", pos: "lexical_item", jyutping: "ding6 haa6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1930, frequency 10; prior audit status handled_structurally. decision/to decide. Source readings: ding6haa6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["板", { label: "lex", pos: "lexical_item", jyutping: "baan2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1931, frequency 10; prior audit status missing. board/plank/plate/slab/stiff/unnatural/pedal. Source readings: baan2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["返番", { label: "lex", pos: "lexical_item", jyutping: "faan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1932, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *faan1|faan2faan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["侮辱", { label: "lex", pos: "lexical_item", jyutping: "mou5 juk6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1933, frequency 10; prior audit status missing. to insult/to humiliate/dishonor. Source readings: mou5juk6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["保護", { label: "lex", pos: "lexical_item", jyutping: "bou2 wu6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1934, frequency 10; prior audit status missing. to protect/to defend/to safeguard/protection/CL:種[júng]. Source readings: bou2wu6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["指示", { label: "lex", pos: "lexical_item", jyutping: "zi2 si6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1936, frequency 10; prior audit status missing. to point out/to indicate/(give) directives or instructions. Source readings: zi2si6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["故", { label: "lex", pos: "lexical_item", jyutping: "gu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1937, frequency 10; prior audit status missing. happening/instance/reason/cause/deceased/old. Source readings: gu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["洲", { label: "lex", pos: "lexical_item", jyutping: "zau1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1939, frequency 10; prior audit status missing. continent/island. Source readings: zau1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["界定", { label: "lex", pos: "lexical_item", jyutping: "gaai3 ding6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1940, frequency 10; prior audit status missing. definition/to delimit. Source readings: gaai3ding6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["紅籌", { label: "lex", pos: "lexical_item", jyutping: "hung4 cau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1941, frequency 10; prior audit status missing. red chip. Source readings: hung4cau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["耶穌", { label: "lex", pos: "lexical_item", jyutping: "je4 sou1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1942, frequency 10; prior audit status missing. Jesus. Source readings: je4sou1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["背後", { label: "lex", pos: "lexical_item", jyutping: "bui3 hau6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1943, frequency 10; prior audit status missing. behind/at the back/in the rear/behind sb's back. Source readings: bui3hau6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["要畫", { label: "lex", pos: "lexical_item", jyutping: "jiu3 waak6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1944, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *jiu3waak6|waa2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["飛碟", { label: "lex", pos: "lexical_item", jyutping: "fei1 dip2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1945, frequency 10; prior audit status handled_structurally. flying saucer/frisbee. Source readings: fei1dip2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["個案", { label: "lex", pos: "lexical_item", jyutping: "go3 on3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1946, frequency 10; prior audit status missing. a case/an individual case/case-by-case. Source readings: go3on3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["原", { label: "lex", pos: "lexical_item", jyutping: "jyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1947, frequency 10; prior audit status missing. former/original/primary/raw/level/cause/source. Source readings: jyun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["哩件", { label: "lex", pos: "lexical_item", jyutping: "lei5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1948, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lei5|le1|lei1|li1gin6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唔知幾", { label: "lex", pos: "lexical_item", jyutping: "m4 zi1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1949, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *m4zi1|zi3gei2|gei1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["孭", { label: "lex", pos: "lexical_item", jyutping: "", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1950, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *?. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["旅遊", { label: "lex", pos: "lexical_item", jyutping: "leoi5 jau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1951, frequency 10; prior audit status missing. trip/journey/tourism/travel/tour. Source readings: leoi5jau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["特色", { label: "lex", pos: "lexical_item", jyutping: "dak6 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1952, frequency 10; prior audit status missing. characteristic/distinguishing feature or quality. Source readings: dak6sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["特殊", { label: "lex", pos: "lexical_item", jyutping: "dak6 syu4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1953, frequency 10; prior audit status missing. special/particular/unusual/extraordinary. Source readings: dak6syu4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["狹窄", { label: "lex", pos: "lexical_item", jyutping: "haap6 zaak3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1954, frequency 10; prior audit status missing. narrow. Source readings: haap6zaak3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["退", { label: "lex", pos: "lexical_item", jyutping: "teoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1955, frequency 10; prior audit status missing. retreat/to decline/to move back/to withdraw. Source readings: teoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["針對", { label: "lex", pos: "lexical_item", jyutping: "zam1 deoi3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1956, frequency 10; prior audit status missing. in connection with/directed towards/to direct at/to aim at/to point against. Source readings: zam1deoi3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["偈", { label: "lex", pos: "lexical_item", jyutping: "gai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1957, frequency 10; prior audit status missing. Buddhist hymn/gatha/Buddhist verse/forceful/martial. Source readings: gai2|gai6|git6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["兜個", { label: "lex", pos: "lexical_item", jyutping: "dau1 go3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1958, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dau1go3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["副", { label: "lex", pos: "lexical_item", jyutping: "fu3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1959, frequency 10; prior audit status missing. secondary/auxiliary/deputy/assistant/vice-/abbr. for 副詞|副词 adverb/classifier for pairs. Source readings: fu3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["唯有", { label: "lex", pos: "lexical_item", jyutping: "wai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1960, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *wai4|wai2jau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["專業", { label: "lex", pos: "lexical_item", jyutping: "zyun1 jip6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1961, frequency 10; prior audit status missing. professional/specialty/specialized field/main field of study (at university)/major/CL:門[mùhn],個[go]. Source readings: zyun1jip6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["得滯", { label: "lex", pos: "lexical_item", jyutping: "dak1 zai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1962, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *dak1zai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["梳打", { label: "lex", pos: "lexical_item", jyutping: "so1 daa2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1963, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *so1daa2|daa1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["淨", { label: "lex", pos: "lexical_item", jyutping: "zing6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1964, frequency 10; prior audit status missing. clean/completely/only. Source readings: zing6|zeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["清晰", { label: "lex", pos: "lexical_item", jyutping: "cing1 sik1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1965, frequency 10; prior audit status missing. clear/distinct. Source readings: cing1sik1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["現實", { label: "lex", pos: "lexical_item", jyutping: "jin6 sat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1966, frequency 10; prior audit status missing. reality/actuality/real/actual. Source readings: jin6sat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["船", { label: "lex", pos: "lexical_item", jyutping: "syun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1967, frequency 10; prior audit status missing. a boat/vessel/ship. Source readings: syun4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["規定", { label: "lex", pos: "lexical_item", jyutping: "kwai1 ding6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1968, frequency 10; prior audit status missing. provision/to fix/to set/to formulate/to stipulate/to provide/regulation/rule. Source readings: kwai1ding6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["貨櫃", { label: "lex", pos: "lexical_item", jyutping: "fo3 gwai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1969, frequency 10; prior audit status missing. container (for freight transport). Source readings: fo3gwai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["責任", { label: "lex", pos: "lexical_item", jyutping: "zaak3 jam6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1970, frequency 10; prior audit status missing. responsibility/blame/duty. Source readings: zaak3jam6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["連埋", { label: "lex", pos: "lexical_item", jyutping: "lin4 maai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1971, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *lin4maai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["陰", { label: "lex", pos: "lexical_item", jyutping: "jam1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1972, frequency 10; prior audit status missing. overcast (weather)/cloudy/shady/to betray/to cheat/to trick/Yin (the negative principle of Yin and Yang)/negative (electric.)/feminine/moon/implicit/hidden/pudenda/surname Yam. Source readings: jam1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["單位", { label: "lex", pos: "lexical_item", jyutping: "daan1 wai2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1973, frequency 10; prior audit status handled_structurally. an apartment/a unit/unit (of measure)/work unit (one's workplace)/CL:個[go]. Source readings: daan1wai2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["期間", { label: "lex", pos: "lexical_item", jyutping: "kei4 gaan1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1975, frequency 10; prior audit status missing. period of time/time/time period/period. Source readings: kei4gaan1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["無謂", { label: "lex", pos: "lexical_item", jyutping: "mou4 wai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1976, frequency 10; prior audit status missing. worthless/useless/pointless. Source readings: mou4wai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["發達", { label: "lex", pos: "lexical_item", jyutping: "faat3 daat6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1977, frequency 10; prior audit status missing. developed (country etc)/flourishing/to develop. Source readings: faat3daat6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["硬係", { label: "lex", pos: "lexical_item", jyutping: "ngaang6 hai6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1978, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *ngaang6hai6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["量", { label: "lex", pos: "lexical_item", jyutping: "loeng4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1979, frequency 10; prior audit status missing. to measure|capacity/quantity/amount/to estimate/abbr. for 量詞[leuhng chìh], classifier (in Chinese grammar)/measure word. Source readings: loeng4|loeng6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["開會", { label: "lex", pos: "lexical_item", jyutping: "hoi1 wui2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1980, frequency 10; prior audit status handled_structurally. to hold a meeting/to attend a meeting. Source readings: hoi1wui2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["傳", { label: "lex", pos: "lexical_item", jyutping: "cyun4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1981, frequency 10; prior audit status missing. biography/to pass on/to spread/to transmit/to infect/to transfer/to circulate/to conduct (electricity). Source readings: cyun4|zyun6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["傳媒", { label: "lex", pos: "lexical_item", jyutping: "cyun4 mui4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1982, frequency 10; prior audit status missing. media. Source readings: cyun4mui4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["傾計", { label: "lex", pos: "lexical_item", jyutping: "king1 gai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1983, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *king1gai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["會有", { label: "lex", pos: "lexical_item", jyutping: "wui5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1984, frequency 10; prior audit status manual_review. frequency-list surface; lexical definition requires later human review. Source readings: *wui5|wui6|wui2jau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["煩", { label: "lex", pos: "lexical_item", jyutping: "faan4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1985, frequency 10; prior audit status missing. to feel vexed/to bother/to trouble/superfluous and confusing/edgy. Source readings: faan4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["猷", { label: "lex", pos: "lexical_item", jyutping: "jau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1986, frequency 10; prior audit status missing. to plan/to scheme. Source readings: jau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["圖標", { label: "lex", pos: "lexical_item", jyutping: "tou4 biu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1988, frequency 10; prior audit status missing. (computer) icon. Source readings: tou4biu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["實際", { label: "lex", pos: "lexical_item", jyutping: "sat6 zai3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1989, frequency 10; prior audit status missing. actual/reality/practice. Source readings: sat6zai3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["監察", { label: "lex", pos: "lexical_item", jyutping: "gaam1 caat3", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1990, frequency 10; prior audit status missing. to supervise/to control. Source readings: gaam1caat3. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["端", { label: "lex", pos: "lexical_item", jyutping: "dyun1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1991, frequency 10; prior audit status missing. end/extremity/item/port/to hold sth level with both hands/to carry/regular. Source readings: dyun1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["網球", { label: "lex", pos: "lexical_item", jyutping: "mong5 kau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1992, frequency 10; prior audit status missing. tennis. Source readings: mong5kau4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["齊", { label: "lex", pos: "lexical_item", jyutping: "cai4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1993, frequency 10; prior audit status missing. (name of states and dynasties at several different periods)/surname Qi/neat/even/level with/identical/simultaneous/all together/to even sth out. Source readings: cai4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["嘴", { label: "lex", pos: "lexical_item", jyutping: "zeoi2", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1995, frequency 10; prior audit status missing. mouth/beak/spout (of teapot etc)/個[go]/CL:張[jēung](Mandarin). Source readings: zeoi2. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["撈", { label: "lex", pos: "lexical_item", jyutping: "laau4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1996, frequency 10; prior audit status missing. 1. to mix and stir/2. to make money, often in a seedy profession/3. to make a living. Source readings: laau4|lou4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["數學", { label: "lex", pos: "lexical_item", jyutping: "sou3 hok6", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1997, frequency 10; prior audit status missing. mathematics/mathematical. Source readings: sou3hok6. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["擁有", { label: "lex", pos: "lexical_item", jyutping: "jung2 jau5", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1998, frequency 10; prior audit status missing. to have/to possess. Source readings: jung2jau5. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["輸", { label: "lex", pos: "lexical_item", jyutping: "syu1", syntax: "lexical_item", note: "Cifu SpokenAdult rank 1999, frequency 10; prior audit status missing. to transport/to lose. Source readings: syu1. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }],
      ["隨時", { label: "lex", pos: "lexical_item", jyutping: "ceoi4 si4", syntax: "lexical_item", note: "Cifu SpokenAdult rank 2000, frequency 10; prior audit status missing. at any time. Source readings: ceoi4si4. Exact surface retained as neutral lexical coverage; POS/grammar-role verification is intentionally separate." }]
    ];
  }
});

// src/runtime-resources/lexicon/token-lexicon/index.js
var require_token_lexicon = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/index.js"(exports2, module2) {
    "use strict";
    var { applyReviewedEntries: applyReviewedR251500Entries } = require_cifu_r251_500_reviewed();
    var { applyRuntimePolicy: applyReviewedR1250RuntimePolicy } = require_cifu_r1_250_runtime_policy();
    var { applyCandidateDefaultReadings } = require_cifu_r1_250_candidate_defaults();
    var { applyNativeReviewCorrections } = require_native_review_corrections();
    var { applyRuntimePolicy: applyReviewedR501750RuntimePolicy } = require_cifu_r501_750_runtime_policy();
    var { applyRuntimePolicy: applyReviewedR7511000RuntimePolicy } = require_cifu_r751_1000_runtime_policy();
    var { applyRuntimePolicy: applyReviewedR10011250RuntimePolicy } = require_cifu_r1001_1250_runtime_policy();
    var { applyRuntimePolicy: applyReviewedR12511500RuntimePolicy } = require_cifu_r1251_1500_runtime_policy();
    var { applyRuntimePolicy: applyReviewedR15011750RuntimePolicy } = require_cifu_r1501_1750_runtime_policy();
    var { applyRuntimePolicy: applyReviewedR17512000RuntimePolicy } = require_cifu_r1751_2000_runtime_policy();
    var { applyVernacularComponentCoverage } = require_vernacular_component_coverage();
    var baseEntries = [
      ...require_people_and_address(),
      ...require_referents_and_boundaries(),
      ...require_things(),
      ...require_places_and_times(),
      ...require_verbs(),
      ...require_statives(),
      ...require_function_words_and_particles(),
      ...require_degree_and_formulas(),
      ...require_study_suite(),
      ...require_lexical_coverage_additions(),
      ...require_frequency_gap_fill_r7()
    ];
    module2.exports = applyVernacularComponentCoverage(
      applyReviewedR17512000RuntimePolicy(
        applyReviewedR15011750RuntimePolicy(
          applyReviewedR12511500RuntimePolicy(
            applyReviewedR10011250RuntimePolicy(
              applyReviewedR7511000RuntimePolicy(
                applyReviewedR501750RuntimePolicy(
                  applyNativeReviewCorrections(
                    applyCandidateDefaultReadings(
                      applyReviewedR1250RuntimePolicy(applyReviewedR251500Entries(baseEntries))
                    )
                  )
                )
              )
            )
          )
        )
      )
    );
  }
});

// src/runtime-resources/lexicon/token-lexicon/explicit-analyses.js
var require_explicit_analyses = __commonJS({
  "src/runtime-resources/lexicon/token-lexicon/explicit-analyses.js"(exports2, module2) {
    "use strict";
    var { EXPLICIT_ANALYSES: REVIEWED_R251_500_ANALYSES } = require_cifu_r251_500_reviewed();
    var { EXPLICIT_ANALYSES: REVIEWED_R1_250_ANALYSES } = require_cifu_r1_250_reviewed();
    var { EXPLICIT_ANALYSES: REVIEWED_R1_250_CANDIDATE_DEFAULTS } = require_cifu_r1_250_candidate_defaults();
    var { EXPLICIT_ANALYSES: NATIVE_REVIEW_CORRECTIONS } = require_native_review_corrections();
    var { EXPLICIT_ANALYSIS_OVERRIDES: REVIEWED_R1_250_RUNTIME_POLICY } = require_cifu_r1_250_runtime_policy();
    var contextualAnalyses = Object.freeze({
      "住": Object.freeze([
        Object.freeze({
          id: "lex:住:residence_verb",
          label: "doing",
          pos: "verb",
          jyutping: "zyu6",
          syntax: "verb residence_verb",
          senses: Object.freeze([{ gloss: "live / reside / stay" }]),
          provenance: Object.freeze({ kind: "existing_runtime_contextual_override", source: "src/parser/tokenization/contextual-overrides.js" })
        }),
        Object.freeze({
          id: "lex:住:durative_marker",
          label: "func",
          pos: "particle",
          jyutping: "zyu6",
          syntax: "durative_or_continuing_state_marker",
          senses: Object.freeze([{ gloss: "durative / continuing-state marker" }]),
          provenance: Object.freeze({ kind: "existing_runtime_contextual_override", source: "src/parser/tokenization/contextual-overrides.js" })
        })
      ]),
      "咪": Object.freeze([
        Object.freeze({
          id: "lex:咪:prohibitive_marker",
          label: "func",
          pos: "function",
          jyutping: "mai5",
          syntax: "prohibitive_marker",
          senses: Object.freeze([{ gloss: "don't; prohibitive marker" }]),
          provenance: Object.freeze({ kind: "existing_runtime_contextual_override", source: "src/parser/tokenization/contextual-overrides.js" })
        }),
        Object.freeze({
          id: "lex:咪:discourse_focus_marker",
          label: "func",
          pos: "function",
          jyutping: "mai6",
          syntax: "discourse_focus_marker",
          senses: Object.freeze([{ gloss: "discourse / focus marker" }]),
          provenance: Object.freeze({ kind: "existing_runtime_contextual_override", source: "src/parser/tokenization/contextual-overrides.js" })
        })
      ])
    });
    var reviewedR1250Polyanalyses = Object.freeze(Object.fromEntries(
      Object.entries(REVIEWED_R1_250_ANALYSES).filter(([, rows]) => Array.isArray(rows) && rows.length >= 2)
    ));
    module2.exports = Object.freeze({
      ...contextualAnalyses,
      ...REVIEWED_R251_500_ANALYSES,
      ...reviewedR1250Polyanalyses,
      ...REVIEWED_R1_250_CANDIDATE_DEFAULTS,
      ...NATIVE_REVIEW_CORRECTIONS,
      ...REVIEWED_R1_250_RUNTIME_POLICY
    });
  }
});

// src/runtime-resources/lexicon/lexical-analyses.js
var require_lexical_analyses = __commonJS({
  "src/runtime-resources/lexicon/lexical-analyses.js"(exports2, module2) {
    "use strict";
    var EXPLICIT_ANALYSES = require_explicit_analyses();
    var { buildExplicitAnalyses: buildR501750ExplicitAnalyses } = require_cifu_r501_750_reviewed();
    var { buildExplicitAnalyses: buildR7511000ExplicitAnalyses } = require_cifu_r751_1000_reviewed();
    var { buildExplicitAnalyses: buildR10011250ExplicitAnalyses } = require_cifu_r1001_1250_reviewed();
    var { buildExplicitAnalyses: buildR12511500ExplicitAnalyses } = require_cifu_r1251_1500_reviewed();
    var { buildExplicitAnalyses: buildR15011750ExplicitAnalyses } = require_cifu_r1501_1750_reviewed();
    var { buildExplicitAnalyses: buildR17512000ExplicitAnalyses } = require_cifu_r1751_2000_reviewed();
    function stableLegacyAnalysisId(surface) {
      return `lex:${String(surface || "")}:default`;
    }
    function normalizeSenseRows(value, fallbackGloss = "") {
      if (Array.isArray(value) && value.length) {
        return value.map((row) => {
          if (typeof row === "string") return Object.freeze({ gloss: row });
          return Object.freeze({ ...row || {} });
        });
      }
      return fallbackGloss ? [Object.freeze({ gloss: fallbackGloss })] : [];
    }
    function normalizeAnalysis(surface, baseEntry = {}, analysis = {}, index = 0) {
      const merged = { ...baseEntry, ...analysis };
      delete merged.analyses;
      const id = analysis.id || (index === 0 ? stableLegacyAnalysisId(surface) : `lex:${surface}:${index + 1}`);
      return Object.freeze({
        id,
        surface,
        label: merged.label || "neutral",
        pos: merged.pos || "",
        jyutping: merged.jyutping || "",
        syntax: merged.syntax || "lexical_candidate",
        senses: Object.freeze(normalizeSenseRows(analysis.senses, merged.note || "")),
        semantic: Object.freeze(Array.isArray(merged.semantic) ? [...merged.semantic] : []),
        verb_class: Object.freeze(Array.isArray(merged.verb_class) ? [...merged.verb_class] : []),
        particle_class: merged.particle_class || "",
        classifier_classes: Object.freeze(Array.isArray(merged.classifier_classes) ? [...merged.classifier_classes] : []),
        note: merged.note || "",
        review: merged.review || "",
        provenance: analysis.provenance || baseEntry.provenance || null
      });
    }
    function lexicalAnalysesForEntry(surface, entry = {}, explicitAnalyses = EXPLICIT_ANALYSES) {
      const inline = Array.isArray(entry.analyses) && entry.analyses.length ? entry.analyses : null;
      const explicit = explicitAnalyses && Array.isArray(explicitAnalyses[surface]) && explicitAnalyses[surface].length ? explicitAnalyses[surface] : null;
      const source = inline || explicit;
      if (!source) return Object.freeze([normalizeAnalysis(surface, entry, entry, 0)]);
      return Object.freeze(source.map((analysis, index) => normalizeAnalysis(surface, entry, analysis, index)));
    }
    function effectiveExplicitAnalyses(entries, explicitAnalyses = EXPLICIT_ANALYSES) {
      if (explicitAnalyses !== EXPLICIT_ANALYSES) return explicitAnalyses;
      return Object.freeze({
        ...EXPLICIT_ANALYSES,
        ...buildR501750ExplicitAnalyses(entries),
        ...buildR7511000ExplicitAnalyses(entries),
        ...buildR10011250ExplicitAnalyses(entries),
        ...buildR12511500ExplicitAnalyses(entries),
        ...buildR15011750ExplicitAnalyses(entries),
        ...buildR17512000ExplicitAnalyses(entries)
      });
    }
    function buildLexicalAnalysisIndex2(entries, explicitAnalyses = EXPLICIT_ANALYSES) {
      const entryRows = entries || [];
      const defaultEntries = Object.fromEntries(entryRows);
      const effectiveExplicit = effectiveExplicitAnalyses(entryRows, explicitAnalyses);
      const out = /* @__PURE__ */ Object.create(null);
      for (const [surface, entry] of Object.entries(defaultEntries)) {
        out[surface] = lexicalAnalysesForEntry(surface, entry, effectiveExplicit);
      }
      return Object.freeze(out);
    }
    function lexicalAnalysisById(index, surface, analysisId) {
      const rows = index && index[surface] || [];
      return rows.find((row) => row.id === analysisId) || null;
    }
    module2.exports = {
      EXPLICIT_ANALYSES,
      stableLegacyAnalysisId,
      normalizeAnalysis,
      lexicalAnalysesForEntry,
      effectiveExplicitAnalyses,
      buildLexicalAnalysisIndex: buildLexicalAnalysisIndex2,
      lexicalAnalysisById
    };
  }
});

// src/runtime-resources/lexicon/formulas.js
var require_formulas = __commonJS({
  "src/runtime-resources/lexicon/formulas.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      "唔該晒",
      "唔好意思",
      "對唔住",
      "冇問題",
      "唔緊要",
      "咁就啱啦",
      "咁啱呀",
      "好主意",
      "早晨",
      "你好",
      "多謝",
      "唔該",
      "拜拜"
    ];
  }
});

// src/runtime-resources/lexicon/address-terms.js
var require_address_terms = __commonJS({
  "src/runtime-resources/lexicon/address-terms.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      protectedAddressTerms: [
        "家姐",
        "小姐",
        "姐姐",
        "姐夫",
        "姐妹",
        "大姐",
        "阿姐",
        "阿太",
        "先生",
        "學生"
      ],
      addressSuffixes: ["先生", "姐", "哥", "生"],
      addressPrefixes: ["阿"],
      addressFollowers: ["快啲"],
      commonSurnameCharacters: Array.from("陳黃張李王梁劉林何吳周鄭郭謝蔡許楊葉盧羅麥馮潘胡蕭朱高馬姚鍾黎")
    };
  }
});

// src/runtime-resources/lexicon/lexical-ingestion-registry.js
var require_lexical_ingestion_registry = __commonJS({
  "src/runtime-resources/lexicon/lexical-ingestion-registry.js"(exports2, module2) {
    "use strict";
    function surfaceSet(values = []) {
      return new Set(values || []);
    }
    function neutralFrequencyCoverageEntry(entry = {}) {
      return String(entry.pos || "") === "lexical_item" && String(entry.syntax || "").split(/\s+/u).includes("lexical_item") && String(entry.note || "").includes("Exact surface retained as neutral lexical coverage");
    }
    function collectBlockedAtomicSurfaces(policyModules = []) {
      const surfaces = /* @__PURE__ */ new Set();
      for (const policy of policyModules) {
        if (!policy || typeof policy !== "object") continue;
        for (const [key, value] of Object.entries(policy)) {
          if (/blocked.*atomic/i.test(key)) {
            if (value instanceof Set || Array.isArray(value)) {
              for (const surface of value) surfaces.add(surface);
            } else if (value && typeof value === "object") {
              for (const surface of Object.keys(value)) surfaces.add(surface);
            }
          }
          if (value && typeof value === "object" && !(value instanceof Set) && !Array.isArray(value)) {
            for (const [surface, disposition] of Object.entries(value)) {
              if (disposition === "blocked_atomic") surfaces.add(surface);
              if (disposition && typeof disposition === "object" && disposition.status === "blocked_atomic") surfaces.add(surface);
            }
          }
        }
      }
      return surfaces;
    }
    var cifuPolicyModules = Object.freeze([
      require_cifu_r1_250_reviewed(),
      require_cifu_r251_500_reviewed(),
      require_cifu_r501_750_reviewed(),
      require_cifu_r751_1000_reviewed(),
      require_cifu_r1001_1250_reviewed(),
      require_cifu_r1251_1500_reviewed(),
      require_cifu_r1501_1750_reviewed(),
      require_cifu_r1751_2000_reviewed()
    ]);
    var lexicalIngestions = Object.freeze([
      Object.freeze({
        id: "cifu-spoken-top-2000",
        source_file: "data/lexical-frequency/cifu-spoken-top-2000.tsv",
        delimiter: "	",
        surface_column: "word",
        rank_column: "rank",
        source_jyutping_column: "cifu_jyutping",
        source_jyutping_unknown_values: Object.freeze(["", "-", "?", "*?"]),
        expected_rows: 2e3,
        require_contiguous_ranks: true,
        require_exact_runtime_coverage: true,
        require_jyutping: true,
        carrier_prefixes: Object.freeze(["", "我"]),
        carrier_suffixes: Object.freeze(["", "呀"]),
        policy_modules: cifuPolicyModules,
        removed_surfaces: surfaceSet(["多少"]),
        contamination_ledger: "data/lexical-frequency/cifu-mandarin-contamination-runtime-audit.tsv"
      })
    ]);
    var blockedAtomicSurfaces = /* @__PURE__ */ new Set();
    var removedIngestionSurfaces = /* @__PURE__ */ new Set();
    for (const ingestion of lexicalIngestions) {
      for (const surface of collectBlockedAtomicSurfaces(ingestion.policy_modules)) blockedAtomicSurfaces.add(surface);
      for (const surface of ingestion.removed_surfaces || []) removedIngestionSurfaces.add(surface);
    }
    function blockedAtomicRuntimeDisposition(surface, tokenLexicon = {}, options = {}) {
      const blocked = options.blocked_surfaces || blockedAtomicSurfaces;
      const removed = options.removed_surfaces || removedIngestionSurfaces;
      if (!blocked.has(surface)) return "not_blocked";
      if (removed.has(surface)) return "removed_from_runtime";
      const entry = tokenLexicon[surface];
      if (!entry) return "promotion_only_no_runtime_entry";
      if (Array.from(String(surface || "")).length <= 1) return "promotion_only_single_character";
      if (neutralFrequencyCoverageEntry(entry)) return "force_compositional_neutral_fallback";
      return "promotion_only_independent_runtime_authority";
    }
    function ingestionForcedCompositionalSurfaces(tokenLexicon = {}) {
      return new Set([...blockedAtomicSurfaces].filter(
        (surface) => blockedAtomicRuntimeDisposition(surface, tokenLexicon) === "force_compositional_neutral_fallback"
      ));
    }
    module2.exports = Object.freeze({
      lexicalIngestions,
      blockedAtomicSurfaces,
      removedIngestionSurfaces,
      neutralFrequencyCoverageEntry,
      collectBlockedAtomicSurfaces,
      blockedAtomicRuntimeDisposition,
      ingestionForcedCompositionalSurfaces
    });
  }
});

// src/runtime-resources/lexicon/compositional-lexical-phrases.js
var require_compositional_lexical_phrases = __commonJS({
  "src/runtime-resources/lexicon/compositional-lexical-phrases.js"(exports2, module2) {
    "use strict";
    var tokenLexicon = Object.fromEntries(require_token_lexicon());
    var { ingestionForcedCompositionalSurfaces } = require_lexical_ingestion_registry();
    var authoredCompositionalPhrases = [
      "唔開心",
      "唔鍾意",
      "一百萬",
      "二百萬",
      "三百萬",
      "四百萬",
      "五百萬",
      "六百萬",
      "七百萬",
      "八百萬",
      "九百萬",
      "一千萬",
      "二千萬",
      "三千萬",
      "四千萬",
      "五千萬",
      "六千萬",
      "七千萬",
      "八千萬",
      "九千萬",
      "一億",
      "我覺得",
      "我知",
      "我唔知",
      "唔知",
      "唔可以",
      "唔要",
      "唔使",
      "唔想",
      "唔得",
      "呢個",
      "嗰個",
      "呢啲",
      "嗰啲",
      "好多人",
      "呢間",
      "邊間",
      "嗰間",
      "呢間餐廳",
      "嗰間餐廳",
      "嗰間新開嘅意大利餐廳",
      "邊間呀",
      "意大利餐廳",
      "特別菜式",
      "其他同事",
      "上個禮拜",
      "下個星期五",
      "上次",
      "下次",
      "有冇興趣",
      "價錢中等",
      "中等價錢",
      "中價位",
      "中價",
      "都算",
      "唔算",
      "好好食",
      "好好飲",
      "好好睇",
      "好好聽",
      "好好味",
      "係咩",
      "好呀",
      "放心啦",
      "到時見啦",
      "得喇",
      "好貴",
      "好靚",
      "好正",
      "好出名",
      "好適合",
      "平啲",
      "聽過",
      "去過",
      "未去過",
      "講緊",
      "試吓",
      "睇睇",
      "Book完",
      "Book完枱",
      "話畀你知"
    ];
    module2.exports = [.../* @__PURE__ */ new Set([
      ...authoredCompositionalPhrases,
      ...ingestionForcedCompositionalSurfaces(tokenLexicon)
    ])];
  }
});

// src/runtime-resources/pronunciation/unknown-cjk-jyutping-fallback.js
var require_unknown_cjk_jyutping_fallback = __commonJS({
  "src/runtime-resources/pronunciation/unknown-cjk-jyutping-fallback.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      ["前", "cin4"],
      ["後", "hau6"],
      ["住", "zyu6"],
      ["所", "so2"],
      ["謂", "wai6"],
      ["媽", "maa1"],
      ["工", "gung1"],
      ["收", "sau1"],
      ["超", "ciu1"],
      // CP019 rendered acceptance-note coverage. These remain unknown_atomic:
      // the map supplies pronunciation only and licenses no lexical or grammatical analysis.
      ["切", "cit3"],
      ["刀", "dou1"],
      ["電", "din6"],
      ["腦", "nou5"],
      ["乜", "mat1"],
      ["把", "baa2"],
      ["門", "mun4"],
      ["來", "lai4"],
      ["減", "gaam2"],
      ["壓", "aat3"],
      ["交", "gaau1"],
      ["租", "zou1"],
      ["愛", "oi3"],
      ["影", "jing2"],
      ["相", "soeng2"],
      ["根", "gan1"],
      ["淨", "zing6"],
      ["堆", "deoi1"],
      ["沓", "daap6"],
      ["紙", "zi2"],
      ["草", "cou2"],
      ["探", "taam3"],
      ["鎖", "so2"],
      ["匙", "si4"],
      ["紅", "hung4"],
      ["改", "goi2"],
      ["簿", "bou6"],
      ["運", "wan6"],
      ["動", "dung6"],
      ["儲", "cyu5"],
      ["旅", "leoi5"],
      ["果", "gwo2"],
      ["店", "dim3"],
      ["讀", "duk6"],
      ["罐", "gun3"],
      ["頭", "tau4"],
      ["途", "tou4"],
      // CP020 rendered acceptance-note coverage. Pronunciation only; all remain
      // unknown_atomic unless independently present in the lexical registry.
      ["俾", "bei2"],
      ["爛", "laan6"],
      ["偷", "tau1"],
      ["蛋", "daan6"],
      ["糕", "gou1"],
      ["貼", "tip3"],
      ["車", "ce1"],
      ["撞", "zong6"],
      ["隻", "zek3"],
      ["腳", "goek3"],
      ["咬", "ngaau5"],
      ["畫", "waa2"],
      ["銀", "ngan4"],
      ["包", "baau1"],
      ["讚", "zaan3"],
      ["米", "mai5"],
      ["粒", "nap1"],
      ["幅", "fuk1"],
      ["堂", "tong4"],
      ["籃", "laam4"],
      ["球", "kau4"],
      ["盯", "ding1"],
      ["實", "sat6"],
      ["囖", "lo1"],
      ["呃", "ngaak1"],
      ["跌", "dit3"],
      ["路", "lou6"],
      ["乾", "gon1"],
      ["老", "lou5"],
      ["闆", "baan2"],
      ["醫", "ji1"],
      ["檢", "gim2"],
      ["查", "caa4"],
      ["污", "wu1"],
      ["糟", "zou1"],
      ["公", "gung1"],
      ["司", "si1"],
      ["哋", "dei6"],
      ["罰", "fat6"],
      ["心", "sam1"]
    ];
  }
});

// src/runtime-resources/grammar/predicate-profiles.js
var require_predicate_profiles = __commonJS({
  "src/runtime-resources/grammar/predicate-profiles.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      environmentalEventPredicates: {
        "落雨": { head: "落", phenomenon: "雨", environmental_subtype: "precipitation_event" },
        "打風": { head: "打", phenomenon: "風", environmental_subtype: "wind_event" }
      },
      predicateOmissionProfiles: [
        {
          id: "modal_permission_ability",
          parser_family: "modal",
          question_match_family: "permission_ability",
          positive_heads: ["可以"],
          question_forms: ["可唔可以"],
          complement_type: "vp_or_proposition",
          missing_slot_type: "modal_or_volitional_complement",
          isolated_status: "context_required_unresolved",
          conventional_bare_status: "not_conventional",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "modal_volition_selection",
          parser_family: "modal",
          question_match_family: "necessity_or_selection",
          positive_heads: ["要"],
          question_forms: ["要唔要"],
          complement_type: "vp_np_or_proposition",
          missing_slot_type: "modal_or_volitional_complement",
          isolated_status: "context_required_unresolved",
          conventional_bare_status: "not_conventional",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "necessity_exemption",
          parser_family: "modal",
          question_match_family: "necessity_or_selection",
          positive_heads: ["使"],
          question_forms: ["使唔使"],
          complement_type: "vp_or_activity",
          missing_slot_type: "modal_or_volitional_complement",
          isolated_status: "context_required_unresolved",
          conventional_bare_status: "response_preferred",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "acceptability_possibility",
          parser_family: "modal",
          question_match_family: "acceptability",
          positive_heads: ["得"],
          question_forms: ["得唔得"],
          complement_type: "acceptability_domain_or_proposition",
          missing_slot_type: "acceptability_domain_or_proposition",
          isolated_status: "ambiguous_response_or_predicate",
          conventional_bare_status: "context_sensitive",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "desiderative_content",
          parser_family: "modal",
          question_match_family: "desiderative_content",
          positive_heads: ["想"],
          question_forms: ["想唔想"],
          complement_type: "vp_np_or_proposition",
          missing_slot_type: "desired_or_cognitive_content",
          isolated_status: "context_required_unresolved",
          conventional_bare_status: "not_conventional",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "factive_cognition",
          parser_family: "cognition",
          question_match_family: "epistemic_knowledge",
          positive_heads: ["知"],
          question_forms: ["知唔知"],
          complement_type: "proposition_or_interrogative_clause",
          missing_slot_type: "content_clause",
          isolated_status: "conventional_bare_statement",
          conventional_bare_status: "conventional_bare_statement",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "opinion_evaluation",
          parser_family: "stance",
          question_match_family: "opinion_content",
          positive_heads: ["覺得"],
          question_forms: [],
          complement_type: "predicate_or_proposition",
          missing_slot_type: "content_clause",
          isolated_status: "context_required_unresolved",
          conventional_bare_status: "not_conventional",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "mistaken_belief",
          parser_family: "stance",
          question_match_family: "belief_content",
          positive_heads: ["以為"],
          question_forms: [],
          complement_type: "proposition",
          missing_slot_type: "content_clause",
          isolated_status: "context_required_unresolved",
          conventional_bare_status: "not_conventional",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "speech_report_event",
          parser_family: "speech",
          question_match_family: "speech_content",
          positive_heads: ["話", "講"],
          question_forms: [],
          complement_type: "np_proposition_or_speech_event",
          missing_slot_type: "reported_content_or_speech_event",
          isolated_status: "speech_event_or_omitted_content_ambiguous",
          conventional_bare_status: "speech_event_context_sensitive",
          polarity_alternatives: ["positive", "negative"]
        },
        {
          id: "future_event_modal",
          parser_family: "modal",
          question_match_family: "epistemic_event_polar",
          positive_heads: ["會"],
          question_forms: ["會唔會"],
          complement_type: "vp_or_proposition",
          missing_slot_type: "modal_or_volitional_complement",
          isolated_status: "context_required_unresolved",
          conventional_bare_status: "not_conventional",
          polarity_alternatives: ["positive", "negative"]
        }
      ]
    };
  }
});

// src/runtime-resources/presentation/learner-display.js
var require_learner_display2 = __commonJS({
  "src/runtime-resources/presentation/learner-display.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      learnerRoleLabels: ["who", "doing", "what", "where", "when", "why", "how", "like", "func", "particle", "measure_word", "neutral"],
      slotNameDisallowedPrefixes: [/^phase\d+_/, /^controlled_/],
      learnerDisplaySlotNames: [
        "action_verb",
        "beneficiary",
        "classifier",
        "degree",
        "demonstrative",
        "head_noun",
        "location",
        "main_verb",
        "modal",
        "modifier",
        "negator",
        "nominal_linker",
        "np",
        "object",
        "particle",
        "predicate",
        "quantity",
        "subject",
        "time",
        "topic",
        "vp",
        "wh_determiner"
      ],
      learnerDisplaySlotLabels: {
        classifier: "measure_word",
        quantity: "how"
      }
    };
  }
});

// src/runtime-resources/diagnostics/trace-metadata.js
var require_trace_metadata = __commonJS({
  "src/runtime-resources/diagnostics/trace-metadata.js"(exports2, module2) {
    "use strict";
    var TRACE_TAXONOMY_SCHEMA = "canto-span-trace-taxonomy-v1";
    var templateTracePassthroughKeys = [
      "retired_label_alias",
      "predicate_subtype",
      "clause_modifier_profile",
      "polarity",
      "existential_subtype",
      "abstract_object_domain",
      "semantic_domain",
      "scalar_domain",
      "np_subtype",
      "role_resolution_note",
      "not_learner_role",
      "not_question_role",
      "wrapper_policy",
      "wrapper_reason",
      "context_requirement_status",
      "missing_argument_slots",
      "antecedent_status",
      "discourse_license_not_observed",
      "fragment_subtype",
      "approximation_scope",
      "approximation_marker_surface",
      "object_semantics",
      "head_recovery_status",
      "not_claims",
      "passive_subtype",
      "reading_candidates",
      "participant_surface",
      "postmarker_participant_surface",
      "retained_patient_candidate_surface",
      "subject_status",
      "subjectless_type",
      "hidden_subject_inserted",
      "environmental_subtype",
      "location_relation",
      "have_relation",
      "locative_frame_subtype",
      "introduced_theme_surface",
      "introduced_participant_surface",
      "presentational_coda_surface",
      "perfective_profile",
      "overt_object_required",
      "aspect_marker_surface",
      "aspect_category",
      "independent_past_tense_licensing",
      "completion_or_result_licensing",
      "experiential_licensing",
      "hidden_object_insertion",
      "selectional_compatibility_bypass",
      "subject_insertion_capability",
      "template_subtype",
      "structural_scope",
      "source_specification",
      "lexical_compound_profile"
    ];
    var parserDecisionTraceKindRegistry2 = [
      ["generative_template", "Pattern-based slot-template match over generated affordances. A generative_template trace may carry template_family=construction_template while the family is still bounded or not yet fully POS-general."],
      ["construction_template", "Temporary template-family classification for bounded or vocabulary/surface-constrained templates that should eventually be promoted to fully generative POS-targeting templates."],
      ["generative_or_heuristic_slot_rule", "Interim slot heuristic that should be audited for promotion to a constrained template when it recurs."],
      ["governed_discourse_wrapper", "Accepted structural wrapper for clause-linking or discourse/coordination sequences. It organizes already-parsed clause-like children and is intentionally not a phrase-internal generative template."],
      ["predicate_heuristic", "Interim predicate wrapper; acceptable only while a reusable predicate template is not yet available."],
      ["atomic_lexicon", "Atomic vocabulary lookup, not itself a construction template."],
      ["unknown_atomic", "Unknown or fallback token lane; should remain visible in diagnostics."],
      ["punctuation_or_plain_text", "Punctuation/plain text boundary trace."],
      ["protected_formula_table", "Closed-list protected formula table; intentionally not generalized as productive grammar."],
      ["surface_specific_phrase_rule", "Surface-specific parser island that should be replaced by a reusable template when recurrence justifies it."],
      ["legacy_surface_rule", "Older fallback rule retained for compatibility until promoted or retired."],
      ["special_ambiguity_rule", "Explicit ambiguity guard; may remain non-generative when safer than broad promotion."],
      ["phase4_controlled_grammar_promotion", "Controlled promotion lane where feature predicates are parser-active only in approved scopes."],
      ["construction_function", "Hand-coded construction function; audit target for future template extraction when structurally recurrent."],
      ["construction_internal_parser_inactive_clone", "Internal child clone used to expose parent construction structure without changing parser-active feature behavior."],
      ["source_linked_runtime_matcher", "Deterministic source-linked runtime matcher with separately reviewed bounded constraints. This is a registered non-template trace kind; taxonomy registration does not itself assign migration debt or linguistic status."]
    ];
    var templateFamilyRegistry2 = [
      ["construction_template", "Temporary classification for bounded, constrained, or vocabulary/surface-anchored templates that are not yet fully POS-general."],
      ["generative_template", "Fully pattern-based template intended to apply to new vocabulary that exposes the same generated slots."]
    ];
    var templateTraceKinds = /* @__PURE__ */ new Set(["generative_template", "construction_template"]);
    var registeredTraceKinds = new Set(parserDecisionTraceKindRegistry2.map(([kind]) => kind));
    var registeredTemplateFamilies = new Set(templateFamilyRegistry2.map(([family]) => family));
    var structuralScopeRegistry = [
      ["clause", "Clause-sized construction or wrapper capable of binding a clause-level subject/topic."],
      ["vp", "Verb-phrase-sized construction that must not itself bind a clause-level subject/topic."],
      ["np", "Noun-phrase-sized construction."],
      ["phrase", "Other phrase-sized construction."],
      ["discourse", "Discourse or interclausal structural wrapper."],
      ["diagnostic", "Diagnostic/ambiguity/error-analysis construction rather than a direct constituent scope claim."],
      ["unspecified", "Structural scope not yet authored or derivable from controlled runtime metadata."]
    ];
    var registeredStructuralScopes = new Set(structuralScopeRegistry.map(([scope]) => scope));
    var clauseLevelStructuralSlots = /* @__PURE__ */ new Set(["subject", "overt_subject", "topic"]);
    var reviewedMixedClauseVpConstructions = /* @__PURE__ */ new Set([
      "ModalVP",
      "DesiderativeVP",
      "MannerAdverbialVP",
      "PreferenceVP"
    ]);
    function templateSlotName(item) {
      return String(item || "").replace(/[!?+*]+$/g, "");
    }
    function traceDeclaresClauseLevelSlot(trace = {}) {
      const assigned = Array.isArray(trace.assigned_slots) ? trace.assigned_slots : [];
      const authored = Array.isArray(trace.template) ? trace.template.map(templateSlotName) : [];
      return [...assigned, ...authored].some((slot) => clauseLevelStructuralSlots.has(slot));
    }
    function deriveStructuralScope(trace = {}, options = {}) {
      const explicit = String(trace.structural_scope || "");
      const constructionType = String(options.constructionType || trace.construction_type || "");
      if (explicit) return { structural_scope: explicit, structural_scope_source: "explicit" };
      if (traceDeclaresClauseLevelSlot(trace)) {
        return { structural_scope: "clause", structural_scope_source: "clause_level_slot" };
      }
      if (reviewedMixedClauseVpConstructions.has(constructionType)) {
        return { structural_scope: "vp", structural_scope_source: "reviewed_mixed_clause_vp_definition" };
      }
      if (trace.kind === "governed_discourse_wrapper") {
        return { structural_scope: "discourse", structural_scope_source: "trace_kind" };
      }
      if (trace.kind === "special_ambiguity_rule") {
        return { structural_scope: "diagnostic", structural_scope_source: "trace_kind" };
      }
      return { structural_scope: "unspecified", structural_scope_source: "not_authored" };
    }
    var legacyTemplateSubtypeFamilyMap = /* @__PURE__ */ new Map([
      ["first_syllable_preference_a_not_a", {
        template_family: "construction_template",
        template_subtype: "first_syllable_preference_a_not_a"
      }],
      ["copular_a_not_a_bounded_complement", {
        template_family: "construction_template",
        template_subtype: "copular_a_not_a_bounded_complement"
      }]
    ]);
    function canonicalize(value) {
      if (Array.isArray(value)) return value.map(canonicalize);
      if (!value || typeof value !== "object") return value;
      return Object.fromEntries(Object.keys(value).sort().map((key) => [key, canonicalize(value[key])]));
    }
    function reviewedDefinitionKey(constructionType, template, constraints) {
      return JSON.stringify(canonicalize({
        construction_type: String(constructionType || ""),
        template: Array.isArray(template) ? template : [],
        constraints: constraints && typeof constraints === "object" ? constraints : {}
      }));
    }
    var MATCHER_VARIANT_SCHEMA = "canto-span-matcher-variant-v1";
    var matcherVariantFamilyRegistry = /* @__PURE__ */ new Set();
    var matcherVariantDefinitionRegistry = /* @__PURE__ */ new Map();
    var matcherVariantIdToDefinitionKey = /* @__PURE__ */ new Map();
    function matcherVariantFamilyKey(spec = {}) {
      return JSON.stringify(canonicalize({
        trace_kind: String(spec.trace_kind || ""),
        construction_type: String(spec.construction_type || ""),
        template_family: String(spec.template_family || ""),
        template: Array.isArray(spec.template) ? spec.template : [],
        rule: String(spec.rule || "")
      }));
    }
    function matcherVariantDefinitionKey(spec = {}) {
      return JSON.stringify(canonicalize({
        trace_kind: String(spec.trace_kind || ""),
        construction_type: String(spec.construction_type || ""),
        template_family: String(spec.template_family || ""),
        template: Array.isArray(spec.template) ? spec.template : [],
        constraints: spec.constraints && typeof spec.constraints === "object" ? spec.constraints : {},
        rule: String(spec.rule || "")
      }));
    }
    function registerReviewedMatcherVariant(id, spec) {
      const variantId = String(id || "");
      if (!variantId) throw new Error("Matcher variant ID must be non-empty.");
      const familyKey = matcherVariantFamilyKey(spec);
      const definitionKey = matcherVariantDefinitionKey(spec);
      if (matcherVariantDefinitionRegistry.has(definitionKey) && matcherVariantDefinitionRegistry.get(definitionKey) !== variantId) {
        throw new Error(`Matcher definition is already registered as ${matcherVariantDefinitionRegistry.get(definitionKey)}.`);
      }
      if (matcherVariantIdToDefinitionKey.has(variantId) && matcherVariantIdToDefinitionKey.get(variantId) !== definitionKey) {
        throw new Error(`Matcher variant ${variantId} maps to more than one controlled definition.`);
      }
      matcherVariantFamilyRegistry.add(familyKey);
      matcherVariantDefinitionRegistry.set(definitionKey, variantId);
      matcherVariantIdToDefinitionKey.set(variantId, definitionKey);
    }
    function matcherVariantSpec(constructionType, trace, templateFamily) {
      return {
        trace_kind: String(trace.kind || ""),
        construction_type: String(constructionType || trace.construction_type || ""),
        template_family: String(templateFamily || trace.template_family || ""),
        template: Array.isArray(trace.template) ? trace.template : [],
        constraints: trace.constraints && typeof trace.constraints === "object" ? trace.constraints : {},
        rule: String(trace.rule || "")
      };
    }
    function resolveMatcherVariant(constructionType, trace, templateFamily) {
      const spec = matcherVariantSpec(constructionType, trace, templateFamily);
      if (!matcherVariantFamilyRegistry.has(matcherVariantFamilyKey(spec))) {
        return {
          matcher_variant_applicability: "not_required",
          matcher_variant_id: "",
          matcher_variant_source: "not_required"
        };
      }
      const id = matcherVariantDefinitionRegistry.get(matcherVariantDefinitionKey(spec)) || "";
      return {
        matcher_variant_applicability: "required",
        matcher_variant_id: id,
        matcher_variant_source: id ? "reviewed_definition_registry" : "unregistered_definition"
      };
    }
    var opinionTemplate = ["subject?", "focus_adverb?", "stance_predicate!", "focus_adverb?", "reported_content!", "particle?"];
    function opinionConstraints(surface) {
      return {
        visible_reviewed_content: true,
        stance_predicate_surfaces: [surface],
        focus_modifier_slot: "focus_adverb"
      };
    }
    registerReviewedMatcherVariant("OpinionStanceFrame.stance_gokdak", {
      trace_kind: "generative_template",
      construction_type: "OpinionStanceFrame",
      template_family: "generative_template",
      template: opinionTemplate,
      constraints: opinionConstraints("覺得"),
      rule: ""
    });
    registerReviewedMatcherVariant("OpinionStanceFrame.stance_jiwai", {
      trace_kind: "generative_template",
      construction_type: "OpinionStanceFrame",
      template_family: "generative_template",
      template: opinionTemplate,
      constraints: opinionConstraints("以為"),
      rule: ""
    });
    registerReviewedMatcherVariant("OpinionStanceFrame.stance_soengseon", {
      trace_kind: "generative_template",
      construction_type: "OpinionStanceFrame",
      template_family: "generative_template",
      template: opinionTemplate,
      constraints: opinionConstraints("相信"),
      rule: ""
    });
    var subjectPredicateTemplate = ["subject!", "predicate!", "particle?"];
    registerReviewedMatcherVariant("SubjectPredicateClause.predicate_allowlist_nonnegative", {
      trace_kind: "generative_template",
      construction_type: "SubjectPredicateClause",
      template_family: "generative_template",
      template: subjectPredicateTemplate,
      constraints: {
        predicate_must_have_any_slots: [
          "transitive_vp",
          "productive_vo",
          "progressive_vp",
          "perfective_vp",
          "completion_vp",
          "locative_posture_vp",
          "directional_motion_vp",
          "motion_goal_vp",
          "motion_purpose_chain",
          "serial_verb_purpose_chain"
        ],
        disallow_child_slots: ["negated_directional_motion_vp", "negated_vp"]
      },
      rule: ""
    });
    registerReviewedMatcherVariant("SubjectPredicateClause.predicate_unconstrained", {
      trace_kind: "generative_template",
      construction_type: "SubjectPredicateClause",
      template_family: "generative_template",
      template: subjectPredicateTemplate,
      constraints: {},
      rule: ""
    });
    registerReviewedMatcherVariant("SubjectPredicateClause.predicate_allowlist_negative", {
      trace_kind: "generative_template",
      construction_type: "SubjectPredicateClause",
      template_family: "generative_template",
      template: subjectPredicateTemplate,
      constraints: { predicate_must_have_any_slots: ["negated_directional_motion_vp", "negated_vp"] },
      rule: ""
    });
    var demonstrativeTemplate = ["demonstrative!", "classifier!", "head_noun!"];
    var demonstrativeGuard = {
      slot_must_not_have_slots: {
        demonstrative: ["quantity", "wh_determiner", "di_determiner"],
        classifier: ["quantity", "wh_determiner", "di_determiner"],
        head_noun: ["quantity", "classifier", "wh_determiner", "di_determiner"]
      }
    };
    registerReviewedMatcherVariant("DemonstrativeClassifierNP.slot_exclusion_guarded", {
      trace_kind: "generative_template",
      construction_type: "DemonstrativeClassifierNP",
      template_family: "generative_template",
      template: demonstrativeTemplate,
      constraints: demonstrativeGuard,
      rule: ""
    });
    registerReviewedMatcherVariant("DemonstrativeClassifierNP.slot_exclusion_unconstrained", {
      trace_kind: "generative_template",
      construction_type: "DemonstrativeClassifierNP",
      template_family: "generative_template",
      template: demonstrativeTemplate,
      constraints: {},
      rule: ""
    });
    var headTemplate = ["head_noun!"];
    var headGuard = {
      disallow_child_slots: [
        "demonstrative",
        "classifier",
        "quantity",
        "modifier",
        "nominal_linker",
        "vp",
        "action_vp",
        "predicate",
        "perfective_vp",
        "progressive_vp",
        "completion_vp",
        "result_complement_vp",
        "potential_result_vp",
        "negative_potential_complement",
        "transitive_vp",
        "productive_vo"
      ]
    };
    registerReviewedMatcherVariant("HeadNP.child_slot_exclusion_guarded", {
      trace_kind: "generative_template",
      construction_type: "HeadNP",
      template_family: "generative_template",
      template: headTemplate,
      constraints: headGuard,
      rule: ""
    });
    registerReviewedMatcherVariant("HeadNP.child_slot_exclusion_unconstrained", {
      trace_kind: "generative_template",
      construction_type: "HeadNP",
      template_family: "generative_template",
      template: headTemplate,
      constraints: {},
      rule: ""
    });
    var transitiveTemplate = ["action_verb!", "object!"];
    var transitiveGuard = {
      slot_must_not_be_bare_quantity_token: ["object"],
      slot_must_not_have_slots: { object: ["approximate_quantity", "vp", "action_vp", "predicate", "productive_vo"] }
    };
    registerReviewedMatcherVariant("TransitiveVP.object_shape_guarded", {
      trace_kind: "generative_template",
      construction_type: "TransitiveVP",
      template_family: "generative_template",
      template: transitiveTemplate,
      constraints: transitiveGuard,
      rule: ""
    });
    registerReviewedMatcherVariant("TransitiveVP.object_shape_unconstrained", {
      trace_kind: "generative_template",
      construction_type: "TransitiveVP",
      template_family: "generative_template",
      template: transitiveTemplate,
      constraints: {},
      rule: ""
    });
    var productiveVoVariantTemplate = ["action_verb!", "object!"];
    var productiveVoLegacySurfaces = require_productive_vo().map(([surface]) => surface);
    var verbObjectCompoundSeedSurfaces = require_verb_object_compounds().map(([surface]) => surface);
    registerReviewedMatcherVariant("ProductiveVO.legacy_whitelist_object_relation", {
      trace_kind: "generative_template",
      construction_type: "ProductiveVO",
      template_family: "generative_template",
      template: productiveVoVariantTemplate,
      constraints: { surface_sequence_in: productiveVoLegacySurfaces },
      rule: ""
    });
    registerReviewedMatcherVariant("ProductiveVO.source_linked_verb_object_compound_seed", {
      trace_kind: "source_linked_runtime_matcher",
      construction_type: "ProductiveVO",
      template_family: "",
      template: productiveVoVariantTemplate,
      constraints: { surface_sequence_in: verbObjectCompoundSeedSurfaces },
      rule: ""
    });
    var reviewedMissingTemplateFamilyDefaults = /* @__PURE__ */ new Map();
    function registerReviewedMissingFamily(constructionType, template, constraints, templateFamily) {
      reviewedMissingTemplateFamilyDefaults.set(
        reviewedDefinitionKey(constructionType, template, constraints),
        templateFamily
      );
    }
    registerReviewedMissingFamily("FormulaDiscourseUnit", ["agreement_marker!", "particle!"], {}, "construction_template");
    registerReviewedMissingFamily("ClassifierObjectNP", ["classifier!", "head_noun!"], {}, "generative_template");
    registerReviewedMissingFamily("CopularIdentificationFrame", ["topic!", "copula!", "np!", "particle?"], {}, "generative_template");
    registerReviewedMissingFamily("PossessiveClassifierNP", ["possessor!", "classifier_np!"], {}, "generative_template");
    registerReviewedMissingFamily("PreferenceVP", ["subject!", "preference_predicate!", "vp!", "particle?"], {
      slot_surface_in: { preference_predicate: ["鍾意"] }
    }, "construction_template");
    registerReviewedMissingFamily("ChangeIntoPredicate", ["change_verb!", "result_complement!"], {}, "generative_template");
    registerReviewedMissingFamily("CoordinatedNP", ["left_np!", "coordinator!", "right_np!"], {}, "generative_template");
    registerReviewedMissingFamily("CopularRelationFrame", ["topic!", "copula!", "copular_complement!", "explanatory_linker!", "explanatory_particle!"], {
      required_copula: "係",
      required_lai_marker: "嚟",
      required_explanatory_particle: "㗎/架",
      complement_kind: "wh_definition_complement",
      formula_guard_reanalysis: "not_needed"
    }, "construction_template");
    registerReviewedMissingFamily("DegreeMannerModifiedVP", ["degree_manner_adverbial!", "directional_motion_vp!", "particle?"], {
      modifier_surface: "快啲",
      preserve_inner_directional_vp: true
    }, "construction_template");
    registerReviewedMissingFamily("IntentionFrame", ["subject!", "intention_predicate!", "vp!", "particle?"], {
      slot_surface_in: { intention_predicate: ["諗住"] }
    }, "construction_template");
    registerReviewedMissingFamily("NamingClause", ["subject!", "naming_verb!", "name!", "particle?"], {}, "generative_template");
    registerReviewedMissingFamily("PathPhrase", ["path_marker!", "location!"], {}, "generative_template");
    registerReviewedMissingFamily("PoliteImperativeClause", ["politeness_formula!", "addressee!", "scalar_adjustment!", "particle?"], {}, "construction_template");
    registerReviewedMissingFamily("PoliteImperativeClause", ["politeness_marker!", "subject!", "path_phrase!", "predicate!", "particle?"], {}, "construction_template");
    registerReviewedMissingFamily("PriorityMarkerClause", ["subject!", "action_vp!", "priority_marker!", "particle!"], {
      surface_sequence: "你打電話先啦"
    }, "construction_template");
    registerReviewedMissingFamily("SubjectModalPredicateClause", ["subject!", "modal_vp!"], {}, "generative_template");
    function taxonomyIssue(code, message, extra = {}) {
      return { code, severity: "error", message, ...extra };
    }
    function normalizeTraceTaxonomy(trace = {}, options = {}) {
      const normalized = { ...trace || {} };
      const constructionType = String(options.constructionType || normalized.construction_type || "");
      const kind = String(normalized.kind || "");
      const issues = [];
      const isTemplateTrace = templateTraceKinds.has(kind);
      let templateFamily = String(normalized.template_family || "");
      let templateSubtype = String(normalized.template_subtype || "");
      let templateFamilySource = templateFamily ? "explicit_controlled" : "not_set";
      if (!registeredTraceKinds.has(kind)) {
        issues.push(taxonomyIssue(
          "unregistered_trace_kind",
          `Trace kind ${kind || "(missing)"} is not registered in parserDecisionTraceKindRegistry.`,
          { trace_kind: kind }
        ));
      }
      if (templateFamily && legacyTemplateSubtypeFamilyMap.has(templateFamily)) {
        const legacyFamily = templateFamily;
        const mapping = legacyTemplateSubtypeFamilyMap.get(templateFamily);
        templateFamily = mapping.template_family;
        templateSubtype = templateSubtype || mapping.template_subtype;
        normalized.legacy_template_family = legacyFamily;
        templateFamilySource = "legacy_subtype_normalized";
      }
      if (isTemplateTrace) {
        normalized.template_family_applicability = "required";
        if (!templateFamily && kind === "construction_template") {
          templateFamily = "construction_template";
          templateFamilySource = "trace_kind_default";
        }
        if (!templateFamily && kind === "generative_template") {
          const reviewedFamily = reviewedMissingTemplateFamilyDefaults.get(reviewedDefinitionKey(
            constructionType,
            normalized.template,
            normalized.constraints
          ));
          if (reviewedFamily) {
            templateFamily = reviewedFamily;
            templateFamilySource = "reviewed_definition_default";
          }
        }
        if (!templateFamily) {
          issues.push(taxonomyIssue(
            "template_family_missing",
            `Template trace ${kind || "(missing)"} requires a controlled template_family.`,
            { construction_type: constructionType }
          ));
        } else if (!registeredTemplateFamilies.has(templateFamily)) {
          issues.push(taxonomyIssue(
            "unregistered_template_family",
            `Template family ${templateFamily} is not registered in templateFamilyRegistry.`,
            { template_family: templateFamily, construction_type: constructionType }
          ));
        }
      } else {
        normalized.template_family_applicability = "not_applicable";
        if (templateFamily && !registeredTemplateFamilies.has(templateFamily)) {
          issues.push(taxonomyIssue(
            "unregistered_template_family",
            `Non-template trace carries unregistered template family ${templateFamily}.`,
            { template_family: templateFamily, construction_type: constructionType }
          ));
        }
        if (!templateFamily) templateFamilySource = "not_applicable";
      }
      const structuralScope = deriveStructuralScope(normalized, { constructionType });
      if (!registeredStructuralScopes.has(structuralScope.structural_scope)) {
        issues.push(taxonomyIssue(
          "unregistered_structural_scope",
          `Structural scope ${structuralScope.structural_scope || "(missing)"} is not registered.`,
          { structural_scope: structuralScope.structural_scope, construction_type: constructionType }
        ));
      }
      if (structuralScope.structural_scope === "vp" && traceDeclaresClauseLevelSlot(normalized)) {
        issues.push(taxonomyIssue(
          "vp_scope_binds_clause_level_slot",
          "A trace structurally categorized as VP declares or realizes a clause-level subject/topic slot.",
          { construction_type: constructionType }
        ));
      }
      normalized.trace_taxonomy_schema = TRACE_TAXONOMY_SCHEMA;
      normalized.template_family = templateFamily;
      normalized.template_subtype = templateSubtype;
      normalized.template_family_source = templateFamilySource;
      normalized.structural_scope = structuralScope.structural_scope;
      normalized.structural_scope_source = structuralScope.structural_scope_source;
      const matcherVariant = resolveMatcherVariant(constructionType, normalized, templateFamily);
      normalized.matcher_variant_schema = MATCHER_VARIANT_SCHEMA;
      normalized.matcher_variant_applicability = matcherVariant.matcher_variant_applicability;
      normalized.matcher_variant_id = matcherVariant.matcher_variant_id;
      normalized.matcher_variant_source = matcherVariant.matcher_variant_source;
      if (matcherVariant.matcher_variant_applicability === "required" && !matcherVariant.matcher_variant_id) {
        issues.push(taxonomyIssue(
          "matcher_variant_unregistered",
          "A reviewed matcher-variant family contains a controlled definition with no authored variant ID.",
          { construction_type: constructionType }
        ));
      }
      normalized.taxonomy_status = issues.length ? "invalid" : "valid";
      normalized.taxonomy_issues = issues;
      return normalized;
    }
    function annotateTraceTaxonomy(nodes = []) {
      const summary = {
        schema: TRACE_TAXONOMY_SCHEMA,
        construction_trace_count: 0,
        valid_count: 0,
        invalid_count: 0,
        issue_counts: {},
        issue_samples: []
      };
      function visit(node) {
        if (!node || typeof node !== "object") return;
        if (node.kind === "construction") {
          node.trace = normalizeTraceTaxonomy(node.trace || {}, { constructionType: node.compatibility_alias || node.type || "" });
          summary.construction_trace_count += 1;
          if (node.trace.taxonomy_status === "valid") {
            summary.valid_count += 1;
          } else {
            summary.invalid_count += 1;
            for (const issue of node.trace.taxonomy_issues || []) {
              summary.issue_counts[issue.code] = (summary.issue_counts[issue.code] || 0) + 1;
              if (summary.issue_samples.length < 20) {
                summary.issue_samples.push({
                  construction: node.compatibility_alias || node.type || "",
                  internal_construction: node.type || "",
                  ...issue
                });
              }
            }
          }
        }
        for (const child of node.children || []) visit(child);
      }
      for (const node of nodes || []) visit(node);
      return summary;
    }
    var parserDecisionLabelPolicy2 = {
      purpose: "classify_active_parser_implementations_without_assuming_one_universal_runtime_form",
      template_family_boundary: "use declarative templates where they cleanly express the reviewed definition; preserve governed specialized implementations where context, composition, ambiguity, or nonlocal constraints require them",
      construction_template: "reviewed_bounded_or_constrained_template_valid_as_first_class_runtime_architecture",
      generative_template: "reusable_slot_or_pos_pattern_template_for_compatible_reviewed_definitions",
      specialized_implementation: "governed_non_template_or_hand_written_logic_may_remain_first_class_when_it_encodes_required_structural_constraints",
      migration_debt: "only_trace_kinds_explicitly_classified_as_legacy_surface_specific_or_heuristic_are_actionable_migration_debt"
    };
    var labelTransitionKindPolicy = {
      generative_template: {
        bucket: "reusable_template",
        status: "accepted_reusable_template",
        priority: 99,
        debt: false,
        action: "Keep: reusable slot-template implementation."
      },
      construction_template: {
        bucket: "bounded_template",
        status: "accepted_bounded_template",
        priority: 95,
        debt: false,
        action: "Keep: reviewed bounded or constrained template; boundedness alone is not migration debt."
      },
      generative_or_heuristic_slot_rule: {
        bucket: "slot_heuristic",
        status: "migration_candidate",
        priority: 50,
        debt: true,
        action: "Review for migration when the structural heuristic can be expressed as a named reusable typed template."
      },
      governed_discourse_wrapper: {
        bucket: "governed_structural_wrapper",
        status: "accepted_specialized_implementation",
        priority: 88,
        debt: false,
        action: "Keep: governed discourse/coordination wrapper over independently parsed clause-like children."
      },
      predicate_heuristic: {
        bucket: "predicate_heuristic",
        status: "migration_candidate",
        priority: 45,
        debt: true,
        action: "Review stable predicate-shape heuristics for a reusable typed template or retire the fallback once covered."
      },
      surface_specific_phrase_rule: {
        bucket: "surface_specific_rule",
        status: "migration_candidate",
        priority: 20,
        debt: true,
        action: "Replace recurrent surface-specific phrase code with a reusable typed matcher when the reviewed boundary supports it."
      },
      legacy_surface_rule: {
        bucket: "legacy_surface_rule",
        status: "migration_candidate",
        priority: 10,
        debt: true,
        action: "Replace legacy fallback matching with a governed typed matcher or narrower reviewed rule."
      },
      special_ambiguity_rule: {
        bucket: "special_ambiguity_rule",
        status: "accepted_guardrail",
        priority: 70,
        debt: false,
        action: "Keep: explicit ambiguity guard unless a demonstrably safer representation replaces it."
      },
      protected_formula_table: {
        bucket: "protected_formula_table",
        status: "accepted_closed_table",
        priority: 80,
        debt: false,
        action: "Keep: intentionally closed protected formula table."
      },
      phase4_controlled_grammar_promotion: {
        bucket: "controlled_transition_trace",
        status: "transition_review",
        priority: 35,
        debt: false,
        action: "Review as transition provenance; do not classify it as migration debt merely because it is not a generative template."
      },
      construction_function: {
        bucket: "construction_function",
        status: "accepted_specialized_implementation",
        priority: 75,
        debt: false,
        action: "Keep when the hand-written implementation captures reviewed context, composition, ambiguity, or nonlocal constraints; migrate only on separate evidence that a typed template is a cleaner equivalent."
      },
      source_linked_runtime_matcher: {
        bucket: "source_linked_runtime_matcher",
        status: "accepted_specialized_implementation",
        priority: 90,
        debt: false,
        action: "Keep: deterministic source-linked bounded matcher with separately reviewed constraints."
      },
      construction_internal_parser_inactive_clone: {
        bucket: "internal_support_trace",
        status: "accepted_internal_support",
        priority: 98,
        debt: false,
        action: "Keep: internal parser-inactive structural support trace; not an independent migration target."
      }
    };
    module2.exports = {
      TRACE_TAXONOMY_SCHEMA,
      templateTracePassthroughKeys,
      parserDecisionTraceKindRegistry: parserDecisionTraceKindRegistry2,
      templateFamilyRegistry: templateFamilyRegistry2,
      templateTraceKinds,
      structuralScopeRegistry,
      clauseLevelStructuralSlots,
      reviewedMixedClauseVpConstructions,
      traceDeclaresClauseLevelSlot,
      deriveStructuralScope,
      legacyTemplateSubtypeFamilyMap,
      reviewedMissingTemplateFamilyDefaults,
      MATCHER_VARIANT_SCHEMA,
      matcherVariantFamilyRegistry,
      matcherVariantDefinitionRegistry,
      matcherVariantIdToDefinitionKey,
      matcherVariantFamilyKey,
      matcherVariantDefinitionKey,
      registerReviewedMatcherVariant,
      resolveMatcherVariant,
      normalizeTraceTaxonomy,
      annotateTraceTaxonomy,
      parserDecisionLabelPolicy: parserDecisionLabelPolicy2,
      labelTransitionKindPolicy
    };
  }
});

// src/parser/slots/clean-slots.js
var require_clean_slots = __commonJS({
  "src/parser/slots/clean-slots.js"(exports2, module2) {
    "use strict";
    var {
      slotNameDisallowedPrefixes: SLOT_NAME_DISALLOWED_PREFIXES2
    } = require_learner_display2();
    function slotNameRegistryIssue2(slot) {
      if (typeof slot !== "string") return "slot_not_string";
      if (!/^[a-z][a-z0-9_]*$/.test(slot)) return "slot_not_snake_case";
      if (SLOT_NAME_DISALLOWED_PREFIXES2.some((pattern) => pattern.test(slot))) return "implementation_phase_marker_in_slot";
      return "";
    }
    function isCleanSlotName2(slot) {
      return !slotNameRegistryIssue2(slot);
    }
    function cleanSlots2(slots = []) {
      return [...new Set((slots || []).filter(isCleanSlotName2))].sort();
    }
    module2.exports = {
      slotNameRegistryIssue: slotNameRegistryIssue2,
      isCleanSlotName: isCleanSlotName2,
      cleanSlots: cleanSlots2
    };
  }
});

// src/runtime-resources/grammar/templates/slot-generation-rules.js
var require_slot_generation_rules = __commonJS({
  "src/runtime-resources/grammar/templates/slot-generation-rules.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      { slot: "subject", when: { labels: ["who"] }, note: "People/pronouns can act as sentence subjects." },
      { slot: "stance_holder", when: { labels: ["who"] }, note: "People/pronouns can hold an opinion or stance." },
      { slot: "recipient", when: { labels: ["who"] }, note: "People/pronouns can receive speech or transferred information." },
      { slot: "co_participant", when: { labels: ["who"] }, note: "People/pronouns can appear as comitative participants." },
      { slot: "topic", when: { labels: ["what", "where", "who"], syntaxIncludes: ["np", "topic", "place"] }, note: "NP-like things can become topics." },
      { slot: "object", when: { labels: ["what", "where"], syntaxIncludes: ["np", "object", "place", "food", "price"] }, note: "Nouns/NPs can be objects." },
      { slot: "head_noun", when: { labels: ["what", "where", "who"], syntaxIncludes: ["noun", "np", "place"] }, note: "Nouns can head noun phrases." },
      { slot: "location", when: { labels: ["where"], syntaxIncludes: ["place"] }, note: "Place-like expressions can be locations." },
      { slot: "ambient_location_frame", when: { syntaxIncludes: ["ambient_location_frame"] }, note: "Spatial localizers such as 出面 can frame an ambient environmental clause without being forced into subject or topic status." },
      { slot: "environmental_event_head", when: { syntaxIncludes: ["environmental_event_head"] }, note: "Lexically licensed event heads such as 落/打 can head conventional environmental predicates when paired with the correct weather phenomenon." },
      { slot: "weather_phenomenon", when: { syntaxIncludes: ["weather_phenomenon"] }, note: "Visible weather nouns such as 雨/風 fill the phenomenon component of an environmental predicate rather than an ordinary patient object." },
      { slot: "environmental_transition_predicate", when: { syntaxIncludes: ["environmental_transition_predicate"] }, note: "Lexical environmental transitions such as 天光 can form genuinely subjectless clauses." },
      { slot: "ambient_property_predicate", when: { syntaxIncludes: ["ambient_property_predicate"] }, note: "Temperature properties may receive an ambient reading only under an overt environmental or location frame; the slot does not make every bare stative impersonal." },
      { slot: "goal", when: { labels: ["where"], syntaxIncludes: ["place"] }, note: "Place-like expressions can be motion goals." },
      { slot: "time", when: { labels: ["when"] }, note: "Time expressions can set the sentence time." },
      { slot: "classifier", when: { labels: ["measure_word"], syntaxIncludes: ["classifier"] }, note: "Learner-facing measure words fill internal classifier slots." },
      { slot: "quantity", when: { syntaxIncludes: ["quantity", "numeral"] }, note: "Numerals and quantity expressions fill quantity slots before classifiers." },
      { slot: "di_determiner", when: { syntaxIncludes: ["di_determiner"] }, note: "Nominal 啲 before a head noun fills a determiner/partitive classifier-like slot without treating 啲 as a pure plural marker." },
      { slot: "degree_particle", when: { surfaces: ["啲"], syntaxIncludes: ["quantity_degree_or_plural"] }, note: "啲 fills a degree-particle slot after stative/adjective or manner heads." },
      { slot: "demonstrative", when: { syntaxIncludes: ["demonstrative_determiner"] }, note: "Demonstratives fill determiner/specifier slots." },
      { slot: "demonstrative_pronoun", when: { surfaces: ["呢個", "嗰個", "呢啲", "嗰啲"] }, note: "Standalone demonstrative pronouns can introduce a following head noun in transparent learner NPs." },
      { slot: "wh_determiner", when: { syntaxIncludes: ["wh_determiner"] }, note: "Wh-determiners fill wh-specifier slots." },
      { slot: "modifier", when: { syntaxIncludes: ["modifier", "noun_modifier", "stative"], labels: ["like"] }, note: "Modifier-like tokens can modify a noun or predicate." },
      { slot: "degree_manner_head", when: { syntaxIncludes: ["stative_predicate", "manner_degree_word", "degree_manner_head"] }, note: "Degreeable stative/adjective or manner heads can combine with 啲 as a productive degree/manner phrase." },
      { slot: "manner_degree_word", when: { syntaxIncludes: ["manner_degree_word"] }, note: "Manner words such as 大聲/細聲/小心 can be degree-modified before 啲." },
      { slot: "ordinal_modifier", when: { syntaxIncludes: ["ordinal_modifier"] }, note: "Ordinal modifiers such as 第二 can modify classifier-headed noun phrases." },
      { slot: "nominal_linker", when: { surfaces: ["嘅"] }, note: "嘅 links a modifier to a noun phrase." },
      { slot: "topic_frame_linker", when: { syntaxIncludes: ["relational_topic_coverb", "discourse_topic_marker"] }, note: "Overt topic-frame linkers such as 關於 and 對於 introduce a visible nominal discourse domain before a linked clause." },
      { slot: "relational_coverb_linker", when: { surfaces: ["對"], syntaxIncludes: ["relational_coverb"] }, note: "Bare relational coverbs such as 對 remain visible wrapper linkers without automatically receiving high-confidence overt-topic-frame provenance." },
      { slot: "time_head", when: { syntaxIncludes: ["weekday", "time_np", "temporal"] }, note: "Time nouns can head time noun phrases." },
      { slot: "temporal_modifier", when: { surfaces: ["下", "上"] }, note: "下/上 can modify time nouns such as 星期五." },
      // Phase 3: stative predicate slots are added by parser-active feature-bundle helpers below,
      // not by direct syntax-substring slot rules.
      { slot: "stative_predicate", when: { labels: ["like"], syntaxIncludes: ["stative"] }, note: "Property/stative tokens can be predicates." },
      { slot: "stative_complement_head", when: { surfaces: ["食", "飲"] }, note: "Sensory complement heads can become stative heads only inside degree-stative constructions such as 好食 / 好飲." },
      { slot: "degree", when: { surfaces: ["好", "太", "幾"], syntaxIncludes: ["degree_marker", "degree_particle"] }, note: "Degree words and degree particles modify stative predicates or manner heads." },
      { slot: "scalar_dimension_predicate", when: { surfaces: ["遠", "近", "貴", "平", "高", "矮", "耐"] }, note: "Scalar dimension predicates can combine with question-use 幾 in scalar-value questions." },
      { slot: "cognition_predicate", when: { syntaxIncludes: ["cognition"], verbClass: ["cognition"] }, note: "Cognition verbs can fill cognition-predicate slots while keeping their learner-visible role." },
      { slot: "vp_complement_predicate", when: { surfaces: ["記得"], syntaxIncludes: ["cognition"] }, note: "Complement-taking predicates such as 記得 can introduce a following VP complement." },
      { slot: "negative_cognition_predicate", when: { surfaces: ["唔知"], syntaxIncludes: ["negative_cognition"] }, note: "Opaque negative cognition predicate / fragment: 唔知 = don't know." },
      { slot: "stance_predicate", when: { surfaces: ["覺得", "認為", "以為", "相信", "懷疑"], syntaxIncludes: ["opinion"] }, note: "Direct opinion/stance predicates introduce opinions or attitudes; reportative 話 should use speech_verb instead." },
      { slot: "reportative_source", when: { surfaces: ["話", "聽講"] }, note: "Reportative/evidential source marker for attributed information." },
      { slot: "opinion_topic", when: { labels: ["what", "where"], syntaxIncludes: ["noun", "np", "place"] }, note: "Topic/content noun phrase inside an opinion frame." },
      { slot: "main_verb", when: { labels: ["doing"] }, note: "Reviewed doing-role verbs fill main-verb slots." },
      { slot: "action_verb", when: { labels: ["doing"] }, note: "Reviewed doing-role action verbs can head VPs." },
      { slot: "consumption_verb", when: { surfaces: ["食", "飲"] }, note: "Consumption verbs can govern a measured-quantity object such as 七杯度." },
      { slot: "posture_verb", when: { surfaces: ["坐", "企", "瞓"] }, note: "Posture verbs can head a locative posture VP before 喺度." },
      { slot: "distributive_quantifier", when: { surfaces: ["每"], syntaxIncludes: ["distributive_quantifier"] }, note: "Distributive determiner such as 每 before a quantified time or noun phrase." },
      { slot: "movement_verb", when: { surfaces: ["去", "返", "嚟", "落", "上"], syntaxIncludes: ["movement"] }, note: "Movement verbs can introduce goals or directional motion compounds." },
      { slot: "return_motion_verb", when: { surfaces: ["返"], syntaxIncludes: ["movement_verb"] }, note: "Return-motion heads such as 返 can combine with deictic/directional complements." },
      { slot: "verb_complement", when: { surfaces: ["返"], syntaxIncludes: ["movement_verb"] }, note: "Verbal complements such as 返 can follow a main verb before an object in broad verb-complement VPs." },
      { slot: "movement_direction", when: { surfaces: ["落", "上", "入", "出"], syntaxIncludes: ["movement_direction"] }, note: "Directional motion components such as 落/上/入/出 can combine with final 嚟/去." },
      { slot: "deictic_motion_marker", when: { surfaces: ["嚟", "去"] }, note: "Deictic motion components such as 嚟/去 complete native directional motion compounds." },
      { slot: "speech_verb", when: { surfaces: ["講", "話"], syntaxIncludes: ["speech"] }, note: "Speech verbs can introduce reported clauses or recipients." },
      { slot: "modal", when: { surfaces: ["想", "要", "可以", "會", "識", "使", "唔使", "可唔可以"], syntaxIncludes: ["modal"] }, note: "Modals/desideratives license following VPs." },
      { slot: "desiderative_modal", when: { surfaces: ["想"], syntaxIncludes: ["modal_desiderative"] }, note: "Specific wanting/desiderative modal; prevents general modals like 可以 from matching WantVP." },
      { slot: "negator", when: { surfaces: ["唔", "冇", "未"] }, note: "Negators fill negation slots." },
      { slot: "m4_negator", when: { surfaces: ["唔"], syntaxIncludes: ["negator"] }, note: "唔 fills the verbal m4-negator slot in productive negated VP patterns." },
      { slot: "existential", when: { surfaces: ["有"] }, note: "有 introduces existence/possession." },
      { slot: "negated_existential", when: { surfaces: ["冇"] }, note: "冇 introduces negated existence/possession." },
      { slot: "existential_question", when: { surfaces: ["有冇"] }, note: "有冇 is an existential yes/no question marker." },
      { slot: "interest_question_frame", when: { surfaces: ["有冇興趣"], syntaxIncludes: ["interest_question_frame"] }, note: "Lexicalized 有冇興趣 carries an existential question over the abstract object 興趣." },
      { slot: "locative_marker", when: { surfaces: ["喺"] }, note: "喺 introduces location." },
      { slot: "coverb_marker", when: { surfaces: ["喺", "同", "用"], syntaxIncludes: ["coverb"] }, note: "Preverbal coverbs introduce locative, interpersonal/addressee, comitative, or instrument phrases before the main predicate." },
      { slot: "identification_marker", when: { surfaces: ["就係"], syntaxIncludes: ["focus_copula"] }, note: "就係 introduces focused identification." },
      { slot: "evaluation_marker", when: { surfaces: ["算"] }, note: "算 marks evaluation or classification." },
      { slot: "focus_adverb", when: { surfaces: ["都", "又", "就"] }, note: "Focus/sequence/result markers." },
      { slot: "suggestion_marker", when: { surfaces: ["不如"] }, note: "不如 introduces a suggestion." },
      { slot: "comitative_marker", when: { surfaces: ["同"] }, note: "同 introduces a co-participant or addressee-like coverb object depending on predicate context." },
      { slot: "list_connector", when: { surfaces: ["同埋"] }, note: "同埋 links list items." },
      { slot: "aspect_marker", when: { surfaces: ["過", "緊", "咗", "吓"] }, note: "Aspect/delimitative markers." },
      { slot: "completion_marker", when: { surfaces: ["完", "晒"] }, note: "完 and 晒 mark completion; 晒 additionally contributes totality." },
      { slot: "result_complement_marker", when: { surfaces: ["到"], syntaxIncludes: ["result_or_attainment"] }, note: "到 marks a result, attainment, or ability-to-reach result complement." },
      { slot: "result_marker", when: { surfaces: ["就", "到"] }, note: "Result/sequence markers." },
      { slot: "question_marker", when: { surfaces: ["未", "咩"] }, note: "Question markers." },
      { slot: "wh_object", when: { surfaces: ["咩"] }, note: "Wh object pronoun: what." },
      { slot: "evaluation_question", when: { surfaces: ["點樣"], syntaxIncludes: ["evaluation_wh"] }, note: "Evaluation question expression." },
      { slot: "location_question", when: { surfaces: ["邊度"], syntaxIncludes: ["wh_place"] }, note: "Location question expression." },
      { slot: "time_question", when: { surfaces: ["幾時"], syntaxIncludes: ["wh_time"] }, note: "Time question expression." },
      { slot: "scalar_value_question", when: { surfaces: ["幾錢"], syntaxIncludes: ["wh_price"] }, note: "Scalar value question expression; 幾錢 is the price-domain realization." },
      { slot: "price_question", when: { surfaces: ["幾錢"], syntaxIncludes: ["wh_price"] }, note: "Price-domain question expression retained as semantic/domain metadata." },
      { slot: "direct_nominal_wh_determiner", when: { syntaxIncludes: ["direct_nominal_wh_determiner"] }, note: "Wh determiner that can directly modify a following noun without an intervening classifier." },
      { slot: "wh_nominal", when: { syntaxIncludes: ["wh_thing", "wh_person", "wh_place", "wh_time", "wh_reason", "wh_manner"] }, note: "Wh pronoun or wh-nominal expression; may fill a question role but is not an ordinary lexical noun head inside another wh NP." },
      { slot: "wh_determiner", when: { surfaces: ["邊", "咩"] }, note: "Wh determiner." },
      { slot: "abstract_object", when: { surfaces: ["興趣", "問題", "消息"], syntaxIncludes: ["abstract"] }, note: "Abstract nouns can fill abstract-object slots." },
      { slot: "scalar_value_noun", when: { surfaces: ["價", "價錢", "價位", "錢", "蚊"], syntaxIncludes: ["price"] }, note: "Scalar value nouns/units; price words are domain-specific members of the value scale." },
      { slot: "price_noun", when: { surfaces: ["價", "價錢", "價位", "錢", "蚊"], syntaxIncludes: ["price"] }, note: "Price nouns/units retained as semantic/domain metadata." },
      { slot: "age_unit", when: { surfaces: ["歲"], syntaxIncludes: ["age_unit"] }, note: "Age-measure unit licensing restricted copula-less age predication." },
      { slot: "measure_unit", when: { surfaces: ["呎", "尺"], syntaxIncludes: ["measure_unit"] }, note: "Physical measure unit licensing restricted copula-less measure predication." },
      { slot: "area_measure_unit", when: { surfaces: ["呎"], syntaxIncludes: ["area_measure_unit"] }, note: "Area-measure use of 呎 in room/property measurement." },
      { slot: "length_measure_unit", when: { surfaces: ["呎", "尺"], syntaxIncludes: ["length_measure_unit"] }, note: "Length-measure unit used with a dimensional predicate when overt." },
      { slot: "dimension_predicate", when: { surfaces: ["長"] }, note: "Overt scalar dimension predicate completing a measure expression." },
      { slot: "scalar_range_noun", when: { surfaces: ["價位"], syntaxIncludes: ["price_range"] }, note: "Scalar range nouns/price-point expressions such as 價位." },
      { slot: "price_range_noun", when: { surfaces: ["價位"], syntaxIncludes: ["price_range"] }, note: "Price-range nouns/price-point expressions retained as domain metadata." },
      { slot: "epistemic_scope_particle", when: { syntaxIncludes: ["epistemic_uncertainty_particle"] }, note: "Sentence-final particle that scopes uncertainty or probability over a proposition-like host." },
      { slot: "evidential_scope_particle", when: { syntaxIncludes: ["evidential_noteworthiness_particle"] }, note: "Sentence-final particle that marks broad reportative, noteworthy, reminder, or counterexpectation stance over proposition-like content without selecting a tone-specific subtype." },
      { slot: "directive_scope_particle", when: { syntaxIncludes: ["directive_closure_particle"] }, note: "Sentence-final 啦 laa1 that scopes directive, suggestion, invitation, or interpersonal closure over a licensed directive-like host." },
      { slot: "change_state_scope_particle", when: { syntaxIncludes: ["change_state_current_relevance_particle"] }, note: "Sentence-final 喇 laa3 that scopes change of situation, current relevance, or transition over a proposition-like host." },
      { slot: "restrictive_focus_particle", when: { syntaxIncludes: ["restrictive_focus_particle"] }, note: "Sentence-final 啫/咋 that marks a restrictive or minimizing focus relation over a visible scalar or quantity host." },
      { slot: "particle", when: { labels: ["particle"] }, note: "Sentence-final/linking particles." }
    ];
  }
});

// src/runtime-resources/grammar/templates/construction-templates.js
var require_construction_templates = __commonJS({
  "src/runtime-resources/grammar/templates/construction-templates.js"(exports2, module2) {
    "use strict";
    module2.exports = [
      {
        type: "OpinionQuestion",
        label: "OpinionQ",
        template: ["subject!", "stance_predicate!", "topic_or_object!", "evaluation_question!", "particle?"],
        constraints: {
          slot_surface_in: {
            stance_predicate: ["覺得"],
            evaluation_question: ["點樣"]
          }
        },
        predicate_subtype: "jyutgok_dimjoeng_evaluation_question",
        not_claims: ["not_all_stance_predicates", "not_all_question_complements", "not_subjectless", "not_referentless"],
        note: "Bounded source-linked evaluation question with an overt subject, 覺得, an overt evaluated referent, and 點樣. Other cognition-question complements remain independently structured."
      },
      {
        type: "NegativeCognitionFragment",
        label: "NegCognition",
        template: ["negative_cognition_predicate!", "particle?"],
        note: "Negative cognition fragment: 唔知 as a stable learner fragment / short answer."
      },
      {
        type: "ANotAQuestion",
        label: "A-not-A",
        template: ["subject?", "action_verb!", "m4_negator!", "action_verb!", "object?", "particle?"],
        constraints: { copied_first_token_surface_slots: ["action_verb"] },
        output_slots: ["question_fragment", "predicate", "vp", "action_vp", "negator", "object"],
        note: "Category-based A-not-A polar question: optional subject + verb + 唔 + copied verb/VO complement, preserving the copied verb and transparent VP/object child."
      },
      {
        type: "NegativePotentialComplement",
        label: "NegPotential",
        template: ["action_verb!", "m4_negator!", "result_complement_marker!", "object?"],
        role_overrides: {
          result_complement_marker: {
            label: "func",
            syntax: "result_potential_complement",
            note: "到 marks the result/attainment complement in a negative potential phrase."
          }
        },
        output_slots: ["negative_potential_complement", "result_potential_complement", "result_complement", "vp", "action_vp", "predicate", "negator", "object"],
        note: "Category-based negative potential/result complement: action verb + 唔 + 到 + optional object."
      },
      {
        type: "ResultComplement",
        label: "Result",
        template: ["action_verb!", "result_complement_marker!", "object?"],
        role_overrides: {
          result_complement_marker: {
            label: "func",
            syntax: "result_complement",
            note: "到 marks that the action reaches or attains a result."
          }
        },
        output_slots: ["result_complement", "result_attainment_complement", "vp", "action_vp", "predicate", "object"],
        note: "Category-based positive result/attainment complement: action verb + 到 + optional object."
      },
      {
        type: "CompletionQuestion",
        label: "V完未",
        template: ["subject?", "completion_vp!", "question_marker!", "particle?"],
        note: "Completion question: optional subject + completed VP + 未 + optional particle."
      },
      {
        type: "ProgressiveWhObjectQuestion",
        label: "ProgWhQ",
        template: ["subject?", "progressive_vp!", "wh_object!", "particle?"],
        role_overrides: {
          wh_object: {
            label: "what",
            syntax: "wh_object",
            slots: ["wh_object", "object"],
            note: "咩 is functioning as the object wh-word here, not as a sentence-final surprise particle."
          }
        },
        note: "Progressive what-object question: subject + progressive VP + wh object."
      },
      {
        type: "ExistentialQuestion",
        label: "Have?",
        template: ["subject?", "existential_question!", "topic_or_object!", "particle?"],
        constraints: { slot_must_not_have_slots: { topic_or_object: ["vp", "action_vp", "predicate", "productive_vo"] } },
        note: "Existential/possessive yes-no question: optional subject + 有冇 + possessed/existing NP."
      },
      {
        type: "NegatedExistentialClause",
        label: "NoHave",
        template: ["subject?", "negated_existential!", "topic_or_object!", "particle?"],
        note: "Negated existential/possessive clause: optional subject + 冇 + possessed/existing NP."
      },
      {
        type: "ExistentialWhQuestion",
        label: "有咩",
        template: ["subject?", "existential!", "wh_object!", "topic_or_object?", "particle?"],
        note: "Existential wh-question with 有 + wh object, optionally followed by a noun phrase."
      },
      {
        type: "ExistentialClause",
        label: "Have",
        template: ["subject?", "existential!", "topic_or_object!", "particle?"],
        note: "Existential/possessive clause: optional subject + 有 + possessed/existing NP."
      },
      {
        type: "SubjectPredicateClause",
        label: "SubjPred",
        template: ["subject!", "stative_predicate!", "particle?"],
        output_slots: ["subject_predicate_clause", "subject", "predicate", "clause", "stative_predicate", "comment", "comment_predicate"],
        retired_label_alias: "SubjectStativePredicateClause",
        template_family: "generative_template",
        predicate_subtype: "stative",
        note: "Subject-led predicate clause with stative predicate subtype: subject + stative predicate, separated from topic-comment evaluation."
      },
      {
        type: "TopicComment",
        label: "TopicComment",
        template: ["opinion_topic!", "comment_predicate!", "particle?"],
        constraints: {
          first_node_must_not_have_slots: ["subject", "co_participant", "stance_holder", "recipient"]
        },
        note: "Topic-comment clause: topic/content NP + safe comment predicate family."
      },
      {
        type: "ReportedSpeech",
        label: "Reported",
        template: ["subject?", "speech_verb!", "reported_content!", "particle?"],
        constraints: {
          slot_must_not_have_slots: {
            reported_content: ["degree_manner_adverbial"]
          }
        },
        note: "Reported speech/evaluation: speech-reporting verb plus reported content. A postverbal degree/manner complement such as 講大聲啲 is not reported content."
      },
      {
        type: "ExperientialClause",
        label: "ExpClause",
        template: ["subject?", "experiential_vp!", "particle?"],
        note: "Experiential clause with optional pro-drop subject and experiential VP anchor."
      },
      {
        type: "IntentionFrame",
        label: "Intention",
        template: ["subject!", "intention_predicate!", "vp!", "particle?"],
        constraints: { slot_surface_in: { intention_predicate: ["諗住"] } },
        note: "Source-linked lexical intention profile: 諗住 followed by a visible VP complement."
      },
      {
        type: "OpinionStanceFrame",
        label: "Opinion/Stance",
        template: ["subject?", "stance_predicate!", "stance_marker?", "opinion_topic?", "degree?", "stative_predicate!", "particle?"],
        note: "Opinion plus evaluation frame: optional subject + stance predicate + optional topic/content + stative predicate."
      },
      {
        type: "ExperientialQuestion",
        label: "Exp未",
        template_family: "construction_template",
        template: ["subject?", "experiential_vp!", "question_marker!", "particle?"],
        constraints: {
          slot_surface_in: { question_marker: ["未"] },
          slot_must_have_any_slots: { experiential_vp: ["object", "goal", "location"] }
        },
        note: "Bounded source-linked experiential question with overt typed experiential predicate/domain followed by final 未 and optional particle. Objectless short forms require separate explicit discourse licensing."
      },
      {
        type: "NegativeExperiential",
        label: "NegExp",
        template: ["subject?", "focus_adverb?", "negator!", "experiential_vp!", "particle?"],
        constraints: { slot_surface_in: { negator: ["未", "冇"] } },
        note: "Negative/not-yet experiential: preverbal 未/冇 + experiential VP."
      },
      {
        type: "ExperientialYesNoQuestion",
        label: "Exp?",
        template: ["subject?", "existential_question!", "experiential_vp!", "topic_or_object?", "particle?"],
        note: "Have-or-not experiential question."
      },
      {
        type: "ExistentialQuestion",
        label: "Have?",
        template: ["subject?", "interest_question_frame!", "vp?", "particle?"],
        output_slots: ["existential_question", "question_fragment", "possessive_question", "predicate", "object", "abstract_object"],
        retired_label_alias: "InterestQuestion",
        existential_subtype: "abstract_object",
        abstract_object_domain: "interest",
        note: "Existential question over the lexicalized abstract-object frame 有冇興趣. Interest is domain metadata, not the active construction label."
      },
      {
        type: "ExistentialQuestion",
        label: "Have?",
        template: ["subject?", "existential_question!", "abstract_object!", "vp?", "particle?"],
        constraints: { slot_must_not_have_slots: { abstract_object: ["vp", "action_vp", "predicate", "productive_vo"] } },
        output_slots: ["existential_question", "question_fragment", "possessive_question", "predicate", "object", "abstract_object"],
        retired_label_alias: "InterestQuestion",
        existential_subtype: "abstract_object",
        abstract_object_domain: "interest",
        note: "Existential question over an abstract object such as 興趣. Interest is domain metadata, not the active construction label."
      },
      {
        type: "DesiderativeVP",
        label: "WantVP",
        template: ["subject?", "degree?", "desiderative_modal!", "vp!", "particle?"],
        note: "Desire/wanting VP: optional subject and degree + specific desiderative modal + VP."
      },
      {
        type: "ModalANotAQuestion",
        label: "ModalQ",
        template: ["subject?", "modal!", "negator?", "modal!", "vp!", "particle?"],
        note: "Broad modal A-not-A question: optional subject + modal A-not-A material + VP. Subtype metadata distinguishes desiderative 想唔想 from permission 可唔可以."
      },
      {
        type: "LocativeWhQuestion",
        label: "WhereQ",
        template: ["subject?", "time?", "directional_motion_vp!", "perfective_aspect!", "location_question!", "particle?"],
        output_slots: ["question_fragment", "location_question", "subject", "time", "perfective_aspect", "directional_motion_vp", "predicate", "vp", "clause", "location"],
        note: "Motion locative wh-question: optional subject/time + directional motion VP + perfective aspect + location question."
      },
      {
        type: "LocativeWhQuestion",
        label: "WhereQ",
        template: ["locative_marker?", "location_question!", "particle?"],
        output_slots: ["question_fragment", "location_question", "location", "clause"],
        note: "Locative wh-question."
      },
      {
        type: "ScalarEvaluation",
        label: "ValueEval",
        template: ["negator!", "evaluation_marker!", "degree?", "stative_predicate!", "particle?"],
        output_slots: ["scalar_evaluation", "evaluation_clause", "predicate", "stative_predicate", "negator"],
        polarity_profile: "negative",
        note: "Negative lexical 算 evaluation with an overt property predicate; subject/topic and focus material remain visible when present."
      },
      {
        type: "ApproximateQuantity",
        label: "Approx",
        template: ["price_noun!", "approximation!", "particle?"],
        note: "Approximate price/quantity fragment: amount + approximation marker."
      },
      {
        type: "AcceptabilityClause",
        label: "Acceptability",
        template: ["subject?", "predicate!", "focus_adverb!", "acceptability_predicate!", "particle?"],
        output_slots: ["acceptability_clause", "acceptability", "focus_adverb", "acceptability_predicate", "predicate", "clause"],
        retired_label_alias: "PermissionAcceptabilityClause",
        acceptability_subtype: "action_feasibility",
        note: "Bounded declarative action-feasibility profile with an overt action predicate followed by 都得. Wh/free-choice 都得 requires separate analysis."
      },
      {
        type: "AcceptabilityANotA",
        label: "得唔得",
        template: ["time?", "acceptability_predicate!", "negator!", "acceptability_predicate!", "particle?"],
        note: "Acceptability A-not-A question: optional time/topic + 得唔得 + optional particle."
      },
      {
        type: "SuggestionQuestion",
        label: "Suggest",
        template: ["discourse_marker?", "suggestion_marker!", "vp!", "particle?"],
        note: "Suggestion construction headed by a suggestion marker."
      },
      {
        type: "ProhibitiveImperative",
        label: "Prohibitive",
        template: ["prohibitive_marker!", "vp!", "particle?"],
        note: "Prohibitive imperative: 唔好 + VP."
      },
      {
        type: "DegreeMannerModifiedVP",
        label: "DegMannerVP",
        template: ["degree_manner_adverbial!", "vp!", "particle?"],
        template_family: "generative_template",
        output_slots: ["degree_manner_modified_vp", "degree_manner_adverbial", "modifier", "degree", "vp", "action_vp", "predicate"],
        wrapper_policy: "transparent_modifier_wrapper",
        wrapper_reason: "v0.5.100 cleanup: attach productive degree/manner + 啲 phrases such as 快啲 to the following motion/action VP instead of leaving multi-root top constructions.",
        not_claims: [
          "not_standalone_degree_fragment",
          "not_motion_predicate_reanalysis",
          "not_serial_chain_collapse",
          "not_full_xbar_tree"
        ],
        note: "Broad transparent wrapper for degree/manner adverbial + following VP/action predicate. Keeps the inner motion or serial-purpose predicate visible while making the whole instruction one predicate span."
      },
      {
        type: "TemporalClause",
        label: "Time",
        template: ["time!", "locative_phrase!", "predicate!", "particle?"],
        constraints: {
          predicate_must_have_any_slots: ["serial_verb_purpose_chain"]
        },
        note: "Time/location clause: time + locative phrase + controlled predicate, preserving the accepted TemporalClause shape without fallback."
      },
      {
        type: "TemporalClause",
        label: "Time",
        template: ["time!", "subject?", "predicate!", "particle?"],
        note: "Time setting plus optional subject and predicate."
      },
      {
        type: "SubjectPredicateClause",
        label: "SubjPred",
        template: ["subject!", "time!", "predicate!", "particle?"],
        output_slots: ["subject_predicate_clause", "subject", "time", "predicate", "clause", "temporal_clause", "time_clause"],
        retired_label_alias: "SubjectTimePredicateClause",
        template_family: "generative_template",
        clause_modifier_profile: "time",
        note: "Subject-led predicate clause with time modifier: subject + time adverbial + predicate, preserving the predicate child."
      },
      {
        type: "SubjectPredicateClause",
        label: "SubjPred",
        template: ["subject!", "time!", "locative_phrase!", "predicate!", "particle?"],
        output_slots: ["subject_predicate_clause", "subject", "time", "location", "predicate", "clause", "temporal_clause", "time_clause"],
        retired_label_alias: "SubjectTimeLocationPredicateClause",
        template_family: "generative_template",
        clause_modifier_profile: "time_location",
        constraints: {
          predicate_must_have_any_slots: ["serial_verb_purpose_chain"]
        },
        note: "Subject-led predicate clause with time/location modifiers: subject + time adverbial + locative phrase + controlled predicate, preserving all child spans."
      },
      {
        type: "SubjectPredicateClause",
        label: "SubjPred",
        template: ["subject!", "predicate!", "particle?"],
        output_slots: ["subject_predicate_clause", "subject", "predicate", "clause", "negative_clause", "negated_predicate", "negator"],
        retired_label_alias: "SubjectNegatedPredicateClause",
        template_family: "generative_template",
        polarity: "negative",
        constraints: {
          predicate_must_have_any_slots: ["negated_directional_motion_vp", "negated_vp"]
        },
        note: "Subject-led predicate clause with negated predicate subtype: subject + negated predicate + optional particle, preserving the negated predicate child."
      },
      {
        type: "SubjectPredicateClause",
        label: "SubjPred",
        template: ["subject!", "predicate!", "particle?"],
        constraints: {
          predicate_must_have_any_slots: ["transitive_vp", "productive_vo", "progressive_vp", "perfective_vp", "completion_vp", "locative_posture_vp", "directional_motion_vp", "motion_goal_vp", "motion_purpose_chain", "serial_verb_purpose_chain"],
          disallow_child_slots: ["negated_directional_motion_vp", "negated_vp"]
        },
        note: "Subject-led predicate clause: subject + ordinary VP/aspect/locative-posture predicate + optional particle, preserving the predicate child while avoiding priority-marker and negated-motion diagnostics."
      },
      {
        type: "LocativeModalPredicateClause",
        label: "LocModal",
        template: ["location!", "time?", "how?", "modal_vp!", "particle?"],
        note: "Location/setting plus modal predicate clause: locative setting + optional frequency/certainty modifier + modal VP."
      },
      {
        type: "SubjectModalPredicateClause",
        label: "SubjModal",
        template: ["subject!", "time?", "how?", "modal_vp!", "particle?"],
        note: "Subject-preserving modal predicate clause: subject + optional time/certainty modifier + modal VP."
      },
      {
        type: "TopicModalPredicateClause",
        label: "TopicModal",
        template: ["topic!", "time?", "how?", "modal_vp!", "particle?"],
        constraints: {
          first_node_must_not_have_slots: ["subject", "co_participant", "stance_holder", "recipient", "location"]
        },
        note: "Topic-preserving modal predicate clause: topic NP + optional time/certainty modifier + modal VP."
      },
      {
        type: "NamingClause",
        label: "Called",
        template: ["subject!", "naming_verb!", "name!", "particle?"],
        constraints: { slot_surface_in: { naming_verb: ["叫"] } },
        note: "Source-linked personal naming clause: subject + 叫 + visible personal name."
      },
      {
        type: "FormulaDiscourseUnit",
        label: "Formula",
        template: ["formula_expression!", "particle?"],
        note: "Formula discourse unit: fixed social expression plus optional sentence-final particle. Former LeaveTakingFormula subtype is retired into this broad category."
      },
      {
        type: "PoliteImperativeClause",
        label: "PoliteImperative",
        template: ["politeness_marker!", "subject!", "time?", "path_phrase!", "predicate!", "particle?"],
        note: "Polite imperative clause: 請 + addressee + optional time + path phrase + motion/action predicate."
      },
      {
        type: "PolarQuestionFrame",
        label: "YesNo?",
        template: ["subject!", "yes_no_question_marker!", "predicate!", "particle?"],
        note: "Broad polar-question frame. Productive paths include subject + 係咪 + predicate and a complete proposition-like host plus a sentence-final polar particle."
      },
      {
        type: "CopularIdentificationFrame",
        label: "Ident",
        template: ["topic!", "copula!", "np!", "particle?"],
        note: "Copular identification frame: topic + 係 + nominal complement."
      },
      {
        type: "CoordinatedNP",
        label: "CoordNP",
        template: ["np!", "coordinator!", "np!"],
        output_slots: ["coordinated_np", "left_np", "right_np", "coordinator", "np", "topic", "object"],
        note: "Coordinated noun phrase joined by 同. Subject use is assigned by a larger clause, not by the NP itself."
      },
      {
        type: "NominalHeadSpan",
        label: "NP",
        template: ["head_noun!"],
        constraints: {
          disallow_child_slots: [
            "demonstrative",
            "classifier",
            "quantity",
            "modifier",
            "nominal_linker",
            "vp",
            "action_vp",
            "predicate",
            "perfective_vp",
            "progressive_vp",
            "completion_vp",
            "result_complement_vp",
            "potential_result_vp",
            "negative_potential_complement",
            "transitive_vp",
            "productive_vo"
          ]
        },
        note: "Internal neutral nominal-head span. It preserves overt nominal material but cannot by itself license definiteness, ellipsis, argument role, or standalone utterance status; excludes already structured classifier/modified NPs and predicate/VP children."
      },
      {
        type: "StativeNominalComplement",
        label: "AdjNP",
        template: ["how?", "how?", "nominal_linker?", "head_noun!"],
        constraints: {
          require_any_assigned_slots: ["how", "nominal_linker"],
          first_node_must_not_have_slots: ["quantity"]
        },
        note: "Stative/modified nominal complement such as 好好嘅朋友; requires a visible stative modifier or nominal linker, and excludes quantity-headed noun phrases."
      },
      {
        type: "CopularRelationFrame",
        label: "Relation",
        template: ["subject!", "copula!", "copular_complement!", "particle?"],
        note: "Copular relation/classification frame: subject + 係 + complement."
      },
      {
        type: "LocativeExistentialClause",
        label: "LocExist",
        template: ["topic!", "location!", "wh_object!", "focus_adverb?", "existential!", "particle?"],
        note: "Locative existential clause: topic + inside/location + wh/everything + 都 + 有."
      },
      {
        type: "ClassifierObjectNP",
        label: "CL-NP",
        template: ["classifier!", "head_noun!"],
        note: "Classifier-headed object NP without an overt numeral/demonstrative, e.g. 樣驚喜."
      },
      {
        type: "CoordinatedSubjectModalPredicateClause",
        label: "CoordSubjModal",
        template: ["subject!", "modal_vp!", "particle?"],
        note: "Coordinated-subject modal clause preserving an existing ModalVP child."
      },
      {
        type: "ClauseRelationGraph",
        label: "ClauseLink",
        template: ["clause_like!", "separator!", "clause_like!"],
        note: "Governed discourse/coordination wrapper over comma-linked clause-like constructions. Child constructions stay visible and unchanged; this is not a phrase-internal generative template."
      },
      {
        type: "ModalVP",
        label: "ModalVP",
        template: ["subject?", "time?", "negator?", "modal!", "manner?", "vp!", "particle?"],
        note: "Modal/desiderative construction matched by generated slot types."
      }
    ];
  }
});

// src/runtime-resources/grammar/unit-word-evidence.json
var require_unit_word_evidence = __commonJS({
  "src/runtime-resources/grammar/unit-word-evidence.json"(exports2, module2) {
    module2.exports = {
      schema: "canto-span-unit-word-evidence-v1",
      version: 1,
      source_ledger: "docs/research/CLASSIFIER-MEASURE-PRIMARY-SOURCE-LEDGER-R1.tsv",
      construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
      unit_word_senses: [
        {
          unit_word_sense_id: "UW-GO3-GENERAL",
          surface: "個",
          jyutping: "go3",
          unit_word_type: "general_classifier",
          semantic_unit_relation: "general_counting",
          source_terms: ["general classifier", "general counting unit"],
          source_ids: ["SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-TSE-LI-LEUNG-2007-CLASSIFIER-ACQUISITION", "SRC-ERBAUGH-2013-CLASSIFIER-DISCOURSE", "SRC-NAGY-LO-2019-CANTONESE-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current person and general_count runtime classes.", "No noun receives general_count automatically.", "Pair-level preference and variation remain unreviewed."]
        },
        {
          unit_word_sense_id: "UW-ZEK3-SORTAL",
          surface: "隻",
          jyutping: "zek3",
          unit_word_type: "sortal_classifier",
          semantic_unit_relation: "sortal_selection",
          source_terms: ["sortal classifier"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current animal runtime class.", "Broader colloquial, evaluative, body-part, or paired-item uses are not added."]
        },
        {
          unit_word_sense_id: "UW-GAA3-SORTAL",
          surface: "架",
          jyutping: "gaa3",
          unit_word_type: "sortal_classifier",
          semantic_unit_relation: "sortal_selection",
          source_terms: ["sortal classifier"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current vehicle runtime class.", "Overlap with 部 remains unadjudicated and is not broadened."]
        },
        {
          unit_word_sense_id: "UW-BOU6-SORTAL",
          surface: "部",
          jyutping: "bou6",
          unit_word_type: "sortal_classifier",
          semantic_unit_relation: "sortal_selection",
          source_terms: ["sortal classifier"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current vehicle and machine_device runtime classes.", "Overlap with 架 remains unadjudicated and is not broadened."]
        },
        {
          unit_word_sense_id: "UW-BUI1-CONTAINER-MEASURE",
          surface: "杯",
          jyutping: "bui1",
          unit_word_type: "container_measure",
          semantic_unit_relation: "container_content_measure",
          source_terms: ["container measure", "quantity expression"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS", "SRC-HER-HSIEH-2010-C-M-SEMANTICS", "SRC-HER-2012-C-M-MATHEMATICAL"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current liquid_measure runtime class.", "Physical-container noun readings and coerced content readings are not adjudicated here."]
        },
        {
          unit_word_sense_id: "UW-WUN2-CONTAINER-MEASURE",
          surface: "碗",
          jyutping: "wun2",
          unit_word_type: "container_measure",
          semantic_unit_relation: "container_content_measure",
          source_terms: ["container measure", "quantity expression"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS", "SRC-HER-HSIEH-2010-C-M-SEMANTICS", "SRC-HER-2012-C-M-MATHEMATICAL"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current food_bowl runtime class.", "Physical-container noun readings and coerced serving readings are not adjudicated here."]
        },
        {
          unit_word_sense_id: "UW-BUN2-SORTAL",
          surface: "本",
          jyutping: "bun2",
          unit_word_type: "sortal_classifier",
          semantic_unit_relation: "sortal_selection",
          source_terms: ["sortal classifier"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current book runtime class.", "No additional bound-volume or publication nouns are added."]
        },
        {
          unit_word_sense_id: "UW-ZOENG1-SORTAL",
          surface: "張",
          jyutping: "zoeng1",
          unit_word_type: "sortal_classifier",
          semantic_unit_relation: "sortal_selection",
          source_terms: ["sortal classifier"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current flat_object runtime class.", "Tables, beds, pictures, and other conventional extensions are not added."]
        },
        {
          unit_word_sense_id: "UW-ZI1-SORTAL",
          surface: "支",
          jyutping: "zi1",
          unit_word_type: "sortal_classifier",
          semantic_unit_relation: "sortal_selection",
          source_terms: ["sortal classifier"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the exact runtime surface 支 and current long_rigid class.", "The Week 19 source surface 枝 is not aliased or treated as equivalent."]
        },
        {
          unit_word_sense_id: "UW-WAI2-HONORIFIC",
          surface: "位",
          jyutping: "wai2",
          unit_word_type: "honorific_classifier",
          semantic_unit_relation: "honorific_person_selection",
          source_terms: ["honorific classifier", "polite person classifier"],
          source_ids: ["SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-TSE-LI-LEUNG-2007-CLASSIFIER-ACQUISITION", "SRC-ERBAUGH-2013-CLASSIFIER-DISCOURSE"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current person runtime class.", "Politeness, social meaning, register, and pair-level preference remain unreviewed."]
        },
        {
          unit_word_sense_id: "UW-GIN6-SORTAL",
          surface: "件",
          jyutping: "gin6",
          unit_word_type: "sortal_classifier",
          semantic_unit_relation: "sortal_selection",
          source_terms: ["sortal classifier"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current clothing runtime class.", "Matter, event, and broader individuated-item extensions are not added."]
        },
        {
          unit_word_sense_id: "UW-GAAN1-SORTAL",
          surface: "間",
          jyutping: "gaan1",
          unit_word_type: "sortal_classifier",
          semantic_unit_relation: "sortal_selection",
          source_terms: ["sortal classifier"],
          source_ids: ["SRC-XIA-2025-CLA-CLASSIFIER-MEASURE", "SRC-MATTHEWS-YIP-COMPREHENSIVE-CH6", "SRC-SYBESMA-2017-NOMINAL-CLASSIFIERS"],
          source_scope: "category_and_construction_profile_not_pair_specific",
          provenance_state: "source_category_supported_pair_unreviewed",
          construction_profiles: ["Dem-UNIT-N", "Num-UNIT-N"],
          migration_limitations: ["The migrated rule retains only the current building_shop runtime class.", "Rooms, institutions, and broader spatial uses are not added."]
        }
      ],
      noun_choice_rule_records: [
        { evidence_id: "UW-RULE-GO3", unit_word_sense_id: "UW-GO3-GENERAL", surface: "個", pair_status: "unreviewed", current_head_classes: ["person", "general_count"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current matches remain allowed by the bounded runtime rule; this migration makes no pair-level naturalness claim." },
        { evidence_id: "UW-RULE-ZEK3", unit_word_sense_id: "UW-ZEK3-SORTAL", surface: "隻", pair_status: "unreviewed", current_head_classes: ["animal"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current matches remain allowed by the bounded runtime rule; broader uses require separate evidence." },
        { evidence_id: "UW-RULE-GAA3", unit_word_sense_id: "UW-GAA3-SORTAL", surface: "架", pair_status: "unreviewed", current_head_classes: ["vehicle"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current matches remain allowed by the bounded runtime rule; overlap with 部 is not adjudicated." },
        { evidence_id: "UW-RULE-BOU6", unit_word_sense_id: "UW-BOU6-SORTAL", surface: "部", pair_status: "unreviewed", current_head_classes: ["vehicle", "machine_device"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current matches remain allowed by the bounded runtime rule; overlap with 架 is not adjudicated." },
        { evidence_id: "UW-RULE-BUI1", unit_word_sense_id: "UW-BUI1-CONTAINER-MEASURE", surface: "杯", pair_status: "unreviewed", current_head_classes: ["liquid_measure"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current content matches remain allowed as a bounded container-measure rule; physical-container and coercion readings are separate." },
        { evidence_id: "UW-RULE-WUN2", unit_word_sense_id: "UW-WUN2-CONTAINER-MEASURE", surface: "碗", pair_status: "unreviewed", current_head_classes: ["food_bowl"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current content matches remain allowed as a bounded container-measure rule; physical-container and coercion readings are separate." },
        { evidence_id: "UW-RULE-BUN2", unit_word_sense_id: "UW-BUN2-SORTAL", surface: "本", pair_status: "unreviewed", current_head_classes: ["book"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current matches remain allowed by the bounded runtime rule; no additional noun class is inferred." },
        { evidence_id: "UW-RULE-ZOENG1", unit_word_sense_id: "UW-ZOENG1-SORTAL", surface: "張", pair_status: "unreviewed", current_head_classes: ["flat_object"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current matches remain allowed by the bounded runtime rule; broader conventional extensions are not inferred." },
        { evidence_id: "UW-RULE-ZI1", unit_word_sense_id: "UW-ZI1-SORTAL", surface: "支", pair_status: "unreviewed", current_head_classes: ["long_rigid"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Only exact 支 is migrated; 枝 remains a separate unresolved source surface." },
        { evidence_id: "UW-RULE-WAI2", unit_word_sense_id: "UW-WAI2-HONORIFIC", surface: "位", pair_status: "unreviewed", current_head_classes: ["person"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current person matches remain allowed; honorific and register conditions remain separate evidence questions." },
        { evidence_id: "UW-RULE-GIN6", unit_word_sense_id: "UW-GIN6-SORTAL", surface: "件", pair_status: "unreviewed", current_head_classes: ["clothing"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current clothing matches remain allowed; broader uses require separate evidence." },
        { evidence_id: "UW-RULE-GAAN1", unit_word_sense_id: "UW-GAAN1-SORTAL", surface: "間", pair_status: "unreviewed", current_head_classes: ["building_shop"], legacy_status_on_match: "verified_compatible", legacy_status_on_missing_evidence: "unverified", legacy_status_on_class_mismatch: "incompatible", structural_np_status_on_complete_profile: "visible_profile_complete", downstream_argument_licensing_on_match: "allowed_by_bounded_runtime_rule", downstream_argument_licensing_otherwise: "blocked_by_conservative_policy", policy_note: "Current building/shop matches remain allowed; broader spatial uses require separate evidence." }
      ],
      unresolved_surfaces: [
        { surface: "枝", jyutping: "zi1", status: "unresolved_source_surface_reconciliation", related_runtime_surface: "支", runtime_rule_created: false, note: "The Week 19 source surface is recorded separately; this migration does not equate or alias it with 支." },
        { surface: "對", jyutping: "deoi3", status: "source_inventory_not_migrated", runtime_rule_created: false, note: "Pair-measure evidence requires the separate item-level review in issue #334 or later accepted work." },
        { surface: "把", jyutping: "baa2", status: "source_inventory_not_migrated", runtime_rule_created: false, note: "The source-listed handled-object form is not added to the compatibility map in this migration." },
        { surface: "條", jyutping: "tiu4", status: "source_inventory_not_migrated", runtime_rule_created: false, note: "The source-listed classifier is not added to the compatibility map in this migration." }
      ]
    };
  }
});

// src/runtime-resources/grammar/templates/category-span-templates.js
var require_category_span_templates = __commonJS({
  "src/runtime-resources/grammar/templates/category-span-templates.js"(exports2, module2) {
    "use strict";
    var PRODUCTIVE_VO2 = Object.fromEntries(require_productive_vo());
    var VERB_OBJECT_COMPOUNDS2 = Object.fromEntries(require_verb_object_compounds());
    var UNIT_WORD_EVIDENCE = require_unit_word_evidence();
    var AB45_CLASSIFIER_TYPES = /* @__PURE__ */ new Set(["general_classifier", "sortal_classifier", "honorific_classifier"]);
    var AB45_CLASSIFIER_SURFACES = UNIT_WORD_EVIDENCE.noun_choice_rule_records.filter((rule) => {
      const sense = UNIT_WORD_EVIDENCE.unit_word_senses.find((entry) => entry.unit_word_sense_id === rule.unit_word_sense_id);
      return sense && AB45_CLASSIFIER_TYPES.has(sense.unit_word_type);
    }).map((rule) => rule.surface);
    module2.exports = [
      {
        type: "DegreeMannerAdverbial",
        label: "Adj+啲",
        template: ["degree_manner_head!", "degree_particle!"],
        template_family: "generative_template",
        role_overrides: {
          degree_manner_head: {
            label: "how",
            syntax: "degree_manner_head modifier",
            note: "Degree/manner head inside a productive stative/adjective or manner + 啲 phrase."
          },
          degree_particle: {
            label: "how",
            syntax: "degree_particle degree",
            note: "啲 functions as the degree particle inside the productive degree/manner phrase."
          }
        },
        output_slots: ["degree_manner_adverbial", "modifier", "degree", "predicate"],
        note: "Productive degree/manner + 啲 phrase over generated slots, covering stative/adjective heads such as 快/慢/貴/平 and manner heads such as 大聲/細聲/小心."
      },
      // Linguistic basis: vocative/address terms are a recognized discourse-pragmatic category;
      // Cantonese named address forms commonly use 阿-prefixes and kinship/title suffixes.
      {
        type: "VocativeAddressTerm",
        label: "Address",
        template: ["address_prefix!", "name_element!", "address_suffix!"],
        output_slots: ["vocative_address_term", "named_address_term", "address_term", "vocative"],
        note: "Category-based Cantonese named address/vocative term: familiar prefix + name element + address suffix, e.g. 阿霞姐."
      },
      {
        type: "VocativeAddressTerm",
        label: "Address",
        template: ["address_prefix!", "name_element!"],
        output_slots: ["vocative_address_term", "named_address_term", "address_term", "vocative"],
        note: "Category-based Cantonese familiar name/address term: familiar prefix + name element, e.g. 阿霞."
      },
      {
        type: "VocativeAddressTerm",
        label: "Address",
        template: ["surname_element!", "address_suffix!"],
        output_slots: ["vocative_address_term", "named_address_term", "address_term", "vocative"],
        note: "Category-based Cantonese surname/title address term: surname element + formal address suffix, e.g. 陳生 / 陳先生."
      },
      {
        type: "VocativeAddressTerm",
        label: "Address",
        template: ["name_element!", "address_suffix!"],
        output_slots: ["vocative_address_term", "named_address_term", "address_term", "vocative"],
        note: "Category-based Cantonese name/title address term: name element + familiar address suffix, e.g. 霞姐 / 明哥."
      },
      {
        type: "CompoundDirectionalMotionVP",
        label: "MotionVP",
        template: ["return_motion_verb!", "movement_direction!", "deictic_motion_marker!"],
        constraints: {
          slot_surface_in: {
            return_motion_verb: ["返"],
            movement_direction: ["上"],
            deictic_motion_marker: ["嚟", "去"]
          }
        },
        role_overrides: {
          return_motion_verb: { label: "doing", syntax: "return_motion_component", note: "返 functions as the return-motion component inside a directional-motion VP." },
          movement_direction: { label: "doing", syntax: "movement_direction_up", note: "上 functions as the upward-direction component inside a compound directional-motion VP." },
          deictic_motion_marker: { label: "doing", syntax: "deictic_motion_marker", note: "嚟/去 functions as doing/deictic motion inside a directional-motion VP; this contrasts with func uses in 係...嚟㗎 and 用嚟 frames." }
        },
        output_slots: ["directional_motion_vp", "vp", "action_vp", "predicate", "movement_verb", "motion_predicate"],
        note: "Slot-based compound directional motion VP: return-motion head + direction + deictic come, e.g. 返上嚟."
      },
      {
        type: "DirectionalMotionVP",
        label: "MotionVP",
        template: ["deictic_motion_marker!"],
        role_overrides: {
          deictic_motion_marker: { label: "doing", syntax: "deictic_motion_marker", note: "嚟 functions as a one-word doing/deictic motion VP; this contrasts with func uses in bounded explanatory/purpose frames." }
        },
        output_slots: ["directional_motion_vp", "vp", "action_vp", "predicate", "movement_verb", "motion_predicate"],
        note: "Narrow one-word deictic motion predicate. Multi-part directional material is excluded from AA49 and handled by separately accepted structures."
      },
      {
        type: "NegatedDirectionalMotionVP",
        label: "NegMotionVP",
        template: ["m4_negator!", "directional_motion_vp!"],
        template_family: "generative_template",
        constraints: { slot_surface_in: { m4_negator: ["唔", "冇"] } },
        role_overrides: {
          m4_negator: { label: "func", syntax: "negator", note: "唔 or aspect-sensitive 冇 negates the following directional-motion VP." }
        },
        output_slots: ["negated_directional_motion_vp", "directional_motion_vp", "vp", "action_vp", "predicate", "movement_verb", "motion_predicate", "negator"],
        note: "Slot-based negated directional motion VP: licensed negator + directional-motion VP, e.g. 唔落嚟 or 冇去."
      },
      {
        type: "LocativePlacePhrase",
        label: "Location",
        template: ["locative_marker!", "location!"],
        note: "Category-based locative place phrase: 喺 + place/location."
      },
      {
        type: "ExperientialMotionGoalVP",
        label: "ExpVP",
        template: ["movement_verb!", "experiential_aspect!", "goal!"],
        constraints: { slot_surface_in: { movement_verb: ["去"] } },
        note: "Source-linked experiential motion-goal VP: attested 去 + 過 + overt goal/location."
      },
      {
        type: "ExperientialVP",
        label: "ExpVP",
        template: ["action_verb!", "experiential_aspect!"],
        note: "Category-based experiential VP: action verb + 過."
      },
      {
        type: "ProgressiveVP",
        label: "ProgVP",
        template: ["action_verb!", "progressive_aspect!", "object!"],
        template_family: "generative_template",
        constraints: {
          slot_must_not_be_bare_quantity_token: ["object"],
          slot_must_not_have_slots: { object: ["wh_object", "question_marker", "yes_no_question_marker"] }
        },
        output_slots: ["progressive_vp", "vp", "action_vp", "predicate", "progressive_aspect", "object"],
        note: "Transparent progressive transitive VP: action verb + 緊 + overt non-interrogative object; wh objects remain owned by the question construction."
      },
      {
        type: "ProgressiveVP",
        label: "ProgVP",
        template: ["action_verb!", "progressive_aspect!"],
        note: "Category-based progressive VP: action verb + 緊."
      },
      {
        type: "DegreeModifiedLexicalStative",
        label: "DegreeLexicalStative",
        template: ["degree!", "lexicalized_stative_predicate!"],
        note: "Degree-modified lexicalized stative — degree word + dictionary-backed 好X stative predicate, e.g. 好 + 好食."
      },
      {
        type: "DegreeStativePredicate",
        label: "DegreeStative",
        template: ["degree!", "degree?", "ordinary_stative_predicate!"],
        note: "Ordinary degree-stative predicate: degree word + independently usable ordinary stative predicate, e.g. 好 + 遠 / 好 + 貴 / 好 + 正. Excludes registry-backed lexicalized 好X statives."
      },
      {
        type: "NegatedStativePredicate",
        label: "NegStative",
        template: ["negator!", "ordinary_stative_predicate!"],
        template_family: "generative_template",
        constraints: { slot_surface_in: { negator: ["唔"] } },
        note: "Controlled negated ordinary stative predicate: 唔 + independently usable ordinary stative, e.g. 唔忙. Excludes lexicalized 好X statives such as 好味."
      },
      {
        type: "NegatedStativePredicate",
        label: "NegStative",
        template: ["negator!", "lexicalized_stative_predicate!"],
        template_family: "generative_template",
        constraints: { slot_surface_in: { negator: ["唔"] } },
        note: "Controlled compositional negation of a dictionary-backed lexical property predicate: 唔 + 好X, e.g. 唔 + 好食. Lexical 難X and prohibitive 唔好 + verb remain separate."
      },
      {
        type: "TopicComment",
        label: "TopicComment",
        template: ["opinion_topic!", "comment_predicate!"],
        subspan_kind: "topic_comment_evaluation",
        constraints: {
          first_node_must_not_have_slots: ["subject", "co_participant", "stance_holder", "recipient"]
        },
        note: "Category-based topic-comment: topic/content NP + safe comment predicate family."
      },
      {
        type: "DelimitedVP",
        label: "V吓",
        template: ["action_verb!", "delimitative_aspect!"],
        role_overrides: {
          delimitative_aspect: {
            label: "func",
            syntax: "delimitative_aspect",
            slots: ["aspect_marker", "delimitative_aspect"],
            note: "吓 marks a light/delimitative action here, not an interjection."
          }
        },
        note: "Category-based delimitative VP: action verb + 吓."
      },
      {
        type: "ReduplicatedVP",
        label: "V一V",
        template: ["action_verb!", "action_verb!"],
        constraints: { same_surface: true },
        note: "Category-based verb reduplication: identical action verb repeated for a light/checking action."
      },
      {
        type: "ImpersonalEnvironmentalClause",
        label: "Environment",
        template: ["environmental_event_head!", "weather_phenomenon!"],
        template_family: "construction_template",
        constraints: { surface_sequence_in: ["落雨", "打風"] },
        role_overrides: {
          environmental_event_head: {
            label: "doing",
            syntax: "environmental_event_predicate",
            slots: ["environmental_event_head", "environmental_predicate", "predicate"],
            note: "Visible event head inside a conventional environmental predicate; no referential subject is inserted."
          },
          weather_phenomenon: {
            label: "what",
            syntax: "weather_phenomenon environmental_phenomenon",
            slots: ["weather_phenomenon", "environmental_phenomenon"],
            note: "Visible weather-phenomenon component; it is not analysed as an ordinary affected object."
          }
        },
        output_slots: ["impersonal_environmental_clause", "subjectless_clause", "environmental_predicate", "predicate", "clause", "weather_phenomenon"],
        subject_status: "impersonal",
        subjectless_type: "genuinely_subjectless_environmental",
        hidden_subject_inserted: false,
        not_claims: ["not_productive_vo_object_relation", "not_null_referential_subject", "not_hidden_expletive_subject"],
        note: "Conventional environmental event predicate such as 落雨 or 打風. The visible components remain transparent, but the clause has no referential subject or ordinary patient object."
      },
      {
        type: "ProductiveVO",
        label: "VP",
        template: ["action_verb!", "object!"],
        trace_kind: "source_linked_runtime_matcher",
        trace_assigned_slots: [],
        constraints: {
          surface_sequence_in: Object.keys(VERB_OBJECT_COMPOUNDS2)
        },
        output_slots: ["productive_vo", "verb_object_compound", "vp", "action_vp", "predicate"],
        structural_scope: "vp",
        lexical_compound_profile: "contiguous_source_linked_seed",
        source_specification: "docs/research/ISSUE-753-AB35-LEXICAL-VO-RUNTIME-CONTRACT-R1.md",
        not_claims: [
          "not_generic_verb_plus_noun_productivity",
          "not_ordinary_object_relation_from_component_order",
          "not_generic_separability_licensing"
        ],
        note: "Source-linked AB35 VerbObjectCompound seed: exact reviewed contiguous lexical compounds with visible components but no compound-internal ordinary semantic object binding."
      },
      {
        type: "ProductiveVO",
        label: "VP",
        template: ["action_verb!", "object!"],
        template_family: "generative_template",
        constraints: {
          surface_sequence_in: Object.keys(PRODUCTIVE_VO2)
        },
        role_overrides: {
          action_verb: { label: "doing", syntax: "verb", note: "Action verb heading a productive verb-object VP." },
          object: { label: "what", syntax: "object", note: "Object noun inside a productive verb-object VP." }
        },
        output_slots: ["productive_vo", "vp", "action_vp", "predicate", "object"],
        note: "Slot-template ProductiveVO: generated action_verb + object pair, constrained to reviewed productive VO sequences while preserving visible children."
      },
      {
        type: "SerialVerbPurposeChain",
        label: "PurposeChain",
        template: ["directional_motion_vp!", "productive_vo!", "purpose_verb!", "particle?"],
        role_overrides: {
          purpose_verb: { label: "doing", syntax: "purpose_verb", note: "食 functions as the explicit purpose verb in a motion/action purpose chain." }
        },
        output_slots: ["serial_verb_purpose_chain", "serial_action_chain", "purpose_chain", "vp", "action_vp", "predicate"],
        note: "Category-based serial purpose chain: optional directional motion already grouped, ProductiveVO, and explicit purpose verb."
      },
      {
        type: "SerialVerbPurposeChain",
        label: "PurposeChain",
        template: ["productive_vo!", "purpose_verb!", "particle?"],
        role_overrides: {
          purpose_verb: { label: "doing", syntax: "purpose_verb", note: "食 functions as the explicit purpose verb after an action/object VP." }
        },
        output_slots: ["serial_verb_purpose_chain", "serial_action_chain", "purpose_chain", "vp", "action_vp", "predicate"],
        note: "Category-based serial purpose chain: ProductiveVO plus explicit purpose verb."
      },
      {
        type: "MotionPurposeChain",
        label: "MotionPurpose",
        template: ["directional_motion_vp!", "productive_vo!", "particle?"],
        output_slots: ["motion_purpose_chain", "motion_action_chain", "purpose_chain", "vp", "action_vp", "predicate"],
        note: "Category-based motion-purpose chain: directional motion plus an action/object VP such as 返嚟食飯 or 落嚟摘芒果."
      },
      {
        type: "ActionStativeVP",
        label: "VP",
        template: ["action_verb!", "stative_predicate!"],
        constraints: { slot_must_not_be_bare_quantity_token: ["stative_predicate"] },
        note: "Category-based action + stative/comparative predicate VP."
      },
      {
        type: "MotionGoalVP",
        label: "MotionGoal",
        template: ["directional_motion_vp!", "perfective_aspect!", "goal!"],
        template_family: "generative_template",
        constraints: {
          slot_must_not_have_slots: {
            goal: ["location_question", "wh_nominal"]
          }
        },
        role_overrides: {
          goal: {
            label: "where",
            syntax: "motion_goal_location place_or_goal",
            slots: ["goal", "location", "np", "topic", "head_noun"],
            note: "Place/location interpreted as the overt destination of a perfective motion event, not as an ordinary object."
          }
        },
        output_slots: ["motion_goal_vp", "directional_motion_vp", "movement_verb", "goal", "location", "perfective_aspect", "predicate", "vp", "action_vp"],
        note: "Broad perfective motion-goal VP: directional motion predicate + perfective aspect + overt destination, e.g. 去咗香港. Wh-place expressions remain in LocativeWhQuestion rather than entering this declarative path."
      },
      {
        type: "MotionGoalVP",
        label: "MotionGoal",
        template: ["movement_verb!", "perfective_aspect!", "goal!"],
        template_family: "generative_template",
        constraints: {
          slot_must_not_have_slots: {
            goal: ["location_question", "wh_nominal"]
          }
        },
        role_overrides: {
          movement_verb: {
            label: "doing",
            syntax: "motion_goal_verb movement_verb",
            slots: ["movement_verb", "action_verb", "main_verb", "predicate"],
            note: "Movement verb heading a perfective motion-to-destination predicate."
          },
          goal: {
            label: "where",
            syntax: "motion_goal_location place_or_goal",
            slots: ["goal", "location", "np", "topic", "head_noun"],
            note: "Place/location interpreted as the overt destination of a perfective motion event, not as an ordinary object."
          }
        },
        output_slots: ["motion_goal_vp", "movement_verb", "goal", "location", "perfective_aspect", "predicate", "vp", "action_vp"],
        note: "Broad perfective motion-goal VP: movement verb + perfective aspect + overt destination, e.g. 返咗屋企. Wh-place expressions remain in LocativeWhQuestion rather than entering this declarative path."
      },
      {
        type: "PostverbalZoPerfectiveVP",
        label: "PerfectiveVP",
        template: ["action_verb!", "perfective_aspect!", "object!"],
        constraints: {
          slot_must_not_be_bare_quantity_token: ["object"],
          slot_must_be_licensed_np: ["object"]
        },
        perfective_profile: "simple_postverbal_zo_overt_object",
        overt_object_required: true,
        aspect_marker_surface: "咗",
        aspect_category: "perfective_viewpoint",
        independent_past_tense_licensing: false,
        completion_or_result_licensing: false,
        completion_semantics_status: "not_decided_by_this_structural_node",
        experiential_licensing: false,
        hidden_object_insertion: false,
        selectional_compatibility_bypass: false,
        subject_insertion_capability: false,
        not_claims: ["not_past_tense_suffix", "not_separate_completion_or_result_node", "not_experiential", "not_hidden_object", "not_unrestricted_verb_object_compatibility"],
        note: "Source-linked structural subtype: an overt action predicate, postverbal 咗, and an overt licensed NP object. This node records perfective structure; it does not insert tense, hidden arguments, an experiential reading, or a separate completion/result node. Completion and current-relevance interpretations are not decided by this structural node, and predicate-object compatibility is reviewed separately."
      },
      {
        type: "PerfectiveVP",
        label: "PerfectiveVP",
        template: ["action_verb!", "perfective_aspect!", "particle?"],
        output_slots: ["perfective_vp", "completion_vp", "vp", "action_vp", "predicate", "perfective_aspect", "particle"],
        note: "Objectless perfective VP: action verb + 咗 + optional final particle. It may receive an activity reading or a context-linked object interpretation; the parent construction determines whether the full utterance is complete."
      },
      {
        type: "CompletionVP",
        label: "CompletionVP",
        template: ["action_verb!", "completion_marker!", "perfective_aspect!", "object!"],
        template_family: "generative_template",
        constraints: { slot_must_not_be_bare_quantity_token: ["object"] },
        output_slots: ["completion_vp", "perfective_vp", "vp", "action_vp", "predicate", "completion_marker", "perfective_aspect", "object"],
        note: "Completion-plus-perfective VP: action verb + result/completion marker + 咗 + overt object; the completion and viewpoint-aspect layers remain visible."
      },
      {
        type: "CompletionVP",
        label: "CompletionVP",
        template: ["action_verb!", "completion_marker!", "object!"],
        constraints: { slot_must_not_be_bare_quantity_token: ["object"] },
        note: "Category-based completion VP: action verb + completion marker + object."
      },
      {
        type: "CompletionVP",
        label: "CompletionVP",
        template: ["action_verb!", "completion_marker!"],
        template_family: "generative_template",
        constraints: { slot_surface_in: { completion_marker: ["完"] } },
        output_slots: ["completion_vp", "vp", "action_vp", "predicate", "completion_marker"],
        note: "Objectless completion predicate with overt 完; any omitted affected domain remains discourse-level rather than a fabricated object token."
      },
      {
        type: "ResultComplementVP",
        label: "ResultVP",
        template: ["action_verb!", "degree!", "perfective_aspect!", "object!"],
        template_family: "generative_template",
        constraints: {
          slot_surface_in: { degree: ["好"] },
          slot_must_not_be_bare_quantity_token: ["object"]
        },
        role_overrides: {
          degree: { label: "how", syntax: "result_state_complement", slots: ["result_complement"], note: "好 expresses the overt attained result state after the action verb." }
        },
        output_slots: ["result_complement_vp", "result_complement", "vp", "action_vp", "predicate", "perfective_aspect", "object"],
        note: "Transparent result-complement VP: action + overt result 好 + perfective 咗 + object."
      },
      {
        type: "ResultComplementVP",
        label: "ResultVP",
        template: ["action_verb!", "degree!", "object!"],
        template_family: "generative_template",
        constraints: {
          slot_surface_in: { degree: ["好"] },
          slot_must_not_be_bare_quantity_token: ["object"]
        },
        role_overrides: {
          degree: { label: "how", syntax: "result_state_complement", slots: ["result_complement"], note: "好 expresses the overt attained result state after the action verb." }
        },
        output_slots: ["result_complement_vp", "result_complement", "vp", "action_vp", "predicate", "object"],
        note: "Transparent result-complement VP: action + overt result 好 + object."
      },
      {
        type: "NegativePotentialComplement",
        label: "NegPotential",
        template: ["action_verb!", "m4_negator!", "completion_marker!", "classifier!", "head_noun!"],
        template_family: "generative_template",
        constraints: { slot_surface_in: { completion_marker: ["完"] } },
        role_overrides: {
          m4_negator: { label: "func", syntax: "negator potential_negator", slots: ["negator", "m4_negator"], note: "唔 negates the potential relation rather than the action itself." },
          completion_marker: { label: "func", syntax: "potential_result_complement", slots: ["result_complement", "completion_marker"], note: "完 is the overt result complement licensed by the negative potential pattern." },
          classifier: { label: "measure_word", syntax: "classifier", slots: ["classifier"], note: "Visible classifier inside the overt potential-construction object." },
          head_noun: { label: "what", syntax: "object head_noun", slots: ["object", "head_noun", "np"], note: "Overt object head inside the potential construction." }
        },
        output_slots: ["negative_potential_complement", "result_potential_complement", "result_complement", "vp", "action_vp", "predicate", "negator", "object", "classifier", "head_noun"],
        note: "Productive negative potential with result 完 and a transparent classifier-headed object."
      },
      {
        type: "NegativePotentialComplement",
        label: "NegPotential",
        template: ["action_verb!", "m4_negator!", "completion_marker!", "object?"],
        template_family: "generative_template",
        constraints: {
          slot_surface_in: { completion_marker: ["完"] },
          slot_must_not_be_bare_quantity_token: ["object"]
        },
        role_overrides: {
          m4_negator: { label: "func", syntax: "negator potential_negator", slots: ["negator", "m4_negator"], note: "唔 negates the potential relation rather than the action itself." },
          completion_marker: { label: "func", syntax: "potential_result_complement", slots: ["result_complement", "completion_marker"], note: "完 is the overt result complement licensed by the negative potential pattern." }
        },
        output_slots: ["negative_potential_complement", "result_potential_complement", "result_complement", "vp", "action_vp", "predicate", "negator", "object"],
        note: "Productive negative potential: action + 唔 + result 完 + optional overt object."
      },
      {
        type: "CompletionVP",
        label: "CompletionVP",
        template: ["action_verb!", "completion_marker!", "particle?"],
        constraints: { slot_surface_in: { completion_marker: ["晒"] } },
        output_slots: ["completion_vp", "vp", "action_vp", "predicate", "completion_marker", "particle"],
        completion_subtype: "totality_completion",
        note: "Objectless totality-completion VP: action verb + 晒, with the affected domain recoverable from the clause or discourse rather than fabricated as a null object."
      },
      {
        type: "DitransitiveSpeechVP",
        label: "tell",
        template: ["speech_verb!", "recipient!", "cognition_predicate!"],
        constraints: {
          slot_surface_in: {
            speech_verb: ["話"],
            cognition_predicate: ["知"]
          }
        },
        role_overrides: {
          speech_verb: { label: "doing", syntax: "speech_transfer_verb", note: "In this speech-transfer VP, 話 is the action tell/let know." }
        },
        note: "Category-based speech/transfer VP: tell someone know / let someone know."
      },
      {
        type: "VerbComplementVP",
        label: "VerbCompVP",
        template: ["action_verb!", "degree_manner_adverbial!"],
        template_family: "generative_template",
        role_overrides: {
          action_verb: {
            label: "doing",
            syntax: "main_verb",
            note: "Main verb inside a transparent verb-complement predicate."
          }
        },
        output_slots: ["verb_complement_vp", "verb_complement", "degree_manner_complement", "degree_manner_adverbial", "vp", "action_vp", "predicate", "main_verb"],
        note: "Broad verb-complement VP: main verb + postverbal degree/manner complement, e.g. 講大聲啲 or 行快啲. Keeps the degree/manner phrase transparent and prevents speech-reporting overgeneration."
      },
      {
        type: "VerbComplementVP",
        label: "VerbCompVP",
        template: ["action_verb!", "directional_motion_vp!"],
        template_family: "generative_template",
        role_overrides: {
          action_verb: {
            label: "doing",
            syntax: "main_verb",
            note: "Main verb inside a transparent verb-complement predicate."
          }
        },
        output_slots: ["verb_complement_vp", "verb_complement", "vp", "action_vp", "predicate", "main_verb", "directional_motion_vp"],
        note: "Broad verb-complement VP: main verb + directional complement phrase, e.g. 還返去. Keeps the directional complement transparent while exposing one predicate span."
      },
      {
        type: "VerbComplementVP",
        label: "VerbCompVP",
        template: ["action_verb!", "verb_complement!", "object!"],
        template_family: "generative_template",
        constraints: {
          slot_must_not_be_bare_quantity_token: ["object"],
          slot_must_not_have_slots: { object: ["vp", "action_vp", "predicate", "productive_vo"] }
        },
        role_overrides: {
          action_verb: {
            label: "doing",
            syntax: "main_verb",
            note: "Main verb inside a transparent verb-complement predicate."
          },
          verb_complement: {
            label: "doing",
            syntax: "verb_complement",
            slots: ["verb_complement", "predicate"],
            note: "Verbal complement after the main verb, kept transparent instead of wrapping as its own transitive VP."
          }
        },
        output_slots: ["verb_complement_vp", "verb_complement", "vp", "action_vp", "predicate", "main_verb", "object"],
        note: "Broad verb-complement VP: main verb + verbal complement + object. Keeps children transparent while preventing nested transitive VP analysis such as 還 [返啲書]."
      },
      {
        type: "MotionGoalVP",
        label: "MotionGoal",
        template: ["movement_verb!", "goal!"],
        template_family: "generative_template",
        role_overrides: {
          movement_verb: {
            label: "doing",
            syntax: "motion_goal_verb movement_verb",
            slots: ["movement_verb", "action_verb", "main_verb", "predicate"],
            note: "Movement verb heading a goal-directed motion VP."
          },
          goal: {
            label: "where",
            syntax: "motion_goal_location place_or_goal",
            slots: ["goal", "location", "np", "topic", "head_noun"],
            note: "Place/location interpreted as the goal of a movement verb, not as an ordinary object."
          }
        },
        output_slots: ["motion_goal_vp", "movement_verb", "goal", "location", "predicate", "vp", "action_vp"],
        note: "Category-based motion-goal VP: movement verb + place/location goal, e.g. 去中環 / 返屋企. Keeps the place as where/goal rather than ordinary transitive object."
      },
      {
        type: "TransitiveVP",
        label: "VP",
        template: ["action_verb!", "object!"],
        constraints: {
          slot_must_not_be_bare_quantity_token: ["object"],
          slot_must_not_have_slots: { object: ["approximate_quantity", "vp", "action_vp", "predicate", "productive_vo"] }
        },
        note: "Category-based transitive VP: action verb + nominal object; bare numeral tokens, approximate measured-quantity objects requiring a licensed predicate, and already typed VPs are not accepted through this generic object path."
      },
      {
        type: "DiMarkedNP",
        label: "NP",
        template: ["di_determiner!", "head_noun!"],
        template_family: "generative_template",
        role_overrides: {
          di_determiner: {
            label: "func",
            syntax: "di_determiner",
            slots: ["quantity", "di_determiner"],
            note: "啲 functions as a determiner/partitive classifier-like marker before a visible head noun."
          }
        },
        output_slots: ["di_marked_np", "np", "object", "topic", "quantity", "head_noun"],
        note: "Productive 啲-marked noun phrase over generated slots: 啲 determiner + head noun, e.g. 啲芒果 / 啲書; avoids claiming English-style plural morphology."
      },
      {
        type: "OvertHeadDemonstrativeClassifierNP",
        label: "NP",
        template: ["demonstrative!", "classifier!", "head_noun!"],
        template_family: "generative_template",
        constraints: {
          slot_must_not_have_slots: {
            demonstrative: ["quantity", "wh_determiner", "di_determiner"],
            classifier: ["quantity", "wh_determiner", "di_determiner"],
            head_noun: ["quantity", "classifier", "wh_determiner", "di_determiner"]
          }
        },
        note: "Bounded overt-head Cantonese demonstrative + classifier + complete nominal head phrase. Requires all three visible components and inserts no hidden numeral or noun."
      },
      {
        type: "QuantifiedTimeNP",
        label: "Time",
        template: ["quantity!", "classifier?", "time_head!", "particle?"],
        role_overrides: {
          quantity: {
            label: "how",
            syntax: "quantity count_value numeral",
            slots: ["quantity"],
            note: "Visible numeral inside a quantified time expression."
          },
          classifier: {
            label: "measure_word",
            syntax: "general_classifier classifier time_measure_classifier",
            slots: ["classifier"],
            note: "Visible measure word inside a quantified time expression."
          },
          time_head: {
            label: "when",
            syntax: "time_head temporal duration_unit",
            slots: ["time", "time_head"],
            note: "Time-unit head inside the quantified time expression."
          }
        },
        output_slots: ["quantified_time_np", "time", "time_head", "quantity", "classifier", "particle", "np", "topic"],
        context_requirement_status: "context_required",
        missing_argument_slots: ["discourse_relation"],
        antecedent_status: "not_observed",
        discourse_license_not_observed: true,
        fragment_subtype: "quantified_time_fragment",
        not_claims: ["not_general_quantity_np", "not_quantified_classifier_object_np", "not_context_free_clause"],
        note: "Productive numeral plus optional classifier plus time-unit phrase, with an optional final particle. As a standalone utterance it is a discourse-linked time or duration fragment."
      },
      {
        type: "QuantifiedTimeNP",
        label: "Time",
        template: ["distributive_quantifier!", "quantity?", "classifier?", "time_head!", "particle?"],
        role_overrides: {
          distributive_quantifier: {
            label: "func",
            syntax: "distributive_quantifier determiner",
            slots: ["distributive_quantifier"],
            note: "Every/each determiner before a time expression."
          },
          quantity: {
            label: "how",
            syntax: "quantity count_value numeral",
            slots: ["quantity"],
            note: "Visible numeral inside the quantified time expression."
          },
          classifier: {
            label: "measure_word",
            syntax: "general_classifier classifier time_measure_classifier",
            slots: ["classifier"],
            note: "Visible measure word inside the quantified time expression."
          },
          time_head: {
            label: "when",
            syntax: "time_head temporal duration_unit",
            slots: ["time", "time_head"],
            note: "Time-unit head inside the quantified time expression."
          }
        },
        output_slots: ["quantified_time_np", "time", "time_head", "quantity", "classifier", "distributive_quantifier", "particle", "np", "topic"],
        not_claims: ["not_general_quantity_np", "not_quantified_classifier_object_np"],
        note: "Productive every/each time-unit phrase. Distributive frequency expressions such as 每一日 are complete time NPs and do not require an external discourse relation."
      },
      {
        type: "ApproximateQuantity",
        label: "Approx",
        template: ["quantity!", "classifier!", "post_classifier_approximation!"],
        template_family: "generative_template",
        role_overrides: {
          quantity: {
            label: "how",
            syntax: "quantity count_value numeral",
            slots: ["quantity"],
            note: "Visible numeral inside an approximate measured-quantity expression."
          },
          classifier: {
            label: "measure_word",
            syntax: "classifier measured_quantity_classifier",
            slots: ["classifier"],
            note: "Visible measure word inside the approximate quantity expression."
          },
          post_classifier_approximation: {
            label: "how",
            syntax: "post_classifier_approximation_marker approximation_marker",
            slots: ["approximation", "modifier"],
            note: "about / approximately; scopes over the preceding numeral and measure word."
          }
        },
        output_slots: ["approximate_quantity", "quantified_object", "quantity", "classifier", "approximation", "object", "np", "topic"],
        approximation_scope: "quantity_classifier_measure",
        approximation_marker_surface: "度",
        object_semantics: "measured_quantity_object",
        head_recovery_status: "not_required_measure_phrase",
        not_claims: ["not_locative_postposition", "not_bare_classifier_head_ellipsis", "not_fabricated_nominal_head", "not_exact_quantity"],
        note: "Productive numeral + measure word + post-classifier approximation marker, e.g. 七杯度. The visible measure phrase functions as an approximate quantity without inventing a noun head."
      },
      {
        type: "QuantifiedClassifierNP",
        label: "NP",
        template_family: "generative_template",
        template: ["quantity!", "classifier!"],
        constraints: {
          slot_first_token_syntax_must_include_any: { quantity: ["numeral"] },
          slot_surface_in: { classifier: AB45_CLASSIFIER_SURFACES }
        },
        role_overrides: {
          quantity: {
            label: "how",
            syntax: "quantity count_value numeral",
            slots: ["quantity"],
            note: "Visible numeral before a classifier in a head-ellipsis noun phrase."
          },
          classifier: {
            label: "measure_word",
            syntax: "general_classifier classifier nominal_head_ellipsis",
            slots: ["classifier"],
            note: "Visible classifier whose noun head must be recovered from discourse rather than fabricated."
          }
        },
        output_slots: ["quantified_classifier_np", "classifier_np", "quantity", "classifier", "np", "topic", "object"],
        context_requirement_status: "context_required",
        missing_argument_slots: ["nominal_head"],
        antecedent_status: "not_observed",
        discourse_license_not_observed: true,
        fragment_subtype: "quantified_classifier_head_ellipsis",
        np_subtype: "quantified_classifier_head_ellipsis",
        not_claims: ["not_bare_numeral", "not_overt_head_noun", "not_fabricated_nominal_head", "not_quantified_time_np"],
        note: "Productive numeral plus classifier NP with an omitted nominal head, e.g. 兩部 / 一個. The broad QuantifiedClassifierNP label is retained while discourse status records the missing head."
      },
      {
        type: "QuantifiedClassifierNP",
        label: "NP",
        template_family: "generative_template",
        template: ["quantity!", "classifier!", "head_noun!"],
        constraints: {
          slot_first_token_syntax_must_include_any: { quantity: ["numeral"] },
          slot_surface_in: { classifier: AB45_CLASSIFIER_SURFACES }
        },
        role_overrides: {
          quantity: {
            label: "how",
            syntax: "quantity count_value numeral",
            slots: ["quantity"],
            note: "Numeral/quantity before a classifier; learner-facing role is how/quantity, not stative-like."
          },
          classifier: {
            label: "measure_word",
            syntax: "general_classifier classifier",
            slots: ["classifier"],
            note: "Visible classifier/measure word inside a quantified-classifier NP."
          },
          head_noun: {
            slots: ["head_noun", "np", "object", "topic", "subject"],
            note: "Head noun inside a quantified-classifier NP."
          }
        },
        output_slots: ["quantified_classifier_np", "classifier_np", "quantity", "classifier", "head_noun", "np", "object", "topic", "subject"],
        note: "Category-based quantity + classifier + head noun phrase. Numerals remain quantity material, not stative predicates."
      },
      {
        type: "QuantifiedPersonNP",
        label: "NP",
        template: ["quantity!", "head_noun!"],
        constraints: {
          slot_surface_in: { head_noun: ["人", "同事", "學生"] }
        },
        role_overrides: {
          quantity: {
            label: "how",
            syntax: "quantity_degree quantity",
            slots: ["quantity"],
            note: "好多 functions as the quantity component inside a transparent person NP."
          },
          head_noun: {
            label: "who",
            syntax: "person_head_noun head_noun",
            slots: ["head_noun", "np", "subject", "topic"],
            note: "人 is the person head noun inside the quantified subject NP."
          }
        },
        output_slots: ["np", "subject", "topic", "object", "head_noun", "quantity"],
        note: "Category-based quantified person noun phrase: quantity + person head noun, e.g. 好多 + 人."
      },
      {
        type: "OrdinalClassifierNP",
        label: "OrdCL",
        template: ["ordinal_modifier!", "classifier!"],
        constraints: {
          surface_sequence_in: ["第二隻"]
        },
        role_overrides: {
          ordinal_modifier: {
            label: "func",
            syntax: "ordinal_modifier sequence_modifier",
            slots: ["ordinal_modifier"],
            note: "Overt ordinal modifier 第二 in the source-attested headless 第二隻 profile."
          }
        },
        np_subtype: "headless_ordinal_classifier_np",
        output_slots: ["ordinal_classifier_np", "classifier_np", "np", "object", "topic", "ordinal_modifier", "classifier"],
        role_resolution_note: "Source-attested 第二隻 means the second one; the omitted referent remains context-supplied and no noun head is inserted.",
        not_learner_role: "how",
        not_question_role: "how_many",
        not_claims: ["not_headed_np", "not_inserted_nominal_head", "not_general_headless_classifier_productivity"],
        note: "Bounded headless ordinal + classifier NP for published 第二隻 'the second one'; preserves the overt classifier and inserts no hidden noun."
      },
      {
        type: "OrdinalClassifierNP",
        label: "OrdCL",
        template: ["ordinal_modifier!", "classifier!", "head_noun!"],
        role_overrides: {
          ordinal_modifier: {
            label: "func",
            syntax: "ordinal_modifier sequence_modifier",
            slots: ["ordinal_modifier"],
            note: "Ordinal/sequence modifier such as 第二; not a how/how-many question role."
          }
        },
        np_subtype: "ordinal_classifier_np",
        output_slots: ["ordinal_classifier_np", "classifier_np", "modified_np", "np", "object", "topic", "head_noun", "ordinal_modifier", "classifier"],
        role_resolution_note: "第二個-style phrases are ordinal/classifier noun phrases; 第二 does not create a how/how-many learner role.",
        not_learner_role: "how",
        not_question_role: "how_many",
        note: "Category-based ordinal modifier + classifier + head noun phrase, e.g. 第二個故仔. This is an ordinal NP, not a how/how-many question."
      },
      {
        type: "ModifiedNP",
        label: "NP",
        template: ["direct_nominal_wh_determiner!", "head_noun!"],
        template_family: "generative_template",
        constraints: {
          slot_must_not_have_slots: { head_noun: ["wh_determiner", "wh_nominal"] }
        },
        role_overrides: {
          direct_nominal_wh_determiner: {
            label: "what",
            syntax: "wh_determiner direct_nominal_wh_determiner",
            slots: ["direct_nominal_wh_determiner", "wh_determiner"],
            note: "咩 directly modifies the following noun as a what/what-kind-of determiner; it is not the noun head or a sentence-final polar particle here."
          }
        },
        output_slots: ["modified_np", "np", "object", "topic", "head_noun", "modifier", "wh_determiner", "direct_nominal_wh_determiner", "wh_object"],
        np_subtype: "direct_wh_determiner_np",
        role_resolution_note: "The determiner remains learner-visible as what, while the following noun supplies the nominal head and its own who/what/where role.",
        not_claims: ["not_sentence_final_polar_particle", "not_bare_wh_object", "not_classifier_ellipsis", "not_hidden_classifier"],
        note: "Productive direct wh-determiner noun phrase: a lexically licensed direct nominal wh determiner plus a visible noun head, e.g. 咩書 / 咩人 / 咩嘢."
      },
      {
        type: "WhClassifierQuestion",
        label: "WhCL",
        template: ["wh_determiner!", "classifier!", "particle?"],
        note: "Category-based wh-determiner + classifier question fragment."
      },
      {
        type: "QuantityNP",
        label: "QuantityNP",
        template: ["quantity!", "head_noun!"],
        role_overrides: {
          quantity: {
            label: "how",
            syntax: "quantity_degree quantity",
            slots: ["quantity"],
            note: "Quantity expression modifying a visible noun head."
          }
        },
        output_slots: ["quantity_np", "quantity", "head_noun", "np", "object", "topic", "subject"],
        note: "Transparent quantity noun phrase such as 好多嘢: quantity + head noun."
      },
      {
        type: "TimeNP",
        label: "Time",
        template: ["temporal_modifier!", "classifier?", "time_head!"],
        role_overrides: {
          temporal_modifier: {
            label: "when",
            syntax: "temporal_modifier",
            jyutping_by_surface: { "上": "soeng6", "下": "haa6" },
            slots: ["modifier", "time", "temporal_modifier"],
            note: "Temporal modifier inside a transparent time expression. 上 uses the temporal reading soeng6 here while keeping soeng5 for upward motion contexts."
          },
          classifier: {
            label: "measure_word",
            syntax: "general_classifier",
            slots: ["classifier"],
            note: "Classifier inside a transparent time expression."
          },
          time_head: {
            label: "when",
            syntax: "time_head temporal",
            slots: ["time", "time_head"],
            note: "Time head inside a transparent time expression."
          }
        },
        note: "Category-based transparent time noun phrase; exposes modifier/classifier/head roles instead of hiding them inside an atomic time token."
      },
      {
        type: "AssociativeNP",
        label: "NP",
        template: ["nominal_modifier!", "nominal_linker!", "head_noun!"],
        role_overrides: {
          nominal_linker: {
            label: "particle",
            syntax: "nominal_linker associative_linker",
            slots: ["nominal_linker"],
            note: "Nominal linker inside a transparent associative noun phrase."
          }
        },
        output_slots: ["associative_np", "modified_np", "np", "object", "topic", "head_noun", "modifier", "nominal_modifier", "nominal_linker"],
        note: "Category-based associative noun phrase: nominal modifier/content NP + 嘅 + head noun, e.g. 音樂嘅嘢. Uses nominal_modifier so noun modifiers are not blocked by stative/degree modifier checks."
      },
      {
        type: "ModifierNP",
        label: "NP",
        template: ["modifier!", "head_noun!"],
        note: "Category-based modifier + head noun phrase."
      },
      {
        type: "NominalHeadSpan",
        label: "NP",
        template: ["head_noun!"],
        note: "Internal neutral nominal-head span. It preserves overt nominal material without independently licensing definiteness, ellipsis, argument role, or standalone utterance status."
      },
      {
        type: "ModifiedNP",
        label: "NP",
        template: ["demonstrative!", "classifier!", "verb_modifier!", "nominal_linker!", "modifier!", "head_noun!"],
        template_family: "generative_template",
        output_slots: ["modified_np", "np", "object", "topic", "demonstrative", "classifier", "verb_modifier", "modifier", "nominal_linker", "head_noun"],
        note: "Transparent demonstrative + classifier + verbal modifier + linker + nominal modifier + head noun. Preserves every overt component and remains outside AB15."
      },
      {
        type: "ModifiedNP",
        label: "NP",
        template: ["demonstrative!", "classifier!", "modifier!", "nominal_linker!", "modifier!", "head_noun!"],
        template_family: "generative_template",
        output_slots: ["modified_np", "np", "object", "topic", "demonstrative", "classifier", "modifier", "nominal_linker", "head_noun"],
        note: "Transparent demonstrative + classifier + modifier + linker + nominal modifier + head noun. Preserves every overt component and remains outside AB15."
      },
      {
        type: "ModifiedNP",
        label: "NP",
        template: ["demonstrative!", "classifier!", "verb_modifier!", "nominal_linker!", "head_noun!"],
        template_family: "generative_template",
        output_slots: ["modified_np", "np", "object", "topic", "demonstrative", "classifier", "verb_modifier", "nominal_linker", "head_noun"],
        note: "Transparent demonstrative + classifier + verbal modifier + linker + head noun. Preserves every overt component and remains outside AB15."
      },
      {
        type: "ModifiedNP",
        label: "NP",
        template: ["demonstrative!", "classifier!", "modifier!", "nominal_linker!", "head_noun!"],
        template_family: "generative_template",
        output_slots: ["modified_np", "np", "object", "topic", "demonstrative", "classifier", "modifier", "nominal_linker", "head_noun"],
        note: "Transparent demonstrative + classifier + modifier + linker + head noun. Preserves every overt component and remains outside AB15."
      },
      {
        type: "ModifiedNP",
        label: "NP",
        template: ["demonstrative?", "classifier?", "modifier?", "verb_modifier?", "nominal_linker?", "modifier?", "head_noun!"],
        role_overrides: {
          nominal_linker: {
            label: "particle",
            syntax: "nominal_linker associative_linker",
            slots: ["nominal_linker"],
            note: "Nominal linker inside a transparent modified/associative noun phrase."
          }
        },
        note: "Category-based modified noun phrase; no memorized full surface string."
      }
    ];
  }
});

// src/runtime-resources/grammar/slot-aliases.js
var require_slot_aliases = __commonJS({
  "src/runtime-resources/grammar/slot-aliases.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      demonstrative: ["specifier", "determiner"],
      wh_determiner: ["wh_specifier", "specifier"],
      head_noun: ["noun_head"],
      time_head: ["time"],
      verb_modifier: ["verb_modifier"],
      ordinal_modifier: ["ordinal_modifier"],
      nominal_modifier: ["np", "topic", "object", "head_noun"],
      degree_manner_head: ["degree_manner_head"],
      degree_particle: ["degree_particle"],
      return_motion_verb: ["return_motion_verb", "movement_verb"],
      m4_negator: ["m4_negator", "negator"],
      topic_or_object: ["topic", "object", "np", "location", "head_noun"],
      opinion_topic: ["topic", "object", "np", "location", "head_noun"],
      vp: ["vp", "action_vp", "main_verb", "movement_verb", "directional_motion_vp", "negated_directional_motion_vp", "predicate", "productive_vo", "experiential_vp", "delimited_vp"],
      predicate: ["predicate", "vp", "stative_predicate", "main_verb", "action_vp", "directional_motion_vp", "negated_directional_motion_vp", "productive_vo"],
      person_np: ["subject", "stance_holder", "co_participant", "recipient"],
      cognition_predicate: ["stance_predicate"],
      stance_marker: ["stance_predicate", "seeming_marker"],
      preference_predicate: ["stance_predicate", "preference_predicate"],
      reported_content: ["comment_predicate", "stative_predicate", "predicate", "vp", "topic_or_object", "np"],
      locative_phrase: ["locative_phrase", "location"],
      comment_predicate: ["comment_predicate", "stative_predicate", "lexicalized_stative_predicate", "negative_lexicalized_stative_predicate", "negated_lexicalized_stative", "negative_lexicalized_stative", "degree_modified_lexical_stative", "degree_stative_predicate", "ordinary_degree_stative_predicate", "ordinary_stative_predicate", "comment"],
      scheduling_quality: ["degree", "stative_predicate", "how"],
      approximation: ["approximation", "how"],
      quantity: ["quantity"],
      scalar_dimension_predicate: ["scalar_dimension_predicate", "ordinary_stative_predicate", "stative_predicate", "comment_predicate"],
      discourse_marker: ["discourse_marker", "focus_adverb"],
      modal: ["modal", "desiderative_modal"]
    };
  }
});

// src/parser/tokenization/punctuation.js
var require_punctuation = __commonJS({
  "src/parser/tokenization/punctuation.js"(exports2, module2) {
    "use strict";
    var PUNCT_RE2 = /^[\s\u3000，。！？、；：,.!?;:]+/u;
    module2.exports = PUNCT_RE2;
  }
});

// src/runtime-resources/normalization/simplified-to-traditional.js
var require_simplified_to_traditional = __commonJS({
  "src/runtime-resources/normalization/simplified-to-traditional.js"(exports2, module2) {
    "use strict";
    var HIGH_CONFIDENCE_SIMPLIFIED_TO_TRADITIONAL = {
      "书": "書",
      "饮": "飲",
      "饭": "飯",
      "听": "聽",
      "说": "說",
      "买": "買",
      "写": "寫",
      "还": "還",
      "个": "個",
      "过": "過",
      "门": "門",
      "车": "車",
      "风": "風",
      "后": "後",
      "开": "開",
      "来": "來",
      "边": "邊",
      "间": "間",
      "东": "東",
      "话": "話",
      "语": "語",
      "学": "學",
      "习": "習",
      "妈": "媽",
      "爷": "爺",
      "奶": "奶",
      // v0.5.104 reviewed high-confidence map hardening.
      // These are one-to-one character-form normalizations used by existing
      // accepted grammar paths. Do not add Cantonese lexical repairs here.
      "讲": "講",
      "紧": "緊",
      "笔": "筆",
      "环": "環",
      "厅": "廳",
      "们": "們",
      "对": "對",
      "点": "點",
      "见": "見",
      "长": "長",
      "气": "氣",
      "声": "聲"
    };
    module2.exports = HIGH_CONFIDENCE_SIMPLIFIED_TO_TRADITIONAL;
  }
});

// src/runtime-resources/normalization/folded-pinyin-repairs.js
var require_folded_pinyin_repairs = __commonJS({
  "src/runtime-resources/normalization/folded-pinyin-repairs.js"(exports2, module2) {
    "use strict";
    var FOLDED_PINYIN_FALLOUT_CANTONESE_LEXICAL_REPAIRS = {
      "给": {
        normalized: "畀",
        confidence: "high",
        source: "pinyin_simplified_typing_fallout",
        note: "Pinyin Simplified 给 is a common input fallback for Cantonese 畀; parser shadow may use 畀 while raw display stays 给."
      },
      "没": {
        normalized: "冇",
        confidence: "high",
        source: "pinyin_simplified_typing_fallout",
        note: "Pinyin Simplified 没 is a common input fallback for Cantonese 冇; parser shadow may use 冇 while raw display stays 没."
      }
    };
    module2.exports = FOLDED_PINYIN_FALLOUT_CANTONESE_LEXICAL_REPAIRS;
  }
});

// src/parser/normalization/normalize-input.js
var require_normalize_input = __commonJS({
  "src/parser/normalization/normalize-input.js"(exports2, module2) {
    "use strict";
    var HIGH_CONFIDENCE_SIMPLIFIED_TO_TRADITIONAL = require_simplified_to_traditional();
    var FOLDED_PINYIN_FALLOUT_CANTONESE_LEXICAL_REPAIRS = require_folded_pinyin_repairs();
    function normalizeSurface2(text) {
      return String(text || "").replace(/[\s\u3000，。！？、；：,.!?;:]/gu, "");
    }
    function foldedPinyinFalloutRepair2(rawChars, index) {
      const raw = rawChars[index];
      const repair = FOLDED_PINYIN_FALLOUT_CANTONESE_LEXICAL_REPAIRS[raw] || null;
      if (!repair) return null;
      if (raw === "没" && rawChars[index + 1] === "有") return null;
      return repair;
    }
    function normalizeInputForParser2(rawSource) {
      const rawText = String(rawSource || "");
      const rawChars = Array.from(rawText);
      const shadowChars = [];
      const normalizationTrace = [];
      const reviewSuggestions = [];
      rawChars.forEach((raw, index) => {
        const foldedRepair = foldedPinyinFalloutRepair2(rawChars, index);
        const traditional = HIGH_CONFIDENCE_SIMPLIFIED_TO_TRADITIONAL[raw];
        const normalized = foldedRepair ? foldedRepair.normalized : traditional || raw;
        shadowChars.push(normalized);
        if (normalized !== raw) {
          const isFoldedRepair = Boolean(foldedRepair);
          normalizationTrace.push({
            index,
            raw,
            normalized,
            type: isFoldedRepair ? "pinyin_fallout_cantonese_lexical_repair" : "simplified_to_traditional",
            confidence: isFoldedRepair ? foldedRepair.confidence : "high",
            source: isFoldedRepair ? foldedRepair.source : "character_form_map",
            status: isFoldedRepair ? "folded_parser_shadow_only" : "applied_parser_shadow_only",
            applied_to_parser_shadow: true,
            learner_display_replaced: false,
            one_to_one_character_mapping: true,
            note: isFoldedRepair ? foldedRepair.note : "High-confidence Simplified-to-Traditional parser-shadow character-form normalization."
          });
        }
      });
      const parserShadowSource = shadowChars.join("");
      return {
        raw_source: rawText,
        parser_shadow_source: parserShadowSource,
        normalization_trace: normalizationTrace,
        review_suggestions: reviewSuggestions,
        raw_first_display: true
      };
    }
    function nodeParserSurface2(node) {
      if (!node) return "";
      if (node.kind === "token") return node.surface || "";
      if (node.kind === "text") return node.text || "";
      if (node.kind === "construction") return (node.children || []).map(nodeParserSurface2).join("");
      return "";
    }
    function nodeDisplaySurface2(node) {
      if (!node) return "";
      if (node.kind === "token") return node.display_surface || node.surface || "";
      if (node.kind === "text") return node.display_text || node.text || "";
      if (node.kind === "construction") return (node.children || []).map(nodeDisplaySurface2).join("");
      return "";
    }
    function annotateRawDisplaySurfaces2(nodes, rawSource, parserShadowSource) {
      const rawChars = Array.from(String(rawSource || ""));
      const shadowChars = Array.from(String(parserShadowSource || ""));
      let cursor = 0;
      const annotate = (node) => {
        if (!node) return;
        if (node.kind === "construction") {
          for (const child of node.children || []) annotate(child);
          const displaySurface = nodeDisplaySurface2(node);
          const parserSurface2 = nodeParserSurface2(node);
          if (displaySurface && displaySurface !== parserSurface2) node.display_surface = displaySurface;
          return;
        }
        const parserSurface = nodeParserSurface2(node);
        const length = Array.from(parserSurface).length;
        const rawSlice = rawChars.slice(cursor, cursor + length).join("");
        const shadowSlice = shadowChars.slice(cursor, cursor + length).join("");
        if (length) cursor += length;
        if (node.kind === "token") {
          if (rawSlice && rawSlice !== node.surface) {
            node.display_surface = rawSlice;
            node.parser_surface = node.surface;
          }
          if (shadowSlice && shadowSlice !== parserSurface) node.shadow_alignment_warning = shadowSlice;
        } else if (node.kind === "text") {
          if (rawSlice && rawSlice !== node.text) node.display_text = rawSlice;
          if (shadowSlice && shadowSlice !== parserSurface) node.shadow_alignment_warning = shadowSlice;
        }
      };
      for (const node of nodes || []) annotate(node);
      return nodes;
    }
    function inputNormalizationHasFindings2(inputNormalization) {
      return !!(inputNormalization && ((inputNormalization.normalization_trace || []).length || (inputNormalization.review_suggestions || []).length || inputNormalization.raw_source !== inputNormalization.parser_shadow_source));
    }
    module2.exports = {
      normalizeSurface: normalizeSurface2,
      foldedPinyinFalloutRepair: foldedPinyinFalloutRepair2,
      normalizeInputForParser: normalizeInputForParser2,
      nodeParserSurface: nodeParserSurface2,
      nodeDisplaySurface: nodeDisplaySurface2,
      annotateRawDisplaySurfaces: annotateRawDisplaySurfaces2,
      inputNormalizationHasFindings: inputNormalizationHasFindings2,
      HIGH_CONFIDENCE_SIMPLIFIED_TO_TRADITIONAL,
      FOLDED_PINYIN_FALLOUT_CANTONESE_LEXICAL_REPAIRS
    };
  }
});

// src/parser/features/token-features.js
var require_token_features = __commonJS({
  "src/parser/features/token-features.js"(exports2, module2) {
    "use strict";
    var SLOT_GENERATION_RULES2 = require_slot_generation_rules();
    module2.exports = function createTokenFeaturePrimitives(dependencies = {}) {
      const {
        normalizeLearnerLabel: normalizeLearnerLabel2,
        cleanSlots: cleanSlots2
      } = dependencies;
      function hasAny2(value, wanted) {
        const values = Array.isArray(value) ? value : [value];
        return values.some((item) => wanted.includes(item));
      }
      function stringIncludesAny2(value, needles) {
        const text = String(value || "");
        return needles.some((needle) => needle && text.includes(needle));
      }
      function contextualRoleAffordances2(row = {}) {
        const slots = new Set(row.slots || []);
        const syntax = String(row.syntax || "");
        const role = row.role || row.label || "";
        const out = [];
        const add = (candidate) => {
          if (!candidate || !candidate.role) return;
          const key = `${candidate.role}||${candidate.slot || ""}||${candidate.source || ""}`;
          if (out.some((item) => `${item.role}||${item.slot || ""}||${item.source || ""}` === key)) return;
          out.push(candidate);
        };
        if (role && role !== "neutral") {
          add({
            role,
            source: "lexical_default",
            active_before_construction_wrapping: true,
            note: "Default learner role from the lexical entry before any construction-level contextual override."
          });
        }
        if (slots.has("time") || slots.has("time_head") || slots.has("temporal_modifier") || syntax.includes("temporal")) {
          add({
            role: "when",
            slot: slots.has("temporal_modifier") ? "temporal_modifier" : slots.has("time_head") ? "time_head" : "time",
            source: "time_affordance",
            active_before_construction_wrapping: role === "when",
            note: "Time/temporal affordance retained for phrases such as 上個禮拜 / 上次."
          });
        }
        if (slots.has("movement_direction") || slots.has("return_motion_verb") || slots.has("deictic_motion_marker") || slots.has("movement_verb") || syntax.includes("movement_direction")) {
          add({
            role: "doing",
            slot: slots.has("movement_direction") ? "movement_direction" : slots.has("return_motion_verb") ? "return_motion_verb" : slots.has("deictic_motion_marker") ? "deictic_motion_marker" : "movement_verb",
            source: "motion_affordance",
            active_before_construction_wrapping: role === "doing",
            note: "Motion affordance retained for directional-motion contexts such as 上嚟 / 返上嚟."
          });
        }
        if (slots.has("degree_manner_head") || syntax.includes("degree_manner_head")) {
          add({
            role: "how",
            slot: "degree_manner_head",
            source: "degree_manner_affordance",
            active_before_construction_wrapping: role === "how",
            note: "Degree/manner affordance retained for stative/adjective or manner + 啲 contexts."
          });
        }
        if (slots.has("object") || slots.has("head_noun") || slots.has("np")) {
          add({
            role: "what",
            slot: slots.has("object") ? "object" : slots.has("head_noun") ? "head_noun" : "np",
            source: "nominal_affordance",
            active_before_construction_wrapping: role === "what",
            note: "Nominal/object affordance retained for NP or object contexts."
          });
        }
        return out;
      }
      function inferTokenFeatures2(surface, entry = {}, overrides = {}) {
        const rawLabel = overrides.label || entry.label || "neutral";
        const syntax = overrides.syntax || entry.syntax || "lexical_candidate";
        const label = normalizeLearnerLabel2(rawLabel, surface, syntax);
        const syntaxParts = new Set(String(syntax).split(/[\s_]+/).filter(Boolean));
        const hasSyntaxPart = (part) => syntaxParts.has(part);
        const features = {
          surface,
          label,
          syntax,
          pos: overrides.pos || entry.pos || "",
          semantic: Array.isArray(entry.semantic) ? [...entry.semantic] : [],
          verb_class: Array.isArray(entry.verb_class) ? [...entry.verb_class] : [],
          particle_class: entry.particle_class || ""
        };
        if (!features.pos) {
          if (label === "who" || syntax.includes("person") || syntax.includes("pronoun")) features.pos = "np";
          else if (label === "where" || syntax.includes("place")) features.pos = "np";
          else if (label === "what" || syntax.includes("noun") || syntax.includes("np")) features.pos = "noun";
          else if (label === "doing") features.pos = "verb";
          else if (label === "stance" || syntax.includes("cognition")) features.pos = "verb";
          else if (label === "like" || syntax.includes("stative")) features.pos = "stative";
          else if (label === "measure_word") features.pos = "classifier";
          else if (label === "particle") features.pos = "particle";
          else if (label === "func") features.pos = "function";
          else if (label === "when") features.pos = "time";
          else if (label === "how" || label === "why") features.pos = "adverbial";
        }
        if (label === "where" || syntax.includes("place")) features.semantic.push("place");
        const foodNounSurfaces = ["飯", "薄餅", "意粉", "食物", "菜式"];
        const drinkLiquidSurfaces = ["水", "茶", "咖啡", "奶", "湯"];
        if (syntax.includes("food") || foodNounSurfaces.includes(surface)) features.semantic.push("food");
        if (syntax.includes("drink") || syntax.includes("liquid") || drinkLiquidSurfaces.includes(surface)) features.semantic.push("drink", "liquid");
        if (surface === "水" || syntax.includes("water")) features.semantic.push("water");
        if (syntax.includes("beverage") || ["茶", "咖啡", "奶"].includes(surface)) features.semantic.push("beverage");
        if (syntax.includes("price") || ["價", "價錢", "價位", "錢", "蚊"].includes(surface)) features.semantic.push("price");
        if (syntax.includes("abstract") || ["興趣", "問題", "消息", "主意"].includes(surface)) features.semantic.push("abstract");
        if (syntax.includes("cognition") || ["覺得", "諗", "知", "記得", "唔知"].includes(surface)) features.verb_class.push("cognition");
        if (label === "stance" || hasSyntaxPart("opinion") || hasSyntaxPart("stance") || ["覺得", "諗住", "記得", "唔知"].includes(surface)) features.verb_class.push("stance");
        if (["去", "返", "嚟", "落", "上"].includes(surface) || syntax.includes("movement_direction")) features.verb_class.push("movement");
        if (["講", "話"].includes(surface)) features.verb_class.push("speech");
        if (["鍾意"].includes(surface)) features.verb_class.push("preference");
        if (["好似"].includes(surface)) features.verb_class.push("seeming");
        features.semantic = [...new Set(features.semantic)];
        features.verb_class = [...new Set(features.verb_class)];
        return features;
      }
      function compactFeatureSummary2(features = {}) {
        return {
          pos: features.pos || "",
          label: features.label || "",
          semantic: features.semantic || [],
          verb_class: features.verb_class || [],
          particle_class: features.particle_class || ""
        };
      }
      function featureList2(...items) {
        return [...new Set(items.flat().filter(Boolean))].sort();
      }
      function syntaxHas2(syntax, value) {
        return String(syntax || "").includes(value);
      }
      function featureBundleFor2(surface, entry = {}, features = {}, slots = []) {
        const syntax = entry.syntax || features.syntax || "";
        const label = entry.label || features.label || "";
        const slotSet = new Set(slots || []);
        const semantic = new Set(features.semantic || []);
        const verbClass = new Set(features.verb_class || []);
        const isLexicalizedStative3 = syntaxHas2(syntax, "lexicalized_stative") && !syntaxHas2(syntax, "negative_lexicalized_stative");
        const isNegativeLexicalizedStative3 = syntaxHas2(syntax, "negative_lexicalized_stative");
        const syntaxSuggestsOrdinaryStative = label === "like" && syntaxHas2(syntax, "stative") && !isLexicalizedStative3 && !isNegativeLexicalizedStative3 && !syntaxHas2(syntax, "sensory_stative_head") && !syntaxHas2(syntax, "degree_stative_predicate");
        const isOrdinaryStative3 = slotSet.has("ordinary_stative_predicate") || syntaxSuggestsOrdinaryStative;
        const isStative = slotSet.has("stative_predicate") || isLexicalizedStative3 || isNegativeLexicalizedStative3 || isOrdinaryStative3;
        const isAction = slotSet.has("action_verb") || slotSet.has("main_verb");
        const isSpeech = slotSet.has("speech_verb") || verbClass.has("speech") || ["話", "講"].includes(surface);
        const isReportative = slotSet.has("reportative_source") || ["話", "聽講"].includes(surface);
        const isStance = (slotSet.has("stance_predicate") || verbClass.has("stance")) && !["話", "聽講"].includes(surface);
        const isCognition = slotSet.has("cognition_predicate") || verbClass.has("cognition");
        const isPhase4CognitionPromotion = syntaxHas2(syntax, "phase4_cognition_promotion");
        const isPhase4NegativeCognitionFragment = isPhase4CognitionPromotion && slotSet.has("negative_cognition_fragment");
        const isPhase4CognitionContentPredicate = isPhase4CognitionPromotion && slotSet.has("non_stance_cognition_predicate");
        const isPhase4CognitionStatement = isPhase4CognitionPromotion && slotSet.has("cognition_statement_clause");
        const isPhase4DesiderativePromotion = syntaxHas2(syntax, "phase4_desiderative_promotion");
        const isPhase4DesiderativeANotAQuestion = isPhase4DesiderativePromotion && slotSet.has("desiderative_a_not_a_question");
        const isPhase4OpinionStancePromotion = syntaxHas2(syntax, "phase4_opinion_stance_promotion");
        const isPhase4OpinionStanceFrame = isPhase4OpinionStancePromotion && slotSet.has("opinion_stance_frame");
        const isPhase4ReportedSpeechPromotion = syntaxHas2(syntax, "phase4_reported_speech_promotion");
        const isPhase4ReportedSpeechFrame = isPhase4ReportedSpeechPromotion && slotSet.has("reported_speech");
        const isPhase4PermissionPromotion = syntaxHas2(syntax, "phase4_permission_promotion");
        const isPhase4PermissionANotAQuestion = isPhase4PermissionPromotion && slotSet.has("permission_a_not_a_question");
        const isDegree = slotSet.has("degree");
        const isParticle2 = label === "particle" || features.pos === "particle";
        const isFunction = label === "func" || label === "measure_word" || features.pos === "function" || features.pos === "quantifier";
        const isNominal = ["who", "what", "where"].includes(label) || ["noun", "np"].includes(features.pos);
        const domains = [];
        if (syntaxHas2(syntax, "sensory_evaluation")) domains.push("sensory_evaluation");
        if (syntaxHas2(syntax, "food_evaluation")) domains.push("food_evaluation");
        else if ((syntaxHas2(syntax, "food") || semantic.has("food")) && isNominal) domains.push("food_item", "edible_item");
        else if (syntaxHas2(syntax, "food")) domains.push("food_evaluation");
        if (syntaxHas2(syntax, "drink") || syntaxHas2(syntax, "drink_evaluation")) domains.push("drink_evaluation");
        if (syntaxHas2(syntax, "sound") || syntaxHas2(syntax, "sound_evaluation")) domains.push("sound_evaluation");
        if (syntaxHas2(syntax, "visual") || syntaxHas2(syntax, "visual_evaluation")) domains.push("visual_evaluation");
        if (syntaxHas2(syntax, "taste") || syntaxHas2(syntax, "taste_evaluation")) domains.push("taste_evaluation");
        if (syntaxHas2(syntax, "distance") || ["遠", "近"].includes(surface)) domains.push("distance_property");
        if (syntaxHas2(syntax, "price") || ["貴", "平", "價", "價錢", "價位", "錢", "蚊"].includes(surface)) domains.push("price_property");
        if (syntaxHas2(syntax, "emotion") || ["開心", "難過"].includes(surface)) domains.push("emotion");
        if (isCognition || ["覺得", "諗", "知", "記得", "唔知"].includes(surface)) domains.push("cognition");
        if (isSpeech) domains.push("speech");
        if (verbClass.has("movement") || ["去", "返", "嚟", "落", "上"].includes(surface)) domains.push("motion");
        if (["食", "飲"].includes(surface)) domains.push("consumption");
        if (syntaxHas2(syntax, "story_np") || syntaxHas2(syntax, "information_content") || semantic.has("information_content")) domains.push("information_content", "readable_content");
        if (syntaxHas2(syntax, "liquid") || semantic.has("liquid") || semantic.has("water") || semantic.has("beverage") || semantic.has("drink")) domains.push("liquid", "drinkable_item");
        if (label === "where" || syntaxHas2(syntax, "place") || semantic.has("place")) domains.push("place");
        let lexicalizationType = "none";
        if (isNegativeLexicalizedStative3) lexicalizationType = "negative_lexicalized_stative";
        else if (isLexicalizedStative3) lexicalizationType = "lexicalized_stative";
        else if (entry.review === "protected_formula" || syntaxHas2(syntax, "formula")) lexicalizationType = "protected_formula";
        else if (syntaxHas2(syntax, "productive_vo")) lexicalizationType = "productive_vo";
        const predicateSubtypes = [];
        if (isAction && !isStative) predicateSubtypes.push("action_verb");
        if (isOrdinaryStative3) predicateSubtypes.push("ordinary_stative");
        if (isLexicalizedStative3) predicateSubtypes.push("lexicalized_stative");
        if (isNegativeLexicalizedStative3) predicateSubtypes.push("negative_lexicalized_stative");
        if (isSpeech) predicateSubtypes.push("speech_predicate");
        if (isReportative) predicateSubtypes.push("reportative_source");
        if (isStance) predicateSubtypes.push("stance_predicate");
        if (isCognition) predicateSubtypes.push("cognition_predicate");
        if (verbClass.has("movement")) predicateSubtypes.push("motion_predicate");
        if (slotSet.has("desiderative_modal") || syntaxHas2(syntax, "modal_desiderative")) predicateSubtypes.push("desiderative_modal");
        if (slotSet.has("permission_modal") || slotSet.has("permission_a_not_a_modal") || syntaxHas2(syntax, "modal_permission")) predicateSubtypes.push("permission_modal");
        if (slotSet.has("prohibitive_marker") || syntaxHas2(syntax, "prohibitive_marker")) predicateSubtypes.push("prohibitive_marker");
        let syntacticCategory = "UNKNOWN";
        if (isParticle2) syntacticCategory = "PARTICLE";
        else if (isFunction || isDegree) syntacticCategory = "FUNCTION";
        else if (isNominal && !isStative) syntacticCategory = "N";
        else if (label === "doing" || label === "stance" || label === "like" || isStative || isSpeech || isCognition) syntacticCategory = "V";
        let morphologicalCategory = "unknown";
        if (syntacticCategory === "V") morphologicalCategory = "verb_like";
        else if (syntacticCategory === "N") morphologicalCategory = "nominal_like";
        else if (syntacticCategory === "PARTICLE") morphologicalCategory = "particle_like";
        else if (syntacticCategory === "FUNCTION") morphologicalCategory = "function_like";
        let gradability = "unknown";
        if (syntaxHas2(syntax, "degreeable") || isStative || isOrdinaryStative3) gradability = "gradable";
        else if (isAction || isSpeech || isCognition || isDegree || isFunction || isParticle2) gradability = "non_gradable";
        let dynamicity = "unknown";
        if (surface === "覺得") dynamicity = "mixed";
        else if (isStative || isNegativeLexicalizedStative3 || isLexicalizedStative3 || isOrdinaryStative3) dynamicity = "non_dynamic";
        else if (isAction || isSpeech || surface === "諗") dynamicity = "dynamic";
        else if (isCognition) dynamicity = "contextual";
        let temporalStability = "unknown";
        if (["遠", "近", "高", "矮", "大", "細"].includes(surface)) temporalStability = "permanent";
        else if (isAction || isSpeech || surface === "諗") temporalStability = "transient";
        else if (isStative || isCognition || surface === "覺得") temporalStability = "contextual";
        let polarityProfile = "neutral";
        if (isNegativeLexicalizedStative3 || syntaxHas2(syntax, "negative")) polarityProfile = "negative";
        else if (isLexicalizedStative3 || syntaxHas2(syntax, "positive")) polarityProfile = "positive";
        else if (["貴", "遠"].includes(surface)) polarityProfile = "contextual";
        const canHeadComment3 = slotSet.has("comment_predicate") || isStative || isOrdinaryStative3;
        const canTakeDegreeModifier = syntaxHas2(syntax, "degreeable") || isStative || isOrdinaryStative3;
        const canHeadProductiveVO = slotSet.has("action_verb") && syntaxHas2(syntax, "transitive_affordance");
        const canBeM4Negated = canHeadComment3 || label === "doing" ? label === "doing" && !canHeadComment3 ? "contextual" : true : "unknown";
        let confidence = "low";
        const sources = ["runtime_inference"];
        if (["好食", "難食", "遠", "食", "覺得", "諗", "知"].includes(surface)) {
          confidence = ["遠"].includes(surface) ? "medium" : "high";
          sources.unshift("manual_gold");
        }
        if (["好食", "好飲", "好睇", "好聽", "好味", "難食", "難飲", "難睇", "難聽"].includes(surface)) {
          if (!sources.includes("words_hk")) sources.push("words_hk");
          if (!sources.includes("jyutdictionary")) sources.push("jyutdictionary");
          confidence = "high";
        }
        if (["話", "聽講"].includes(surface)) {
          confidence = "medium";
          sources.unshift("manual_gold");
          sources.push("research_generalization");
        }
        const shorthandParts = [];
        if (gradability === "gradable") shorthandParts.push("+gradable");
        if (gradability === "non_gradable") shorthandParts.push("-gradable");
        if (dynamicity === "dynamic") shorthandParts.push("+dynamic");
        if (dynamicity === "non_dynamic") shorthandParts.push("-dynamic");
        if (isLexicalizedStative3) shorthandParts.push("+lexicalized_stative");
        if (isNegativeLexicalizedStative3) shorthandParts.push("+negative_lexicalized_stative");
        if (isOrdinaryStative3) shorthandParts.push("+ordinary_stative");
        for (const subtype of predicateSubtypes) shorthandParts.push(`+${subtype}`);
        for (const domain of domains) shorthandParts.push(`+${domain}`);
        const parserActiveUses = [];
        if (lexicalizationType === "lexicalized_stative") parserActiveUses.push("lexicalization_type:lexicalized_stative");
        if (lexicalizationType === "negative_lexicalized_stative") parserActiveUses.push("lexicalization_type:negative_lexicalized_stative");
        if (predicateSubtypes.includes("ordinary_stative")) parserActiveUses.push("predicate_subtype:ordinary_stative");
        if (isStative && gradability === "gradable") parserActiveUses.push("core_dimensions:gradability");
        if (isStative && canHeadComment3) parserActiveUses.push("construction_affordances:can_head_comment");
        if (isPhase4CognitionPromotion && isCognition) parserActiveUses.push("predicate_subtype:cognition_predicate");
        if (isPhase4CognitionContentPredicate && isCognition) parserActiveUses.push("construction_affordances:can_take_content_clause");
        if (isPhase4NegativeCognitionFragment && isCognition) parserActiveUses.push("construction_affordances:can_stand_as_negative_cognition_fragment");
        if (isPhase4CognitionStatement && isCognition) parserActiveUses.push("construction_affordances:can_stand_as_cognition_statement");
        if (isPhase4DesiderativePromotion && predicateSubtypes.includes("desiderative_modal")) parserActiveUses.push("predicate_subtype:desiderative_modal");
        if (isPhase4DesiderativeANotAQuestion && predicateSubtypes.includes("desiderative_modal")) parserActiveUses.push("construction_affordances:can_form_desiderative_a_not_a_question");
        if (isPhase4OpinionStancePromotion && predicateSubtypes.includes("stance_predicate")) parserActiveUses.push("predicate_subtype:stance_predicate");
        if (isPhase4OpinionStanceFrame && predicateSubtypes.includes("stance_predicate")) parserActiveUses.push("construction_affordances:can_head_opinion_frame");
        if (isPhase4ReportedSpeechPromotion && predicateSubtypes.includes("speech_predicate")) parserActiveUses.push("predicate_subtype:speech_predicate");
        if (isPhase4ReportedSpeechFrame && predicateSubtypes.includes("speech_predicate")) parserActiveUses.push("construction_affordances:can_introduce_reported_content");
        if (isPhase4PermissionPromotion && predicateSubtypes.includes("permission_modal")) parserActiveUses.push("predicate_subtype:permission_modal");
        if (isPhase4PermissionANotAQuestion && predicateSubtypes.includes("permission_modal")) parserActiveUses.push("construction_affordances:can_form_permission_a_not_a_question");
        if (canHeadProductiveVO) parserActiveUses.push("construction_affordances:can_head_productive_vo");
        const topicChainParserActive = parserActiveUses.includes("construction_affordances:can_head_productive_vo");
        const phase4CognitionParserActive = parserActiveUses.some((use) => use.includes("cognition_predicate") || use.includes("can_take_content_clause"));
        const phase4DesiderativeParserActive = parserActiveUses.some((use) => use.includes("desiderative_modal") || use.includes("desiderative_a_not_a"));
        const phase4OpinionStanceParserActive = parserActiveUses.some((use) => use.includes("stance_predicate") || use.includes("can_head_opinion_frame"));
        const phase4ReportedSpeechParserActive = parserActiveUses.some((use) => use.includes("speech_predicate") || use.includes("can_introduce_reported_content"));
        const phase4PermissionParserActive = parserActiveUses.some((use) => use.includes("permission_modal") || use.includes("can_form_permission_a_not_a_question"));
        const parserBehavior = phase4CognitionParserActive ? "phase4_controlled_cognition_logic_uses_feature_predicates" : phase4DesiderativeParserActive ? "phase4_controlled_desiderative_a_not_a_logic_uses_feature_predicates" : phase4OpinionStanceParserActive ? "phase4_controlled_opinion_stance_logic_uses_feature_predicates" : phase4ReportedSpeechParserActive ? "phase4_controlled_reported_speech_logic_uses_feature_predicates" : phase4PermissionParserActive ? "phase4_controlled_permission_a_not_a_logic_uses_feature_predicates" : topicChainParserActive ? "a1_topic_chain_null_object_logic_uses_object_selecting_affordance" : parserActiveUses.length ? "stative_logic_uses_feature_predicates" : "unchanged";
        const bundleStatus = phase4CognitionParserActive ? "derived_runtime_helper_phase_4_controlled_cognition_parser_active" : phase4DesiderativeParserActive ? "derived_runtime_helper_phase_4_controlled_desiderative_parser_active" : phase4OpinionStanceParserActive ? "derived_runtime_helper_phase_4_controlled_opinion_stance_parser_active" : phase4ReportedSpeechParserActive ? "derived_runtime_helper_phase_4_controlled_reported_speech_parser_active" : phase4PermissionParserActive ? "derived_runtime_helper_phase_4_controlled_permission_parser_active" : topicChainParserActive ? "derived_runtime_helper_a1_topic_chain_parser_active" : parserActiveUses.length ? "derived_runtime_helper_phase_3_stative_parser_active" : "derived_runtime_helper_phase_3_parser_inactive";
        return {
          status: bundleStatus,
          parser_behavior: parserBehavior,
          core_dimensions: {
            syntactic_category: syntacticCategory,
            morphological_category: morphologicalCategory,
            gradability,
            dynamicity,
            temporal_stability: temporalStability
          },
          parser_features: {
            lexicalization_type: lexicalizationType,
            semantic_domain: featureList2(domains),
            polarity_profile: polarityProfile,
            predicate_subtype: featureList2(predicateSubtypes)
          },
          construction_affordances: {
            can_head_comment: canHeadComment3,
            can_take_degree_modifier: canTakeDegreeModifier,
            can_be_m4_negated: canBeM4Negated,
            can_head_productive_vo: canHeadProductiveVO,
            can_license_following_vp: slotSet.has("prohibitive_marker") || surface === "唔好",
            can_introduce_reported_content: slotSet.has("reportative_source") || ["話", "聽講"].includes(surface),
            can_take_content_clause: isPhase4CognitionContentPredicate && isCognition,
            can_stand_as_negative_cognition_fragment: isPhase4NegativeCognitionFragment && isCognition,
            can_stand_as_cognition_statement: isPhase4CognitionStatement && isCognition,
            can_form_desiderative_a_not_a_question: isPhase4DesiderativeANotAQuestion && predicateSubtypes.includes("desiderative_modal"),
            can_form_permission_a_not_a_question: isPhase4PermissionANotAQuestion && predicateSubtypes.includes("permission_modal"),
            can_take_recipient: ["話", "畀"].includes(surface),
            can_head_opinion_frame: isPhase4OpinionStanceFrame || slotSet.has("stance_predicate") || ["覺得", "認為", "以為", "相信", "懷疑"].includes(surface)
          },
          evidence_controls: {
            feature_confidence: confidence,
            feature_source: featureList2(sources),
            parser_active: Boolean(parserActiveUses.length),
            parser_active_scope: featureList2(parserActiveUses)
          },
          diagnostic_shorthand: featureList2(shorthandParts).join(" ")
        };
      }
      function getFeatureBundle2(nodeOrBundle) {
        if (!nodeOrBundle) return null;
        if (nodeOrBundle.core_dimensions && nodeOrBundle.parser_features) return nodeOrBundle;
        return nodeOrBundle.feature_bundle || nodeOrBundle.trace && nodeOrBundle.trace.feature_bundle || null;
      }
      function getParserFeatures2(nodeOrBundle) {
        const bundle = getFeatureBundle2(nodeOrBundle);
        return bundle && bundle.parser_features || {};
      }
      function getConstructionAffordances2(nodeOrBundle) {
        const bundle = getFeatureBundle2(nodeOrBundle);
        return bundle && bundle.construction_affordances || {};
      }
      function getCoreDimensions2(nodeOrBundle) {
        const bundle = getFeatureBundle2(nodeOrBundle);
        return bundle && bundle.core_dimensions || {};
      }
      function getLexicalizationType2(nodeOrBundle) {
        return getParserFeatures2(nodeOrBundle).lexicalization_type || "none";
      }
      function hasPredicateSubtype2(nodeOrBundle, subtype) {
        const subtypes = getParserFeatures2(nodeOrBundle).predicate_subtype || [];
        return Array.isArray(subtypes) && subtypes.includes(subtype);
      }
      function isLexicalizedStative2(nodeOrBundle) {
        return getLexicalizationType2(nodeOrBundle) === "lexicalized_stative";
      }
      function isNegativeLexicalizedStative2(nodeOrBundle) {
        return getLexicalizationType2(nodeOrBundle) === "negative_lexicalized_stative";
      }
      function isOrdinaryStative2(nodeOrBundle) {
        return hasPredicateSubtype2(nodeOrBundle, "ordinary_stative");
      }
      function isStativePredicateByBundle2(nodeOrBundle) {
        if (isLexicalizedStative2(nodeOrBundle) || isNegativeLexicalizedStative2(nodeOrBundle) || isOrdinaryStative2(nodeOrBundle)) return true;
        const core = getCoreDimensions2(nodeOrBundle);
        const affordances = getConstructionAffordances2(nodeOrBundle);
        return core.syntactic_category === "V" && affordances.can_head_comment === true;
      }
      function isGradablePredicate2(nodeOrBundle) {
        return isStativePredicateByBundle2(nodeOrBundle) && getCoreDimensions2(nodeOrBundle).gradability === "gradable";
      }
      function canHeadComment2(nodeOrBundle) {
        const affordances = getConstructionAffordances2(nodeOrBundle);
        return isStativePredicateByBundle2(nodeOrBundle) && affordances.can_head_comment === true;
      }
      function bundleCanFillStativeSlot2(node, slot) {
        if (!node || node.kind !== "token") return null;
        const bundle = getFeatureBundle2(node);
        if (!bundle) return null;
        switch (slot) {
          case "lexicalized_stative_predicate":
            return isLexicalizedStative2(bundle) || isNegativeLexicalizedStative2(bundle);
          case "negative_lexicalized_stative_predicate":
            return isNegativeLexicalizedStative2(bundle);
          case "negative_lexicalized_stative":
            return isNegativeLexicalizedStative2(bundle);
          case "ordinary_stative_predicate":
          case "ordinary_degree_stative_predicate":
            return isOrdinaryStative2(bundle);
          case "stative_predicate":
            return isStativePredicateByBundle2(bundle);
          case "comment_predicate":
          case "comment":
            return canHeadComment2(bundle);
          case "modifier":
            return isGradablePredicate2(bundle);
          default:
            return null;
        }
      }
      function parserActiveStativeSlotsForBundle2(bundle) {
        const slots = [];
        if (!bundle) return slots;
        if (isNegativeLexicalizedStative2(bundle)) {
          slots.push("negative_lexicalized_stative_predicate", "negative_lexicalized_stative");
        }
        if (isLexicalizedStative2(bundle) || isNegativeLexicalizedStative2(bundle)) {
          slots.push("lexicalized_stative_predicate");
        }
        if (isOrdinaryStative2(bundle)) {
          slots.push("ordinary_stative_predicate");
        }
        if (isStativePredicateByBundle2(bundle)) {
          slots.push("stative_predicate");
        }
        if (isGradablePredicate2(bundle)) {
          slots.push("modifier");
        }
        if (canHeadComment2(bundle)) {
          slots.push("comment_predicate");
        }
        return featureList2(slots);
      }
      function conditionMatches2(features, condition = {}) {
        if (condition.surfaces && condition.surfaces.includes(features.surface)) return true;
        if (condition.labels && condition.labels.includes(features.label)) return true;
        if (condition.pos && condition.pos.includes(features.pos)) return true;
        if (condition.syntaxIncludes && stringIncludesAny2(features.syntax, condition.syntaxIncludes)) return true;
        if (condition.semantic && hasAny2(features.semantic, condition.semantic)) return true;
        if (condition.verbClass && hasAny2(features.verb_class, condition.verbClass)) return true;
        return false;
      }
      function generateTokenSlots2(features) {
        const slots = /* @__PURE__ */ new Set();
        for (const rule of SLOT_GENERATION_RULES2) {
          if (conditionMatches2(features, rule.when)) slots.add(rule.slot);
        }
        if (features.pos === "noun" || features.pos === "np") slots.add("np");
        if (features.pos === "verb") slots.add("predicate");
        if (features.label === "how") slots.add("how");
        if (features.label === "why") slots.add("why");
        if (features.label === "when") slots.add("time");
        if (features.syntax.includes("demonstrative_determiner")) slots.add("demonstrative");
        if (["呢個", "嗰個", "呢啲", "嗰啲"].includes(features.surface)) slots.add("demonstrative_pronoun");
        if (features.syntax.includes("wh_determiner")) slots.add("wh_determiner");
        if (features.syntax.includes("quantity") || features.syntax.includes("numeral")) slots.add("quantity");
        if (features.syntax.includes("di_determiner")) slots.add("di_determiner");
        if (features.syntax.includes("modifier") || features.syntax.includes("noun_modifier")) slots.add("modifier");
        if (features.syntax.includes("weekday") || features.syntax.includes("time_np")) slots.add("time_head");
        if (features.surface === "嘅") slots.add("nominal_linker");
        if (features.surface === "下" || features.surface === "上") slots.add("temporal_modifier");
        if (features.surface === "開" || features.syntax.includes("verb_in_modifier")) slots.add("verb_modifier");
        if (features.surface === "就係" || features.syntax.includes("focus_copula")) slots.add("identification_marker");
        if (features.surface === "未") {
          slots.add("question_marker");
          slots.add("negator");
        }
        if (features.surface === "得" || features.syntax.includes("acceptability_predicate")) {
          slots.add("acceptability_predicate");
        }
        if (features.surface === "唔好" || features.syntax.includes("prohibitive_marker")) {
          slots.add("prohibitive_marker");
        }
        if (features.surface === "過") slots.add("experiential_aspect");
        if (features.surface === "緊") slots.add("progressive_aspect");
        if (features.surface === "咗") slots.add("perfective_aspect");
        if (features.surface === "吓") slots.add("delimitative_aspect");
        const stativeBundle = featureBundleFor2(features.surface, { label: features.label, syntax: features.syntax }, features, [...slots]);
        for (const slot of parserActiveStativeSlotsForBundle2(stativeBundle)) slots.add(slot);
        if (features.surface === "好似") slots.add("seeming_marker");
        if (features.surface === "諗住") slots.add("intention_predicate");
        if (features.surface === "一齊") slots.add("manner");
        if (features.surface === "左右" || features.surface === "大概") slots.add("approximation");
        if (features.surface === "度") slots.add("post_classifier_approximation");
        if (["咁", "噉"].includes(features.surface)) slots.add("discourse_marker");
        if (features.surface === "都") slots.add("focus_adverb");
        if (features.surface === "啲") slots.add("quantity_modifier");
        if (features.surface === "鍾意") slots.add("preference_predicate");
        if (features.surface === "有冇") slots.add("existential_question");
        if (features.surface === "食") slots.add("purpose_verb");
        if (features.syntax.includes("verb_complement")) slots.add("verb_complement");
        if (features.syntax.includes("result_complement")) slots.add("result_complement");
        if (features.syntax.includes("directional_result_complement")) slots.add("directional_result_complement");
        return cleanSlots2([...slots]);
      }
      return {
        hasAny: hasAny2,
        stringIncludesAny: stringIncludesAny2,
        contextualRoleAffordances: contextualRoleAffordances2,
        inferTokenFeatures: inferTokenFeatures2,
        compactFeatureSummary: compactFeatureSummary2,
        featureList: featureList2,
        syntaxHas: syntaxHas2,
        featureBundleFor: featureBundleFor2,
        getFeatureBundle: getFeatureBundle2,
        getParserFeatures: getParserFeatures2,
        getConstructionAffordances: getConstructionAffordances2,
        getCoreDimensions: getCoreDimensions2,
        getLexicalizationType: getLexicalizationType2,
        hasPredicateSubtype: hasPredicateSubtype2,
        isLexicalizedStative: isLexicalizedStative2,
        isNegativeLexicalizedStative: isNegativeLexicalizedStative2,
        isOrdinaryStative: isOrdinaryStative2,
        isStativePredicateByBundle: isStativePredicateByBundle2,
        isGradablePredicate: isGradablePredicate2,
        canHeadComment: canHeadComment2,
        bundleCanFillStativeSlot: bundleCanFillStativeSlot2,
        parserActiveStativeSlotsForBundle: parserActiveStativeSlotsForBundle2,
        conditionMatches: conditionMatches2,
        generateTokenSlots: generateTokenSlots2
      };
    };
  }
});

// src/parser/nodes/node-shape.js
var require_node_shape = __commonJS({
  "src/parser/nodes/node-shape.js"(exports2, module2) {
    "use strict";
    module2.exports = function createNodeShapePrimitives(dependencies = {}) {
      const { bundleCanFillStativeSlot: bundleCanFillStativeSlot2 } = dependencies;
      function traceInfo2(kind, detail = {}) {
        return { kind, ...detail };
      }
      function traceKind2(node) {
        return node && node.trace && node.trace.kind ? node.trace.kind : "unspecified";
      }
      function isSurfaceSpecificTrace2(node) {
        const kind = traceKind2(node);
        return [
          "surface_rule",
          "surface_specific_phrase_rule",
          "protected_formula_table",
          "legacy_surface_rule",
          "special_ambiguity_rule"
        ].includes(kind);
      }
      function sameNodeSequence2(a, b) {
        return a.length === b.length && a.every((node, index) => node === b[index]);
      }
      function phraseMatch2(length, node) {
        return { length, node };
      }
      function flattenSurface2(node) {
        if (!node) return "";
        if (node.kind === "token") return node.surface;
        if (node.kind === "text") return node.text;
        if (node.kind === "construction") return node.children.map(flattenSurface2).join("");
        return "";
      }
      function flattenDisplaySurface2(node) {
        if (!node) return "";
        if (node.kind === "token") return node.display_surface || node.surface;
        if (node.kind === "text") return node.display_text || node.text;
        if (node.kind === "construction") return node.children.map(flattenDisplaySurface2).join("");
        return "";
      }
      function firstToken2(node) {
        if (!node) return null;
        if (node.kind === "token") return node;
        if (node.kind === "construction") {
          for (const child of node.children) {
            const found = firstToken2(child);
            if (found) return found;
          }
        }
        return null;
      }
      function lastToken2(node) {
        if (!node) return null;
        if (node.kind === "token") return node;
        if (node.kind === "construction") {
          for (let i = node.children.length - 1; i >= 0; i--) {
            const found = lastToken2(node.children[i]);
            if (found) return found;
          }
        }
        return null;
      }
      function isToken2(node, surface) {
        return node && node.kind === "token" && node.surface === surface;
      }
      function isVerbLike2(node) {
        const t = firstToken2(node);
        return Boolean(t && (t.label === "doing" || t.syntax.includes("verb")));
      }
      function isObjectLike2(node) {
        const t = firstToken2(node);
        return Boolean(t && ["what", "where", "who"].includes(t.label));
      }
      function isProductiveVo2(node) {
        return node && node.kind === "construction" && node.type === "ProductiveVO";
      }
      function isModalToken2(node) {
        return node && node.kind === "token" && ["想", "要", "可以", "會", "識", "使", "唔使", "可唔可以"].includes(node.surface);
      }
      function isParticle2(node) {
        return node && node.kind === "token" && node.label === "particle";
      }
      function isStativeToken2(node) {
        if (!node || node.kind !== "token") return false;
        const bundleDecision = bundleCanFillStativeSlot2(node, "stative_predicate");
        if (bundleDecision !== null) return bundleDecision;
        return node.label === "like" || node.syntax.includes("stative");
      }
      function isTopicCandidate2(node) {
        return node && node.kind === "token" && ["呢個", "嗰個", "呢啲", "嗰啲", "呢間", "嗰間", "呢間餐廳", "嗰間餐廳"].includes(node.surface);
      }
      function surfaceOf2(node) {
        const t = firstToken2(node);
        return t ? t.surface : flattenSurface2(node);
      }
      function hasSurface2(nodes, surface) {
        return nodes.some((node) => surfaceOf2(node) === surface || flattenSurface2(node) === surface);
      }
      function indexOfSurface2(nodes, surface) {
        return nodes.findIndex((node) => surfaceOf2(node) === surface || flattenSurface2(node) === surface);
      }
      function hasConstruction2(nodes, type) {
        return nodes.some((node) => node && node.kind === "construction" && node.type === type);
      }
      return {
        traceInfo: traceInfo2,
        traceKind: traceKind2,
        isSurfaceSpecificTrace: isSurfaceSpecificTrace2,
        sameNodeSequence: sameNodeSequence2,
        phraseMatch: phraseMatch2,
        flattenSurface: flattenSurface2,
        flattenDisplaySurface: flattenDisplaySurface2,
        firstToken: firstToken2,
        lastToken: lastToken2,
        isToken: isToken2,
        isVerbLike: isVerbLike2,
        isObjectLike: isObjectLike2,
        isProductiveVo: isProductiveVo2,
        isModalToken: isModalToken2,
        isParticle: isParticle2,
        isStativeToken: isStativeToken2,
        isTopicCandidate: isTopicCandidate2,
        surfaceOf: surfaceOf2,
        hasSurface: hasSurface2,
        indexOfSurface: indexOfSurface2,
        hasConstruction: hasConstruction2
      };
    };
  }
});

// src/parser/slots/slot-primitives.js
var require_slot_primitives = __commonJS({
  "src/parser/slots/slot-primitives.js"(exports2, module2) {
    "use strict";
    var SLOT_ALIASES2 = require_slot_aliases();
    module2.exports = function createSlotPrimitives(dependencies = {}) {
      const {
        cleanSlots: cleanSlots2,
        bundleCanFillStativeSlot: bundleCanFillStativeSlot2
      } = dependencies;
      function mergeUnique2(...lists) {
        return [...new Set(lists.flat().filter(Boolean))].sort();
      }
      function constructionSlotsByType2(type, children = []) {
        const childSlots = children.flatMap(nodeSlots2);
        const has = (slot) => childSlots.includes(slot);
        const slots = [];
        if (["ExperientialQuestion"].includes(type)) slots.push("experiential_question", "question_fragment", "experiential_vp");
        if (["ExperientialClause"].includes(type)) slots.push("experiential_clause", "experiential_vp", "predicate");
        if (["NegativeExperiential"].includes(type)) slots.push("negative_experiential", "experiential_vp", "predicate");
        if (["SourceMotionClause"].includes(type)) slots.push("source_motion_clause", "source", "source_marker", "location", "movement_verb", "directional_motion_vp", "vp", "predicate", "clause", "subject");
        if (["DirectedMannerMotionVP"].includes(type)) slots.push("directed_manner_motion_vp", "movement_verb", "manner_motion", "movement_direction", "path_component", "path_phrase", "deictic_motion_marker", "goal", "location", "predicate", "vp", "action_vp");
        if (["GoalAttainmentMotionVP"].includes(type)) slots.push("goal_attainment_motion_vp", "movement_verb", "goal_attainment_complement", "result_marker", "goal", "location", "predicate", "vp", "action_vp");
        if (["NegativeCognitionFragment"].includes(type)) slots.push("negative_cognition_fragment", "cognition_predicate", "negative_cognition_predicate", "predicate");
        if (["NegatedExistentialFragment"].includes(type)) slots.push("negated_existential_fragment", "negated_existential", "answer_fragment", "discourse_response", "predicate", "particle");
        if (["CognitionContentFrame"].includes(type)) slots.push("cognition_content_frame", "cognition_predicate", "reported_content", "content_clause", "predicate");
        if (["IntentionFrame"].includes(type)) slots.push("intention_frame", "vp", "predicate", "intention_predicate");
        if (["IntransitiveVP"].includes(type)) slots.push("intransitive_vp", "action_vp", "vp", "predicate", "action_verb");
        if (["DesiderativeVP"].includes(type)) slots.push("desiderative_vp", "vp", "predicate", "modal");
        if (["MalformedCandidate"].includes(type)) slots.push("malformed_candidate", "needs_review", "predicate", "problem_span", "action_verb", "quantity");
        if (["FragmentAnswer"].includes(type)) slots.push("fragment_answer", "possessive_fragment", "answer_fragment", "np", "subject");
        if (["ComplementEllipsisFragment"].includes(type)) slots.push("complement_ellipsis_fragment", "fragment_answer", "reported_content", "content_clause", "predicate", "clause");
        if (["DegreeMannerModifiedVP"].includes(type)) slots.push("degree_manner_modified_vp", "degree_manner_adverbial", "modifier", "degree", "vp", "action_vp", "predicate");
        if (["ANotAQuestion"].includes(type)) slots.push("question_fragment", "predicate", "vp", "action_vp", "negator");
        if (["ModalANotAQuestion"].includes(type)) {
          slots.push("modal_a_not_a_question", "question_fragment", "modal", "predicate", "vp");
          if (has("desiderative_a_not_a_question") || has("desiderative_modal")) slots.push("desiderative_modal");
          if (has("permission_a_not_a_question") || has("permission_a_not_a_modal") || has("permission_modal")) slots.push("permission_modal");
        }
        if (["ApproximateQuantity"].includes(type)) slots.push("approximate_quantity", "quantified_object", "quantity", "classifier", "approximation", "object", "np", "topic", "price_noun", "scalar_value_noun");
        if (["ScalarValueQuestion"].includes(type)) {
          slots.push("scalar_value_question", "scalar_dimension_question", "question_fragment", "scalar_wh_degree", "scalar_dimension_predicate", "predicate");
          if (has("price_question") || has("price_noun") || has("scalar_value_noun")) slots.push("price_question");
        }
        if (["ScalarEvaluation"].includes(type)) slots.push("scalar_evaluation", "evaluation_clause", "predicate", "stative_predicate", "comment", "comment_predicate", "negator", "evaluation_marker");
        if (["ClauseRelationEdge"].includes(type)) slots.push("clause_relation", "left_relation_member", "right_relation_member", "antecedent_clause", "consequent_clause", "reason_clause", "result_clause", "concession_clause", "counterexpectation_clause", "earlier_event", "later_event", "temporal_subordinate", "matrix_clause", "linker_left", "linker_right", "predicate", "clause", "clause_like", "reported_content", "content_clause");
        if (["ClauseRelationMemberSpan"].includes(type)) slots.push("clause_relation_member", "left_relation_member", "right_relation_member", "predicate", "clause", "clause_like");
        if (["ConditionalClause"].includes(type)) slots.push("conditional_clause", "condition_clause", "conditional_antecedent", "conditional_marker", "predicate", "clause");
        if (["AcceptabilityANotA"].includes(type)) slots.push("acceptability_question", "question_fragment", "acceptability_predicate");
        if (["CompletionQuestion"].includes(type)) slots.push("completion_question", "completion_vp", "question_fragment", "question_marker");
        if (["ProhibitiveImperative"].includes(type)) slots.push("prohibitive_marker", "imperative", "prohibitive_imperative", "predicate");
        if (["ReportedSpeech"].includes(type)) slots.push("reported_speech", "speech_verb", "reported_content");
        if (["OpinionStanceFrame"].includes(type)) slots.push("opinion_stance_frame", "stance_predicate", "reported_content", "predicate", "content_clause");
        if (["DiscourseParticleFrame"].includes(type)) {
          slots.push("discourse_particle_frame", "scope_host", "discourse_scope_particle", "particle", "predicate", "clause");
          if (has("epistemic_scope_particle")) slots.push("epistemic_stance", "epistemic_scope_particle");
          if (has("evidential_scope_particle")) slots.push("evidential_stance", "evidential_scope_particle");
          if (has("directive_scope_particle")) slots.push("directive_stance", "directive_scope_particle");
          if (has("change_state_scope_particle")) slots.push("change_state_stance", "change_state_scope_particle");
        }
        if (["FocusParticleFrame"].includes(type)) slots.push("focus_particle_frame", "restriction_marker", "scalar_host", "restrictive_focus_particle", "focus_adverb", "particle", "quantity", "degree", "np", "topic", "object");
        if (["IdentificationFragment"].includes(type)) slots.push("identification_fragment", "topic_or_object", "np");
        if (["DefinitionExplanatoryFrame"].includes(type)) slots.push("definition_explanatory_frame", "definition_frame", "identification_clause", "copular_clause", "explanatory_clause", "topic", "object", "np", "predicate", "clause");
        if (["DefinitionComplement"].includes(type)) slots.push("definition_complement", "object", "np", "topic_or_object");
        if (["IntendedFunctionRelation", "ResourceUseLaiFunctionRelation"].includes(type)) slots.push("intended_function_relation", "function_relation", "function_topic", "topic", "user_subject", "modal", "copula", "negator", "purpose_use_verb", "purpose_lai_marker", "purpose_predicate", "predicate", "vp", "action_vp", "clause");
        if (["LocativeWhQuestion"].includes(type)) slots.push("question_fragment", "location_question", "location", "predicate", "vp", "clause");
        if (["ProgressiveWhObjectQuestion"].includes(type)) slots.push("progressive_wh_object_question", "question_fragment", "progressive_vp", "wh_object", "predicate");
        if (["ExistentialClause"].includes(type)) slots.push("existential_clause", "possessive_clause", "predicate", "existential", "object");
        if (["NegatedExistentialClause"].includes(type)) slots.push("negated_existential_clause", "possessive_clause", "predicate", "negated_existential", "object");
        if (["ExistentialQuestion"].includes(type)) slots.push("existential_question", "question_fragment", "possessive_question", "predicate", "object");
        if (["TopicComment"].includes(type)) slots.push("topic_comment", "evaluation_clause", "reported_content", "predicate", "content_topic", "opinion_topic", "comment", "comment_predicate", "stative_predicate");
        if (["PostThemeParticipantRelation"].includes(type)) slots.push("post_theme_participant_relation", "post_theme_link_marker", "post_theme_participant", "person_np", "np", "predicate", "vp", "action_vp");
        if (["BenefactivePurposeVP"].includes(type)) slots.push("benefactive_purpose_vp", "benefactive_action_chain", "purpose_chain", "vp", "action_vp", "predicate", "recipient");
        if (["LocativePlacePhrase"].includes(type)) slots.push("locative_phrase", "location", "goal");
        if (["LocativePostureVP"].includes(type)) slots.push("locative_posture_vp", "posture_verb", "locative_phrase", "location", "vp", "action_vp", "predicate");
        if (["MannerAdverbialVP"].includes(type)) slots.push("manner_adverbial_vp", "manner", "modifier", "vp", "action_vp", "predicate", "subject");
        if (["ProductiveVO"].includes(type)) slots.push("vp", "productive_vo", "action_vp", "predicate", "object");
        if (["ResultComplement", "ResultComplementVP", "NegativePotentialComplement", "PotentialResultVP"].includes(type)) slots.push("vp", "action_vp", "predicate", "result_complement", "result_potential_complement");
        if (["ResultComplementVP"].includes(type)) slots.push("result_complement_vp");
        if (["PotentialResultVP"].includes(type)) slots.push("potential_result_vp", "potential_marker");
        if (["NegativePotentialComplement"].includes(type)) slots.push("negative_potential_complement", "negator");
        if (["ResultComplement"].includes(type)) slots.push("result_attainment_complement");
        if (["ExperientialVP", "ExperientialMotionGoalVP"].includes(type)) slots.push("vp", "experiential_vp", "action_vp", "predicate");
        if (["ExperientialMotionGoalVP"].includes(type)) slots.push("motion_goal_vp", "movement_verb", "goal", "location");
        if (["MotionGoalVP"].includes(type)) slots.push("motion_goal_vp", "movement_verb", "goal", "location", "predicate", "vp", "action_vp");
        if (["ProgressiveVP"].includes(type)) slots.push("vp", "progressive_vp", "action_vp", "predicate");
        if (["PerfectiveVP", "PostverbalZoPerfectiveVP"].includes(type)) slots.push("vp", "perfective_vp", "completion_vp", "action_vp", "predicate");
        if (["VerbComplementVP"].includes(type)) slots.push("verb_complement_vp", "verb_complement", "vp", "action_vp", "predicate", "main_verb", "object");
        if (["LexicalGiveRelation"].includes(type)) slots.push("lexical_give_relation", "give_relation", "vp", "action_vp", "predicate", "transfer_predicate");
        if (["PassivePermissiveRelation"].includes(type)) slots.push("passive_permissive_relation", "bei_relation", "clause", "predicate", "vp", "pre_marker_participant", "postmarker_participant", "retained_patient_candidate");
        if (["CoverbFrame"].includes(type)) slots.push("coverb_frame", "coverb_phrase", "coverb_marker", "coverb_object", "preverbal_modifier", "predicate", "vp", "clause");
        if (["DelimitedVP", "ReduplicatedVP", "CompletionVP", "DitransitiveSpeechVP", "TransitiveVP", "ActionStativeVP"].includes(type)) slots.push("vp", "action_vp", "predicate");
        if (["TransitiveVP"].includes(type)) slots.push("transitive_vp");
        if (["DirectionalMotionVP", "CompoundDirectionalMotionVP"].includes(type)) slots.push("vp", "action_vp", "predicate", "movement_verb", "motion_predicate", "directional_motion_vp");
        if (["NegatedDirectionalMotionVP"].includes(type)) slots.push("vp", "action_vp", "predicate", "movement_verb", "motion_predicate", "directional_motion_vp", "negated_directional_motion_vp", "negator");
        if (["NegatedVP"].includes(type)) slots.push("negated_vp", "vp", "action_vp", "predicate", "negator");
        if (["MotionPurposeChain"].includes(type)) slots.push("motion_purpose_chain", "motion_action_chain", "purpose_chain", "vp", "action_vp", "predicate");
        if (["CompletionVP"].includes(type)) slots.push("completion_vp");
        if (["DitransitiveSpeechVP"].includes(type)) slots.push("speech_transfer_vp", "reported_content");
        if (["OvertHeadDemonstrativeClassifierNP", "DemonstrativeClassifierNP", "QuantifiedClassifierNP", "QuantifiedPersonNP", "QuantifiedTimeNP", "QuantityNP", "DiMarkedNP", "OrdinalClassifierNP", "AssociativeNP", "ModifiedNP", "ModifierNP", "NominalHeadSpan"].includes(type)) slots.push("np", "topic", "object", "head_noun");
        if (["HeadlessDemonstrativeClassifierNP"].includes(type)) slots.push("np", "topic", "object", "demonstrative", "classifier");
        if (["QuantifiedTimeNP"].includes(type)) slots.push("quantified_time_np", "time", "time_head", "quantity", "distributive_quantifier");
        if (["QuantityNP"].includes(type)) slots.push("quantity_np", "quantity");
        if (["MeasureExpression"].includes(type)) slots.push("measure_expression", "nominal_predicate", "predicate", "quantity", "nominal_measure_unit", "age_unit", "currency_unit", "area_measure_unit", "length_measure_unit", "dimension_predicate");
        if (["NominalPredicateClause"].includes(type)) slots.push("nominal_predicate_clause", "subject", "predicate", "nominal_predicate", "measure_expression", "clause");
        if (["QuantifiedPersonNP"].includes(type)) slots.push("subject", "quantity");
        if (["AssociativeNP"].includes(type)) slots.push("associative_np", "modified_np", "nominal_linker", "modifier");
        if (["DiMarkedNP"].includes(type)) slots.push("di_marked_np", "di_determiner", "quantity");
        if (type === "WhClassifierQuestion") slots.push("wh_fragment", "question_fragment", "topic_or_object");
        if (type === "TimeNP") slots.push("time");
        if (["Topic"].includes(type)) slots.push("topic", "np", "definition_topic", "purpose_topic", "function_topic");
        if (["StativePredicate", "DegreeStativePredicate", "DegreeModifiedLexicalStative", "NegatedStativePredicate"].includes(type)) slots.push("stative_predicate", "predicate", "comment", "comment_predicate");
        if (["LexicalizedStativePredicate", "DegreeModifiedLexicalStative"].includes(type)) slots.push("lexicalized_stative_predicate", "stative_predicate", "predicate", "comment", "comment_predicate");
        if (["NegatedStativePredicate"].includes(type)) slots.push("negated_stative_predicate", "ordinary_stative_predicate", "negator");
        if (["DegreeStativePredicate"].includes(type)) slots.push("degree_stative_predicate", "ordinary_degree_stative_predicate", "ordinary_stative_predicate", "degree");
        if (["DegreeStativePredicate"].includes(type) && has("ambient_property_predicate")) slots.push("ambient_environmental_predicate");
        if (["ImpersonalEnvironmentalClause"].includes(type)) slots.push("impersonal_environmental_clause", "subjectless_clause", "environmental_predicate", "predicate", "clause", "weather_phenomenon");
        if (["LocativeFrameClause"].includes(type)) slots.push("locative_frame_clause", "ambient_frame_clause", "location", "environmental_predicate", "predicate", "clause");
        if (["ExistentialPresentationalClause"].includes(type)) slots.push("existential_presentational_clause", "existential_clause", "predicate", "existential", "negated_existential", "introduced_participant", "presentational_coda", "location", "clause");
        if (["DegreeModifiedLexicalStative"].includes(type)) slots.push("degree_modified_lexical_stative", "degree");
        if (["FormulaDiscourseUnit"].includes(type)) slots.push("formula_discourse_unit", "formula", "formula_expression", "discourse_response", "agreement_response", "confirmation_response", "particle", "clause");
        if (["FragmentQuestion"].includes(type)) slots.push("fragment_question", "question_fragment", "discourse_fragment", "clause");
        if (["TemporalClause"].includes(type)) slots.push("temporal_clause", "time_clause", "time", "predicate");
        if (["RelativeClauseNP"].includes(type)) slots.push("relative_clause_np", "relative_clause", "nominal_linker", "head_noun", "np", "subject", "topic", "object");
        if (["SubjectPredicateClause"].includes(type)) {
          slots.push("subject_predicate_clause", "subject", "predicate", "clause");
          if (has("time")) slots.push("temporal_clause", "time_clause", "time");
          if (has("location") || has("locative_phrase")) slots.push("location");
          if (has("stative_predicate")) slots.push("stative_predicate", "comment", "comment_predicate");
          if (has("negated_directional_motion_vp") || has("negator")) slots.push("negative_clause", "negated_predicate", "negator");
        }
        if (["ModalVP"].includes(type)) slots.push("modal_vp", "modal_predicate", "modal", "predicate", "vp");
        if (["JauDakMouDakAvailabilityPredicate"].includes(type)) slots.push("availability_predicate", "availability_opportunity", "modal_predicate", "predicate", "vp", "clause");
        if (["LocativeModalPredicateClause"].includes(type)) slots.push("locative_modal_predicate_clause", "modal_predicate_clause", "location", "modal_vp", "modal", "predicate", "clause");
        if (["SubjectModalPredicateClause"].includes(type)) slots.push("subject_modal_predicate_clause", "modal_predicate_clause", "subject", "modal_vp", "modal", "predicate", "clause");
        if (["TopicModalPredicateClause"].includes(type)) slots.push("topic_modal_predicate_clause", "modal_predicate_clause", "topic", "modal_vp", "modal", "predicate", "clause");
        if (["NamingClause"].includes(type)) slots.push("naming_clause", "naming_frame", "naming_frame", "subject", "naming_verb", "name", "np", "predicate", "clause");
        if (["PathPhrase"].includes(type)) slots.push("path_phrase", "path_marker", "location", "goal");
        if (["PoliteImperativeClause"].includes(type)) slots.push("polite_imperative_clause", "imperative", "politeness_marker", "subject", "time", "path_phrase", "location", "predicate", "clause");
        if (["PolarQuestionFrame"].includes(type)) slots.push("polar_question_frame", "yes_no_question", "question_fragment", "yes_no_question_marker", "subject", "predicate", "particle", "clause");
        if (["CopularIdentificationFrame"].includes(type)) slots.push("copular_identification_frame", "identification_clause", "copular_clause", "topic", "copula", "np", "object", "predicate", "clause");
        if (["CoordinatedNP"].includes(type)) slots.push("coordinated_np", "left_np", "right_np", "coordinator", "np", "topic", "object");
        if (["StativeNominalComplement"].includes(type)) slots.push("stative_nominal_complement", "copular_complement", "np", "predicate", "head_noun", "nominal_linker");
        if (["CopularRelationFrame"].includes(type)) slots.push("copular_relation_frame", "copular_clause", "subject", "copula", "copular_complement", "predicate", "clause");
        if (["CopularANotAQuestion"].includes(type)) slots.push("copular_a_not_a_question", "question_fragment", "subject", "copula", "negator", "copular_complement", "predicate", "clause");
        if (["LocativeExistentialClause"].includes(type)) slots.push("locative_existential_clause", "existential_clause", "location", "locative_domain", "introduced_theme", "existential", "negated_existential", "predicate", "clause");
        if (["OrdinalClassifierNP"].includes(type)) slots.push("ordinal_classifier_np", "classifier_np", "ordinal_modifier", "classifier", "head_noun", "np", "object", "topic");
        if (["PossessiveClassifierNP"].includes(type)) slots.push("possessive_classifier_np", "possessive_np", "possessor", "classifier", "head_noun", "np", "object", "topic");
        if (["ChangeIntoPredicate"].includes(type)) slots.push("change_into_predicate", "change_verb", "result_complement", "predicate", "vp", "action_vp");
        if (["ClassifierObjectNP"].includes(type)) slots.push("classifier_object_np", "np", "object", "topic", "classifier", "head_noun");
        if (["CoordinatedSubjectModalPredicateClause"].includes(type)) slots.push("coordinated_subject_modal_predicate_clause", "modal_predicate_clause", "subject", "coordinated_subject", "modal_vp", "modal", "predicate", "clause");
        if (["ClauseSequence", "ClauseRelationGraph"].includes(type)) slots.push("clause_linking_sequence", "clause_sequence", "multi_clause_sequence", "utterance_sequence", "discourse_sequence", "clause", "clause_like", "separator");
        if (["VocativeAddressTerm"].includes(type)) slots.push("vocative_address_term", "named_address_term", "address_term", "vocative");
        if (["UrgencyMarker"].includes(type)) slots.push("urgency_marker", "imperative_adverb", "how");
        if (["PriorityMarkerClause"].includes(type)) slots.push("priority_marker_clause", "sequence_priority_marker", "priority_marker", "vp", "action_vp", "predicate");
        if (["AcceptabilityClause"].includes(type)) slots.push("acceptability_clause", "acceptability", "focus_adverb", "acceptability_predicate", "predicate", "clause");
        if (["SerialVerbPurposeChain"].includes(type)) slots.push("serial_verb_purpose_chain", "serial_action_chain", "purpose_chain", "vp", "action_vp", "predicate");
        if (has("location")) slots.push("location", "goal");
        if (has("subject")) slots.push("subject");
        return cleanSlots2(mergeUnique2(childSlots, slots));
      }
      function nodeSlots2(node) {
        if (!node) return [];
        if (node.kind === "token") return node.slots || [];
        if (node.kind === "construction") return node.slots || constructionSlotsByType2(node.type, node.children || []);
        return [];
      }
      function slotAlternatives2(slot) {
        return [slot, ...SLOT_ALIASES2[slot] || []];
      }
      function nodeCanFillSlot2(node, slot) {
        if (slot === "particle" && node && node.kind === "construction") return false;
        const alternatives = slotAlternatives2(slot);
        for (const candidate of alternatives) {
          const bundleDecision = bundleCanFillStativeSlot2(node, candidate);
          if (bundleDecision !== null) return bundleDecision;
        }
        const slots = nodeSlots2(node);
        return alternatives.some((candidate) => slots.includes(candidate));
      }
      function isBareQuantityTokenObject2(node) {
        if (!node || node.kind !== "token") return false;
        const slots = nodeSlots2(node);
        const syntax = String(node.syntax || "");
        return slots.includes("quantity") && slots.includes("object") && !slots.includes("classifier") && /(?:^|\s)numeral_quantity(?:\s|$)/.test(syntax);
      }
      function templateDerivedSlots2(type, children) {
        return cleanSlots2(mergeUnique2(["construction_span"], constructionSlotsByType2(type, children)));
      }
      return {
        mergeUnique: mergeUnique2,
        constructionSlotsByType: constructionSlotsByType2,
        nodeSlots: nodeSlots2,
        slotAlternatives: slotAlternatives2,
        nodeCanFillSlot: nodeCanFillSlot2,
        isBareQuantityTokenObject: isBareQuantityTokenObject2,
        templateDerivedSlots: templateDerivedSlots2
      };
    };
  }
});

// src/parser/templates/template-matcher.js
var require_template_matcher = __commonJS({
  "src/parser/templates/template-matcher.js"(exports2, module2) {
    "use strict";
    module2.exports = function createTemplateMatcher(dependencies = {}) {
      const {
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isBareQuantityTokenObject: isBareQuantityTokenObject2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeCanLicenseEvidenceGatedObject: nodeCanLicenseEvidenceGatedObject2,
        nodeSlots: nodeSlots2
      } = dependencies;
      function parseTemplateSlot2(spec) {
        const required = spec.endsWith("!");
        const optional = spec.endsWith("?");
        const slot = required || optional ? spec.slice(0, -1) : spec;
        return { slot, required: required || !optional, optional };
      }
      function templateConstraintsPass2(assignments, templateDef = {}) {
        const constraints = templateDef.constraints || {};
        if (constraints.same_surface) {
          const surfaces = assignments.map((item) => flattenSurface2(item.node));
          if (!surfaces.length) return false;
          if (new Set(surfaces).size !== 1) return false;
        }
        if (constraints.copied_first_token_surface_slots) {
          const copiedSlots = constraints.copied_first_token_surface_slots || [];
          for (const slot of copiedSlots) {
            const matchingAssignments = assignments.filter((item) => item.slot === slot);
            if (matchingAssignments.length < 2) return false;
            const firstSurfaces = matchingAssignments.map((item) => {
              const tok = firstToken2(item.node);
              return tok ? tok.surface : flattenSurface2(item.node);
            });
            if (!firstSurfaces[0] || firstSurfaces.some((surface) => surface !== firstSurfaces[0])) return false;
          }
        }
        if (constraints.disallow_child_slots) {
          const disallowed = constraints.disallow_child_slots || [];
          for (const item of assignments) {
            const slots = nodeSlots2(item.node);
            if (disallowed.some((slot) => slots.includes(slot))) return false;
          }
        }
        if (constraints.first_node_must_not_have_slots) {
          const first = assignments[0] && assignments[0].node;
          const firstSlots = nodeSlots2(first);
          const disallowed = constraints.first_node_must_not_have_slots || [];
          if (disallowed.some((slot) => firstSlots.includes(slot))) return false;
        }
        if (constraints.first_node_must_have_surface) {
          const first = assignments[0] && assignments[0].node;
          if (flattenSurface2(first) !== constraints.first_node_must_have_surface) return false;
        }
        if (constraints.predicate_must_have_any_slots) {
          const allowed = constraints.predicate_must_have_any_slots || [];
          const predicateAssignments = assignments.filter((item) => item.slot === "predicate");
          if (!predicateAssignments.length) return false;
          if (!predicateAssignments.some((item) => {
            const slots = nodeSlots2(item.node);
            return allowed.some((slot) => slots.includes(slot));
          })) return false;
        }
        if (constraints.require_any_assigned_slots) {
          const requiredSlots = constraints.require_any_assigned_slots || [];
          if (!assignments.some((item) => requiredSlots.includes(item.slot))) return false;
        }
        if (constraints.surface_sequence_in) {
          const surfaceSequence = assignments.map((item) => flattenSurface2(item.node)).join("");
          const allowedSequences = constraints.surface_sequence_in || [];
          if (!allowedSequences.includes(surfaceSequence)) return false;
        }
        if (constraints.slot_first_token_syntax_must_include_any) {
          for (const [slot, requiredTerms] of Object.entries(constraints.slot_first_token_syntax_must_include_any || {})) {
            const required = requiredTerms || [];
            const matchingAssignments = assignments.filter((item) => item.slot === slot);
            if (!matchingAssignments.length) return false;
            if (matchingAssignments.some((item) => {
              const tok = firstToken2(item.node);
              const syntax = String(tok && tok.syntax || "");
              return !required.some((term) => syntax.includes(term));
            })) return false;
          }
        }
        if (constraints.slot_surface_in) {
          for (const [slot, allowedSurfaces] of Object.entries(constraints.slot_surface_in || {})) {
            const allowed = allowedSurfaces || [];
            const matchingAssignments = assignments.filter((item) => item.slot === slot);
            if (!matchingAssignments.length) continue;
            if (!matchingAssignments.every((item) => allowed.includes(flattenSurface2(item.node)))) return false;
          }
        }
        if (constraints.slot_surface_not_in) {
          for (const [slot, disallowedSurfaces] of Object.entries(constraints.slot_surface_not_in || {})) {
            const disallowed = disallowedSurfaces || [];
            const matchingAssignments = assignments.filter((item) => item.slot === slot);
            if (matchingAssignments.some((item) => disallowed.includes(flattenSurface2(item.node)))) return false;
          }
        }
        if (constraints.slot_must_not_be_bare_quantity_token) {
          const guardedSlots = constraints.slot_must_not_be_bare_quantity_token || [];
          for (const slot of guardedSlots) {
            const matchingAssignments = assignments.filter((item) => item.slot === slot);
            if (matchingAssignments.some((item) => isBareQuantityTokenObject2(item.node))) return false;
          }
        }
        if (constraints.slot_must_be_licensed_np) {
          const guardedSlots = constraints.slot_must_be_licensed_np || [];
          for (const slot of guardedSlots) {
            const matchingAssignments = assignments.filter((item) => item.slot === slot);
            if (!matchingAssignments.length || matchingAssignments.some((item) => !nodeCanLicenseEvidenceGatedObject2(item.node))) return false;
          }
        }
        if (constraints.slot_must_have_any_slots) {
          for (const [slot, requiredSlots] of Object.entries(constraints.slot_must_have_any_slots || {})) {
            const required = requiredSlots || [];
            const matchingAssignments = assignments.filter((item) => item.slot === slot);
            if (!matchingAssignments.length) return false;
            if (matchingAssignments.some((item) => {
              const slots = nodeSlots2(item.node);
              return !required.some((requiredSlot) => slots.includes(requiredSlot));
            })) return false;
          }
        }
        if (constraints.slot_must_not_have_slots) {
          for (const [slot, disallowedSlots] of Object.entries(constraints.slot_must_not_have_slots || {})) {
            const disallowed = disallowedSlots || [];
            const matchingAssignments = assignments.filter((item) => item.slot === slot);
            if (matchingAssignments.some((item) => {
              const slots = nodeSlots2(item.node);
              return disallowed.some((disallowedSlot) => slots.includes(disallowedSlot));
            })) return false;
          }
        }
        return true;
      }
      function matchTemplate2(nodes, template) {
        const specs = template.map(parseTemplateSlot2);
        const memo = /* @__PURE__ */ new Map();
        function step(i, j, assignments) {
          const key = `${i}:${j}`;
          if (memo.has(key)) return null;
          if (j === specs.length) return i === nodes.length ? assignments : null;
          const spec = specs[j];
          if (spec.optional && i < nodes.length && nodeCanFillSlot2(nodes[i], spec.slot)) {
            const matched = step(i + 1, j + 1, [...assignments, { slot: spec.slot, node: nodes[i] }]);
            if (matched) return matched;
          }
          if (spec.optional) {
            const skipped = step(i, j + 1, assignments);
            if (skipped) return skipped;
          }
          if (i < nodes.length && nodeCanFillSlot2(nodes[i], spec.slot)) {
            const matched = step(i + 1, j + 1, [...assignments, { slot: spec.slot, node: nodes[i] }]);
            if (matched) return matched;
          }
          memo.set(key, true);
          return null;
        }
        return step(0, 0, []);
      }
      return {
        parseTemplateSlot: parseTemplateSlot2,
        templateConstraintsPass: templateConstraintsPass2,
        matchTemplate: matchTemplate2
      };
    };
  }
});

// src/parser/nodes/node-factories.js
var require_node_factories = __commonJS({
  "src/parser/nodes/node-factories.js"(exports2, module2) {
    "use strict";
    module2.exports = function createNodeFactories(dependencies = {}) {
      const {
        TOKEN_LEXICON: TOKEN_LEXICON2,
        TOKEN_LEXICAL_ANALYSES: TOKEN_LEXICAL_ANALYSES2,
        UNKNOWN_CJK_JYUTPING_FALLBACK: UNKNOWN_CJK_JYUTPING_FALLBACK2,
        normalizeLearnerLabel: normalizeLearnerLabel2,
        cleanSlots: cleanSlots2,
        contextualRoleAffordances: contextualRoleAffordances2,
        inferTokenFeatures: inferTokenFeatures2,
        compactFeatureSummary: compactFeatureSummary2,
        featureBundleFor: featureBundleFor2,
        generateTokenSlots: generateTokenSlots2,
        traceInfo: traceInfo2,
        traceKind: traceKind2,
        constructionSlotsByType: constructionSlotsByType2,
        nodeParserSurface: nodeParserSurface2,
        nodeDisplaySurface: nodeDisplaySurface2,
        INTERNAL_CONSTRUCTION_COMPATIBILITY_ALIASES: INTERNAL_CONSTRUCTION_COMPATIBILITY_ALIASES2,
        INTERNAL_ONLY_CONSTRUCTION_SCOPES: INTERNAL_ONLY_CONSTRUCTION_SCOPES2,
        internalConstructionTypeFor: internalConstructionTypeFor2,
        clauseSpanProfileForCompatibilityType: clauseSpanProfileForCompatibilityType2,
        npLicenseMetadata: npLicenseMetadata2
      } = dependencies;
      function pronunciationOnlyJyutpingForUnknown2(surface) {
        return UNKNOWN_CJK_JYUTPING_FALLBACK2[String(surface || "")] || "";
      }
      function token3(surface, overrides = {}) {
        const baseEntry = TOKEN_LEXICON2[surface] || {};
        const lexicalAnalyses = TOKEN_LEXICAL_ANALYSES2 && TOKEN_LEXICAL_ANALYSES2[surface] ? TOKEN_LEXICAL_ANALYSES2[surface] : [];
        const requestedAnalysisId = overrides.analysis_id || "";
        const selectedAnalysis = requestedAnalysisId ? lexicalAnalyses.find((analysis) => analysis.id === requestedAnalysisId) || null : lexicalAnalyses.length === 1 ? lexicalAnalyses[0] : null;
        const entry = selectedAnalysis ? { ...baseEntry, ...selectedAnalysis } : baseEntry;
        const rawLabel = overrides.label || entry.label || "neutral";
        const syntax = overrides.syntax || entry.syntax || "lexical_candidate";
        const label = normalizeLearnerLabel2(rawLabel, surface, syntax);
        const features = inferTokenFeatures2(surface, { ...entry, label, syntax }, overrides);
        const slots = overrides.slots ? cleanSlots2(overrides.slots) : generateTokenSlots2(features);
        const featureBundle = featureBundleFor2(surface, { ...entry, label, syntax }, features, slots);
        const lexicalAnalysisResolution = {
          status: selectedAnalysis ? "selected" : lexicalAnalyses.length > 1 ? "unresolved" : lexicalAnalyses.length === 1 ? "single" : "none",
          requested_analysis_id: requestedAnalysisId || void 0,
          active_analysis_id: selectedAnalysis ? selectedAnalysis.id : "",
          candidate_analysis_ids: lexicalAnalyses.map((analysis) => analysis.id)
        };
        const traceDetail = {
          surface,
          lexical_analyses: lexicalAnalyses,
          lexical_analysis_resolution: lexicalAnalysisResolution,
          generated_slots: slots,
          feature_summary: compactFeatureSummary2(features),
          feature_bundle: featureBundle
        };
        if (!Object.keys(entry).length && pronunciationOnlyJyutpingForUnknown2(surface)) {
          traceDetail.learner_gloss_lines = [
            "meaning not yet confirmed",
            "Pronunciation is shown without assigning this word a grammatical analysis."
          ];
        }
        const roleAffordances = contextualRoleAffordances2({ surface, role: label, label, syntax, slots, features });
        if (roleAffordances.length > 1) traceDetail.contextual_role_affordances = roleAffordances;
        if (overrides.selection_decision) traceDetail.selection_decision = overrides.selection_decision;
        return {
          kind: "token",
          surface,
          display_surface: overrides.display_surface || void 0,
          parser_surface: overrides.parser_surface || surface,
          label,
          jyutping: overrides.jyutping || entry.jyutping || pronunciationOnlyJyutpingForUnknown2(surface) || "",
          syntax,
          lexical_analyses: lexicalAnalyses,
          active_lexical_analysis_id: selectedAnalysis ? selectedAnalysis.id : "",
          lexical_analysis_resolution: lexicalAnalysisResolution,
          note: overrides.note || entry.note || (pronunciationOnlyJyutpingForUnknown2(surface) ? "meaning not yet confirmed" : "Neutral lexical item; no reviewed learner role yet."),
          review: overrides.review || entry.review || "reviewed_or_seeded_runtime",
          features,
          feature_bundle: featureBundle,
          slots,
          trace: overrides.trace || traceInfo2(entry && Object.keys(entry).length ? "atomic_lexicon" : "unknown_atomic", traceDetail)
        };
      }
      function textNode2(text) {
        return { kind: "text", text, trace: traceInfo2("punctuation_or_plain_text", { surface: text }) };
      }
      function construction2(type, label, children, options = {}) {
        const requestedType = type;
        const internalType = internalConstructionTypeFor2(requestedType);
        const baseSlots = options.slots || constructionSlotsByType2(requestedType, children);
        const compatibilityAlias = options.compatibility_alias || (internalType !== requestedType ? requestedType : INTERNAL_CONSTRUCTION_COMPATIBILITY_ALIASES2[internalType] || "");
        const internalRepresentationScope = INTERNAL_ONLY_CONSTRUCTION_SCOPES2[internalType] || "";
        const rawTrace = options.trace || traceInfo2("construction_function", { construction_type: requestedType });
        const normalizedTrace = internalType === "ClauseSpan" ? {
          ...rawTrace,
          construction_type: "ClauseSpan",
          compatibility_construction_type: compatibilityAlias || requestedType,
          clause_span_profile: clauseSpanProfileForCompatibilityType2(compatibilityAlias || requestedType),
          clause_span_semantic_status: "neutral_overt_span_accounting_only",
          typed_predicate_child_preserved: true,
          independent_grammar_licensing: false,
          subject_insertion_capability: false,
          topic_insertion_capability: false,
          argument_omission_licensing: false,
          context_resolution_capability: false,
          predicate_subtype_licensing: false,
          modal_licensing: false
        } : rawTrace;
        const npMetadata = npLicenseMetadata2(internalType, children, normalizedTrace);
        const finalTrace = npMetadata ? { ...normalizedTrace, ...npMetadata } : normalizedTrace;
        return {
          kind: "construction",
          type: internalType,
          compatibility_alias: compatibilityAlias,
          internal_representation_scope: internalRepresentationScope,
          internal_only: Boolean(internalRepresentationScope),
          label,
          children,
          display_surface: options.display_surface || nodeDisplaySurface2({ kind: "construction", children }),
          parser_surface: options.parser_surface || nodeParserSurface2({ kind: "construction", children }),
          primary: options.primary || "",
          note: options.note || "Parent construction span. Child tokens keep their own learner roles.",
          slots: cleanSlots2(baseSlots),
          trace: finalTrace
        };
      }
      function parserInactiveTokenClone2(node, overrides = {}) {
        if (!node || node.kind !== "token") return node;
        const surface = node.surface;
        const syntax = overrides.syntax || node.syntax || "";
        const label = normalizeLearnerLabel2(overrides.label || node.label || "neutral", surface, syntax);
        const slots = cleanSlots2(overrides.slots || []);
        const jyutping = Object.prototype.hasOwnProperty.call(overrides, "jyutping") ? overrides.jyutping : node.jyutping;
        const note = Object.prototype.hasOwnProperty.call(overrides, "note") ? overrides.note : node.note;
        const features = {
          ...node.features || {},
          pos: overrides.pos || node.features && node.features.pos || "",
          label,
          syntax,
          semantic: overrides.semantic || [],
          verb_class: overrides.verb_class || [],
          particle_class: overrides.particle_class || ""
        };
        const featureBundle = featureBundleFor2(surface, { label, syntax }, features, slots);
        const inheritedAffordanceCandidates = node.trace && node.trace.contextual_role_affordance_resolution && Array.isArray(node.trace.contextual_role_affordance_resolution.candidate_affordances) ? node.trace.contextual_role_affordance_resolution.candidate_affordances.map((item) => ({ ...item })) : [];
        const contextualRoleAffordanceResolution = {
          lexical_default_role: node.label || "",
          active_role: label,
          candidate_affordances: overrides.preserve_existing_affordances && inheritedAffordanceCandidates.length ? inheritedAffordanceCandidates : contextualRoleAffordances2({ surface, role: node.label || label, label: node.label || label, syntax: node.syntax || syntax, slots: node.slots || [], features: node.features || {} }),
          active_affordance_source: overrides.reason || "Token is parser-inactive inside a parent construction wrapper.",
          active_affordance_match: overrides.active_affordance_match || void 0,
          note: "Construction context selects the active learner role without deleting other lexical affordances."
        };
        return {
          ...node,
          label,
          role: label,
          syntax,
          jyutping,
          note,
          slots,
          features,
          feature_bundle: featureBundle,
          trace: traceInfo2("construction_internal_parser_inactive_clone", {
            surface,
            original_trace: traceKind2(node),
            reason: overrides.reason || "Token is parser-inactive inside a parent construction wrapper.",
            contextual_role_affordance_resolution: contextualRoleAffordanceResolution,
            role_resolution_note: overrides.role_resolution_note || void 0,
            feature_bundle: featureBundle,
            ...overrides.trace_detail || {}
          })
        };
      }
      function learnerDisplayOnlyTokenClone2(node, overrides = {}) {
        if (!node || node.kind !== "token") return node;
        const note = Object.prototype.hasOwnProperty.call(overrides, "note") ? overrides.note : node.note;
        const learnerGlossLines = overrides.learner_gloss_lines || (note ? [note] : []);
        return {
          ...node,
          note,
          display_surface: overrides.display_surface || node.display_surface,
          trace: {
            ...node.trace || {},
            ...learnerGlossLines.length ? { learner_gloss_lines: learnerGlossLines } : {}
          }
        };
      }
      function contextualLearnerRoleOnlyTokenClone2(node, overrides = {}) {
        if (!node || node.kind !== "token") return node;
        const surface = node.surface;
        const syntax = overrides.syntax || node.syntax || "";
        const label = normalizeLearnerLabel2(overrides.label || node.label || "neutral", surface, syntax);
        const note = Object.prototype.hasOwnProperty.call(overrides, "note") ? overrides.note : node.note;
        const learnerGlossLines = overrides.learner_gloss_lines || overrides.trace_detail && overrides.trace_detail.learner_gloss_lines || (note ? [note] : []);
        return {
          ...node,
          label,
          role: label,
          syntax,
          note,
          features: { ...node.features || {}, label, syntax, pos: overrides.pos || node.features && node.features.pos || "" },
          trace: {
            ...node.trace || {},
            ...learnerGlossLines.length ? { learner_gloss_lines: learnerGlossLines } : {},
            contextual_learner_role_override: {
              lexical_default_role: node.label || "",
              active_role: label,
              reason: overrides.reason || "Learner display role is adjusted contextually without changing parser or semantic provenance."
            }
          }
        };
      }
      return {
        pronunciationOnlyJyutpingForUnknown: pronunciationOnlyJyutpingForUnknown2,
        token: token3,
        textNode: textNode2,
        construction: construction2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        learnerDisplayOnlyTokenClone: learnerDisplayOnlyTokenClone2,
        contextualLearnerRoleOnlyTokenClone: contextualLearnerRoleOnlyTokenClone2
      };
    };
  }
});

// src/parser/detectors/complements/result.js
var require_result = __commonJS({
  "src/parser/detectors/complements/result.js"(exports2, module2) {
    "use strict";
    function createResultFramePartClone(dependencies = {}) {
      const { firstToken: firstToken2, flattenSurface: flattenSurface2, parserInactiveTokenClone: parserInactiveTokenClone2, token: token3 } = dependencies;
      function resultFramePartClone2(node, overrides = {}) {
        const surface = overrides.surface || flattenSurface2(node);
        const base = firstToken2(node) || token3(surface);
        return parserInactiveTokenClone2(base, {
          label: overrides.label || base.label || "func",
          pos: overrides.pos || (overrides.label === "doing" ? "verb" : overrides.label === "particle" ? "particle" : overrides.label === "when" ? "adverbial" : "function"),
          syntax: overrides.syntax || base.syntax || "result_frame_part",
          slots: overrides.slots || [],
          reason: overrides.reason || "Token is parser-inactive inside a bounded v0.5.34 change/result frame; the parent exposes the result relation while child tokens stay visible."
        });
      }
      return resultFramePartClone2;
    }
    function createResultComplementDetectors(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nominalComplementFromNodes: nominalComplementFromNodes2,
        resultFramePartClone: resultFramePartClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function resultTopicFromNodes2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        if (compact.length === 1 && (nodeCanFillSlot2(compact[0], "topic") || nodeCanFillSlot2(compact[0], "subject") || nodeCanFillSlot2(compact[0], "location") || nodeCanFillSlot2(compact[0], "np") || nodeCanFillSlot2(compact[0], "head_noun"))) return compact[0];
        const templated = categorySubspanFor2(compact, ["OvertHeadDemonstrativeClassifierNP", "QuantifiedClassifierNP", "DiMarkedNP", "OrdinalClassifierNP", "NominalHeadSpan", "CoordinatedNP"]);
        if (templated && (nodeCanFillSlot2(templated, "topic") || nodeCanFillSlot2(templated, "subject") || nodeCanFillSlot2(templated, "np") || nodeCanFillSlot2(templated, "head_noun"))) return templated;
        return null;
      }
      function resultComplementFromNodes2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        const nominal = nominalComplementFromNodes2(compact);
        if (nominal) return nominal;
        if (compact.length === 1 && (nodeCanFillSlot2(compact[0], "object") || nodeCanFillSlot2(compact[0], "np") || nodeCanFillSlot2(compact[0], "head_noun") || nodeCanFillSlot2(compact[0], "location"))) return compact[0];
        return null;
      }
      function makeChangeIntoPredicate2(changeNode, complement) {
        if (!isToken2(changeNode, "變成") || !isToken2(complement, "點")) return null;
        const children = [
          resultFramePartClone2(changeNode, {
            label: "doing",
            pos: "verb",
            syntax: "change_into_verb result_change_verb",
            slots: ["change_verb", "action_verb", "main_verb", "predicate"],
            reason: "變成 is the overt lexical change head in the exact AA11 變成 + 點 result-state/outcome wh profile."
          }),
          complement
        ];
        return construction2("ChangeIntoPredicate", "變成", children, {
          note: "AA11 exact result-state/outcome wh predicate: adjacent 變成 + 點 only. Other 變成 complements remain outside this identity.",
          slots: templateDerivedSlots2("ChangeIntoPredicate", children),
          trace: traceInfo2("generative_template", {
            construction_type: "ChangeIntoPredicate",
            template_family: "construction_template",
            structural_scope: "vp",
            rule: "exact adjacent 變成 + 點",
            template: ["change_verb!", "result_complement!"],
            constraints: {
              slot_surface_in: {
                change_verb: ["變成"],
                result_complement: ["點"]
              },
              exact_adjacent_surface: true
            },
            result_profile: "dim_result_state_or_outcome_wh",
            assigned_slots: ["change_verb", "result_complement"],
            surfaces: children.map((node) => flattenSurface2(node)),
            subspan: true,
            reason: "The reviewed AA11 identity is only exact adjacent 變成 + 點. Generic lexical 變成 valency, 點樣/乜嘢, nominal results, aspectual results, and other change/result families do not inherit this construction identity.",
            not_claims: [
              "not_generic_change_into_complement",
              "not_binsing_dimjoeng",
              "not_binsing_matje",
              "not_nominal_result",
              "not_aspectual_result",
              "not_hidden_result_argument"
            ]
          })
        });
      }
      function wrapChangeIntoPredicateSubspans2(nodes) {
        const result = [];
        let i = 0;
        while (i < nodes.length) {
          if (isToken2(nodes[i], "變成") && isToken2(nodes[i + 1], "點")) {
            const exactAa11 = makeChangeIntoPredicate2(nodes[i], nodes[i + 1]);
            if (exactAa11) {
              result.push(exactAa11);
              i += 2;
              continue;
            }
          }
          result.push(nodes[i]);
          i += 1;
        }
        return result;
      }
      return {
        resultTopicFromNodes: resultTopicFromNodes2,
        resultComplementFromNodes: resultComplementFromNodes2,
        makeChangeIntoPredicate: makeChangeIntoPredicate2,
        wrapChangeIntoPredicateSubspans: wrapChangeIntoPredicateSubspans2
      };
    }
    module2.exports = { createResultFramePartClone, createResultComplementDetectors };
  }
});

// src/parser/detectors/np/core.js
var require_core = __commonJS({
  "src/parser/detectors/np/core.js"(exports2, module2) {
    "use strict";
    module2.exports = function createNpDetectors2(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        hasSurface: hasSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        resultFramePartClone: resultFramePartClone2,
        templateConstructionFor: templateConstructionFor2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      const COMPOSITIONAL_NP_SUBSPAN_TYPES = [
        "OvertHeadDemonstrativeClassifierNP",
        "QuantifiedClassifierNP",
        "AssociativeNP",
        "OrdinalClassifierNP",
        "ClassifierObjectNP"
      ];
      function compositionalNpSubspanFor2(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length || compact.some((node) => node.kind === "text")) return null;
        if (compact.length === 3 && (isToken2(compact[1], "同") || isToken2(compact[1], "同埋"))) {
          const coordinated = coordinatedNPFromParts2(compact);
          if (coordinated) return coordinated;
        }
        return categorySubspanFor2(compact, COMPOSITIONAL_NP_SUBSPAN_TYPES);
      }
      function wrapCompositionalNpSubspans2(nodes = []) {
        const result = [];
        let index = 0;
        const maxWindow = 7;
        while (index < nodes.length) {
          let match = null;
          for (let length = Math.min(maxWindow, nodes.length - index); length >= 2; length -= 1) {
            const candidate = compositionalNpSubspanFor2(nodes.slice(index, index + length));
            if (!candidate) continue;
            if (candidate.type === "QuantifiedClassifierNP") {
              if (candidate.trace && candidate.trace.fragment_subtype === "quantified_classifier_head_ellipsis") continue;
              const head = (candidate.children || [])[candidate.children.length - 1];
              const headSlots = nodeSlots2(head);
              const next = nodes[index + length];
              if (headSlots.includes("time") || headSlots.includes("time_head") || flattenSurface2(head) === "字") continue;
              if (next && nodeCanFillSlot2(next, "post_classifier_approximation")) continue;
            }
            match = { candidate, length };
            break;
          }
          if (match) {
            result.push(match.candidate);
            index += match.length;
          } else {
            result.push(nodes[index]);
            index += 1;
          }
        }
        return result;
      }
      function shouldDeferApproximateQuantityForUnlicensedGovernor2(candidate, nodes, index) {
        if (!candidate || candidate.type !== "ApproximateQuantity") return false;
        if (index === 0) return false;
        const governor = nodes[index - 1];
        return !nodeCanFillSlot2(governor, "consumption_verb");
      }
      function deicticClassifierTopicFromParts2(parts) {
        if (!parts || parts.length !== 2) return null;
        const [dem, classifier] = parts;
        if (!nodeCanFillSlot2(dem, "demonstrative") || !nodeCanFillSlot2(classifier, "classifier")) return null;
        return construction2("Topic", "Topic", parts, {
          note: "Demonstrative-classifier topic: demonstrative + classifier, e.g. 呢部 = this one. The old fused DeicticClassifierTopic label is retired; Topic carries the clause/discourse role while deictic/classifier metadata preserves the NP-internal form.",
          slots: cleanSlots2(["topic", "np", "demonstrative", "classifier", "deictic_classifier_topic"]),
          trace: traceInfo2("generative_template", {
            construction_type: "Topic",
            retired_label_alias: "DeicticClassifierTopic",
            template: ["demonstrative!", "classifier!"],
            assigned_slots: ["demonstrative", "classifier"],
            topic_subtype: "deictic_classifier_ellipsis",
            np_subtype: "demonstrative_classifier_ellipsis",
            surfaces: parts.map((node) => flattenSurface2(node)),
            subspan: true,
            reason: "The phrase is a Topic in this frame; demonstrative/classifier details are metadata rather than a fused active construction label."
          })
        });
      }
      function nominalComplementFromNodes2(nodes) {
        if (!nodes || !nodes.length) return null;
        if (nodes.length === 1 && (nodeCanFillSlot2(nodes[0], "np") || nodeCanFillSlot2(nodes[0], "head_noun"))) return nodes[0];
        const templated = templateConstructionFor2(nodes, ["QuantifiedClassifierNP"]) || categorySubspanFor2(nodes, ["QuantifiedClassifierNP", "OvertHeadDemonstrativeClassifierNP", "OrdinalClassifierNP", "ModifiedNP", "ModifierNP", "NominalHeadSpan"]);
        if (templated && (nodeCanFillSlot2(templated, "np") || nodeCanFillSlot2(templated, "head_noun"))) return templated;
        const last = nodes[nodes.length - 1];
        if (!nodeCanFillSlot2(last, "head_noun") && !nodeCanFillSlot2(last, "np")) return null;
        const hasVisibleStativeOrLinker = nodes.some((node) => nodeCanFillSlot2(node, "how") || nodeCanFillSlot2(node, "nominal_linker"));
        if (!hasVisibleStativeOrLinker) return null;
        return construction2("StativeNominalComplement", "AdjNP", nodes, {
          note: "Stative/modified nominal complement inside a bounded copular relation frame; requires a visible stative modifier or nominal linker.",
          slots: templateDerivedSlots2("StativeNominalComplement", nodes),
          trace: traceInfo2("generative_template", {
            construction_type: "StativeNominalComplement",
            template: nodes.map((node) => nodeCanFillSlot2(node, "how") ? "how?" : nodeCanFillSlot2(node, "nominal_linker") ? "nominal_linker?" : nodeCanFillSlot2(node, "head_noun") ? "head_noun!" : "modifier?"),
            assigned_slots: nodes.map((node) => nodeCanFillSlot2(node, "how") ? "how" : nodeCanFillSlot2(node, "nominal_linker") ? "nominal_linker" : nodeCanFillSlot2(node, "head_noun") ? "head_noun" : "modifier"),
            surfaces: nodes.map((node) => flattenSurface2(node)),
            subspan: true,
            reason: "v0.5.39 scope guard prevents bare/simple NPs from being mislabeled as stative nominal complements."
          })
        });
      }
      function coordinatedNPFromParts2(parts) {
        if (!parts || parts.length !== 3) return null;
        const [left, coordinator, right] = parts;
        if (!nodeCanFillSlot2(left, "np") || !(isToken2(coordinator, "同") || isToken2(coordinator, "同埋")) || !nodeCanFillSlot2(right, "np")) return null;
        const coord = parserInactiveTokenClone2(coordinator, {
          label: "func",
          pos: "function",
          syntax: "coordinator",
          slots: ["coordinator"],
          reason: `${flattenSurface2(coordinator)} is interpreted as the coordinator inside a bounded coordinated NP, not as a coverb/comitative marker.`
        });
        const children = [left, coord, right];
        return construction2("CoordinatedNP", "CoordNP", children, {
          note: "Coordinated NP: left NP + 同 + right NP. Subject use is assigned only by a larger clause.",
          slots: cleanSlots2(["coordinated_np", "left_np", "right_np", "coordinator", "np", "topic", "object"]),
          trace: traceInfo2("generative_template", {
            construction_type: "CoordinatedNP",
            template: ["left_np!", "coordinator!", "right_np!"],
            assigned_slots: ["left_np", "coordinator", "right_np"],
            surfaces: children.map((node) => flattenSurface2(node)),
            subspan: true
          })
        });
      }
      function possessiveClassifierNPFromNodes2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length !== 3) return null;
        const possessor = compact[0];
        if (!nodeCanFillSlot2(possessor, "subject") && !nodeCanFillSlot2(possessor, "np")) return null;
        if (!nodeCanFillSlot2(compact[1], "classifier")) return null;
        if (!nodeCanFillSlot2(compact[2], "head_noun") && !nodeCanFillSlot2(compact[2], "object")) return null;
        const nounPhraseNodes = compact.slice(1);
        const nounPhrase = categorySubspanFor2(nounPhraseNodes, ["ModifiedNP", "NominalHeadSpan"]);
        if (!nounPhrase) return null;
        if (!nodeCanFillSlot2(nounPhrase, "np") && !nodeCanFillSlot2(nounPhrase, "head_noun")) return null;
        const possessorChild = resultFramePartClone2(possessor, {
          label: (firstToken2(possessor) || possessor).label || "who",
          pos: "np",
          syntax: `${possessor.syntax || "possessor_np"} possessive_np`,
          slots: ["possessor", "np", "subject", "topic"],
          reason: "This overt NP is the possessor in the sourced possessor + classifier + noun profile."
        });
        return construction2("PossessiveClassifierNP", "PossNP", [possessorChild, nounPhrase], {
          note: "Source-linked possessive classifier NP: possessor + overt classifier + overt nominal head, e.g. 我架車 = my car.",
          slots: templateDerivedSlots2("PossessiveClassifierNP", [possessorChild, nounPhrase]),
          trace: traceInfo2("generative_template", {
            construction_type: "PossessiveClassifierNP",
            template: ["possessor!", "classifier_np!"],
            assigned_slots: ["possessor", "classifier_np"],
            surfaces: [flattenSurface2(possessor), flattenSurface2(nounPhrase)],
            subspan: true,
            reason: "The retained node requires the overt classifier and nominal head documented for Cantonese POSS-CL-N phrases."
          })
        });
      }
      function wrapPossessiveClassifierNPSubspans2(nodes) {
        const result = [];
        let i = 0;
        while (i < nodes.length) {
          const possessive = possessiveClassifierNPFromNodes2(nodes.slice(i, i + 3));
          if (possessive) {
            result.push(possessive);
            i += 3;
            continue;
          }
          result.push(nodes[i]);
          i += 1;
        }
        return result;
      }
      function classifierObjectNPFromNodes2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length !== 2) return null;
        const [classifier, head] = compact;
        if (!nodeCanFillSlot2(classifier, "classifier")) return null;
        if (!nodeCanFillSlot2(head, "head_noun") && !nodeCanFillSlot2(head, "object") && !nodeCanFillSlot2(head, "np")) return null;
        return construction2("ClassifierObjectNP", "CL-NP", [classifier, head], {
          note: "v0.5.35 classifier-headed object NP without overt numeral/demonstrative, e.g. 樣驚喜.",
          slots: templateDerivedSlots2("ClassifierObjectNP", [classifier, head]),
          trace: traceInfo2("generative_template", {
            construction_type: "ClassifierObjectNP",
            template: ["classifier!", "head_noun!"],
            assigned_slots: ["classifier", "head_noun"],
            surfaces: [flattenSurface2(classifier), flattenSurface2(head)],
            subspan: true
          })
        });
      }
      function coordinatedNPFragmentFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length !== 3 || particles.length) return null;
        const coord = coordinatedNPFromParts2(compact);
        if (!coord) return null;
        return construction2("CoordinatedNP", "CoordNP", coord.children, {
          note: "Standalone coordinated NP fragment: left NP + 同 + right NP. This prevents the former false-positive guardrail where 我同你 merely had no top construction without leaking clause-subject slots into the NP.",
          slots: cleanSlots2(["coordinated_np", "left_np", "right_np", "coordinator", "np", "topic", "object"]),
          trace: traceInfo2("generative_template", {
            construction_type: "CoordinatedNP",
            template_family: "generative_template",
            template: ["left_np!", "coordinator!", "right_np!"],
            assigned_slots: ["left_np", "coordinator", "right_np"],
            surfaces: coord.children.map((node) => flattenSurface2(node)),
            reason: "Direct diagnostic review rejected bare [] as a successful negative guardrail and rejected the intermediate subject+coordinator+subject template as clause-role leakage. 我同你 is a coordinated NP/fragment, not a parse absence and not automatically a coordinated subject.",
            not_claims: ["not_coverb_frame", "not_parse_absence", "not_clause_subject_assignment"]
          })
        });
      }
      function approximateQuantityFallback2(core) {
        if (!hasSurface2(core, "左右")) return null;
        return construction2("ApproximateQuantity", "Approx", core, {
          note: "Approximate quantity/price fallback with 左右.",
          trace: traceInfo2("construction_function", {
            construction_type: "ApproximateQuantity",
            reason: "Fallback only; generative ApproximateQuantity should normally catch this."
          })
        });
      }
      return {
        approximateQuantityFallback: approximateQuantityFallback2,
        classifierObjectNPFromNodes: classifierObjectNPFromNodes2,
        compositionalNpSubspanFor: compositionalNpSubspanFor2,
        coordinatedNPFragmentFallback: coordinatedNPFragmentFallback2,
        coordinatedNPFromParts: coordinatedNPFromParts2,
        deicticClassifierTopicFromParts: deicticClassifierTopicFromParts2,
        nominalComplementFromNodes: nominalComplementFromNodes2,
        possessiveClassifierNPFromNodes: possessiveClassifierNPFromNodes2,
        shouldDeferApproximateQuantityForUnlicensedGovernor: shouldDeferApproximateQuantityForUnlicensedGovernor2,
        wrapCompositionalNpSubspans: wrapCompositionalNpSubspans2,
        wrapPossessiveClassifierNPSubspans: wrapPossessiveClassifierNPSubspans2
      };
    };
  }
});

// src/parser/tokenization/lexical-selection.js
var require_lexical_selection = __commonJS({
  "src/parser/tokenization/lexical-selection.js"(exports2, module2) {
    "use strict";
    module2.exports = function createLexicalSelection(dependencies = {}) {
      const {
        TOKEN_LEXICON: TOKEN_LEXICON2,
        PRODUCTIVE_VO: PRODUCTIVE_VO2,
        COMPOSITIONAL_LEXICAL_PHRASES: COMPOSITIONAL_LEXICAL_PHRASES2,
        inferTokenFeatures: inferTokenFeatures2,
        featureBundleFor: featureBundleFor2,
        getLexicalizationType: getLexicalizationType2
      } = dependencies;
      function lexicalizedStativeRegistryKind2(surface) {
        const entry = TOKEN_LEXICON2[surface] || {};
        const label = entry.label || "neutral";
        const syntax = entry.syntax || "";
        const features = inferTokenFeatures2(surface, { ...entry, label, syntax }, { label, syntax });
        const bundle = featureBundleFor2(surface, { ...entry, label, syntax }, features, []);
        const lexicalizationType = getLexicalizationType2(bundle);
        if (lexicalizationType === "negative_lexicalized_stative") return "negative_lexicalized_stative_registry";
        if (lexicalizationType === "lexicalized_stative") return "lexicalized_stative_registry";
        return "";
      }
      function neutralFrequencyCoverageFallback(surface) {
        const entry = TOKEN_LEXICON2[surface] || {};
        if (String(entry.pos || "") !== "lexical_item") return false;
        if (!String(entry.syntax || "").split(/\s+/u).includes("lexical_item")) return false;
        return String(entry.note || "").includes("Exact surface retained as neutral lexical coverage");
      }
      function transparentClassifierObjectParts2(surface) {
        const phrase = String(surface || "");
        const entry = TOKEN_LEXICON2[phrase] || {};
        const syntax = String(entry.syntax || "");
        if (!syntax.split(/\s+/u).includes("classifier_object_np")) return null;
        const classifier = Object.keys(TOKEN_LEXICON2).filter((candidate) => candidate.length < phrase.length && phrase.startsWith(candidate)).filter((candidate) => (TOKEN_LEXICON2[candidate] || {}).label === "measure_word").sort((a, b) => b.length - a.length || a.localeCompare(b))[0];
        if (!classifier) return null;
        const head = phrase.slice(classifier.length);
        const headEntry = TOKEN_LEXICON2[head] || {};
        if (!head || !["what", "who", "where"].includes(headEntry.label || "")) return null;
        return { classifier, head };
      }
      function shouldForceCompositional2(surface) {
        if (lexicalizedStativeRegistryKind2(surface)) return false;
        if (transparentClassifierObjectParts2(surface)) return true;
        return COMPOSITIONAL_LEXICAL_PHRASES2.has(surface);
      }
      const ALL_LEXICON_TERMS2 = Object.keys(TOKEN_LEXICON2).filter((surface) => !PRODUCTIVE_VO2[surface]).sort((a, b) => b.length - a.length || a.localeCompare(b));
      const LEXICON_TERMS2 = ALL_LEXICON_TERMS2.filter((surface) => !shouldForceCompositional2(surface)).sort((a, b) => b.length - a.length || a.localeCompare(b));
      const LEXICALIZED_STATIVE_SELECTION_WEIGHT2 = 1e4;
      const NEUTRAL_FREQUENCY_FALLBACK_PENALTY = 1e3;
      function lexicalCandidateScore2(surface) {
        const registryKind = lexicalizedStativeRegistryKind2(surface);
        const registryScore = registryKind ? LEXICALIZED_STATIVE_SELECTION_WEIGHT2 : 0;
        return registryScore + surface.length;
      }
      function lexicalCandidatePriorityScore(surface) {
        const fallbackPenalty = neutralFrequencyCoverageFallback(surface) ? NEUTRAL_FREQUENCY_FALLBACK_PENALTY : 0;
        return lexicalCandidateScore2(surface) - fallbackPenalty;
      }
      function followingLexicalizedStativeAfterChoice2(choice, rest) {
        const afterChoice = String(rest || "").slice(String(choice || "").length);
        if (!afterChoice) return null;
        const following = ALL_LEXICON_TERMS2.filter((surface) => afterChoice.startsWith(surface) && lexicalizedStativeRegistryKind2(surface)).sort((a, b) => b.length - a.length || a.localeCompare(b))[0];
        if (!following) return null;
        return { surface: following, registry_kind: lexicalizedStativeRegistryKind2(following) };
      }
      function lexicalSelectionReason2(choice, candidates, excluded, rest = "") {
        const registryKind = lexicalizedStativeRegistryKind2(choice);
        const competingRegistry = candidates.filter((surface) => surface !== choice && lexicalizedStativeRegistryKind2(surface)).map((surface) => lexicalizedStativeRegistryKind2(surface));
        const hasLongerExcluded = excluded.some((item) => item.length > choice.length);
        const followingRegistry = followingLexicalizedStativeAfterChoice2(choice, rest);
        const displacedNeutralFallback = candidates.some((surface) => surface !== choice && neutralFrequencyCoverageFallback(surface) && lexicalCandidatePriorityScore(surface) < lexicalCandidatePriorityScore(choice));
        if (registryKind) {
          return `${registryKind} candidate wins over compositional or shorter candidates by lexicalized-stative selection scoring.`;
        }
        if (hasLongerExcluded) {
          return "Chosen after longer forced-compositional phrase was excluded so learner-visible internal structure can remain available.";
        }
        if (displacedNeutralFallback && !neutralFrequencyCoverageFallback(choice)) {
          return "Chosen over a longer neutral frequency-list coverage fallback because independently typed lexical evidence has higher parser priority.";
        }
        if (followingRegistry && choice === "好") {
          return `Chosen as degree modifier before following ${followingRegistry.registry_kind} candidate ${followingRegistry.surface}; learner-visible composition remains available.`;
        }
        if (competingRegistry.length) {
          return "Chosen by score/length after comparing with registry-backed candidates.";
        }
        return "Chosen by ordinary lexical lookup; no registry-backed lexicalized stative candidate applies at this cursor.";
      }
      function lexicalSelectionDecision2(rest, candidates, excluded, choice) {
        return {
          rule: "prefer_lexicalized_stative_over_compositional_when_registry_evidence_exists",
          chosen: choice,
          chosen_registry_kind: lexicalizedStativeRegistryKind2(choice),
          chosen_score: lexicalCandidateScore2(choice),
          reason: lexicalSelectionReason2(choice, candidates, excluded, rest),
          candidates: candidates.map((surface) => ({
            surface,
            registry_kind: lexicalizedStativeRegistryKind2(surface),
            score: lexicalCandidateScore2(surface),
            forced_compositional: false
          })),
          excluded_compositional_candidates: excluded.map((surface) => ({
            surface,
            registry_kind: lexicalizedStativeRegistryKind2(surface),
            forced_compositional: true,
            reason: transparentClassifierObjectParts2(surface) ? "Explicit classifier_object_np reference entry split into an independently evidenced classifier plus noun head." : "Listed in COMPOSITIONAL_LEXICAL_PHRASES and not protected by lexicalized-stative registry evidence."
          })),
          rest_preview: rest.slice(0, 12)
        };
      }
      function selectionDecisionForSurface2(surface, rest = surface) {
        const candidates = LEXICON_TERMS2.filter((candidate) => rest.startsWith(candidate));
        const excluded = ALL_LEXICON_TERMS2.filter((candidate) => rest.startsWith(candidate) && shouldForceCompositional2(candidate));
        const fallbackCandidates = candidates.includes(surface) ? candidates : [surface, ...candidates];
        return lexicalSelectionDecision2(rest, fallbackCandidates, excluded, surface);
      }
      function selectLexiconTerm2(rest) {
        const candidates = LEXICON_TERMS2.filter((surface2) => rest.startsWith(surface2)).filter((surface2) => {
          if (surface2 !== "第二個") return true;
          const after = rest.slice(surface2.length);
          const overtHead = Object.keys(TOKEN_LEXICON2).filter((candidate) => after.startsWith(candidate)).filter((candidate) => ["what", "who", "where"].includes((TOKEN_LEXICON2[candidate] || {}).label || "")).sort((a, b) => b.length - a.length || a.localeCompare(b))[0];
          return !overtHead;
        });
        if (!candidates.length) return null;
        const excluded = ALL_LEXICON_TERMS2.filter((surface2) => rest.startsWith(surface2) && shouldForceCompositional2(surface2));
        candidates.sort((a, b) => {
          const priorityDiff = lexicalCandidatePriorityScore(b) - lexicalCandidatePriorityScore(a);
          if (priorityDiff) return priorityDiff;
          const scoreDiff = lexicalCandidateScore2(b) - lexicalCandidateScore2(a);
          if (scoreDiff) return scoreDiff;
          const lengthDiff = b.length - a.length;
          if (lengthDiff) return lengthDiff;
          return a.localeCompare(b);
        });
        const surface = candidates[0];
        return {
          surface,
          registry_kind: lexicalizedStativeRegistryKind2(surface),
          score: lexicalCandidateScore2(surface),
          candidate_count: candidates.length,
          selection_decision: lexicalSelectionDecision2(rest, candidates, excluded, surface)
        };
      }
      return {
        lexicalizedStativeRegistryKind: lexicalizedStativeRegistryKind2,
        neutralFrequencyCoverageFallback,
        transparentClassifierObjectParts: transparentClassifierObjectParts2,
        shouldForceCompositional: shouldForceCompositional2,
        ALL_LEXICON_TERMS: ALL_LEXICON_TERMS2,
        LEXICON_TERMS: LEXICON_TERMS2,
        LEXICALIZED_STATIVE_SELECTION_WEIGHT: LEXICALIZED_STATIVE_SELECTION_WEIGHT2,
        NEUTRAL_FREQUENCY_FALLBACK_PENALTY,
        lexicalCandidateScore: lexicalCandidateScore2,
        lexicalCandidatePriorityScore,
        followingLexicalizedStativeAfterChoice: followingLexicalizedStativeAfterChoice2,
        lexicalSelectionReason: lexicalSelectionReason2,
        lexicalSelectionDecision: lexicalSelectionDecision2,
        selectionDecisionForSurface: selectionDecisionForSurface2,
        selectLexiconTerm: selectLexiconTerm2
      };
    };
  }
});

// src/runtime-resources/grammar/classifier-head-rules.js
var require_classifier_head_rules = __commonJS({
  "src/runtime-resources/grammar/classifier-head-rules.js"(exports2, module2) {
    "use strict";
    var model = require_unit_word_evidence();
    var rules = Object.fromEntries(
      model.noun_choice_rule_records.map((record) => [
        record.surface,
        Object.freeze(record.current_head_classes.slice())
      ])
    );
    module2.exports = Object.freeze(rules);
  }
});

// src/parser/detectors/discourse/protected-formulas.js
var require_protected_formulas = __commonJS({
  "src/parser/detectors/discourse/protected-formulas.js"(exports2, module2) {
    "use strict";
    var AGREEMENT_RESPONSE_PARTICLES = /* @__PURE__ */ new Set(["呀", "啊", "喇"]);
    module2.exports = function createProtectedFormulaDetectors(dependencies = {}) {
      const {
        construction: construction2,
        flattenSurface: flattenSurface2,
        isParticle: isParticle2,
        isToken: isToken2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        traceInfo: traceInfo2
      } = dependencies;
      function agreementResponsePartClone(node, overrides = {}) {
        return parserInactiveTokenClone2(node, {
          label: overrides.label || (isParticle2(node) ? "particle" : "func"),
          pos: overrides.pos || (isParticle2(node) ? "particle" : "function"),
          syntax: overrides.syntax || (isParticle2(node) ? "sentence_final_particle" : "agreement_confirmation_marker"),
          slots: overrides.slots || (isParticle2(node) ? ["particle"] : ["agreement_marker", "confirmation_marker"]),
          reason: overrides.reason || "Token is parser-inactive inside an agreement/confirmation response formula; the parent exposes the discourse-response function."
        });
      }
      function makeAgreementResponseFormula(markerNode, particleNode) {
        const particleSurface = flattenSurface2(particleNode);
        const children = [
          agreementResponsePartClone(markerNode, {
            label: "func",
            pos: "function",
            syntax: "agreement_confirmation_marker",
            slots: ["agreement_marker", "confirmation_marker"],
            reason: "係 is interpreted here only as an agreement/confirmation response marker, not as a broad copula-clause rule."
          }),
          agreementResponsePartClone(particleNode, {
            label: "particle",
            pos: "particle",
            syntax: "sentence_final_particle acknowledgement_particle",
            slots: ["particle"],
            reason: `${particleSurface} stays parser-inactive inside the agreement/confirmation response formula.`
          })
        ];
        return construction2("FormulaDiscourseUnit", "Formula", children, {
          slots: ["formula_discourse_unit", "formula", "discourse_response", "agreement_response", "confirmation_response"],
          note: "Agreement/confirmation response formula: 係 + an approved acknowledgement particle. The wrapper does not introduce a general copula-clause rule.",
          trace: traceInfo2("generative_template", {
            construction_type: "FormulaDiscourseUnit",
            retired_label_alias: "AgreementResponseFormula",
            template: ["agreement_marker!", "particle!"],
            assigned_slots: ["agreement_marker", "particle"],
            rule: "agreement marker 係 + approved acknowledgement particle 呀/啊/喇",
            pattern: "agreement_marker + acknowledgement_particle",
            reason: "Short 係 + particle responses function as agreement/acknowledgement formulae. Keep both children visible and avoid broadening 係 into a general copula parser here.",
            surfaces: children.map((node) => flattenSurface2(node))
          })
        });
      }
      function agreementResponsePatternAt(nodes, index) {
        if (!isToken2(nodes[index], "係")) return null;
        if (!isParticle2(nodes[index + 1])) return null;
        const particleSurface = flattenSurface2(nodes[index + 1]);
        if (!AGREEMENT_RESPONSE_PARTICLES.has(particleSurface)) return null;
        return { length: 2, marker: nodes[index], particle: nodes[index + 1] };
      }
      function wrapAgreementResponseSubspans2(nodes) {
        const result = [];
        let i = 0;
        while (i < nodes.length) {
          const match = agreementResponsePatternAt(nodes, i);
          if (match) {
            result.push(makeAgreementResponseFormula(match.marker, match.particle));
            i += match.length;
            continue;
          }
          result.push(nodes[i]);
          i += 1;
        }
        return result;
      }
      return {
        wrapAgreementResponseSubspans: wrapAgreementResponseSubspans2
      };
    };
  }
});

// src/parser/detectors/discourse/priority.js
var require_priority = __commonJS({
  "src/parser/detectors/discourse/priority.js"(exports2, module2) {
    "use strict";
    module2.exports = function createPriorityDetectors2(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isParticle: isParticle2,
        isToken: isToken2,
        isVerbLike: isVerbLike2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function isPriorityActionCandidate(node) {
        if (!node || node.kind === "text") return false;
        if (node.kind === "construction") {
          const slots = nodeSlots2(node);
          return slots.includes("vp") || slots.includes("action_vp") || slots.includes("predicate") || slots.includes("directional_motion_vp") || slots.includes("productive_vo");
        }
        return isVerbLike2(node);
      }
      function priorityMarkerTokenClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "how",
          pos: "adverbial",
          syntax: "priority_sequence_marker",
          slots: ["priority_marker", "sequence_marker", "how"],
          reason: "先 is being interpreted as a priority/sequence marker here, so it stays parser-inactive inside the parent clause wrapper."
        });
      }
      function priorityParticleClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle",
          slots: ["particle"],
          reason: "Final particle stays parser-inactive inside a priority-marker clause wrapper."
        });
      }
      function makePriorityMarkerClause(actionNode, markerNode, particleNode = null) {
        const children = [actionNode, priorityMarkerTokenClone(markerNode)];
        if (particleNode) children.push(priorityParticleClone(particleNode));
        return construction2("PriorityMarkerClause", "Priority先", children, {
          slots: ["priority_marker_clause", "sequence_priority_marker", "priority_marker", "vp", "action_vp", "predicate"],
          note: "Priority/sequence marker clause: action + 先 + optional final particle, e.g. 你食先啦 = you eat first.",
          trace: traceInfo2("generative_template", {
            construction_type: "PriorityMarkerClause",
            template_family: "generative_template",
            template: particleNode ? ["action_vp!", "priority_marker!", "particle?"] : ["action_vp!", "priority_marker!"],
            assigned_slots: particleNode ? ["action_vp", "priority_marker", "particle"] : ["action_vp", "priority_marker"],
            pattern: particleNode ? "predicate + priority_marker + final_particle" : "predicate + priority_marker",
            reason: "Native speech uses 先 after an action predicate to mark priority/order ('do this first'); the action remains visible as the child predicate.",
            surfaces: children.map((node) => flattenSurface2(node))
          })
        });
      }
      function priorityMarkerClauseWithTrailingParticle2(node, particleNode) {
        if (!node || node.kind !== "construction" || node.type !== "PriorityMarkerClause" || !particleNode) return node;
        if ((node.children || []).some((child) => isParticle2(child))) return node;
        return {
          ...node,
          children: [...node.children || [], priorityParticleClone(particleNode)],
          trace: {
            ...node.trace || {},
            pattern: "predicate + priority_marker + final_particle",
            attached_trailing_particle: flattenSurface2(particleNode)
          }
        };
      }
      function priorityMarkerPatternAt(nodes, index) {
        if (!isPriorityActionCandidate(nodes[index])) return null;
        if (!isToken2(nodes[index + 1], "先")) return null;
        if (isParticle2(nodes[index + 2]) && ["啦", "喇", "呀", "啊"].includes(flattenSurface2(nodes[index + 2]))) {
          return { length: 3, particle: nodes[index + 2] };
        }
        return { length: 2, particle: null };
      }
      function wrapPriorityMarkerSubspans2(nodes) {
        const result = [];
        let i = 0;
        while (i < nodes.length) {
          const match = priorityMarkerPatternAt(nodes, i);
          if (match) {
            result.push(makePriorityMarkerClause(nodes[i], nodes[i + 1], match.particle));
            i += match.length;
            continue;
          }
          result.push(nodes[i]);
          i += 1;
        }
        return result;
      }
      function sourceLinkedPriorityMarkerClauseFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3 || !nodeCanFillSlot2(compact[0], "subject")) return null;
        const markerIndex = compact.findIndex((node, index) => index > 1 && isToken2(node, "先"));
        if (markerIndex < 0 || markerIndex !== compact.length - 1) return null;
        const action = categorySubspanFor2(compact.slice(1, markerIndex), ["ProductiveVO", "TransitiveVP"]);
        if (!action || flattenSurface2(action) !== "打電話") return null;
        if (particles.length !== 1 || !isToken2(particles[0], "啦")) return null;
        const children = [compact[0], action, priorityMarkerTokenClone(compact[markerIndex]), priorityParticleClone(particles[0])];
        return construction2("PriorityMarkerClause", "Priority先", children, {
          note: "Source-linked postverbal 先 profile in 你打電話先啦.",
          slots: cleanSlots2(["priority_marker_clause", "sequence_priority_marker", "priority_marker", "subject", "vp", "action_vp", "predicate"]),
          trace: traceInfo2("generative_template", {
            construction_type: "PriorityMarkerClause",
            template: ["subject!", "action_vp!", "priority_marker!", "particle!"],
            constraints: { surface_sequence: "你打電話先啦" },
            assigned_slots: ["subject", "action_vp", "priority_marker", "particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Retains the exact attested action + postverbal 先 + 啦 profile while excluding preverbal and deferral uses."
          })
        });
      }
      return {
        priorityMarkerClauseWithTrailingParticle: priorityMarkerClauseWithTrailingParticle2,
        sourceLinkedPriorityMarkerClauseFallback: sourceLinkedPriorityMarkerClauseFallback2,
        wrapPriorityMarkerSubspans: wrapPriorityMarkerSubspans2
      };
    };
  }
});

// src/parser/detectors/predicates/basic.js
var require_basic = __commonJS({
  "src/parser/detectors/predicates/basic.js"(exports2, module2) {
    "use strict";
    module2.exports = function createBasicPredicateDetectors2(dependencies = {}) {
      const {
        construction: construction2,
        flattenSurface: flattenSurface2,
        hasSurface: hasSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2
      } = dependencies;
      function wrapNegatedVPSubspans2(nodes) {
        const result = [];
        let index = 0;
        while (index < nodes.length) {
          const negator = nodes[index];
          const vp = nodes[index + 1];
          if (isToken2(negator, "唔") && vp && vp.kind === "construction" && nodeCanFillSlot2(vp, "vp") && !nodeCanFillSlot2(vp, "negated_directional_motion_vp") && vp.type !== "NegatedVP") {
            const negatorChild = parserInactiveTokenClone2(negator, {
              label: "func",
              pos: "function",
              syntax: "negator m4_negator",
              slots: ["negator", "m4_negator"],
              reason: "唔 is the visible negator of the following VP."
            });
            const children = [negatorChild, vp];
            result.push(construction2("NegatedVP", "NegVP", children, {
              note: "Productive Cantonese negated VP: 唔 + VP, preserving the positive VP as a visible child.",
              slots: templateDerivedSlots2("NegatedVP", children),
              trace: traceInfo2("generative_template", {
                construction_type: "NegatedVP",
                template_family: "generative_template",
                template: ["m4_negator!", "vp!"],
                assigned_slots: ["m4_negator", "vp"],
                surfaces: children.map((node) => flattenSurface2(node)),
                reason: "Negation wraps an already formed VP so the negator cannot detach and the VP's internal object/complement structure remains visible."
              })
            }));
            index += 2;
            continue;
          }
          result.push(nodes[index]);
          index += 1;
        }
        return result;
      }
      function scalarEvaluationFallback2(core) {
        if (!hasSurface2(core, "唔") || !hasSurface2(core, "算") || !hasSurface2(core, "貴")) return null;
        return construction2("ScalarEvaluation", "ValueEval", core, {
          slots: ["scalar_evaluation", "evaluation_clause", "predicate", "stative_predicate", "negator", "evaluation_marker"],
          note: "Negative lexical 算 evaluation with an overt property predicate; visible subject/topic material remains outside the lexical predicate relation.",
          trace: traceInfo2("generative_template", {
            construction_type: "ScalarEvaluation",
            template_family: "generative_template",
            template: ["negator!", "evaluation_marker!", "degree?", "stative_predicate!", "particle?"],
            assigned_slots: ["negator", "evaluation_marker", "stative_predicate"],
            polarity_profile: "negative",
            scalar_domain: "price",
            semantic_domain: "price_property",
            rule: "negator + evaluation marker + stative/scalar predicate",
            reason: "Uses the source-linked negative 算 evaluation profile; price is contextual metadata and negative polarity remains trace metadata.",
            surfaces: core.map((node) => flattenSurface2(node))
          })
        });
      }
      return {
        wrapNegatedVPSubspans: wrapNegatedVPSubspans2,
        scalarEvaluationFallback: scalarEvaluationFallback2
      };
    };
  }
});

// src/runtime-resources/grammar/postpredicate-gwo3-comparison.js
var require_postpredicate_gwo3_comparison = __commonJS({
  "src/runtime-resources/grammar/postpredicate-gwo3-comparison.js"(exports2, module2) {
    "use strict";
    module2.exports = Object.freeze([
      "大",
      "細",
      "貴",
      "平",
      "快",
      "慢",
      "遠",
      "近",
      "高",
      "矮",
      "熱",
      "凍",
      "靚",
      "甜"
    ]);
  }
});

// src/parser/orchestration/wrap-predicate.js
var require_wrap_predicate = __commonJS({
  "src/parser/orchestration/wrap-predicate.js"(exports2, module2) {
    "use strict";
    var POSTPREDICATE_GWO3_COMPARISON_PREDICATES = new Set(
      require_postpredicate_gwo3_comparison()
    );
    module2.exports = function createWrapPredicate(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        isStativeToken: isStativeToken2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        traceInfo: traceInfo2
      } = dependencies;
      function simpleNominalComparisonArgument(node) {
        if (!node || node.kind !== "token") return false;
        if (nodeCanFillSlot2(node, "subject") || nodeCanFillSlot2(node, "np") || nodeCanFillSlot2(node, "object")) return true;
        return ["who", "what"].includes(node.label);
      }
      function comparisonMarkerClone(node) {
        return {
          ...node,
          label: "func",
          role: "func",
          pos: "function",
          syntax: "comparison_marker surpass_comparative_marker",
          slots: ["comparison_marker"],
          note: "post-predicate comparison marker: surpass / more ... than",
          features: void 0,
          feature_bundle: void 0,
          trace: traceInfo2("generative_or_heuristic_slot_rule", {
            surface: node.surface,
            contextual_role_override: "postpredicate_gwo3_comparison_marker",
            original_trace: node.trace && node.trace.kind || "",
            generated_slots: ["comparison_marker"],
            reason: "Inside the bounded stative + 過 + overt-standard behavior, 過 is interpreted as the visible comparison marker rather than experiential aspect."
          })
        };
      }
      function postPredicateGwo3Comparison(nodes) {
        if (nodes.length !== 4) return null;
        const [target, predicate, marker, standard] = nodes;
        if (!simpleNominalComparisonArgument(target) || !simpleNominalComparisonArgument(standard)) return null;
        if (!isStativeToken2(predicate) || !POSTPREDICATE_GWO3_COMPARISON_PREDICATES.has(predicate.surface)) return null;
        if (!isToken2(marker, "過")) return null;
        const markerChild = comparisonMarkerClone(marker);
        const children = [target, predicate, markerChild, standard];
        return construction2("SubjectPredicateClause", "SubjPred", children, {
          slots: [
            "subject_predicate_clause",
            "subject",
            "predicate",
            "clause",
            "stative_predicate",
            "comparison_target",
            "comparison_predicate",
            "comparison_marker",
            "comparison_standard"
          ],
          note: "Behavior-first overt-standard post-predicate 過 comparison. SubjectPredicateClause remains the structural label; the comparison function is exposed through explicit trace bindings.",
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            predicate_subtype: "postpredicate_gwo3_surpass_comparison",
            template: ["comparison_target!", "comparison_predicate!", "comparison_marker!", "comparison_standard!"],
            assigned_slots: ["comparison_target", "comparison_predicate", "comparison_marker", "comparison_standard"],
            surfaces: children.map((node) => node.surface || ""),
            structural_scope: "clause",
            not_claims: [
              "not_experiential_gwo3",
              "not_directional_gwo3",
              "not_quantity_comparison_generalization_yet",
              "not_temporal_comparison_generalization_yet",
              "not_bei2_comparative",
              "not_bare_di1_comparative",
              "not_equative_or_superlative",
              "not_open_class_productivity_claim"
            ],
            reason: "Cycle 1 GREEN behavior: overt simple nominal target + bounded gradable stative predicate + 過 + overt simple nominal comparison standard."
          })
        });
      }
      function wrapPredicate2(nodes) {
        if (!nodes.length) return nodes;
        const comparative = postPredicateGwo3Comparison(nodes);
        if (comparative) return [comparative];
        const degreeMannerIndex = nodes.findIndex((node, index) => nodeCanFillSlot2(node, "degree_manner_head") && nodeCanFillSlot2(nodes[index + 1], "degree_particle"));
        if (degreeMannerIndex >= 0) {
          const degreeManner = categorySubspanFor2(nodes.slice(degreeMannerIndex, degreeMannerIndex + 2), ["DegreeMannerAdverbial"]);
          if (degreeManner) {
            return [
              ...nodes.slice(0, degreeMannerIndex),
              degreeManner,
              ...wrapPredicate2(nodes.slice(degreeMannerIndex + 2))
            ];
          }
        }
        if (nodes.length === 1 && isStativeToken2(nodes[0])) {
          return [construction2("StativePredicate", "Stative", nodes, { note: "Stative/property predicate fragment.", trace: traceInfo2("generative_template", { construction_type: "StativePredicate", template_family: "generative_template", template: ["stative_predicate!"], assigned_slots: ["stative_predicate"], rule: "single stative token", reason: "Generated label/syntax indicates stative." }) })];
        }
        if (nodes.length >= 2 && ["好", "太", "幾", "有啲"].some((s) => isToken2(nodes[0], s)) && isStativeToken2(nodes[1])) {
          return [construction2("StativePredicate", "Stative", nodes, { note: "Degree + stative/property predicate.", trace: traceInfo2("generative_template", { construction_type: "StativePredicate", template_family: "generative_template", template: ["degree!", "stative_predicate!"], assigned_slots: ["degree", "stative_predicate"], rule: "degree + stative", reason: "Structural stative predicate template." }) })];
        }
        if (nodes.length >= 2 && isToken2(nodes[0], "唔") && isStativeToken2(nodes[1])) {
          return [construction2("StativePredicate", "NegStative", nodes, { note: "Negated stative/property predicate.", trace: traceInfo2("generative_template", { construction_type: "StativePredicate", template_family: "generative_template", template: ["negator!", "stative_predicate!"], assigned_slots: ["negator", "stative_predicate"], rule: "唔 + stative", reason: "Structural negated stative predicate template." }) })];
        }
        if (nodes.length === 1 && isStativeToken2(nodes[0])) {
          return [construction2("StativePredicate", "Stative", nodes, { note: "Stative/property predicate fragment.", trace: traceInfo2("generative_template", { construction_type: "StativePredicate", template_family: "generative_template", template: ["stative_predicate!"], assigned_slots: ["stative_predicate"], rule: "single stative token", reason: "Generated label/syntax indicates stative." }) })];
        }
        return nodes;
      }
      return {
        wrapPredicate: wrapPredicate2
      };
    };
  }
});

// src/parser/detectors/acceptability/clauses.js
var require_clauses = __commonJS({
  "src/parser/detectors/acceptability/clauses.js"(exports2, module2) {
    "use strict";
    module2.exports = function createAcceptabilityDetectors2(dependencies = {}) {
      const {
        cleanSlots: cleanSlots2,
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isParticle: isParticle2,
        isToken: isToken2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        token: token3,
        traceInfo: traceInfo2
      } = dependencies;
      function isSubjectLike(node) {
        return nodeSlots2(node).includes("subject");
      }
      function isAcceptabilityActionCandidate(node) {
        if (!node || node.kind === "text") return false;
        const slots = nodeSlots2(node);
        return slots.includes("directional_motion_vp") || slots.includes("negated_directional_motion_vp") || slots.includes("vp") || slots.includes("action_vp") || slots.includes("predicate");
      }
      function isDakFormulaNode(node) {
        return node && node.kind === "construction" && flattenSurface2(node) === "得喇";
      }
      function acceptabilityPartClone(node, role = "func", overrides = {}) {
        const surface = overrides.surface || flattenSurface2(node);
        return parserInactiveTokenClone2(token3(surface), {
          label: overrides.label || role,
          pos: overrides.pos || (role === "how" ? "adverbial" : role === "particle" ? "particle" : "function"),
          syntax: overrides.syntax || "acceptability_part",
          slots: overrides.slots || ["acceptability_part"],
          reason: overrides.reason || "Token is parser-inactive inside an acceptability clause wrapper; the parent exposes the acceptability affordance."
        });
      }
      function acceptabilityFocusClone(node) {
        return parserInactiveTokenClone2(firstToken2(node) || token3(flattenSurface2(node)), {
          label: "how",
          pos: "adverbial",
          syntax: "focus_adverb",
          slots: ["focus_adverb", "how"],
          reason: "都 scopes the acceptability predicate inside 都得, so it stays parser-inactive inside the parent wrapper."
        });
      }
      function acceptabilityDakClone(node) {
        return acceptabilityPartClone(node, "func", {
          surface: "得",
          label: "func",
          pos: "function",
          syntax: "acceptability_predicate",
          slots: ["acceptability_predicate"],
          reason: "得 is interpreted here as the acceptability predicate in 都得, so it stays parser-inactive while the parent exposes acceptability."
        });
      }
      function acceptabilityParticleClone(node) {
        return acceptabilityPartClone(node, "particle", {
          surface: flattenSurface2(node),
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle",
          slots: ["particle"],
          reason: "Final particle stays parser-inactive inside an acceptability clause wrapper."
        });
      }
      function matchAcceptabilityTail(nodes, index) {
        if (!isToken2(nodes[index], "都")) return null;
        const dak = nodes[index + 1];
        if (isToken2(dak, "得")) {
          const particle = isParticle2(nodes[index + 2]) && ["喇", "啦", "呀", "啊"].includes(flattenSurface2(nodes[index + 2])) ? nodes[index + 2] : null;
          return { length: 2 + (particle ? 1 : 0), focus: nodes[index], dak, particle };
        }
        if (isDakFormulaNode(dak)) {
          return { length: 2, focus: nodes[index], dak, particle: token3("喇", { label: "particle", syntax: "sentence_final_particle", note: "Final particle split from protected 得喇 formula inside 都得 acceptability." }) };
        }
        return null;
      }
      function acceptabilityClausePatternAt(nodes, index) {
        let subject = null;
        let action = null;
        let tailIndex = index;
        if (isAcceptabilityActionCandidate(nodes[index])) {
          action = nodes[index];
          tailIndex = index + 1;
          const tail = matchAcceptabilityTail(nodes, tailIndex);
          if (tail) return { ...tail, length: 1 + tail.length, subject, action };
        }
        if (isSubjectLike(nodes[index]) && isAcceptabilityActionCandidate(nodes[index + 1])) {
          subject = nodes[index];
          action = nodes[index + 1];
          tailIndex = index + 2;
          const tail = matchAcceptabilityTail(nodes, tailIndex);
          if (tail) return { ...tail, length: 2 + tail.length, subject, action };
        }
        return null;
      }
      function acceptabilitySubjectPredicateChild(subject, action) {
        if (!subject || !action) return null;
        const slots = nodeSlots2(action);
        if (slots.includes("negated_directional_motion_vp")) {
          const children = [subject, action];
          return construction2("SubjectPredicateClause", "SubjPred", children, {
            slots: cleanSlots2(["subject_predicate_clause", "subject", "predicate", "clause", "negative_clause", "negated_predicate", "negator"]),
            note: "Transparent subject + negated predicate child preserved inside a larger acceptability clause.",
            trace: traceInfo2("generative_template", {
              construction_type: "SubjectPredicateClause",
              retired_label_alias: "SubjectNegatedPredicateClause",
              template_family: "generative_template",
              polarity: "negative",
              template: ["subject!", "predicate!"],
              constraints: { predicate_must_have_any_slots: ["negated_directional_motion_vp"] },
              assigned_slots: ["subject", "predicate"],
              reason: "Expose the broad subject/predicate relation inside AcceptabilityClause without changing the top-level acceptability meaning.",
              surfaces: children.map((node) => flattenSurface2(node))
            })
          });
        }
        if (slots.includes("productive_vo") || slots.includes("directional_motion_vp")) {
          const children = [subject, action];
          return construction2("SubjectPredicateClause", "SubjPred", children, {
            slots: templateDerivedSlots2("SubjectPredicateClause", children),
            note: "Transparent subject + predicate child preserved inside a larger acceptability clause.",
            trace: traceInfo2("generative_template", {
              construction_type: "SubjectPredicateClause",
              template: ["subject!", "predicate!"],
              constraints: {
                predicate_must_have_any_slots: ["productive_vo", "directional_motion_vp", "motion_purpose_chain", "serial_verb_purpose_chain"],
                disallow_child_slots: ["negated_directional_motion_vp"]
              },
              assigned_slots: ["subject", "predicate"],
              reason: "Expose the subject/predicate relation inside AcceptabilityClause without changing the top-level acceptability meaning.",
              surfaces: children.map((node) => flattenSurface2(node))
            })
          });
        }
        return null;
      }
      function makeAcceptabilityClause(match) {
        const children = [];
        const subjectPredicateChild = acceptabilitySubjectPredicateChild(match.subject, match.action);
        if (subjectPredicateChild) {
          children.push(subjectPredicateChild);
        } else {
          if (match.subject) children.push(match.subject);
          if (match.action) children.push(match.action);
        }
        children.push(acceptabilityFocusClone(match.focus));
        children.push(acceptabilityDakClone(match.dak));
        if (match.particle) children.push(acceptabilityParticleClone(match.particle));
        const traceTemplate = subjectPredicateChild ? ["predicate!", "focus_adverb!", "acceptability_predicate!", "particle?"] : ["subject?", "predicate!", "focus_adverb!", "acceptability_predicate!", "particle?"];
        const assignedSlots = subjectPredicateChild ? ["predicate", "focus_adverb", "acceptability_predicate", ...match.particle ? ["particle"] : []] : [...match.subject ? ["subject"] : [], "predicate", "focus_adverb", "acceptability_predicate", ...match.particle ? ["particle"] : []];
        return construction2("AcceptabilityClause", "Acceptability", children, {
          slots: ["acceptability_clause", "acceptability", "focus_adverb", "acceptability_predicate", "predicate", "clause"],
          note: "Bounded action-feasibility clause: an overt action predicate followed by 都得 and an optional final particle. Wh/free-choice 都得 remains outside this node. When a subject + transparent predicate is present, preserve it as a child clause for learner visibility.",
          trace: traceInfo2("generative_template", {
            construction_type: "AcceptabilityClause",
            template_family: "generative_template",
            retired_label_alias: "PermissionAcceptabilityClause",
            acceptability_subtype: "action_feasibility",
            template: traceTemplate,
            assigned_slots: assignedSlots,
            rule: "subject? + overt action predicate + focus adverb 都 + acceptability predicate 得 + optional final particle",
            pattern: match.particle ? "subject? + predicate + 都 + 得 + final_particle" : "subject? + predicate + 都 + 得",
            reason: "Checked source evidence directly supports action material followed by 都得 as feasible. The matcher requires that overt host and leaves wh/free-choice 都得 for separate analysis.",
            child_subject_predicate_construction: subjectPredicateChild ? subjectPredicateChild.type : "",
            surfaces: children.map((node) => flattenSurface2(node))
          })
        });
      }
      function wrapPermissionAcceptabilitySubspans2(nodes) {
        const result = [];
        let i = 0;
        while (i < nodes.length) {
          const match = acceptabilityClausePatternAt(nodes, i);
          if (match) {
            result.push(makeAcceptabilityClause(match));
            i += match.length;
            continue;
          }
          result.push(nodes[i]);
          i += 1;
        }
        return result;
      }
      return {
        isSubjectLike,
        isAcceptabilityActionCandidate,
        isDakFormulaNode,
        acceptabilityPartClone,
        acceptabilityFocusClone,
        acceptabilityDakClone,
        acceptabilityParticleClone,
        matchAcceptabilityTail,
        acceptabilityClausePatternAt,
        acceptabilitySubjectPredicateChild,
        makeAcceptabilityClause,
        wrapPermissionAcceptabilitySubspans: wrapPermissionAcceptabilitySubspans2
      };
    };
  }
});

// src/parser/detectors/manner/adjustment.js
var require_adjustment = __commonJS({
  "src/parser/detectors/manner/adjustment.js"(exports2, module2) {
    "use strict";
    module2.exports = function createMannerAdjustmentDetectors2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        categorySubspanFor: categorySubspanFor2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSurfaceMatches: nodeSurfaceMatches2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function sourceLinkedDegreeMannerModifiedVPFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        const fusedModifier = isToken2(compact[0], "快啲");
        const splitModifier = isToken2(compact[0], "快") && isToken2(compact[1], "啲");
        if (!fusedModifier && !splitModifier) return null;
        const modifierLength = fusedModifier ? 1 : 2;
        if (compact.length <= modifierLength) return null;
        const modifier = categorySubspanFor2(compact.slice(0, modifierLength), ["DegreeMannerAdverbial"]);
        let predicate = categorySubspanFor2(compact.slice(modifierLength), [
          "CompoundDirectionalMotionVP",
          "DirectedMannerMotionVP",
          "DirectionalMotionVP",
          "VerbComplementVP"
        ]);
        if (!predicate) {
          const wrappedPredicate = applyConstructionPatterns2(compact.slice(modifierLength));
          if (wrappedPredicate.length === 1 && wrappedPredicate[0].kind === "construction" && ["CompoundDirectionalMotionVP", "DirectedMannerMotionVP", "DirectionalMotionVP", "VerbComplementVP"].includes(wrappedPredicate[0].type)) {
            predicate = wrappedPredicate[0];
          }
        }
        if (!predicate && compact.length === modifierLength + 3) {
          const directional = categorySubspanFor2(compact.slice(modifierLength + 1), ["DirectionalMotionVP"]);
          predicate = directional ? categorySubspanFor2([compact[modifierLength], directional], ["VerbComplementVP"]) : null;
        }
        if (!modifier || !predicate || !nodeCanFillSlot2(predicate, "vp")) return null;
        const children = [modifier, predicate, ...particles];
        return construction2("DegreeMannerModifiedVP", "DegMannerVP", children, {
          note: "Source-linked preposed 快啲 modifier over a visible directional VP.",
          slots: templateDerivedSlots2("DegreeMannerModifiedVP", children),
          trace: traceInfo2("generative_template", {
            construction_type: "DegreeMannerModifiedVP",
            template: ["degree_manner_adverbial!", "directional_motion_vp!", "particle?"],
            constraints: { modifier_surface: "快啲", preserve_inner_directional_vp: true },
            assigned_slots: ["degree_manner_adverbial", "directional_motion_vp", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Preserves the exact sourced preposed order without conflating postverbal 行快啲 or punctuation-separated material."
          })
        });
      }
      const AA84_MODIFIER_PROFILE = Object.freeze({
        id: "source_linked_maan6_maan6",
        surfaces: ["慢", "慢"]
      });
      function sourceLinkedReduplicatedMannerModifier(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length !== AA84_MODIFIER_PROFILE.surfaces.length) return null;
        for (let index = 0; index < compact.length; index += 1) {
          const node = compact[index];
          if (flattenSurface2(node) !== AA84_MODIFIER_PROFILE.surfaces[index]) return null;
          if (!nodeCanFillSlot2(node, "stative_predicate") || !nodeCanFillSlot2(node, "modifier")) return null;
        }
        return {
          profile: AA84_MODIFIER_PROFILE.id,
          children: compact.map((node) => parserInactiveTokenClone2(node, {
            label: "how",
            pos: "adverb",
            syntax: "source_linked_reduplicated_manner_modifier",
            slots: ["manner", "modifier", "how", "reduplicated_manner"],
            reason: "AA84 uses the independently typed, source-linked 慢慢 reduplicated-manner profile; arbitrary adjacent equal surfaces do not license the construction."
          }))
        };
      }
      function independentlyTypedFollowingVP(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        const wrapped = applyConstructionPatterns2(compact);
        if (wrapped.length !== 1) return null;
        const candidate = wrapped[0];
        if (!candidate || candidate.kind !== "construction" || !nodeCanFillSlot2(candidate, "vp")) return null;
        if (nodeCanFillSlot2(candidate, "clause") || nodeCanFillSlot2(candidate, "subject") || nodeCanFillSlot2(candidate, "time")) return null;
        return candidate;
      }
      function markedMannerVPForCore(core) {
        const compact = withoutIgnorableSpaceText2(core || []);
        if (compact.length < 4) return null;
        const markerIndex = compact.findIndex((node) => nodeSurfaceMatches2(node, ["咁", "噉"]));
        if (markerIndex <= 0 || markerIndex >= compact.length - 1) return null;
        const modifier = sourceLinkedReduplicatedMannerModifier(compact.slice(0, markerIndex));
        if (!modifier) return null;
        const predicate = independentlyTypedFollowingVP(compact.slice(markerIndex + 1));
        if (!predicate) return null;
        const marker = parserInactiveTokenClone2(compact[markerIndex], {
          label: "how",
          pos: "adverbializer",
          syntax: "manner_adverbializer",
          slots: ["manner", "modifier", "how", "manner_adverbializer"],
          reason: "Overt 咁/噉 links the independently typed reduplicated manner constituent to the following independently typed VP in AA84."
        });
        const children = [...modifier.children, marker, predicate];
        return construction2("MannerAdverbialVP", "MannerVP", children, {
          note: "AA84 retained narrowly: source-linked reduplicated manner constituent + overt 咁/噉 + independently typed VP.",
          // Do not inherit the legacy MannerAdverbialVP template's optional subject.
          // The construction helper still propagates slots from the typed VP child.
          slots: cleanSlots2(["manner_adverbial_vp", "manner", "modifier", "how", "vp", "action_vp", "predicate"]),
          trace: traceInfo2("generative_template", {
            construction_type: "MannerAdverbialVP",
            template_family: "construction_template",
            template_subtype: "aa84_overt_gam_marked_reduplicated_manner",
            template: ["reduplicated_manner_part!", "reduplicated_manner_part!", "manner_adverbializer!", "vp!"],
            rule: "typed source-linked reduplicated manner constituent + overt 咁/噉 + independently typed VP",
            constraints: {
              modifier_profile: modifier.profile,
              marker_surface_in: ["咁", "噉"],
              overt_marker_required: true,
              bare_reduplication_route: false,
              following_vp_typing: "independent_required",
              following_vp_clause_time_material: "excluded"
            },
            assigned_slots: ["reduplicated_manner_part", "reduplicated_manner_part", "manner_adverbializer", "vp"],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Implements the accepted #611/#629 AA84 boundary. Generic bare A+A+VP and raw same-surface equality do not license this construction.",
            not_claims: ["not_generic_bare_reduplication", "not_arbitrary_same_surface_repetition", "not_every_gam_is_manner_adverbializer"]
          })
        });
      }
      function outerWrapper(type, label, prefixNode, predicate, particles, options) {
        const children = [prefixNode, predicate, ...particles];
        const assignedSlots = [options.prefixSlot, "predicate", ...particles.map(() => "particle")];
        return construction2(type, label, children, {
          note: options.note,
          slots: cleanSlots2(options.slots),
          trace: traceInfo2("generative_template", {
            construction_type: type,
            template_family: "construction_template",
            template_subtype: options.templateSubtype,
            template: [`${options.prefixSlot}!`, "predicate!", "particle?"],
            rule: options.rule,
            assigned_slots: assignedSlots,
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: options.reason
          })
        });
      }
      function mannerAdverbialVPFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        const direct = markedMannerVPForCore(compact);
        if (direct && !particles.length) return direct;
        if (compact.length >= 5 && nodeCanFillSlot2(compact[0], "subject")) {
          const predicate = markedMannerVPForCore(compact.slice(1));
          if (!predicate) return null;
          return outerWrapper("SubjectPredicateClause", "SubjPred", compact[0], predicate, particles, {
            prefixSlot: "subject",
            templateSubtype: "aa84_subject_wrapper",
            rule: "subject + AA84 overt-marked reduplicated manner VP + particle?",
            note: "Transparent clause wrapper around a narrow AA84 marked manner VP; subject/final-particle material remains outside AA84.",
            slots: ["subject_predicate_clause", "clause", "subject", "predicate"],
            reason: "Keeps clause-level subject and final-particle material outside the narrow AA84 VP while preserving all visible material."
          });
        }
        if (compact.length >= 5 && nodeCanFillSlot2(compact[0], "time")) {
          const predicate = markedMannerVPForCore(compact.slice(1));
          if (!predicate) return null;
          return outerWrapper("TemporalClause", "Time", compact[0], predicate, particles, {
            prefixSlot: "time",
            templateSubtype: "aa84_temporal_wrapper",
            rule: "time + AA84 overt-marked reduplicated manner VP + particle?",
            note: "Transparent temporal wrapper around a narrow AA84 marked manner VP; overt time remains outside AA84.",
            slots: ["temporal_clause", "time_clause", "clause", "time", "predicate"],
            reason: "Preserves the attested 琴日 + marked manner VP composition while keeping temporal material outside the narrow AA84 node."
          });
        }
        return null;
      }
      return {
        sourceLinkedDegreeMannerModifiedVPFallback: sourceLinkedDegreeMannerModifiedVPFallback2,
        mannerAdverbialVPFallback: mannerAdverbialVPFallback2
      };
    };
  }
});

// src/parser/detectors/imperatives/clauses.js
var require_clauses2 = __commonJS({
  "src/parser/detectors/imperatives/clauses.js"(exports2, module2) {
    "use strict";
    module2.exports = function createImperativeDetectors2(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        directPredicateCapableNode: directPredicateCapableNode2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        mergeUnique: mergeUnique2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        pathPhraseFromParts: pathPhraseFromParts2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        transparentDiscourseFormulaFallback: transparentDiscourseFormulaFallback2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function politeRequestAdjustmentFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3) return null;
        const formulaSurface = flattenSurface2(compact[0]);
        if (formulaSurface !== "唔該") return null;
        const addressee = compact[1];
        if (!nodeCanFillSlot2(addressee, "subject")) return null;
        const adjustmentNodes = compact.slice(2);
        const adjustment = adjustmentNodes.length === 1 && adjustmentNodes[0].kind === "construction" && adjustmentNodes[0].type === "DegreeMannerAdverbial" ? adjustmentNodes[0] : categorySubspanFor2(adjustmentNodes, ["DegreeMannerAdverbial"]);
        if (!adjustment || flattenSurface2(adjustment) !== adjustmentNodes.map((node) => flattenSurface2(node)).join("")) return null;
        const formula = compact[0].kind === "construction" && compact[0].type === "FormulaDiscourseUnit" ? compact[0] : transparentDiscourseFormulaFallback2([compact[0]]);
        if (!formula || formula.type !== "FormulaDiscourseUnit") return null;
        const children = [formula, addressee, adjustment, ...particles];
        return construction2("PoliteImperativeClause", "PoliteImperative", children, {
          note: "Source-linked polite adjustment request: 唔該 + addressee + scalar adjustment.",
          slots: mergeUnique2(templateDerivedSlots2("PoliteImperativeClause", children), ["polite_imperative_clause", "imperative", "politeness_marker", "subject", "modifier", "predicate", "clause"]),
          trace: traceInfo2("generative_template", {
            construction_type: "PoliteImperativeClause",
            template: ["politeness_formula!", "addressee!", "scalar_adjustment!", "particle?"],
            assigned_slots: ["politeness_marker", "subject", "modifier", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "A conventional 唔該 request formula addresses a person and scopes over a following scalar adjustment while all overt material remains visible.",
            not_claims: ["not_every_m4_goi1_is_an_imperative", "not_equivalent_to_cing2_in_all_registers", "not_unrestricted_productivity"]
          })
        });
      }
      function politePathImperativeFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 5) return null;
        if (!isToken2(compact[0], "請")) return null;
        if (!nodeCanFillSlot2(compact[1], "subject")) return null;
        let index = 2;
        const time = nodeCanFillSlot2(compact[index], "time") ? compact[index++] : null;
        const marker = compact[index++];
        const path = compact[index++];
        const predicate = compact[index++];
        if (index !== compact.length) return null;
        if (!isToken2(marker, "沿住")) return null;
        if (!nodeCanFillSlot2(path, "location") && !nodeCanFillSlot2(path, "goal")) return null;
        if (!directPredicateCapableNode2(predicate)) return null;
        const polite = parserInactiveTokenClone2(compact[0], {
          label: "func",
          pos: "function",
          syntax: "politeness_imperative_marker",
          slots: ["politeness_marker", "imperative_marker"],
          reason: "請 is interpreted as the politeness marker inside a bounded path imperative."
        });
        const pathPhrase = pathPhraseFromParts2(marker, path);
        const children = [polite, compact[1], ...time ? [time] : [], pathPhrase, predicate, ...particles];
        return construction2("PoliteImperativeClause", "PoliteImperative", children, {
          note: "v0.5.32 polite path imperative: 請 + addressee + optional time + 沿住 + path + motion/action predicate.",
          slots: templateDerivedSlots2("PoliteImperativeClause", children),
          trace: traceInfo2("generative_template", {
            construction_type: "PoliteImperativeClause",
            template: ["politeness_marker!", "subject!", ...time ? ["time?"] : [], "path_phrase!", "predicate!", "particle?"],
            assigned_slots: ["politeness_marker", "subject", ...time ? ["time"] : [], "path_phrase", "predicate", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Promotes the reviewed path-command shape without creating broad imperative grammar."
          })
        });
      }
      function prohibitiveImperativeFallback2(core) {
        if (!isToken2(core[0], "唔好") || core.length < 2) return null;
        return construction2("ProhibitiveImperative", "Prohibitive", core, {
          note: "Negative imperative / command.",
          trace: traceInfo2("legacy_surface_rule", {
            rule: "唔好 + predicate",
            reason: "Surface prohibitive marker fallback."
          })
        });
      }
      return {
        politeRequestAdjustmentFallback: politeRequestAdjustmentFallback2,
        politePathImperativeFallback: politePathImperativeFallback2,
        prohibitiveImperativeFallback: prohibitiveImperativeFallback2
      };
    };
  }
});

// src/parser/detectors/np/token-splits.js
var require_token_splits = __commonJS({
  "src/parser/detectors/np/token-splits.js"(exports2, module2) {
    "use strict";
    module2.exports = function createNpTokenSplits2(dependencies = {}) {
      const {
        PUNCT_RE: PUNCT_RE2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        nodeCanFillSlot: nodeCanFillSlot2,
        phraseMatch: phraseMatch2,
        selectLexiconTerm: selectLexiconTerm2,
        token: token3,
        traceInfo: traceInfo2
      } = dependencies;
      const TRANSPARENT_DEMONSTRATIVE_CLASSIFIER_SPLITS = [
        { surface: "呢個", demonstrative: "呢", classifier: "個", classifierSyntax: "general_classifier" },
        { surface: "嗰個", demonstrative: "嗰", classifier: "個", classifierSyntax: "general_classifier" },
        { surface: "呢本", demonstrative: "呢", classifier: "本", classifierSyntax: "classifier_book" },
        { surface: "嗰本", demonstrative: "嗰", classifier: "本", classifierSyntax: "classifier_book" },
        { surface: "呢間", demonstrative: "呢", classifier: "間", classifierSyntax: "classifier_building_shop" },
        { surface: "嗰間", demonstrative: "嗰", classifier: "間", classifierSyntax: "classifier_building_shop" },
        { surface: "呢杯", demonstrative: "呢", classifier: "杯", classifierSyntax: "classifier_container_cup" },
        { surface: "嗰杯", demonstrative: "嗰", classifier: "杯", classifierSyntax: "classifier_container_cup" },
        { surface: "呢套", demonstrative: "呢", classifier: "套", classifierSyntax: "classifier_set_media" },
        { surface: "嗰套", demonstrative: "嗰", classifier: "套", classifierSyntax: "classifier_set_media" },
        { surface: "呢首", demonstrative: "呢", classifier: "首", classifierSyntax: "classifier_song_poem" },
        { surface: "嗰首", demonstrative: "嗰", classifier: "首", classifierSyntax: "classifier_song_poem" },
        { surface: "呢件", demonstrative: "呢", classifier: "件", classifierSyntax: "classifier_clothing_item" },
        { surface: "嗰件", demonstrative: "嗰", classifier: "件", classifierSyntax: "classifier_clothing_item" }
      ];
      function makeTransparentDemonstrativeClassifierEllipsis(spec) {
        const children = [
          token3(spec.demonstrative, {
            label: "func",
            syntax: "demonstrative_determiner",
            note: `${spec.demonstrative} is the demonstrative determiner inside an elliptical demonstrative-classifier NP.`
          }),
          token3(spec.classifier, {
            label: "measure_word",
            syntax: spec.classifierSyntax,
            note: `${spec.classifier} is the visible measure word/classifier inside an elliptical demonstrative-classifier NP.`
          })
        ];
        return construction2("HeadlessDemonstrativeClassifierNP", "NP", children, {
          primary: "object",
          slots: cleanSlots2(["headless_demonstrative_classifier_np", "np", "topic", "object", "demonstrative", "classifier"]),
          note: `${spec.surface} is rendered transparently as a headless demonstrative-classifier NP. No nominal head is inserted; discourse licensing remains a separate research question.`,
          trace: traceInfo2("construction_template", {
            construction_type: "HeadlessDemonstrativeClassifierNP",
            template_family: "construction_template",
            template: ["demonstrative!", "classifier!"],
            assigned_slots: ["demonstrative", "classifier"],
            surfaces: children.map((node) => flattenSurface2(node)),
            np_subtype: "headless_demonstrative_classifier_np",
            omitted_head_status: "overtly_absent_not_reconstructed",
            independent_productivity_status: "research_pending",
            reason: "The NP subsystem separates overt-head D-CL-N from headless D-CL and inserts no missing noun."
          })
        });
      }
      function transparentEllipticalDemonstrativeClassifierFromRest2(rest) {
        const match = TRANSPARENT_DEMONSTRATIVE_CLASSIFIER_SPLITS.find((spec) => rest.startsWith(spec.surface));
        if (!match) return null;
        const after = rest.slice(match.surface.length);
        const nextTerm = after && !PUNCT_RE2.test(after) ? selectLexiconTerm2(after) : null;
        if (nextTerm) {
          const nextNode = token3(nextTerm.surface);
          if (nodeCanFillSlot2(nextNode, "head_noun")) return null;
        }
        return phraseMatch2(match.surface.length, makeTransparentDemonstrativeClassifierEllipsis(match));
      }
      const TRANSPARENT_CUP_NOUN_DEMONSTRATIVE_NPS = [
        { surface: "呢個杯", demonstrative: "呢", classifier: "個", classifierSyntax: "general_classifier classifier" },
        { surface: "嗰個杯", demonstrative: "嗰", classifier: "個", classifierSyntax: "general_classifier classifier" },
        { surface: "呢隻杯", demonstrative: "呢", classifier: "隻", classifierSyntax: "classifier_animal_body_part_one_of_pair" },
        { surface: "嗰隻杯", demonstrative: "嗰", classifier: "隻", classifierSyntax: "classifier_animal_body_part_one_of_pair" }
      ];
      function transparentCupNounDemonstrativeNpFromRest2(rest) {
        const match = TRANSPARENT_CUP_NOUN_DEMONSTRATIVE_NPS.find((spec) => rest.startsWith(spec.surface));
        if (!match) return null;
        const children = [
          token3(match.demonstrative, {
            label: "func",
            syntax: "demonstrative_determiner",
            slots: ["demonstrative"],
            note: `${match.demonstrative} is the demonstrative determiner in a transparent demonstrative-classifier noun phrase.`
          }),
          token3(match.classifier, {
            label: "measure_word",
            syntax: match.classifierSyntax,
            slots: ["classifier"],
            note: `${match.classifier} is the visible classifier before the noun 杯.`
          }),
          token3("杯", {
            label: "what",
            syntax: "head_noun object_np container_noun",
            slots: ["head_noun", "np", "object", "topic"],
            note: "杯 bui1 is the noun ‘cup’ here, not the homophonous cupful classifier. This bounded lexical disambiguation is independently dictionary-attested."
          })
        ];
        return phraseMatch2(match.surface.length, construction2("OvertHeadDemonstrativeClassifierNP", "NP", children, {
          compatibility_alias: "DemonstrativeClassifierNP",
          primary: "object",
          slots: cleanSlots2(["overt_head_demonstrative_classifier_np", "np", "topic", "object", "head_noun", "demonstrative", "classifier"]),
          note: `${match.surface} is rendered transparently as the narrow overt-head demonstrative + classifier + noun subtype.`,
          trace: traceInfo2("generative_template", {
            construction_type: "OvertHeadDemonstrativeClassifierNP",
            compatibility_construction_type: "DemonstrativeClassifierNP",
            template_family: "generative_template",
            template: ["demonstrative!", "classifier!", "head_noun!"],
            assigned_slots: ["demonstrative", "classifier", "head_noun"],
            surfaces: children.map((node) => flattenSurface2(node)),
            np_subtype: "demonstrative_classifier_cup_noun",
            lexical_disambiguation: "杯_noun_not_classifier",
            reason: "The NP subsystem generalizes the rendered 杯 noun repair to the independently observed 個/隻 classifier profiles. The noun sense bui1 ‘cup’ is independently dictionary-attested; this repair changes lexical role only and adds no grammar license."
          })
        }));
      }
      function transparentOneCountClassifierSplitFromRest2(rest) {
        const quantitySurface = "一";
        const classifierSurface = "個";
        const prefix = quantitySurface + classifierSurface;
        if (!rest.startsWith(prefix)) return null;
        const after = rest.slice(prefix.length);
        if (!after || PUNCT_RE2.test(after)) return null;
        const nextTerm = selectLexiconTerm2(after);
        if (!nextTerm) return null;
        const nextNode = token3(nextTerm.surface);
        if (!nodeCanFillSlot2(nextNode, "head_noun")) return null;
        return phraseMatch2(prefix.length, [
          token3(quantitySurface, {
            label: "how",
            syntax: "quantity count_value numeral_one",
            slots: ["quantity"],
            note: "一 is the visible numeral/quantity before 個; it is not a stative/like predicate in this NP.",
            trace: traceInfo2("atomic_lexicon", {
              surface: quantitySurface,
              generated_slots: ["quantity"],
              transparent_token_split: "one_count_quantified_classifier_np",
              following_classifier: classifierSurface,
              following_head_noun: nextTerm.surface,
              reason: "v0.5.97 splits 一個 + head noun before fused lexical lookup so QuantifiedClassifierNP can carry the generated template trace."
            })
          }),
          token3(classifierSurface, {
            label: "measure_word",
            syntax: "general_classifier classifier",
            slots: ["classifier"],
            note: "個 is the visible classifier/measure word after 一.",
            trace: traceInfo2("atomic_lexicon", {
              surface: classifierSurface,
              generated_slots: ["classifier"],
              transparent_token_split: "one_count_quantified_classifier_np",
              following_head_noun: nextTerm.surface,
              reason: "v0.5.97 keeps 個 learner-visible as measure_word instead of letting it disappear inside a fused 一個人 token."
            })
          })
        ]);
      }
      function transparentQuantifiedPersonNpFromRest2(rest) {
        if (!rest.startsWith("好多人")) return null;
        const children = [
          token3("好多", {
            label: "how",
            syntax: "quantity_degree quantity",
            slots: ["quantity"],
            note: "好多 is the quantity component inside 好多人."
          }),
          token3("人", {
            label: "who",
            syntax: "person_head_noun head_noun",
            slots: ["head_noun", "np", "subject", "topic"],
            note: "人 is the person head noun inside 好多人."
          })
        ];
        return phraseMatch2("好多人".length, construction2("QuantifiedPersonNP", "NP", children, {
          primary: "subject",
          slots: cleanSlots2(["np", "subject", "topic", "object", "head_noun", "quantity"]),
          note: "好多人 is rendered transparently as quantity + person head noun while the whole phrase can still fill the subject/who role.",
          trace: traceInfo2("generative_template", {
            construction_type: "QuantifiedPersonNP",
            template_family: "generative_template",
            template: ["quantity!", "head_noun!"],
            assigned_slots: ["quantity", "head_noun"],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "v0.5.82 exposes 好多人 internally instead of flattening it into one who token."
          })
        }));
      }
      function transparentDemonstrativeClassifierSplitFromRest2(rest) {
        const match = TRANSPARENT_DEMONSTRATIVE_CLASSIFIER_SPLITS.find((spec) => rest.startsWith(spec.surface));
        if (!match) return null;
        const after = rest.slice(match.surface.length);
        if (!after || PUNCT_RE2.test(after)) return null;
        const nextTerm = selectLexiconTerm2(after);
        if (!nextTerm) return null;
        const nextNode = token3(nextTerm.surface);
        if (!nodeCanFillSlot2(nextNode, "head_noun")) return null;
        return phraseMatch2(match.surface.length, [
          token3(match.demonstrative, {
            label: "func",
            syntax: "demonstrative_determiner",
            note: `${match.demonstrative} is a transparent demonstrative determiner before a classifier.`,
            trace: traceInfo2("atomic_lexicon", {
              surface: match.demonstrative,
              generated_slots: ["demonstrative"],
              transparent_token_split: "demonstrative_classifier_np",
              parent_surface: match.surface,
              reason: "Transparent split token inside a generated OvertHeadDemonstrativeClassifierNP; the compatibility display label remains DemonstrativeClassifierNP."
            })
          }),
          token3(match.classifier, {
            label: "measure_word",
            syntax: match.classifierSyntax,
            note: `${match.classifier} is a transparent measure word/classifier after a demonstrative determiner.`,
            trace: traceInfo2("atomic_lexicon", {
              surface: match.classifier,
              generated_slots: ["classifier"],
              transparent_token_split: "demonstrative_classifier_np",
              parent_surface: match.surface,
              reason: "Transparent split token inside a generated OvertHeadDemonstrativeClassifierNP; the compatibility display label remains DemonstrativeClassifierNP."
            })
          })
        ]);
      }
      function transparentNominalDiDeterminerFromRest2(rest) {
        if (!rest.startsWith("啲")) return null;
        const after = rest.slice("啲".length);
        if (!after || PUNCT_RE2.test(after)) return null;
        const nextTerm = selectLexiconTerm2(after);
        if (!nextTerm) return null;
        const nextNode = token3(nextTerm.surface);
        if (!nodeCanFillSlot2(nextNode, "head_noun")) return null;
        return phraseMatch2("啲".length, token3("啲", {
          label: "func",
          syntax: "di_determiner",
          note: "啲 is a nominal determiner/partitive marker before a visible head noun here, not an adverbial how token.",
          trace: traceInfo2("atomic_lexicon", {
            surface: "啲",
            generated_slots: ["di_determiner", "quantity"],
            transparent_token_split: "di_marked_np",
            following_head_noun: nextTerm.surface,
            reason: "Transparent determiner token inside a generated DiMarkedNP; the NP construction carries the generative template trace."
          })
        }));
      }
      function transparentDeicticClassifierTopicFromNode2(node, contextType) {
        const surface = flattenSurface2(node);
        const spec = TRANSPARENT_DEMONSTRATIVE_CLASSIFIER_SPLITS.find((item) => item.surface === surface);
        if (!spec) return null;
        const dem = token3(spec.demonstrative, {
          label: "func",
          syntax: "demonstrative_determiner",
          note: `${spec.demonstrative} is a transparent demonstrative determiner inside ${contextType || "a bounded topic"}.`,
          trace: traceInfo2("generative_template", {
            construction_type: "Topic",
            template: ["demonstrative!", "classifier!"],
            assigned_slot: "demonstrative",
            parent_surface: surface,
            reason: "v0.5.74 semantic contract cleanup splits fused demonstrative-classifier topics such as 呢個 when they are the visible topic of a definition frame."
          })
        });
        const classifier = token3(spec.classifier, {
          label: "measure_word",
          syntax: spec.classifierSyntax,
          note: `${spec.classifier} is the transparent classifier inside ${contextType || "a bounded topic"}.`,
          trace: traceInfo2("generative_template", {
            construction_type: "Topic",
            template: ["demonstrative!", "classifier!"],
            assigned_slot: "classifier",
            parent_surface: surface,
            reason: "v0.5.74 semantic contract cleanup splits fused demonstrative-classifier topics such as 呢個 when they are the visible topic of a definition frame."
          })
        });
        return construction2("Topic", "Topic", [dem, classifier], {
          slots: cleanSlots2(["topic", "np", "demonstrative", "classifier", "deictic_classifier_topic"]),
          note: `${surface} is preserved as transparent demonstrative + classifier topic material, not an opaque fused topic token.`,
          trace: traceInfo2("generative_template", {
            construction_type: "Topic",
            template_family: "generative_template",
            template: ["demonstrative!", "classifier!"],
            assigned_slots: ["demonstrative", "classifier"],
            topic_subtype: "deictic_classifier_ellipsis",
            surfaces: [spec.demonstrative, spec.classifier],
            reason: "v0.5.74 semantic contract cleanup: Definition topics such as 呢個 keep transparent demonstrative/classifier internals.",
            not_claims: ["not_opaque_deictic_classifier_topic"]
          })
        });
      }
      function quantifiedPersonNPFromFusedNode2(node) {
        if (!node || flattenSurface2(node) !== "一個人") return null;
        const quantity = token3("一", {
          label: "how",
          syntax: "quantity count_value numeral_one",
          slots: ["quantity"],
          note: "一 is the visible numeral/quantity descriptor in 一個人, not a stative/like predicate.",
          trace: traceInfo2("generative_template", { construction_type: "QuantifiedClassifierNP", assigned_slot: "quantity", learner_role: "how", parent_surface: "一個人" })
        });
        const classifier = token3("個", {
          label: "measure_word",
          syntax: "general_classifier",
          note: "個 is the visible classifier in 一個人.",
          trace: traceInfo2("generative_template", { construction_type: "QuantifiedClassifierNP", assigned_slot: "classifier", parent_surface: "一個人" })
        });
        const head = token3("人", {
          label: "who",
          syntax: "person_head_noun",
          note: "人 is the visible person head noun in 一個人.",
          trace: traceInfo2("generative_template", { construction_type: "QuantifiedClassifierNP", assigned_slot: "head_noun", parent_surface: "一個人" })
        });
        return construction2("QuantifiedClassifierNP", "NP", [quantity, classifier, head], {
          slots: cleanSlots2(["quantified_classifier_np", "quantity", "classifier", "head_noun", "person_np", "np", "topic", "object", "subject"]),
          note: "Transparent quantified classifier NP: 一 + 個 + 人, used here as the per-person scalar domain.",
          trace: traceInfo2("generative_template", {
            construction_type: "QuantifiedClassifierNP",
            template_family: "generative_template",
            template: ["quantity!", "classifier!", "head_noun!"],
            assigned_slots: ["quantity", "classifier", "head_noun"],
            surfaces: ["一", "個", "人"],
            reason: "v0.5.97 cleanup: 一個人 is transparent classifier/person NP material; 一 is quantity/numeral material, not a stative-like predicate.",
            not_claims: ["not_opaque_quantity_person_np"]
          })
        });
      }
      return {
        quantifiedPersonNPFromFusedNode: quantifiedPersonNPFromFusedNode2,
        transparentCupNounDemonstrativeNpFromRest: transparentCupNounDemonstrativeNpFromRest2,
        transparentDeicticClassifierTopicFromNode: transparentDeicticClassifierTopicFromNode2,
        transparentDemonstrativeClassifierSplitFromRest: transparentDemonstrativeClassifierSplitFromRest2,
        transparentEllipticalDemonstrativeClassifierFromRest: transparentEllipticalDemonstrativeClassifierFromRest2,
        transparentNominalDiDeterminerFromRest: transparentNominalDiDeterminerFromRest2,
        transparentOneCountClassifierSplitFromRest: transparentOneCountClassifierSplitFromRest2,
        transparentQuantifiedPersonNpFromRest: transparentQuantifiedPersonNpFromRest2
      };
    };
  }
});

// src/parser/tokenization/contextual-overrides.js
var require_contextual_overrides = __commonJS({
  "src/parser/tokenization/contextual-overrides.js"(exports2, module2) {
    "use strict";
    module2.exports = function createContextualLexiconOverrides2(dependencies = {}) {
      const {
        PROHIBITIVE_OBJECT_STARTERS: PROHIBITIVE_OBJECT_STARTERS2,
        TOKEN_LEXICON: TOKEN_LEXICON2,
        generateTokenSlots: generateTokenSlots2,
        inferTokenFeatures: inferTokenFeatures2,
        isTopicCandidate: isTopicCandidate2,
        normalizeSurface: normalizeSurface2,
        selectLexiconTerm: selectLexiconTerm2,
        selectionDecisionForSurface: selectionDecisionForSurface2,
        token: token3,
        traceInfo: traceInfo2
      } = dependencies;
      function startsWithProhibitiveObject(text) {
        const normalized = normalizeSurface2(text);
        return PROHIBITIVE_OBJECT_STARTERS2.some((surface) => normalized.startsWith(surface));
      }
      function shouldSplitNegatedLexicalizedStative(nodes, text, cursor, spec) {
        const after = text.slice(cursor + spec.surface.length);
        const bare = !nodes.length && normalizeSurface2(text.slice(cursor)) === spec.surface;
        if (bare) return spec.surface === "唔好味";
        if (startsWithProhibitiveObject(after)) return false;
        return nodes.length > 0;
      }
      function pushNegatedLexicalizedStativeSplit2(nodes, text, cursor, spec) {
        if (shouldSplitNegatedLexicalizedStative(nodes, text, cursor, spec)) {
          const after = text.slice(cursor + spec.surface.length);
          nodes.push(token3("唔"));
          nodes.push(token3(spec.predicate, { selection_decision: selectionDecisionForSurface2(spec.predicate, spec.predicate + after) }));
          return spec.surface.length;
        }
        nodes.push(token3("唔好", { label: "func", syntax: "prohibitive_marker" }));
        if (spec.verb !== "味") nodes.push(token3(spec.verb, { label: "doing", syntax: "verb" }));
        else nodes.push(token3("味", { label: "neutral", syntax: "unknown_cjk_or_text", note: "Bare 唔好味 is treated as negated 好味; prohibitive 唔好 + 味 is not a normal command pattern." }));
        return spec.surface.length;
      }
      function pushSpecialNotGoodEat2(nodes, text, cursor) {
        const previous = nodes.length ? nodes[nodes.length - 1] : null;
        const beforeTopic = previous && isTopicCandidate2(previous);
        const after = text.slice(cursor + "唔好食".length);
        const followedByObject = ["呢個", "嗰個", "呢啲", "嗰啲", "飯", "嘢", "水"].some((surface) => after.startsWith(surface));
        const bare = !previous && normalizeSurface2(text.slice(cursor)) === "唔好食";
        if (bare) {
          const candidateAnalyses = [
            {
              construction: "NegatedStativePredicate",
              split: ["唔", "好食"],
              meaning_hint: "not tasty",
              parser_active: false
            },
            {
              construction: "ProhibitiveImperative",
              split: ["唔好", "食"],
              meaning_hint: "don't eat",
              parser_active: false
            }
          ];
          nodes.push(token3("唔好食", {
            label: "neutral",
            jyutping: "m4 hou2 sik6",
            syntax: "ambiguous_needs_context",
            note: "Ambiguous: 唔 + 好食 = not tasty; 唔好 + 食 = don't eat.",
            review: "needs_context",
            trace: traceInfo2("special_ambiguity_rule", { surface: "唔好食", reason: "Bare ambiguous string requires context.", candidate_analyses: candidateAnalyses })
          }));
          return "唔好食".length;
        }
        if (beforeTopic || !followedByObject) {
          nodes.push(token3("唔"));
          nodes.push(token3("好食", { selection_decision: selectionDecisionForSurface2("好食", "好食" + after) }));
          return "唔好食".length;
        }
        nodes.push(token3("唔好", { label: "func", syntax: "prohibitive_marker" }));
        nodes.push(token3("食", { label: "doing", syntax: "verb" }));
        return "唔好食".length;
      }
      function contextualLexiconOverrides2(surface, rest) {
        const after = String(rest || "").slice(surface.length);
        if (surface === "住") {
          if (/^喺/u.test(after)) {
            return {
              analysis_id: "lex:住:residence_verb",
              label: "doing",
              jyutping: "zyu6",
              syntax: "verb residence_verb",
              note: "residence verb 住 before an overt locative phrase"
            };
          }
          if (/^[。！？!?…]*$/u.test(after)) {
            return {
              analysis_id: "lex:住:durative_marker",
              label: "func",
              jyutping: "zyu6",
              syntax: "durative_or_continuing_state_marker",
              slots: [],
              note: "utterance-final durative/state 住 in the bounded audited context"
            };
          }
          return {};
        }
        if (surface === "定") {
          if (/^(?:咗|緊|過|住)/u.test(after)) {
            return {
              analysis_id: "lex:定:decide_fix_verb",
              label: "doing",
              jyutping: "ding6",
              syntax: "verb decide_fix_schedule_verb",
              note: "decide / settle / fix before overt aspect"
            };
          }
          return {
            analysis_id: "lex:定:alternative_connector",
            label: "func",
            jyutping: "ding6",
            syntax: "alternative_question_connector",
            slots: [],
            note: "bare alternative connector 定 in the bounded audited context"
          };
        }
        if (surface !== "咪") return {};
        if (/^(?:又|就|係|過)/u.test(after)) {
          return {
            analysis_id: "lex:咪:discourse_focus_marker",
            label: "func",
            jyutping: "mai6",
            syntax: "discourse_focus_marker",
            slots: [],
            note: "discourse/focus 咪 mai6 in this context"
          };
        }
        const nextChoice = selectLexiconTerm2(after);
        if (nextChoice) {
          const nextEntry = TOKEN_LEXICON2[nextChoice.surface] || {};
          const nextFeatures = inferTokenFeatures2(nextChoice.surface, nextEntry, {});
          const nextSlots = generateTokenSlots2(nextFeatures);
          if (nextFeatures.label === "doing" || nextFeatures.label === "like" || nextSlots.includes("predicate") || nextSlots.includes("action_verb")) {
            return {
              analysis_id: "lex:咪:prohibitive_marker",
              label: "func",
              jyutping: "mai5",
              syntax: "prohibitive_marker",
              note: "prohibitive 咪 mai5 before a predicate"
            };
          }
        }
        return {
          analysis_id: "lex:咪:discourse_focus_marker",
          label: "func",
          jyutping: "mai6",
          syntax: "discourse_marker_review",
          slots: [],
          note: "context-sensitive 咪; defaulted to discourse mai6 because no following predicate was licensed",
          review: "contextual_lexical_disambiguation"
        };
      }
      return {
        contextualLexiconOverrides: contextualLexiconOverrides2,
        pushNegatedLexicalizedStativeSplit: pushNegatedLexicalizedStativeSplit2,
        pushSpecialNotGoodEat: pushSpecialNotGoodEat2
      };
    };
  }
});

// src/parser/detectors/address/vocative.js
var require_vocative = __commonJS({
  "src/parser/detectors/address/vocative.js"(exports2, module2) {
    "use strict";
    module2.exports = function createVocativeAddressDetector2(dependencies = {}) {
      const {
        ADDRESS_FOLLOWERS: ADDRESS_FOLLOWERS2,
        ADDRESS_PREFIXES: ADDRESS_PREFIXES2,
        ADDRESS_SUFFIXES: ADDRESS_SUFFIXES2,
        COMMON_SURNAME_CHARS: COMMON_SURNAME_CHARS2,
        PROTECTED_ADDRESS_TERMS: PROTECTED_ADDRESS_TERMS2,
        PUNCT_RE: PUNCT_RE2,
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        phraseMatch: phraseMatch2,
        token: token3,
        traceInfo: traceInfo2
      } = dependencies;
      function isLikelyCjkNameChar(ch) {
        return /^[\p{Script=Han}]$/u.test(ch || "");
      }
      function addressBoundaryFollows(text) {
        if (!text) return true;
        if (PUNCT_RE2.test(text)) return true;
        return ADDRESS_FOLLOWERS2.some((surface) => text.startsWith(surface));
      }
      function protectedAddressTermAt(rest) {
        return [...PROTECTED_ADDRESS_TERMS2].sort((a, b) => b.length - a.length || a.localeCompare(b)).find((surface) => rest.startsWith(surface) && addressBoundaryFollows(rest.slice(surface.length)));
      }
      function addressPartToken(surface, syntax, note) {
        return parserInactiveTokenClone2(token3(surface, {
          label: "who",
          syntax,
          note
        }), {
          label: "who",
          pos: "noun",
          syntax,
          slots: [syntax],
          reason: "Token is parser-inactive inside a category-based Cantonese address-form candidate; the parent exposes vocative/address affordances."
        });
      }
      function makeVocativeAddressTerm(parts, detail) {
        const surface = parts.map((part) => part.surface).join("");
        const children = parts.map((part) => addressPartToken(part.surface, part.syntax, part.note));
        const templated = categorySubspanFor2(children, ["VocativeAddressTerm"]);
        if (templated) return templated;
        return construction2("VocativeAddressTerm", "Address", children, {
          slots: ["vocative_address_term", "named_address_term", "address_term", "vocative"],
          note: "Guarded Cantonese named address/vocative candidate. It preserves a likely name + address-form span while protected kinship/title words stay ungenerated.",
          trace: traceInfo2("generative_or_heuristic_slot_rule", {
            rule: "candidate name/prefix + Cantonese address suffix",
            surface,
            pattern: detail.pattern,
            confidence: detail.confidence || "medium",
            guardrail: "Fallback only after protected address terms such as 家姐/小姐/姐姐/姐夫/阿姐 are refused and no category template matched."
          })
        });
      }
      function candidateNamedAddressFormFromRest2(rest) {
        if (protectedAddressTermAt(rest)) return null;
        const chars = Array.from(rest);
        if (!chars.length) return null;
        for (const prefix of ADDRESS_PREFIXES2) {
          if (!rest.startsWith(prefix)) continue;
          for (const suffix of ADDRESS_SUFFIXES2) {
            const nameStart = prefix.length;
            const name2 = rest.slice(nameStart, nameStart + 1);
            const candidate2 = `${prefix}${name2}${suffix}`;
            if (!isLikelyCjkNameChar(name2)) continue;
            if (!rest.startsWith(candidate2)) continue;
            if (!addressBoundaryFollows(rest.slice(candidate2.length))) continue;
            if (PROTECTED_ADDRESS_TERMS2.has(candidate2)) continue;
            return phraseMatch2(candidate2.length, makeVocativeAddressTerm([
              { surface: prefix, syntax: "address_prefix", note: "Familiar Cantonese name/address prefix 阿." },
              { surface: name2, syntax: "name_element", note: "Name element inside a fallback address-form candidate." },
              { surface: suffix, syntax: "address_suffix", note: "Cantonese address suffix/title element." }
            ], { pattern: "address_prefix + name_element + address_suffix" }));
          }
          const name = rest.slice(prefix.length, prefix.length + 1);
          const candidate = `${prefix}${name}`;
          if (isLikelyCjkNameChar(name) && rest.startsWith(candidate) && addressBoundaryFollows(rest.slice(candidate.length)) && !PROTECTED_ADDRESS_TERMS2.has(candidate)) {
            return phraseMatch2(candidate.length, makeVocativeAddressTerm([
              { surface: prefix, syntax: "address_prefix", note: "Familiar Cantonese name/address prefix 阿." },
              { surface: name, syntax: "name_element", note: "Name element inside a fallback address-form candidate." }
            ], { pattern: "address_prefix + name_element", confidence: "medium_low" }));
          }
        }
        for (const suffix of ADDRESS_SUFFIXES2) {
          const name = rest.slice(0, 1);
          const candidate = `${name}${suffix}`;
          if (!isLikelyCjkNameChar(name)) continue;
          if (!rest.startsWith(candidate)) continue;
          if (!addressBoundaryFollows(rest.slice(candidate.length))) continue;
          if (PROTECTED_ADDRESS_TERMS2.has(candidate)) continue;
          if ((suffix === "生" || suffix === "先生") && !COMMON_SURNAME_CHARS2.has(name)) continue;
          return phraseMatch2(candidate.length, makeVocativeAddressTerm([
            { surface: name, syntax: suffix === "生" || suffix === "先生" ? "surname_element" : "name_element", note: "Name/surname element inside a fallback address-form candidate." },
            { surface: suffix, syntax: "address_suffix", note: "Cantonese address suffix/title element." }
          ], { pattern: "name_or_surname_element + address_suffix" }));
        }
        return null;
      }
      return {
        candidateNamedAddressFormFromRest: candidateNamedAddressFormFromRest2
      };
    };
  }
});

// src/parser/clause-relations/punctuation.js
var require_punctuation2 = __commonJS({
  "src/parser/clause-relations/punctuation.js"(exports2, module2) {
    "use strict";
    function hasSentencePunctuation2(text) {
      return /[，。！？、；：,.!?;:]/u.test(String(text || ""));
    }
    function isClauseSequenceSeparator2(node) {
      return node && node.kind === "text" && /[，,；;]/u.test(String(node.text || ""));
    }
    function isClauseSequenceTerminal2(node) {
      return node && node.kind === "text" && /[。！？.!?]/u.test(String(node.text || ""));
    }
    module2.exports = { hasSentencePunctuation: hasSentencePunctuation2, isClauseSequenceSeparator: isClauseSequenceSeparator2, isClauseSequenceTerminal: isClauseSequenceTerminal2 };
  }
});

// src/parser/clause-relations/markers.js
var require_markers = __commonJS({
  "src/parser/clause-relations/markers.js"(exports2, module2) {
    "use strict";
    module2.exports = function createClauseRelationMarkers2(dependencies = {}) {
      const { flattenSurface: flattenSurface2, isClauseSequenceSeparator: isClauseSequenceSeparator2, learnerDisplaySlots: learnerDisplaySlots2, nodeSlots: nodeSlots2 } = dependencies;
      const CLAUSE_LINKER_SURFACES2 = /* @__PURE__ */ new Set(["之後", "之前", "然後", "跟住", "跟住就", "先", "再", "就", "咁", "噉", "所以", "因為", "雖然", "不過", "但係", "如果"]);
      const ASSIGNED_SLOT_WRAPPER_COVERAGE_TYPES = /* @__PURE__ */ new Set(["ModalANotAQuestion"]);
      function isTopicFrameLinker2(node) {
        return !!(node && node.kind === "token" && nodeSlots2(node).includes("topic_frame_linker"));
      }
      function isRelationalCoverbLinker2(node) {
        return !!(node && node.kind === "token" && nodeSlots2(node).includes("relational_coverb_linker"));
      }
      function directWrapperItemSurface2(node) {
        if (!node) return "";
        if (node.kind === "text") return node.text || "";
        return flattenSurface2(node);
      }
      function clauseLinkingPivotIndex2(children = [], separatorIndex = -1) {
        if (separatorIndex >= 0) return separatorIndex;
        const pivotSurfaces = /* @__PURE__ */ new Set(["就", "所以", "但係", "不過", "之後", "之前", "然後", "跟住", "跟住就"]);
        for (let index = 1; index < children.length; index++) {
          const node = children[index];
          if (node && node.kind === "token" && pivotSurfaces.has(node.surface || "")) return index;
        }
        return -1;
      }
      function clauseLinkerRole2(node, index, pivotIndex) {
        if (!node || node.kind !== "token") return "";
        const surface = node.surface || "";
        const slots = nodeSlots2(node);
        const side = pivotIndex >= 0 && index > pivotIndex ? "pre_child" : "post_child";
        if (surface === "如果") return "condition_introducer";
        if (surface === "因為") return "reason_introducer";
        if (surface === "所以") return "result_linker";
        if (surface === "與其") return "disfavored_option_introducer";
        if (surface === "不如") return "preferred_option_introducer";
        if (surface === "但係" || surface === "不過") return "contrast_linker";
        if (slots.includes("topic_frame_linker")) return "topic_frame_linker";
        if (slots.includes("relational_coverb_linker")) return "relational_coverb_linker";
        if (surface === "之後" || surface === "之前" || slots.includes("time") || slots.includes("time_head")) return `${side}_temporal_linker`;
        if (surface === "先" || surface === "再" || surface === "然後" || surface === "跟住" || surface === "跟住就") return `${side}_sequence_linker`;
        if (slots.includes("subject")) return `${side}_clause_subject`;
        if (slots.includes("focus_adverb")) return `${side}_focus_adverb`;
        if (surface === "就" || slots.includes("result_marker")) return `${side}_sequence_linker`;
        if (CLAUSE_LINKER_SURFACES2.has(surface) || slots.includes("discourse_marker")) return `${side}_discourse_linker`;
        return "";
      }
      function clauseLinkerInventory2(children = []) {
        const separatorIndex = children.findIndex(isClauseSequenceSeparator2);
        const pivotIndex = clauseLinkingPivotIndex2(children, separatorIndex);
        return children.map((node, index) => {
          if (!node || node.kind !== "token") return null;
          const role = clauseLinkerRole2(node, index, pivotIndex);
          return role ? { surface: node.surface || "", role } : null;
        }).filter(Boolean);
      }
      function clauseLinkingWrapperCoverage2(children = []) {
        const separatorIndexes = children.map((node, index) => isClauseSequenceSeparator2(node) ? index : -1).filter((index) => index >= 0);
        const separatorIndex = separatorIndexes.length ? separatorIndexes[0] : -1;
        const pivotIndex = clauseLinkingPivotIndex2(children, separatorIndex);
        const accountedChildren = [];
        const accountedLinkers = [];
        const accountedSeparators = [];
        const unaccountedTokens = [];
        children.forEach((node, index) => {
          const surface = directWrapperItemSurface2(node);
          if (!node) return;
          if (node.kind === "construction") {
            const precededByTopicFrameLinker = index > 0 && isTopicFrameLinker2(children[index - 1]);
            const precededByRelationalCoverbLinker = index > 0 && isRelationalCoverbLinker2(children[index - 1]);
            accountedChildren.push({
              surface,
              construction: node.type,
              role: precededByTopicFrameLinker ? "topic_frame_domain" : precededByRelationalCoverbLinker ? "relational_coverb_domain" : index < pivotIndex || pivotIndex < 0 ? "left_clause_like" : "right_clause_like"
            });
            return;
          }
          if (isClauseSequenceSeparator2(node)) {
            accountedSeparators.push({ surface, role: "visible_separator" });
            return;
          }
          if (node.kind === "token") {
            const role = clauseLinkerRole2(node, index, pivotIndex);
            if (role) {
              accountedLinkers.push({ surface, role, slots: learnerDisplaySlots2(nodeSlots2(node)) });
            } else {
              unaccountedTokens.push({ surface, kind: "token", index });
            }
            return;
          }
          if (node.kind === "text" && surface.trim()) {
            unaccountedTokens.push({ surface, kind: "text", index });
          }
        });
        return {
          status: unaccountedTokens.length ? "WARN" : "PASS",
          policy: "ClauseRelationGraph may group linked clause-like material, but it must not hide wrapper holes. Every direct item inside the wrapper must be accounted for as a child construction, linker material, or separator material.",
          accounted_children: accountedChildren,
          accounted_linkers: accountedLinkers,
          accounted_separators: accountedSeparators,
          unaccounted_tokens: unaccountedTokens,
          unaccounted_wrapper_token_count: unaccountedTokens.length
        };
      }
      function wrapperSlotDisplayRole2(type, slot) {
        if (type === "ModalANotAQuestion") {
          const roles = {
            subject: "subject",
            modal_a_not_a: "modal_a_not_a",
            modal_positive_arm: "positive_modal_arm",
            negator: "negator",
            modal_negative_arm: "negative_modal_arm",
            vp: "requested_action_vp",
            particle: "final_particle"
          };
          return roles[slot] || slot || "";
        }
        return slot || "";
      }
      function assignedSlotWrapperCoverage2(type, children = [], assignedSlots = []) {
        if (!ASSIGNED_SLOT_WRAPPER_COVERAGE_TYPES.has(type)) return null;
        const accountedParts = [];
        const unaccountedTokens = [];
        children.forEach((node, index) => {
          const surface = directWrapperItemSurface2(node);
          const slot = assignedSlots[index] || "";
          const role = wrapperSlotDisplayRole2(type, slot);
          if (slot && role) {
            const part = {
              surface,
              role,
              assigned_slot: slot,
              kind: node && node.kind ? node.kind : ""
            };
            if (node && node.kind === "construction") part.construction = node.type || "";
            accountedParts.push(part);
            return;
          }
          if (node && node.kind === "text" && !String(surface || "").trim()) return;
          unaccountedTokens.push({ surface, kind: node && node.kind ? node.kind : "unknown", index });
        });
        return {
          status: unaccountedTokens.length ? "WARN" : "PASS",
          coverage_kind: "assigned_slot_wrapper",
          policy: "Greedy-looking parent wrappers may be collapsed in normal learner display only when every direct child is explicitly accounted for by an assigned slot. Collapse must not hide wrapper holes.",
          accounted_parts: accountedParts,
          unaccounted_tokens: unaccountedTokens,
          unaccounted_wrapper_token_count: unaccountedTokens.length
        };
      }
      function wrapperCoverageForConstructionNode2(node) {
        if (!node || node.kind !== "construction") return null;
        if (node.type === "ClauseRelationGraph") {
          return node.trace && node.trace.wrapper_coverage || clauseLinkingWrapperCoverage2(node.children || []);
        }
        if (ASSIGNED_SLOT_WRAPPER_COVERAGE_TYPES.has(node.type)) {
          const trace = node.trace || {};
          return trace.wrapper_coverage || assignedSlotWrapperCoverage2(node.type, node.children || [], trace.assigned_slots || []);
        }
        return null;
      }
      return {
        CLAUSE_LINKER_SURFACES: CLAUSE_LINKER_SURFACES2,
        isTopicFrameLinker: isTopicFrameLinker2,
        isRelationalCoverbLinker: isRelationalCoverbLinker2,
        directWrapperItemSurface: directWrapperItemSurface2,
        clauseLinkingPivotIndex: clauseLinkingPivotIndex2,
        clauseLinkerRole: clauseLinkerRole2,
        clauseLinkerInventory: clauseLinkerInventory2,
        clauseLinkingWrapperCoverage: clauseLinkingWrapperCoverage2,
        wrapperSlotDisplayRole: wrapperSlotDisplayRole2,
        assignedSlotWrapperCoverage: assignedSlotWrapperCoverage2,
        wrapperCoverageForConstructionNode: wrapperCoverageForConstructionNode2
      };
    };
  }
});

// src/parser/clause-relations/conditional.js
var require_conditional = __commonJS({
  "src/parser/clause-relations/conditional.js"(exports2, module2) {
    "use strict";
    module2.exports = function createConditionalClauseRelations2(dependencies = {}) {
      const { cleanSlots: cleanSlots2, construction: construction2, flattenSurface: flattenSurface2, isToken: isToken2, nodeSlots: nodeSlots2, parserInactiveTokenClone: parserInactiveTokenClone2, token: token3, traceInfo: traceInfo2, withoutTrailingParticles: withoutTrailingParticles2 } = dependencies;
      function protectedConditionalMarkerToken2() {
        return token3("嘅話", {
          label: "func",
          pos: "function",
          jyutping: "ge3 waa6",
          syntax: "conditional_marker protected_formula",
          slots: ["conditional_marker"],
          note: "if / in the case that",
          review: "protected_formula",
          trace: traceInfo2("protected_formula_table", {
            surface: "嘅話",
            formula_type: "conditional_marker",
            reason: "嘅話 is a conventional two-character conditional marker. It stays grouped for learner display while the preceding predicate and the full ConditionalClause remain productive and transparent.",
            not_claims: ["not_formula_discourse_unit", "not_reported_speech", "not_whole_clause_lexicalization"]
          })
        });
      }
      function conditionalGeWaaClauseFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (bareCore.length < 2) return null;
        let predicateNodes = [];
        let markerChildren = [];
        const finalNode = bareCore[bareCore.length - 1];
        if (isToken2(finalNode, "嘅話")) {
          predicateNodes = bareCore.slice(0, -1);
          markerChildren = [protectedConditionalMarkerToken2()];
        } else if (bareCore.length >= 3) {
          const ge = bareCore[bareCore.length - 2];
          const waa = bareCore[bareCore.length - 1];
          if (!isToken2(ge, "嘅") || !isToken2(waa, "話")) return null;
          predicateNodes = bareCore.slice(0, -2);
          markerChildren = [protectedConditionalMarkerToken2()];
        } else {
          return null;
        }
        if (predicateNodes.length !== 1) return null;
        const predicate = predicateNodes[0];
        const predicateSurface = flattenSurface2(predicate);
        const predicateSlots = nodeSlots2(predicate);
        const licensedPredicate = ["有", "冇", "得"].includes(predicateSurface) || predicateSlots.some((slot) => ["predicate", "vp", "action_vp", "stative_predicate", "existential", "negated_existential", "acceptability_predicate"].includes(slot));
        if (!licensedPredicate) return null;
        const predicateChild = predicate.kind === "token" ? parserInactiveTokenClone2(predicate, {
          label: predicate.label || "func",
          pos: predicate.features && predicate.features.pos || "function",
          syntax: `${predicate.syntax || "predicate"} conditional_antecedent_predicate`,
          slots: cleanSlots2([...predicate.slots || [], "conditional_antecedent", "conditional_antecedent_predicate", "predicate"]),
          reason: "The predicate is the overt antecedent inside a predicate + 嘅話 conditional clause."
        }) : predicate;
        const children = [predicateChild, ...markerChildren, ...particles];
        return construction2("ConditionalClause", "IfClause", children, {
          slots: cleanSlots2(["conditional_clause", "condition_clause", "conditional_antecedent", "conditional_marker", "predicate", "clause"]),
          note: "Conditional antecedent clause formed by a productive predicate plus the protected marker 嘅話. A following result clause is required unless discourse supplies it.",
          trace: traceInfo2("generative_template", {
            construction_type: "ConditionalClause",
            template_family: "generative_template",
            template: ["conditional_antecedent_predicate!", "protected_conditional_marker!", "particle?"],
            assigned_slots: ["conditional_antecedent_predicate", "conditional_marker", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            conditional_marker_surface: "嘅話",
            conditional_marker_trace: "protected_formula_table",
            context_requirement_status: "context_required",
            missing_argument_slots: ["result_clause"],
            missing_slot_details: [{ slot: "result_clause", license_status: "unresolved" }],
            antecedent_status: "not_applicable",
            reason: "Predicate + 嘅話 is productive. The marker is protected as one learner-visible functional unit so its internal 話 cannot leak the unrelated speech-verb gloss.",
            not_claims: ["not_reported_speech", "not_sentence_final_particle_use", "not_complete_condition_result_without_result", "not_whole_clause_protected_formula"]
          })
        });
      }
      return { protectedConditionalMarkerToken: protectedConditionalMarkerToken2, conditionalGeWaaClauseFallback: conditionalGeWaaClauseFallback2 };
    };
  }
});

// src/parser/tokenization/tokenize-line.js
var require_tokenize_line = __commonJS({
  "src/parser/tokenization/tokenize-line.js"(exports2, module2) {
    "use strict";
    var PUNCT_RE2 = require_punctuation();
    module2.exports = function createTokenizer(dependencies = {}) {
      const {
        FORMULAS: FORMULAS2,
        NEGATED_LEXICALIZED_STATIVE_SPLITS: NEGATED_LEXICALIZED_STATIVE_SPLITS2,
        PRODUCTIVE_TERMS: PRODUCTIVE_TERMS2,
        candidateNamedAddressFormFromRest: candidateNamedAddressFormFromRest2,
        construction: construction2,
        contextualLexiconOverrides: contextualLexiconOverrides2,
        productiveVoComponentTokens: productiveVoComponentTokens2,
        protectedConditionalMarkerToken: protectedConditionalMarkerToken2,
        pushNegatedLexicalizedStativeSplit: pushNegatedLexicalizedStativeSplit2,
        pushSpecialNotGoodEat: pushSpecialNotGoodEat2,
        selectLexiconTerm: selectLexiconTerm2,
        textNode: textNode2,
        token: token3,
        traceInfo: traceInfo2,
        transparentCupNounDemonstrativeNpFromRest: transparentCupNounDemonstrativeNpFromRest2,
        transparentDemonstrativeClassifierSplitFromRest: transparentDemonstrativeClassifierSplitFromRest2,
        transparentEllipticalDemonstrativeClassifierFromRest: transparentEllipticalDemonstrativeClassifierFromRest2,
        transparentNominalDiDeterminerFromRest: transparentNominalDiDeterminerFromRest2,
        transparentOneCountClassifierSplitFromRest: transparentOneCountClassifierSplitFromRest2,
        transparentQuantifiedPersonNpFromRest: transparentQuantifiedPersonNpFromRest2
      } = dependencies;
      function tokenizeLine2(source) {
        const text = String(source || "");
        const nodes = [];
        let cursor = 0;
        while (cursor < text.length) {
          const rest = text.slice(cursor);
          const punctuation = rest.match(PUNCT_RE2);
          if (punctuation) {
            nodes.push(textNode2(punctuation[0]));
            cursor += punctuation[0].length;
            continue;
          }
          if (rest.startsWith("唔好食")) {
            cursor += pushSpecialNotGoodEat2(nodes, text, cursor);
            continue;
          }
          const negatedLexicalizedStative = NEGATED_LEXICALIZED_STATIVE_SPLITS2.find((spec) => spec.surface !== "唔好食" && rest.startsWith(spec.surface));
          if (negatedLexicalizedStative) {
            cursor += pushNegatedLexicalizedStativeSplit2(nodes, text, cursor, negatedLexicalizedStative);
            continue;
          }
          const formula = FORMULAS2.find((surface) => rest.startsWith(surface));
          if (formula) {
            nodes.push(construction2("FormulaDiscourseUnit", "Formula", [token3(formula)], {
              note: "Protected formula stays grouped.",
              trace: traceInfo2("protected_formula_table", { surface: formula, reason: "Protected formula is intentionally opaque." })
            }));
            cursor += formula.length;
            continue;
          }
          const addressForm = candidateNamedAddressFormFromRest2(rest);
          if (addressForm) {
            nodes.push(addressForm.node);
            cursor += addressForm.length;
            continue;
          }
          const vo = PRODUCTIVE_TERMS2.find((surface) => rest.startsWith(surface));
          if (vo) {
            nodes.push(...productiveVoComponentTokens2(vo));
            cursor += vo.length;
            continue;
          }
          const cupNounDemonstrativeNp = transparentCupNounDemonstrativeNpFromRest2(rest);
          if (cupNounDemonstrativeNp) {
            nodes.push(cupNounDemonstrativeNp.node);
            cursor += cupNounDemonstrativeNp.length;
            continue;
          }
          const demonstrativeClassifierSplit = transparentDemonstrativeClassifierSplitFromRest2(rest);
          if (demonstrativeClassifierSplit) {
            nodes.push(...demonstrativeClassifierSplit.node);
            cursor += demonstrativeClassifierSplit.length;
            continue;
          }
          const ellipticalDemonstrativeClassifier = transparentEllipticalDemonstrativeClassifierFromRest2(rest);
          if (ellipticalDemonstrativeClassifier) {
            nodes.push(ellipticalDemonstrativeClassifier.node);
            cursor += ellipticalDemonstrativeClassifier.length;
            continue;
          }
          const oneCountClassifierSplit = transparentOneCountClassifierSplitFromRest2(rest);
          if (oneCountClassifierSplit) {
            nodes.push(...oneCountClassifierSplit.node);
            cursor += oneCountClassifierSplit.length;
            continue;
          }
          const quantifiedPersonNp = transparentQuantifiedPersonNpFromRest2(rest);
          if (quantifiedPersonNp) {
            nodes.push(quantifiedPersonNp.node);
            cursor += quantifiedPersonNp.length;
            continue;
          }
          const nominalDiDeterminer = transparentNominalDiDeterminerFromRest2(rest);
          if (nominalDiDeterminer) {
            nodes.push(nominalDiDeterminer.node);
            cursor += nominalDiDeterminer.length;
            continue;
          }
          const termChoice = selectLexiconTerm2(rest);
          if (termChoice) {
            nodes.push(termChoice.surface === "嘅話" ? protectedConditionalMarkerToken2() : token3(termChoice.surface, {
              selection_decision: termChoice.selection_decision,
              ...contextualLexiconOverrides2(termChoice.surface, rest)
            }));
            cursor += termChoice.surface.length;
            continue;
          }
          const char = Array.from(rest)[0] || "";
          nodes.push(token3(char, { label: "neutral", syntax: "unknown_cjk_or_text", note: "Unknown item; shown neutrally." }));
          cursor += char.length;
        }
        return nodes;
      }
      return { tokenizeLine: tokenizeLine2 };
    };
  }
});

// src/parser/orchestration/apply-construction-patterns.js
var require_apply_construction_patterns = __commonJS({
  "src/parser/orchestration/apply-construction-patterns.js"(exports2, module2) {
    "use strict";
    module2.exports = function createApplyConstructionPatterns(dependencies = {}) {
      const {
        isParticle: isParticle2,
        nodeSlots: nodeSlots2,
        priorityMarkerClauseWithTrailingParticle: priorityMarkerClauseWithTrailingParticle2,
        serialVerbPurposeChainWithTrailingParticle: serialVerbPurposeChainWithTrailingParticle2,
        wrapCore
      } = dependencies;
      function withoutTrailingParticles2(nodes) {
        let end = nodes.length;
        while (end > 0 && isParticle2(nodes[end - 1])) end--;
        return { core: nodes.slice(0, end), particles: nodes.slice(end) };
      }
      function applyConstructionPatterns2(nodes) {
        if (!nodes.length) return nodes;
        const { core, particles } = withoutTrailingParticles2(nodes);
        if (particles.length) {
          const withParticles = wrapCore([...core, ...particles]);
          if (withParticles.length === 1 && withParticles[0].kind === "construction") return withParticles;
        }
        const wrapped = wrapCore(core);
        if (particles.length && wrapped.length) {
          const last = wrapped[wrapped.length - 1];
          if (last && last.kind === "construction" && last.type === "PriorityMarkerClause") {
            return [
              ...wrapped.slice(0, -1),
              priorityMarkerClauseWithTrailingParticle2(last, particles[0]),
              ...particles.slice(1)
            ];
          }
          if (last && last.kind === "construction" && ["SerialVerbPurposeChain", "MotionPurposeChain"].includes(last.type)) {
            return [
              ...wrapped.slice(0, -1),
              serialVerbPurposeChainWithTrailingParticle2(last, particles[0]),
              ...particles.slice(1)
            ];
          }
        }
        return [...wrapped, ...particles];
      }
      function optionalSubjectOffset2(core) {
        if (!core.length) return 0;
        const slots = nodeSlots2(core[0]);
        return slots.includes("subject") ? 1 : 0;
      }
      return {
        applyConstructionPatterns: applyConstructionPatterns2,
        optionalSubjectOffset: optionalSubjectOffset2,
        withoutTrailingParticles: withoutTrailingParticles2
      };
    };
  }
});

// src/parser/detectors/boundaries/fragments.js
var require_fragments = __commonJS({
  "src/parser/detectors/boundaries/fragments.js"(exports2, module2) {
    "use strict";
    module2.exports = function createBoundaryFragmentDetectors(dependencies = {}) {
      const {
        cleanSlots: cleanSlots2,
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSurfaceMatches: nodeSurfaceMatches2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function possessiveFragmentAnswerCandidate2(core) {
        if (!core || core.length !== 2) return null;
        const possessor = core[0];
        const linker = core[1];
        if (!nodeSurfaceMatches2(linker, ["嘅"])) return null;
        if (!nodeCanFillSlot2(possessor, "subject") && !nodeCanFillSlot2(possessor, "np") && !nodeCanFillSlot2(possessor, "topic")) return null;
        if (!nodeCanFillSlot2(linker, "nominal_linker") && !nodeCanFillSlot2(linker, "particle")) return null;
        return construction2("FragmentAnswer", "fragment answer", core, {
          slots: ["fragment_answer", "possessive_fragment", "answer_fragment", "np", "subject", "nominal_linker"],
          note: "Possessive answer fragment: possessor + 嘅 is valid as a short answer whose head noun comes from context.",
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "FragmentAnswer",
            fragment_subtype: "possessive_answer",
            template: ["possessor!", "nominal_linker!"],
            assigned_slots: ["possessor", "nominal_linker"],
            surfaces: core.map((node) => flattenSurface2(node)),
            reason: "Treat possessor + 嘅 as a context-dependent answer fragment, not a full clean NominalHeadSpan.",
            context_requirement_status: "context_required",
            missing_argument_slots: ["nominal_head"],
            missing_slot_details: [{ slot: "nominal_head", license_status: "unresolved" }],
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            antecedent_required: true,
            overt_head: "嘅",
            omission_analysis_candidates: ["nominal_head_ellipsis"],
            semantic_review_flags: ["fragment_answer_parse", "context_dependent_possessive_fragment"],
            not_claims: ["not_full_np_without_context", "not_silent_head_noun_insertion"]
          })
        });
      }
      function fragmentQuestionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (!bareCore.length || bareCore.length > 2) return null;
        const marker = bareCore[bareCore.length - 1];
        if (!isToken2(marker, "呢")) return null;
        const topic = bareCore.length === 2 ? bareCore[0] : null;
        if (topic && !nodeCanFillSlot2(topic, "subject") && !nodeCanFillSlot2(topic, "topic") && !nodeCanFillSlot2(topic, "np")) return null;
        const markerChild = parserInactiveTokenClone2(marker, {
          label: "func",
          pos: "function",
          syntax: "discourse_fragment_question",
          jyutping: "ne1",
          note: "what about…? / and…?",
          slots: ["question_fragment", "discourse_fragment"],
          reason: "Standalone 呢 is interpreted as a context-dependent 'what about...?' fragment question, not as a demonstrative determiner."
        });
        const topicChildren = topic ? [parserInactiveTokenClone2(topic, {
          label: firstToken2(topic) && firstToken2(topic).label || "who",
          pos: "np",
          syntax: "topic_return_fragment",
          slots: ["topic", "np"],
          reason: "The overt NP/pronoun is the visible topic returned to the prior discourse question."
        })] : [];
        const children = [...topicChildren, markerChild, ...particles];
        return construction2("FragmentQuestion", "FragmentQ", children, {
          note: "Context-dependent fragment question headed by standalone 呢.",
          slots: cleanSlots2(["fragment_question", "question_fragment", "discourse_fragment", "clause", ...templateDerivedSlots2("FragmentQuestion", children)]),
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "FragmentQuestion",
            template_family: "generative_template",
            template: ["topic_return?", "fragment_question_marker!", "particle?"],
            assigned_slots: [...topicChildren.map(() => "topic_return"), "fragment_question_marker", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Standalone 呢 conventionally asks for a context-supplied topic or alternative.",
            context_requirement_status: "context_required",
            missing_argument_slots: [topic ? "contrast_set_or_predicate" : "topic_or_alternative"],
            missing_slot_details: [{ slot: topic ? "contrast_set_or_predicate" : "topic_or_alternative", license_status: "unresolved" }],
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            antecedent_required: true,
            topic_return: true,
            topic_return_surface: topic ? flattenSurface2(topic) : "",
            omission_analysis_candidates: ["fragment_question", "topic_return_ellipsis"],
            semantic_review_flags: ["needs_context_parse", "fragment_question_context_required"],
            not_claims: ["not_complete_polar_question", "not_demonstrative_np"]
          })
        });
      }
      return {
        possessiveFragmentAnswerCandidate: possessiveFragmentAnswerCandidate2,
        fragmentQuestionFallback: fragmentQuestionFallback2
      };
    };
  }
});

// src/parser/detectors/questions/a-not-a.js
var require_a_not_a = __commonJS({
  "src/parser/detectors/questions/a-not-a.js"(exports2, module2) {
    "use strict";
    module2.exports = function createANotAQuestionDetectors2(dependencies = {}) {
      const {
        assignedSlotWrapperCoverage: assignedSlotWrapperCoverage2,
        applyConstructionPatterns: applyConstructionPatterns2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        directPredicateCapableNode: directPredicateCapableNode2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isParticle: isParticle2,
        isToken: isToken2,
        isVerbLike: isVerbLike2,
        nodeCanFillSlot: nodeCanFillSlot2,
        optionalSubjectOffset: optionalSubjectOffset2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        phase4DesiderativeActiveTokenClone: phase4DesiderativeActiveTokenClone2,
        phase4PermissionActiveTokenClone: phase4PermissionActiveTokenClone2,
        possessiveFragmentAnswerCandidate: possessiveFragmentAnswerCandidate2,
        surfaceOf: surfaceOf2,
        templateDerivedSlots: templateDerivedSlots2,
        token: token3,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2,
        wrapCategorySubspans: wrapCategorySubspans2,
        yesNoQuestionMarkerClone: yesNoQuestionMarkerClone2
      } = dependencies;
      function aNotAQuestionFallback2(core) {
        const preferenceANotA = preferenceANotAQuestionFallback(core);
        if (preferenceANotA) return preferenceANotA;
        const offset = optionalSubjectOffset2(core);
        if (core.length - offset < 3) return null;
        const first = core[offset];
        const negator = core[offset + 1];
        const second = core[offset + 2];
        const firstTok = firstToken2(first);
        const secondTok = firstToken2(second);
        if (!firstTok || !secondTok) return null;
        if (!isVerbLike2(first) || !isToken2(negator, "唔")) return null;
        if (firstTok.surface !== secondTok.surface) return null;
        return construction2("ANotAQuestion", "A-not-A", core, {
          note: "A-not-A polar question construction with optional subject and following object/goal.",
          trace: traceInfo2("generative_or_heuristic_slot_rule", {
            rule: "subject? + verb + 唔 + copied verb + complement?",
            reason: "Structural A-not-A heuristic runs before VP subspan wrapping so the copied verb remains visible."
          })
        });
      }
      function splitFinalQuestionMaterial(core, options = {}) {
        let questionCore = core;
        const punctuation = [];
        while (questionCore.length && isQuestionPunctuationText(questionCore[questionCore.length - 1])) {
          punctuation.unshift(questionCore[questionCore.length - 1]);
          questionCore = questionCore.slice(0, -1);
        }
        let particleStart = questionCore.length;
        while (particleStart > 0 && isParticle2(questionCore[particleStart - 1]) && !(options.keepGeParticle && isToken2(questionCore[particleStart - 1], "嘅"))) {
          particleStart -= 1;
        }
        return {
          bareCore: questionCore.slice(0, particleStart),
          particles: questionCore.slice(particleStart),
          punctuation
        };
      }
      function alternativeScalarQuestionTail(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length < 4) return null;
        const connectorIndex = compact.findIndex((node) => isToken2(node, "定係"));
        if (connectorIndex <= 0 || connectorIndex >= compact.length - 2) return null;
        if (surfaceOf2(compact[compact.length - 1]) !== "多啲") return null;
        const left = compact.slice(0, connectorIndex);
        const right = compact.slice(connectorIndex + 1, -1);
        if (!left.length || !right.length) return null;
        if ([...left, ...right].some((node) => isParticle2(node) || isQuestionPunctuationText(node))) return null;
        return {
          children: compact,
          assignedSlots: compact.map((node, index) => {
            if (index === connectorIndex) return "question_fragment";
            if (index === compact.length - 1) return "degree";
            return "predicate";
          })
        };
      }
      function preferenceANotAQuestionFallback(core) {
        const { bareCore, particles } = splitFinalQuestionMaterial(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        const offset = compact.length >= 5 && nodeCanFillSlot2(compact[0], "subject") ? 1 : 0;
        if (compact.length - offset < 4) return null;
        const firstSyllable = compact[offset];
        const negator = compact[offset + 1];
        const preferencePredicate = compact[offset + 2];
        if (!isToken2(firstSyllable, "鍾") || !isToken2(negator, "唔") || !isToken2(preferencePredicate, "鍾意")) return null;
        const complementCore = compact.slice(offset + 3);
        if (!complementCore.length) return null;
        const wrappedComplement = applyConstructionPatterns2(complementCore);
        if (wrappedComplement.length === 1) {
          const complement = wrappedComplement[0];
          const complementSlot = nodeCanFillSlot2(complement, "vp") ? "vp" : nodeCanFillSlot2(complement, "np") || nodeCanFillSlot2(complement, "object") ? "object" : "";
          if (!complementSlot) return null;
          const children2 = [...compact.slice(0, offset), firstSyllable, negator, preferencePredicate, complement, ...particles];
          const assignedSlots2 = [
            ...compact.slice(0, offset).map(() => "subject"),
            "a_not_a_first_syllable",
            "negator",
            "preference_predicate",
            complementSlot,
            ...particles.map(() => "particle")
          ];
          return construction2("ANotAQuestion", "A-not-A", children2, {
            note: "Bounded first-syllable preference A-not-A question: optional subject + 鍾唔鍾意 + overt typed NP/VP complement.",
            slots: cleanSlots2(["question_fragment", "predicate", "negator", complementSlot, ...templateDerivedSlots2("ANotAQuestion", children2)]),
            trace: traceInfo2("generative_template", {
              construction_type: "ANotAQuestion",
              template_family: "first_syllable_preference_a_not_a",
              template: ["subject?", "a_not_a_first_syllable!", "negator!", "preference_predicate!", `${complementSlot}!`, "particle?"],
              assigned_slots: assignedSlots2,
              surfaces: children2.map((node) => flattenSurface2(node)),
              copied_surface_profile: "first_syllable_plus_full_preference_predicate",
              reason: "Preserves reviewed 鍾唔鍾意 + overt typed complement questions without reclassifying them as AB33 PreferenceVP."
            })
          });
        }
        const alternative = alternativeScalarQuestionTail(complementCore);
        if (!alternative) return null;
        const children = [
          ...compact.slice(0, offset),
          firstSyllable,
          negator,
          preferencePredicate,
          ...alternative.children,
          ...particles
        ];
        const assignedSlots = [
          ...compact.slice(0, offset).map(() => "subject"),
          "a_not_a_first_syllable",
          "negator",
          "preference_predicate",
          ...alternative.assignedSlots,
          ...particles.map(() => "particle")
        ];
        return construction2("ANotAQuestion", "A-not-A", children, {
          note: "Bounded first-syllable preference A-not-A question with separately preserved alternative/scalar material.",
          slots: cleanSlots2(["question_fragment", "predicate", "negator", "degree", ...templateDerivedSlots2("ANotAQuestion", children)]),
          trace: traceInfo2("construction_function", {
            construction_type: "ANotAQuestion",
            rule: "subject? + 鍾 + 唔 + 鍾意 + alternative material + 定係 + alternative material + 多啲 + particle?",
            assigned_slots: assignedSlots,
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Preserves the reviewed outer first-syllable A-not-A question while leaving alternative/scalar material transparent and outside AB33 PreferenceVP."
          })
        });
      }
      function desiderativeANotAQuestionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const offset = optionalSubjectOffset2(bareCore);
        if (bareCore.length - offset < 4) return null;
        const firstModal = bareCore[offset];
        const negator = bareCore[offset + 1];
        const secondModal = bareCore[offset + 2];
        const firstTok = firstToken2(firstModal);
        const secondTok = firstToken2(secondModal);
        if (!firstTok || !secondTok) return null;
        if (!nodeCanFillSlot2(firstModal, "desiderative_modal")) return null;
        if (!nodeCanFillSlot2(secondModal, "desiderative_modal")) return null;
        if (firstTok.surface !== "想" || secondTok.surface !== "想") return null;
        if (!isToken2(negator, "唔")) return null;
        const complementCore = bareCore.slice(offset + 3);
        if (!complementCore.length) return null;
        const wrappedComplement = wrapCategorySubspans2(complementCore);
        if (wrappedComplement.length !== 1 || !nodeCanFillSlot2(wrappedComplement[0], "vp")) return null;
        const promotedFirst = phase4DesiderativeActiveTokenClone2(firstModal, {
          syntax: `${firstTok.syntax || "modal_desiderative"} desiderative_a_not_a_question`,
          slots: ["desiderative_a_not_a_question"],
          reason: "Phase 4 controlled grammar promotion: first 想 is parser-active only as the positive arm of an approved 想唔想 + VP question."
        });
        const promotedSecond = phase4DesiderativeActiveTokenClone2(secondModal, {
          syntax: `${secondTok.syntax || "modal_desiderative"} desiderative_a_not_a_question`,
          slots: ["desiderative_a_not_a_question"],
          reason: "Phase 4 controlled grammar promotion: second 想 is parser-active only as the copied negative arm of an approved 想唔想 + VP question."
        });
        const children = [
          ...bareCore.slice(0, offset),
          promotedFirst,
          negator,
          promotedSecond,
          ...wrappedComplement,
          ...particles
        ];
        const assignedSlots = [
          ...bareCore.slice(0, offset).map(() => "subject"),
          "modal_positive_arm",
          "negator",
          "modal_negative_arm",
          "vp",
          ...particles.map(() => "particle")
        ];
        const traceDetail = {
          construction_type: "ModalANotAQuestion",
          retired_label_alias: "DesiderativeANotAQuestion",
          modal_subtype: "desiderative",
          template_family: "generative_template",
          template: ["subject?", "modal_positive_arm!", "negator!", "modal_negative_arm!", "vp!", "particle?"],
          assigned_slots: assignedSlots,
          rule: "subject? + 想 + 唔 + 想 + vp + particle?",
          reason: "Promote reviewed desiderative A-not-A questions under the broad ModalANotAQuestion category while preserving desiderative subtype metadata and the visible VP complement requirement.",
          surfaces: children.map((node) => flattenSurface2(node))
        };
        const wrapperCoverage = assignedSlotWrapperCoverage2("ModalANotAQuestion", children, assignedSlots);
        if (wrapperCoverage) traceDetail.wrapper_coverage = wrapperCoverage;
        return construction2("ModalANotAQuestion", "ModalQ", children, {
          note: "Broad modal A-not-A question with desiderative subtype: optional subject + 想唔想 + reviewed VP.",
          slots: templateDerivedSlots2("ModalANotAQuestion", children),
          trace: traceInfo2("generative_template", traceDetail)
        });
      }
      function permissionANotAQuestionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const offset = optionalSubjectOffset2(bareCore);
        if (bareCore.length - offset < 2) return null;
        const modal = bareCore[offset];
        const modalToken = firstToken2(modal);
        if (!modalToken || modalToken.surface !== "可唔可以") return null;
        if (!nodeCanFillSlot2(modal, "modal")) return null;
        const complementCore = bareCore.slice(offset + 1);
        if (!complementCore.length) return null;
        let wrappedComplement = wrapCategorySubspans2(complementCore);
        if (wrappedComplement.length !== 1 && complementCore.length === 2 && nodeCanFillSlot2(complementCore[0], "action_verb") && (nodeCanFillSlot2(complementCore[1], "recipient") || nodeCanFillSlot2(complementCore[1], "subject"))) {
          const personObject = parserInactiveTokenClone2(complementCore[1], {
            label: complementCore[1].label || "who",
            pos: "np",
            syntax: `${complementCore[1].syntax || "person_np"} person_object recipient`,
            slots: ["object", "recipient", "np"],
            reason: "The person after the action verb is its visible person-object/recipient inside the permission question VP."
          });
          const vpChildren = [complementCore[0], personObject];
          wrappedComplement = [construction2("TransitiveVP", "VP", vpChildren, {
            note: "Transitive VP with a person object, preserved inside a permission A-not-A question.",
            slots: templateDerivedSlots2("TransitiveVP", vpChildren),
            trace: traceInfo2("generative_template", {
              construction_type: "TransitiveVP",
              template_family: "generative_template",
              template: ["action_verb!", "person_object!"],
              assigned_slots: ["action_verb", "person_object"],
              surfaces: vpChildren.map((node) => flattenSurface2(node)),
              reason: "Cantonese transitive verbs such as 幫 can take a person pronoun as their object; the person keeps the learner role who."
            })
          })];
        }
        if (wrappedComplement.length !== 1 || !nodeCanFillSlot2(wrappedComplement[0], "vp")) return null;
        const promotedModal = phase4PermissionActiveTokenClone2(modal, {
          syntax: `${modalToken.syntax || "modal_permission_or_ability"} modal_a_not_a_question permission_a_not_a_question`,
          slots: ["modal_a_not_a_question", "permission_a_not_a_question", "permission_a_not_a_modal"],
          reason: "Phase 4 controlled grammar promotion: 可唔可以 is parser-active only as an approved permission A-not-A modal with a visible VP complement."
        });
        const children = [
          ...bareCore.slice(0, offset),
          promotedModal,
          ...wrappedComplement,
          ...particles
        ];
        const assignedSlots = [
          ...bareCore.slice(0, offset).map(() => "subject"),
          "modal_a_not_a",
          "vp",
          ...particles.map(() => "particle")
        ];
        const traceDetail = {
          construction_type: "ModalANotAQuestion",
          retired_label_alias: "PermissionANotAQuestion",
          modal_subtype: "permission",
          template_family: "generative_template",
          template: ["subject?", "modal_a_not_a!", "vp!", "particle?"],
          assigned_slots: assignedSlots,
          surfaces: children.map((node) => flattenSurface2(node)),
          reason: "Promotes reviewed permission A-not-A questions under the broad ModalANotAQuestion category while keeping the 可唔可以 modal token parser-active only in this approved scope."
        };
        const wrapperCoverage = assignedSlotWrapperCoverage2("ModalANotAQuestion", children, assignedSlots);
        if (wrapperCoverage) traceDetail.wrapper_coverage = wrapperCoverage;
        return construction2("ModalANotAQuestion", "ModalQ", children, {
          note: "Broad modal A-not-A question with permission subtype: optional subject + 可唔可以 + reviewed VP.",
          slots: templateDerivedSlots2("ModalANotAQuestion", children),
          trace: traceInfo2("generative_template", traceDetail)
        });
      }
      function copularANotAComplementCandidate(complementCore) {
        const possessive = possessiveFragmentAnswerCandidate2(complementCore);
        if (possessive) return { node: possessive, profile: "possessive_fragment" };
        const quantifiedPreferenceClause = copularANotAQuantifiedPreferenceClauseCandidate(complementCore);
        if (quantifiedPreferenceClause) return { node: quantifiedPreferenceClause, profile: "clausal_or_predicate" };
        const wrapped = applyConstructionPatterns2(complementCore);
        if (wrapped.length === 1 && wrapped[0] && wrapped[0].kind === "construction") {
          const candidate = wrapped[0];
          const nominalTypes = /* @__PURE__ */ new Set([
            "NominalHeadSpan",
            "OvertHeadDemonstrativeClassifierNP",
            "HeadlessDemonstrativeClassifierNP",
            "QuantifiedClassifierNP",
            "QuantifiedPersonNP",
            "DiMarkedNP",
            "OrdinalClassifierNP",
            "ClassifierObjectNP",
            "CoordinatedNP",
            "FragmentAnswer"
          ]);
          const blockedPredicateTypes = /* @__PURE__ */ new Set([
            "ModifierNP",
            "ModifiedNP",
            "NominalHeadSpan",
            "OvertHeadDemonstrativeClassifierNP",
            "HeadlessDemonstrativeClassifierNP",
            "QuantifiedClassifierNP",
            "QuantifiedPersonNP",
            "DiMarkedNP",
            "OrdinalClassifierNP",
            "ClassifierObjectNP",
            "CoordinatedNP",
            "FragmentAnswer",
            "NeedsContext"
          ]);
          const predicateLike = !blockedPredicateTypes.has(candidate.type) && (nodeCanFillSlot2(candidate, "predicate") || directPredicateCapableNode2(candidate));
          if (predicateLike) return { node: candidate, profile: "clausal_or_predicate" };
          if (nominalTypes.has(candidate.type)) return { node: candidate, profile: "nominal" };
        }
        if (complementCore.length === 1 && (nodeCanFillSlot2(complementCore[0], "np") || nodeCanFillSlot2(complementCore[0], "subject"))) {
          return { node: complementCore[0], profile: "nominal" };
        }
        return null;
      }
      function typedPreferencePredicateParts(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length === 1 && compact[0] && compact[0].kind === "construction" && compact[0].type === "ModifierNP") {
          const modifierChildren = withoutIgnorableSpaceText2(compact[0].children || []);
          if (modifierChildren.length !== 2 || !isToken2(modifierChildren[0], "鍾意")) return null;
          const complement = modifierChildren[1];
          const complementSlot = nodeCanFillSlot2(complement, "vp") ? "vp" : nodeCanFillSlot2(complement, "np") || nodeCanFillSlot2(complement, "object") ? "object" : "";
          if (!complementSlot) return null;
          return {
            children: [modifierChildren[0], complement],
            assignedSlots: ["preference_predicate", complementSlot],
            profile: `typed_${complementSlot}`
          };
        }
        if (compact.length === 2 && isToken2(compact[0], "鍾意")) {
          const complementSlot = nodeCanFillSlot2(compact[1], "vp") ? "vp" : nodeCanFillSlot2(compact[1], "np") || nodeCanFillSlot2(compact[1], "object") ? "object" : "";
          if (complementSlot) {
            return {
              children: compact,
              assignedSlots: ["preference_predicate", complementSlot],
              profile: `typed_${complementSlot}`
            };
          }
        }
        if (compact.length === 3 && isToken2(compact[0], "鍾意") && isToken2(compact[1], "咗") && (nodeCanFillSlot2(compact[2], "np") || nodeCanFillSlot2(compact[2], "object"))) {
          return {
            children: compact,
            assignedSlots: ["preference_predicate", "perfective_aspect", "object"],
            profile: "perfective_np_object"
          };
        }
        if (compact.length >= 5 && isToken2(compact[0], "鍾意")) {
          const alternative = alternativeScalarQuestionTail(compact.slice(1));
          if (alternative) {
            return {
              children: [compact[0], ...alternative.children],
              assignedSlots: ["preference_predicate", ...alternative.assignedSlots],
              profile: "alternative_scalar"
            };
          }
        }
        return null;
      }
      function copularANotAQuantifiedPreferenceClauseCandidate(complementCore) {
        const compact = withoutIgnorableSpaceText2(applyConstructionPatterns2(complementCore));
        if (compact.length < 4) return null;
        const [quantifier, subject, focus, ...predicateNodes] = compact;
        if (!isToken2(quantifier, "每")) return null;
        if (!nodeCanFillSlot2(subject, "subject")) return null;
        if (!isToken2(focus, "都") || !nodeCanFillSlot2(focus, "focus_adverb")) return null;
        const predicateParts = typedPreferencePredicateParts(predicateNodes);
        if (!predicateParts) return null;
        const children = [quantifier, subject, focus, ...predicateParts.children];
        const assignedSlots = ["distributive_quantifier", "subject", "focus_adverb", ...predicateParts.assignedSlots];
        return construction2("SubjectPredicateClause", "SubjPred", children, {
          note: "Bounded copular A-not-A complement: 每 + overt subject + 都 + visible preference predicate material.",
          slots: cleanSlots2([
            "subject_predicate_clause",
            "clause",
            "subject",
            "focus_adverb",
            "predicate",
            "preference_predicate",
            ...templateDerivedSlots2("SubjectPredicateClause", children)
          ]),
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "copular_a_not_a_bounded_complement",
            template: assignedSlots.map((slot) => `${slot}!`),
            rule: {
              typed_vp: "每 + subject + 都 + 鍾意 + typed VP",
              typed_object: "每 + subject + 都 + 鍾意 + typed NP",
              perfective_np_object: "每 + subject + 都 + 鍾意 + 咗 + typed NP",
              alternative_scalar: "每 + subject + 都 + 鍾意 + alternative material + 定係 + alternative material + 多啲"
            }[predicateParts.profile] || `每 + subject + 都 + 鍾意 + reviewed profile ${predicateParts.profile}`,
            assigned_slots: assignedSlots,
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Reconstructs the reviewed bounded copular-question complement from visible preference material without certifying an accidental ModifierNP or restoring broad AB33 PreferenceVP matching."
          })
        });
      }
      function isQuestionPunctuationText(node) {
        if (!node || node.kind !== "text") return false;
        return /^[?？]+$/.test(surfaceOf2(node) || node.text || "");
      }
      function copularANotAQuestionFallback2(core) {
        const { bareCore, particles, punctuation } = splitFinalQuestionMaterial(core, { keepGeParticle: true });
        const offset = bareCore.length >= 4 && nodeCanFillSlot2(bareCore[0], "subject") && !isToken2(bareCore[0], "係") ? 1 : 0;
        if (bareCore.length - offset < 3) return null;
        if (!isToken2(bareCore[offset], "係")) return null;
        const fusedNegativeArm = isToken2(bareCore[offset + 1], "唔係");
        const splitNegativeArm = isToken2(bareCore[offset + 1], "唔") && isToken2(bareCore[offset + 2], "係");
        if (!fusedNegativeArm && !splitNegativeArm) return null;
        const complementStart = offset + (fusedNegativeArm ? 2 : 3);
        const complementCore = bareCore.slice(complementStart);
        if (!complementCore.length) return null;
        const complementCandidate = copularANotAComplementCandidate(complementCore);
        if (!complementCandidate) return null;
        const complement = complementCandidate.node;
        const complementSlot = complementCandidate.profile === "clausal_or_predicate" ? "copular_predicate_complement" : "copular_nominal_complement";
        const positiveCopula = parserInactiveTokenClone2(bareCore[offset], {
          label: "func",
          pos: "function",
          syntax: "copula copular_a_not_a_positive",
          slots: ["copula", "copular_positive_arm"],
          reason: "係 is the positive arm of a copular A-not-A question."
        });
        const negator = fusedNegativeArm ? token3("唔", {
          label: "func",
          pos: "function",
          syntax: "negator copular_a_not_a_negator",
          slots: ["negator", "m4_negator"],
          jyutping: "m4",
          note: "Visible negator split transparently from the lexical token 唔係 inside the copular A-not-A pattern."
        }) : parserInactiveTokenClone2(bareCore[offset + 1], {
          label: "func",
          pos: "function",
          syntax: "negator copular_a_not_a_negator",
          slots: ["negator", "m4_negator"],
          reason: "唔 separates the positive and negative copular arms."
        });
        const negativeCopula = fusedNegativeArm ? token3("係", {
          label: "func",
          pos: "function",
          syntax: "copula copular_a_not_a_negative",
          slots: ["copula", "copular_negative_arm"],
          jyutping: "hai6",
          note: "Visible negative copular arm split transparently from the lexical token 唔係."
        }) : parserInactiveTokenClone2(bareCore[offset + 2], {
          label: "func",
          pos: "function",
          syntax: "copula copular_a_not_a_negative",
          slots: ["copula", "copular_negative_arm"],
          reason: "係 is the copied negative arm of the copular A-not-A question."
        });
        const children = [...bareCore.slice(0, offset), positiveCopula, negator, negativeCopula, complement, ...particles, ...punctuation];
        const assignedSlots = [
          ...bareCore.slice(0, offset).map(() => "subject"),
          "copula_positive_arm",
          "negator",
          "copula_negative_arm",
          complementSlot,
          ...particles.map(() => "particle"),
          ...punctuation.map(() => "question_punctuation")
        ];
        return construction2("CopularANotAQuestion", "CopularQ", children, {
          note: complementCandidate.profile === "clausal_or_predicate" ? "Copular A-not-A question: optional subject/topic + 係唔係 + overt predicate or clause complement." : "Copular A-not-A question: optional subject + 係唔係 + bounded nominal or possessive complement.",
          slots: cleanSlots2(["copular_a_not_a_question", "question_fragment", "copula", "negator", "copular_complement", complementSlot, "predicate", "clause", offset ? "subject" : "", ...templateDerivedSlots2("CopularANotAQuestion", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "CopularANotAQuestion",
            template_family: "generative_template",
            template: ["subject_or_topic?", "copula_positive_arm!", "negator!", "copula_negative_arm!", `${complementSlot}!`, "particle?"],
            assigned_slots: assignedSlots,
            surfaces: children.map((node) => flattenSurface2(node)),
            tokenization_path: fusedNegativeArm ? "positive_copula_plus_fused_negative_copula" : "three_token_copular_a_not_a",
            complement_profile: complementCandidate.profile,
            tag_profile: "terminal_hai6_m4_hai6_excluded",
            contracted_marker_profile: "hai6_mai6_remains_separate_polar_question_path",
            reason: "Preserve both copular arms and classify the overt following material as a typed predicate/clause or a bounded nominal/possessive complement; terminal 係唔係 tags are outside this node."
          })
        });
      }
      function polarQuestionFrameFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length !== 3) return null;
        const [subject, marker, predicate] = compact;
        if (!nodeCanFillSlot2(subject, "subject")) return null;
        if (!isToken2(marker, "係咪")) return null;
        if (!directPredicateCapableNode2(predicate)) return null;
        const children = [subject, yesNoQuestionMarkerClone2(marker), predicate, ...particles];
        return construction2("PolarQuestionFrame", "YesNo?", children, {
          note: "v0.5.32 bounded 係咪 polar question: subject + 係咪 + predicate + optional particle.",
          slots: templateDerivedSlots2("PolarQuestionFrame", children),
          trace: traceInfo2("generative_template", {
            construction_type: "PolarQuestionFrame",
            template: ["subject!", "yes_no_question_marker!", "predicate!", "particle?"],
            assigned_slots: ["subject", "yes_no_question_marker", "predicate", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Promotes only the reviewed 係咪 + predicate yes/no question shape."
          })
        });
      }
      function acceptabilityANotAQuestionFallback2(core) {
        const acceptabilityANotAIndex = core.findIndex(
          (node, index) => surfaceOf2(node) === "得" && surfaceOf2(core[index + 1]) === "唔" && surfaceOf2(core[index + 2]) === "得"
        );
        if (acceptabilityANotAIndex < 0) return null;
        const tail = core.slice(acceptabilityANotAIndex + 3).map(surfaceOf2);
        if (tail.length !== 0 && !(tail.length === 1 && tail[0] === "先")) return null;
        return construction2("AcceptabilityANotA", "得唔得", core, {
          note: "Terminal A-not-A acceptability predicate: 得唔得, optionally followed by 先.",
          trace: traceInfo2("legacy_surface_rule", {
            rule: "terminal adjacent 得唔得 (先)",
            reason: "Narrow fallback for an overt terminal acceptability A-not-A sequence."
          })
        });
      }
      function inlineANotAQuestionFallback2(core) {
        if (core.length < 3 || !isVerbLike2(core[0]) || !isToken2(core[1], "唔")) return null;
        const first = firstToken2(core[0]);
        const third = firstToken2(core[2]);
        if (!first || !third || first.surface !== third.surface) return null;
        return construction2("ANotAQuestion", "A-not-A", core, {
          note: "A-not-A polar question construction.",
          trace: traceInfo2("generative_or_heuristic_slot_rule", {
            rule: "verb + 唔 + copied verb",
            reason: "Structural A-not-A heuristic."
          })
        });
      }
      return {
        aNotAQuestionFallback: aNotAQuestionFallback2,
        acceptabilityANotAQuestionFallback: acceptabilityANotAQuestionFallback2,
        copularANotAQuestionFallback: copularANotAQuestionFallback2,
        desiderativeANotAQuestionFallback: desiderativeANotAQuestionFallback2,
        inlineANotAQuestionFallback: inlineANotAQuestionFallback2,
        permissionANotAQuestionFallback: permissionANotAQuestionFallback2,
        polarQuestionFrameFallback: polarQuestionFrameFallback2
      };
    };
  }
});

// src/parser/detectors/questions/wh-scalar.js
var require_wh_scalar = __commonJS({
  "src/parser/detectors/questions/wh-scalar.js"(exports2, module2) {
    "use strict";
    module2.exports = function createWhScalarQuestionDetectors2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        hasSurface: hasSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        surfaceOf: surfaceOf2,
        templateDerivedSlots: templateDerivedSlots2,
        token: token3,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function whObjectTokenClone(node) {
        const surface = flattenSurface2(node);
        if (!["咩", "乜嘢"].includes(surface)) return node;
        return token3(surface, {
          label: "what",
          syntax: "wh_object",
          slots: ["wh_object", "object"],
          note: `${surface} functions as the object wh-word here, not as a sentence-final surprise particle.`,
          jyutping: firstToken2(node) && firstToken2(node).jyutping,
          trace: traceInfo2("atomic_lexicon", {
            surface,
            generated_slots: ["wh_object", "object"],
            contextual_role_override: "ProgressiveWhObjectQuestion",
            reason: "v0.5.82 resolves 咩 as wh_object when it is the object of a progressive/transitive predicate."
          })
        });
      }
      function progressiveWhObjectQuestionFallback2(core) {
        if (!core || !core.length) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const subject = nodeCanFillSlot2(bareCore[0], "subject") ? bareCore[0] : null;
        const predicate = subject ? bareCore[1] : bareCore[0];
        if (!predicate || predicate.kind !== "construction") return null;
        let progressive = null;
        let wh = null;
        if (predicate.type === "TransitiveVP" && predicate.children && predicate.children.length === 2) {
          const [first, second] = predicate.children;
          if (nodeCanFillSlot2(first, "progressive_vp") && nodeCanFillSlot2(second, "wh_object")) {
            progressive = first;
            wh = whObjectTokenClone(second);
          }
        } else if (nodeCanFillSlot2(predicate, "progressive_vp")) {
          const candidateWh = subject ? bareCore[2] : bareCore[1];
          if (candidateWh && nodeCanFillSlot2(candidateWh, "wh_object")) {
            progressive = predicate;
            wh = whObjectTokenClone(candidateWh);
          }
        }
        if (!progressive || !wh) return null;
        const children = [...subject ? [subject] : [], progressive, wh, ...particles];
        return construction2("ProgressiveWhObjectQuestion", "ProgWhQ", children, {
          slots: templateDerivedSlots2("ProgressiveWhObjectQuestion", children),
          note: "Progressive what-object question: subject + progressive VP + wh object.",
          trace: traceInfo2("generative_template", {
            construction_type: "ProgressiveWhObjectQuestion",
            template_family: "generative_template",
            template: ["subject?", "progressive_vp!", "wh_object!", "particle?"],
            assigned_slots: [...subject ? ["subject"] : [], "progressive_vp", "wh_object", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "v0.5.82 promotes progressive + wh-object questions before broad SubjectPredicateClause/TransitiveVP display can leave 咩 with generic wh_or_particle syntax."
          })
        });
      }
      function scalarValueQuestionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length !== 1 || !isToken2(compact[0], "幾錢")) return null;
        const children = [compact[0], ...particles];
        return construction2("ScalarValueQuestion", "ValueQ", children, {
          slots: ["scalar_value_question", "question_fragment", "price_question"],
          note: "Source-linked lexical price question 幾錢 with an optional final particle.",
          trace: traceInfo2("generative_template", {
            construction_type: "ScalarValueQuestion",
            retired_label_alias: "PriceQuestion",
            template_family: "generative_template",
            template: ["scalar_value_question!", "particle?"],
            assigned_slots: ["scalar_value_question", ...particles.map(() => "particle")],
            scalar_domain: "price",
            scalar_question_subtype: "lexical_price_question",
            semantic_domain: "price_property",
            rule: "幾錢 + particle?",
            reason: "Retains the exact attested lexical price-question profile without the former unsourced quantity/person/approximation cross-product.",
            surfaces: children.map((node) => flattenSurface2(node))
          })
        });
      }
      function locativeWhQuestionFallback2(core) {
        if (!isToken2(core[0], "喺") || !hasSurface2(core, "邊度")) return null;
        return construction2("LocativeWhQuestion", "WhereQ", core, {
          note: "Locative wh-question: 喺邊度.",
          trace: traceInfo2("legacy_surface_rule", {
            rule: "喺 + 邊度",
            reason: "Surface marker + wh-place fallback."
          })
        });
      }
      function suggestionQuestionFallback2(core) {
        if (surfaceOf2(core[0]) !== "不如") return null;
        return construction2("SuggestionQuestion", "Suggest", core, {
          note: "Suggestion fallback with 不如.",
          trace: traceInfo2("construction_function", {
            construction_type: "SuggestionQuestion",
            reason: "Fallback only; generative SuggestionQuestion should normally catch this."
          })
        });
      }
      function existentialWhQuestionFallback2(core) {
        if (!hasSurface2(core, "有") || !hasSurface2(core, "咩")) return null;
        return construction2("ExistentialWhQuestion", "有咩", core, {
          note: "Existential wh-question: 有咩 + noun phrase."
        });
      }
      function scalarWhDegreeTokenClone(node) {
        if (!node || flattenSurface2(node) !== "幾") return node;
        return token3("幾", {
          label: "how",
          syntax: "wh_scalar_degree scalar_value_question",
          slots: ["scalar_wh_degree", "scalar_value_question", "how"],
          jyutping: firstToken2(node) && firstToken2(node).jyutping,
          note: "how much / to what degree",
          trace: traceInfo2("atomic_lexicon", {
            surface: "幾",
            generated_slots: ["scalar_wh_degree", "scalar_value_question", "how"],
            contextual_role_override: "ScalarValueQuestion",
            reason: "v0.5.86-r2 resolves question-use 幾 as learner-role how in scalar-dimension questions such as 幾遠 / 幾貴 / 幾高 / 幾耐."
          })
        });
      }
      function scalarDimensionDomainFor(node) {
        const surface = flattenSurface2(node);
        const syntax = String((firstToken2(node) || node || {}).syntax || "");
        if (syntax.includes("distance") || ["遠", "近"].includes(surface)) return "distance";
        if (syntax.includes("height") || ["高", "矮"].includes(surface)) return "height";
        if (syntax.includes("duration") || ["耐"].includes(surface)) return "duration";
        if (syntax.includes("price") || ["貴", "平"].includes(surface)) return "price";
        return "scalar";
      }
      function scalarDimensionQuestionFallbackForPunctuation2(segment, terminalText = "") {
        if (!/[？?]/u.test(String(terminalText || ""))) return null;
        if (!segment || !segment.length) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(segment);
        if (bareCore.length < 2) return null;
        const whIndex = bareCore.findIndex((node) => flattenSurface2(node) === "幾");
        if (whIndex < 0 || whIndex >= bareCore.length - 1) return null;
        const dimension = bareCore[whIndex + 1];
        if (!nodeCanFillSlot2(dimension, "scalar_dimension_predicate")) return null;
        if (bareCore.length > whIndex + 2) return null;
        const topicNodes = bareCore.slice(0, whIndex);
        const topicChildren = topicNodes.length ? applyConstructionPatterns2(topicNodes) : [];
        if (topicChildren.length > 1) return null;
        const topic = topicChildren[0] || null;
        if (topic && !(nodeCanFillSlot2(topic, "topic") || nodeCanFillSlot2(topic, "topic_or_object") || nodeCanFillSlot2(topic, "location") || nodeCanFillSlot2(topic, "time"))) return null;
        const wh = scalarWhDegreeTokenClone(bareCore[whIndex]);
        const children = [...topic ? [topic] : [], wh, dimension, ...particles];
        const assignedSlots = [...topic ? ["topic_or_object"] : [], "scalar_wh_degree", "scalar_dimension_predicate", ...particles.map(() => "particle")];
        const scalarDomain = scalarDimensionDomainFor(dimension);
        return construction2("ScalarValueQuestion", "ValueQ", children, {
          slots: templateDerivedSlots2("ScalarValueQuestion", children),
          note: "Scalar value question: optional topic plus question-use 幾 and a scalar dimension predicate.",
          trace: traceInfo2("generative_template", {
            construction_type: "ScalarValueQuestion",
            template_family: "generative_template",
            template: ["topic_or_object?", "scalar_wh_degree!", "scalar_dimension_predicate!", "particle?"],
            assigned_slots: assignedSlots,
            scalar_domain: scalarDomain,
            semantic_domain: `${scalarDomain}_property`,
            role_resolution_note: "Question punctuation activates scalar-question 幾; non-question degree-stative statements keep ordinary degree-stative/topic-comment parsing.",
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "v0.5.86 promotes 幾 + scalar predicate questions such as 幾遠 / 幾貴 / 幾高 / 幾耐 without globally lexicalizing those strings or stealing degree-stative statements."
          })
        });
      }
      return {
        existentialWhQuestionFallback: existentialWhQuestionFallback2,
        locativeWhQuestionFallback: locativeWhQuestionFallback2,
        progressiveWhObjectQuestionFallback: progressiveWhObjectQuestionFallback2,
        scalarDimensionQuestionFallbackForPunctuation: scalarDimensionQuestionFallbackForPunctuation2,
        scalarValueQuestionFallback: scalarValueQuestionFallback2,
        suggestionQuestionFallback: suggestionQuestionFallback2
      };
    };
  }
});

// src/parser/detectors/questions/completion-experiential.js
var require_completion_experiential = __commonJS({
  "src/parser/detectors/questions/completion-experiential.js"(exports2, module2) {
    "use strict";
    module2.exports = function createCompletionExperientialQuestionDetectors2(dependencies = {}) {
      const {
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        hasConstruction: hasConstruction2,
        hasSurface: hasSurface2,
        isParticle: isParticle2,
        isProductiveVo: isProductiveVo2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        optionalSubjectOffset: optionalSubjectOffset2,
        traceInfo: traceInfo2
      } = dependencies;
      function objectlessExperientialFinalMeiNeedsContext(core) {
        const offset = optionalSubjectOffset2(core);
        const experientialIndex = core.findIndex((node, index) => index >= offset && nodeCanFillSlot2(node, "experiential_vp"));
        if (experientialIndex !== offset) return null;
        const meiIndex = core.findIndex((node, index) => index > experientialIndex && isToken2(node, "未"));
        if (meiIndex !== experientialIndex + 1) return null;
        if (!core.slice(meiIndex + 1).every((node) => node.kind === "text" || isParticle2(node))) return null;
        const experiential = core[experientialIndex];
        if (["object", "goal", "location"].some((slot) => nodeCanFillSlot2(experiential, slot))) return null;
        const head = firstToken2(experiential);
        if (!head || !nodeCanFillSlot2(head, "action_verb")) return null;
        return construction2("NeedsContext", "needs context", core, {
          slots: ["needs_context", "review_candidate", "problem_span"],
          note: "Objectless V過未 is not accepted as AA61 without an explicit compatible discourse antecedent.",
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "NeedsContext",
            candidate_construction_type: "ExperientialQuestion",
            candidate_profile: "objectless_experiential_final_mei",
            context_requirement_status: "context_required",
            missing_argument_slots: ["object_or_experiential_domain"],
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            event_head_surface: flattenSurface2(head),
            question_marker_surface: "未",
            source_backed_contextual_short_head_surfaces: ["食"],
            hidden_object_insertion: false,
            reason: "The overt V過 + final 未 sequence is structurally compatible with AA61, but the reviewed objectless short profile is discourse-dependent. Only separately licensed context may resolve it.",
            not_claims: ["not_context_free_aa61", "not_fabricated_object", "not_generalized_object_omission"]
          })
        });
      }
      function completionQuestionFallback2(core) {
        const objectlessExperiential = objectlessExperientialFinalMeiNeedsContext(core);
        if (objectlessExperiential) return objectlessExperiential;
        const offset = optionalSubjectOffset2(core);
        if (core.length - offset < 2) return null;
        const meiIndex = core.findIndex((node, index) => index >= offset && isToken2(node, "未"));
        if (meiIndex <= offset) return null;
        const predicateNodes = core.slice(offset, meiIndex);
        if (!predicateNodes.length) return null;
        const hasCompletionLike = predicateNodes.some((node) => {
          if (!node) return false;
          if (node.kind === "construction" && (node.slots || []).some((slot) => ["completion_vp", "perfective_vp", "productive_vo", "vp"].includes(slot))) return true;
          return isToken2(node, "完") || isToken2(node, "咗") || isProductiveVo2(node);
        });
        if (!hasCompletionLike) return null;
        return construction2("CompletionQuestion", "CompletionQ", core, {
          note: "Completion/not-yet question with 完, 咗, or a bare reviewed VO followed by 未.",
          trace: traceInfo2("generative_or_heuristic_slot_rule", {
            rule: "subject? + completed/perfective/productive VP + 未 + particle?",
            reason: "A1 completion-question heuristic covering 咗 and bare VO variants."
          })
        });
      }
      function experientialYesNoQuestionFallback2(core) {
        if (!hasSurface2(core, "有冇") || !hasConstruction2(core, "ExperientialVP")) return null;
        return construction2("ExperientialYesNoQuestion", "Exp?", core, {
          note: "Have-or-not experiential question, e.g. 有冇聽過.",
          trace: traceInfo2("legacy_surface_rule", {
            rule: "has 有冇 and ExperientialVP",
            reason: "Fallback construction/surface rule."
          })
        });
      }
      function interestDomainExistentialQuestionFallback2(core) {
        if (!hasSurface2(core, "有冇興趣") && !(hasSurface2(core, "有冇") && hasSurface2(core, "興趣"))) return null;
        return construction2("ExistentialQuestion", "Have?", core, {
          note: "Existential question over the abstract object/domain 興趣. Interest is metadata, not the active construction label.",
          slots: ["existential_question", "question_fragment", "possessive_question", "predicate", "object", "abstract_object"],
          trace: traceInfo2("generative_template", {
            construction_type: "ExistentialQuestion",
            retired_label_alias: "InterestQuestion",
            existential_subtype: "abstract_object",
            abstract_object_domain: "interest",
            template: ["subject?", "existential_question_or_interest_frame!", "abstract_object?", "vp?", "particle?"],
            assigned_slots: core.map((node) => flattenSurface2(node) === "有冇興趣" ? "interest_question_frame" : nodeCanFillSlot2(node, "subject") ? "subject" : nodeCanFillSlot2(node, "abstract_object") ? "abstract_object" : nodeCanFillSlot2(node, "vp") ? "vp" : nodeCanFillSlot2(node, "particle") ? "particle" : "existential_question"),
            surfaces: core.map((node) => flattenSurface2(node)),
            reason: "Compatibility fallback for lexicalized 有冇興趣; active construction remains broad ExistentialQuestion."
          })
        });
      }
      function experientialQuestionBoundaryFallback2(core) {
        const experientialIndex = core.findIndex((node) => nodeCanFillSlot2(node, "experiential_vp"));
        const negativeExperientialIndex = core.findIndex(
          (node, index) => index < experientialIndex && ["未", "冇"].includes(flattenSurface2(node))
        );
        if (experientialIndex >= 0 && negativeExperientialIndex >= 0) {
          const negatorSurface = flattenSurface2(core[negativeExperientialIndex]);
          return construction2("NegativeExperiential", "NegExp", core, {
            note: "Source-linked preverbal experiential negation: 未/冇 precedes a VP containing experiential 過.",
            trace: traceInfo2("generative_template", {
              construction_type: "NegativeExperiential",
              template_family: "generative_template",
              template: ["subject?", "focus_adverb?", "negator!", "experiential_vp!", "topic_or_object?", "particle?"],
              constraints: { slot_surface_in: { negator: ["未", "冇"] }, marker_precedes_experiential_vp: true },
              assigned_slots: core.map((node, index) => index === negativeExperientialIndex ? "negator" : index === experientialIndex ? "experiential_vp" : nodeCanFillSlot2(node, "subject") ? "subject" : nodeCanFillSlot2(node, "topic_or_object") ? "topic_or_object" : "retained_material"),
              polarity_profile: negatorSurface === "未" ? "not_yet" : "aspectual_negative",
              surfaces: core.map((node) => flattenSurface2(node)),
              reason: "Order-sensitive fallback preserves source-attested preverbal negation when an overt object is not fully grouped."
            })
          });
        }
        const finalMeiIndex = core.findIndex((node, index) => index > experientialIndex && isToken2(node, "未"));
        const finalMeiTailIsOnlyParticles = finalMeiIndex >= 0 && core.slice(finalMeiIndex + 1).every((node) => node.kind === "text" || isParticle2(node));
        const prefix = experientialIndex >= 0 ? core.slice(0, experientialIndex) : [];
        const prefixIsOnlyOptionalSubject = prefix.length === 0 || prefix.length === 1 && nodeCanFillSlot2(prefix[0], "subject");
        const intervening = experientialIndex >= 0 && finalMeiIndex >= 0 ? core.slice(experientialIndex + 1, finalMeiIndex) : [];
        const interveningIsTypedDomain = intervening.every((node) => ["object", "goal", "location"].some((slot) => nodeCanFillSlot2(node, slot)));
        const experientialCarriesTypedDomain = experientialIndex >= 0 && ["object", "goal", "location"].some((slot) => nodeCanFillSlot2(core[experientialIndex], slot));
        const hasOvertTypedDomain = experientialCarriesTypedDomain || intervening.length > 0;
        if (experientialIndex >= 0 && finalMeiIndex >= 0 && finalMeiTailIsOnlyParticles && prefixIsOnlyOptionalSubject && interveningIsTypedDomain && hasOvertTypedDomain) {
          const assignedSlots = core.map((node, index) => {
            if (index < experientialIndex) return "subject";
            if (index === experientialIndex) return "experiential_vp";
            if (index > experientialIndex && index < finalMeiIndex) return "topic_or_object";
            if (index === finalMeiIndex) return "question_marker";
            return isParticle2(node) ? "particle" : "retained_terminal_text";
          });
          return construction2("ExperientialQuestion", "Exp未", core, {
            note: "Source-linked final-未 experiential question with typed overt experiential domain and controlled final-particle tail.",
            trace: traceInfo2("construction_template", {
              construction_type: "ExperientialQuestion",
              template_family: "construction_template",
              template: ["subject?", "experiential_vp!", "topic_or_object?", "question_marker!", "particle?"],
              constraints: {
                slot_surface_in: { question_marker: ["未"] },
                typed_experiential_domain_required: true,
                unrelated_intervening_material_disallowed: true
              },
              assigned_slots: assignedSlots,
              surfaces: core.map((node) => flattenSurface2(node)),
              reason: "AA61 requires a typed experiential child plus overt typed domain material and final 未; unrelated interveners and context-free object omission remain outside this matcher."
            })
          });
        }
        return null;
      }
      function completionQuestionWithPerfectiveMarkerFallback2(core) {
        const meiIndex = core.findIndex((node) => isToken2(node, "未"));
        if (meiIndex <= 0 || !core.some((node) => isToken2(node, "完"))) return null;
        return construction2("CompletionQuestion", "CompletionQ", core, {
          note: "Completion / not-yet question.",
          trace: traceInfo2("generative_or_heuristic_slot_rule", {
            rule: "verb + 完 + object? + 未",
            reason: "Structural completion-question heuristic."
          })
        });
      }
      return {
        completionQuestionFallback: completionQuestionFallback2,
        completionQuestionWithPerfectiveMarkerFallback: completionQuestionWithPerfectiveMarkerFallback2,
        experientialQuestionBoundaryFallback: experientialQuestionBoundaryFallback2,
        experientialYesNoQuestionFallback: experientialYesNoQuestionFallback2,
        interestDomainExistentialQuestionFallback: interestDomainExistentialQuestionFallback2
      };
    };
  }
});

// src/parser/terminal/questions/question-fallbacks.js
var require_question_fallbacks = __commonJS({
  "src/parser/terminal/questions/question-fallbacks.js"(exports2, module2) {
    "use strict";
    module2.exports = function createQuestionFallbacks2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenNodes: flattenNodes2,
        flattenSurface: flattenSurface2,
        hasConstruction: hasConstruction2,
        isParticle: isParticle2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2
      } = dependencies;
      function haveOrNotQuestionParticleClone(node) {
        const surface = flattenSurface2(node);
        if (surface === "呢") {
          return parserInactiveTokenClone2(node, {
            label: "particle",
            pos: "particle",
            syntax: "sentence_final_question_particle",
            jyutping: "ne1",
            slots: ["particle", "question_marker"],
            note: "question particle / asks for an answer",
            reason: "Question punctuation and final position resolve 呢 as a sentence-final question particle, not a demonstrative determiner or standalone fragment question."
          });
        }
        return node;
      }
      const HAVE_OR_NOT_EVENT_VP_TYPES = /* @__PURE__ */ new Set([
        "ActionStativeVP",
        "CompletionVP",
        "DelimitedVP",
        "DesiderativeVP",
        "DirectionalMotionVP",
        "ExperientialVP",
        "ModalVP",
        "MotionGoalVP",
        "NegativePotentialComplement",
        "NegatedVP",
        "PerfectiveVP",
        "ProductiveVO",
        "ProgressiveVP",
        "ReduplicatedVP",
        "ResultComplement",
        "TransitiveVP",
        "VerbComplementVP"
      ]);
      function unwrapHaveOrNotEventVp(node) {
        if (!node || node.kind !== "construction") return null;
        if (HAVE_OR_NOT_EVENT_VP_TYPES.has(node.type)) return node;
        for (const child of node.children || []) {
          const candidate = unwrapHaveOrNotEventVp(child);
          if (candidate) return candidate;
        }
        return nodeCanFillSlot2(node, "vp") && !nodeCanFillSlot2(node, "np") ? node : null;
      }
      function parsedSingleQuestionComplement(nodes) {
        if (!nodes || !nodes.length) return null;
        const parsed = applyConstructionPatterns2(nodes);
        if (parsed.length !== 1) return null;
        const only = parsed[0];
        if (!only) return null;
        if (only.kind === "construction") {
          const vp = unwrapHaveOrNotEventVp(only);
          if (vp) return vp;
        }
        if (nodeCanFillSlot2(only, "vp") || nodeCanFillSlot2(only, "predicate")) return only;
        return null;
      }
      function haveOrNotEventMarkerClone(node, subtype) {
        const experiential = subtype === "experiential";
        return parserInactiveTokenClone2(node, {
          label: "func",
          pos: "function",
          syntax: experiential ? "have_or_not_experiential_question_marker" : "have_or_not_event_question_marker",
          slots: ["existential_question", "question_marker"],
          note: experiential ? "ever ... or not?" : "did ... or not?",
          reason: experiential ? "有冇 scopes over an experiential VP; it asks whether the experience occurred rather than whether an NP exists." : "有冇 scopes over a dynamic VP; it asks whether the event occurred rather than whether an NP exists."
        });
      }
      function haveOrNotQuestionFallbackForPunctuation2(segment, terminalText = "") {
        if (!/[？?]/u.test(String(terminalText || ""))) return null;
        if (!segment || !segment.length) return null;
        const markerIndexes = segment.map((node, index) => isToken2(node, "有冇") ? index : -1).filter((index) => index >= 0);
        if (markerIndexes.length !== 1) return null;
        const markerIndex = markerIndexes[0];
        const prefix = segment.slice(0, markerIndex);
        if (prefix.length > 2) return null;
        if (prefix.some((node) => !nodeCanFillSlot2(node, "subject") && !nodeCanFillSlot2(node, "topic") && !nodeCanFillSlot2(node, "location") && !nodeCanFillSlot2(node, "time"))) return null;
        const marker = segment[markerIndex];
        const after = segment.slice(markerIndex + 1);
        const particles = [];
        while (after.length) {
          const last = after[after.length - 1];
          if (isParticle2(last) || flattenSurface2(last) === "呢") {
            particles.unshift(haveOrNotQuestionParticleClone(after.pop()));
            continue;
          }
          break;
        }
        const prefixSubject = prefix.find((node) => nodeCanFillSlot2(node, "subject")) || null;
        const prefixAssigned = prefix.map((node) => node === prefixSubject ? "subject" : nodeCanFillSlot2(node, "time") ? "time" : nodeCanFillSlot2(node, "location") ? "location" : "topic");
        if (!after.length) {
          const children = [...prefix, marker, ...particles];
          return construction2("ExistentialQuestion", "Have?", children, {
            slots: cleanSlots2(["existential_question", "question_fragment", "possessive_question", "predicate", ...templateDerivedSlots2("ExistentialQuestion", children)]),
            note: "Elliptical 有冇 question whose possession/existence domain must be supplied by the immediately preceding discourse.",
            trace: traceInfo2("generative_template", {
              construction_type: "ExistentialQuestion",
              template_family: "generative_template",
              template: [...prefixAssigned.map((slot) => `${slot}?`), "existential_question!", "particle?"],
              assigned_slots: [...prefixAssigned, "existential_question", ...particles.map(() => "particle")],
              surfaces: children.map((node) => flattenSurface2(node)),
              question_family: "have_or_not",
              existential_subtype: "elliptical_domain",
              complement_type: "context_supplied_np_or_domain",
              context_requirement_status: "context_required",
              missing_argument_slots: ["existential_domain"],
              missing_slot_details: [{ slot: "existential_domain", license_status: "unresolved" }],
              antecedent_status: "not_observed",
              discourse_license_not_observed: true,
              not_claims: ["not_fabricated_object", "not_event_occurrence_question", "not_clean_context_free_question"],
              reason: "Question punctuation licenses the 有冇 question frame, but no overt possession/existence domain follows the marker."
            })
          });
        }
        let postMarkerSubject = null;
        let eventNodes = after;
        if (after.length >= 2 && nodeCanFillSlot2(after[0], "subject")) {
          const candidate = parsedSingleQuestionComplement(after.slice(1));
          if (candidate) {
            postMarkerSubject = after[0];
            eventNodes = after.slice(1);
          }
        }
        const event = parsedSingleQuestionComplement(eventNodes);
        if (event) {
          const experiential = nodeCanFillSlot2(event, "experiential_vp") || hasConstruction2([event], "ExperientialVP");
          const markerChild = haveOrNotEventMarkerClone(marker, experiential ? "experiential" : "event");
          const children = [...prefix, markerChild, ...postMarkerSubject ? [postMarkerSubject] : [], event, ...particles];
          const assignedSlots = [
            ...prefixAssigned,
            "existential_question",
            ...postMarkerSubject ? ["event_subject"] : [],
            experiential ? "experiential_vp" : "vp",
            ...particles.map(() => "particle")
          ];
          const type = experiential ? "ExperientialYesNoQuestion" : "ANotAQuestion";
          const firstPersonSubject = prefixSubject && flattenSurface2(prefixSubject) === "我";
          const eventRows = flattenNodes2([event]);
          const eventTokens = eventRows.filter((row) => row.kind === "token");
          const eventHead = eventTokens.find((row) => (row.slots || []).includes("action_verb")) || null;
          const eventHeadIndex = eventHead ? eventTokens.indexOf(eventHead) : -1;
          const eventHasOvertDomain = eventHeadIndex >= 0 && eventTokens.slice(eventHeadIndex + 1).some((row) => {
            const rowSlots = row.slots || [];
            return !rowSlots.includes("completion_marker") && !rowSlots.includes("particle") && rowSlots.some((slot) => ["object", "theme", "head_noun", "np"].includes(slot));
          });
          const objectlessTotalityCompletion = !experiential && event.type === "CompletionVP" && eventTokens.some((row) => row.surface === "晒" && (row.slots || []).includes("completion_marker")) && !eventHasOvertDomain;
          const eventMissingSlots = objectlessTotalityCompletion ? ["object_or_activity_domain"] : [];
          return construction2(type, experiential ? "Exp?" : "A-not-A", children, {
            slots: templateDerivedSlots2(type, children),
            note: experiential ? "有冇 scopes over a transparent experiential VP and asks whether the experience occurred." : "有冇 scopes over a transparent dynamic VP and asks whether the event occurred.",
            trace: traceInfo2("generative_template", {
              construction_type: type,
              template_family: "generative_template",
              template: [
                ...prefixAssigned.map((slot) => `${slot}?`),
                "existential_question!",
                ...postMarkerSubject ? ["event_subject!"] : [],
                experiential ? "experiential_vp!" : "vp!",
                "particle?"
              ],
              assigned_slots: assignedSlots,
              surfaces: children.map((node) => flattenSurface2(node)),
              question_family: "have_or_not",
              question_subtype: experiential ? "experiential_occurrence" : "event_occurrence",
              complement_type: experiential ? "experiential_vp" : "dynamic_vp",
              event_subject_position: postMarkerSubject ? "postmarker" : prefixSubject ? "premarker" : "omitted_or_generic",
              negation_type: "event_nonoccurrence_alternative",
              first_person_pragmatics: firstPersonSubject ? "review_bearing_but_grammatical" : "not_applicable",
              event_head_surface: eventHead ? eventHead.surface : "",
              event_surface: flattenSurface2(event),
              event_domain_status: objectlessTotalityCompletion ? "discourse_recoverable_totality_domain" : eventHasOvertDomain ? "overt" : "unspecified_activity_reading",
              context_requirement_status: objectlessTotalityCompletion ? "context_required" : "context_not_required",
              missing_argument_slots: eventMissingSlots,
              missing_slot_details: eventMissingSlots.map((slot) => ({ slot, license_status: "unresolved" })),
              antecedent_status: objectlessTotalityCompletion ? "not_observed" : "not_applicable",
              discourse_license_not_observed: objectlessTotalityCompletion,
              not_claims: ["not_existential_np_question", "not_malformed_candidate", "not_fabricated_event_argument", ...objectlessTotalityCompletion ? ["not_context_free_totality_domain"] : []],
              reason: experiential ? "Question punctuation plus embedded experiential structure outranks the old malformed 有冇 + VP guard." : "Question punctuation distinguishes grammatical event-occurrence 有冇 + VP from the punctuation-free learner-error guardrail."
            })
          });
        }
        if (after.length === 2 && nodeCanFillSlot2(after[0], "object") && !nodeCanFillSlot2(after[0], "subject") && (nodeCanFillSlot2(after[1], "action_verb") || nodeCanFillSlot2(after[1], "purpose_verb"))) {
          const children = [...prefix, marker, ...after, ...particles];
          return construction2("ExistentialQuestion", "Have?", children, {
            slots: templateDerivedSlots2("ExistentialQuestion", children),
            note: "Existential/availability 有冇 question with a transparent nominal domain such as 嘢食 or 書睇.",
            trace: traceInfo2("generative_template", {
              construction_type: "ExistentialQuestion",
              template_family: "generative_template",
              template: [...prefixAssigned.map((slot) => `${slot}?`), "existential_question!", "availability_head!", "availability_predicate!", "particle?"],
              assigned_slots: [...prefixAssigned, "existential_question", "availability_head", "availability_predicate", ...particles.map(() => "particle")],
              surfaces: children.map((node) => flattenSurface2(node)),
              question_family: "have_or_not",
              existential_subtype: "availability",
              complement_type: "availability_np",
              availability_domain_surface: after.map((node) => flattenSurface2(node)).join(""),
              context_requirement_status: "context_not_required",
              missing_argument_slots: [],
              not_claims: ["not_event_occurrence_question", "not_experiential_question", "not_opaque_whole_phrase_lexicalization"],
              reason: "A non-person nominal head followed by an action/purpose verb forms the available-item domain; 有冇 asks whether such an item is available."
            })
          });
        }
        if (after.length === 1 && (nodeCanFillSlot2(after[0], "topic_or_object") || nodeCanFillSlot2(after[0], "np") || nodeCanFillSlot2(after[0], "subject"))) {
          const children = [...prefix, marker, after[0], ...particles];
          return construction2("ExistentialQuestion", "Have?", children, {
            slots: templateDerivedSlots2("ExistentialQuestion", children),
            note: "Existential/possessive 有冇 question with an overt NP domain.",
            trace: traceInfo2("generative_template", {
              construction_type: "ExistentialQuestion",
              template_family: "generative_template",
              template: [...prefixAssigned.map((slot) => `${slot}?`), "existential_question!", "topic_or_object!", "particle?"],
              assigned_slots: [...prefixAssigned, "existential_question", "topic_or_object", ...particles.map(() => "particle")],
              surfaces: children.map((node) => flattenSurface2(node)),
              question_family: "have_or_not",
              existential_subtype: prefix.some((node) => nodeCanFillSlot2(node, "location")) ? "locative_existence" : "possession_or_existence",
              complement_type: "np",
              context_requirement_status: "context_not_required",
              missing_argument_slots: [],
              not_claims: ["not_event_occurrence_question", "not_malformed_candidate"],
              reason: "The complement after 有冇 is an overt NP rather than a VP."
            })
          });
        }
        return null;
      }
      return { haveOrNotQuestionFallbackForPunctuation: haveOrNotQuestionFallbackForPunctuation2 };
    };
  }
});

// src/parser/detectors/cognition/fragments-content.js
var require_fragments_content = __commonJS({
  "src/parser/detectors/cognition/fragments-content.js"(exports2, module2) {
    "use strict";
    module2.exports = function createCognitionDetectors2(dependencies = {}) {
      const {
        aNotAQuestionFallback: aNotAQuestionFallback2,
        applyConstructionPatterns: applyConstructionPatterns2,
        completionQuestionFallback: completionQuestionFallback2,
        construction: construction2,
        desiderativeANotAQuestionFallback: desiderativeANotAQuestionFallback2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSurfaceMatches: nodeSurfaceMatches2,
        optionalSubjectOffset: optionalSubjectOffset2,
        permissionANotAQuestionFallback: permissionANotAQuestionFallback2,
        phase4CognitionActiveTokenClone: phase4CognitionActiveTokenClone2,
        predicateOmissionProfileForHead: predicateOmissionProfileForHead2,
        templateConstructionFor: templateConstructionFor2,
        templateDerivedSlots: templateDerivedSlots2,
        token: token3,
        traceInfo: traceInfo2,
        withoutTrailingParticles: withoutTrailingParticles2,
        wrapCategorySubspans: wrapCategorySubspans2
      } = dependencies;
      function negativeCognitionFragmentFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        let negator = null;
        let predicate = null;
        if (bareCore.length === 2 && nodeCanFillSlot2(bareCore[0], "negator") && nodeCanFillSlot2(bareCore[1], "cognition_predicate")) {
          negator = bareCore[0];
          predicate = bareCore[1];
        } else if (bareCore.length === 1 && flattenSurface2(bareCore[0]) === "唔知") {
          negator = token3("唔");
          predicate = token3("知");
        } else {
          return null;
        }
        if (!nodeCanFillSlot2(negator, "negator")) return null;
        if (!nodeCanFillSlot2(predicate, "cognition_predicate")) return null;
        const promoted = phase4CognitionActiveTokenClone2(predicate, {
          label: predicate.label || "doing",
          syntax: `${predicate.syntax || "cognition_verb"} negative_cognition_predicate`,
          slots: ["negative_cognition_predicate", "negative_cognition_fragment"],
          reason: "Phase 4 controlled grammar promotion: 知 is parser-active only inside the reviewed 唔知 negative-cognition fragment; 唔 remains a learner-visible func negator."
        });
        const children = [negator, promoted, ...particles];
        return construction2("NegativeCognitionFragment", "NegCognition", children, {
          note: "Phase 4 controlled negative cognition fragment: 唔 + 知 as a stable short answer / learner fragment, with 唔 kept as func.",
          slots: templateDerivedSlots2("NegativeCognitionFragment", children),
          trace: traceInfo2("phase4_controlled_grammar_promotion", {
            rule: "negator + cognition_predicate + particle?",
            reason: "Promote only reviewed 唔知-style negative cognition fragments. The negator remains a learner-visible func token; this does not promote general cognition, speech, or stance domains.",
            surfaces: children.map((node) => flattenSurface2(node))
          })
        });
      }
      function cognitionStatementFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (bareCore.length !== 2) return null;
        const subject = bareCore[0];
        const predicate = bareCore[1];
        if (!nodeCanFillSlot2(subject, "subject")) return null;
        if (!nodeCanFillSlot2(predicate, "cognition_predicate")) return null;
        if (nodeCanFillSlot2(predicate, "negative_cognition_predicate")) return null;
        if (nodeCanFillSlot2(predicate, "stance_predicate")) return null;
        const promoted = phase4CognitionActiveTokenClone2(predicate, {
          label: predicate.label || "doing",
          syntax: `${predicate.syntax || "cognition_verb"} cognition_statement_predicate`,
          slots: ["cognition_statement_clause"],
          reason: "Phase 4 controlled grammar promotion: bare subject + 知 is parser-active as a reviewed cognition statement, not a broad cognition-content frame."
        });
        const children = [subject, promoted, ...particles];
        return construction2("CognitionStatementClause", "Know", children, {
          note: "Phase 4 controlled cognition statement: subject + 知 + optional particle.",
          slots: templateDerivedSlots2("CognitionStatementClause", children),
          trace: traceInfo2("phase4_controlled_grammar_promotion", {
            rule: "subject + non_stance_cognition_predicate + particle?",
            reason: "Promote only reviewed bare cognition statements such as 我知 / 我知呀. Content frames still require visible clause-like content, and desiderative 我想知 remains separate.",
            predicate_omission_profile: "factive_cognition",
            omission_status: "conventional_bare_statement",
            complement_type: "proposition_or_interrogative_clause",
            conventionality_status: "conventional_bare_statement",
            polarity: "positive",
            speech_event_use: "not_applicable",
            not_claims: ["not_fabricated_content_clause", "not_forced_fragment_answer"],
            surfaces: children.map((node) => flattenSurface2(node))
          })
        });
      }
      function cognitionContentFrameFallback2(core) {
        let cursor = optionalSubjectOffset2(core);
        const subjectNodes = core.slice(0, cursor);
        const negator = isToken2(core[cursor], "唔") ? core[cursor++] : null;
        if (core.length - cursor < 2) return null;
        const cognition = core[cursor];
        if (!nodeCanFillSlot2(cognition, "cognition_predicate")) return null;
        if (nodeCanFillSlot2(cognition, "negative_cognition_predicate")) return null;
        if (nodeCanFillSlot2(cognition, "stance_predicate")) return null;
        if (nodeCanFillSlot2(cognition, "vp_complement_predicate") && !nodeSurfaceMatches2(cognition, ["知"])) return null;
        const rawContent = core.slice(cursor + 1);
        const { core: contentCore, particles } = withoutTrailingParticles2(rawContent);
        const directClauseRelationEdge = contentCore.length === 1 && contentCore[0].kind === "construction" && contentCore[0].type === "ClauseRelationEdge" ? contentCore[0] : null;
        if (!directClauseRelationEdge && contentCore.length < 2) return null;
        const rawEmbeddedQuestion = directClauseRelationEdge ? null : aNotAQuestionFallback2(contentCore) || permissionANotAQuestionFallback2(contentCore) || desiderativeANotAQuestionFallback2(contentCore) || completionQuestionFallback2(contentCore);
        const parsedContent = directClauseRelationEdge ? [directClauseRelationEdge] : rawEmbeddedQuestion ? [rawEmbeddedQuestion] : applyConstructionPatterns2(contentCore);
        const wrappedContent = parsedContent.length === 1 ? parsedContent : wrapCategorySubspans2(contentCore);
        let contentNode = directClauseRelationEdge;
        if (!contentNode && (wrappedContent.length === 1 && wrappedContent[0].kind === "construction" && (nodeCanFillSlot2(wrappedContent[0], "reported_content") || nodeCanFillSlot2(wrappedContent[0], "clause") || nodeCanFillSlot2(wrappedContent[0], "vp") || nodeCanFillSlot2(wrappedContent[0], "predicate") || nodeCanFillSlot2(wrappedContent[0], "question_fragment")))) {
          contentNode = wrappedContent[0];
        } else if (!contentNode) {
          const generatedContent = templateConstructionFor2(wrappedContent, [
            "ANotAQuestion",
            "OpinionStanceFrame",
            "ReportedSpeech",
            "ExistentialClause",
            "NegatedExistentialClause",
            "ExistentialQuestion",
            "DesiderativeVP",
            "ModalVP",
            "CompletionQuestion"
          ]);
          if (generatedContent && (nodeCanFillSlot2(generatedContent, "reported_content") || nodeCanFillSlot2(generatedContent, "clause") || nodeCanFillSlot2(generatedContent, "vp") || nodeCanFillSlot2(generatedContent, "predicate") || nodeCanFillSlot2(generatedContent, "question_fragment"))) contentNode = generatedContent;
        }
        if (!contentNode) return null;
        const promotedCognition = phase4CognitionActiveTokenClone2(cognition, {
          label: cognition.label || "doing",
          syntax: `${cognition.syntax || "cognition_verb"} non_stance_cognition_predicate`,
          slots: ["non_stance_cognition_predicate"],
          reason: "A1 broad cognition architecture: a 知-type predicate is parser-active when it licenses a visible declarative or interrogative content complement."
        });
        const children = [...subjectNodes, ...negator ? [negator] : [], promotedCognition, contentNode, ...particles];
        const profile = predicateOmissionProfileForHead2(flattenSurface2(cognition));
        return construction2("CognitionContentFrame", "Know+Content", children, {
          note: "Cognition content frame with visible polarity and a transparent declarative or interrogative complement.",
          slots: templateDerivedSlots2("CognitionContentFrame", children),
          trace: traceInfo2("phase4_controlled_grammar_promotion", {
            rule: "subject? + polarity? + non_stance_cognition_predicate + content_clause + particle?",
            reason: "The content remains an overt child construction. Negative polarity composes outside the cognition head, and embedded A-not-A content is not flattened into multiple roots.",
            content_construction: contentNode.type,
            predicate_omission_profile: profile ? profile.id : "factive_cognition",
            omission_status: "overt_content_saturated",
            complement_type: contentNode.type && String(contentNode.type).includes("Question") ? "interrogative_clause" : "proposition",
            conventionality_status: "overt_content",
            polarity: negator ? "negative" : "positive",
            speech_event_use: "not_applicable",
            surfaces: children.map((node) => flattenSurface2(node)),
            not_claims: ["not_hidden_content_clause", "not_stance_frame", "not_reported_speech"]
          })
        });
      }
      return {
        cognitionContentFrameFallback: cognitionContentFrameFallback2,
        cognitionStatementFallback: cognitionStatementFallback2,
        negativeCognitionFragmentFallback: negativeCognitionFragmentFallback2
      };
    };
  }
});

// src/parser/detectors/modality/modal-predicates.js
var require_modal_predicates = __commonJS({
  "src/parser/detectors/modality/modal-predicates.js"(exports2, module2) {
    "use strict";
    module2.exports = function createModalPredicateDetectors2(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        coordinatedNPFromParts: coordinatedNPFromParts2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isModalToken: isModalToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        templateConstructionFor: templateConstructionFor2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function isModalPredicateConstruction(node) {
        return node && node.kind === "construction" && node.type === "ModalVP";
      }
      function isPremodalClauseModifier(node) {
        if (!node) return false;
        if (node.kind === "text") return false;
        return nodeCanFillSlot2(node, "time") || nodeCanFillSlot2(node, "how") || nodeCanFillSlot2(node, "manner");
      }
      function modalClauseTypeForLeadingNode(node) {
        if (!node) return "";
        if (nodeCanFillSlot2(node, "location") || nodeCanFillSlot2(node, "locative_phrase")) return "LocativeModalPredicateClause";
        if (nodeCanFillSlot2(node, "subject") && !nodeCanFillSlot2(node, "object")) return "SubjectModalPredicateClause";
        if (nodeCanFillSlot2(node, "subject") && (firstToken2(node) || {}).label === "who") return "SubjectModalPredicateClause";
        if (nodeCanFillSlot2(node, "topic") || nodeCanFillSlot2(node, "np") || nodeCanFillSlot2(node, "head_noun")) return "TopicModalPredicateClause";
        return "";
      }
      function modalClauseLabelForType(type) {
        if (type === "LocativeModalPredicateClause") return "LocModal";
        if (type === "SubjectModalPredicateClause") return "SubjModal";
        if (type === "TopicModalPredicateClause") return "TopicModal";
        return "ModalClause";
      }
      function modalPredicateFromNodes(nodes) {
        if (!nodes || !nodes.length) return null;
        if (nodes.length === 1 && nodeCanFillSlot2(nodes[0], "vp")) return nodes[0];
        const templated = categorySubspanFor2(nodes, ["VerbComplementVP", "DirectionalMotionVP", "CompoundDirectionalMotionVP", "NegatedDirectionalMotionVP", "DegreeMannerAdverbial"]) || templateConstructionFor2(nodes, ["VerbComplementVP", "TransitiveVP", "ProductiveVO", "CompletionVP", "DirectionalMotionVP", "MotionPurposeChain", "SerialVerbPurposeChain"]);
        if (templated && nodeCanFillSlot2(templated, "vp")) return templated;
        return null;
      }
      function modalVPFromNodes2(nodes) {
        if (!nodes || !nodes.length) return null;
        const generated = templateConstructionFor2(nodes, ["ModalVP"]);
        if (generated) return generated;
        const { core: bareCore, particles } = withoutTrailingParticles2(nodes);
        if (!bareCore.length || !nodeCanFillSlot2(bareCore[0], "modal")) return null;
        const modal = bareCore[0];
        const predicateNodes = bareCore.slice(1);
        if (!predicateNodes.length) return null;
        const predicate = modalPredicateFromNodes(predicateNodes);
        if (!predicate || !nodeCanFillSlot2(predicate, "vp")) return null;
        const children = [modal, predicate, ...particles];
        return construction2("ModalVP", "ModalVP", children, {
          note: "Broad modal VP: modal auxiliary plus VP/predicate complement. Matched by governed generated slots rather than the old modal-token heuristic.",
          slots: templateDerivedSlots2("ModalVP", children),
          trace: traceInfo2("generative_template", {
            construction_type: "ModalVP",
            template_family: "generative_template",
            template: ["modal!", "vp!", "particle?"],
            assigned_slots: ["modal", "vp", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Promotes modal + VP/predicate material into the governed generative-template lane while preserving the predicate child span."
          })
        });
      }
      function modalClauseTemplateForType(type, modifiers) {
        const first = type === "LocativeModalPredicateClause" ? "location!" : type === "SubjectModalPredicateClause" ? "subject!" : "topic!";
        const middle = modifiers.map((node) => nodeCanFillSlot2(node, "time") ? "time?" : nodeCanFillSlot2(node, "how") ? "how?" : "manner?");
        return [first, ...middle, "modal_vp!"];
      }
      function modalPredicateClauseFromParts(beforeModal, modalSpan) {
        if (!isModalPredicateConstruction(modalSpan)) return null;
        if (!beforeModal.length) return null;
        if (beforeModal.some((node) => node.kind === "text")) return null;
        const leading = beforeModal[0];
        const modifiers = beforeModal.slice(1);
        if (modifiers.some((node) => !isPremodalClauseModifier(node))) return null;
        const type = modalClauseTypeForLeadingNode(leading);
        if (!type) return null;
        const children = [...beforeModal, modalSpan];
        return construction2(type, modalClauseLabelForType(type), children, {
          note: "v0.5.31 subject/topic/location-preserving modal clause. The existing ModalVP is kept as the visible predicate child instead of becoming a separate top-level span.",
          slots: templateDerivedSlots2(type, children),
          trace: traceInfo2("generative_template", {
            construction_type: type,
            template_family: "generative_template",
            template: modalClauseTemplateForType(type, modifiers),
            assigned_slots: [type === "LocativeModalPredicateClause" ? "location" : type === "SubjectModalPredicateClause" ? "subject" : "topic", ...modifiers.map((node) => nodeCanFillSlot2(node, "time") ? "time" : nodeCanFillSlot2(node, "how") ? "how" : "manner"), "modal_vp"],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Wraps a generated topic/subject/location prefix plus an existing ModalVP without changing the ModalVP internals."
          })
        });
      }
      function coordinatedSubjectModalPredicateClauseFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 5) return null;
        const coord = coordinatedNPFromParts2(compact.slice(0, 3));
        if (!coord) return null;
        const rest = compact.slice(3);
        const modalVP = modalVPFromNodes2(rest);
        if (!modalVP || !nodeCanFillSlot2(modalVP, "modal_vp")) return null;
        const children = [coord, modalVP, ...particles];
        return construction2("CoordinatedSubjectModalPredicateClause", "CoordSubjModal", children, {
          note: "v0.5.35 coordinated-subject modal clause: coordinated subject + existing ModalVP child.",
          slots: templateDerivedSlots2("CoordinatedSubjectModalPredicateClause", children),
          trace: traceInfo2("generative_template", {
            construction_type: "CoordinatedSubjectModalPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "modal_vp!", "particle?"],
            assigned_slots: ["subject", "modal_vp", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Adds a subject-preserving wrapper for the reviewed coordinated-subject modal pattern while keeping the ModalVP internals unchanged."
          })
        });
      }
      function modalPredicateWrapCoreFallback2(core) {
        const modalIndex = core.findIndex(isModalToken2);
        if (modalIndex < 0 || modalIndex >= core.length - 1) return null;
        const before = core.slice(0, modalIndex);
        let modalSpan = modalVPFromNodes2(core.slice(modalIndex));
        if (!modalSpan) {
          modalSpan = construction2("ModalVP", "ModalVP", core.slice(modalIndex), {
            note: "Modal/desiderative construction wrapping following predicate.",
            trace: traceInfo2("generative_or_heuristic_slot_rule", {
              rule: "modal token followed by predicate material",
              reason: "Fallback only; governed ModalVP should normally catch modal + VP material."
            })
          });
        }
        const modalClause = modalPredicateClauseFromParts(before, modalSpan);
        if (modalClause) return [modalClause];
        return [...before, modalSpan];
      }
      return {
        coordinatedSubjectModalPredicateClauseFallback: coordinatedSubjectModalPredicateClauseFallback2,
        modalPredicateWrapCoreFallback: modalPredicateWrapCoreFallback2,
        modalVPFromNodes: modalVPFromNodes2
      };
    };
  }
});

// src/parser/detectors/modality/availability.js
var require_availability = __commonJS({
  "src/parser/detectors/modality/availability.js"(exports2, module2) {
    "use strict";
    module2.exports = function createAvailabilityDetectors2(dependencies = {}) {
      const {
        construction: construction2,
        constructionSlotsByType: constructionSlotsByType2,
        flattenSurface: flattenSurface2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateConstructionFor: templateConstructionFor2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      const LEXICAL_QUARANTINE = /* @__PURE__ */ new Set([
        "冇得頂",
        "冇得講",
        "冇得計",
        "冇得搞",
        "有得諗"
      ]);
      function surfaceIs(node, values) {
        return values.includes(flattenSurface2(node));
      }
      function availabilityHeadPlan(nodes) {
        if (!nodes.length) return null;
        if (surfaceIs(nodes[0], ["有得"])) {
          return { end: 1, polarity: "affirmative", headSurface: "有得", questionStrategy: "none" };
        }
        if (surfaceIs(nodes[0], ["冇得"])) {
          return { end: 1, polarity: "negative", headSurface: "冇得", questionStrategy: "none" };
        }
        if (surfaceIs(nodes[0], ["有冇得"])) {
          return { end: 1, polarity: "interrogative", headSurface: "有冇得", questionStrategy: "suppletive_jau_mou" };
        }
        if (nodes.length >= 2 && surfaceIs(nodes[0], ["有"]) && surfaceIs(nodes[1], ["得"])) {
          return { end: 2, polarity: "affirmative", headSurface: "有+得", questionStrategy: "none" };
        }
        if (nodes.length >= 2 && surfaceIs(nodes[0], ["冇"]) && surfaceIs(nodes[1], ["得"])) {
          return { end: 2, polarity: "negative", headSurface: "冇+得", questionStrategy: "none" };
        }
        if (nodes.length >= 2 && surfaceIs(nodes[0], ["有冇"]) && surfaceIs(nodes[1], ["得"])) {
          return { end: 2, polarity: "interrogative", headSurface: "有冇+得", questionStrategy: "suppletive_jau_mou" };
        }
        if (nodes.length >= 3 && surfaceIs(nodes[0], ["有"]) && surfaceIs(nodes[1], ["冇"]) && surfaceIs(nodes[2], ["得"])) {
          return { end: 3, polarity: "interrogative", headSurface: "有+冇+得", questionStrategy: "suppletive_jau_mou" };
        }
        return null;
      }
      function predicateFromNodes(nodes) {
        if (!nodes.length) return null;
        if (nodes.length === 1 && (nodeCanFillSlot2(nodes[0], "predicate") || nodeCanFillSlot2(nodes[0], "vp") || nodeCanFillSlot2(nodes[0], "action_verb") || nodeCanFillSlot2(nodes[0], "stative_predicate"))) return nodes[0];
        const templated = templateConstructionFor2(nodes, [
          "ActionStativeVP",
          "DegreeMannerModifiedVP",
          "DirectionalMotionVP",
          "IntransitiveVP",
          "MannerAdverbialVP",
          "MotionGoalVP",
          "MotionPurposeChain",
          "ProductiveVO",
          "SerialVerbPurposeChain",
          "StativePredicate",
          "TransitiveVP",
          "VerbComplementVP"
        ]);
        if (templated && nodeCanFillSlot2(templated, "predicate")) return templated;
        if (nodes.length === 2 && nodeCanFillSlot2(nodes[0], "action_verb") && (nodeCanFillSlot2(nodes[1], "object") || nodeCanFillSlot2(nodes[1], "np") || nodeCanFillSlot2(nodes[1], "head_noun"))) {
          return construction2("TransitiveVP", "VP", nodes, {
            slots: constructionSlotsByType2("TransitiveVP", nodes),
            trace: traceInfo2("generative_template", {
              construction_type: "TransitiveVP",
              template_family: "generative_template",
              template: ["action_verb!", "object!"],
              assigned_slots: ["action_verb", "object"],
              surfaces: nodes.map((node) => flattenSurface2(node)),
              subspan: true,
              reason: "Provides an independently typed object-bearing predicate child for the 有得／冇得 availability relation."
            })
          });
        }
        return null;
      }
      function availabilityHeadToken(node, plan, index) {
        const surface = flattenSurface2(node);
        return parserInactiveTokenClone2(node, {
          label: "func",
          syntax: "availability_opportunity_marker",
          slots: ["availability_marker", "polarity_marker"],
          note: `${surface} marks the 有得／冇得 availability relation.`,
          reason: "有得／冇得 is active as the overt availability head, not ordinary possessive/existential 有／冇 plus independent 得.",
          trace_detail: {
            relation_subtype: "availability_opportunity",
            availability_polarity: plan.polarity,
            head_surface: plan.headSurface,
            question_strategy: plan.questionStrategy,
            head_index: index
          }
        });
      }
      function lexicalQuarantineApplies(headNodes, predicate) {
        const headSurface = headNodes.map((node) => flattenSurface2(node)).join("");
        const predicateSurface = flattenSurface2(predicate);
        return LEXICAL_QUARANTINE.has(`${headSurface}${predicateSurface}`);
      }
      function availabilityPredicateFromNodes(nodes) {
        const { core, particles } = withoutTrailingParticles2(nodes || []);
        const compact = withoutIgnorableSpaceText2(core);
        if (!compact.length) return null;
        const plan = availabilityHeadPlan(compact);
        if (!plan) return null;
        const predicateNodes = compact.slice(plan.end);
        const predicate = predicateFromNodes(predicateNodes);
        if (!predicate) return null;
        const headNodes = compact.slice(0, plan.end);
        if (lexicalQuarantineApplies(headNodes, predicate)) return null;
        const children = [
          ...headNodes.map((node, index) => availabilityHeadToken(node, plan, index)),
          predicate
        ];
        return {
          span: construction2("JauDakMouDakAvailabilityPredicate", "Availability", children, {
            slots: constructionSlotsByType2("JauDakMouDakAvailabilityPredicate", children),
            note: "Preverbal 有得／冇得 marks whether the following overt predicate event is available, possible, unavailable, or impossible.",
            trace: traceInfo2("source_linked_runtime_matcher", {
              construction_type: "JauDakMouDakAvailabilityPredicate",
              relation_subtype: "availability_opportunity",
              polarity: plan.polarity,
              head_surface: plan.headSurface,
              question_strategy: plan.questionStrategy,
              predicate_surface: flattenSurface2(predicate),
              lexical_quarantine_checked: true,
              hidden_material_inserted: false,
              source_specification: "docs/research/JAU-MOU-DAK-AVAILABILITY-IDENTITY-SPECIFICATION-R1.md",
              reason: "Matches the accepted source-first 有得／冇得 + overt predicate availability identity while preserving the predicate as an independently typed child."
            })
          }),
          particles
        };
      }
      function availabilityPredicateWrapCoreFallback2(core) {
        const compact = withoutIgnorableSpaceText2(core || []);
        for (let index = 0; index < compact.length; index += 1) {
          const planned = availabilityPredicateFromNodes(compact.slice(index));
          if (!planned) continue;
          return [
            ...compact.slice(0, index),
            planned.span,
            ...planned.particles
          ];
        }
        return null;
      }
      return {
        availabilityPredicateWrapCoreFallback: availabilityPredicateWrapCoreFallback2
      };
    };
  }
});

// src/parser/detectors/modality/intention-preference.js
var require_intention_preference = __commonJS({
  "src/parser/detectors/modality/intention-preference.js"(exports2, module2) {
    "use strict";
    module2.exports = function createIntentionPreferenceDetectors2(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        hasSurface: hasSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        templateConstructionFor: templateConstructionFor2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function sourceLinkedIntentionFrameFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3) return null;
        const [subject, intentionPredicate, ...predicateNodes] = compact;
        if (!nodeCanFillSlot2(subject, "subject") || !isToken2(intentionPredicate, "諗住")) return null;
        const predicate = categorySubspanFor2(predicateNodes, [
          "DirectionalMotionVP",
          "CompoundDirectionalMotionVP",
          "VerbComplementVP",
          "TransitiveVP",
          "ProductiveVO"
        ]);
        if (!predicate || !nodeCanFillSlot2(predicate, "vp")) return null;
        const children = [subject, intentionPredicate, predicate, ...particles];
        return construction2("IntentionFrame", "Intention", children, {
          note: "Source-linked lexical intention profile: overt subject + 諗住 + visible VP.",
          slots: templateDerivedSlots2("IntentionFrame", children),
          trace: traceInfo2("generative_template", {
            construction_type: "IntentionFrame",
            template: ["subject!", "intention_predicate!", "vp!", "particle?"],
            constraints: { slot_surface_in: { intention_predicate: ["諗住"] } },
            assigned_slots: ["subject", "intention_predicate", "vp", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Runs before broad VP-complement subspan wrapping so the overt subject and lexical intention predicate remain visible."
          })
        });
      }
      function sourceLinkedPreferenceVPFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3 || !nodeCanFillSlot2(compact[0], "subject") || !isToken2(compact[1], "鍾意")) return null;
        const complement = categorySubspanFor2(compact.slice(2), [
          "TransitiveVP",
          "ProductiveVO",
          "DirectionalMotionVP",
          "CompoundDirectionalMotionVP",
          "VerbComplementVP"
        ]);
        if (!complement || !nodeCanFillSlot2(complement, "vp")) return null;
        const children = [compact[0], compact[1], complement, ...particles];
        return construction2("PreferenceVP", "Preference", children, {
          note: "Source-linked preference predicate with an overt subject and visible activity VP complement.",
          slots: templateDerivedSlots2("PreferenceVP", children),
          trace: traceInfo2("generative_template", {
            construction_type: "PreferenceVP",
            template: ["subject!", "preference_predicate!", "vp!", "particle?"],
            constraints: { slot_surface_in: { preference_predicate: ["鍾意"] } },
            assigned_slots: ["subject", "preference_predicate", "vp", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Runs before broad nominal wrapping so the sourced 鍾意 + activity complement remains visible."
          })
        });
      }
      function rawPreferenceTemplateFallback2(core) {
        return null;
      }
      function desiderativeVPWrapCoreFallback2(core) {
        if (!hasSurface2(core, "想") || !hasSurface2(core, "好") && !hasSurface2(core, "試吓")) return null;
        return construction2("DesiderativeVP", "WantVP", core, {
          note: "Desire/wanting construction, often 好想 + VP.",
          trace: traceInfo2("legacy_surface_rule", {
            rule: "has 想 plus degree/VP",
            reason: "Surface modal fallback."
          })
        });
      }
      function preferenceVPWrapCoreFallback2(core) {
        return null;
      }
      return {
        desiderativeVPWrapCoreFallback: desiderativeVPWrapCoreFallback2,
        preferenceVPWrapCoreFallback: preferenceVPWrapCoreFallback2,
        rawPreferenceTemplateFallback: rawPreferenceTemplateFallback2,
        sourceLinkedIntentionFrameFallback: sourceLinkedIntentionFrameFallback2,
        sourceLinkedPreferenceVPFallback: sourceLinkedPreferenceVPFallback2
      };
    };
  }
});

// src/parser/detectors/opinion/stance.js
var require_stance = __commonJS({
  "src/parser/detectors/opinion/stance.js"(exports2, module2) {
    "use strict";
    module2.exports = function createOpinionDetectors2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        categorySubspanFor: categorySubspanFor2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        contextualOpinionPlaceholderChildren: contextualOpinionPlaceholderChildren2,
        copulaClone: copulaClone2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        hasSurface: hasSurface2,
        isToken: isToken2,
        modalVPFromNodes: modalVPFromNodes2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        nominalComplementFromNodes: nominalComplementFromNodes2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        phase4OpinionStanceActiveTokenClone: phase4OpinionStanceActiveTokenClone2,
        predicateOmissionProfileForHead: predicateOmissionProfileForHead2,
        subjectStativePredicateClauseFallback: subjectStativePredicateClauseFallback2,
        templateConstructionFor: templateConstructionFor2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutTrailingParticles: withoutTrailingParticles2,
        wrapCategorySubspans: wrapCategorySubspans2
      } = dependencies;
      function opinionSeemingFallback2(core) {
        if (!(hasSurface2(core, "覺得") || hasSurface2(core, "我覺得")) || !hasSurface2(core, "好似")) return null;
        const opinionChildren = contextualOpinionPlaceholderChildren2(core);
        return construction2("OpinionStanceFrame", "Opinion/Stance", opinionChildren, {
          note: "Opinion/seeming fallback: 覺得 + 好似 + predicate.",
          trace: traceInfo2("legacy_surface_rule", {
            rule: "has 覺得/我覺得 and 好似",
            reason: "Fallback only; generative OpinionStanceFrame should normally catch this."
          })
        });
      }
      function splitOpinionTrailingParticles(core) {
        const split = withoutTrailingParticles2(core);
        const bareCore = split.core.slice();
        const particles = split.particles.slice();
        if (particles.length && isToken2(particles[0], "嘅") && bareCore.some((node) => isToken2(node, "係"))) {
          bareCore.push(particles.shift());
        }
        return { core: bareCore, particles };
      }
      function isOpinionFrameFocusModifier(node) {
        return Boolean(node) && node.kind !== "text" && nodeCanFillSlot2(node, "focus_adverb");
      }
      function splitLeadingOpinionContentModifiers(nodes) {
        const modifiers = [];
        let index = 0;
        while (index < nodes.length && isOpinionFrameFocusModifier(nodes[index])) {
          modifiers.push(nodes[index]);
          index += 1;
        }
        return { modifiers, core: nodes.slice(index) };
      }
      function opinionComplementTokenClone(node, subtype) {
        if (!node || node.kind !== "token") return node;
        const surface = flattenSurface2(node);
        if (["係", "唔係"].includes(surface)) {
          return parserInactiveTokenClone2(node, {
            label: "func",
            pos: "function",
            syntax: `${surface === "唔係" ? "negated_copula" : "copula"} complement_ellipsis_predicate`,
            slots: cleanSlots2([
              surface === "唔係" ? "negated_copula" : "copula",
              surface === "唔係" ? "negator" : "",
              "predicate",
              "reported_content",
              "content_clause"
            ]),
            reason: `The visible ${surface} predicate is retained inside a ${subtype} complement-ellipsis fragment; no missing complement words are fabricated.`
          });
        }
        if (["有", "冇"].includes(surface)) {
          return parserInactiveTokenClone2(node, {
            label: "func",
            pos: "function",
            syntax: `${surface === "冇" ? "negated_existential" : "existential"} complement_ellipsis_predicate`,
            slots: cleanSlots2([
              surface === "冇" ? "negated_existential" : "existential",
              "predicate",
              "reported_content",
              "content_clause"
            ]),
            reason: `The visible ${surface} predicate is retained inside an existential-domain complement-ellipsis fragment; no missing domain is fabricated.`
          });
        }
        return node;
      }
      function opinionComplementEllipsisFragment(nodes, subtype, missingSlot) {
        const children = nodes.map((node) => opinionComplementTokenClone(node, subtype));
        return construction2("ComplementEllipsisFragment", "ComplementEllipsis", children, {
          slots: cleanSlots2([
            "complement_ellipsis_fragment",
            "fragment_answer",
            "reported_content",
            "content_clause",
            "predicate",
            "clause",
            ...children.flatMap((node) => nodeSlots2(node))
          ]),
          note: "Visible opinion content with a typed complement/domain omitted and recoverable only from discourse.",
          trace: traceInfo2("generative_template", {
            construction_type: "ComplementEllipsisFragment",
            template_family: "generative_template",
            template: subtype === "existential_domain_ellipsis" ? ["existential_predicate!"] : nodes.length > 1 ? ["subject?", "copular_predicate!"] : ["copular_predicate!"],
            assigned_slots: nodes.map((node, index) => {
              if (index === 0 && nodes.length > 1 && nodeCanFillSlot2(node, "subject")) return "subject";
              return subtype === "existential_domain_ellipsis" ? "existential_predicate" : "copular_predicate";
            }),
            fragment_subtype: subtype,
            context_requirement_status: "context_required",
            missing_argument_slots: [missingSlot],
            missing_slot_details: [{ slot: missingSlot, license_status: "unresolved" }],
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "The overt predicate is structurally valid, but its selected complement/domain is absent and must remain linked to discourse rather than being invented.",
            not_claims: ["not_context_free_complete_clause", "not_fabricated_complement", "not_lexicalized_whole_sentence"]
          })
        });
      }
      function nominalizedStativeComplementForOpinion(nodes) {
        if (!nodes || nodes.length !== 2) return null;
        const stative = nodes[0];
        const linker = nodes[1];
        if (!nodeCanFillSlot2(stative, "stative_predicate") || !isToken2(linker, "嘅")) return null;
        const nominalizer = parserInactiveTokenClone2(linker, {
          label: "func",
          pos: "function",
          syntax: "nominalizer nominal_linker",
          slots: ["nominal_linker"],
          reason: "嘅 nominalizes the visible stative predicate inside a copular opinion complement; it is not treated as a sentence-final particle."
        });
        const children = [stative, nominalizer];
        return construction2("StativeNominalComplement", "AdjNP", children, {
          slots: cleanSlots2([
            "stative_nominal_complement",
            "copular_complement",
            "np",
            "predicate",
            "stative_predicate",
            "nominal_linker",
            ...children.flatMap((node) => nodeSlots2(node))
          ]),
          note: "Headless nominalized stative complement such as 真嘅 in 係真嘅.",
          trace: traceInfo2("generative_template", {
            construction_type: "StativeNominalComplement",
            template_family: "generative_template",
            template: ["stative_predicate!", "nominal_linker!"],
            assigned_slots: ["stative_predicate", "nominal_linker"],
            complement_subtype: "headless_nominalized_stative",
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "A visible stative predicate plus nominalizer forms the copular complement without inventing a silent noun head.",
            not_claims: ["not_overt_head_noun", "not_sentence_final_particle_use"]
          })
        });
      }
      function opinionCopularContentNode(nodes) {
        if (!nodes || nodes.length < 2 || !isToken2(nodes[0], "係")) return null;
        const complementNodes = nodes.slice(1);
        const complement = nominalizedStativeComplementForOpinion(complementNodes) || nominalComplementFromNodes2(complementNodes) || categorySubspanFor2(complementNodes, ["DegreeStativePredicate", "NegatedStativePredicate", "StativePredicate"]);
        if (!complement) return null;
        const copula = copulaClone2(
          nodes[0],
          "copula subordinate_opinion_copula",
          ["copula"],
          "係 heads a visible copular proposition inside the opinion complement. Its understood proposition subject is licensed by the embedding stance frame, not fabricated as a token."
        );
        const children = [copula, complement];
        return construction2("CopularRelationFrame", "Relation", children, {
          slots: cleanSlots2([
            "copular_relation_frame",
            "copular_clause",
            "copula",
            "copular_complement",
            "reported_content",
            "content_clause",
            "predicate",
            "clause",
            ...children.flatMap((node) => nodeSlots2(node))
          ]),
          note: "Subordinate copular proposition used as visible opinion content.",
          trace: traceInfo2("generative_template", {
            construction_type: "CopularRelationFrame",
            template_family: "generative_template",
            template: ["copula!", "copular_complement!"],
            assigned_slots: ["copula", "copular_complement"],
            subject_status: "null_licensed_by_opinion_content",
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            antecedent_status: "not_applicable",
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Inside a stance complement, 係 plus a visible nominalized/stative complement forms a complete proposition without requiring an invented overt subject.",
            not_claims: ["not_standalone_context_free_subject_claim", "not_complement_ellipsis"]
          })
        });
      }
      function contentNodeForOpinionStanceFrame(contentCore) {
        if (!contentCore.length) return null;
        const split = splitLeadingOpinionContentModifiers(contentCore);
        const leadingModifiers = split.modifiers;
        const core = split.core;
        if (!core.length) return null;
        if (core.length === 1 && core[0].kind === "construction" && core[0].type === "ClauseRelationEdge") {
          return { leadingModifiers, contentNode: core[0] };
        }
        if (core.length === 1 && ["有", "冇"].includes(flattenSurface2(core[0]))) {
          return {
            leadingModifiers,
            contentNode: opinionComplementEllipsisFragment(core, "existential_domain_ellipsis", "existential_domain")
          };
        }
        const finalSurface = flattenSurface2(core[core.length - 1]);
        const copularEllipsis = ["係", "唔係"].includes(finalSurface) && (core.length === 1 || core.length === 2 && nodeCanFillSlot2(core[0], "subject"));
        if (copularEllipsis) {
          return {
            leadingModifiers,
            contentNode: opinionComplementEllipsisFragment(core, "copular_complement_ellipsis", "copular_complement")
          };
        }
        const copularContent = opinionCopularContentNode(core);
        if (copularContent) return { leadingModifiers, contentNode: copularContent };
        const modalContent = modalVPFromNodes2(core);
        if (modalContent) return { leadingModifiers, contentNode: modalContent };
        const stativeContent = categorySubspanFor2(core, [
          "DegreeStativePredicate",
          "NegatedStativePredicate",
          "DegreeModifiedLexicalStative",
          "StativePredicate"
        ]);
        if (stativeContent) return { leadingModifiers, contentNode: stativeContent };
        const fullyWrappedContent = applyConstructionPatterns2(core);
        if (fullyWrappedContent.length === 1 && fullyWrappedContent[0].kind === "construction" && [
          "ClauseSpan",
          "TopicComment",
          "CopularRelationFrame",
          "CopularIdentificationFrame",
          "ExistentialClause",
          "NegatedExistentialClause",
          "DesiderativeVP",
          "ModalVP",
          "CompletionQuestion",
          "ANotAQuestion",
          "PotentialResultVP",
          "PerfectiveVP",
          "PostverbalZoPerfectiveVP",
          "DirectionalMotionVP",
          "DegreeStativePredicate",
          "NegatedStativePredicate",
          "StativePredicate",
          "ComplementEllipsisFragment",
          "ClauseRelationEdge"
        ].includes(fullyWrappedContent[0].type)) {
          return { leadingModifiers, contentNode: fullyWrappedContent[0] };
        }
        const wrappedContent = wrapCategorySubspans2(core);
        const subjectStative = subjectStativePredicateClauseFallback2(wrappedContent);
        if (subjectStative) return { leadingModifiers, contentNode: subjectStative };
        if (wrappedContent.length === 1 && wrappedContent[0].kind === "construction" && [
          "ClauseSpan",
          "TopicComment",
          "CopularRelationFrame",
          "CopularIdentificationFrame",
          "ExistentialClause",
          "NegatedExistentialClause",
          "DesiderativeVP",
          "ModalVP",
          "CompletionQuestion",
          "DegreeStativePredicate",
          "NegatedStativePredicate",
          "StativePredicate",
          "ComplementEllipsisFragment",
          "ClauseRelationEdge"
        ].includes(wrappedContent[0].type)) {
          return { leadingModifiers, contentNode: wrappedContent[0] };
        }
        const generatedContent = templateConstructionFor2(wrappedContent, [
          "ExistentialClause",
          "NegatedExistentialClause",
          "DesiderativeVP",
          "ModalVP",
          "CompletionQuestion"
        ]);
        if (generatedContent) return { leadingModifiers, contentNode: generatedContent };
        return null;
      }
      function opinionStanceFrameFallback2(core) {
        const split = splitOpinionTrailingParticles(core);
        const bareCore = split.core;
        const particles = split.particles;
        if (bareCore.length < 3) return null;
        let cursor = 0;
        const subject = nodeCanFillSlot2(bareCore[cursor], "subject") ? bareCore[cursor++] : null;
        const preStanceModifiers = [];
        while (cursor < bareCore.length && isOpinionFrameFocusModifier(bareCore[cursor])) {
          preStanceModifiers.push(bareCore[cursor]);
          cursor += 1;
        }
        const stancePredicate = bareCore[cursor];
        const stanceToken = firstToken2(stancePredicate);
        if (!stanceToken || !nodeCanFillSlot2(stancePredicate, "stance_predicate")) return null;
        const profile = predicateOmissionProfileForHead2(stanceToken.surface);
        cursor += 1;
        if (cursor >= bareCore.length) return null;
        const rawStanceContent = bareCore.slice(cursor);
        const contentAnalysis = rawStanceContent.length === 1 && rawStanceContent[0].kind === "construction" && rawStanceContent[0].type === "ClauseRelationEdge" ? { leadingModifiers: [], contentNode: rawStanceContent[0] } : contentNodeForOpinionStanceFrame(rawStanceContent);
        if (!contentAnalysis || !contentAnalysis.contentNode) return null;
        const contentNode = contentAnalysis.contentNode;
        const postStanceModifiers = contentAnalysis.leadingModifiers || [];
        const promotedStance = phase4OpinionStanceActiveTokenClone2(stancePredicate, {
          label: stancePredicate.label || "doing",
          syntax: `${stanceToken.syntax || "cognition_opinion_predicate"} opinion_stance_predicate`,
          slots: ["opinion_stance_frame"],
          reason: "A1 broad stance architecture: a registered stance or belief predicate is parser-active when it licenses visible reviewed content."
        });
        const children = [
          ...subject ? [subject] : [],
          ...preStanceModifiers,
          promotedStance,
          ...postStanceModifiers,
          contentNode,
          ...particles
        ];
        const assignedSlots = [
          ...subject ? ["subject"] : [],
          ...preStanceModifiers.map(() => "focus_adverb"),
          "stance_predicate",
          ...postStanceModifiers.map(() => "focus_adverb"),
          "reported_content",
          ...particles.map(() => "particle")
        ];
        const contentTrace = contentNode.trace || {};
        const contextRequirementStatus = contentTrace.context_requirement_status || "context_not_required";
        const missingArgumentSlots = Array.isArray(contentTrace.missing_argument_slots) ? contentTrace.missing_argument_slots.slice() : [];
        const missingSlotDetails = Array.isArray(contentTrace.missing_slot_details) ? contentTrace.missing_slot_details.map((item) => ({ ...item })) : missingArgumentSlots.map((slot) => ({ slot, license_status: "unresolved" }));
        return construction2("OpinionStanceFrame", "Opinion/Stance", children, {
          note: "Opinion, evaluation, or belief frame with a visible transparent proposition or typed complement ellipsis.",
          slots: cleanSlots2([
            ...templateDerivedSlots2("OpinionStanceFrame", children),
            "opinion_stance_frame",
            "stance_predicate",
            "reported_content",
            "content_clause",
            "predicate",
            "clause"
          ]),
          trace: traceInfo2("generative_template", {
            construction_type: "OpinionStanceFrame",
            template_family: "generative_template",
            template: ["subject?", "focus_adverb?", "stance_predicate!", "focus_adverb?", "reported_content!", "particle?"],
            constraints: {
              visible_reviewed_content: true,
              stance_predicate_surfaces: [stanceToken.surface],
              focus_modifier_slot: "focus_adverb"
            },
            assigned_slots: assignedSlots,
            content_construction: contentNode.type,
            pre_stance_modifier_surfaces: preStanceModifiers.map((node) => flattenSurface2(node)),
            post_stance_modifier_surfaces: postStanceModifiers.map((node) => flattenSurface2(node)),
            context_requirement_status: contextRequirementStatus,
            missing_argument_slots: missingArgumentSlots,
            missing_slot_details: missingSlotDetails,
            antecedent_status: contextRequirementStatus === "context_required" ? "not_observed" : "not_applicable",
            discourse_license_not_observed: contextRequirementStatus === "context_required",
            attached_particles: particles.map((node) => flattenSurface2(node)),
            surfaces: children.map((node) => flattenSurface2(node)),
            predicate_omission_profile: profile ? profile.id : "opinion_evaluation",
            reason: "The reusable stance frame is keyed by registered stance-predicate affordance, not one hard-coded surface. It wraps—but does not replace—the visible content construction.",
            not_claims: ["not_reported_speech", "not_action_stative_vp_overcapture", "not_fabricated_opinion_content"]
          })
        });
      }
      return { opinionSeemingFallback: opinionSeemingFallback2, opinionStanceFrameFallback: opinionStanceFrameFallback2 };
    };
  }
});

// src/parser/detectors/reported-speech/composition.js
var require_composition = __commonJS({
  "src/parser/detectors/reported-speech/composition.js"(exports2, module2) {
    "use strict";
    module2.exports = function createReportedSpeechDetectors2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        construction: construction2,
        contextualReportedSpeechLearnerChildren: contextualReportedSpeechLearnerChildren2,
        firstToken: firstToken2,
        indexOfSurface: indexOfSurface2,
        flattenSurface: flattenSurface2,
        nodeCanFillSlot: nodeCanFillSlot2,
        optionalSubjectOffset: optionalSubjectOffset2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        phase4ReportedSpeechActiveTokenClone: phase4ReportedSpeechActiveTokenClone2,
        subjectStativePredicateClauseFallback: subjectStativePredicateClauseFallback2,
        templateConstructionFor: templateConstructionFor2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutTrailingParticles: withoutTrailingParticles2,
        wrapCategorySubspans: wrapCategorySubspans2
      } = dependencies;
      function reportedSpeechSurfaceFallback2(core) {
        const waaIndex = indexOfSurface2(core, "話");
        if (waaIndex <= 0 || waaIndex >= core.length - 1) return null;
        const reportedChildren = contextualReportedSpeechLearnerChildren2(core);
        return construction2("ReportedSpeech", "Reported", reportedChildren, {
          note: "Reported speech/thought: NP 話 + clause.",
          trace: traceInfo2("legacy_surface_rule", {
            rule: "NP before 話 and material after",
            reason: "Surface speech verb fallback."
          })
        });
      }
      function contentNodeForReportedSpeech(contentCore) {
        if (!contentCore.length) return null;
        if (contentCore.length === 1 && contentCore[0].kind === "construction" && contentCore[0].type === "ClauseRelationEdge") {
          return contentCore[0];
        }
        if (contentCore.length === 1 && contentCore[0].kind === "token") {
          const contentToken = contentCore[0];
          if (["有", "冇"].includes(flattenSurface2(contentToken)) && (nodeCanFillSlot2(contentToken, "existential") || nodeCanFillSlot2(contentToken, "negated_existential"))) {
            return parserInactiveTokenClone2(contentToken, {
              label: contentToken.label || contentToken.role || "func",
              syntax: `${contentToken.syntax || "predicate"} reported_content_predicate`,
              slots: [...contentToken.slots || [], "reported_content", "predicate"],
              reason: "A visible short predicate is functioning as the reported content of 話; no standalone response-fragment reading or hidden clause is introduced."
            });
          }
        }
        const parsedContent = applyConstructionPatterns2(contentCore);
        const wrappedContent = parsedContent.length === 1 ? parsedContent : wrapCategorySubspans2(contentCore);
        const subjectStative = subjectStativePredicateClauseFallback2(wrappedContent);
        if (subjectStative) return subjectStative;
        if (wrappedContent.length === 1 && wrappedContent[0].kind === "construction" && (nodeCanFillSlot2(wrappedContent[0], "reported_content") || nodeCanFillSlot2(wrappedContent[0], "clause") || nodeCanFillSlot2(wrappedContent[0], "vp") || nodeCanFillSlot2(wrappedContent[0], "predicate"))) {
          return wrappedContent[0];
        }
        if (wrappedContent.length === 1 && wrappedContent[0].kind === "token") {
          const contentToken = wrappedContent[0];
          if (nodeCanFillSlot2(contentToken, "predicate") || nodeCanFillSlot2(contentToken, "existential") || nodeCanFillSlot2(contentToken, "negated_existential") || nodeCanFillSlot2(contentToken, "vp") || nodeCanFillSlot2(contentToken, "clause")) {
            return parserInactiveTokenClone2(contentToken, {
              label: contentToken.label || contentToken.role || "func",
              syntax: `${contentToken.syntax || "predicate"} reported_content_predicate`,
              slots: [...contentToken.slots || [], "reported_content", "predicate"],
              reason: "A visible short predicate is functioning as the reported content of 話; no hidden clause or lexicalized whole-sentence formula is introduced."
            });
          }
        }
        const generatedContent = templateConstructionFor2(wrappedContent, [
          "CognitionContentFrame",
          "OpinionStanceFrame",
          "ExistentialClause",
          "NegatedExistentialClause",
          "DesiderativeVP",
          "ModalVP",
          "CompletionQuestion"
        ]);
        if (generatedContent && (nodeCanFillSlot2(generatedContent, "reported_content") || nodeCanFillSlot2(generatedContent, "clause") || nodeCanFillSlot2(generatedContent, "vp") || nodeCanFillSlot2(generatedContent, "predicate"))) return generatedContent;
        return null;
      }
      function reportedSpeechFrameFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const offset = optionalSubjectOffset2(bareCore);
        if (bareCore.length - offset < 2) return null;
        const speechVerb = bareCore[offset];
        const speechToken = firstToken2(speechVerb);
        if (!speechToken || speechToken.surface !== "話") return null;
        if (!nodeCanFillSlot2(speechVerb, "speech_verb")) return null;
        const contentNode = contentNodeForReportedSpeech(bareCore.slice(offset + 1));
        if (!contentNode) return null;
        const promotedSpeech = phase4ReportedSpeechActiveTokenClone2(speechVerb, {
          label: speechVerb.label || "doing",
          syntax: `${speechToken.syntax || "speech_reporting_verb"} reported_speech_predicate`,
          slots: ["reported_speech"],
          reason: "Phase 4 controlled grammar promotion: 話 is parser-active only when it introduces a visible reviewed reported-content frame."
        });
        const children = [
          ...bareCore.slice(0, offset),
          promotedSpeech,
          contentNode,
          ...particles
        ];
        const assignedSlots = [
          ...bareCore.slice(0, offset).map(() => "subject"),
          "speech_verb",
          "reported_content",
          ...particles.map(() => "particle")
        ];
        return construction2("ReportedSpeech", "Reported", children, {
          note: "Reported speech frame: optional subject + speech-reporting verb + visible reviewed reported content.",
          slots: templateDerivedSlots2("ReportedSpeech", children),
          trace: traceInfo2("generative_template", {
            construction_type: "ReportedSpeech",
            template_family: "generative_template",
            template: ["subject?", "speech_verb!", "reported_content!", "particle?"],
            constraints: {
              visible_reviewed_content: true,
              speech_verb_surfaces: ["話"]
            },
            assigned_slots: assignedSlots,
            content_construction: contentNode.type,
            attached_particles: particles.map((node) => flattenSurface2(node)),
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "The frame is now reported as a named reusable generative template; the controlled parser-active speech predicate remains scoped to this approved construction."
          })
        });
      }
      return { reportedSpeechFrameFallback: reportedSpeechFrameFallback2, reportedSpeechSurfaceFallback: reportedSpeechSurfaceFallback2 };
    };
  }
});

// src/parser/detectors/aspect/potential-result.js
var require_potential_result = __commonJS({
  "src/parser/detectors/aspect/potential-result.js"(exports2, module2) {
    "use strict";
    module2.exports = function createPotentialResultDetectors2(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        classifierObjectNPFromNodes: classifierObjectNPFromNodes2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        isVerbLike: isVerbLike2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function potentialResultComplementFallback2(core) {
        if (core.length >= 3 && isVerbLike2(core[0]) && isToken2(core[1], "唔") && isToken2(core[2], "到")) {
          return construction2("NegativePotentialComplement", "NegPotential", core, {
            note: "Negative potential/result complement.",
            trace: traceInfo2("generative_or_heuristic_slot_rule", {
              rule: "verb + 唔 + 到",
              reason: "Structural potential complement heuristic."
            })
          });
        }
        if (core.length >= 2 && isVerbLike2(core[0]) && isToken2(core[1], "到")) {
          return construction2("ResultComplement", "Result", core, {
            note: "Positive result/attainment complement.",
            trace: traceInfo2("generative_or_heuristic_slot_rule", {
              rule: "verb + 到",
              reason: "Structural result complement heuristic."
            })
          });
        }
        return null;
      }
      function potentialResultVPFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (!bareCore.length) return null;
        let cursor = 0;
        const subject = nodeCanFillSlot2(bareCore[cursor], "subject") ? bareCore[cursor++] : null;
        const action = bareCore[cursor++];
        if (!action || !isVerbLike2(action) || !isToken2(bareCore[cursor], "得")) return null;
        const markerSource = bareCore[cursor++];
        const resultNodes = bareCore.slice(cursor);
        if (!resultNodes.length) return null;
        if (!resultNodes.some((node) => nodeCanFillSlot2(node, "result_complement") || nodeCanFillSlot2(node, "completion_marker") || ["完", "到", "掂", "切"].includes(flattenSurface2(node)))) return null;
        const resultHead = resultNodes[0];
        const objectSource = resultNodes.slice(1);
        let objectNode = null;
        if (objectSource.length === 1 && (nodeCanFillSlot2(objectSource[0], "object") || nodeCanFillSlot2(objectSource[0], "np") || nodeCanFillSlot2(objectSource[0], "head_noun"))) {
          objectNode = objectSource[0].kind === "construction" ? objectSource[0] : categorySubspanFor2(objectSource, ["NominalHeadSpan"]) || objectSource[0];
        } else if (objectSource.length > 1) {
          objectNode = classifierObjectNPFromNodes2(objectSource) || categorySubspanFor2(objectSource, ["OvertHeadDemonstrativeClassifierNP", "QuantifiedClassifierNP", "QuantifiedPersonNP", "OrdinalClassifierNP", "DiMarkedNP", "ModifiedNP", "NominalHeadSpan"]);
        }
        if (objectSource.length && !objectNode) return null;
        const marker = parserInactiveTokenClone2(markerSource, {
          label: "func",
          pos: "function",
          syntax: "potential_marker",
          slots: ["potential_marker"],
          reason: "Between an action predicate and an overt result complement, 得 is the productive positive potential linker, not a standalone acceptability response."
        });
        const children = [action, marker, resultHead, ...objectNode ? [objectNode] : []];
        const potential = construction2("PotentialResultVP", "Potential", children, {
          slots: cleanSlots2(["potential_result_vp", "potential_marker", "result_complement", "vp", "action_vp", "predicate"]),
          note: "Productive positive potential construction: action + 得 + overt result complement.",
          trace: traceInfo2("generative_template", {
            construction_type: "PotentialResultVP",
            template_family: "generative_template",
            template: ["action_verb!", "potential_marker!", "result_complement!"],
            assigned_slots: ["action_verb", "potential_marker", "result_complement", ...objectNode ? ["object"] : []],
            surfaces: children.map((node) => flattenSurface2(node)),
            potential_polarity: "positive",
            not_claims: ["not_acceptability_response", "not_hidden_result_complement"]
          })
        });
        if (!subject) return potential;
        const clauseChildren = [subject, potential, ...particles];
        return construction2("SubjectPredicateClause", "SubjPred", clauseChildren, {
          slots: templateDerivedSlots2("SubjectPredicateClause", clauseChildren),
          note: "Subject plus a productive positive potential predicate.",
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "predicate!", "particle?"],
            assigned_slots: ["subject", "predicate", ...particles.map(() => "particle")],
            surfaces: clauseChildren.map((node) => flattenSurface2(node))
          })
        });
      }
      function incompletePotentialResultCandidate2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        let cursor = 0;
        const subject = nodeCanFillSlot2(bareCore[cursor], "subject") ? bareCore[cursor++] : null;
        const action = bareCore[cursor++];
        const marker = bareCore[cursor++];
        if (cursor !== bareCore.length || !action || !isVerbLike2(action) || !isToken2(marker, "得")) return null;
        const children = [...subject ? [subject] : [], action, marker, ...particles];
        return construction2("NeedsContext", "needs context", children, {
          slots: cleanSlots2(["needs_context", "review_candidate", "predicate", "problem_span", subject ? "subject" : ""]),
          note: "Potential/acceptability boundary with no overt result complement.",
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "NeedsContext",
            predicate_omission_profile: "acceptability_possibility",
            omission_status: "potential_result_or_contextual_ellipsis_ambiguous",
            template: ["subject?", "action_verb!", "potential_marker_or_acceptability_predicate!", "particle?"],
            assigned_slots: [...subject ? ["subject"] : [], "action_verb", "potential_marker_or_acceptability_predicate", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            missing_argument_slots: ["result_or_activity_domain"],
            missing_slot_details: [{ slot: "result_or_activity_domain", license_status: "unresolved" }],
            complement_type: "result_complement_or_contextual_activity",
            context_requirement_status: "context_required",
            antecedent_status: "not_observed",
            selected_alternative: "underdetermined",
            subject_status: subject ? "explicit" : "omitted_unlicensed",
            polarity: "positive",
            conventionality_status: "context_sensitive",
            speech_event_use: "not_applicable",
            semantic_review_flags: ["needs_context_parse", "acceptability_potential_boundary"],
            not_claims: ["not_clean_acceptability_response", "not_fabricated_result_complement", "not_sentence_specific_surface_rule"]
          })
        });
      }
      return { potentialResultComplementFallback: potentialResultComplementFallback2, potentialResultVPFallback: potentialResultVPFallback2, incompletePotentialResultCandidate: incompletePotentialResultCandidate2 };
    };
  }
});

// src/parser/detectors/discourse/formula-responses.js
var require_formula_responses = __commonJS({
  "src/parser/detectors/discourse/formula-responses.js"(exports2, module2) {
    "use strict";
    module2.exports = function createFormulaResponseFamily(dependencies = {}) {
      const {
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isClauseSequenceSeparator: isClauseSequenceSeparator2,
        isClauseSequenceTerminal: isClauseSequenceTerminal2,
        isParticle: isParticle2,
        isToken: isToken2,
        mergeUnique: mergeUnique2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        token: token3,
        traceInfo: traceInfo2,
        traceKind: traceKind2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function protectedOpaqueFormulaPassthrough2(core) {
        if (!core || core.length !== 1) return null;
        const only = core[0];
        if (!only || only.kind !== "construction") return null;
        if (only.type !== "FormulaDiscourseUnit") return null;
        if (traceKind2(only) !== "protected_formula_table") return null;
        return only;
      }
      function transparentFormulaPartClone(node, overrides = {}) {
        return parserInactiveTokenClone2(node, {
          label: overrides.label || node.label || "func",
          pos: overrides.pos || (overrides.label === "particle" ? "particle" : "function"),
          syntax: overrides.syntax || node.syntax || "formula_part",
          slots: overrides.slots || [],
          reason: overrides.reason || "Token stays learner-visible inside a transparent discourse formula while the parent carries the formula/discourse function."
        });
      }
      function makeTransparentDiscourseFormula(children, detail = {}) {
        const surface = children.map((node) => flattenSurface2(node)).join("");
        const childSlots = cleanSlots2(children.flatMap((node) => nodeSlots2(node)));
        return construction2("FormulaDiscourseUnit", "Formula", children, {
          note: detail.note || "Transparent discourse formula: parent keeps the formula/discourse function while child tokens stay learner-visible.",
          slots: mergeUnique2(childSlots, ["formula_discourse_unit", "formula", "discourse_response", "clause"]),
          trace: traceInfo2("governed_discourse_wrapper", {
            construction_type: "FormulaDiscourseUnit",
            formula_transparency: "transparent_children",
            formula_doctrine: "v0.5.87 separates protected opaque formulae from transparent discourse formulae.",
            formula_type: detail.formula_type || "transparent_discourse_formula",
            template: detail.template || [],
            assigned_slots: detail.assigned_slots || [],
            surfaces: children.map((node) => flattenSurface2(node)),
            surface,
            reason: detail.reason || "Accepted doctrine: this common discourse formula is conventional as a parent unit, but its child tokens are useful and should remain visible to learners.",
            ...detail.context_trace || {}
          })
        });
      }
      const NEGATED_EXISTENTIAL_RESPONSE_PARTICLES = /* @__PURE__ */ new Set(["呀", "啊", "喇", "喎", "啫"]);
      function isNegatedExistentialResponseParticle(node) {
        return Boolean(node) && node.kind === "token" && isParticle2(node) && NEGATED_EXISTENTIAL_RESPONSE_PARTICLES.has(flattenSurface2(node));
      }
      function negatedExistentialSubjectClone(node) {
        return parserInactiveTokenClone2(node, {
          label: node.label || "who",
          pos: node.pos || "pronoun",
          syntax: "subject_of_negated_existential_domain_ellipsis_fragment",
          slots: ["subject"],
          reason: "The overt subject remains visible inside a negative existential response whose possession/existence domain is discourse-dependent."
        });
      }
      function negatedExistentialFocusClone(node) {
        return parserInactiveTokenClone2(node, {
          label: node.label || "how",
          pos: node.pos || "adverbial",
          syntax: "focus_adverb negated_existential_response_modifier",
          slots: ["focus_adverb", "how"],
          reason: "The overt focus adverb remains visible and scopes over the negative existential response."
        });
      }
      function negatedExistentialPredicateClone(node, repeated = false) {
        return parserInactiveTokenClone2(node, {
          label: "func",
          pos: "function",
          syntax: repeated ? "negated_existential_predicate repeated_discourse_fragment" : "negated_existential_predicate discourse_fragment",
          slots: ["negated_existential", "predicate"],
          reason: repeated ? "Each visible 冇 repeats the same negative existential response; no missing domain words are inserted." : "冇 is the overt negative existential predicate in the short response fragment."
        });
      }
      function negatedExistentialParticleClone(node) {
        const surface = flattenSurface2(node);
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle negative_existential_response_particle",
          slots: ["particle"],
          reason: `${surface} remains visible as the sentence-final particle contributing discourse stance to the negative response.`
        });
      }
      function buildNegatedExistentialResponseFragment(children, detail = {}) {
        const subjectSurface = detail.subject_surface || "";
        const focusSurfaces = detail.focus_surfaces || [];
        const particleSurface = detail.response_particle || "";
        const repetitionCount = Number(detail.repetition_count || 1);
        const template = [
          ...subjectSurface ? ["subject?"] : [],
          ...focusSurfaces.map(() => "focus_adverb?"),
          "negated_existential!",
          ...repetitionCount > 1 ? ["separator!", "negated_existential!"] : [],
          ...particleSurface ? ["particle?"] : []
        ];
        const assignedSlots = [
          ...subjectSurface ? ["subject"] : [],
          ...focusSurfaces.map(() => "focus_adverb"),
          "negated_existential",
          ...repetitionCount > 1 ? ["separator", "negated_existential"] : [],
          ...particleSurface ? ["particle"] : []
        ];
        return construction2("NegatedExistentialFragment", "negative existential fragment", children, {
          slots: cleanSlots2([
            "negated_existential_fragment",
            "negated_existential",
            "answer_fragment",
            "discourse_response",
            "predicate",
            ...subjectSurface ? ["subject"] : [],
            ...focusSurfaces.length ? ["focus_adverb"] : [],
            ...particleSurface ? ["particle"] : []
          ]),
          note: "Typed negative existential response fragment. Visible subjects, focus adverbs, repetition, and particles remain transparent; the omitted possession/existence domain must come from discourse.",
          trace: traceInfo2("governed_discourse_wrapper", {
            construction_type: "NegatedExistentialFragment",
            fragment_subtype: repetitionCount > 1 ? "repeated_negated_existential_response" : subjectSurface ? "subject_bearing_negated_existential_response" : "negated_existential_response",
            template_family: "generative_template",
            template,
            assigned_slots: assignedSlots,
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: repetitionCount > 1 ? "Repeated 冇 forms one transparent emphatic negative response unit rather than two linked clauses. The omitted domain remains discourse-dependent." : "The visible material forms a bounded negative existential response. It is not promoted to a clean existential clause while its possession/existence domain is absent.",
            context_requirement_status: "context_required",
            missing_argument_slots: ["negative_response_domain"],
            missing_slot_details: [{ slot: "negative_response_domain", license_status: "unresolved" }],
            subject_status: subjectSurface ? "explicit" : "omitted",
            subject_surface: subjectSurface,
            focus_modifier_surfaces: focusSurfaces,
            response_particle: particleSurface,
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            particle_contribution: particleSurface ? [particleSurface] : [],
            response_repetition_count: repetitionCount,
            repetition_separator_surfaces: detail.repetition_separator_surfaces || [],
            selected_alternative: "negative",
            omission_analysis_candidates: ["negative_existential_response_fragment", "domain_ellipsis"],
            not_claims: [
              "not_clean_existential_without_domain",
              "not_fabricated_domain",
              "not_generic_fragment_answer_slot_inheritance",
              "not_syntactic_null_object_claim",
              "not_action_predicate_negation"
            ]
          })
        });
      }
      function negatedExistentialResponseFragmentFallback(core) {
        const compact = withoutIgnorableSpaceText2(core || []);
        if (!compact.length) return null;
        const working = compact.slice();
        let particle = null;
        if (working.length && isNegatedExistentialResponseParticle(working[working.length - 1])) {
          particle = working.pop();
        }
        if (!working.length || !isToken2(working[working.length - 1], "冇")) return null;
        const predicate = working.pop();
        let subject = null;
        const focus = [];
        if (working.length && nodeCanFillSlot2(working[0], "subject")) subject = working.shift();
        while (working.length && nodeCanFillSlot2(working[0], "focus_adverb")) focus.push(working.shift());
        if (working.length) return null;
        if (focus.length > 1) return null;
        const children = [
          ...subject ? [negatedExistentialSubjectClone(subject)] : [],
          ...focus.map((node) => negatedExistentialFocusClone(node)),
          negatedExistentialPredicateClone(predicate),
          ...particle ? [negatedExistentialParticleClone(particle)] : []
        ];
        return buildNegatedExistentialResponseFragment(children, {
          subject_surface: subject ? flattenSurface2(subject) : "",
          focus_surfaces: focus.map((node) => flattenSurface2(node)),
          response_particle: particle ? flattenSurface2(particle) : ""
        });
      }
      function repeatedNegatedExistentialResponseForPunctuation2(nodes) {
        const working = (nodes || []).slice();
        const terminal = [];
        while (working.length && isClauseSequenceTerminal2(working[working.length - 1])) terminal.unshift(working.pop());
        const compact = withoutIgnorableSpaceText2(working);
        let particle = null;
        if (compact.length && isNegatedExistentialResponseParticle(compact[compact.length - 1])) particle = compact.pop();
        if (compact.length !== 3) return null;
        const [first, separator, second] = compact;
        if (!isToken2(first, "冇") || !isClauseSequenceSeparator2(separator) || !isToken2(second, "冇")) return null;
        if (!terminal.length) return null;
        const children = [
          negatedExistentialPredicateClone(first, true),
          separator,
          negatedExistentialPredicateClone(second, true),
          ...particle ? [negatedExistentialParticleClone(particle)] : []
        ];
        const fragment = buildNegatedExistentialResponseFragment(children, {
          response_particle: particle ? flattenSurface2(particle) : "",
          repetition_count: 2,
          repetition_separator_surfaces: [flattenSurface2(separator)]
        });
        return [fragment, ...terminal];
      }
      const ACKNOWLEDGEMENT_REPETITION_PARTICLES = /* @__PURE__ */ new Set(["呀", "啊", "喇", "啦", "喎", "啫"]);
      const ACKNOWLEDGEMENT_REPETITION_FAMILIES = [
        {
          key: "positive_existential",
          subtype: "positive_existential_acknowledgement_repetition",
          layouts: /* @__PURE__ */ new Set(["contiguous", "comma_separated"]),
          pattern: [
            {
              surface: "有",
              required_slot: "existential",
              label: "func",
              pos: "function",
              syntax: "positive_existential_response repeated_formula_part",
              slots: ["positive_response", "existential", "formula_expression"],
              reason: "The visible 有 is the positive existential/possessive response predicate; its discourse domain is represented as a typed omitted slot rather than an inserted token."
            }
          ]
        },
        {
          key: "agreement",
          subtype: "agreement_acknowledgement_repetition",
          layouts: /* @__PURE__ */ new Set(["contiguous", "comma_separated"]),
          pattern: [
            {
              surface: "係",
              label: "func",
              pos: "function",
              syntax: "agreement_response_marker repeated_formula_part",
              slots: ["agreement_response", "confirmation_response", "formula_expression"],
              reason: "The visible 係 is an agreement/confirmation response marker, not an incomplete copular clause."
            }
          ]
        },
        {
          key: "approval",
          subtype: "approval_acknowledgement_repetition",
          layouts: /* @__PURE__ */ new Set(["comma_separated"]),
          pattern: [
            {
              surface: "好",
              label: "like",
              pos: "stative",
              syntax: "approval_response_stative repeated_formula_part",
              slots: ["approval_response", "stative_predicate", "formula_expression"],
              reason: "The visible 好 is an approval/acceptance response unit, not a degree modifier over an omitted adjective."
            }
          ]
        },
        {
          key: "acceptability",
          subtype: "acceptability_acknowledgement_repetition",
          layouts: /* @__PURE__ */ new Set(["comma_separated"]),
          pattern: [
            {
              surface: "得",
              label: "func",
              pos: "function",
              syntax: "acceptability_response_predicate repeated_formula_part",
              slots: ["acceptability_predicate", "formula_expression"],
              reason: "The visible 得 is the acceptability/closure response predicate inside the repeated formula."
            }
          ]
        },
        {
          key: "cognition_acknowledgement",
          subtype: "cognition_acknowledgement_repetition",
          layouts: /* @__PURE__ */ new Set(["contiguous", "comma_separated"]),
          pattern: [
            {
              surface: "我",
              required_slot: "subject",
              label: "who",
              pos: "pronoun",
              syntax: "subject_of_repeated_cognition_acknowledgement",
              slots: ["subject"],
              reason: "The visible 我 remains the overt experiencer in each repeated cognition acknowledgement unit."
            },
            {
              surface: "知",
              required_slot: "cognition_predicate",
              label: "doing",
              pos: "verb",
              syntax: "cognition_acknowledgement_predicate repeated_formula_part",
              slots: ["cognition_predicate", "predicate"],
              reason: "The visible 知 remains a cognition predicate; repetition blocks a false predicate-plus-propositional-content analysis."
            }
          ]
        }
      ];
      function acknowledgementRepetitionSeparator(node) {
        return Boolean(node) && node.kind === "text" && ["，", ","].includes(node.text || "");
      }
      function acknowledgementRepetitionParticle(node) {
        return Boolean(node) && node.kind === "token" && isParticle2(node) && ACKNOWLEDGEMENT_REPETITION_PARTICLES.has(flattenSurface2(node));
      }
      function acknowledgementPatternPartMatches(node, part) {
        if (!node || node.kind !== "token") return false;
        if (part.surface && flattenSurface2(node) !== part.surface) return false;
        if (part.required_slot && !nodeCanFillSlot2(node, part.required_slot)) return false;
        return true;
      }
      function acknowledgementRepetitionPartClone(node, part) {
        return parserInactiveTokenClone2(node, {
          label: part.label,
          pos: part.pos,
          syntax: part.syntax,
          slots: part.slots,
          reason: part.reason
        });
      }
      function acknowledgementRepetitionParticleClone(node) {
        const surface = flattenSurface2(node);
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle acknowledgement_repetition_particle",
          slots: ["particle"],
          reason: `${surface} remains visible as an optional discourse particle inside the repeated formula unit.`
        });
      }
      function acknowledgementUnitDescriptor(segment) {
        let compact = withoutIgnorableSpaceText2(segment || []);
        if (!compact.length || compact.some((node) => node.kind === "text")) return null;
        if (compact.length === 1 && compact[0].kind === "construction" && compact[0].type === "FormulaDiscourseUnit" && traceKind2(compact[0]) === "protected_formula_table") {
          const protectedSurface = flattenSurface2(compact[0]);
          const particleSurface = Array.from(ACKNOWLEDGEMENT_REPETITION_PARTICLES).sort((left, right) => right.length - left.length).find((candidate) => protectedSurface.endsWith(candidate) && protectedSurface.length > candidate.length);
          if (particleSurface) {
            const baseSurface = protectedSurface.slice(0, -particleSurface.length);
            compact = [token3(baseSurface), token3(particleSurface)];
          }
        }
        const working = compact.slice();
        const particle = acknowledgementRepetitionParticle(working[working.length - 1]) ? working.pop() : null;
        const family = ACKNOWLEDGEMENT_REPETITION_FAMILIES.find(
          (candidate) => candidate.pattern.length === working.length && candidate.pattern.every((part, index) => acknowledgementPatternPartMatches(working[index], part))
        );
        if (!family) return null;
        const children = family.pattern.map((part, index) => acknowledgementRepetitionPartClone(working[index], part));
        if (particle) children.push(acknowledgementRepetitionParticleClone(particle));
        return {
          key: family.key,
          subtype: family.subtype,
          layouts: family.layouts,
          canonical_unit: family.pattern.map((part) => part.surface).join(""),
          surface: compact.map((node) => flattenSurface2(node)).join(""),
          children
        };
      }
      function sameAcknowledgementUnitFamily(units) {
        if (!units.length) return false;
        const first = units[0];
        return units.every((unit) => unit && unit.key === first.key && unit.canonical_unit === first.canonical_unit);
      }
      function commaSeparatedAcknowledgementUnits(core) {
        const segments = [];
        const separators = [];
        let segment = [];
        for (const node of core) {
          if (acknowledgementRepetitionSeparator(node)) {
            if (!segment.length) return null;
            segments.push(segment);
            separators.push(node);
            segment = [];
          } else {
            segment.push(node);
          }
        }
        if (!segment.length || !separators.length) return null;
        segments.push(segment);
        const units = segments.map(acknowledgementUnitDescriptor);
        if (units.length < 2 || units.length > 4 || !sameAcknowledgementUnitFamily(units)) return null;
        if (!units[0].layouts.has("comma_separated")) return null;
        return { units, separators, layout: "comma_separated" };
      }
      function contiguousAcknowledgementUnits(core) {
        const compact = withoutIgnorableSpaceText2(core || []);
        if (!compact.length || compact.some((node) => node.kind === "text")) return null;
        for (let width = 1; width <= Math.floor(compact.length / 2); width += 1) {
          if (compact.length % width !== 0) continue;
          const count = compact.length / width;
          if (count < 2 || count > 4) continue;
          const units = [];
          for (let index = 0; index < count; index += 1) {
            units.push(acknowledgementUnitDescriptor(compact.slice(index * width, (index + 1) * width)));
          }
          if (!sameAcknowledgementUnitFamily(units)) continue;
          if (!units[0].layouts.has("contiguous")) continue;
          return { units, separators: [], layout: "contiguous" };
        }
        return null;
      }
      function acknowledgementAssignedSlot(node) {
        const slots = node.slots || [];
        for (const slot of [
          "particle",
          "subject",
          "cognition_predicate",
          "approval_response",
          "acceptability_predicate",
          "positive_response",
          "agreement_response"
        ]) {
          if (slots.includes(slot)) return slot;
        }
        return "formula_expression";
      }
      function buildAcknowledgementRepetitionFormula(match) {
        const children = [];
        const assignedSlots = [];
        const repeatedUnitSpans = [];
        match.units.forEach((unit, index) => {
          const start = children.length;
          for (const child of unit.children) {
            children.push(child);
            if (child.kind === "token") assignedSlots.push(acknowledgementAssignedSlot(child));
          }
          repeatedUnitSpans.push({
            surface: unit.surface,
            canonical_unit: unit.canonical_unit,
            child_start_index: start,
            child_end_index: children.length - 1
          });
          if (index < match.separators.length) {
            children.push(match.separators[index]);
            assignedSlots.push("separator");
          }
        });
        const key = match.units[0].key;
        const contextTrace = key === "positive_existential" ? {
          context_requirement_status: "context_required",
          missing_argument_slots: ["positive_response_domain"],
          missing_slot_details: [{ slot: "positive_response_domain", license_status: "unresolved" }],
          antecedent_status: "not_observed",
          discourse_license_not_observed: true,
          selected_alternative: "positive",
          response_domain_surface: "",
          not_claims: [
            "not_fabricated_domain",
            "not_clean_existential_without_domain",
            "not_syntactic_null_object_claim",
            "not_arbitrary_repeated_token_rule"
          ]
        } : {
          context_requirement_status: "context_not_required",
          missing_argument_slots: [],
          missing_slot_details: [],
          antecedent_status: "not_applicable",
          discourse_license_not_observed: false,
          not_claims: ["not_arbitrary_repeated_token_rule"]
        };
        return construction2("FormulaDiscourseUnit", "Formula", children, {
          slots: cleanSlots2([
            "formula_discourse_unit",
            "formula",
            "formula_expression",
            "discourse_response",
            "acknowledgement_response",
            ...key === "positive_existential" ? ["positive_response", "existential"] : [],
            ...key === "agreement" ? ["agreement_response", "confirmation_response"] : [],
            ...key === "approval" ? ["approval_response"] : [],
            ...key === "acceptability" ? ["acceptability_predicate"] : [],
            ...key === "cognition_acknowledgement" ? ["cognition_predicate", "subject"] : [],
            ...match.separators.length ? ["separator"] : [],
            "clause"
          ]),
          note: "Bounded acknowledgement-repetition construction. A repeated formula unit may carry optional particles and visible separators; the grammar is defined by a shared formula family rather than by a memorized full sentence.",
          trace: traceInfo2("governed_discourse_wrapper", {
            construction_type: "FormulaDiscourseUnit",
            formula_type: "acknowledgement_repetition",
            repetition_subtype: match.units[0].subtype,
            repetition_layout: match.layout,
            acknowledgement_family: key,
            approved_acknowledgement_inventory_key: key,
            canonical_repeated_unit: match.units[0].canonical_unit,
            repeated_unit_surfaces: match.units.map((unit) => unit.surface),
            repeated_unit_count: match.units.length,
            repeated_unit_spans: repeatedUnitSpans,
            repetition_separator_surfaces: match.separators.map((node) => flattenSurface2(node)),
            template_family: "construction_template",
            template: match.units.flatMap((unit, index) => [
              "acknowledgement_unit!",
              ...index < match.separators.length ? ["separator!"] : []
            ]),
            assigned_slots: assignedSlots,
            surfaces: children.map((node) => flattenSurface2(node)),
            formula_transparency: "transparent_children",
            reason: "Two to four structurally equivalent units from an approved acknowledgement family perform one repeated discourse-response function. Optional particles do not change the family, and visible separators remain transparent children.",
            ...contextTrace
          })
        });
      }
      function boundedAcknowledgementRepetitionForPunctuation2(nodes) {
        const working = (nodes || []).slice();
        const terminal = [];
        while (working.length && isClauseSequenceTerminal2(working[working.length - 1])) terminal.unshift(working.pop());
        if (!terminal.length) return null;
        const core = withoutIgnorableSpaceText2(working);
        if (!core.length) return null;
        const match = commaSeparatedAcknowledgementUnits(core) || contiguousAcknowledgementUnits(core);
        if (!match) return null;
        return [buildAcknowledgementRepetitionFormula(match), ...terminal];
      }
      function discourseMarkedAgreementResponseFallback(compact = []) {
        if (compact.length !== 3) return null;
        const [marker, additive, agreement] = compact;
        if (!["咁", "噉"].includes(flattenSurface2(marker))) return null;
        if (!nodeCanFillSlot2(marker, "discourse_marker")) return null;
        if (!isToken2(additive, "又") || !nodeCanFillSlot2(additive, "focus_adverb")) return null;
        if (!isToken2(agreement, "係")) return null;
        const children = [
          transparentFormulaPartClone(marker, {
            label: "func",
            pos: "function",
            syntax: "discourse_marker demonstrative_manner",
            slots: ["discourse_marker", "formula_expression"],
            reason: `${flattenSurface2(marker)} introduces the discourse-qualified acknowledgement and remains visible as the 'then / in that case' element.`
          }),
          transparentFormulaPartClone(additive, {
            label: additive.label || "how",
            pos: "adverbial",
            syntax: "focus_adverb additive_agreement_modifier",
            slots: ["focus_adverb", "how"],
            reason: "又 remains visible as the additive/focus modifier 'also / again'."
          }),
          transparentFormulaPartClone(agreement, {
            label: "func",
            pos: "function",
            syntax: "agreement_confirmation_marker",
            slots: ["agreement_response", "confirmation_response", "formula_expression"],
            reason: "係 supplies the agreement/confirmation response rather than an incomplete copular clause."
          })
        ];
        return makeTransparentDiscourseFormula(children, {
          formula_type: "discourse_marked_agreement_response",
          template: ["discourse_marker!", "focus_adverb!", "agreement_response!"],
          assigned_slots: ["discourse_marker", "focus_adverb", "agreement_response"],
          reason: "A discourse marker plus additive focus and 係 forms a transparent qualified acknowledgement such as 噉又係 / 咁又係. The pattern is structural and orthography-tolerant, not a memorized whole sentence."
        });
      }
      function transparentDiscourseFormulaFallback2(core) {
        if (!core || !core.length) return null;
        const compact = withoutIgnorableSpaceText2(core);
        const surface = compact.map((node) => flattenSurface2(node)).join("");
        const discourseMarkedAgreement = discourseMarkedAgreementResponseFallback(compact);
        if (discourseMarkedAgreement) return discourseMarkedAgreement;
        if (surface === "係咩" && compact.length === 2 && isToken2(compact[0], "係") && isToken2(compact[1], "咩")) {
          const children = [
            transparentFormulaPartClone(compact[0], {
              label: "func",
              pos: "function",
              syntax: "copula_confirmation_marker",
              slots: ["formula_expression"],
              reason: "係 is visible as the confirmation/copula element in the discourse formula 係咩."
            }),
            transparentFormulaPartClone(compact[1], {
              label: "particle",
              pos: "particle",
              syntax: "surprise_question_particle sentence_final_particle",
              slots: ["particle"],
              reason: "咩 is visible as the surprise/question particle in 係咩, not as a wh-object here."
            })
          ];
          return makeTransparentDiscourseFormula(children, {
            formula_type: "confirmation_surprise_question",
            template: ["confirmation_marker!", "particle!"],
            assigned_slots: ["confirmation_marker", "particle"],
            reason: "係咩 is a conventional response formula meaning 'really? / is that so?', but 係 and 咩 are useful learner-visible pieces."
          });
        }
        if (surface === "好呀" && compact.length === 2 && isToken2(compact[0], "好") && isParticle2(compact[1])) {
          const children = [
            transparentFormulaPartClone(compact[0], {
              label: "like",
              pos: "stative",
              syntax: "approval_response_stative",
              slots: ["stative_predicate", "formula_expression"],
              reason: "好 is visible as the approval/okay element in 好呀, not merely a degree marker here."
            }),
            transparentFormulaPartClone(compact[1], {
              label: "particle",
              pos: "particle",
              syntax: "sentence_final_particle",
              slots: ["particle"],
              reason: "呀 stays visible as the sentence-final softening particle in 好呀."
            })
          ];
          return makeTransparentDiscourseFormula(children, {
            formula_type: "agreement_response",
            template: ["approval_response!", "particle!"],
            assigned_slots: ["approval_response", "particle"],
            reason: "好呀 is a conventional agreement response, but 好 + 呀 remains useful and transparent for learners."
          });
        }
        if (surface === "放心啦" && compact.length === 2 && isToken2(compact[0], "放心") && isParticle2(compact[1])) {
          const children = [
            transparentFormulaPartClone(compact[0], {
              label: "like",
              pos: "stative",
              syntax: "reassurance_stative formula_expression",
              slots: ["stative_predicate", "formula_expression"],
              reason: "放心 is visible as a reusable reassurance expression meaning be at ease / don't worry."
            }),
            transparentFormulaPartClone(compact[1], {
              label: "particle",
              pos: "particle",
              syntax: "sentence_final_particle",
              slots: ["particle"],
              reason: "啦 stays visible as the sentence-final reassurance/softening particle."
            })
          ];
          return makeTransparentDiscourseFormula(children, {
            formula_type: "reassurance_formula",
            template: ["reassurance_expression!", "particle!"],
            assigned_slots: ["reassurance_expression", "particle"],
            reason: "放心啦 is formulaic as reassurance, but 放心 and 啦 are useful learner-visible pieces."
          });
        }
        if (surface === "到時見啦" && compact.length === 3 && isToken2(compact[0], "到時") && isToken2(compact[1], "見") && isParticle2(compact[2])) {
          const children = [
            transparentFormulaPartClone(compact[0], {
              label: "when",
              pos: "time",
              syntax: "temporal_adjunct",
              slots: ["time"],
              reason: "到時 stays visible as the temporal expression 'then / when the time comes'."
            }),
            transparentFormulaPartClone(compact[1], {
              label: "doing",
              pos: "verb",
              syntax: "leave_taking_verb",
              slots: ["action_verb", "predicate"],
              reason: "見 stays visible as the action verb 'see / meet' inside the leave-taking formula."
            }),
            transparentFormulaPartClone(compact[2], {
              label: "particle",
              pos: "particle",
              syntax: "sentence_final_particle",
              slots: ["particle"],
              reason: "啦 stays visible as the sentence-final softening particle."
            })
          ];
          return makeTransparentDiscourseFormula(children, {
            formula_type: "transparent_leave_taking_formula",
            template: ["time!", "action_verb!", "particle!"],
            assigned_slots: ["time", "action_verb", "particle"],
            reason: "到時見啦 is conventional as 'see you then', but its time + see + particle structure is useful enough not to hide."
          });
        }
        if (["可以呀", "可以啊"].includes(surface) && compact.length === 2 && isToken2(compact[0], "可以") && isParticle2(compact[1])) {
          const children = [
            transparentFormulaPartClone(compact[0], {
              label: "func",
              pos: "function",
              syntax: "permission_acceptability_response modal_response",
              slots: ["modal", "formula_expression", "permission_response"],
              reason: "可以 is interpreted as a context-dependent permission/acceptability response here, not as a complete standalone ModalVP with an unexpressed VP complement."
            }),
            transparentFormulaPartClone(compact[1], {
              label: "particle",
              pos: "particle",
              syntax: "sentence_final_particle",
              slots: ["particle"],
              reason: "呀/啊 stays visible as the sentence-final response particle."
            })
          ];
          return makeTransparentDiscourseFormula(children, {
            formula_type: "permission_acceptability_response",
            template: ["permission_response!", "particle!"],
            assigned_slots: ["permission_response", "particle"],
            reason: "可以呀/可以啊 is a conventional context-dependent affirmative permission/acceptability response. The parent is a broad discourse formula while 可以 and the particle remain visible."
          });
        }
        const negatedExistentialResponse = negatedExistentialResponseFragmentFallback(compact);
        if (negatedExistentialResponse) return negatedExistentialResponse;
        if (compact.length === 2 && isToken2(compact[0], "得") && isParticle2(compact[1]) && ["啦", "喇", "嘞"].includes(flattenSurface2(compact[1]))) {
          const particleSurface = flattenSurface2(compact[1]);
          const children = [
            transparentFormulaPartClone(compact[0], { label: "func", pos: "function", syntax: "acceptability_predicate discourse_fragment", slots: ["acceptability_predicate", "formula_expression"], reason: "得 supplies the okay/enough acceptability response." }),
            transparentFormulaPartClone(compact[1], { label: "particle", pos: "particle", syntax: "sentence_final_particle closure_particle", slots: ["particle"], reason: `${particleSurface} marks the response as settled, final, or softened.` })
          ];
          return makeTransparentDiscourseFormula(children, {
            formula_type: "acceptability_fragment",
            template: ["acceptability_predicate!", "particle!"],
            assigned_slots: ["acceptability_predicate", "particle"],
            reason: `得 + ${particleSurface} is a conventional acceptability/closure response with transparent children. The construction is licensed by the broad predicate-plus-closure-particle pattern.`
          });
        }
        if (surface === "係囉" && compact.length === 2 && isToken2(compact[0], "係") && isParticle2(compact[1])) {
          const children = [
            transparentFormulaPartClone(compact[0], { label: "func", pos: "function", syntax: "agreement_copula formula_expression", slots: ["agreement_response", "formula_expression"], reason: "係 supplies the agreement/confirmation element." }),
            transparentFormulaPartClone(compact[1], { label: "particle", pos: "particle", syntax: "agreement_particle sentence_final_particle", slots: ["particle"], reason: "囉 marks agreement or obviousness in the discourse response." })
          ];
          return makeTransparentDiscourseFormula(children, { formula_type: "discourse_agreement_fragment", template: ["agreement_response!", "particle!"], assigned_slots: ["agreement_response", "particle"], reason: "係囉 is a conventional agreement response while 係 and 囉 remain learner-visible." });
        }
        return null;
      }
      function leaveTakingFormulaFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length !== 1) return null;
        const formula = compact[0];
        if (!isToken2(formula, "早唞")) return null;
        const formulaChild = parserInactiveTokenClone2(formula, {
          label: "func",
          pos: "function",
          syntax: "leave_taking_formula formula_discourse_unit",
          slots: ["formula_expression", "leave_taking_formula"],
          reason: "早唞 is interpreted as a leave-taking/social formula here, with the sentence-final particle kept visible."
        });
        const children = [formulaChild, ...particles];
        return construction2("FormulaDiscourseUnit", "Formula", children, {
          note: "Transparent leave-taking formula. This keeps 早唞 and 啦 visible under the broad FormulaDiscourseUnit category instead of a subtype-specific construction label.",
          slots: mergeUnique2(templateDerivedSlots2("FormulaDiscourseUnit", children), ["formula_discourse_unit", "formula_expression", "formula", "particle", "clause"]),
          trace: traceInfo2("generative_template", {
            construction_type: "FormulaDiscourseUnit",
            retired_label_alias: "LeaveTakingFormula",
            template: ["formula_expression!", "particle?"],
            assigned_slots: ["formula_expression", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Promotes a common formula while preserving child tokens for learner display."
          })
        });
      }
      return {
        protectedOpaqueFormulaPassthrough: protectedOpaqueFormulaPassthrough2,
        repeatedNegatedExistentialResponseForPunctuation: repeatedNegatedExistentialResponseForPunctuation2,
        boundedAcknowledgementRepetitionForPunctuation: boundedAcknowledgementRepetitionForPunctuation2,
        transparentDiscourseFormulaFallback: transparentDiscourseFormulaFallback2,
        leaveTakingFormulaFallback: leaveTakingFormulaFallback2
      };
    };
  }
});

// src/parser/detectors/existential/clauses.js
var require_clauses3 = __commonJS({
  "src/parser/detectors/existential/clauses.js"(exports2, module2) {
    "use strict";
    module2.exports = function createDetectors(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function interiorExistentialFrameFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 6) return null;
        const yauIndex = compact.findIndex((node) => isToken2(node, "有"));
        if (yauIndex < 0) return null;
        const locationIndex = compact.findIndex((node) => isToken2(node, "入面") || isToken2(node, "入邊"));
        if (locationIndex <= 0 || locationIndex >= yauIndex) return null;
        const topicNodes = compact.slice(0, locationIndex);
        const between = compact.slice(locationIndex + 1, yauIndex);
        if (between.length < 1 || between.length > 2) return null;
        const topic = categorySubspanFor2(topicNodes, ["OvertHeadDemonstrativeClassifierNP", "OrdinalClassifierNP", "ModifiedNP", "NominalHeadSpan"]) || (topicNodes.length === 1 ? topicNodes[0] : null);
        if (!topic || !nodeCanFillSlot2(topic, "topic")) return null;
        const wh = between[0];
        const focus = between.length === 2 ? between[1] : null;
        if (!nodeCanFillSlot2(wh, "wh_object") && !nodeCanFillSlot2(wh, "head_noun") && !nodeCanFillSlot2(wh, "object")) return null;
        if (focus && !nodeCanFillSlot2(focus, "focus_adverb") && !nodeCanFillSlot2(focus, "how")) return null;
        const existential = parserInactiveTokenClone2(compact[yauIndex], {
          label: "func",
          pos: "function",
          syntax: "existential interior_existential",
          slots: ["existential", "predicate"],
          reason: "有 is interpreted as the existential predicate inside a bounded interior-existential frame."
        });
        const children = [topic, compact[locationIndex], wh, ...focus ? [focus] : [], existential, ...particles];
        return construction2("LocativeExistentialClause", "LocExist", children, {
          note: "v0.5.33 interior existential frame: topic + 入面/入邊 + 乜嘢 + 都 + 有.",
          slots: templateDerivedSlots2("LocativeExistentialClause", children),
          trace: traceInfo2("generative_template", {
            construction_type: "LocativeExistentialClause",
            template: ["topic!", "location!", "wh_object!", ...focus ? ["focus_adverb?"] : [], "existential!", "particle?"],
            assigned_slots: ["topic", "location", "wh_object", ...focus ? ["focus_adverb"] : [], "existential", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Promotes the bounded corpus pattern for 'has everything inside' without creating broad 有 existential grammar."
          })
        });
      }
      return {
        interiorExistentialFrameFallback: interiorExistentialFrameFallback2
      };
    };
  }
});

// src/parser/detectors/transfer/legacy-recipient.js
var require_legacy_recipient = __commonJS({
  "src/parser/detectors/transfer/legacy-recipient.js"(exports2, module2) {
    "use strict";
    module2.exports = function createLegacyRecipientDetectors2(dependencies = {}) {
      const {
        bridgeFramePartClone: bridgeFramePartClone2,
        bridgeNPFromNodes: bridgeNPFromNodes2,
        categorySubspanFor: categorySubspanFor2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        rawNodeHasSlot: rawNodeHasSlot2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function transferPredicateFromNodes2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length < 2) return null;
        const give = compact[0];
        const recipient = compact.length === 2 ? compact[1] : bridgeNPFromNodes2(compact.slice(1));
        if (!recipient) return null;
        if (!isToken2(give, "畀")) return null;
        if (!nodeCanFillSlot2(recipient, "recipient") && !nodeCanFillSlot2(recipient, "subject") && !nodeCanFillSlot2(recipient, "np") && !nodeCanFillSlot2(recipient, "object")) return null;
        const marker = bridgeFramePartClone2(give, {
          label: "doing",
          pos: "verb",
          syntax: "transfer_predicate benefactive_transfer_verb",
          slots: ["transfer_predicate", "benefactive_marker", "action_verb", "main_verb", "predicate"],
          reason: "畀 is the transfer/give predicate inside a bounded giving frame."
        });
        const recipientChild = bridgeFramePartClone2(recipient, {
          label: (firstToken2(recipient) || recipient).label || "who",
          pos: "np",
          syntax: `${recipient.syntax || "recipient_np"} transfer_recipient`,
          slots: ["recipient", "beneficiary", "np", "subject"],
          reason: "Recipient is visible inside the bounded giving/benefactive frame."
        });
        return construction2("RecipientFrame", "Recipient", [marker, recipientChild], {
          note: "Recipient frame inside a bounded giving/benefactive clause; benefactive/transfer meaning stays in slots and trace metadata.",
          slots: templateDerivedSlots2("RecipientFrame", [marker, recipientChild]),
          trace: traceInfo2("generative_template", {
            construction_type: "RecipientFrame",
            retired_label_alias: "BenefactiveRecipientFrame",
            template_family: "generative_template",
            template: ["benefactive_marker!", "recipient!"],
            assigned_slots: ["benefactive_marker", "recipient"],
            recipient_relation_subtype: "transfer_or_benefactive",
            surfaces: [flattenSurface2(marker), flattenSurface2(recipientChild)],
            subspan: true
          })
        });
      }
      function recipientFrameFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (particles.length) return null;
        const compact = withoutIgnorableSpaceText2(bareCore);
        return transferPredicateFromNodes2(compact);
      }
      function actionVPFromNodesForRecipient(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        if (compact.length === 1 && compact[0] && compact[0].kind === "construction" && (rawNodeHasSlot2(compact[0], "vp") || rawNodeHasSlot2(compact[0], "predicate"))) return compact[0];
        return categorySubspanFor2(compact, ["TransitiveVP", "ProductiveVO", "CompletionVP", "VerbComplementVP"]);
      }
      function benefactiveRecipientVPFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        const giveIndex = compact.findIndex((node, index) => index > 0 && isToken2(node, "畀"));
        if (giveIndex <= 0 || giveIndex >= compact.length - 1) return null;
        const actionVp = actionVPFromNodesForRecipient(compact.slice(0, giveIndex));
        if (!actionVp) return null;
        const recipientFrame = transferPredicateFromNodes2(compact.slice(giveIndex));
        if (!recipientFrame) return null;
        const children = [actionVp, recipientFrame, ...particles];
        return construction2("RecipientFrame", "Recipient", children, {
          note: "Postverbal recipient/benefactive frame: action VP + 畀 + recipient. This is separate from true transfer ditransitives and from passive/affectedness 畀 frames.",
          slots: cleanSlots2(["recipient_frame", "benefactive_recipient_frame", "vp", "action_vp", "predicate", "recipient", "beneficiary", ...templateDerivedSlots2("RecipientFrame", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "RecipientFrame",
            template_family: "generative_template",
            template: ["action_vp!", "benefactive_marker!", "recipient!", "particle?"],
            assigned_slots: ["action_vp", "recipient_frame", ...particles.map(() => "particle")],
            recipient_relation_subtype: "postverbal_benefactive_recipient",
            boundary_guardrail: "not_comitative_action_motion_vp",
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "畀 + recipient after an action VP marks a recipient/benefactive relation, not a comitative action/motion participant. Keeps the action VP and recipient frame transparent."
          })
        });
      }
      function transferDitransitiveVPFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3 || compact.length > 5) return null;
        let subject = null;
        let giveIndex = -1;
        if (isToken2(compact[0], "畀")) {
          giveIndex = 0;
        } else if (compact.length >= 4 && nodeCanFillSlot2(compact[0], "subject") && isToken2(compact[1], "畀")) {
          subject = compact[0];
          giveIndex = 1;
        } else {
          return null;
        }
        const give = compact[giveIndex];
        const argumentsAfterGive = compact.slice(giveIndex + 1);
        if (argumentsAfterGive.length < 2) return null;
        const looksExplicitlyPersonLike = (node) => {
          const tok = firstToken2(node) || node;
          const slots = nodeSlots2(node);
          return tok && tok.label === "who" || slots.includes("recipient") || slots.includes("co_participant") || slots.includes("stance_holder") || slots.includes("person_np");
        };
        const looksThemeLike = (node) => {
          const tok = firstToken2(node) || node;
          const slots = nodeSlots2(node);
          return tok && tok.label === "what" || slots.includes("object") || slots.includes("head_noun") || slots.includes("np");
        };
        const cloneTransferArgument = (node, roleKind) => {
          const roleSlots = roleKind === "recipient" ? ["recipient", "goal", "np"] : ["theme", "object", "np"];
          const roleSyntax = roleKind === "recipient" ? "transfer_recipient goal_recipient" : "transfer_theme transferred_object";
          const reason = roleKind === "recipient" ? "This NP is the recipient/goal in the selected Cantonese transfer order." : "This NP is the transferred theme/object in the selected Cantonese transfer order.";
          if (node.kind === "construction") {
            return {
              ...node,
              slots: cleanSlots2([...nodeSlots2(node), ...roleSlots]),
              parent_role_assignment: {
                construction_type: "TransferDitransitiveVP",
                assigned_role: roleKind,
                reason
              }
            };
          }
          const base = firstToken2(node) || node;
          return bridgeFramePartClone2(base, {
            label: base.label || (roleKind === "recipient" ? "who" : "what"),
            pos: roleKind === "recipient" ? "np" : base.features && base.features.pos ? base.features.pos : "noun",
            syntax: `${base.syntax || (roleKind === "recipient" ? "recipient_np" : "object_np")} ${roleSyntax}`,
            slots: roleSlots,
            reason
          });
        };
        const firstArgument = argumentsAfterGive[0];
        const lastArgument = argumentsAfterGive[argumentsAfterGive.length - 1];
        const recipientFirstTheme = bridgeNPFromNodes2(argumentsAfterGive.slice(1));
        const themeFirstTheme = bridgeNPFromNodes2(argumentsAfterGive.slice(0, -1));
        const recipientFirst = looksExplicitlyPersonLike(firstArgument) && recipientFirstTheme && looksThemeLike(recipientFirstTheme) && !looksExplicitlyPersonLike(recipientFirstTheme);
        let theme = null;
        let recipient = null;
        let order = "";
        if (recipientFirst) {
          recipient = firstArgument;
          theme = recipientFirstTheme;
          order = "recipient_theme";
        } else {
          theme = themeFirstTheme;
          recipient = lastArgument;
          order = "theme_recipient";
        }
        if (!theme || !recipient) return null;
        if (!looksThemeLike(theme)) return null;
        if (!looksExplicitlyPersonLike(recipient) && !nodeCanFillSlot2(recipient, "recipient") && !nodeCanFillSlot2(recipient, "subject") && !nodeCanFillSlot2(recipient, "np")) return null;
        const transferVerb = bridgeFramePartClone2(give, {
          label: "doing",
          pos: "verb",
          syntax: "transfer_predicate ditransitive_transfer_verb",
          slots: ["transfer_predicate", "action_verb", "main_verb", "predicate"],
          reason: "畀 is the transfer/give predicate in a true transfer ditransitive frame, not a postverbal benefactive-purpose marker."
        });
        const themeChild = cloneTransferArgument(theme, "theme");
        const recipientChild = cloneTransferArgument(recipient, "recipient");
        const orderedArguments = order === "recipient_theme" ? [recipientChild, themeChild] : [themeChild, recipientChild];
        const orderedSlots = order === "recipient_theme" ? ["recipient", "theme"] : ["theme", "recipient"];
        const children = [
          ...subject ? [subject] : [],
          transferVerb,
          ...orderedArguments,
          ...particles
        ];
        const assignedSlots = [
          ...subject ? ["subject"] : [],
          "transfer_predicate",
          ...orderedSlots,
          ...particles.map(() => "particle")
        ];
        const template = [
          ...subject ? ["subject?"] : [],
          "transfer_predicate!",
          ...order === "recipient_theme" ? ["recipient!", "theme!"] : ["theme!", "recipient!"],
          "particle?"
        ];
        return construction2("TransferDitransitiveVP", "TransferVP", children, {
          note: "True Cantonese transfer ditransitive with evidence-controlled Theme/Recipient assignment. Both Verb + Theme + Recipient and Verb + Recipient + Theme orders remain available when the visible NP roles support them.",
          slots: templateDerivedSlots2("TransferDitransitiveVP", children),
          trace: traceInfo2("generative_template", {
            construction_type: "TransferDitransitiveVP",
            template_family: "generative_template",
            template,
            assigned_slots: assignedSlots,
            ditransitive_subtype: order === "recipient_theme" ? "true_transfer_recipient_theme" : "true_transfer_theme_recipient",
            argument_order: order,
            boundary_guardrail: "not_serial_verb_purpose_chain",
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: order === "recipient_theme" ? "The first postverbal NP is explicitly person/recipient-like and the following NP is theme-like, so this row uses Verb + Recipient + Theme order." : "The pre-final postverbal material forms the transferred theme and the final person/recipient-like NP is the goal, so this row uses Verb + Theme + Recipient order."
          })
        });
      }
      return {
        benefactiveRecipientVPFallback: benefactiveRecipientVPFallback2,
        recipientFrameFallback: recipientFrameFallback2,
        transferDitransitiveVPFallback: transferDitransitiveVPFallback2,
        transferPredicateFromNodes: transferPredicateFromNodes2
      };
    };
  }
});

// src/parser/detectors/transfer/lexical-give.js
var require_lexical_give = __commonJS({
  "src/parser/detectors/transfer/lexical-give.js"(exports2, module2) {
    "use strict";
    module2.exports = function createLexicalGiveDetectors2(dependencies = {}) {
      const {
        bridgeFramePartClone: bridgeFramePartClone2,
        categorySubspanFor: categorySubspanFor2,
        classifierObjectNPFromNodes: classifierObjectNPFromNodes2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function cp021bIsBei2Marker2(node) {
        return isToken2(node, "畀") || isToken2(node, "俾");
      }
      const CP021B_REVIEWED_PERSON_SURFACES = /* @__PURE__ */ new Set([
        "我",
        "你",
        "佢",
        "我哋",
        "你哋",
        "佢哋",
        "阿媽",
        "媽媽",
        "阿明",
        "張三",
        "細佬",
        "老師",
        "學生",
        "老闆"
      ]);
      function cp021bNodeIsPersonEvidence2(node) {
        const tok = firstToken2(node) || node;
        const slots = nodeSlots2(node);
        const surface = flattenSurface2(node);
        return CP021B_REVIEWED_PERSON_SURFACES.has(surface) || Boolean(tok && tok.label === "who") || slots.includes("person_np") || slots.includes("proper_name") || slots.includes("named_address_term") || slots.includes("vocative_address_term");
      }
      function cp021bSpanIsPersonNP2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length || compact.some(cp021bIsBei2Marker2)) return false;
        const surface = compact.map(flattenSurface2).join("");
        if (CP021B_REVIEWED_PERSON_SURFACES.has(surface)) return true;
        return compact.every(cp021bNodeIsPersonEvidence2);
      }
      function cp021bNodeIsThingEvidence(node) {
        const tok = firstToken2(node) || node;
        const slots = nodeSlots2(node);
        const syntax = String(tok && tok.syntax || node.syntax || "");
        return Boolean(tok && (tok.label === "what" || tok.label === "measure_word")) || slots.includes("object") || slots.includes("head_noun") || slots.includes("classifier") || slots.includes("currency_unit") || /object_np|currency_unit|classifier/u.test(syntax);
      }
      function cp021bSpanIsThingNP2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length || compact.some(cp021bIsBei2Marker2) || cp021bSpanIsPersonNP2(compact)) return false;
        const last = compact[compact.length - 1];
        const hasThingEvidence = compact.some(cp021bNodeIsThingEvidence);
        const hasNominalHead = cp021bNodeIsThingEvidence(last) || nodeCanFillSlot2(last, "np") || nodeCanFillSlot2(last, "head_noun");
        return hasThingEvidence && hasNominalHead;
      }
      function cp021bArgumentSpan2(nodes, options = {}) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        const slots = cleanSlots2(options.slots || ["np"]);
        const role = options.role || "participant";
        const reason = options.reason || "The overt nominal span is preserved without inserting a hidden participant.";
        if (compact.length === 1 && compact[0].kind === "token") {
          const base = compact[0];
          return bridgeFramePartClone2(base, {
            label: base.label || options.label || "neutral",
            pos: options.label === "who" ? "np" : "noun",
            syntax: `${base.syntax || "nominal"} cp021b_${role}`,
            slots,
            reason
          });
        }
        if (compact.length === 1 && compact[0].kind === "construction") {
          return {
            ...compact[0],
            slots,
            parent_role_assignment: {
              construction_type: options.parent_type || "CP021BRelation",
              assigned_role: role,
              reason
            }
          };
        }
        const typed = categorySubspanFor2(compact, [
          "QuantityNP",
          "QuantifiedClassifierNP",
          "ClassifierObjectNP",
          "RelativeClauseNP",
          "ModifiedNP",
          "NominalHeadSpan"
        ]) || classifierObjectNPFromNodes2(compact);
        if (typed && flattenSurface2(typed) === compact.map(flattenSurface2).join("")) {
          return {
            ...typed,
            slots,
            parent_role_assignment: {
              construction_type: options.parent_type || "CP021BRelation",
              assigned_role: role,
              reason
            }
          };
        }
        const type = options.label === "who" ? "NominalHeadSpan" : "ModifiedNP";
        return construction2(type, options.label === "who" ? "NP" : "ModNP", compact, {
          note: reason,
          slots,
          trace: traceInfo2("generative_template", {
            construction_type: type,
            template_family: "construction_template",
            cp021b_design_family: "overt_nominal_span",
            assigned_slots: compact.map(() => "nominal_material"),
            surfaces: compact.map(flattenSurface2),
            subspan: true,
            reason
          })
        });
      }
      function cp021bLexicalGiveSplit(argumentNodes) {
        const compact = withoutIgnorableSpaceText2(argumentNodes || []);
        const candidates = [];
        for (let index = 1; index < compact.length; index += 1) {
          const first = compact.slice(0, index);
          const second = compact.slice(index);
          const firstPerson = cp021bSpanIsPersonNP2(first);
          const secondPerson = cp021bSpanIsPersonNP2(second);
          const firstThing = cp021bSpanIsThingNP2(first);
          const secondThing = cp021bSpanIsThingNP2(second);
          if (firstThing && !firstPerson && secondPerson) {
            candidates.push({ profile: "theme_recipient_baseline", first, second });
          }
          if (firstPerson && secondThing && !secondPerson) {
            candidates.push({ profile: "nonbaseline_participant_order_unresolved", first, second });
          }
        }
        return candidates.length === 1 ? candidates[0] : null;
      }
      function lexicalGiveRelationFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (particles.length) return null;
        const compact = withoutIgnorableSpaceText2(bareCore);
        const markerIndexes = compact.map((node, index) => cp021bIsBei2Marker2(node) ? index : -1).filter((index) => index >= 0);
        if (markerIndexes.length !== 1) return null;
        const markerIndex = markerIndexes[0];
        const subjectNodes = compact.slice(0, markerIndex);
        if (subjectNodes.length && !cp021bSpanIsPersonNP2(subjectNodes)) return null;
        const marker = compact[markerIndex];
        let argumentStart = markerIndex + 1;
        let aspect = null;
        if (compact[argumentStart] && isToken2(compact[argumentStart], "咗")) {
          aspect = compact[argumentStart];
          argumentStart += 1;
        }
        const argumentNodes = compact.slice(argumentStart);
        if (argumentNodes.length < 2 || argumentNodes.some(cp021bIsBei2Marker2)) return null;
        const split = cp021bLexicalGiveSplit(argumentNodes);
        if (!split) return null;
        const baseline = split.profile === "theme_recipient_baseline";
        const subject = subjectNodes.length ? cp021bArgumentSpan2(subjectNodes, {
          parent_type: "LexicalGiveRelation",
          label: "who",
          role: "subject",
          slots: ["subject", "person_np", "np"],
          reason: "This independently person-denoting span is the overt material before lexical GIVE; no subject is inserted when it is absent."
        }) : null;
        const give = bridgeFramePartClone2(marker, {
          label: "doing",
          pos: "verb",
          syntax: "lexical_give_predicate",
          slots: ["transfer_predicate", "action_verb", "main_verb", "predicate"],
          reason: "畀/俾 is the lexical giving predicate in this bounded CP021B profile, not a post-theme linker or passive/permissive marker."
        });
        const aspectChild = aspect ? bridgeFramePartClone2(aspect, {
          label: "func",
          pos: "aspect",
          syntax: "perfective_aspect",
          slots: ["perfective_aspect", "aspect_marker"],
          reason: "咗 is the overt perfective marker immediately after lexical GIVE."
        }) : null;
        const firstArgument = cp021bArgumentSpan2(split.first, baseline ? {
          parent_type: "LexicalGiveRelation",
          label: "what",
          role: "theme",
          slots: ["theme", "object", "np"],
          reason: "The uniquely split first nominal span has independent thing evidence and receives Theme/Object only in the reviewed theme-before-recipient profile."
        } : {
          parent_type: "LexicalGiveRelation",
          label: "who",
          role: "post_give_participant_1",
          slots: ["post_give_participant_1", "person_np", "np"],
          reason: "The first visible participant is person-like, but its semantic role is unresolved in the nonbaseline order."
        });
        const secondArgument = cp021bArgumentSpan2(split.second, baseline ? {
          parent_type: "LexicalGiveRelation",
          label: "who",
          role: "recipient_candidate",
          slots: ["recipient_candidate", "goal_candidate", "person_np", "np"],
          reason: "Recipient-candidate status follows from the reviewed lexical-GIVE profile plus independent person evidence, not final position alone."
        } : {
          parent_type: "LexicalGiveRelation",
          label: "what",
          role: "post_give_participant_2",
          slots: ["post_give_participant_2", "np"],
          reason: "The second visible participant is thing-like, but no Theme/Object role is exported in the nonbaseline order."
        });
        const children = [subject, give, aspectChild, firstArgument, secondArgument].filter(Boolean);
        const semanticCode = baseline ? "cp021b_provisional_lexical_give_relation" : "lexical_give_argument_order_unresolved";
        const explanation = baseline ? "The thing given is followed by the person who receives it in this reviewed pattern." : "A giving meaning is visible, but this order needs context; the parser does not decide who receives what.";
        return construction2("LexicalGiveRelation", "Give", children, {
          note: explanation,
          slots: cleanSlots2([
            "lexical_give_relation",
            "give_relation",
            "vp",
            "action_vp",
            "predicate",
            "transfer_predicate",
            ...subject ? ["subject"] : [],
            ...baseline ? ["theme", "object", "recipient_candidate", "goal_candidate"] : ["post_give_participant_1", "post_give_participant_2"]
          ]),
          trace: traceInfo2("generative_template", {
            construction_type: "LexicalGiveRelation",
            template_family: "construction_template",
            cp021b_design_family: "frozen_lexical_give",
            template: ["subject?", "lexical_give_predicate!", "perfective_aspect?", "postverbal_nominal_1!", "postverbal_nominal_2!"],
            assigned_slots: [
              ...subject ? ["subject"] : [],
              "transfer_predicate",
              ...aspect ? ["perfective_aspect"] : [],
              ...baseline ? ["theme", "recipient_candidate"] : ["post_give_participant_1", "post_give_participant_2"]
            ],
            relation_profile: split.profile,
            marker_surface: flattenSurface2(marker),
            aspect_surface: aspect ? flattenSurface2(aspect) : "",
            overt_subject_surface: subjectNodes.map(flattenSurface2).join(""),
            postverbal_argument_surfaces: [split.first.map(flattenSurface2).join(""), split.second.map(flattenSurface2).join("")],
            visible_order: baseline ? "thing_before_person" : "person_before_thing",
            semantic_role_assignment: baseline ? "theme_and_recipient_candidate_from_reviewed_profile" : "unresolved",
            orthographic_parity: "畀=俾",
            hidden_participants_inserted: false,
            weight_rule_used: false,
            semantic_review_flags: [semanticCode],
            learner_gloss_lines: ["give", explanation],
            not_claims: ["not_free_order_alternation", "not_benefactive_linker", "not_passive_or_permissive", "not_supported_productive"],
            surfaces: children.map(flattenSurface2),
            reason: explanation
          })
        });
      }
      return {
        cp021bArgumentSpan: cp021bArgumentSpan2,
        cp021bIsBei2Marker: cp021bIsBei2Marker2,
        cp021bNodeIsPersonEvidence: cp021bNodeIsPersonEvidence2,
        cp021bSpanIsPersonNP: cp021bSpanIsPersonNP2,
        cp021bSpanIsThingNP: cp021bSpanIsThingNP2,
        lexicalGiveRelationFallback: lexicalGiveRelationFallback2
      };
    };
  }
});

// src/parser/detectors/transfer/post-theme.js
var require_post_theme = __commonJS({
  "src/parser/detectors/transfer/post-theme.js"(exports2, module2) {
    "use strict";
    module2.exports = function createPostThemeDetectors2(dependencies = {}) {
      const {
        bridgeFramePartClone: bridgeFramePartClone2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        cp020NodeIsPredicateEvidence: cp020NodeIsPredicateEvidence2,
        cp021bArgumentSpan: cp021bArgumentSpan2,
        cp021bIsBei2Marker: cp021bIsBei2Marker2,
        cp021bSpanIsPersonNP: cp021bSpanIsPersonNP2,
        cp021bSpanIsThingNP: cp021bSpanIsThingNP2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      const CP021B_POST_THEME_PREDICATE_PROFILES2 = Object.freeze({
        "借": {
          profile: "predicate_profile_borrow_lend",
          candidates: ["goal_or_lend_to_candidate"],
          final_predicate_required: false
        },
        "交": {
          profile: "predicate_profile_transfer",
          candidates: ["goal_or_recipient_candidate"],
          final_predicate_required: false
        },
        "織": {
          profile: "predicate_profile_creation",
          candidates: ["beneficiary_or_intended_user_candidate"],
          final_predicate_required: false
        },
        "買": {
          profile: "predicate_profile_acquisition",
          candidates: ["beneficiary_or_intended_recipient_candidate"],
          final_predicate_required: false
        },
        "攞": {
          profile: "predicate_profile_final_predicate",
          candidates: ["final_predicate_participant_or_beneficiary_candidate"],
          final_predicate_required: true
        }
      });
      function cp021bMakePostThemeRelation2(fields) {
        const marker = bridgeFramePartClone2(fields.marker, {
          label: "func",
          pos: "function",
          syntax: "post_theme_link_marker",
          slots: ["post_theme_link_marker"],
          reason: "畀/俾 links an overt upstream predicate-theme VP to a following person; no fixed marker category or participant role is selected."
        });
        const participant = cp021bArgumentSpan2(fields.participantNodes, {
          parent_type: "PostThemeParticipantRelation",
          label: "who",
          role: "post_theme_participant",
          slots: ["post_theme_participant", "person_np", "np"],
          reason: "The overt post-theme person remains structurally visible; recipient, goal, beneficiary, agent, and source are candidate readings only, never asserted slots."
        });
        const following = fields.followingPredicate ? bridgeFramePartClone2(fields.followingPredicate, {
          label: "doing",
          pos: "verb",
          syntax: `${(firstToken2(fields.followingPredicate) || fields.followingPredicate).syntax || "verb"} following_predicate`,
          slots: ["following_predicate", "action_verb", "predicate"],
          reason: "The overt final predicate remains visible; no hidden shared participant is inserted."
        }) : null;
        const children = [fields.upstreamVP, marker, participant, following].filter(Boolean);
        return construction2("PostThemeParticipantRelation", "For / to", children, {
          note: "Links the preceding action and thing to a following person. The exact link depends on the verb and context.",
          slots: cleanSlots2([
            "post_theme_participant_relation",
            "post_theme_link_marker",
            "post_theme_participant",
            "person_np",
            "np",
            "predicate",
            "vp",
            "action_vp",
            ...following ? ["following_predicate"] : []
          ]),
          trace: traceInfo2("generative_template", {
            construction_type: "PostThemeParticipantRelation",
            template_family: "construction_template",
            cp021b_design_family: "frozen_post_theme_participant",
            template: ["upstream_predicate_theme_vp!", "post_theme_link_marker!", "post_theme_participant!", "following_predicate?"],
            assigned_slots: ["upstream_vp", "post_theme_link_marker", "post_theme_participant", ...following ? ["following_predicate"] : []],
            relation_profile: "theory_neutral_post_theme_participant",
            upstream_predicate_surface: fields.upstreamPredicateSurface,
            upstream_theme_surface: fields.upstreamThemeSurface,
            marker_surface: flattenSurface2(fields.marker),
            postmarker_participant_surface: fields.participantNodes.map(flattenSurface2).join(""),
            following_predicate_surface: following ? flattenSurface2(fields.followingPredicate) : "",
            participant_role_candidates: fields.profile.candidates,
            participant_role_status: "unresolved_or_context_dependent",
            marker_category_status: "not_selected",
            upstream_predicate_profile: fields.profile.profile,
            orthographic_parity: "畀=俾",
            hidden_participants_inserted: false,
            semantic_review_flags: ["post_theme_participant_role_context_dependent"],
            learner_gloss_lines: ["for / to", "Links the preceding action and thing to a following person. The exact link depends on the verb and context."],
            not_claims: ["not_lexical_give", "not_passive_or_permissive", "not_unified_recipient_or_beneficiary", "not_fixed_marker_category", "not_supported_productive"],
            surfaces: children.map(flattenSurface2),
            reason: "This bounded CP021B relation preserves the overt predicate, theme, marker, person, and optional final predicate while leaving the exact participant role and marker category unresolved."
          })
        });
      }
      function postThemeParticipantRelationFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (particles.length) return null;
        const compact = withoutIgnorableSpaceText2(bareCore);
        const markerIndexes = compact.map((node, index) => cp021bIsBei2Marker2(node) ? index : -1).filter((index) => index >= 0);
        if (markerIndexes.length !== 1) return null;
        const markerIndex = markerIndexes[0];
        if (markerIndex < 2 || markerIndex >= compact.length - 1) return null;
        const profileCandidates = [];
        for (let predicateIndex = 0; predicateIndex < markerIndex; predicateIndex += 1) {
          const predicateSurface = flattenSurface2(compact[predicateIndex]);
          const profile = CP021B_POST_THEME_PREDICATE_PROFILES2[predicateSurface];
          if (!profile) continue;
          const subjectNodes = compact.slice(0, predicateIndex);
          if (subjectNodes.length && !cp021bSpanIsPersonNP2(subjectNodes)) continue;
          let themeStart = predicateIndex + 1;
          let aspect = null;
          if (isToken2(compact[themeStart], "咗")) {
            aspect = compact[themeStart];
            themeStart += 1;
          }
          const themeNodes = compact.slice(themeStart, markerIndex);
          if (!cp021bSpanIsThingNP2(themeNodes)) continue;
          profileCandidates.push({ predicateIndex, predicateSurface, profile, subjectNodes, aspect, themeNodes });
        }
        if (profileCandidates.length !== 1) return null;
        const candidate = profileCandidates[0];
        const afterMarker = compact.slice(markerIndex + 1);
        let participantNodes = afterMarker;
        let followingPredicate = null;
        if (candidate.profile.final_predicate_required) {
          if (afterMarker.length < 2) return null;
          followingPredicate = afterMarker[afterMarker.length - 1];
          participantNodes = afterMarker.slice(0, -1);
          if (!cp020NodeIsPredicateEvidence2(followingPredicate)) return null;
        } else if (afterMarker.some(cp020NodeIsPredicateEvidence2)) {
          return null;
        }
        if (!cp021bSpanIsPersonNP2(participantNodes)) return null;
        const predicateNode = compact[candidate.predicateIndex];
        const predicateChild = bridgeFramePartClone2(predicateNode, {
          label: "doing",
          pos: "verb",
          syntax: `${(firstToken2(predicateNode) || predicateNode).syntax || "verb"} cp021b_upstream_predicate`,
          slots: ["action_verb", "main_verb", "predicate"],
          reason: "The reviewed upstream lexical predicate supplies the valency profile; the later marker alone does not determine the relation."
        });
        const aspectChild = candidate.aspect ? bridgeFramePartClone2(candidate.aspect, {
          label: "func",
          pos: "aspect",
          syntax: "perfective_aspect",
          slots: ["perfective_aspect", "aspect_marker"],
          reason: "The overt aspect remains inside the upstream predicate-theme VP."
        }) : null;
        const theme = cp021bArgumentSpan2(candidate.themeNodes, {
          parent_type: candidate.aspect ? "PerfectiveVP" : "TransitiveVP",
          label: "what",
          role: "theme",
          slots: ["theme", "object", "np"],
          reason: "The overt theme remains inside the upstream VP; its presence is required before post-theme linking can be considered."
        });
        const upstreamChildren = [predicateChild, aspectChild, theme].filter(Boolean);
        const upstreamType = candidate.aspect ? "PerfectiveVP" : "TransitiveVP";
        const upstreamVP = construction2(upstreamType, candidate.aspect ? "PerfectiveVP" : "VP", upstreamChildren, {
          note: "Overt upstream predicate and theme retained as one VP before the post-theme link marker.",
          slots: cleanSlots2(["vp", "action_vp", "predicate", "theme", "object", ...candidate.aspect ? ["perfective_aspect"] : []]),
          trace: traceInfo2("generative_template", {
            construction_type: upstreamType,
            template_family: "construction_template",
            cp021b_design_family: "upstream_predicate_theme_vp",
            template: ["action_verb!", "perfective_aspect?", "theme!"],
            assigned_slots: ["action_verb", ...candidate.aspect ? ["perfective_aspect"] : [], "theme"],
            surfaces: upstreamChildren.map(flattenSurface2),
            subspan: true,
            reason: "C02 requires an overt non-GIVE predicate and overt local theme before the marker."
          })
        });
        const relation = cp021bMakePostThemeRelation2({
          upstreamVP,
          upstreamPredicateSurface: candidate.predicateSurface,
          upstreamThemeSurface: candidate.themeNodes.map(flattenSurface2).join(""),
          marker: compact[markerIndex],
          participantNodes,
          followingPredicate,
          profile: candidate.profile
        });
        if (!candidate.subjectNodes.length) return relation;
        const subject = cp021bArgumentSpan2(candidate.subjectNodes, {
          parent_type: "SubjectPredicateClause",
          label: "who",
          role: "subject",
          slots: ["subject", "person_np", "np"],
          reason: "The overt subject is kept outside PostThemeParticipantRelation, as required by the frozen predicate-level design."
        });
        return construction2("SubjectPredicateClause", "Clause", [subject, relation], {
          note: "Subject plus the bounded post-theme participant predicate relation.",
          slots: cleanSlots2(["subject_predicate_clause", "subject", "predicate", "clause"]),
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "construction_template",
            cp021b_design_family: "subject_wrapper",
            template: ["subject!", "post_theme_participant_relation!"],
            assigned_slots: ["subject", "predicate"],
            surfaces: [flattenSurface2(subject), flattenSurface2(relation)],
            reason: "The optional overt subject is outside the new predicate-level relation."
          })
        });
      }
      function cp021bBoundaryReviewFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (particles.length) return null;
        const compact = withoutIgnorableSpaceText2(bareCore);
        const markerIndexes = compact.map((node, index) => cp021bIsBei2Marker2(node) ? index : -1).filter((index) => index >= 0);
        if (!markerIndexes.length) return null;
        const boundaryClause = (subjectNodes, profile, code) => {
          if (!subjectNodes.length || !cp021bSpanIsPersonNP2(subjectNodes)) return null;
          const subject = cp021bArgumentSpan2(subjectNodes, {
            parent_type: "SubjectPredicateClause",
            label: "who",
            role: "subject",
            slots: ["subject", "person_np", "np"],
            reason: "The overt subject remains visible in a boundary-only wrapper; no participant relation is inferred."
          });
          const remainder = compact.slice(subjectNodes.length);
          const children = [subject, ...remainder];
          return construction2("SubjectPredicateClause", "Clause", children, {
            note: "Overt clause material preserved for review outside the two frozen CP021B relation designs.",
            slots: cleanSlots2(["subject_predicate_clause", "subject", "predicate", "clause"]),
            trace: traceInfo2("generative_template", {
              construction_type: "SubjectPredicateClause",
              template_family: "construction_template",
              cp021b_design_family: "nonemitting_boundary_wrapper",
              template: ["subject!", "boundary_predicate_material!"],
              assigned_slots: ["subject", "predicate_material"],
              boundary_profile: profile,
              semantic_review_flags: [code],
              hidden_participants_inserted: false,
              semantic_role_assignment: "none",
              surfaces: children.map(flattenSurface2),
              reason: "The wrapper preserves every overt token and full root coverage while refusing a new lexical-GIVE or post-theme participant relation."
            })
          });
        };
        if (markerIndexes.length >= 2) {
          return boundaryClause(compact.slice(0, markerIndexes[0]), "double_marker_heavy_theme", "lexical_give_double_marker_heavy_theme_unfrozen");
        }
        if (compact.some((node) => isToken2(node, "將"))) {
          const zoengIndex = compact.findIndex((node) => isToken2(node, "將"));
          return boundaryClause(compact.slice(0, zoengIndex), "zoeng_restructure", "lexical_give_zoeng_restructure_outside_design");
        }
        const markerIndex = markerIndexes[0];
        const beforeMarker = compact.slice(0, markerIndex);
        const afterMarker = compact.slice(markerIndex + 1).filter((node) => !isToken2(node, "咗"));
        if (!cp021bSpanIsPersonNP2(afterMarker)) return null;
        for (let splitIndex = 1; splitIndex < beforeMarker.length; splitIndex += 1) {
          const frontedNodes = beforeMarker.slice(0, splitIndex);
          const subjectNodes = beforeMarker.slice(splitIndex);
          if (!cp021bSpanIsThingNP2(frontedNodes) || !cp021bSpanIsPersonNP2(subjectNodes)) continue;
          const topic = cp021bArgumentSpan2(frontedNodes, {
            parent_type: "TopicComment",
            label: "what",
            role: "fronted_theme_candidate",
            slots: ["fronted_theme_candidate", "topic", "np"],
            reason: "The overt fronted thing is preserved as topic material; it is not copied into the local GIVE predicate or assigned by a hidden gap."
          });
          const subject = cp021bArgumentSpan2(subjectNodes, {
            parent_type: "SubjectPredicateClause",
            label: "who",
            role: "subject",
            slots: ["subject", "person_np", "np"],
            reason: "The overt subject remains inside the comment clause after the fronted topic."
          });
          const marker = bridgeFramePartClone2(compact[markerIndex], {
            label: "doing",
            pos: "verb",
            syntax: "lexical_give_predicate_fronted_theme_boundary",
            slots: ["transfer_predicate", "action_verb", "predicate"],
            reason: "Lexical GIVE is visible, but the local relation is deliberately not emitted because its theme is fronted outside the candidate span."
          });
          const aspectNode = compact[markerIndex + 1] && isToken2(compact[markerIndex + 1], "咗") ? bridgeFramePartClone2(compact[markerIndex + 1], {
            label: "func",
            pos: "aspect",
            syntax: "perfective_aspect",
            slots: ["perfective_aspect", "aspect_marker"],
            reason: "Overt perfective aspect retained in the local predicate material."
          }) : null;
          const participant = cp021bArgumentSpan2(afterMarker, {
            parent_type: "SubjectPredicateClause",
            label: "who",
            role: "post_give_participant",
            slots: ["post_give_participant", "person_np", "np"],
            reason: "The overt post-GIVE person remains visible without an asserted recipient, goal, or beneficiary role."
          });
          const commentChildren = [subject, marker, aspectNode, participant].filter(Boolean);
          const comment = construction2("SubjectPredicateClause", "Clause", commentChildren, {
            note: "Overt local comment material under a fronted-topic boundary.",
            slots: cleanSlots2(["subject_predicate_clause", "subject", "predicate", "clause"]),
            trace: traceInfo2("generative_template", {
              construction_type: "SubjectPredicateClause",
              template_family: "construction_template",
              cp021b_design_family: "fronted_theme_comment",
              assigned_slots: ["subject", "predicate_material"],
              hidden_participants_inserted: false,
              surfaces: commentChildren.map(flattenSurface2)
            })
          });
          return construction2("TopicComment", "TopicComment", [topic, comment], {
            note: "Fronted topic plus overt comment, preserved without a new local lexical-GIVE relation.",
            slots: cleanSlots2(["topic_comment", "topic", "comment", "predicate"]),
            trace: traceInfo2("generative_template", {
              construction_type: "TopicComment",
              template_family: "construction_template",
              cp021b_design_family: "nonemitting_boundary_wrapper",
              template: ["fronted_topic!", "comment!"],
              assigned_slots: ["topic", "comment"],
              boundary_profile: "fronted_theme",
              semantic_review_flags: ["lexical_give_fronted_theme_outside_design"],
              hidden_participants_inserted: false,
              semantic_role_assignment: "none",
              surfaces: [flattenSurface2(topic), flattenSurface2(comment)],
              reason: "The fronted theme and discourse link remain overt, but no copied theme or full local relation is introduced."
            })
          });
        }
        return null;
      }
      return {
        CP021B_POST_THEME_PREDICATE_PROFILES: CP021B_POST_THEME_PREDICATE_PROFILES2,
        cp021bBoundaryReviewFallback: cp021bBoundaryReviewFallback2,
        cp021bMakePostThemeRelation: cp021bMakePostThemeRelation2,
        postThemeParticipantRelationFallback: postThemeParticipantRelationFallback2
      };
    };
  }
});

// src/parser/detectors/transfer/passive-permissive.js
var require_passive_permissive = __commonJS({
  "src/parser/detectors/transfer/passive-permissive.js"(exports2, module2) {
    "use strict";
    module2.exports = function createPassivePermissiveDetectors2(dependencies = {}) {
      const {
        bridgeFramePartClone: bridgeFramePartClone2,
        categorySubspanFor: categorySubspanFor2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        cp021bNodeIsPersonEvidence: cp021bNodeIsPersonEvidence2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function cp020NodeIsPersonEvidence2(node) {
        const tok = firstToken2(node) || node;
        const slots = nodeSlots2(node);
        const localAnimateAgent = flattenSurface2(node) === "蚊";
        return localAnimateAgent || Boolean(tok && tok.label === "who") || slots.includes("person_np") || slots.includes("co_participant") || slots.includes("recipient") || slots.includes("stance_holder") || slots.includes("subject");
      }
      function cp020NodeIsObjectEvidence(node) {
        const tok = firstToken2(node) || node;
        const slots = nodeSlots2(node);
        return Boolean(tok && (tok.label === "what" || tok.label === "where" || tok.label === "measure_word")) || slots.includes("object") || slots.includes("head_noun") || slots.includes("classifier") || slots.includes("np");
      }
      function cp020NodeIsPredicateEvidence2(node) {
        const slots = nodeSlots2(node);
        return slots.includes("predicate") || slots.includes("action_verb") || slots.includes("main_verb") || slots.includes("movement_verb") || slots.includes("stative_predicate") || slots.includes("vp");
      }
      function cp020NodeIsBlockingPreMarkerMaterial(node) {
        const slots = nodeSlots2(node);
        return slots.includes("action_verb") || slots.includes("main_verb") || slots.includes("predicate") || slots.includes("perfective_aspect") || slots.includes("progressive_aspect") || slots.includes("negator");
      }
      const CP020_PERMISSIVE_FAVORING_PREDICATE_SURFACES = /* @__PURE__ */ new Set([
        "打籃球",
        "食蛋糕",
        "跌低"
      ]);
      function cp020PredicateFavorsPermissive(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        const surface = compact.map(flattenSurface2).join("");
        if (CP020_PERMISSIVE_FAVORING_PREDICATE_SURFACES.has(surface)) return true;
        return compact.some((node) => {
          const slots = nodeSlots2(node);
          const nodeSurface = flattenSurface2(node);
          return slots.includes("movement_verb") || slots.includes("motion_predicate") || slots.includes("motion_goal_vp") || nodeSurface === "去" || nodeSurface.startsWith("去旅行");
        });
      }
      function cp020TrailingObjectCandidateSpan(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        for (let i = 1; i < compact.length; i += 1) {
          const node = compact[i];
          const slots = nodeSlots2(node);
          const tok = firstToken2(node) || node;
          const syntax = String(tok && tok.syntax || node.syntax || "");
          if (node.kind === "construction" && (slots.includes("np") || slots.includes("object"))) {
            return { start: i, end: i + 1 };
          }
          if (slots.includes("classifier")) {
            let end = i + 1;
            while (end < compact.length) {
              const next = compact[end];
              const nextTok = firstToken2(next) || next;
              const nextSlots = nodeSlots2(next);
              const nextSyntax = String(nextTok && nextTok.syntax || next.syntax || "");
              if (next.kind === "text" || nodeCanFillSlot2(next, "particle") || nodeCanFillSlot2(next, "aspect_marker") || cp020NodeIsPredicateEvidence2(next) || nextSlots.includes("location") || nextSlots.includes("time") || /location|direction|temporal/u.test(nextSyntax) || Boolean(nextTok && ["doing", "where", "when", "particle", "func"].includes(nextTok.label))) break;
              end += 1;
            }
            return { start: i, end: Math.max(i + 1, end) };
          }
          if (i === compact.length - 1 && tok && (tok.label === "what" || slots.includes("object")) && !slots.includes("location") && !/location/u.test(syntax)) {
            const previous = compact[i - 1];
            const previousSlots = previous ? nodeSlots2(previous) : [];
            const previousSurface = previous ? flattenSurface2(previous) : "";
            const start = previous && (previousSlots.includes("quantity") || previousSlots.includes("degree") || ["好多", "幾多", "少少"].includes(previousSurface)) ? i - 1 : i;
            return { start, end: i + 1 };
          }
        }
        return null;
      }
      function cp020ContextualPredicateLearnerChildren(nodes) {
        const children = (nodes || []).map((node) => node);
        const surfaceAt = (index) => index >= 0 && index < children.length ? flattenSurface2(children[index]) : "";
        for (let i = 0; i < children.length - 1; i += 1) {
          if (surfaceAt(i) !== "上" || surfaceAt(i + 1) !== "牆") continue;
          children[i] = bridgeFramePartClone2(children[i], {
            label: "func",
            pos: "directional_linker",
            syntax: "spatial_goal_linker movement_direction_up path_component",
            slots: ["movement_direction", "path_component"],
            note: "onto / up onto",
            reason: "In 貼上牆, 上 links the placement event to its spatial goal; it is not the temporal word 'previous'.",
            trace_detail: {
              learner_gloss_lines: ["onto / up onto", "Spatial direction toward the following goal, not time."]
            }
          });
          children[i + 1] = bridgeFramePartClone2(children[i + 1], {
            label: "where",
            pos: "location",
            syntax: "spatial_goal location_np wall_location",
            slots: ["goal", "location", "np"],
            note: "wall / onto the wall",
            reason: "After spatial 上 in 貼上牆, 牆 is the placement goal rather than an ordinary object.",
            trace_detail: {
              learner_gloss_lines: ["wall / onto the wall", "The spatial goal of the placement event."]
            }
          });
          i += 1;
        }
        return children;
      }
      function passivePermissiveRelationFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.some((node) => node.kind === "text" && /[，,；;：:。！？?!]/u.test(String(node.text || node.surface || "")))) return null;
        const beiIndex = compact.findIndex((node, index) => index > 0 && (isToken2(node, "畀") || isToken2(node, "俾")));
        if (beiIndex <= 0 || beiIndex >= compact.length - 2) return null;
        let participantNodes = compact.slice(0, beiIndex);
        let modal = null;
        if (participantNodes.length >= 2 && nodeCanFillSlot2(participantNodes[participantNodes.length - 1], "modal")) {
          modal = participantNodes[participantNodes.length - 1];
          participantNodes = participantNodes.slice(0, -1);
        }
        if (!participantNodes.length || participantNodes.length > 5) return null;
        if (participantNodes.some(cp020NodeIsBlockingPreMarkerMaterial)) return null;
        if (participantNodes.some((node) => {
          const tok = firstToken2(node) || node;
          return Boolean(tok && ["doing", "like", "how", "when", "particle"].includes(tok.label));
        })) return null;
        if (!participantNodes.some((node) => cp020NodeIsPersonEvidence2(node) || cp020NodeIsObjectEvidence(node))) return null;
        const post = compact.slice(beiIndex + 1);
        if (nodeCanFillSlot2(post[0], "perfective_aspect") || nodeCanFillSlot2(post[0], "progressive_aspect") || nodeCanFillSlot2(post[0], "aspect_marker")) return null;
        let agentNodes = [];
        let predicateNodes = [];
        if (cp020NodeIsPersonEvidence2(post[0])) {
          if (flattenSurface2(post[0]) === "阿" && post.length >= 3) {
            agentNodes = post.slice(0, 2);
            predicateNodes = post.slice(2);
          } else {
            agentNodes = [post[0]];
            predicateNodes = post.slice(1);
          }
        } else {
          const predicateStart = post.findIndex((node, index) => index > 0 && cp020NodeIsPredicateEvidence2(node));
          if (predicateStart <= 0) return null;
          agentNodes = post.slice(0, predicateStart);
          predicateNodes = post.slice(predicateStart);
        }
        if (!agentNodes.length || !predicateNodes.length) return null;
        if (predicateNodes.every(cp021bNodeIsPersonEvidence2)) return null;
        const agentIsClassifierPersonNP = agentNodes.length === 2 && nodeCanFillSlot2(agentNodes[0], "classifier") && cp020NodeIsPersonEvidence2(agentNodes[1]);
        if (agentNodes.some((node, index) => {
          if (cp020NodeIsPersonEvidence2(node)) return false;
          if (agentIsClassifierPersonNP && index === 0) return false;
          const tok = firstToken2(node) || node;
          return nodeCanFillSlot2(node, "classifier") || Boolean(tok && (tok.label === "what" || tok.label === "measure_word"));
        })) return null;
        if (agentNodes.some(cp020NodeIsPredicateEvidence2)) return null;
        if (predicateNodes.every((node) => node.kind === "text" || nodeCanFillSlot2(node, "particle"))) return null;
        const internalParticleIndex = predicateNodes.findIndex((node, index) => index < predicateNodes.length - 1 && nodeCanFillSlot2(node, "particle"));
        if (internalParticleIndex >= 0) return null;
        {
          const firstPred = predicateNodes[0];
          const tok = firstToken2(firstPred) || firstPred;
          const syntax = String(tok && tok.syntax || firstPred.syntax || "");
          if (!cp020NodeIsPredicateEvidence2(firstPred) && (nodeCanFillSlot2(firstPred, "classifier") || nodeCanFillSlot2(firstPred, "object") || nodeCanFillSlot2(firstPred, "demonstrative") || /determiner/u.test(syntax) || Boolean(tok && (tok.label === "what" || tok.label === "measure_word")))) return null;
        }
        const marker = bridgeFramePartClone2(compact[beiIndex], {
          label: "func",
          pos: "function",
          syntax: "bei_passive_permissive_marker",
          slots: ["passive_marker", "bei_marker"],
          reason: "畀/俾 links the preceding participant with a following participant and predicate; the isolated surface may express passive voice or permissive 'let'."
        });
        const participantHasPerson = participantNodes.some(cp020NodeIsPersonEvidence2);
        const participantHasObjectShape = participantNodes.some((node) => {
          const tok = firstToken2(node) || node;
          const slots = nodeSlots2(node);
          return Boolean(tok && (tok.label === "what" || tok.label === "measure_word")) || slots.includes("classifier") || slots.includes("object");
        });
        const participantIsPersonOnly = participantHasPerson && !participantHasObjectShape;
        const groupedPredicate = categorySubspanFor2(predicateNodes, ["ProductiveVO", "TransitiveVP"]);
        const predicateAnalysisNodes = groupedPredicate ? [groupedPredicate] : predicateNodes;
        const retainedSpan = cp020TrailingObjectCandidateSpan(predicateAnalysisNodes);
        const retainedSurface = retainedSpan ? predicateAnalysisNodes.slice(retainedSpan.start, retainedSpan.end).map(flattenSurface2).join("") : "";
        const agentSurface = agentNodes.map(flattenSurface2).join("");
        const participantSurface = participantNodes.map(flattenSurface2).join("");
        const semanticReviewFlags = [];
        const readingCandidates = [];
        const permissiveFavored = participantIsPersonOnly && !retainedSurface && cp020PredicateFavorsPermissive(predicateAnalysisNodes);
        let passiveSubtype = "canonical_passive_candidate";
        let learnerLabel = "Passive";
        if (retainedSurface) {
          semanticReviewFlags.push("passive_permissive_surface_ambiguity", "retained_patient_role_requires_review");
          readingCandidates.push("indirect_passive_candidate", "permissive_let_allow");
          if (!participantIsPersonOnly) readingCandidates.push("canonical_passive_role_boundary");
          learnerLabel = "Passive / let";
          passiveSubtype = "retained_object_passive_permissive_ambiguity";
        } else if (permissiveFavored) {
          readingCandidates.push("permissive_let_allow");
          learnerLabel = "Let / allow";
          passiveSubtype = "permissive_candidate";
        } else if (participantIsPersonOnly) {
          semanticReviewFlags.push("passive_permissive_surface_ambiguity");
          readingCandidates.push("canonical_or_indirect_passive", "permissive_let_allow");
          learnerLabel = "Passive / let";
          passiveSubtype = "canonical_passive_permissive_ambiguity";
        } else {
          readingCandidates.push("canonical_passive");
        }
        const learnerGlossLines = learnerLabel === "Passive" ? ["passive relation", "The first participant is presented as undergoing the following event."] : learnerLabel === "Let / allow" ? ["let / allow relation", "The first participant allows the following participant to carry out the action."] : ["passive or let / allow relation", "The surface may describe something happening to the first participant or someone being allowed to act; context may decide."];
        const agentSpanIsKnownPersonExpression = agentNodes.map(flattenSurface2).join("") === "阿媽";
        const agentChildren = agentNodes.map((node) => {
          const surface = flattenSurface2(node);
          if (agentSpanIsKnownPersonExpression && surface === "阿") {
            return bridgeFramePartClone2(node, {
              label: "func",
              pos: "prefix",
              syntax: "familiar_kinship_prefix bei_postmarker_participant_part",
              slots: ["postmarker_participant"],
              note: "familiar kinship/name prefix; part of 阿媽",
              trace_detail: { learner_gloss_lines: ["familiar kinship/name prefix", "Part of 阿媽; not the person referent by itself."] },
              reason: "Inside the overt post-畀/俾 participant 阿媽, 阿 is a familiar kinship/name prefix; 媽 carries the person reference."
            });
          }
          if (cp020NodeIsPersonEvidence2(node) || agentSpanIsKnownPersonExpression) {
            return bridgeFramePartClone2(node, {
              label: "who",
              pos: "np",
              syntax: `${node.syntax || "agent_np"} bei_postmarker_participant`,
              slots: ["postmarker_participant", "person_np", "np", "subject"],
              note: agentSpanIsKnownPersonExpression ? "mum / mother" : node.note,
              trace_detail: agentSpanIsKnownPersonExpression ? { learner_gloss_lines: ["mum / mother", "The person-denoting head of 阿媽."] } : void 0,
              reason: "Overt post-畀/俾 participant in the passive/permissive relation; this participant may be a passive agent or the actor who is permitted to act."
            });
          }
          return node;
        });
        const predicateChildren = groupedPredicate ? [groupedPredicate] : cp020ContextualPredicateLearnerChildren(predicateNodes);
        const children = [...participantNodes, ...modal ? [modal] : [], marker, ...agentChildren, ...predicateChildren, ...particles];
        return construction2("PassivePermissiveRelation", learnerLabel, children, {
          note: "Transparent 畀/俾 passive–permissive relation. The relation preserves canonical-passive, retained-object/indirect-passive, and permissive alternatives instead of presenting generic affectedness as Cantonese grammar.",
          slots: cleanSlots2(["passive_permissive_relation", "bei_relation", "clause", "predicate", "vp", "pre_marker_participant", "postmarker_participant", ...retainedSurface ? ["retained_patient_candidate", "object"] : [], ...templateDerivedSlots2("PassivePermissiveRelation", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "PassivePermissiveRelation",
            template_family: "generative_template",
            template: ["participant!", "modal?", "bei_marker!", "postmarker_participant!", "predicate_material!", "particle?"],
            assigned_slots: ["pre_marker_participant", ...modal ? ["modal"] : [], "bei_marker", "postmarker_participant", "predicate", ...retainedSurface ? ["retained_patient_candidate"] : [], ...particles.map(() => "particle")],
            passive_subtype: passiveSubtype,
            reading_candidates: readingCandidates,
            participant_surface: participantSurface,
            postmarker_participant_surface: agentSurface,
            retained_patient_candidate_surface: retainedSurface,
            semantic_review_flags: semanticReviewFlags,
            learner_gloss_lines: learnerGlossLines,
            not_claims: ["not_generic_affectedness_construction", "not_deterministic_passive_classifier", "not_generic_force_causative"],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "CP020 replaces the debunked AffectednessFrame with one transparent passive/permissive relation. Inanimate/full-patient shapes may support canonical passive; independently supported motion and bounded actor-oriented predicates may favor permissive let/allow; genuinely ambiguous animate and retained-object strings preserve competing readings."
          })
        });
      }
      return {
        cp020NodeIsPersonEvidence: cp020NodeIsPersonEvidence2,
        cp020NodeIsPredicateEvidence: cp020NodeIsPredicateEvidence2,
        passivePermissiveRelationFallback: passivePermissiveRelationFallback2
      };
    };
  }
});

// src/parser/detectors/locatives/coverb-place.js
var require_coverb_place = __commonJS({
  "src/parser/detectors/locatives/coverb-place.js"(exports2, module2) {
    "use strict";
    module2.exports = function createCoverbPlaceDetectors2(dependencies = {}) {
      const {
        bridgeFramePartClone: bridgeFramePartClone2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function locativeCoverbPhraseFromNodes2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length < 2 || compact.length > 3) return null;
        const [marker, location, postposition] = compact;
        if (!isToken2(marker, "喺") && !nodeCanFillSlot2(marker, "locative_marker")) return null;
        if (!nodeCanFillSlot2(location, "location") && !nodeCanFillSlot2(location, "goal")) return null;
        if (postposition && !isToken2(postposition, "度")) return null;
        const markerChild = bridgeFramePartClone2(marker, {
          label: "func",
          pos: "function",
          syntax: "locative_coverb coverb_marker",
          slots: ["coverb_marker", "locative_marker"],
          reason: "喺 introduces a preverbal locative coverb phrase before the main predicate."
        });
        const children = [markerChild, location];
        if (postposition) children.push(bridgeFramePartClone2(postposition, {
          label: "func",
          pos: "function",
          syntax: "locative_postposition coverb_phrase_postposition",
          slots: ["locative_postposition"],
          reason: "度 closes the locative phrase inside a preverbal coverb frame."
        }));
        return construction2("LocativePlacePhrase", "Location", children, {
          note: "Locative coverb phrase: 喺 + location before a main predicate.",
          slots: templateDerivedSlots2("LocativePlacePhrase", children),
          trace: traceInfo2("generative_template", {
            construction_type: "LocativePlacePhrase",
            template_family: "generative_template",
            template: ["coverb_marker!", "location!", ...postposition ? ["locative_postposition?"] : []],
            assigned_slots: ["coverb_marker", "location", ...postposition ? ["locative_postposition"] : []],
            surfaces: children.map((node) => flattenSurface2(node)),
            subspan: true,
            coverb_subtype: "locative"
          })
        });
      }
      function locativePredicatePhraseFromNodes2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length < 2 || compact.length > 3) return null;
        const [marker, location, postposition] = compact;
        if (!isToken2(marker, "喺") && !nodeCanFillSlot2(marker, "locative_marker")) return null;
        if (!nodeCanFillSlot2(location, "location") && !nodeCanFillSlot2(location, "goal")) return null;
        if (postposition && !isToken2(postposition, "度")) return null;
        const markerChild = bridgeFramePartClone2(marker, {
          label: "func",
          pos: "function",
          syntax: "locative_predicate_marker locative_marker",
          slots: ["locative_marker"],
          reason: "喺 introduces a locative predicate in a subject-led location clause, not a preverbal coverb frame."
        });
        const children = [markerChild, location];
        if (postposition) children.push(bridgeFramePartClone2(postposition, {
          label: "func",
          pos: "function",
          syntax: "locative_postposition",
          slots: ["locative_postposition"],
          reason: "度 closes the locative predicate phrase."
        }));
        return construction2("LocativePlacePhrase", "Location", children, {
          note: "Locative predicate phrase: 喺 + location as the predicate of a subject-led clause.",
          slots: cleanSlots2(["locative_phrase", "locative_predicate", "predicate", "location", "goal", ...templateDerivedSlots2("LocativePlacePhrase", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "LocativePlacePhrase",
            template_family: "generative_template",
            template: ["locative_marker!", "location!", ...postposition ? ["locative_postposition?"] : []],
            assigned_slots: ["locative_marker", "location", ...postposition ? ["locative_postposition"] : []],
            surfaces: children.map((node) => flattenSurface2(node)),
            subspan: true,
            predicate_subtype: "locative",
            not_claims: ["not_coverb_frame_without_following_predicate"]
          })
        });
      }
      function subjectLocativePredicateClauseFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3 || compact.length > 4) return null;
        const subject = compact[0];
        if (!nodeCanFillSlot2(subject, "subject")) return null;
        const locPhrase = locativePredicatePhraseFromNodes2(compact.slice(1));
        if (!locPhrase) return null;
        const children = [subject, locPhrase, ...particles];
        return construction2("SubjectPredicateClause", "SubjPred", children, {
          note: "Subject-led locative predicate clause: subject + 喺 location. This is not a CoverbFrame because there is no following main predicate for the locative phrase to modify.",
          slots: cleanSlots2(["subject_predicate_clause", "subject", "predicate", "clause", "location", "locative_phrase", ...templateDerivedSlots2("SubjectPredicateClause", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            predicate_subtype: "locative",
            template: ["subject!", "locative_predicate!", "particle?"],
            assigned_slots: ["subject", "locative_predicate", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Direct diagnostic review rejected the previous false positive where 我喺屋企 rendered only as LocativePlacePhrase. The subject must be preserved at clause level, and the assigned slot must match the locative_predicate template.",
            not_claims: ["not_coverb_frame", "not_bare_locative_phrase"]
          })
        });
      }
      return {
        locativeCoverbPhraseFromNodes: locativeCoverbPhraseFromNodes2,
        locativePredicatePhraseFromNodes: locativePredicatePhraseFromNodes2,
        subjectLocativePredicateClauseFallback: subjectLocativePredicateClauseFallback2
      };
    };
  }
});

// src/parser/detectors/locatives/posture.js
var require_posture = __commonJS({
  "src/parser/detectors/locatives/posture.js"(exports2, module2) {
    "use strict";
    module2.exports = function createLocativePostureDetectors2(dependencies = {}) {
      const {
        bridgeFramePartClone: bridgeFramePartClone2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function locativePostureVPFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length !== 4 || !nodeCanFillSlot2(compact[0], "subject")) return null;
        const [subject, posture, locative, followingEvent] = compact;
        if (!isToken2(posture, "坐") || !isToken2(locative, "喺度") || !isToken2(followingEvent, "等")) return null;
        const locativeChild = bridgeFramePartClone2(locative, {
          label: "where",
          pos: "location",
          syntax: "locative_deictic locative_posture_complement",
          slots: ["locative_phrase", "location"],
          reason: "After a posture verb, 喺度 supplies the posture location rather than progressive aspect."
        });
        const followingEventChild = bridgeFramePartClone2(followingEvent, {
          label: "doing",
          pos: "verb",
          syntax: "following_event_predicate",
          slots: ["following_event", "action_verb", "predicate", "vp"],
          reason: "The overt following event remains visible and is not reclassified as an object of the posture predicate."
        });
        const vpChildren = [posture, locativeChild, followingEventChild, ...particles];
        const vp = construction2("LocativePostureVP", "PostureLoc", vpChildren, {
          note: "Source-linked posture-location profile: 坐 + 喺度 with the overt following event preserved.",
          slots: cleanSlots2(["locative_posture_vp", "posture_verb", "locative_phrase", "location", "vp", "action_vp", "predicate", ...templateDerivedSlots2("LocativePostureVP", vpChildren)]),
          trace: traceInfo2("generative_template", {
            construction_type: "LocativePostureVP",
            template_family: "generative_template",
            template: ["posture_verb!", "locative_phrase!", "following_event!"],
            assigned_slots: ["posture_verb", "locative_phrase", "following_event", ...particles.map(() => "particle")],
            surfaces: vpChildren.map((node) => flattenSurface2(node)),
            reason: "Retains the exact attested 坐喺度等 sequence without licensing a posture-verb cross-product.",
            not_claims: ["not_progressive_aspect", "not_preverbal_coverb_frame", "not_general_posture_verb_class", "not_following_event_object"]
          })
        });
        const children = [subject, vp];
        return construction2("SubjectPredicateClause", "SubjPred", children, {
          note: "Subject-led locative posture clause preserving the LocativePostureVP predicate child.",
          slots: cleanSlots2(["subject_predicate_clause", "subject", "predicate", "clause", "location", ...templateDerivedSlots2("SubjectPredicateClause", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            predicate_subtype: "locative_posture",
            template: ["subject!", "locative_posture_vp!"],
            assigned_slots: ["subject", "locative_posture_vp"],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Attach the subject without flattening the locative posture predicate."
          })
        });
      }
      return {
        locativePostureVPFallback: locativePostureVPFallback2
      };
    };
  }
});

// src/parser/detectors/coverbs/frames.js
var require_frames = __commonJS({
  "src/parser/detectors/coverbs/frames.js"(exports2, module2) {
    "use strict";
    module2.exports = function createCoverbDetectors2(dependencies = {}) {
      const {
        bridgeFramePartClone: bridgeFramePartClone2,
        bridgeNPFromNodes: bridgeNPFromNodes2,
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        locativeCoverbPhraseFromNodes: locativeCoverbPhraseFromNodes2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        rawNodeHasSlot: rawNodeHasSlot2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function predicateLikeForCoverbFrame(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []).filter((node) => node && node.kind !== "text");
        if (!compact.length) return null;
        const productiveVoPredicate = categorySubspanFor2(compact, ["ProductiveVO"]);
        if (productiveVoPredicate) return [productiveVoPredicate];
        const reduplicatedPredicate = categorySubspanFor2(compact, ["ReduplicatedVP"]);
        if (reduplicatedPredicate) return [reduplicatedPredicate];
        const last = compact[compact.length - 1];
        const lastIsPredicate = rawNodeHasSlot2(last, "predicate") || rawNodeHasSlot2(last, "vp") || rawNodeHasSlot2(last, "action_verb") || rawNodeHasSlot2(last, "main_verb") || rawNodeHasSlot2(last, "movement_verb") || rawNodeHasSlot2(last, "speech_verb");
        if (!lastIsPredicate) return null;
        if (compact.length === 1) return compact;
        if (compact.length === 2 && rawNodeHasSlot2(compact[0], "manner")) return compact;
        return null;
      }
      function instrumentCoverbObjectFromNodes(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length !== 1) return null;
        const object = compact[0];
        if (!nodeCanFillSlot2(object, "object") && !nodeCanFillSlot2(object, "head_noun") && !nodeCanFillSlot2(object, "np")) return null;
        if (object.kind === "token") {
          return bridgeFramePartClone2(object, {
            label: object.label || "what",
            pos: object.features && object.features.pos ? object.features.pos : "noun",
            syntax: `${object.syntax || "object_np"} instrument_np coverb_object`,
            slots: ["coverb_object", "instrument", "object", "np"],
            reason: "The NP after 用 is the visible instrument object of a preverbal coverb phrase, not the main action object."
          });
        }
        return object;
      }
      function comitativeCoverbObject(node) {
        if (!node) return null;
        if (!rawNodeHasSlot2(node, "co_participant") && !rawNodeHasSlot2(node, "np") && !rawNodeHasSlot2(node, "subject")) return null;
        if (node.kind === "token") {
          return bridgeFramePartClone2(node, {
            label: node.label || "who",
            pos: "np",
            syntax: `${node.syntax || "participant_np"} coverb_object co_participant`,
            slots: ["coverb_object", "co_participant", "np"],
            reason: "The NP after 同 is the object of a preverbal coverb phrase. Depending on the following predicate it may be an addressee/interpersonal participant or a true co-participant, not the clause subject."
          });
        }
        return node;
      }
      function nominalModifierClone(node, reason) {
        if (!node || node.kind !== "token") return node;
        if (!isToken2(node, "其他")) return node;
        return parserInactiveTokenClone2(node, {
          label: "func",
          pos: "determiner",
          syntax: "nominal_modifier determiner_like_modifier modifier",
          slots: ["modifier"],
          reason: reason || "其他 is a determiner-like modifier inside a modified NP; the head noun carries the who/what role.",
          role_resolution_note: "其他 modifies the following head noun and should not learner-display as a head/object what role in 其他同事."
        });
      }
      function comitativeCoverbObjectFromNodes(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length === 1) return comitativeCoverbObject(compact[0]);
        if (compact.length === 2 && rawNodeHasSlot2(compact[0], "modifier") && (rawNodeHasSlot2(compact[1], "co_participant") || rawNodeHasSlot2(compact[1], "head_noun") || rawNodeHasSlot2(compact[1], "subject"))) {
          const phraseChildren = [
            nominalModifierClone(compact[0], "其他 modifies 同事 inside the 同 coverb object NP; 同事 carries the participant/head role."),
            compact[1]
          ];
          const phrase = construction2("ModifiedNP", "NP", phraseChildren, {
            primary: "co_participant",
            note: "Modified NP inside an interpersonal/comitative 同 coverb phrase.",
            slots: templateDerivedSlots2("ModifiedNP", phraseChildren),
            trace: traceInfo2("generative_template", {
              construction_type: "ModifiedNP",
              template_family: "generative_template",
              template: ["modifier!", "head_noun!"],
              assigned_slots: ["modifier", "head_noun"],
              surfaces: phraseChildren.map((node) => flattenSurface2(node)),
              subspan: true,
              np_subtype: "modified_co_participant_np",
              reason: "其他 is contextualized as a modifier/determiner-like child of 同事, not as a learner-visible what/object role."
            })
          });
          return phrase;
        }
        return bridgeNPFromNodes2(compact);
      }
      function tongCoverbSubtype(predicateNodes) {
        const surfaces = (predicateNodes || []).map((node) => flattenSurface2(node));
        const hasTogetherMarker = surfaces.some((surface) => surface.includes("一齊"));
        if (hasTogetherMarker) return "comitative";
        const hasSpeechPredicate = (predicateNodes || []).some((node) => rawNodeHasSlot2(node, "speech_verb") || ["講", "話"].includes(flattenSurface2(node)));
        if (hasSpeechPredicate) return "co_participant_or_addressee";
        return "co_participant_or_interpersonal";
      }
      function tongCoverbDoctrineReason(subtype) {
        if (subtype === "comitative") {
          return "同 + co-participant modifies the following predicate; 一齊 supports a true comitative/together-with reading. CoverbFrame remains a learner structural frame and does not settle VP-adjunct versus PredP-adjunct X-bar attachment.";
        }
        if (subtype === "co_participant_or_addressee") {
          return "同 + NP before a speech predicate marks an interpersonal addressee/co-participant relation, not necessarily literal together-with comitative. CoverbFrame remains a learner structural frame and does not settle VP-adjunct versus PredP-adjunct X-bar attachment.";
        }
        return "同 + NP introduces a preverbal interpersonal/co-participant coverb relation to the following predicate. CoverbFrame remains a learner structural frame and does not settle VP-adjunct versus PredP-adjunct X-bar attachment.";
      }
      function coverbFrameFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3 || compact.length > 7) return null;
        let subject = null;
        let markerIndex = 0;
        const preCoverbModifiers = [];
        if (compact.length >= 4 && nodeCanFillSlot2(compact[0], "subject")) {
          subject = compact[0];
          markerIndex = 1;
        }
        while (markerIndex < compact.length) {
          const candidate = compact[markerIndex];
          if (isToken2(candidate, "喺") || isToken2(candidate, "同") || isToken2(candidate, "用")) break;
          const candidateSurface = flattenSurface2(candidate);
          const candidateSlots = nodeSlots2(candidate);
          const modifierLike = ["再", "先", "又", "都"].includes(candidateSurface) || candidateSlots.some((slot) => ["time", "time_head", "manner", "how", "focus_adverb"].includes(slot));
          if (!modifierLike || preCoverbModifiers.length >= 2) break;
          preCoverbModifiers.push(candidate);
          markerIndex += 1;
        }
        const marker = compact[markerIndex];
        if (isToken2(marker, "用")) {
          if (compact.length <= markerIndex + 2) return null;
          const instrument = instrumentCoverbObjectFromNodes(compact.slice(markerIndex + 1, markerIndex + 2));
          if (!instrument) return null;
          const predicateNodes = predicateLikeForCoverbFrame(compact.slice(markerIndex + 2));
          if (!predicateNodes) return null;
          const markerChild = bridgeFramePartClone2(marker, {
            label: "func",
            pos: "function",
            syntax: "instrument_coverb coverb_marker",
            slots: ["coverb_marker", "instrument_marker"],
            reason: "用 introduces a preverbal instrument coverb phrase before the main predicate."
          });
          const children = [...subject ? [subject] : [], ...preCoverbModifiers, markerChild, instrument, ...predicateNodes, ...particles];
          return construction2("CoverbFrame", "Coverb", children, {
            note: "Preverbal instrument coverb frame: optional subject + 用 instrument NP + main predicate. It keeps the instrument object distinct from the main action object.",
            slots: templateDerivedSlots2("CoverbFrame", children),
            trace: traceInfo2("generative_template", {
              construction_type: "CoverbFrame",
              template_family: "generative_template",
              template: [
                ...subject ? ["subject?"] : [],
                ...preCoverbModifiers.map(() => "manner?"),
                "coverb_marker!",
                "coverb_object!",
                ...predicateNodes.map((node, index) => index === predicateNodes.length - 1 ? "predicate!" : "manner?"),
                "particle?"
              ],
              assigned_slots: [...subject ? ["subject"] : [], ...preCoverbModifiers.map(() => "manner"), "coverb_marker", "coverb_object", ...predicateNodes.map((node, index) => index === predicateNodes.length - 1 ? "predicate" : "manner"), ...particles.map(() => "particle")],
              coverb_subtype: "instrument",
              boundary_guardrail: "instrument_object_not_main_action_object",
              surfaces: children.map((node) => flattenSurface2(node)),
              reason: "用 + NP is a preverbal instrument coverb phrase modifying the main predicate; it should not collapse into NominalHeadSpan or treat the instrument NP as the action object.",
              xbar_status: "surface_learner_frame_not_final_xbar_claim",
              compatible_attachment_analyses: ["vp_adjunct", "predp_adjunct"],
              not_claims: ["not_head_np", "not_transitive_vp_object_boundary", "not_full_xbar_tree"]
            })
          });
        }
        if (isToken2(marker, "喺")) {
          const locPhrase = locativeCoverbPhraseFromNodes2(compact.slice(markerIndex, markerIndex + 2));
          if (!locPhrase) return null;
          const predicateNodes = predicateLikeForCoverbFrame(compact.slice(markerIndex + 2));
          if (!predicateNodes) return null;
          const children = [...subject ? [subject] : [], ...preCoverbModifiers, locPhrase, ...predicateNodes, ...particles];
          return construction2("CoverbFrame", "Coverb", children, {
            note: "Preverbal locative coverb frame: optional subject + 喺 location + main predicate. This is a learner structural frame compatible with VP-adjunct or PredP-adjunct analyses, not a full X-bar commitment.",
            slots: templateDerivedSlots2("CoverbFrame", children),
            trace: traceInfo2("generative_template", {
              construction_type: "CoverbFrame",
              template_family: "generative_template",
              template: [
                ...subject ? ["subject?"] : [],
                ...preCoverbModifiers.map(() => "manner?"),
                "coverb_phrase!",
                ...predicateNodes.map((node, index) => index === predicateNodes.length - 1 ? "predicate!" : "manner?"),
                "particle?"
              ],
              assigned_slots: [...subject ? ["subject"] : [], ...preCoverbModifiers.map(() => "manner"), "coverb_phrase", ...predicateNodes.map((node, index) => index === predicateNodes.length - 1 ? "predicate" : "manner"), ...particles.map(() => "particle")],
              coverb_subtype: "locative",
              boundary_guardrail: "preverbal_coverb_not_postverbal_transfer_or_serial_purpose",
              surfaces: children.map((node) => flattenSurface2(node)),
              reason: "喺 + location is a preverbal coverb phrase modifying the main predicate; it should not be collapsed into TransferDitransitiveVP, RecipientFrame, or SerialVerbPurposeChain. This parser frame does not claim to settle VP-adjunct versus PredP-adjunct X-bar attachment or coverb/control-verb edge cases.",
              xbar_status: "surface_learner_frame_not_final_xbar_claim",
              compatible_attachment_analyses: ["vp_adjunct", "predp_adjunct"],
              not_claims: ["not_ordinary_preposition", "not_transfer_ditransitive", "not_serial_purpose_chain", "not_full_xbar_tree"]
            })
          });
        }
        if (isToken2(marker, "同")) {
          if (compact.length <= markerIndex + 2) return null;
          let object = null;
          let predicateNodes = null;
          for (const objectLength of [2, 1]) {
            const objectStart = markerIndex + 1;
            const objectEnd = objectStart + objectLength;
            if (compact.length <= objectEnd) continue;
            const candidateObject = comitativeCoverbObjectFromNodes(compact.slice(objectStart, objectEnd));
            const candidatePredicate = predicateLikeForCoverbFrame(compact.slice(objectEnd));
            if (candidateObject && candidatePredicate) {
              object = candidateObject;
              predicateNodes = candidatePredicate;
              break;
            }
          }
          if (!object || !predicateNodes) return null;
          const coverbSubtype = tongCoverbSubtype(predicateNodes);
          const coverbReason = tongCoverbDoctrineReason(coverbSubtype);
          const markerChild = bridgeFramePartClone2(marker, {
            label: "func",
            pos: "function",
            syntax: coverbSubtype === "comitative" ? "comitative_coverb coverb_marker" : "interpersonal_coverb coverb_marker",
            slots: ["coverb_marker", "comitative_marker"],
            reason: coverbSubtype === "comitative" ? "同 introduces a preverbal comitative coverb phrase before a together-with predicate." : "同 introduces a preverbal interpersonal/addressee coverb phrase before the main predicate."
          });
          const children = [...subject ? [subject] : [], ...preCoverbModifiers, markerChild, object, ...predicateNodes, ...particles];
          return construction2("CoverbFrame", "Coverb", children, {
            note: "Preverbal 同 coverb frame: optional subject + 同 coverb object + main predicate. It may be addressee/interpersonal or truly comitative depending on the predicate, and is distinct from transfer ditransitives and serial-purpose chains.",
            slots: templateDerivedSlots2("CoverbFrame", children),
            trace: traceInfo2("generative_template", {
              construction_type: "CoverbFrame",
              template_family: "generative_template",
              template: [
                ...subject ? ["subject?"] : [],
                ...preCoverbModifiers.map(() => "manner?"),
                "coverb_marker!",
                "coverb_object!",
                ...predicateNodes.map((node, index) => index === predicateNodes.length - 1 ? "predicate!" : "manner?"),
                "particle?"
              ],
              assigned_slots: [...subject ? ["subject"] : [], ...preCoverbModifiers.map(() => "manner"), "coverb_marker", "coverb_object", ...predicateNodes.map((node, index) => index === predicateNodes.length - 1 ? "predicate" : "manner"), ...particles.map(() => "particle")],
              coverb_subtype: coverbSubtype,
              boundary_guardrail: "preverbal_coverb_not_postverbal_transfer_or_serial_purpose",
              surfaces: children.map((node) => flattenSurface2(node)),
              reason: coverbReason,
              xbar_status: "surface_learner_frame_not_final_xbar_claim",
              compatible_attachment_analyses: ["vp_adjunct", "predp_adjunct"],
              not_claims: ["not_ordinary_preposition", "not_transfer_ditransitive", "not_serial_purpose_chain", "not_full_xbar_tree"]
            })
          });
        }
        return null;
      }
      return {
        coverbFrameFallback: coverbFrameFallback2
      };
    };
  }
});

// src/parser/detectors/motion/directional.js
var require_directional = __commonJS({
  "src/parser/detectors/motion/directional.js"(exports2, module2) {
    "use strict";
    module2.exports = function createDirectionalMotionDetectors2(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        compositionPartClone: compositionPartClone2,
        construction: construction2,
        constructionSlotsByType: constructionSlotsByType2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        token: token3,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      const DIRECTIONAL_MOTION_PATTERNS = [
        {
          surfaces: ["返", "上", "嚟"],
          type: "CompoundDirectionalMotionVP",
          label: "MotionVP",
          note: "Compound directional motion VP: 返 + 上 + 嚟 = come back up.",
          pattern: "return + upward_direction + deictic_come"
        },
        {
          surfaces: ["返", "上", "去"],
          type: "CompoundDirectionalMotionVP",
          label: "MotionVP",
          note: "Compound directional motion VP: 返 + 上 + 去 = go back up.",
          pattern: "return + upward_direction + deictic_go"
        },
        {
          surfaces: ["嚟"],
          type: "DirectionalMotionVP",
          label: "MotionVP",
          note: "Independent motion predicate: 嚟 = come.",
          pattern: "independent_deictic_come"
        },
        {
          surfaces: ["去"],
          type: "DirectionalMotionVP",
          label: "MotionVP",
          note: "Independent motion predicate: 去 = go.",
          pattern: "independent_deictic_go"
        }
      ];
      const MULTIPART_DIRECTIONAL_LEFT = /* @__PURE__ */ new Set(["返", "上", "落", "入", "出", "過"]);
      const MULTIPART_DIRECTIONAL_RIGHT = /* @__PURE__ */ new Set(["嚟", "去"]);
      const POSTVERBAL_BLOCKING_SLOTS = ["object", "goal", "vp", "action_vp", "predicate", "action_verb", "main_verb"];
      function significantNeighbor(nodes, index, direction) {
        let cursor = index + direction;
        while (cursor >= 0 && cursor < nodes.length) {
          const node = nodes[cursor];
          if (!(node && node.kind === "text" && /^\s*$/u.test(flattenSurface2(node)))) return node;
          cursor += direction;
        }
        return null;
      }
      function independentMotionPatternAllowedAt(nodes, index, pattern) {
        if (pattern.type !== "DirectionalMotionVP" || pattern.surfaces.length !== 1) return true;
        const surface = pattern.surfaces[0];
        const previous = significantNeighbor(nodes, index, -1);
        const next = significantNeighbor(nodes, index, 1);
        const previousSurface = previous ? flattenSurface2(previous) : "";
        const nextSurface = next ? flattenSurface2(next) : "";
        if (surface === "落" && MULTIPART_DIRECTIONAL_RIGHT.has(nextSurface)) return false;
        if (MULTIPART_DIRECTIONAL_RIGHT.has(surface) && MULTIPART_DIRECTIONAL_LEFT.has(previousSurface)) return false;
        if (previous && POSTVERBAL_BLOCKING_SLOTS.some((slot) => nodeCanFillSlot2(previous, slot))) return false;
        return true;
      }
      function directionalMotionPartClone(node, role) {
        const surface = flattenSurface2(node);
        const syntaxBySurface = {
          "返": "return_motion_component",
          "落": "independent_downward_motion_predicate",
          "上": "movement_direction_up",
          "嚟": "independent_deictic_motion_predicate",
          "去": "independent_deictic_motion_predicate",
          "走": "independent_transition_motion_predicate"
        };
        const slotBySurface = {
          "返": "movement_verb",
          "落": "movement_verb",
          "上": "movement_direction",
          "嚟": "movement_verb",
          "去": "movement_verb",
          "走": "movement_verb"
        };
        return parserInactiveTokenClone2(firstToken2(node) || token3(surface), {
          label: role || "doing",
          pos: role === "func" ? "function" : "verb",
          syntax: syntaxBySurface[surface] || "directional_motion_part",
          slots: [slotBySurface[surface] || "directional_motion_part"],
          reason: "Visible motion material is parser-inactive inside its owned motion construction; the parent exposes the relevant predicate affordance."
        });
      }
      function makeDirectionalMotionVP(nodes, pattern) {
        const children = nodes.map((node) => directionalMotionPartClone(node, "doing"));
        const independent = pattern.type === "DirectionalMotionVP" && pattern.surfaces.length === 1;
        return construction2(pattern.type, pattern.label, children, {
          slots: ["directional_motion_vp", "vp", "action_vp", "predicate", "movement_verb", "motion_predicate"],
          note: pattern.note,
          trace: traceInfo2(independent ? "construction_template" : "generative_or_heuristic_slot_rule", {
            construction_type: pattern.type,
            template_family: independent ? "construction_template" : void 0,
            template: independent ? ["independent_motion_predicate!"] : void 0,
            assigned_slots: independent ? ["movement_verb"] : void 0,
            rule: independent ? "independently predicative single motion/path item" : "Cantonese directional motion compound",
            pattern: pattern.pattern,
            surfaces: pattern.surfaces,
            aa49_scope: independent ? "single_independent_motion_predicate" : void 0,
            not_claims: independent ? ["not_postverbal_directional_complement", "not_compound_directional", "not_manner_directional_sequence"] : void 0,
            reason: independent ? "AA49 represents a single lexical motion/path item in independently predicative position; shared directional vocabulary elsewhere does not inherit this identity." : "The multi-part directional form is represented by its separately controlled construction identity."
          })
        });
      }
      function directionalMotionTemplateFor(nodes, pattern) {
        const templated = categorySubspanFor2(nodes, [pattern.type]);
        if (templated) return templated;
        return makeDirectionalMotionVP(nodes, pattern);
      }
      function negatedDirectionalMotionTemplateFor(negatorNode, vp) {
        const templated = categorySubspanFor2([negatorNode, vp], ["NegatedDirectionalMotionVP"]);
        if (templated) return templated;
        return null;
      }
      function directionalPatternAt(nodes, index) {
        for (const pattern of DIRECTIONAL_MOTION_PATTERNS) {
          if (index + pattern.surfaces.length > nodes.length) continue;
          const window2 = nodes.slice(index, index + pattern.surfaces.length);
          if (window2.some((node) => node && node.kind === "text")) continue;
          const surfaces = window2.map((node) => flattenSurface2(node));
          if (surfaces.every((surface, i) => surface === pattern.surfaces[i]) && independentMotionPatternAllowedAt(nodes, index, pattern)) {
            return { pattern, length: pattern.surfaces.length };
          }
        }
        return null;
      }
      function wrapDirectionalMotionSubspans2(nodes) {
        const result = [];
        let i = 0;
        while (i < nodes.length) {
          if (isToken2(nodes[i], "唔")) {
            const negated = directionalPatternAt(nodes, i + 1);
            if (negated) {
              const vp = directionalMotionTemplateFor(nodes.slice(i + 1, i + 1 + negated.length), negated.pattern);
              const templatedNegatedVp = negatedDirectionalMotionTemplateFor(nodes[i], vp);
              result.push(templatedNegatedVp || construction2("NegatedDirectionalMotionVP", "NegMotionVP", [parserInactiveTokenClone2(nodes[i], {
                label: "func",
                pos: "function",
                syntax: "negator",
                slots: ["negator"],
                reason: "Negator is parser-inactive inside a negated directional-motion VP wrapper."
              }), vp], {
                slots: ["negated_directional_motion_vp", "directional_motion_vp", "vp", "action_vp", "predicate", "movement_verb", "motion_predicate", "negator"],
                note: "Negated independent motion predicate.",
                trace: traceInfo2("construction_template", {
                  construction_type: "NegatedDirectionalMotionVP",
                  template_family: "construction_template",
                  template: ["negator!", "independent_motion_predicate!"],
                  assigned_slots: ["negator", "motion_predicate"],
                  surfaces: [flattenSurface2(nodes[i]), flattenSurface2(vp)],
                  pattern: negated.pattern.pattern,
                  reason: "Negation is an outer wrapper around the narrow independent motion predicate."
                })
              }));
              i += 1 + negated.length;
              continue;
            }
          }
          const match = directionalPatternAt(nodes, i);
          if (match) {
            result.push(directionalMotionTemplateFor(nodes.slice(i, i + match.length), match.pattern));
            i += match.length;
            continue;
          }
          result.push(nodes[i]);
          i += 1;
        }
        return result;
      }
      function independentMotionPredicateNode(sourceNode, profile) {
        const surface = flattenSurface2(sourceNode);
        const movement = parserInactiveTokenClone2(firstToken2(sourceNode) || sourceNode, {
          label: "doing",
          pos: "verb",
          syntax: profile.syntax,
          slots: ["action_verb", "main_verb", "movement_verb", "predicate"],
          jyutping: profile.jyutping || "",
          note: profile.note,
          reason: profile.reason
        });
        return construction2("DirectionalMotionVP", "MotionVP", [movement], {
          slots: ["directional_motion_vp", "vp", "action_vp", "predicate", "movement_verb", "motion_predicate"],
          note: profile.note,
          trace: traceInfo2("construction_template", {
            construction_type: "DirectionalMotionVP",
            template_family: "construction_template",
            template: ["independent_motion_predicate!"],
            assigned_slots: ["movement_verb"],
            surfaces: [surface],
            rule: "independently predicative single motion/path item",
            aa49_scope: "single_independent_motion_predicate",
            contextual_role_resolution: profile.contextual_role_resolution,
            not_claims: ["not_postverbal_directional_complement", "not_compound_directional", "not_manner_directional_sequence"]
          })
        });
      }
      function transitionMotionPredicateFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (particles.length || !bareCore.length) return null;
        let cursor = 0;
        const subjectCandidate = bareCore[cursor];
        const hasFollowingTransition = isToken2(bareCore[cursor + 1], "走");
        const subject = subjectCandidate && (nodeCanFillSlot2(subjectCandidate, "subject") || hasFollowingTransition && nodeCanFillSlot2(subjectCandidate, "np")) ? bareCore[cursor++] : null;
        if (!isToken2(bareCore[cursor], "走")) return null;
        const movementSource = bareCore[cursor++];
        const aspect = cursor < bareCore.length && ["咗", "過", "緊"].includes(flattenSurface2(bareCore[cursor])) ? bareCore[cursor++] : null;
        if (cursor !== bareCore.length || !aspect) return null;
        const aa49 = independentMotionPredicateNode(movementSource, {
          syntax: "intransitive_motion_verb transition_motion_predicate",
          jyutping: "zau2",
          note: "One-word transition motion predicate headed by standalone 走.",
          reason: "At predicate onset, standalone 走 is an independent transition-motion verb rather than a postverbal directional result complement.",
          contextual_role_resolution: "standalone_motion_predicate_not_result_complement"
        });
        const motion = construction2("PerfectiveVP", "PerfectiveVP", [aa49, aspect], {
          slots: ["perfective_vp", "vp", "action_vp", "predicate", "perfective_aspect", "directional_motion_vp", "movement_verb"],
          note: "Aspect-marked transition-motion predicate with a narrow independent-motion child.",
          trace: traceInfo2("generative_template", {
            construction_type: "PerfectiveVP",
            template_family: "generative_template",
            template: ["directional_motion_vp!", "perfective_aspect!"],
            assigned_slots: ["motion_predicate", "perfective_aspect"],
            surfaces: [flattenSurface2(aa49), flattenSurface2(aspect)],
            contextual_role_resolution: "standalone_motion_predicate_not_result_complement",
            subspan: Boolean(subject)
          })
        });
        if (!subject) return motion;
        const children = [subject, motion];
        return construction2("SubjectPredicateClause", "SubjPred", children, {
          slots: templateDerivedSlots2("SubjectPredicateClause", children),
          note: "Subject plus an aspect-marked independent transition-motion predicate.",
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "predicate!"],
            assigned_slots: ["subject", "predicate"],
            surfaces: children.map((node) => flattenSurface2(node)),
            predicate_subtype: "transition_motion"
          })
        });
      }
      function downwardMotionPredicateFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (particles.length || !bareCore.length) return null;
        let cursor = 0;
        const subject = nodeCanFillSlot2(bareCore[cursor], "subject") ? bareCore[cursor++] : null;
        if (!isToken2(bareCore[cursor], "落")) return null;
        const movementSource = bareCore[cursor++];
        if (!isToken2(bareCore[cursor], "咗")) return null;
        const aspect = bareCore[cursor++];
        const goal = bareCore[cursor++];
        if (!goal || !nodeCanFillSlot2(goal, "location") && !nodeCanFillSlot2(goal, "goal")) return null;
        if (cursor !== bareCore.length) return null;
        const aa49 = independentMotionPredicateNode(movementSource, {
          syntax: "independent_downward_motion_predicate movement_verb",
          jyutping: "lok6",
          note: "Independent downward motion predicate headed by 落.",
          reason: "Before perfective aspect plus an overt location, 落 heads the motion event; environmental 落雨 and compound 落嚟 remain separately owned.",
          contextual_role_resolution: "independent_downward_predicate_before_aspect_and_goal"
        });
        const children = [aa49, aspect, goal];
        const motion = construction2("MotionGoalVP", "MotionGoal", children, {
          slots: ["motion_goal_vp", "directional_motion_vp", "movement_verb", "goal", "location", "perfective_aspect", "predicate", "vp", "action_vp"],
          note: "Perfective downward motion to an overt location, with AA49 restricted to the lexical predicate 落.",
          trace: traceInfo2("generative_template", {
            construction_type: "MotionGoalVP",
            template_family: "generative_template",
            template: ["directional_motion_vp!", "perfective_aspect!", "goal!"],
            assigned_slots: ["motion_predicate", "perfective_aspect", "goal"],
            surfaces: children.map((node) => flattenSurface2(node)),
            constraints: { slot_must_not_have_slots: { goal: ["location_question", "wh_nominal"] } },
            aa49_child_scope: "single_independent_motion_predicate"
          })
        });
        if (!subject) return motion;
        const clauseChildren = [subject, motion];
        return construction2("SubjectPredicateClause", "SubjPred", clauseChildren, {
          slots: templateDerivedSlots2("SubjectPredicateClause", clauseChildren),
          note: "Subject plus perfective downward motion to an overt location.",
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "predicate!"],
            assigned_slots: ["subject", "predicate"],
            surfaces: clauseChildren.map((node) => flattenSurface2(node)),
            predicate_subtype: "downward_motion_goal"
          })
        });
      }
      function directionalComplexPart(node, semanticSlot) {
        const surface = flattenSurface2(node);
        const syntax = {
          "行": "main_motion_verb",
          "攞": "caused_motion_action",
          "入": "movement_direction_in",
          "出": "movement_direction_out",
          "返": "return_directional_complement",
          "過": "path_across_component",
          "嚟": "deictic_motion_marker_toward",
          "去": "deictic_motion_marker_away",
          "緊": "progressive_aspect",
          "咗": "perfective_aspect",
          "得": "potential_marker",
          "唔": "potential_negator"
        }[surface] || "directional_composition_part";
        const label = ["緊", "咗", "得", "唔"].includes(surface) ? "func" : "doing";
        return compositionPartClone2(node, { label, syntax, slots: [semanticSlot], reason: `${surface} is overt and owned by the directional complex in source order.` });
      }
      function directionalCompositionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        const surfaces = compact.map(flattenSurface2);
        const finish = (type, label, nodes, detail = {}) => construction2(type, label, [...nodes, ...particles], {
          slots: constructionSlotsByType2(type, nodes),
          note: "Transparent Cantonese directional composition with path, potential/aspect, and final deixis represented in one predicate.",
          trace: traceInfo2("generative_template", {
            construction_type: type,
            template_family: "generative_template",
            template: detail.template || [],
            assigned_slots: detail.assigned_slots || [],
            surfaces: nodes.map(flattenSurface2),
            directional_subtype: detail.directional_subtype || "",
            deictic_position_status: "outermost_visible_deictic",
            not_claims: ["not_hidden_path", "not_hidden_subject", "not_reordered_surface"]
          })
        });
        if (surfaces.length === 3 && nodeCanFillSlot2(compact[0], "action_verb") && surfaces[1] === "返" && ["嚟", "去"].includes(surfaces[2])) {
          const children = [
            directionalComplexPart(compact[0], "main_verb"),
            directionalComplexPart(compact[1], "verb_complement"),
            directionalComplexPart(compact[2], "deictic_motion_marker")
          ];
          return construction2("VerbComplementVP", "VerbCompVP", [...children, ...particles], {
            slots: ["verb_complement_vp", "verb_complement", "vp", "action_vp", "predicate", "main_verb", "return_motion_verb", "movement_direction", "deictic_motion_marker"],
            note: "Action predicate plus overt return-direction complement; directional material is not AA49.",
            trace: traceInfo2("generative_template", {
              construction_type: "VerbComplementVP",
              template_family: "generative_template",
              template: ["action_verb!", "return_directional_complement!", "deictic_motion_marker!", "particle?"],
              assigned_slots: ["main_verb", "verb_complement", "deictic_motion_marker", ...particles.map(() => "particle")],
              surfaces: children.map(flattenSurface2),
              directional_subtype: "postverbal_return_direction",
              not_claims: ["not_aa49_independent_motion_predicate"]
            })
          });
        }
        if (surfaces.length === 3 && surfaces[0] === "行" && ["入", "出", "上", "落"].includes(surfaces[1]) && ["嚟", "去"].includes(surfaces[2])) {
          return finish("DirectedMannerMotionVP", "DirectedMotion", [directionalComplexPart(compact[0], "movement_verb"), directionalComplexPart(compact[1], "movement_direction"), directionalComplexPart(compact[2], "deictic_motion_marker")], { template: ["manner_motion_verb!", "movement_direction!", "deictic_motion_marker!"], assigned_slots: ["movement_verb", "movement_direction", "deictic_motion_marker"], directional_subtype: "self_motion_path_deictic" });
        }
        if (surfaces.join("") === "行返過嚟") {
          return finish("DirectedMannerMotionVP", "DirectedMotion", [
            directionalComplexPart(compact[0], "movement_verb"),
            directionalComplexPart(compact[1], "return_motion_verb"),
            directionalComplexPart(compact[2], "path_component"),
            directionalComplexPart(compact[3], "deictic_motion_marker")
          ], {
            template: ["manner_motion_verb!", "return_motion_verb!", "path_component!", "deictic_motion_marker!"],
            assigned_slots: ["movement_verb", "return_motion_verb", "path_component", "deictic_motion_marker"],
            directional_subtype: "self_motion_complex_path_deictic"
          });
        }
        if (surfaces.length === 3 && ["入", "落", "上", "出"].includes(surfaces[0]) && surfaces[1] === "咗" && ["嚟", "去"].includes(surfaces[2])) {
          return finish("PerfectiveDirectionalVP", "PerfMotion", [directionalComplexPart(compact[0], "movement_direction"), directionalComplexPart(compact[1], "perfective_aspect"), directionalComplexPart(compact[2], "deictic_motion_marker")], { template: ["directional_head!", "perfective_aspect!", "deictic_motion_marker!"], assigned_slots: ["movement_direction", "perfective_aspect", "deictic_motion_marker"], directional_subtype: "perfective_directional" });
        }
        if (surfaces.length === 4 && surfaces[0] === "行" && surfaces[1] === "緊" && ["入", "出"].includes(surfaces[2]) && ["嚟", "去"].includes(surfaces[3])) {
          return finish("ProgressiveDirectionalVP", "ProgMotion", [directionalComplexPart(compact[0], "movement_verb"), directionalComplexPart(compact[1], "progressive_aspect"), directionalComplexPart(compact[2], "movement_direction"), directionalComplexPart(compact[3], "deictic_motion_marker")], { template: ["movement_verb!", "progressive_aspect!", "movement_direction!", "deictic_motion_marker!"], assigned_slots: ["movement_verb", "progressive_aspect", "movement_direction", "deictic_motion_marker"], directional_subtype: "progressive_path_deictic" });
        }
        if (surfaces.length === 4 && nodeCanFillSlot2(compact[0], "action_verb") && ["得", "唔"].includes(surfaces[1]) && ["入", "出", "返"].includes(surfaces[2]) && ["嚟", "去"].includes(surfaces[3])) {
          const positive = surfaces[1] === "得";
          const type = positive ? "PotentialDirectionalVP" : "NegativePotentialDirectionalVP";
          return finish(type, positive ? "PotentialMotion" : "NegPotentialMotion", [directionalComplexPart(compact[0], surfaces[0] === "行" ? "movement_verb" : "action_verb"), directionalComplexPart(compact[1], positive ? "potential_marker" : "negator"), directionalComplexPart(compact[2], surfaces[2] === "返" ? "return_motion_verb" : "movement_direction"), directionalComplexPart(compact[3], "deictic_motion_marker")], { template: ["action_or_motion_verb!", positive ? "potential_marker!" : "potential_negator!", "directional_complement!", "deictic_motion_marker!"], assigned_slots: [surfaces[0] === "行" ? "movement_verb" : "action_verb", positive ? "potential_marker" : "negator", surfaces[2] === "返" ? "return_motion_verb" : "movement_direction", "deictic_motion_marker"], directional_subtype: positive ? "positive_potential_directional" : "negative_potential_directional" });
        }
        return null;
      }
      return {
        directionalCompositionFallback: directionalCompositionFallback2,
        downwardMotionPredicateFallback: downwardMotionPredicateFallback2,
        transitionMotionPredicateFallback: transitionMotionPredicateFallback2,
        wrapDirectionalMotionSubspans: wrapDirectionalMotionSubspans2
      };
    };
  }
});

// src/parser/detectors/motion/purpose-chains.js
var require_purpose_chains = __commonJS({
  "src/parser/detectors/motion/purpose-chains.js"(exports2, module2) {
    "use strict";
    module2.exports = function createMotionPurposeDetectors2(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        compositionPartClone: compositionPartClone2,
        construction: construction2,
        constructionSlotsByType: constructionSlotsByType2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isParticle: isParticle2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        token: token3,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      const SERIAL_PURPOSE_ACTION_VO_SURFACES = /* @__PURE__ */ new Set(["摘芒果", "買嘢", "食飯"]);
      const SERIAL_PURPOSE_ACTIONS_THAT_TAKE_EATING_PURPOSE = /* @__PURE__ */ new Set(["摘芒果", "買嘢"]);
      function isSerialPurposeActionVo(node) {
        return node && node.kind === "construction" && node.type === "ProductiveVO" && SERIAL_PURPOSE_ACTION_VO_SURFACES.has(flattenSurface2(node));
      }
      function actionVoCanTakeEatingPurpose(node) {
        return node && SERIAL_PURPOSE_ACTIONS_THAT_TAKE_EATING_PURPOSE.has(flattenSurface2(node));
      }
      function isEatingPurposeVerb(node) {
        return isToken2(node, "食");
      }
      function isMotionPurposeCandidate(node) {
        if (!node || node.kind === "text") return false;
        const slots = nodeSlots2(node);
        return slots.includes("directional_motion_vp") || slots.includes("negated_directional_motion_vp") || slots.includes("motion_predicate");
      }
      function serialPurposeVerbClone(node) {
        return parserInactiveTokenClone2(firstToken2(node) || token3(flattenSurface2(node)), {
          label: "doing",
          pos: "verb",
          syntax: "purpose_verb",
          slots: ["purpose_verb", "action_verb", "predicate"],
          reason: "食 is interpreted here as a purpose verb after a reviewed action/object VP, so it stays parser-inactive while the parent exposes the serial-purpose chain."
        });
      }
      function serialPurposeParticleClone(node) {
        return parserInactiveTokenClone2(firstToken2(node) || token3(flattenSurface2(node)), {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle",
          slots: ["particle"],
          reason: "Final particle stays parser-inactive inside a motion/serial purpose-chain wrapper."
        });
      }
      function serialChainKindForMatch(match) {
        return match && match.motion && !match.purpose ? "MotionPurposeChain" : "SerialVerbPurposeChain";
      }
      function serialChainLabelForKind(kind) {
        return kind === "MotionPurposeChain" ? "MotionPurpose" : "PurposeChain";
      }
      function serialChainSlotsForKind(kind) {
        return kind === "MotionPurposeChain" ? ["motion_purpose_chain", "motion_action_chain", "purpose_chain", "vp", "action_vp", "predicate"] : ["serial_verb_purpose_chain", "serial_action_chain", "purpose_chain", "vp", "action_vp", "predicate"];
      }
      function makeSerialVerbPurposeChain(match) {
        const children = [];
        if (match.motion) children.push(match.motion);
        children.push(match.action);
        if (match.purpose) children.push(serialPurposeVerbClone(match.purpose));
        if (match.particle) children.push(serialPurposeParticleClone(match.particle));
        const kind = serialChainKindForMatch(match);
        const isMotionOnly = kind === "MotionPurposeChain";
        return construction2(kind, serialChainLabelForKind(kind), children, {
          slots: serialChainSlotsForKind(kind),
          note: isMotionOnly ? "Motion-purpose chain: directional motion plus a reviewed action VP, such as 返嚟食飯 or 落嚟摘芒果." : "Serial verb / purpose chain: reviewed action sequence with an explicit purpose 食, such as 摘芒果食, 落嚟摘芒果食, or 買嘢食.",
          trace: traceInfo2("generative_template", {
            template_family: "generative_template",
            construction_type: kind,
            template: match.motion ? match.purpose ? ["directional_motion_vp!", "productive_vo!", "purpose_verb!", "particle?"] : ["directional_motion_vp!", "productive_vo!", "particle?"] : ["productive_vo!", "purpose_verb!", "particle?"],
            assigned_slots: [
              ...match.motion ? ["directional_motion_vp"] : [],
              "productive_vo",
              ...match.purpose ? ["purpose_verb"] : [],
              ...match.particle ? ["particle"] : []
            ],
            rule: isMotionOnly ? "directional_motion_vp + productive_vo + optional final particle" : "directional_motion? + productive_vo + purpose_verb + optional final particle",
            pattern: match.motion ? match.purpose ? "directional_motion_vp + productive_vo + purpose_verb" : "directional_motion_vp + productive_vo" : "productive_vo + purpose_verb",
            reason: isMotionOnly ? "Native speech can use motion plus an action VP as a purpose/action chain. This now uses the generative template transition lane while keeping the learner display precise." : "Native speech often chains an action/object VP with an explicit purpose verb. Keep the ProductiveVO transparent and keep the purpose verb parser-inactive inside the parent purpose-chain wrapper.",
            surfaces: children.map((node) => flattenSurface2(node))
          })
        });
      }
      function serialVerbPurposeChainWithTrailingParticle2(node, particleNode) {
        if (!node || node.kind !== "construction" || !["SerialVerbPurposeChain", "MotionPurposeChain"].includes(node.type) || !particleNode) return node;
        if ((node.children || []).some((child) => isParticle2(child))) return node;
        return {
          ...node,
          children: [...node.children || [], serialPurposeParticleClone(particleNode)],
          trace: {
            ...node.trace || {},
            attached_trailing_particle: flattenSurface2(particleNode)
          }
        };
      }
      function serialPurposeParticleAt(nodes, index) {
        return isParticle2(nodes[index]) && ["呀", "啊", "啦", "喇"].includes(flattenSurface2(nodes[index])) ? nodes[index] : null;
      }
      function serialVerbPurposePatternAt(nodes, index) {
        let motion = null;
        let i = index;
        if (isMotionPurposeCandidate(nodes[i]) && isSerialPurposeActionVo(nodes[i + 1])) {
          motion = nodes[i];
          i += 1;
        }
        if (!isSerialPurposeActionVo(nodes[i])) return null;
        let purpose = null;
        let particleIndex = i + 1;
        if (actionVoCanTakeEatingPurpose(nodes[i]) && isEatingPurposeVerb(nodes[i + 1])) {
          purpose = nodes[i + 1];
          particleIndex = i + 2;
        }
        if (!motion && !purpose) return null;
        const particle = serialPurposeParticleAt(nodes, particleIndex);
        return {
          length: (motion ? 1 : 0) + 1 + (purpose ? 1 : 0) + (particle ? 1 : 0),
          motion,
          action: nodes[i],
          purpose,
          particle
        };
      }
      function serialPurposeTemplateSubspanAt(nodes, index) {
        const allowedTypes = ["SerialVerbPurposeChain", "MotionPurposeChain"];
        const remaining = nodes.length - index;
        for (let length = Math.min(4, remaining); length >= 2; length -= 1) {
          const window2 = nodes.slice(index, index + length);
          if (window2.some((node) => node.kind === "text")) continue;
          const candidate = categorySubspanFor2(window2, allowedTypes);
          if (candidate) return { node: candidate, length };
        }
        return null;
      }
      function wrapSerialPurposeTemplateSubspans2(nodes) {
        const result = [];
        let i = 0;
        while (i < nodes.length) {
          const match = serialPurposeTemplateSubspanAt(nodes, i);
          if (match) {
            result.push(match.node);
            i += match.length;
            continue;
          }
          result.push(nodes[i]);
          i += 1;
        }
        return result;
      }
      function wrapSerialVerbPurposeSubspans2(nodes) {
        const result = [];
        let i = 0;
        while (i < nodes.length) {
          const match = serialVerbPurposePatternAt(nodes, i);
          if (match) {
            result.push(makeSerialVerbPurposeChain(match));
            i += match.length;
            continue;
          }
          result.push(nodes[i]);
          i += 1;
        }
        return result;
      }
      function purposeLinkingMotionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length !== 5 || !nodeCanFillSlot2(compact[0], "action_verb") || !["嚟", "去"].includes(flattenSurface2(compact[3])) || !nodeCanFillSlot2(compact[4], "action_verb")) return null;
        const object = categorySubspanFor2(compact.slice(1, 3), ["DiMarkedNP", "ClassifierObjectNP", "ModifiedNP", "NominalHeadSpan"]);
        if (!object) return null;
        const action = categorySubspanFor2([compact[0], object], ["ProductiveVO", "TransitiveVP"]) || construction2("ProductiveVO", "VO", [compact[0], object], { slots: ["productive_vo", "vp", "action_vp", "predicate", "object"], trace: traceInfo2("generative_template", { construction_type: "ProductiveVO", template_family: "generative_template", template: ["action_verb!", "object!"], assigned_slots: ["action_verb", "object"], surfaces: [flattenSurface2(compact[0]), flattenSurface2(object)] }) });
        const linker = compositionPartClone2(compact[3], { label: "func", syntax: "purpose_linking_motion_element", slots: ["purpose_linker"], reason: `${flattenSurface2(compact[3])} links the overt acquisition/action event to the following purpose predicate.` });
        const purpose = compositionPartClone2(compact[4], { label: "doing", syntax: "purpose_verb", slots: ["purpose_verb", "action_verb", "predicate"] });
        const children = [action, linker, purpose, ...particles];
        return construction2("SerialVerbPurposeChain", "PurposeChain", children, {
          slots: constructionSlotsByType2("SerialVerbPurposeChain", children),
          trace: traceInfo2("generative_template", { construction_type: "SerialVerbPurposeChain", template_family: "generative_template", template: ["action_object_vp!", "purpose_linking_motion_element!", "purpose_verb!", "particle?"], assigned_slots: ["productive_vo", "purpose_linker", "purpose_verb", ...particles.map(() => "particle")], surfaces: children.map(flattenSurface2), purpose_linker_surface: flattenSurface2(compact[3]), not_claims: ["not_deictic_motion_vp", "not_hidden_object", "not_hidden_purpose"] })
        });
      }
      return {
        purposeLinkingMotionFallback: purposeLinkingMotionFallback2,
        serialVerbPurposeChainWithTrailingParticle: serialVerbPurposeChainWithTrailingParticle2,
        wrapSerialPurposeTemplateSubspans: wrapSerialPurposeTemplateSubspans2,
        wrapSerialVerbPurposeSubspans: wrapSerialVerbPurposeSubspans2
      };
    };
  }
});

// src/parser/detectors/motion/ordering-review.js
var require_ordering_review = __commonJS({
  "src/parser/detectors/motion/ordering-review.js"(exports2, module2) {
    "use strict";
    module2.exports = function createMotionOrderingReviewDetectors2(dependencies = {}) {
      const { cleanSlots: cleanSlots2, construction: construction2, flattenSurface: flattenSurface2, traceInfo: traceInfo2 } = dependencies;
      function motionOrderingReviewCandidate2(children, subtype, problem, expectedRepairs = [], options = {}) {
        const family = options.family || "motion_event_ordering_or_attachment";
        const reviewFlag = options.review_flag || "motion_event_order_or_attachment_review";
        const missingSlot = options.missing_slot || "licensed_motion_event_order_or_attachment";
        const note = options.note || "Review-bearing motion-event ordering or attachment candidate. Visible learner text is preserved and no repair is inserted.";
        const notClaims = options.not_claims || ["not_hard_asterisk_judgment", "not_silent_reordering", "not_hidden_motion_component"];
        return construction2("MalformedCandidate", "Malformed", children, {
          slots: cleanSlots2(["malformed_candidate", "needs_review", "predicate", "problem_span"]),
          note,
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "MalformedCandidate",
            malformed_family: family,
            malformed_subtype: subtype,
            surfaces: children.map(flattenSurface2),
            problem,
            expected_repairs: expectedRepairs,
            semantic_review_flags: ["malformed_candidate_parse", reviewFlag],
            context_requirement_status: "context_required",
            missing_argument_slots: [missingSlot],
            not_claims: notClaims
          })
        });
      }
      return { motionOrderingReviewCandidate: motionOrderingReviewCandidate2 };
    };
  }
});

// src/parser/detectors/motion/path-goal-source.js
var require_path_goal_source = __commonJS({
  "src/parser/detectors/motion/path-goal-source.js"(exports2, module2) {
    "use strict";
    module2.exports = function createMotionPathGoalSourceDetectors2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        categorySubspanFor: categorySubspanFor2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        constructionSlotsByType: constructionSlotsByType2,
        CP021B_POST_THEME_PREDICATE_PROFILES: CP021B_POST_THEME_PREDICATE_PROFILES2,
        cp021bMakePostThemeRelation: cp021bMakePostThemeRelation2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        motionOrderingReviewCandidate: motionOrderingReviewCandidate2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSurfaceMatches: nodeSurfaceMatches2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        token: token3,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function pathMarkerClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "func",
          pos: "function",
          syntax: "path_coverb path_marker",
          slots: ["path_marker"],
          reason: "沿住 is interpreted as the path marker inside a bounded polite path imperative."
        });
      }
      function pathPhraseFromParts2(marker, path) {
        const children = [pathMarkerClone(marker), path];
        return construction2("PathPhrase", "Path", children, {
          note: "Path phrase: 沿住 + path/location.",
          slots: templateDerivedSlots2("PathPhrase", children),
          trace: traceInfo2("generative_template", {
            construction_type: "PathPhrase",
            template: ["path_marker!", "location!"],
            assigned_slots: ["path_marker", "location"],
            surfaces: children.map((node) => flattenSurface2(node)),
            subspan: true
          })
        });
      }
      function sourceMotionClauseFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const offset = bareCore.length >= 4 && nodeCanFillSlot2(bareCore[0], "subject") ? 1 : 0;
        if (bareCore.length - offset < 3) return null;
        const sourceMarker = bareCore[offset];
        const sourceLocation = bareCore[offset + 1];
        const motionCore = bareCore.slice(offset + 2);
        if (!nodeSurfaceMatches2(sourceMarker, ["由"]) || !nodeCanFillSlot2(sourceLocation, "location")) return null;
        const treeCanFillSlot = (node, slot) => {
          if (!node) return false;
          if (nodeCanFillSlot2(node, slot)) return true;
          return node.kind === "construction" && (node.children || []).some((child) => treeCanFillSlot(child, slot));
        };
        const wrappedMotion = applyConstructionPatterns2(motionCore);
        if (wrappedMotion.length !== 1) return null;
        let motionNode = wrappedMotion[0];
        if (motionNode.kind === "construction" && motionNode.type === "ProductiveVO" && flattenSurface2(motionNode) === "返學" && (motionNode.children || []).length === 2) {
          const [movement, destination] = motionNode.children;
          const motionChildren = [
            motionEventPartClone(movement, {
              label: "doing",
              syntax: "conventional_motion_goal_verb",
              slots: ["movement_verb"],
              reason: "返 is the overt motion predicate in the conventional destination expression 返學."
            }),
            motionEventPartClone(destination, {
              label: "where",
              syntax: "conventional_institutional_destination schooling_destination",
              slots: ["goal", "location", "np", "head_noun"],
              reason: "學 denotes the overt conventional schooling destination/domain after motion 返 in this source-motion frame; no hidden 學校 token is inserted."
            })
          ];
          motionNode = construction2("MotionGoalVP", "MotionGoal", motionChildren, {
            slots: constructionSlotsByType2("MotionGoalVP", motionChildren),
            trace: traceInfo2("generative_template", {
              construction_type: "MotionGoalVP",
              template_family: "generative_template",
              template: ["movement_verb!", "conventional_institutional_destination!"],
              assigned_slots: ["movement_verb", "goal"],
              surfaces: motionChildren.map(flattenSurface2),
              motion_goal_subtype: "conventional_institutional_destination",
              not_claims: ["not_productive_transitive_object", "not_hidden_school_location", "not_lexicalized_whole_clause"]
            })
          });
        }
        if (!treeCanFillSlot(motionNode, "directional_motion_vp") && !treeCanFillSlot(motionNode, "movement_verb")) return null;
        const sourceChild = parserInactiveTokenClone2(sourceMarker, {
          label: "func",
          pos: "function",
          syntax: "source_coverb source_marker",
          slots: ["source_marker", "coverb_marker"],
          reason: "由 introduces the source location of the motion event."
        });
        const locationChild = parserInactiveTokenClone2(sourceLocation, {
          label: "where",
          pos: "location",
          syntax: `${sourceLocation.syntax || "place_np"} source_location`,
          slots: ["source", "location", "np"],
          reason: "Place NP interpreted as the source of motion after 由."
        });
        const children = [...bareCore.slice(0, offset), sourceChild, locationChild, motionNode, ...particles];
        return construction2("SourceMotionClause", "SourceMotion", children, {
          note: "Source-motion clause: optional subject + 由 + source location + motion predicate.",
          slots: cleanSlots2(["source_motion_clause", "source", "source_marker", "location", "movement_verb", "directional_motion_vp", "vp", "predicate", "clause", offset ? "subject" : "", ...templateDerivedSlots2("SourceMotionClause", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "SourceMotionClause",
            template_family: "generative_template",
            template: ["subject?", "source_marker!", "source_location!", "motion_predicate!", "particle?"],
            assigned_slots: [...bareCore.slice(0, offset).map(() => "subject"), "source_marker", "source_location", "motion_predicate", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "The source coverb phrase is attached to the motion predicate so the full clause span remains visible and the location keeps its where role."
          })
        });
      }
      function motionEventPartClone(node, { label = "doing", syntax = "motion_event_part", slots = [], reason = "Visible motion-event material is assigned by its position and event-semantic role." } = {}) {
        const surface = flattenSurface2(node);
        const base = firstToken2(node) || token3(surface, { jyutping: "" });
        return parserInactiveTokenClone2(base, {
          label,
          pos: label === "where" ? "location" : label === "when" ? "time" : label === "func" ? "function" : "verb",
          syntax,
          slots,
          reason
        });
      }
      function motionSubjectPredicateClause(subject, predicate, particles = [], detail = {}) {
        if (!subject) return predicate;
        const children = [subject, predicate, ...particles];
        return construction2("SubjectPredicateClause", "SubjPred", children, {
          slots: templateDerivedSlots2("SubjectPredicateClause", children),
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "predicate!", "particle?"],
            assigned_slots: ["subject", "predicate", ...particles.map(() => "particle")],
            surfaces: children.map(flattenSurface2),
            ...detail
          })
        });
      }
      function motionGoalNode(node, reason = "The postverbal place is the overt goal of the motion event, not an ordinary object.") {
        return motionEventPartClone(node, {
          label: "where",
          syntax: `${(firstToken2(node) || {}).syntax || "place_or_goal"} motion_goal_location`,
          slots: ["goal", "location", "np", "head_noun"],
          reason
        });
      }
      function motionEventSpatialFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3) return null;
        const surfaces = compact.map(flattenSurface2);
        let cursor = 0;
        const subject = compact[cursor] && nodeCanFillSlot2(compact[cursor], "subject") ? compact[cursor++] : null;
        const rest = compact.slice(cursor);
        const rs = rest.map(flattenSurface2);
        if (subject && rs.length >= 4 && rs[0] === "喺" && nodeCanFillSlot2(rest[1], "location") && nodeCanFillSlot2(rest[2], "time")) {
          return motionOrderingReviewCandidate2(
            [...compact, ...particles],
            "post_locative_time_order",
            "The temporal adjunct follows the low preverbal locative phrase before the main event.",
            ["subject_time_locative_predicate", "separate_discourse_or_native_review"],
            {
              family: "clause_adjunct_ordering_or_attachment",
              review_flag: "clause_adjunct_order_or_attachment_review",
              missing_slot: "licensed_clause_adjunct_order_or_attachment",
              note: "Review-bearing clause-adjunct ordering or attachment candidate. Visible learner text is preserved and no repair is inserted.",
              not_claims: ["not_hard_asterisk_judgment", "not_silent_reordering", "not_hidden_time_or_location"]
            }
          );
        }
        if (subject && rs.length === 3 && rs[0] === "行" && nodeCanFillSlot2(rest[1], "location") && rs[2] === "去") {
          return motionOrderingReviewCandidate2([...compact, ...particles], "goal_before_final_deictic", "The goal NP intervenes between the manner-motion verb and final 去 instead of following the directional predicate.", ["行去_goal", "separate_serial_event"]);
        }
        if (subject && rs.length === 3 && ["返", "行"].includes(rs[0]) && nodeCanFillSlot2(rest[1], "location") && rs[2] === "到") {
          return motionOrderingReviewCandidate2([...compact, ...particles], "goal_before_attainment_complement", "到 follows an already expressed goal NP instead of forming the motion-goal-attainment predicate before the goal.", ["motion_verb_到_goal"]);
        }
        if (subject && rs.length === 3 && rs[0] === "到" && rs[1] === "返" && nodeCanFillSlot2(rest[2], "location")) {
          return motionOrderingReviewCandidate2([...compact, ...particles], "arrival_return_order_conflict", "Main arrival 到 precedes return-motion 返 without a licensed compositional relation.", ["到咗_goal", "返到_goal", "返_goal"]);
        }
        if (subject && rs.length === 5 && ["上", "落", "入", "出"].includes(rs[0]) && nodeCanFillSlot2(rest[1], "action_verb") && ["嚟", "去"].includes(rs[3]) && nodeCanFillSlot2(rest[4], "action_verb")) {
          return motionOrderingReviewCandidate2([...compact, ...particles], "unlicensed_motion_action_deictic_order", "A bare directional head is separated from its deictic element by an action-object event.", ["directional_complex_then_action_purpose", "separate_clause_or_native_review"]);
        }
        if (subject && rs.length === 6 && nodeCanFillSlot2(rest[0], "time") && rs[1] === "由" && nodeCanFillSlot2(rest[2], "location") && rs[3] === "行" && rs[4] === "去" && nodeCanFillSlot2(rest[5], "location")) {
          const directedChildren = [
            motionEventPartClone(rest[3], { label: "doing", syntax: "manner_motion_verb", slots: ["movement_verb", "manner_motion"], reason: "行 supplies the manner of self-motion." }),
            motionEventPartClone(rest[4], { label: "doing", syntax: "directional_path_element", slots: ["movement_direction", "path_component"], reason: "去 forms the postverbal directional component before the overt goal." }),
            motionGoalNode(rest[5])
          ];
          const directed = construction2("DirectedMannerMotionVP", "DirectedMotion", directedChildren, {
            slots: constructionSlotsByType2("DirectedMannerMotionVP", directedChildren),
            trace: traceInfo2("generative_template", { construction_type: "DirectedMannerMotionVP", template_family: "generative_template", template: ["manner_motion_verb!", "directional_element!", "goal!"], assigned_slots: ["movement_verb", "movement_direction", "goal"], surfaces: directedChildren.map(flattenSurface2), not_claims: ["not_purpose_chain", "not_transitive_object"] })
          });
          const sourceMarker = motionEventPartClone(rest[1], { label: "func", syntax: "source_coverb source_marker", slots: ["source_marker", "coverb_marker"], reason: "由 introduces the overt source of the motion event." });
          const source = motionEventPartClone(rest[2], { label: "where", syntax: "source_location", slots: ["source", "location", "np"], reason: "The place after 由 is the source, not the goal." });
          const children = [subject, rest[0], sourceMarker, source, directed, ...particles];
          return construction2("SourceMotionClause", "SourceMotion", children, {
            slots: constructionSlotsByType2("SourceMotionClause", children),
            trace: traceInfo2("generative_template", { construction_type: "SourceMotionClause", template_family: "generative_template", template: ["subject!", "time?", "source_marker!", "source_location!", "directed_motion_vp!", "particle?"], assigned_slots: ["subject", "time", "source_marker", "source_location", "motion_predicate", ...particles.map(() => "particle")], surfaces: children.map(flattenSurface2), spatial_order_status: "time_before_source_before_motion_goal" })
          });
        }
        if (subject && rs.length === 3 && rs[0] === "向" && rs[2] === "行") {
          const orientationSurface = rs[1];
          const orientation = orientationSurface === "前" ? token3("前", { label: "where", pos: "location", jyutping: "cin4", syntax: "orientation_ground path_direction", slots: ["path", "orientation", "location"], note: "forward / front", trace: traceInfo2("construction_internal_parser_inactive_clone", { reason: "前 receives a context-local orientation/path reading after 向; no global nominal lexicon entry is introduced." }) }) : motionEventPartClone(rest[1], { label: "where", syntax: "orientation_ground path_goal", slots: ["path", "orientation", "location"], reason: "The NP after 向 is the orientation/path ground." });
          const pathChildren = [motionEventPartClone(rest[0], { label: "func", syntax: "orientation_coverb path_marker", slots: ["path_marker", "coverb_marker"], reason: "向 introduces a preverbal orientation/path phrase." }), orientation];
          const path = construction2("PathPhrase", "Path", pathChildren, { slots: constructionSlotsByType2("PathPhrase", pathChildren), trace: traceInfo2("generative_template", { construction_type: "PathPhrase", template_family: "generative_template", template: ["path_marker!", "orientation_ground!"], assigned_slots: ["path_marker", "location"], surfaces: pathChildren.map(flattenSurface2), subspan: true }) });
          const motion = motionEventPartClone(rest[2], { label: "doing", syntax: "manner_motion_verb", slots: ["movement_verb", "manner_motion", "predicate"], reason: "行 supplies the manner of motion under the preceding orientation phrase." });
          const predicate = construction2("DirectedMannerMotionVP", "DirectedMotion", [path, motion], { slots: constructionSlotsByType2("DirectedMannerMotionVP", [path, motion]), trace: traceInfo2("generative_template", { construction_type: "DirectedMannerMotionVP", template_family: "generative_template", template: ["path_phrase!", "manner_motion_verb!"], assigned_slots: ["path_phrase", "movement_verb"], surfaces: [flattenSurface2(path), flattenSurface2(motion)], not_claims: ["not_postverbal_goal", "not_purpose_chain"] }) });
          return motionSubjectPredicateClause(subject, predicate, particles);
        }
        if (subject && rs.length === 3 && rs[0] === "行" && rs[1] === "去" && nodeCanFillSlot2(rest[2], "location")) {
          const children = [
            motionEventPartClone(rest[0], { label: "doing", syntax: "manner_motion_verb", slots: ["movement_verb", "manner_motion"], reason: "行 supplies manner of motion." }),
            motionEventPartClone(rest[1], { label: "doing", syntax: "directional_path_element", slots: ["movement_direction", "path_component"], reason: "去 is the postverbal directional element; it is not the purpose verb in this frame." }),
            motionGoalNode(rest[2])
          ];
          const predicate = construction2("DirectedMannerMotionVP", "DirectedMotion", children, { slots: constructionSlotsByType2("DirectedMannerMotionVP", children), trace: traceInfo2("generative_template", { construction_type: "DirectedMannerMotionVP", template_family: "generative_template", template: ["manner_motion_verb!", "directional_element!", "goal!"], assigned_slots: ["movement_verb", "movement_direction", "goal"], surfaces: children.map(flattenSurface2), not_claims: ["not_purpose_chain", "not_transitive_object"] }) });
          return motionSubjectPredicateClause(subject, predicate, particles);
        }
        if (subject && rs.length === 3 && rs[0] === "行" && ["入", "出", "上", "落"].includes(rs[1]) && ["嚟", "去"].includes(rs[2])) {
          const children = [
            motionEventPartClone(rest[0], { label: "doing", syntax: "manner_motion_verb", slots: ["movement_verb", "manner_motion"] }),
            motionEventPartClone(rest[1], { label: "doing", syntax: "movement_direction", slots: ["movement_direction", "path_component"] }),
            motionEventPartClone(rest[2], { label: "doing", syntax: "deictic_motion_marker", slots: ["deictic_motion_marker"] })
          ];
          const predicate = construction2("DirectedMannerMotionVP", "DirectedMotion", children, { slots: constructionSlotsByType2("DirectedMannerMotionVP", children), trace: traceInfo2("generative_template", { construction_type: "DirectedMannerMotionVP", template_family: "generative_template", template: ["manner_motion_verb!", "movement_direction!", "deictic_motion_marker!"], assigned_slots: ["movement_verb", "movement_direction", "deictic_motion_marker"], surfaces: children.map(flattenSurface2), deictic_position_status: "outermost_visible_deictic" }) });
          return motionSubjectPredicateClause(subject, predicate, particles);
        }
        if (subject && rs.length === 3 && ["返", "行"].includes(rs[0]) && rs[1] === "到" && nodeCanFillSlot2(rest[2], "location")) {
          const children = [motionEventPartClone(rest[0], { label: "doing", syntax: "movement_verb", slots: ["movement_verb", "main_verb"] }), motionEventPartClone(rest[1], { label: "func", syntax: "goal_attainment_complement", slots: ["goal_attainment_complement", "result_marker"], reason: "到 marks successful arrival/attainment before the overt goal." }), motionGoalNode(rest[2])];
          const predicate = construction2("GoalAttainmentMotionVP", "GoalAttainment", children, { slots: constructionSlotsByType2("GoalAttainmentMotionVP", children), trace: traceInfo2("generative_template", { construction_type: "GoalAttainmentMotionVP", template_family: "generative_template", template: ["movement_verb!", "goal_attainment_complement!", "goal!"], assigned_slots: ["movement_verb", "result_marker", "goal"], surfaces: children.map(flattenSurface2), attainment_domain: "spatial_goal" }) });
          return motionSubjectPredicateClause(subject, predicate, particles);
        }
        if (subject && rs.length === 3 && rs[0] === "到" && rs[1] === "咗" && nodeCanFillSlot2(rest[2], "location")) {
          const children = [motionEventPartClone(rest[0], { label: "doing", syntax: "arrival_motion_verb", slots: ["movement_verb", "main_verb", "predicate"], reason: "到 is the main arrival verb here, not a postverbal result marker." }), motionEventPartClone(rest[1], { label: "func", syntax: "perfective_aspect", slots: ["perfective_aspect", "aspect_marker"] }), motionGoalNode(rest[2])];
          const predicate = construction2("MotionGoalVP", "MotionGoal", children, { slots: constructionSlotsByType2("MotionGoalVP", children), trace: traceInfo2("generative_template", { construction_type: "MotionGoalVP", template_family: "generative_template", template: ["arrival_motion_verb!", "perfective_aspect!", "goal!"], assigned_slots: ["movement_verb", "perfective_aspect", "goal"], surfaces: children.map(flattenSurface2), arrival_verb_status: "main_verb_not_complement" }) });
          return motionSubjectPredicateClause(subject, predicate, particles);
        }
        if (subject && rs.length === 3 && nodeCanFillSlot2(rest[0], "action_verb") && rs[1] === "到" && nodeCanFillSlot2(rest[2], "object") && !nodeCanFillSlot2(rest[2], "location")) {
          const children = [rest[0], motionEventPartClone(rest[1], { label: "func", syntax: "nonspatial_attainment_result_complement", slots: ["result_complement", "result_marker"], reason: "到 is a nonspatial attainment/result complement licensed by the action and object." }), rest[2]];
          const predicate = construction2("ResultComplementVP", "ResultVP", children, { slots: constructionSlotsByType2("ResultComplementVP", children), trace: traceInfo2("generative_template", { construction_type: "ResultComplementVP", template_family: "generative_template", template: ["action_verb!", "attainment_result_complement!", "object!"], assigned_slots: ["action_verb", "result_complement", "object"], surfaces: children.map(flattenSurface2), attainment_domain: "nonspatial", not_claims: ["not_motion_goal", "not_coverb"] }) });
          return motionSubjectPredicateClause(subject, predicate, particles);
        }
        if (subject && rs.length === 4 && nodeCanFillSlot2(rest[0], "action_verb") && rs[1] === "唔" && rs[2] === "到" && nodeCanFillSlot2(rest[3], "object") && !nodeCanFillSlot2(rest[3], "location")) {
          const children = [rest[0], motionEventPartClone(rest[1], { label: "func", syntax: "potential_negator", slots: ["negator"] }), motionEventPartClone(rest[2], { label: "func", syntax: "nonspatial_attainment_result_complement", slots: ["result_complement", "result_marker"] }), rest[3]];
          const predicate = construction2("NegativePotentialComplement", "NegPotential", children, { slots: constructionSlotsByType2("NegativePotentialComplement", children), trace: traceInfo2("generative_template", { construction_type: "NegativePotentialComplement", template_family: "generative_template", template: ["action_verb!", "potential_negator!", "attainment_result_complement!", "object!"], assigned_slots: ["action_verb", "negator", "result_complement", "object"], surfaces: children.map(flattenSurface2), attainment_domain: "nonspatial", not_claims: ["not_motion_goal", "not_hidden_result"] }) });
          return motionSubjectPredicateClause(subject, predicate, particles);
        }
        if (subject && rs.length === 4 && rs[0] === "去" && (nodeCanFillSlot2(rest[1], "location") || /restaurant_np|place_or_goal/.test((firstToken2(rest[1]) || {}).syntax || "")) && nodeCanFillSlot2(rest[2], "action_verb") && nodeCanFillSlot2(rest[3], "object")) {
          const motionChildren = [motionEventPartClone(rest[0], { label: "doing", syntax: "movement_verb", slots: ["movement_verb"] }), motionGoalNode(rest[1])];
          const motion = construction2("MotionGoalVP", "MotionGoal", motionChildren, { slots: constructionSlotsByType2("MotionGoalVP", motionChildren), trace: traceInfo2("generative_template", { construction_type: "MotionGoalVP", template_family: "generative_template", template: ["movement_verb!", "goal!"], assigned_slots: ["movement_verb", "goal"], surfaces: motionChildren.map(flattenSurface2), subspan: true }) });
          const purposeChildren = [rest[2], rest[3]];
          const purpose = construction2("TransitiveVP", "VP", purposeChildren, { slots: constructionSlotsByType2("TransitiveVP", purposeChildren), trace: traceInfo2("generative_template", { construction_type: "TransitiveVP", template_family: "generative_template", template: ["action_verb!", "object!"], assigned_slots: ["action_verb", "object"], surfaces: purposeChildren.map(flattenSurface2), subspan: true }) });
          const chainChildren = [motion, purpose];
          const chain = construction2("MotionPurposeChain", "MotionPurpose", chainChildren, { slots: constructionSlotsByType2("MotionPurposeChain", chainChildren), trace: traceInfo2("generative_template", { construction_type: "MotionPurposeChain", template_family: "generative_template", template: ["motion_goal_vp!", "purpose_vp!"], assigned_slots: ["motion_goal_vp", "purpose_verb"], surfaces: chainChildren.map(flattenSurface2), shared_subject_provenance: { overt_subject_surface: flattenSurface2(subject), licensed_members: [flattenSurface2(motion), flattenSurface2(purpose)], hidden_subject_inserted: false } }) });
          return motionSubjectPredicateClause(subject, chain, particles);
        }
        if (subject && rs.length === 6 && rs[0] === "去" && nodeCanFillSlot2(rest[1], "location") && nodeCanFillSlot2(rest[2], "action_verb") && nodeCanFillSlot2(rest[3], "object") && nodeCanFillSlot2(rest[4], "action_verb") && nodeCanFillSlot2(rest[5], "object")) {
          const motionChildren = [motionEventPartClone(rest[0], { label: "doing", syntax: "movement_verb", slots: ["movement_verb"] }), motionGoalNode(rest[1])];
          const motion = construction2("MotionGoalVP", "MotionGoal", motionChildren, { slots: constructionSlotsByType2("MotionGoalVP", motionChildren), trace: traceInfo2("generative_template", { construction_type: "MotionGoalVP", template_family: "generative_template", template: ["movement_verb!", "goal!"], assigned_slots: ["movement_verb", "goal"], surfaces: motionChildren.map(flattenSurface2), subspan: true }) });
          const action1 = construction2("TransitiveVP", "VP", [rest[2], rest[3]], { slots: constructionSlotsByType2("TransitiveVP", [rest[2], rest[3]]), trace: traceInfo2("generative_template", { construction_type: "TransitiveVP", template_family: "generative_template", template: ["action_verb!", "object!"], assigned_slots: ["action_verb", "object"], surfaces: [rs[2], rs[3]], subspan: true }) });
          const action2 = construction2("TransitiveVP", "VP", [rest[4], rest[5]], { slots: constructionSlotsByType2("TransitiveVP", [rest[4], rest[5]]), trace: traceInfo2("generative_template", { construction_type: "TransitiveVP", template_family: "generative_template", template: ["action_verb!", "object!"], assigned_slots: ["action_verb", "object"], surfaces: [rs[4], rs[5]], subspan: true }) });
          const chainChildren = [motion, action1, action2];
          const chain = construction2("SerialVerbPurposeChain", "PurposeChain", chainChildren, { slots: constructionSlotsByType2("SerialVerbPurposeChain", chainChildren), trace: traceInfo2("generative_template", { construction_type: "SerialVerbPurposeChain", template_family: "generative_template", template: ["motion_goal_vp!", "action_object_vp!", "purpose_vp!"], assigned_slots: ["motion_goal_vp", "action_vp", "purpose_verb"], surfaces: chainChildren.map(flattenSurface2), shared_subject_provenance: { overt_subject_surface: flattenSurface2(subject), licensed_members: chainChildren.map(flattenSurface2), hidden_subject_inserted: false } }) });
          return motionSubjectPredicateClause(subject, chain, particles);
        }
        if (subject && rs.length === 6 && rest[0] && rest[0].kind === "construction" && rest[0].type === "TransitiveVP" && /^攞/u.test(rs[0]) && rs[1] === "返" && rs[2] === "嚟" && rs[3] === "畀" && nodeCanFillSlot2(rest[4], "subject") && nodeCanFillSlot2(rest[5], "action_verb")) {
          const action = rest[0];
          const returnDirection = motionEventPartClone(rest[1], { label: "doing", syntax: "return_directional_complement", slots: ["verb_complement", "return_motion_verb", "movement_direction"] });
          const returnDeixis = motionEventPartClone(rest[2], { label: "doing", syntax: "deictic_motion_marker directional_complement_part", slots: ["verb_complement", "deictic_motion_marker"] });
          const causedMotionChildren = [action, returnDirection, returnDeixis];
          const causedMotion = construction2("VerbComplementVP", "VerbCompVP", causedMotionChildren, {
            slots: ["verb_complement_vp", "verb_complement", "vp", "action_vp", "predicate", "main_verb", "object", "return_motion_verb", "movement_direction", "deictic_motion_marker"],
            trace: traceInfo2("generative_template", {
              construction_type: "VerbComplementVP",
              template_family: "generative_template",
              template: ["action_object_vp!", "return_directional_complement!", "deictic_motion_marker!"],
              assigned_slots: ["action_vp", "verb_complement", "deictic_motion_marker"],
              surfaces: causedMotionChildren.map(flattenSurface2),
              caused_motion_status: "overt_theme_plus_return_direction",
              not_claims: ["not_aa49_independent_motion_predicate"]
            })
          });
          const relation = cp021bMakePostThemeRelation2({
            upstreamVP: causedMotion,
            upstreamPredicateSurface: "攞",
            upstreamThemeSurface: rs[0].replace(/^攞/u, ""),
            marker: rest[3],
            participantNodes: [rest[4]],
            followingPredicate: rest[5],
            profile: CP021B_POST_THEME_PREDICATE_PROFILES2["攞"]
          });
          return motionSubjectPredicateClause(subject, relation, particles);
        }
        if (subject && rs.length === 7 && rs[0] === "攞" && nodeCanFillSlot2(rest[1], "object") && rs[2] === "返" && rs[3] === "嚟" && rs[4] === "畀" && nodeCanFillSlot2(rest[5], "subject") && nodeCanFillSlot2(rest[6], "action_verb")) {
          const action = construction2("TransitiveVP", "VP", [rest[0], rest[1]], {
            slots: constructionSlotsByType2("TransitiveVP", [rest[0], rest[1]]),
            trace: traceInfo2("generative_template", { construction_type: "TransitiveVP", template_family: "generative_template", template: ["action_verb!", "object!"], assigned_slots: ["action_verb", "object"], surfaces: [rs[0], rs[1]], subspan: true })
          });
          const returnDirection = motionEventPartClone(rest[2], { label: "doing", syntax: "return_directional_complement", slots: ["verb_complement", "return_motion_verb", "movement_direction"] });
          const returnDeixis = motionEventPartClone(rest[3], { label: "doing", syntax: "deictic_motion_marker directional_complement_part", slots: ["verb_complement", "deictic_motion_marker"] });
          const causedMotionChildren = [action, returnDirection, returnDeixis];
          const causedMotion = construction2("VerbComplementVP", "VerbCompVP", causedMotionChildren, {
            slots: ["verb_complement_vp", "verb_complement", "vp", "action_vp", "predicate", "main_verb", "object", "return_motion_verb", "movement_direction", "deictic_motion_marker"],
            trace: traceInfo2("generative_template", {
              construction_type: "VerbComplementVP",
              template_family: "generative_template",
              template: ["action_object_vp!", "return_directional_complement!", "deictic_motion_marker!"],
              assigned_slots: ["action_vp", "verb_complement", "deictic_motion_marker"],
              surfaces: causedMotionChildren.map(flattenSurface2),
              caused_motion_status: "overt_theme_plus_return_direction",
              not_claims: ["not_aa49_independent_motion_predicate"]
            })
          });
          const relation = cp021bMakePostThemeRelation2({
            upstreamVP: causedMotion,
            upstreamPredicateSurface: "攞",
            upstreamThemeSurface: flattenSurface2(rest[1]),
            marker: rest[4],
            participantNodes: [rest[5]],
            followingPredicate: rest[6],
            profile: CP021B_POST_THEME_PREDICATE_PROFILES2["攞"]
          });
          return motionSubjectPredicateClause(subject, relation, particles);
        }
        return null;
      }
      return {
        motionEventSpatialFallback: motionEventSpatialFallback2,
        pathPhraseFromParts: pathPhraseFromParts2,
        sourceMotionClauseFallback: sourceMotionClauseFallback2
      };
    };
  }
});

// src/parser/detectors/boundaries/malformed.js
var require_malformed = __commonJS({
  "src/parser/detectors/boundaries/malformed.js"(exports2, module2) {
    "use strict";
    var OVERT_OBJECT_SELECTION_REVIEW_TYPES = /* @__PURE__ */ new Set(["PerfectiveVP", "PostverbalZoPerfectiveVP", "TransitiveVP", "CompletionVP", "ProductiveVO"]);
    module2.exports = function createMalformedBoundaryDetectors(dependencies = {}) {
      const {
        cleanSlots: cleanSlots2,
        construction: construction2,
        firstToken: firstToken2,
        flattenNodes: flattenNodes2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        productiveObjectHeadToken: productiveObjectHeadToken2,
        tokenSemanticDomains: tokenSemanticDomains2,
        topicChainAntecedentCompatibility: topicChainAntecedentCompatibility2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function isBareQuantityNumeralNode(node) {
        const t = firstToken2(node);
        if (!t) return false;
        const surface = flattenSurface2(node);
        const syntax = t.syntax || "";
        const slots = nodeSlots2(node);
        return ["一", "兩", "二", "三", "四", "五", "六", "七", "八", "九", "十"].includes(surface) && (syntax.includes("quantity") || syntax.includes("count_value") || slots.includes("quantity"));
      }
      function bareNumeralObjectMalformedCandidate2(core) {
        const compact = withoutIgnorableSpaceText2(core || []);
        if (compact.length < 2 || compact.length > 4) return null;
        const numeralNode = compact[compact.length - 1];
        if (!isBareQuantityNumeralNode(numeralNode)) return null;
        const prefix = compact.slice(0, -1);
        const hasSubject = prefix.length >= 2 && nodeCanFillSlot2(prefix[0], "subject");
        const subjectNode = hasSubject ? prefix[0] : null;
        const governorNodes = hasSubject ? prefix.slice(1) : prefix;
        if (governorNodes.length < 1 || governorNodes.length > 2) return null;
        let spec = null;
        const firstGovernor = governorNodes[0];
        const secondGovernor = governorNodes[1] || null;
        if (governorNodes.length === 1 && nodeCanFillSlot2(firstGovernor, "action_verb")) {
          spec = {
            malformedSubtype: "bare_numeral_object",
            governorType: "action_verb",
            problem: "Action verb followed by a bare numeral without classifier or object head.",
            expectedAfterNumeral: ["classifier", "object_head"],
            missingAfterNumeral: "missing_classifier_or_object_head",
            note: "Malformed or incomplete action-predicate + bare numeral object candidate. A classifier/object head is expected after the numeral.",
            semanticFlags: ["malformed_candidate_parse", "suspicious_bare_numeral_object"],
            notClaims: ["not_action_stative_vp", "not_complete_transitive_vp", "not_quantified_classifier_np", "not_full_xbar_tree"],
            slots: ["action_verb"],
            childSpecs: [{
              node: firstGovernor,
              label: "doing",
              syntax: "action_verb malformed_bare_numeral_predicate",
              slots: ["action_verb", "main_verb", "predicate"],
              assignedSlot: "action_verb",
              reason: "Visible action verb inside malformed predicate + bare numeral candidate."
            }]
          };
        } else if (governorNodes.length === 2 && nodeCanFillSlot2(firstGovernor, "action_verb") && nodeCanFillSlot2(secondGovernor, "perfective_aspect")) {
          spec = {
            malformedSubtype: "perfective_bare_numeral_object",
            governorType: "perfective_action_predicate",
            problem: "Perfective action predicate followed by a bare numeral without classifier or object head.",
            expectedAfterNumeral: ["classifier", "object_head"],
            missingAfterNumeral: "missing_classifier_or_object_head",
            note: "Malformed or incomplete perfective action-predicate + bare numeral object candidate. Perfective aspect does not license a numeral as a complete object.",
            semanticFlags: ["malformed_candidate_parse", "suspicious_bare_numeral_object", "perfective_predicate_boundary"],
            notClaims: ["not_complete_perfective_vp", "not_complete_transitive_vp", "not_quantified_classifier_np", "not_context_licensed_fragment"],
            slots: ["action_verb", "perfective_aspect"],
            childSpecs: [
              {
                node: firstGovernor,
                label: "doing",
                syntax: "action_verb malformed_bare_numeral_predicate",
                slots: ["action_verb", "main_verb", "predicate"],
                assignedSlot: "action_verb",
                reason: "Visible action verb inside malformed perfective predicate + bare numeral candidate."
              },
              {
                node: secondGovernor,
                label: "func",
                syntax: "perfective_aspect malformed_bare_numeral_predicate",
                slots: ["perfective_aspect", "aspect_marker"],
                assignedSlot: "perfective_aspect",
                reason: "Visible perfective marker remains transparent inside the malformed candidate."
              }
            ]
          };
        } else if (governorNodes.length === 1 && nodeCanFillSlot2(firstGovernor, "desiderative_modal")) {
          spec = {
            malformedSubtype: "desiderative_bare_numeral_complement",
            governorType: "desiderative_modal",
            problem: "Desiderative predicate followed by a bare numeral that does not form a valid content complement.",
            expectedAfterNumeral: ["classifier_and_nominal_head", "overt_np", "overt_vp", "content_clause"],
            missingAfterNumeral: "missing_desiderative_content_structure",
            note: "Malformed desiderative + bare numeral complement candidate. A bare numeral alone does not saturate the desired or cognitive content slot.",
            semanticFlags: ["malformed_candidate_parse", "suspicious_bare_numeral_complement", "desiderative_complement_boundary"],
            notClaims: ["not_complete_modal_vp", "not_stative_predicate_complement", "not_context_licensed_fragment", "not_quantified_classifier_np"],
            slots: ["modal", "desiderative_modal"],
            childSpecs: [{
              node: firstGovernor,
              label: "func",
              syntax: "desiderative_modal malformed_bare_numeral_governor",
              slots: ["modal", "desiderative_modal"],
              assignedSlot: "desiderative_modal",
              reason: "Visible desiderative head inside malformed desiderative + bare numeral candidate."
            }]
          };
        } else if (governorNodes.length === 1 && nodeCanFillSlot2(firstGovernor, "locative_marker")) {
          spec = {
            malformedSubtype: "locative_bare_numeral_complement",
            governorType: "locative_marker",
            problem: "Locative marker followed by a bare numeral that does not form a location phrase.",
            expectedAfterNumeral: ["location_head", "overt_location_np", "place_phrase"],
            missingAfterNumeral: "missing_location_structure",
            note: "Malformed locative + bare numeral complement candidate. A bare numeral alone does not saturate the location slot.",
            semanticFlags: ["malformed_candidate_parse", "suspicious_bare_numeral_complement", "locative_complement_boundary"],
            notClaims: ["not_complete_locative_predicate", "not_location_np", "not_context_licensed_fragment", "not_quantified_classifier_np"],
            slots: ["locative_marker"],
            childSpecs: [{
              node: firstGovernor,
              label: "func",
              syntax: "locative_marker malformed_bare_numeral_governor",
              slots: ["locative_marker", "coverb_marker"],
              assignedSlot: "locative_marker",
              reason: "Visible locative marker inside malformed locative + bare numeral candidate."
            }]
          };
        }
        if (!spec) return null;
        const subjectSurface = subjectNode ? flattenSurface2(subjectNode) : "";
        const numeralSurface = flattenSurface2(numeralNode);
        const subjectChild = subjectNode ? parserInactiveTokenClone2(firstToken2(subjectNode), {
          label: "who",
          syntax: "subject malformed_candidate_subject",
          slots: ["subject"],
          reason: "Visible subject inside malformed governed-predicate + bare numeral candidate."
        }) : null;
        const governorChildren = spec.childSpecs.map((childSpec) => parserInactiveTokenClone2(firstToken2(childSpec.node), {
          label: childSpec.label,
          syntax: childSpec.syntax,
          slots: childSpec.slots,
          reason: childSpec.reason
        }));
        const numeralChild = parserInactiveTokenClone2(firstToken2(numeralNode), {
          label: "how",
          syntax: "bare_quantity missing_required_complement_structure",
          slots: ["quantity", "bare_numeral", "problem_span"],
          reason: "Visible bare numeral inside a malformed governed complement; required classifier, nominal head, location, VP, or content structure is absent."
        });
        const children = [...subjectChild ? [subjectChild] : [], ...governorChildren, numeralChild];
        const template = [...subjectChild ? ["subject?"] : [], ...spec.childSpecs.map((childSpec) => `${childSpec.assignedSlot}!`), "bare_quantity!"];
        const assignedSlots = [...subjectChild ? ["subject"] : [], ...spec.childSpecs.map((childSpec) => childSpec.assignedSlot), "bare_quantity"];
        const surfaces = [...subjectChild ? [subjectSurface] : [], ...governorNodes.map((node) => flattenSurface2(node)), numeralSurface];
        return construction2("MalformedCandidate", "Malformed", children, {
          slots: cleanSlots2(["malformed_candidate", "needs_review", "predicate", "problem_span", "quantity", ...spec.slots, subjectChild ? "subject" : ""]),
          note: spec.note,
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "MalformedCandidate",
            malformed_family: "bare_numeral_complement",
            malformed_subtype: spec.malformedSubtype,
            governor_type: spec.governorType,
            template,
            assigned_slots: assignedSlots,
            surfaces,
            problem: spec.problem,
            missing_after_numeral: spec.missingAfterNumeral,
            expected_after_numeral: spec.expectedAfterNumeral,
            semantic_review_flags: spec.semanticFlags,
            not_claims: spec.notClaims,
            reason: "A1 malformed-boundary guard: a bare numeral cannot independently saturate object, desiderative-content, or location structure, and perfective aspect cannot rescue it."
          })
        });
      }
      function existentialQuestionWithVpMalformedCandidate2(core) {
        if (!core || core.length !== 2 && core.length !== 3) return null;
        const hasSubject = core.length === 3 && nodeCanFillSlot2(core[0], "subject");
        const existentialNode = hasSubject ? core[1] : core[0];
        const predicateNode = hasSubject ? core[2] : core[1];
        if (!isToken2(existentialNode, "有冇")) return null;
        if (!predicateNode || !nodeCanFillSlot2(predicateNode, "vp")) return null;
        const children = hasSubject ? [core[0], existentialNode, predicateNode] : [existentialNode, predicateNode];
        return construction2("MalformedCandidate", "Malformed", children, {
          slots: cleanSlots2(["malformed_candidate", "needs_review", "question_fragment", "existential_question", "problem_span", "predicate", hasSubject ? "subject" : ""]),
          note: "Malformed existential-question candidate: 有冇 expects an NP object/domain here, not a bare VP predicate.",
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "MalformedCandidate",
            malformed_subtype: "existential_question_with_vp",
            template: hasSubject ? ["subject?", "existential_question!", "vp!"] : ["existential_question!", "vp!"],
            assigned_slots: hasSubject ? ["subject", "existential_question", "vp"] : ["existential_question", "vp"],
            surfaces: children.map((node) => flattenSurface2(node)),
            problem: "有冇 is followed by a VP predicate rather than a noun phrase object/domain.",
            expected_after_existential_question: ["np", "topic_or_object", "abstract_object"],
            semantic_review_flags: ["malformed_candidate_parse", "existential_question_vp_misuse"],
            not_claims: [
              "not_clean_existential_question",
              "not_productive_vo_question",
              "not_broad_mandarin_conversion"
            ],
            reason: "v0.5.110 pre-intermediate guardrail: prevent learner-error rows such as 我有冇食飯 from being accepted as clean ExistentialQuestion parses."
          })
        });
      }
      function assignedConstructionChild(node, slotNames = []) {
        if (!node || node.kind !== "construction") return null;
        const trace = node.trace || {};
        const assignedSlots = Array.isArray(trace.assigned_slots) ? trace.assigned_slots : [];
        const wanted = new Set(slotNames || []);
        for (let index = 0; index < assignedSlots.length; index += 1) {
          if (wanted.has(assignedSlots[index])) return (node.children || [])[index] || null;
        }
        return null;
      }
      function nominalSelectionHeadToken(node) {
        const rows = flattenNodes2([node]).filter((row) => row.kind === "token");
        const candidates = rows.filter((row) => {
          const slots = row.slots || [];
          if (["func", "particle", "measure_word"].includes(row.role || "")) return false;
          return slots.some((slot) => ["head_noun", "object", "np", "topic"].includes(slot));
        });
        return candidates.length ? candidates[candidates.length - 1] : null;
      }
      function overtObjectSelectionCompatibilityFinding(node) {
        if (!node || node.kind !== "construction" || !OVERT_OBJECT_SELECTION_REVIEW_TYPES.has(node.type)) return null;
        const predicateNode = assignedConstructionChild(node, ["action_verb", "consumption_verb", "main_verb"]) || productiveObjectHeadToken2(node);
        const objectNode = assignedConstructionChild(node, ["object", "theme"]);
        const predicateHead = firstToken2(predicateNode);
        const objectHead = nominalSelectionHeadToken(objectNode);
        if (!predicateHead || !objectHead) return null;
        const objectDomains = tokenSemanticDomains2(objectHead);
        const compatibility = topicChainAntecedentCompatibility2({ semantic_domains: objectDomains }, predicateHead);
        if (compatibility.status !== "incompatible") return null;
        return {
          construction: node,
          predicate_head: predicateHead,
          object_node: objectNode,
          object_head: objectHead,
          compatibility
        };
      }
      function overtObjectSelectionReviewCandidate2(core = []) {
        if (core.length === 1 && core[0] && core[0].kind === "construction" && core[0].type === "NeedsContext") return null;
        let finding = null;
        const inspect = (node) => {
          if (!node || finding) return;
          if (node.kind === "construction" && OVERT_OBJECT_SELECTION_REVIEW_TYPES.has(node.type)) {
            finding = overtObjectSelectionCompatibilityFinding(node);
            return;
          }
          if (node.kind === "construction") {
            for (const child of node.children || []) inspect(child);
          }
        };
        for (const root of core || []) {
          inspect(root);
          if (finding) break;
        }
        if (!finding) return null;
        const predicateSurface = finding.predicate_head.surface || "";
        const objectSurface = flattenSurface2(finding.object_node);
        const objectHeadSurface = finding.object_head.surface || objectSurface;
        const sourceSurface = (core || []).map((node) => flattenSurface2(node)).join("");
        const preservedRootConstruction = core.length === 1 && core[0] && core[0].kind === "construction" ? core[0].type || "" : "";
        return construction2("NeedsContext", "needs context", core, {
          slots: cleanSlots2(["needs_context", "review_candidate", "clause", "predicate", "object"]),
          note: "Needs context: the overt object is structurally present, but its known semantic domain is incompatible with the predicate's high-confidence literal selection profile. A coerced or metonymic reading remains possible only with supporting context.",
          trace: traceInfo2("special_ambiguity_rule", {
            surface: sourceSurface,
            rule: "high-confidence predicate selection profile + overt incompatible nominal object",
            reason: "The parser preserves the transparent VP but blocks a clean context-free clause because the overt object's known semantic domain does not satisfy the predicate's high-confidence selection profile. This is a review guard, not a claim that contextual coercion is impossible.",
            context_requirement_status: "context_required",
            missing_argument_slots: ["selectional_coercion_context"],
            missing_slot_details: [{ slot: "selectional_coercion_context", license_status: "unresolved" }],
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            overt_predicate_surface: predicateSurface,
            overt_object_surface: objectSurface,
            overt_object_head_surface: objectHeadSurface,
            overt_object_semantic_domains: finding.compatibility.antecedent_domains || [],
            predicate_object_compatibility_status: finding.compatibility.status,
            predicate_object_compatibility_reason: finding.compatibility.reason,
            embedded_construction: finding.construction.type || "",
            preserved_root_construction: preservedRootConstruction,
            structural_preservation_status: preservedRootConstruction ? "preserved_as_review_child" : "transparent_material_preserved",
            candidate_analyses: [
              {
                analysis: "literal_predicate_object_relation",
                status: "selectionally_incompatible",
                predicate: predicateSurface,
                object: objectSurface
              },
              {
                analysis: "coerced_or_metonymic_object_reading",
                status: "requires_supporting_context",
                predicate: predicateSurface,
                object: objectSurface
              }
            ],
            semantic_review_flags: ["overt_object_selection_compatibility_review", "context_required_unresolved"],
            not_claims: [
              "not_clean_context_free_clause",
              "not_absolute_ungrammaticality",
              "not_hard_coded_surface_pair",
              "not_hidden_object_repair",
              "not_semantic_role_relabeling"
            ]
          })
        });
      }
      return {
        bareNumeralObjectMalformedCandidate: bareNumeralObjectMalformedCandidate2,
        existentialQuestionWithVpMalformedCandidate: existentialQuestionWithVpMalformedCandidate2,
        overtObjectSelectionReviewCandidate: overtObjectSelectionReviewCandidate2
      };
    };
  }
});

// src/parser/detectors/boundaries/needs-context.js
var require_needs_context = __commonJS({
  "src/parser/detectors/boundaries/needs-context.js"(exports2, module2) {
    "use strict";
    module2.exports = function createNeedsContextBoundaryDetectors(dependencies = {}) {
      const {
        cleanSlots: cleanSlots2,
        construction: construction2,
        firstToken: firstToken2,
        flattenDisplaySurface: flattenDisplaySurface2,
        flattenSurface: flattenSurface2,
        isModalToken: isModalToken2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        nodeSurfaceMatches: nodeSurfaceMatches2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        phase4CognitionActiveTokenClone: phase4CognitionActiveTokenClone2,
        predicateOmissionProfileForHead: predicateOmissionProfileForHead2,
        templateDerivedSlots: templateDerivedSlots2,
        tokenSemanticDomains: tokenSemanticDomains2,
        traceInfo: traceInfo2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function ambiguousNeedsContextCandidate2(core) {
        if (!core || core.length !== 1) return null;
        const first = firstToken2(core[0]);
        if (!first || first.syntax !== "ambiguous_needs_context") return null;
        const candidateAnalyses = first.trace && first.trace.candidate_analyses || [
          { construction: "NegatedStativePredicate", split: ["唔", "好食"], meaning_hint: "not tasty", parser_active: false },
          { construction: "ProhibitiveImperative", split: ["唔好", "食"], meaning_hint: "don't eat", parser_active: false }
        ];
        return construction2("NeedsContext", "needs context", core, {
          note: "Ambiguous split: 唔 + 好食 or 唔好 + 食.",
          trace: traceInfo2("special_ambiguity_rule", {
            surface: "唔好食",
            reason: "Needs context ambiguity.",
            candidate_analyses: candidateAnalyses
          })
        });
      }
      function mandarinNegatorNeedsContextCandidate2(core) {
        if (!core || core.length < 2) return null;
        const first = firstToken2(core[0]);
        if (!first || first.surface !== "不") return null;
        const rest = core.slice(1);
        if (!rest.some((node) => nodeCanFillSlot2(node, "vp") || nodeCanFillSlot2(node, "predicate"))) return null;
        return construction2("NeedsContext", "needs context", core, {
          slots: ["needs_context", "review_candidate", "negator", "predicate"],
          note: "Needs review: 不 is a Mandarin/Standard Chinese negator here. Canto Span does not silently convert it to Cantonese 唔.",
          trace: traceInfo2("special_ambiguity_rule", {
            surface: core.map((node) => flattenSurface2(node)).join(""),
            reason: "Mandarin negator before a Cantonese VP is review-only; raw-first parser-shadow policy forbids silent 不→唔 conversion.",
            candidate_analyses: [
              { construction: "NegatedVP", split: ["不", rest.map((node) => flattenSurface2(node)).join("")], status: "blocked_pending_review", parser_active: false },
              { construction: "NeedsContext", split: core.map((node) => flattenSurface2(node)), status: "accepted_guardrail", parser_active: false }
            ],
            semantic_review_flags: ["needs_context_parse", "mandarin_negator_review"],
            not_claims: [
              "not_clean_productive_vo",
              "not_silent_mandarin_to_cantonese_conversion",
              "not_parser_shadow_repair"
            ]
          })
        });
      }
      function incompleteProhibitiveNeedsContextCandidate2(core) {
        if (!core || !core.length) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (bareCore.length !== 1) return null;
        const node = bareCore[0];
        if (!nodeSurfaceMatches2(node, ["唔好"])) return null;
        if (!nodeCanFillSlot2(node, "prohibitive_marker")) return null;
        const children = [...bareCore, ...particles];
        return construction2("NeedsContext", "needs context", children, {
          slots: ["needs_context", "review_candidate", "prohibitive_marker", "problem_span"],
          note: "Needs context: standalone 唔好 is an incomplete prohibitive/review fragment without the action or prior context.",
          trace: traceInfo2("special_ambiguity_rule", {
            surface: flattenSurface2(node),
            reason: "Standalone prohibitive marker needs a following VP or discourse context.",
            context_requirement_status: "context_required",
            missing_argument_slots: ["prohibited_action_or_evaluation_target"],
            missing_slot_details: [
              { slot: "prohibited_action", license_status: "unresolved", candidate_reading: "prohibitive_fragment" },
              { slot: "evaluation_target", license_status: "unresolved", candidate_reading: "negative_evaluation" }
            ],
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            candidate_readings: ["prohibitive_fragment", "negative_evaluation"],
            overt_head: "唔好",
            particle_contribution: particles.map((child) => flattenSurface2(child)),
            omission_analysis_candidates: ["argument_drop", "predicate_fragment"],
            semantic_review_flags: ["needs_context_parse", "incomplete_prohibitive_marker"],
            not_claims: ["not_clean_imperative", "not_complete_formula"]
          })
        });
      }
      function predicateOmissionParts(core) {
        if (!core || !core.length) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (!bareCore.length) return null;
        let cursor = 0;
        const subject = nodeCanFillSlot2(bareCore[cursor], "subject") ? bareCore[cursor++] : null;
        const negator = isToken2(bareCore[cursor], "唔") ? bareCore[cursor++] : null;
        if (cursor !== bareCore.length - 1) return null;
        const predicate = bareCore[cursor];
        const predicateSurface = flattenSurface2(predicate);
        const profile = predicateOmissionProfileForHead2(predicateSurface);
        if (!profile) return null;
        return {
          bareCore,
          particles,
          subject,
          negator,
          predicate,
          predicateSurface,
          profile,
          polarity: negator ? "negative" : "positive"
        };
      }
      function predicateOmissionTrace(parts, overrides = {}) {
        const { profile, predicateSurface, subject, negator, particles } = parts;
        const missingSlots = overrides.missing_argument_slots || [
          profile.missing_slot_type,
          ...!subject && overrides.subject_optional !== true ? ["subject"] : []
        ];
        return {
          predicate_omission_profile: profile.id,
          omission_status: overrides.omission_status || profile.isolated_status,
          missing_argument_slots: missingSlots,
          missing_slot_details: missingSlots.map((slot) => ({ slot, license_status: "unresolved" })),
          complement_type: profile.complement_type,
          context_requirement_status: overrides.context_requirement_status || "context_required",
          antecedent_status: overrides.antecedent_status || "not_observed",
          selected_alternative: parts.polarity,
          subject_status: subject ? "explicit" : "omitted_unlicensed",
          polarity: parts.polarity,
          conventionality_status: overrides.conventionality_status || profile.conventional_bare_status,
          speech_event_use: overrides.speech_event_use || "not_applicable",
          overt_head: predicateSurface,
          particle_contribution: particles.map((node) => flattenSurface2(node)),
          discourse_license_not_observed: (overrides.antecedent_status || "not_observed") !== "linked",
          omission_analysis_candidates: overrides.omission_analysis_candidates || ["typed_predicate_ellipsis", "predicate_response_fragment"],
          semantic_review_flags: overrides.semantic_review_flags || ["needs_context_parse", "typed_predicate_omission"],
          not_claims: overrides.not_claims || [
            "not_fabricated_complement_token",
            "not_unrestricted_argument_deletion",
            "not_sentence_specific_surface_rule"
          ],
          ...overrides
        };
      }
      function incompleteRestrictiveFocusBoundaryCandidate2(core) {
        if (!core || !core.length) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (bareCore.length !== 1 || !isToken2(bareCore[0], "得") || particles.length !== 1) return null;
        const particleSurface = flattenSurface2(particles[0]);
        if (!["啫", "咋"].includes(particleSurface)) return null;
        const children = [bareCore[0], particles[0]];
        return construction2("NeedsContext", "needs context", children, {
          slots: cleanSlots2(["needs_context", "review_candidate", "predicate", "particle", "problem_span"]),
          note: "Needs context: 得 plus a restrictive particle is visible, but the scalar or quantity host required for a clean restrictive-focus reading is absent.",
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "NeedsContext",
            predicate_omission_profile: "acceptability_possibility",
            omission_status: "restrictive_scalar_host_or_acceptability_context_unresolved",
            template: ["acceptability_or_restriction_marker!", "restrictive_focus_particle!"],
            assigned_slots: ["predicate", "particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            missing_argument_slots: ["scalar_host"],
            missing_slot_details: [{ slot: "scalar_host", license_status: "unresolved" }],
            complement_type: "scalar_or_quantity_host_or_acceptability_domain",
            context_requirement_status: "context_required",
            antecedent_status: "not_observed",
            selected_alternative: "acceptability_response_or_restrictive_focus",
            polarity: "positive",
            conventionality_status: "context_sensitive",
            particle_contribution: [particleSurface],
            omission_analysis_candidates: ["restrictive_focus_with_omitted_scalar_host", "acceptability_response_with_focus_particle"],
            semantic_review_flags: ["needs_context_parse", "restrictive_focus_missing_scalar_host", "acceptability_focus_boundary"],
            not_claims: ["not_clean_focus_particle_frame", "not_fabricated_scalar_host", "not_productive_potential_result", "not_sentence_specific_surface_rule"],
            reason: "Bare 得啫/得咋 lacks the overt scalar host required by the productive restrictive FocusParticleFrame. Preserve both tokens under one review span instead of ignoring the particle or inventing the missing host."
          })
        });
      }
      function predicateOmissionCandidate2(core) {
        const parts = predicateOmissionParts(core);
        if (!parts) return null;
        const { subject, negator, predicate, predicateSurface, profile, particles } = parts;
        const fullSurface = core.map((node) => flattenSurface2(node)).join("");
        if (["可以呀", "可以啊", "得啦", "得喇"].includes(fullSurface)) return null;
        const directiveSpeechUse = profile.id === "speech_report_event" && subject && predicateSurface === "講" && particles.some((node) => ["啦", "喇"].includes(flattenSurface2(node)));
        const conventionalCognitionUse = profile.id === "factive_cognition" && subject;
        if (particles.length && !directiveSpeechUse && !conventionalCognitionUse) return null;
        if (profile.id === "factive_cognition" && !subject && negator) {
          return null;
        }
        if (profile.id === "factive_cognition" && subject && !negator) {
          return null;
        }
        if (profile.id === "factive_cognition" && subject) {
          const promoted = phase4CognitionActiveTokenClone2(predicate, {
            label: predicate.label || "doing",
            syntax: `${predicate.syntax || "cognition_verb"} cognition_statement_predicate`,
            slots: ["cognition_statement_clause"],
            reason: "A1 typed omission profile: 知 is a conventional bare cognition statement with visible polarity and no fabricated proposition."
          });
          const children2 = [subject, ...negator ? [negator] : [], promoted, ...particles];
          return construction2("CognitionStatementClause", "Know", children2, {
            note: "Conventional bare cognition statement. The proposition may be supplied by discourse, but no hidden content clause is inserted.",
            slots: templateDerivedSlots2("CognitionStatementClause", children2),
            trace: traceInfo2("generative_template", {
              construction_type: "CognitionStatementClause",
              template_family: "generative_template",
              template: ["subject!", "polarity?", "cognition_predicate!", "particle?"],
              assigned_slots: ["subject", ...negator ? ["polarity"] : [], "cognition_predicate", ...particles.map(() => "particle")],
              surfaces: children2.map((node) => flattenSurface2(node)),
              ...predicateOmissionTrace(parts, {
                omission_status: "conventional_bare_statement",
                missing_argument_slots: [],
                context_requirement_status: "context_not_required",
                antecedent_status: "not_applicable",
                conventionality_status: "conventional_bare_statement",
                semantic_review_flags: [],
                omission_analysis_candidates: ["conventional_bare_cognition_statement", "optional_contextual_proposition_link"],
                not_claims: ["not_fabricated_content_clause", "not_forced_fragment_answer"]
              }),
              reason: "Knowledge predicates can stand as conventional statements with positive or negative polarity. Explicit context may later link the proposition without changing the visible construction."
            })
          });
        }
        if (directiveSpeechUse) {
          const speech = parserInactiveTokenClone2(predicate, {
            label: predicate.label || "doing",
            pos: "verb",
            syntax: `${predicate.syntax || "speech_verb"} speech_event_predicate directive_speech_event`,
            slots: ["speech_verb", "action_verb", "main_verb", "predicate", "vp"],
            reason: "講 is used as an overt speech-event predicate under directive closure; content is optional rather than silently omitted."
          });
          const children2 = [subject, speech, ...particles];
          return construction2("SubjectPredicateClause", "SubjPred", children2, {
            note: "Subject-led directive speech event with optional content.",
            slots: templateDerivedSlots2("SubjectPredicateClause", children2),
            trace: traceInfo2("generative_template", {
              construction_type: "SubjectPredicateClause",
              template_family: "generative_template",
              template: ["subject!", "speech_event_predicate!", "directive_particle!"],
              assigned_slots: ["subject", "predicate", ...particles.map(() => "particle")],
              surfaces: children2.map((node) => flattenSurface2(node)),
              ...predicateOmissionTrace(parts, {
                omission_status: "speech_event_complete_without_content",
                missing_argument_slots: [],
                context_requirement_status: "context_not_required",
                antecedent_status: "not_applicable",
                conventionality_status: "conventional_directive_speech_event",
                speech_event_use: "imperative_or_directive",
                semantic_review_flags: [],
                omission_analysis_candidates: ["speech_event_use", "content_optional"],
                not_claims: ["not_fabricated_reported_content", "not_forced_reported_speech"]
              }),
              reason: "A directive such as 你講啦 can denote the speech event itself. The parser therefore does not force an omitted reported-content analysis."
            })
          });
        }
        const children = [
          ...subject ? [subject] : [],
          ...negator ? [negator] : [],
          predicate,
          ...particles
        ];
        const ambiguity = profile.id === "acceptability_possibility" ? ["acceptability_response", "modal_possibility_predicate", "potential_or_resultative_boundary"] : profile.id === "speech_report_event" ? ["completed_speech_event", "omitted_reported_content"] : ["typed_predicate_ellipsis", "predicate_response_fragment"];
        const semanticFlags = profile.id === "speech_report_event" ? ["needs_context_parse", "speech_event_content_ambiguity"] : profile.id === "acceptability_possibility" ? ["needs_context_parse", "acceptability_potential_boundary"] : ["needs_context_parse", "typed_predicate_omission"];
        return construction2("NeedsContext", "needs context", children, {
          slots: cleanSlots2(["needs_context", "review_candidate", "predicate", "problem_span", subject ? "subject" : "", profile.parser_family === "modal" ? "modal" : ""]),
          note: "Needs context: a typed predicate is visible, but its complement/domain or discourse-response license is unresolved.",
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "NeedsContext",
            template: ["subject?", "polarity?", "typed_predicate!", "particle?"],
            assigned_slots: [...subject ? ["subject"] : [], ...negator ? ["polarity"] : [], "predicate", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            ...predicateOmissionTrace(parts, {
              omission_analysis_candidates: ambiguity,
              semantic_review_flags: semanticFlags,
              speech_event_use: profile.id === "speech_report_event" ? "ambiguous" : "not_applicable"
            }),
            reason: profile.id === "speech_report_event" ? "A bare speech predicate may denote a completed speech event or omit reported content. Keep both analyses available until context resolves the use." : profile.id === "acceptability_possibility" ? "Standalone 得 can be an acceptability/possibility response, while 得 also participates in potential/resultative grammar. Do not force one clean reading without context." : "The omission profile records the predicate family, polarity, and complement type. It does not invent the missing complement or rely on literal sentence matching."
          })
        });
      }
      function incompleteModalNeedsContextCandidate2(core) {
        if (!core || !core.length) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (!bareCore.length || bareCore.length > 2) return null;
        const hasSubject = bareCore.length === 2 && nodeCanFillSlot2(bareCore[0], "subject");
        const modalNode = hasSubject ? bareCore[1] : bareCore[0];
        if (!isModalToken2(modalNode)) return null;
        const modalSurface = flattenSurface2(modalNode);
        if (modalSurface.includes("唔")) return null;
        const fullSurface = core.map((node) => flattenSurface2(node)).join("");
        if (["可以呀", "可以啊"].includes(fullSurface)) return null;
        const missingSlot = modalSurface === "想" ? "desired_or_cognitive_content" : "modal_or_volitional_complement";
        const missingSlots = [missingSlot, ...!hasSubject ? ["subject"] : []];
        const children = [...bareCore, ...particles];
        return construction2("NeedsContext", "needs context", children, {
          slots: cleanSlots2(["needs_context", "review_candidate", "modal", "desiderative_modal", "problem_span", hasSubject ? "subject" : ""]),
          note: "Needs context: the overt modal/desiderative predicate has no represented complement or licensed prior question.",
          trace: traceInfo2("special_ambiguity_rule", {
            surface: fullSurface,
            template: hasSubject ? ["subject?", "modal!"] : ["modal!"],
            reason: "Incomplete modal/desiderative span; do not accept it as a clean predicate clause without an overt or explicitly licensed complement.",
            context_requirement_status: "context_required",
            missing_argument_slots: missingSlots,
            missing_slot_details: missingSlots.map((slot) => ({ slot, license_status: "unresolved" })),
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            subject_status: hasSubject ? "explicit" : "omitted_unlicensed",
            overt_head: modalSurface,
            particle_contribution: particles.map((node) => flattenSurface2(node)),
            omission_analysis_candidates: ["complement_ellipsis", "modal_response_fragment"],
            semantic_review_flags: ["needs_context_parse", "incomplete_modal_predicate"],
            not_claims: ["not_clean_modal_vp", "not_subject_predicate_clause"]
          })
        });
      }
      function isContextuallyUnsaturatedPredicate(node) {
        if (!node) return false;
        const slots = nodeSlots2(node);
        if (slots.includes("preference_predicate")) return true;
        if (slots.includes("stance_predicate")) return true;
        return nodeCanFillSlot2(node, "action_verb") && tokenSemanticDomains2(node).includes("consumption");
      }
      function incompleteContextualPredicateCandidate2(core) {
        if (!core || !core.length) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (!bareCore.length) return null;
        let cursor = 0;
        const hasSubject = nodeCanFillSlot2(bareCore[0], "subject");
        if (hasSubject) cursor += 1;
        let polarity = "positive";
        if (bareCore[cursor] && nodeSurfaceMatches2(bareCore[cursor], ["唔", "未"])) {
          polarity = nodeSurfaceMatches2(bareCore[cursor], ["未"]) ? "not_yet" : "negative";
          cursor += 1;
        }
        if (cursor !== bareCore.length - 1) return null;
        const predicate = bareCore[cursor];
        if (!isContextuallyUnsaturatedPredicate(predicate)) return null;
        const predicateSurface = flattenSurface2(predicate);
        const predicateSlots = nodeSlots2(predicate);
        const predicateFamily = predicateSlots.includes("preference_predicate") ? "preference" : predicateSlots.includes("stance_predicate") ? "cognition_or_opinion" : "consumption_or_activity";
        const missingSlot = predicateFamily === "preference" ? "preference_object_or_domain" : predicateFamily === "cognition_or_opinion" ? "content_clause" : "object_or_activity_domain";
        const missingSlots = [missingSlot, ...!hasSubject ? ["subject"] : []];
        const children = [...bareCore, ...particles];
        const reason = predicateFamily === "cognition_or_opinion" ? "A cognition/opinion predicate remains overt while its proposition-like content is absent; the parser records the missing content clause without fabricating it." : "A context-sensitive predicate remains overt while its object/domain is absent; the parser records the missing slot without fabricating its content.";
        const note = predicateFamily === "cognition_or_opinion" ? "Needs context: the overt cognition/opinion predicate is structurally valid, but its proposition-like content is not represented." : "Needs context: the overt predicate is structurally valid, but its object or activity domain is not represented.";
        const omissionCandidates = predicateFamily === "cognition_or_opinion" ? ["content_clause_ellipsis", "opinion_content_recovery"] : ["argument_drop", "predicate_repetition_answer"];
        const semanticFlags = predicateFamily === "cognition_or_opinion" ? ["needs_context_parse", "missing_cognition_or_opinion_content"] : ["needs_context_parse", "missing_predicate_domain"];
        const notClaims = predicateFamily === "cognition_or_opinion" ? ["not_clean_subject_predicate_clause", "not_fabricated_content_clause"] : ["not_clean_subject_predicate_clause", "not_fabricated_object_or_domain"];
        return construction2("NeedsContext", "needs context", children, {
          slots: cleanSlots2(["needs_context", "review_candidate", "predicate", "problem_span", hasSubject ? "subject" : ""]),
          note,
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "NeedsContext",
            template: hasSubject ? ["subject?", "polarity?", "predicate!", "particle?"] : ["polarity?", "predicate!", "particle?"],
            assigned_slots: [...hasSubject ? ["subject"] : [], ...polarity === "positive" ? [] : ["polarity"], "predicate", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            predicate_family: predicateFamily,
            reason,
            context_requirement_status: "context_required",
            missing_argument_slots: missingSlots,
            missing_slot_details: missingSlots.map((slot) => ({ slot, license_status: "unresolved" })),
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            subject_status: hasSubject ? "explicit" : "omitted_unlicensed",
            overt_head: predicateSurface,
            selected_alternative: polarity,
            particle_contribution: particles.map((node) => flattenSurface2(node)),
            omission_analysis_candidates: omissionCandidates,
            semantic_review_flags: semanticFlags,
            not_claims: notClaims
          })
        });
      }
      function incompleteLocativeNeedsContextCandidate2(core) {
        if (!core || !core.length) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (bareCore.length !== 1 && bareCore.length !== 2) return null;
        const hasSubject = bareCore.length === 2 && nodeCanFillSlot2(bareCore[0], "subject");
        if (bareCore.length === 2 && !hasSubject) return null;
        const locativeNode = hasSubject ? bareCore[1] : bareCore[0];
        if (!nodeSurfaceMatches2(locativeNode, ["喺"])) return null;
        if (!nodeCanFillSlot2(locativeNode, "locative_marker") && !nodeCanFillSlot2(locativeNode, "coverb_marker")) return null;
        const children = [...bareCore, ...particles];
        return construction2("NeedsContext", "needs context", children, {
          slots: cleanSlots2(["needs_context", "review_candidate", "locative_marker", "coverb_marker", "problem_span", hasSubject ? "subject" : ""]),
          note: "Needs context: 喺 needs a following location, progressive predicate, or prior discourse context.",
          trace: traceInfo2("special_ambiguity_rule", {
            surface: children.map((node) => flattenSurface2(node)).join(""),
            template: hasSubject ? ["subject?", "locative_marker!", "particle?"] : ["locative_marker!", "particle?"],
            reason: "Incomplete locative/coverb span; do not accept it as a clean clause without a complement.",
            context_requirement_status: "context_required",
            missing_argument_slots: ["location"],
            missing_slot_details: [{ slot: "location", license_status: "unresolved" }],
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            subject_status: hasSubject ? "explicit" : "omitted_unlicensed",
            overt_head: flattenSurface2(locativeNode),
            particle_contribution: particles.map((node) => flattenSurface2(node)),
            omission_analysis_candidates: ["locative_complement_ellipsis", "deictic_location_recovery"],
            semantic_review_flags: ["needs_context_parse", "incomplete_locative_marker"],
            not_claims: ["not_clean_locative_clause", "not_coverb_frame"]
          })
        });
      }
      function mandarinReviewNeedsContextCandidate2(core) {
        if (!core || !core.length) return null;
        const parserSurface = core.map((node) => flattenSurface2(node)).join("");
        const displaySurface = core.map((node) => flattenDisplaySurface2(node)).join("");
        const tokenSurfaces = core.flatMap((node) => {
          const t = firstToken2(node);
          return [flattenSurface2(node), flattenDisplaySurface2(node), t && t.surface, t && t.display_surface].filter(Boolean);
        });
        const startsWithMeiYou = parserSurface.startsWith("没有") || displaySurface.startsWith("没有") || core.length >= 2 && nodeSurfaceMatches2(core[0], ["没"]) && nodeSurfaceMatches2(core[1], ["有"]);
        const hasMandarinThirdPerson = tokenSurfaces.includes("他") || tokenSurfaces.includes("她") || tokenSurfaces.includes("它");
        const hasShi = tokenSurfaces.includes("是");
        const hasGeiTa = parserSurface.startsWith("给他") || displaySurface.startsWith("给他") || parserSurface.startsWith("畀他") || displaySurface.startsWith("畀他");
        if (!startsWithMeiYou && !hasMandarinThirdPerson && !hasShi && !hasGeiTa) return null;
        return construction2("NeedsContext", "needs context", core, {
          slots: ["needs_context", "review_candidate", "problem_span"],
          note: "Needs review: this row contains Mandarin/Standard Chinese material that Canto Span should not silently translate into Cantonese.",
          trace: traceInfo2("special_ambiguity_rule", {
            surface: parserSurface,
            display_surface: displaySurface,
            reason: "Raw-first parser-shadow policy: Mandarin/Standard Chinese review rows stay NeedsContext instead of becoming clean Cantonese parses.",
            semantic_review_flags: ["needs_context_parse", "mandarin_review_row"],
            triggers: cleanSlots2([
              startsWithMeiYou ? "mandarin_meiyou" : "",
              hasMandarinThirdPerson ? "mandarin_third_person_pronoun" : "",
              hasShi ? "mandarin_copula_shi" : "",
              hasGeiTa ? "mandarin_gei_ta_transfer" : ""
            ]),
            not_claims: [
              "not_silent_mandarin_to_cantonese_conversion",
              "not_parser_shadow_repair",
              "not_clean_productive_vo"
            ]
          })
        });
      }
      return {
        ambiguousNeedsContextCandidate: ambiguousNeedsContextCandidate2,
        mandarinNegatorNeedsContextCandidate: mandarinNegatorNeedsContextCandidate2,
        incompleteProhibitiveNeedsContextCandidate: incompleteProhibitiveNeedsContextCandidate2,
        incompleteRestrictiveFocusBoundaryCandidate: incompleteRestrictiveFocusBoundaryCandidate2,
        predicateOmissionCandidate: predicateOmissionCandidate2,
        incompleteModalNeedsContextCandidate: incompleteModalNeedsContextCandidate2,
        incompleteContextualPredicateCandidate: incompleteContextualPredicateCandidate2,
        incompleteLocativeNeedsContextCandidate: incompleteLocativeNeedsContextCandidate2,
        mandarinReviewNeedsContextCandidate: mandarinReviewNeedsContextCandidate2
      };
    };
  }
});

// src/parser/topic-chain/linkage.js
var require_linkage = __commonJS({
  "src/parser/topic-chain/linkage.js"(exports2, module2) {
    "use strict";
    module2.exports = function createTopicChainLinkage2(dependencies = {}) {
      const {
        clauseSequenceSegments: clauseSequenceSegments2,
        flattenNodes: flattenNodes2,
        flattenSurface: flattenSurface2,
        getConstructionAffordances: getConstructionAffordances2,
        isClauseSequenceSeparator: isClauseSequenceSeparator2,
        isRelationalCoverbLinker: isRelationalCoverbLinker2,
        isTopicFrameLinker: isTopicFrameLinker2,
        nodeCanFillSlot: nodeCanFillSlot2,
        tokenSemanticDomains: tokenSemanticDomains2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function topicChainAntecedentDescriptor(segment = []) {
        const rows = flattenNodes2(segment || []);
        const constructions = rows.filter((row) => row.kind === "construction");
        const npRows = constructions.filter((row) => {
          const type = String(row.type || "");
          return /NP$/u.test(type) || ["NominalHeadSpan", "ModifiedNP", "AssociativeNP", "PluralMarkedNP"].includes(type);
        });
        if (!npRows.length) return null;
        const onlyMeaningful = withoutIgnorableSpaceText2(segment).filter((node) => !isClauseSequenceSeparator2(node));
        const standaloneTopic = onlyMeaningful.length === 1 && onlyMeaningful[0].kind === "construction" && npRows.some((row) => row.surface === flattenSurface2(onlyMeaningful[0]));
        const topicFrameLinker = onlyMeaningful.length >= 2 && isTopicFrameLinker2(onlyMeaningful[0]) ? onlyMeaningful[0] : null;
        const relationalCoverbLinker = onlyMeaningful.length >= 2 && isRelationalCoverbLinker2(onlyMeaningful[0]) ? onlyMeaningful[0] : null;
        const candidate = standaloneTopic ? npRows[0] : npRows[npRows.length - 1];
        if (!candidate || !candidate.surface) return null;
        const semanticDomains = Array.from(new Set(
          rows.filter((row) => row.kind === "token").flatMap((row) => tokenSemanticDomains2(row))
        ));
        return {
          surface: candidate.surface,
          construction: candidate.type || "",
          source: topicFrameLinker ? "overt_topic_frame" : standaloneTopic ? "overt_initial_topic" : "prior_clause_nominal_domain",
          semantic_domains: semanticDomains,
          topic_frame_linker_surface: topicFrameLinker ? topicFrameLinker.surface || "" : "",
          topic_frame_status: topicFrameLinker ? "overt_topic_frame_linked" : "not_applicable",
          relational_coverb_linker_surface: relationalCoverbLinker ? relationalCoverbLinker.surface || "" : "",
          relational_frame_status: relationalCoverbLinker ? "overt_relational_coverb_accounted" : "not_applicable"
        };
      }
      function visitConstructionNodes(node, visit, depth = 0) {
        if (!node) return;
        if (node.kind === "construction") {
          visit(node, depth);
          for (const child of node.children || []) visitConstructionNodes(child, visit, depth + 1);
        }
      }
      function productiveObjectHeadToken2(node) {
        const rows = flattenNodes2([node]);
        return rows.find((row) => row.kind === "token" && (row.slots || []).includes("action_verb") && getConstructionAffordances2(row).can_head_productive_vo === true) || null;
      }
      function constructionHasOvertObject(node) {
        const rows = flattenNodes2([node]);
        const head = productiveObjectHeadToken2(node);
        if (!head) return false;
        const headIndex = rows.findIndex((row) => row.kind === "token" && row.surface === head.surface && row.depth === head.depth);
        return rows.slice(Math.max(0, headIndex + 1)).some((row) => {
          if (row.kind === "text") return false;
          const slots = row.slots || [];
          if (slots.includes("subject")) return false;
          return slots.some((slot) => ["object", "theme", "head_noun", "np"].includes(slot));
        });
      }
      function constructionContainsConstruction(root, target) {
        if (!root || root.kind !== "construction") return false;
        for (const child of root.children || []) {
          if (child === target) return true;
          if (child && child.kind === "construction" && constructionContainsConstruction(child, target)) return true;
        }
        return false;
      }
      function segmentHasOvertObjectAfterHead(segment = [], headSurface = "") {
        const rows = flattenNodes2(segment || []);
        const headIndex = rows.findIndex((row) => row.kind === "token" && row.surface === headSurface && (row.slots || []).includes("action_verb"));
        if (headIndex < 0) return false;
        return rows.slice(headIndex + 1).some((row) => {
          if (row.kind === "text") return false;
          const slots = row.slots || [];
          if (slots.includes("subject") || slots.includes("particle")) return false;
          return slots.some((slot) => ["object", "theme", "head_noun", "np"].includes(slot));
        });
      }
      function topicChainGapCandidates(segment = []) {
        const candidates = [];
        for (const root of segment || []) {
          visitConstructionNodes(root, (node, depth) => {
            if (!nodeCanFillSlot2(node, "vp") && !nodeCanFillSlot2(node, "predicate")) return;
            const head = productiveObjectHeadToken2(node);
            if (!head || constructionHasOvertObject(node) || segmentHasOvertObjectAfterHead(segment, head.surface)) return;
            candidates.push({ node, depth, head_surface: head.surface, surface: flattenSurface2(node) });
          });
        }
        return candidates.filter((candidate) => !candidates.some(
          (other) => other !== candidate && constructionContainsConstruction(candidate.node, other.node)
        ));
      }
      function topicChainAntecedentCompatibility2(antecedent, head) {
        const antecedentDomains = new Set(antecedent && antecedent.semantic_domains || []);
        const syntax = String(head && head.syntax || "");
        const result = (status, reason) => ({ status, reason, antecedent_domains: [...antecedentDomains] });
        if (syntax.includes("chain_select_perception")) {
          return result("compatible", "broad_perception_predicate_accepts_overt_nominal_topic");
        }
        if (syntax.includes("chain_select_discourse_content")) {
          return result("compatible", "discourse_content_predicate_accepts_overt_topic_domain");
        }
        if (syntax.includes("chain_select_edible")) {
          return antecedentDomains.has("food_item") || antecedentDomains.has("edible_item") ? result("compatible", "edible_topic_for_consumption_predicate") : antecedentDomains.size ? result("incompatible", "known_antecedent_domain_is_not_edible") : result("unverified", "eating_predicate_requires_known_edible_topic");
        }
        if (syntax.includes("chain_select_drinkable")) {
          return antecedentDomains.has("drinkable_item") || antecedentDomains.has("liquid") ? result("compatible", "drinkable_topic_for_consumption_predicate") : antecedentDomains.size ? result("incompatible", "known_antecedent_domain_is_not_drinkable") : result("unverified", "drinking_predicate_requires_known_drinkable_topic");
        }
        if (syntax.includes("chain_select_bookable_resource")) {
          return antecedentDomains.has("bookable_resource") ? result("compatible", "bookable_resource_topic_for_reservation_predicate") : result("unverified", "reservation_predicate_requires_known_bookable_resource_topic");
        }
        return result("unverified", "object_selecting_predicate_lacks_topic_chain_selection_profile");
      }
      function applyTopicChainNullObjectLinkage2(children = []) {
        const segments = clauseSequenceSegments2(children);
        if (segments.length < 2) return null;
        const antecedent = topicChainAntecedentDescriptor(segments[0]);
        if (!antecedent) return null;
        const candidates = [];
        for (const segment of segments.slice(1)) {
          for (const candidate of topicChainGapCandidates(segment)) {
            candidate.compatibility = topicChainAntecedentCompatibility2(antecedent, productiveObjectHeadToken2(candidate.node));
            candidates.push(candidate);
          }
        }
        if (!candidates.length) return null;
        const blocked = candidates.filter((candidate) => candidate.compatibility.status !== "compatible");
        const topicChainId = `topic-chain:${antecedent.surface}`;
        const topicFrameTrace = antecedent.topic_frame_linker_surface ? {
          topic_frame_status: antecedent.topic_frame_status,
          topic_frame_linker_surface: antecedent.topic_frame_linker_surface,
          topic_frame_domain_surface: antecedent.surface
        } : {};
        const relationalFrameTrace = antecedent.relational_coverb_linker_surface ? {
          relational_frame_status: antecedent.relational_frame_status,
          relational_coverb_linker_surface: antecedent.relational_coverb_linker_surface,
          relational_coverb_domain_surface: antecedent.surface
        } : {};
        if (blocked.length) {
          for (const candidate of candidates) {
            const trace = candidate.node.trace || {};
            candidate.node.trace = {
              ...trace,
              context_requirement_status: "context_required",
              missing_argument_slots: ["object_or_activity_domain"],
              missing_slot_details: [{ slot: "object_or_activity_domain", license_status: "unresolved" }],
              antecedent_status: candidate.compatibility.status === "incompatible" ? "incompatible" : "unverified",
              antecedent_span: antecedent.surface,
              discourse_license_not_observed: true,
              topic_chain_id: topicChainId,
              topic_chain_role: "unresolved_null_object_candidate",
              topic_antecedent_surface: antecedent.surface,
              topic_antecedent_source: antecedent.source,
              ...topicFrameTrace,
              ...relationalFrameTrace,
              topic_antecedent_semantic_domains: antecedent.semantic_domains || [],
              null_object: "unresolved",
              null_object_link: "not_licensed_antecedent_predicate_compatibility",
              antecedent_predicate_compatibility_status: candidate.compatibility.status,
              antecedent_predicate_compatibility_reason: candidate.compatibility.reason,
              semantic_review_flags: Array.from(/* @__PURE__ */ new Set([...trace.semantic_review_flags || [], "topic_chain_antecedent_predicate_compatibility_review", "context_required_unresolved"])),
              reason: "An overt topic is available, but the parser cannot license it as this predicate's omitted object unless their broad semantic domains are compatible. No hidden object is inserted.",
              not_claims: Array.from(/* @__PURE__ */ new Set([...trace.not_claims || [], "not_fabricated_object_token", "not_unrestricted_argument_deletion", "not_semantically_incompatible_topic_link"]))
            };
          }
          return {
            topic_chain_id: topicChainId,
            topic_chain_status: "antecedent_predicate_compatibility_review",
            topic_antecedent_surface: antecedent.surface,
            topic_antecedent_construction: antecedent.construction,
            topic_antecedent_source: antecedent.source,
            ...topicFrameTrace,
            ...relationalFrameTrace,
            topic_antecedent_semantic_domains: antecedent.semantic_domains || [],
            linked_null_object_count: 0,
            linked_predicate_surfaces: [],
            unresolved_predicate_surfaces: candidates.map((row) => row.surface),
            unresolved_predicate_heads: candidates.map((row) => row.head_surface),
            null_object_link: "not_licensed_antecedent_predicate_compatibility",
            context_requirement_status: "context_required",
            missing_argument_slots: ["object_or_activity_domain"],
            missing_slot_details: [{ slot: "object_or_activity_domain", license_status: "unresolved" }],
            antecedent_status: blocked.some((row) => row.compatibility.status === "incompatible") ? "incompatible" : "unverified",
            antecedent_span: antecedent.surface,
            discourse_license_not_observed: true,
            semantic_review_flags: ["topic_chain_antecedent_predicate_compatibility_review", "context_required_unresolved"],
            not_claims: ["not_fabricated_object_token", "not_implicit_topic_inference", "not_unrestricted_argument_deletion", "not_semantically_incompatible_topic_link"]
          };
        }
        for (const candidate of candidates) {
          const trace = candidate.node.trace || {};
          candidate.node.trace = {
            ...trace,
            context_requirement_status: "context_licensed",
            missing_argument_slots: ["object_or_activity_domain"],
            missing_slot_details: [{ slot: "object_or_activity_domain", license_status: "licensed", licensed_by: topicChainId }],
            antecedent_status: "linked",
            antecedent_span: antecedent.surface,
            discourse_license_not_observed: false,
            topic_chain_id: topicChainId,
            topic_chain_role: "null_object_target",
            topic_antecedent_surface: antecedent.surface,
            topic_antecedent_source: antecedent.source,
            ...topicFrameTrace,
            ...relationalFrameTrace,
            topic_antecedent_semantic_domains: antecedent.semantic_domains || [],
            null_object: "licensed",
            null_object_link: "licensed_to_overt_topic_chain",
            antecedent_predicate_compatibility_status: "compatible",
            antecedent_predicate_compatibility_reason: candidate.compatibility.reason,
            reason: "An overt nominal topic/domain in the first linked segment licenses this later object-selecting predicate's unspoken object. The visible predicate remains unchanged and no hidden noun token is inserted.",
            not_claims: Array.from(/* @__PURE__ */ new Set([...trace.not_claims || [], "not_fabricated_object_token", "not_context_free_object_drop", "not_unrestricted_argument_deletion"]))
          };
        }
        return {
          topic_chain_id: topicChainId,
          topic_chain_status: "licensed_overt_topic_chain",
          topic_antecedent_surface: antecedent.surface,
          topic_antecedent_construction: antecedent.construction,
          topic_antecedent_source: antecedent.source,
          ...topicFrameTrace,
          ...relationalFrameTrace,
          topic_antecedent_semantic_domains: antecedent.semantic_domains || [],
          linked_null_object_count: candidates.length,
          linked_predicate_surfaces: candidates.map((row) => row.surface),
          linked_predicate_heads: candidates.map((row) => row.head_surface),
          null_object_link: "licensed_to_overt_topic_chain",
          context_requirement_status: "context_licensed",
          missing_argument_slots: ["object_or_activity_domain"],
          missing_slot_details: [{ slot: "object_or_activity_domain", license_status: "licensed", licensed_by: topicChainId }],
          antecedent_status: "linked",
          antecedent_span: antecedent.surface,
          discourse_license_not_observed: false,
          not_claims: ["not_fabricated_object_token", "not_implicit_topic_inference", "not_unrestricted_argument_deletion"]
        };
      }
      return {
        applyTopicChainNullObjectLinkage: applyTopicChainNullObjectLinkage2,
        productiveObjectHeadToken: productiveObjectHeadToken2,
        topicChainAntecedentCompatibility: topicChainAntecedentCompatibility2
      };
    };
  }
});

// src/parser/detectors/aspect/composition.js
var require_composition2 = __commonJS({
  "src/parser/detectors/aspect/composition.js"(exports2, module2) {
    "use strict";
    module2.exports = function createAspectCompositionDetectors2(dependencies = {}) {
      const {
        classifierObjectNPFromNodes: classifierObjectNPFromNodes2,
        cleanSlots: cleanSlots2,
        compositionPartClone: compositionPartClone2,
        construction: construction2,
        constructionSlotsByType: constructionSlotsByType2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function durativeAspectCompositionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length !== 3 || !nodeCanFillSlot2(compact[0], "action_verb") || flattenSurface2(compact[1]) !== "住") return null;
        if (!nodeCanFillSlot2(compact[2], "object") && !nodeCanFillSlot2(compact[2], "head_noun") && !nodeCanFillSlot2(compact[2], "np")) return null;
        const marker = parserInactiveTokenClone2(firstToken2(compact[1]) || token("住"), {
          label: "func",
          pos: "function",
          syntax: "durative_aspect",
          slots: ["durative_aspect", "aspect_marker"],
          jyutping: "zyu6",
          note: "住 marks the continuing wearing/resultant state, distinct from progressive 緊.",
          reason: "住 is interpreted as durative aspect only inside the licensed action + 住 + object pattern."
        });
        const children = [compact[0], marker, compact[2], ...particles];
        return construction2("DurativeVP", "DurativeVP", children, {
          slots: constructionSlotsByType2("DurativeVP", children),
          trace: traceInfo2("generative_template", {
            construction_type: "DurativeVP",
            template_family: "generative_template",
            template: ["action_verb!", "durative_aspect!", "object!", "particle?"],
            assigned_slots: ["action_verb", "durative_aspect", "object", ...particles.map(() => "particle")],
            surfaces: children.map(flattenSurface2),
            aspect_type: "durative_continuing_state",
            not_claims: ["not_progressive_event", "not_global_住_lexicalization"]
          })
        });
      }
      function perfectiveResultCompositionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        let cursor = 0;
        const subject = compact[cursor] && nodeCanFillSlot2(compact[cursor], "subject") ? compact[cursor++] : null;
        if (compact.length - cursor !== 4) return null;
        const [action, result, aspect, object] = compact.slice(cursor);
        if (!nodeCanFillSlot2(action, "action_verb") || !nodeCanFillSlot2(aspect, "perfective_aspect")) return null;
        if (!nodeCanFillSlot2(object, "object") && !nodeCanFillSlot2(object, "head_noun") && !nodeCanFillSlot2(object, "np")) return null;
        const resultSurface = flattenSurface2(result);
        if (!nodeCanFillSlot2(result, "completion_marker") && resultSurface !== "好") return null;
        const innerType = resultSurface === "好" ? "ResultComplementVP" : "CompletionVP";
        const inner = construction2(innerType, resultSurface === "好" ? "ResultVP" : "CompletionVP", [
          action,
          compositionPartClone2(result, {
            label: resultSurface === "好" ? "how" : "func",
            syntax: resultSurface === "好" ? "result_state_complement" : "completion_result_complement",
            slots: resultSurface === "好" ? ["result_complement"] : ["completion_marker", "result_complement"],
            reason: `${resultSurface} is the overt result/phase complement formed with the action before perfective 咗 scopes over the complex predicate.`
          })
        ], {
          slots: cleanSlots2([innerType === "CompletionVP" ? "completion_vp" : "result_complement_vp", "result_complement", "vp", "action_vp", "predicate"]),
          note: "Inner result/phase predicate formed before perfective aspect.",
          trace: traceInfo2("generative_template", {
            construction_type: innerType,
            template_family: "generative_template",
            template: ["action_verb!", "result_or_phase_complement!"],
            assigned_slots: ["action_verb", "result_complement"],
            surfaces: [flattenSurface2(action), resultSurface],
            aspect_scope_status: "inside_perfective_scope"
          })
        });
        const perfective = construction2("PerfectiveVP", "PerfectiveVP", [
          inner,
          compositionPartClone2(aspect, { label: "func", syntax: "perfective_aspect", slots: ["perfective_aspect", "aspect_marker"] }),
          object,
          ...particles
        ], {
          slots: cleanSlots2(["perfective_vp", "vp", "action_vp", "predicate", "perfective_aspect", "object"]),
          note: "Perfective aspect scopes over an already formed result/phase predicate while surface order remains unchanged.",
          trace: traceInfo2("generative_template", {
            construction_type: "PerfectiveVP",
            template_family: "generative_template",
            template: ["result_or_phase_vp!", "perfective_aspect!", "object!", "particle?"],
            assigned_slots: ["predicate", "perfective_aspect", "object", ...particles.map(() => "particle")],
            surfaces: [flattenSurface2(inner), flattenSurface2(aspect), flattenSurface2(object), ...particles.map(flattenSurface2)],
            aspect_scope_status: "perfective_over_result_complex",
            not_claims: ["not_flat_aspect_stack", "not_hidden_object"]
          })
        });
        if (!subject) return perfective;
        const children = [subject, perfective];
        return construction2("SubjectPredicateClause", "SubjPred", children, {
          slots: templateDerivedSlots2("SubjectPredicateClause", children),
          trace: traceInfo2("generative_template", { construction_type: "SubjectPredicateClause", template_family: "generative_template", template: ["subject!", "predicate!"], assigned_slots: ["subject", "predicate"], surfaces: children.map(flattenSurface2) })
        });
      }
      function restorativeRepetitiveComplementFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3 || !nodeCanFillSlot2(compact[0], "action_verb") || !isToken2(compact[1], "返")) return null;
        const action = compact[0];
        const marker = (reading) => compositionPartClone2(compact[1], {
          label: "func",
          syntax: reading === "restorative" ? "restorative_complement_marker" : "repetitive_resumptive_complement_marker",
          slots: reading === "restorative" ? ["verb_complement", "restorative_complement"] : ["verb_complement", "repetitive_complement"],
          reason: reading === "restorative" ? "返 marks restoration of a prior or expected state here; it is not literal return motion." : "返 marks repetition or resumption of the overt action here; it is not literal return motion."
        });
        const thirdSurface = flattenSurface2(compact[2]);
        if (thirdSurface === "好" && compact.length === 3) {
          const children = [action, marker("restorative"), compositionPartClone2(compact[2], { label: "how", syntax: "restored_result_state", slots: ["result_complement"] }), ...particles];
          return construction2("RestorativeComplementVP", "RestoreVP", children, { slots: constructionSlotsByType2("RestorativeComplementVP", children), trace: traceInfo2("generative_template", { construction_type: "RestorativeComplementVP", template_family: "generative_template", template: ["action_verb!", "restorative_complement!", "result_state!", "particle?"], assigned_slots: ["action_verb", "restorative_complement", "result_complement", ...particles.map(() => "particle")], surfaces: children.map(flattenSurface2), return_reading: "restorative", not_claims: ["not_literal_return_motion", "not_generic_aspect"] }) });
        }
        if (compact.length === 3 && (nodeCanFillSlot2(compact[2], "frequency_quantity") || /(^|\s)frequency_quantity(\s|$)/.test((firstToken2(compact[2]) || {}).syntax || ""))) {
          const children = [action, marker("repetitive"), compact[2], ...particles];
          return construction2("RepetitiveComplementVP", "RepeatVP", children, { slots: constructionSlotsByType2("RepetitiveComplementVP", children), trace: traceInfo2("generative_template", { construction_type: "RepetitiveComplementVP", template_family: "generative_template", template: ["action_verb!", "repetitive_complement!", "frequency_quantity!", "particle?"], assigned_slots: ["action_verb", "repetitive_complement", "frequency_quantity", ...particles.map(() => "particle")], surfaces: children.map(flattenSurface2), return_reading: "repetitive_or_resumptive", not_claims: ["not_literal_return_motion", "not_generic_aspect"] }) });
        }
        if (compact.length === 4) {
          const object = classifierObjectNPFromNodes2(compact.slice(2));
          if (object) {
            const actionSlots = new Set(nodeSlots2(action));
            const actionSyntax = String((firstToken2(action) || {}).syntax || "");
            const licensesResumptiveObjectReading = actionSlots.has("consumption_verb") || actionSyntax.includes("chain_select_perception") || actionSyntax.includes("chain_select_discourse_content");
            const reading = licensesResumptiveObjectReading ? "repetitive" : "underdetermined";
            const returnPart = reading === "repetitive" ? marker("repetitive") : compositionPartClone2(compact[1], {
              label: "func",
              syntax: "return_or_resumptive_complement_marker",
              slots: ["verb_complement"],
              reason: "返 is a non-finite complement marker here. Available evidence does not force literal motion or a repetitive reading."
            });
            const children = [action, returnPart, object, ...particles];
            const type = reading === "repetitive" ? "RepetitiveComplementVP" : "VerbComplementVP";
            const label = reading === "repetitive" ? "RepeatVP" : "VerbCompVP";
            return construction2(type, label, children, {
              slots: constructionSlotsByType2(type, children),
              trace: traceInfo2("generative_template", {
                construction_type: type,
                template_family: "generative_template",
                template: ["action_verb!", reading === "repetitive" ? "repetitive_complement!" : "return_or_resumptive_complement!", "object!", "particle?"],
                assigned_slots: ["action_verb", reading === "repetitive" ? "repetitive_complement" : "verb_complement", "object", ...particles.map(() => "particle")],
                surfaces: children.map(flattenSurface2),
                return_reading: reading === "repetitive" ? "repetitive_or_resumptive" : "context_underdetermined_return_or_resumptive",
                not_claims: reading === "repetitive" ? ["not_literal_return_motion", "not_generic_aspect"] : ["not_forced_literal_return_motion", "not_forced_repetitive_reading", "not_generic_aspect"]
              })
            });
          }
        }
        return null;
      }
      function incompatibleAspectCompositionMalformedCandidate2(core) {
        if (!core || core.length < 3) return null;
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        let cursor = 0;
        const subject = bareCore[cursor] && nodeCanFillSlot2(bareCore[cursor], "subject") ? bareCore[cursor++] : null;
        const action = bareCore[cursor++];
        if (!action || !nodeCanFillSlot2(action, "action_verb")) return null;
        const firstMarker = bareCore[cursor++];
        const secondMarker = bareCore[cursor++];
        if (!firstMarker || !secondMarker) return null;
        let malformedSubtype = "";
        let problem = "";
        let expected = [];
        if (nodeCanFillSlot2(firstMarker, "perfective_aspect") && nodeCanFillSlot2(secondMarker, "progressive_aspect")) {
          malformedSubtype = "incompatible_perfective_progressive_stack";
          problem = "Perfective 咗 and progressive 緊 are stacked in an incompatible order on one predicate.";
          expected = ["one_licensed_aspect_layer", "separate_clause_or_repair"];
        } else if ((isToken2(firstMarker, "得") || isToken2(firstMarker, "唔")) && nodeCanFillSlot2(secondMarker, "perfective_aspect")) {
          malformedSubtype = isToken2(firstMarker, "得") ? "potential_marker_followed_by_perfective" : "potential_negator_followed_by_perfective";
          problem = "Potential 得/唔 is followed by perfective 咗 instead of an overt result complement.";
          expected = ["overt_result_complement_after_potential_marker", "ordinary_perfective_vp_without_potential_marker"];
        } else if (["嚟", "去"].includes(flattenSurface2(firstMarker)) && ["入", "出", "返", "上", "落"].includes(flattenSurface2(secondMarker))) {
          malformedSubtype = "deictic_marker_not_outermost";
          problem = "Final deictic 嚟/去 precedes a path or return component instead of occupying the outer edge of the directional complex.";
          expected = ["path_or_return_before_final_deictic_marker", "separate_clause_or_repair"];
        } else {
          return null;
        }
        const children = [...subject ? [subject] : [], action, firstMarker, secondMarker, ...bareCore.slice(cursor), ...particles];
        return construction2("MalformedCandidate", "Malformed", children, {
          slots: cleanSlots2(["malformed_candidate", "needs_review", "predicate", "problem_span", "action_verb", subject ? "subject" : ""]),
          note: "Review-bearing incompatible aspect/potential composition; all visible material is preserved without repairing the learner input.",
          trace: traceInfo2("special_ambiguity_rule", {
            construction_type: "MalformedCandidate",
            malformed_family: "aspect_result_potential_composition",
            malformed_subtype: malformedSubtype,
            template: ["subject?", "action_verb!", "incompatible_marker_1!", "incompatible_marker_2!", "remainder?", "particle?"],
            assigned_slots: [...subject ? ["subject"] : [], "action_verb", "incompatible_marker_1", "incompatible_marker_2", ...bareCore.slice(cursor).length ? ["remainder"] : [], ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            problem,
            expected_repairs: expected,
            semantic_review_flags: ["malformed_candidate_parse", "incompatible_aspect_or_potential_order"],
            not_claims: ["not_clean_aspect_stack", "not_hidden_result_complement", "not_silent_input_repair"],
            reason: "Aspect, potential, and result layers must compose in a licensed order; incompatible visible markers remain review-bearing."
          })
        });
      }
      return {
        durativeAspectCompositionFallback: durativeAspectCompositionFallback2,
        perfectiveResultCompositionFallback: perfectiveResultCompositionFallback2,
        restorativeRepetitiveComplementFallback: restorativeRepetitiveComplementFallback2,
        incompatibleAspectCompositionMalformedCandidate: incompatibleAspectCompositionMalformedCandidate2
      };
    };
  }
});

// src/parser/detectors/environmental/clauses.js
var require_clauses4 = __commonJS({
  "src/parser/detectors/environmental/clauses.js"(exports2, module2) {
    "use strict";
    module2.exports = function createDetectors(dependencies = {}) {
      const {
        ENVIRONMENTAL_EVENT_PREDICATES: ENVIRONMENTAL_EVENT_PREDICATES2,
        categorySubspanFor: categorySubspanFor2,
        construction: construction2,
        constructionSlotsByType: constructionSlotsByType2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function temporalClauseFallback2(core = []) {
        if (core.length < 2 || !firstToken2(core[0]) || firstToken2(core[0]).label !== "when") return null;
        if (core.length === 2 && nodeCanFillSlot2(core[0], "movement_direction") && nodeCanFillSlot2(core[1], "deictic_motion_marker")) return null;
        return construction2("TemporalClause", "Time", core, {
          note: "Time expression fallback frames the following predicate.",
          trace: traceInfo2("construction_function", {
            construction_type: "TemporalClause",
            reason: "Fallback only; generative TemporalClause should normally catch this."
          })
        });
      }
      function conventionalEnvironmentalEventConstruction2(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length !== 2 || compact.some((node) => node.kind === "construction" || node.kind === "text")) return null;
        const eventSurface = compact.map((node) => flattenSurface2(node)).join("");
        const eventRule = ENVIRONMENTAL_EVENT_PREDICATES2[eventSurface];
        if (!eventRule || flattenSurface2(compact[0]) !== eventRule.head || flattenSurface2(compact[1]) !== eventRule.phenomenon) return null;
        const head = parserInactiveTokenClone2(compact[0], {
          label: "doing",
          syntax: "environmental_event_predicate",
          slots: ["environmental_event_head", "environmental_predicate", "predicate"],
          reason: `${eventRule.head} is licensed as an environmental event head only inside the conventional ${eventSurface} predicate.`,
          active_affordance_match: { role: "doing", slot: "environmental_event_head", source: "construction_override" },
          preserve_existing_affordances: true
        });
        const phenomenon = parserInactiveTokenClone2(compact[1], {
          label: "what",
          syntax: "weather_phenomenon environmental_phenomenon",
          slots: ["weather_phenomenon", "environmental_phenomenon"],
          reason: `${eventRule.phenomenon} is the visible weather-phenomenon component of ${eventSurface}, not an ordinary affected object.`,
          active_affordance_match: { role: "what", slot: "weather_phenomenon", source: "construction_override" },
          preserve_existing_affordances: true
        });
        return construction2("ImpersonalEnvironmentalClause", "Environment", [head, phenomenon], {
          slots: constructionSlotsByType2("ImpersonalEnvironmentalClause", [head, phenomenon]),
          note: "Lexically licensed conventional environmental event predicate with transparent visible components.",
          trace: traceInfo2("generative_template", {
            construction_type: "ImpersonalEnvironmentalClause",
            template_family: "construction_template",
            template: ["environmental_event_head!", "weather_phenomenon!"],
            assigned_slots: ["environmental_event_head", "weather_phenomenon"],
            surfaces: [eventRule.head, eventRule.phenomenon],
            subject_status: "impersonal",
            subjectless_type: "genuinely_subjectless_environmental",
            hidden_subject_inserted: false,
            environmental_subtype: eventRule.environmental_subtype,
            not_claims: ["not_productive_vo_object_relation", "not_null_referential_subject", "not_hidden_expletive_subject"]
          })
        });
      }
      function environmentalPredicateParts(core = []) {
        const compact = withoutIgnorableSpaceText2(core || []);
        if (!compact.length) return null;
        const conventionalEnvironmental = conventionalEnvironmentalEventConstruction2(compact);
        if (conventionalEnvironmental) {
          return {
            predicate: conventionalEnvironmental,
            environmental_subtype: (conventionalEnvironmental.trace || {}).environmental_subtype || "environmental_event"
          };
        }
        const event = categorySubspanFor2(compact, ["ImpersonalEnvironmentalClause"]);
        if (event) {
          return {
            predicate: event,
            environmental_subtype: flattenSurface2(event) === "落雨" ? "precipitation_event" : "wind_event"
          };
        }
        if (compact.length === 1 && nodeCanFillSlot2(compact[0], "environmental_transition_predicate")) {
          const child = parserInactiveTokenClone2(compact[0], {
            label: "doing",
            syntax: "environmental_transition_predicate impersonal_predicate",
            slots: ["environmental_transition_predicate", "environmental_predicate", "predicate"],
            reason: "天光 is a visible environmental transition predicate; no null referential or expletive subject is inserted.",
            active_affordance_match: { role: "doing", slot: "environmental_transition_predicate", source: "construction_override" },
            preserve_existing_affordances: true
          });
          const predicate = construction2("ImpersonalEnvironmentalClause", "Environment", [child], {
            slots: constructionSlotsByType2("ImpersonalEnvironmentalClause", [child]),
            note: "Genuinely subjectless environmental transition clause.",
            trace: traceInfo2("generative_template", {
              construction_type: "ImpersonalEnvironmentalClause",
              template_family: "generative_template",
              template: ["environmental_transition_predicate!"],
              assigned_slots: ["environmental_transition_predicate"],
              surfaces: [flattenSurface2(child)],
              subject_status: "impersonal",
              subjectless_type: "genuinely_subjectless_environmental",
              hidden_subject_inserted: false,
              environmental_subtype: "daylight_transition",
              not_claims: ["not_null_referential_subject", "not_hidden_expletive_subject", "not_nominal_subject天"]
            })
          });
          return { predicate, environmental_subtype: "daylight_transition" };
        }
        const ambient = categorySubspanFor2(compact, ["DegreeStativePredicate"]);
        if (ambient && nodeCanFillSlot2(ambient, "ambient_environmental_predicate")) {
          return { predicate: ambient, environmental_subtype: "ambient_temperature_property" };
        }
        return null;
      }
      function impersonalEnvironmentalClauseFallback2(core = []) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core || []);
        if (!bareCore.length) return null;
        if (bareCore.length >= 2 && nodeCanFillSlot2(bareCore[0], "time")) {
          const environmental2 = environmentalPredicateParts(bareCore.slice(1));
          if (environmental2) {
            const child = environmental2.predicate.type === "ImpersonalEnvironmentalClause" ? environmental2.predicate : construction2("ImpersonalEnvironmentalClause", "Environment", [environmental2.predicate], {
              slots: constructionSlotsByType2("ImpersonalEnvironmentalClause", [environmental2.predicate]),
              note: "Ambient environmental property licensed by an overt temporal frame.",
              trace: traceInfo2("generative_template", {
                construction_type: "ImpersonalEnvironmentalClause",
                template_family: "generative_template",
                template: ["ambient_environmental_predicate!"],
                assigned_slots: ["ambient_environmental_predicate"],
                surfaces: [flattenSurface2(environmental2.predicate)],
                subject_status: "impersonal",
                subjectless_type: "genuinely_subjectless_environmental",
                hidden_subject_inserted: false,
                environmental_subtype: environmental2.environmental_subtype,
                not_claims: ["not_null_referential_subject", "not_hidden_expletive_subject"]
              })
            });
            const children2 = [bareCore[0], child, ...particles];
            return construction2("TemporalClause", "Time", children2, {
              slots: templateDerivedSlots2("TemporalClause", children2),
              note: "Time-framed environmental clause with no fabricated referential subject.",
              trace: traceInfo2("generative_template", {
                construction_type: "TemporalClause",
                template_family: "generative_template",
                template: ["time!", "impersonal_environmental_clause!", "particle?"],
                assigned_slots: ["time", "impersonal_environmental_clause", ...particles.map(() => "particle")],
                surfaces: children2.map(flattenSurface2),
                clause_modifier_profile: "temporal_environmental_frame",
                subject_status: "impersonal",
                subjectless_type: "genuinely_subjectless_environmental",
                hidden_subject_inserted: false,
                not_claims: ["not_null_referential_subject", "not_hidden_expletive_subject"]
              })
            });
          }
        }
        if (bareCore.length >= 2 && (nodeCanFillSlot2(bareCore[0], "ambient_location_frame") || nodeCanFillSlot2(bareCore[0], "location"))) {
          const environmental2 = environmentalPredicateParts(bareCore.slice(1));
          if (environmental2 && environmental2.environmental_subtype === "ambient_temperature_property") {
            const ambientClause = construction2("ImpersonalEnvironmentalClause", "Environment", [environmental2.predicate], {
              slots: constructionSlotsByType2("ImpersonalEnvironmentalClause", [environmental2.predicate]),
              note: "Ambient temperature proposition licensed by an overt spatial frame.",
              trace: traceInfo2("generative_template", {
                construction_type: "ImpersonalEnvironmentalClause",
                template_family: "generative_template",
                template: ["ambient_environmental_predicate!"],
                assigned_slots: ["ambient_environmental_predicate"],
                surfaces: [flattenSurface2(environmental2.predicate)],
                subject_status: "impersonal",
                subjectless_type: "genuinely_subjectless_environmental",
                hidden_subject_inserted: false,
                environmental_subtype: environmental2.environmental_subtype,
                not_claims: ["not_null_referential_subject", "not_hidden_expletive_subject"]
              })
            });
            const location = parserInactiveTokenClone2(bareCore[0], {
              label: "where",
              syntax: "ambient_location_frame spatial_localizer",
              slots: ["location", "ambient_location_frame"],
              reason: "The overt place expression frames the ambient proposition; its grammatical subject/topic status is not forced.",
              active_affordance_match: { role: "where", slot: "ambient_location_frame", source: "construction_override" },
              preserve_existing_affordances: true
            });
            const children2 = [location, ambientClause, ...particles];
            return construction2("LocativeFrameClause", "LocativeFrame", children2, {
              slots: constructionSlotsByType2("LocativeFrameClause", children2),
              note: "Location-framed ambient clause. Location semantics are represented independently from grammatical subjecthood.",
              trace: traceInfo2("generative_template", {
                construction_type: "LocativeFrameClause",
                template_family: "generative_template",
                template: ["ambient_location_frame!", "impersonal_environmental_clause!", "particle?"],
                assigned_slots: ["ambient_location_frame", "impersonal_environmental_clause", ...particles.map(() => "particle")],
                surfaces: children2.map(flattenSurface2),
                location_relation: "ambient_frame_not_forced_subject_or_topic",
                subject_status: "impersonal",
                subjectless_type: "location_framed_ambient",
                hidden_subject_inserted: false,
                not_claims: ["not_location_as_forced_subject", "not_location_as_forced_topic", "not_hidden_expletive_subject"]
              })
            });
          }
        }
        const environmental = environmentalPredicateParts(bareCore);
        if (!environmental || environmental.environmental_subtype === "ambient_temperature_property") return null;
        const predicate = environmental.predicate;
        const children = [...predicate.children || [predicate], ...particles];
        return construction2("ImpersonalEnvironmentalClause", "Environment", children, {
          slots: constructionSlotsByType2("ImpersonalEnvironmentalClause", children),
          note: "Genuinely subjectless environmental clause; all visible predicate material remains transparent.",
          trace: traceInfo2("generative_template", {
            construction_type: "ImpersonalEnvironmentalClause",
            template_family: predicate.trace && predicate.trace.template_family ? predicate.trace.template_family : "generative_template",
            template: predicate.trace && predicate.trace.template ? predicate.trace.template : ["environmental_predicate!", "particle?"],
            assigned_slots: predicate.trace && predicate.trace.assigned_slots ? [...predicate.trace.assigned_slots, ...particles.map(() => "particle")] : ["environmental_predicate", ...particles.map(() => "particle")],
            surfaces: children.map(flattenSurface2),
            subject_status: "impersonal",
            subjectless_type: "genuinely_subjectless_environmental",
            hidden_subject_inserted: false,
            environmental_subtype: environmental.environmental_subtype,
            not_claims: ["not_productive_vo_object_relation", "not_null_referential_subject", "not_hidden_expletive_subject"]
          })
        });
      }
      return {
        conventionalEnvironmentalEventConstruction: conventionalEnvironmentalEventConstruction2,
        environmentalPredicateParts,
        impersonalEnvironmentalClauseFallback: impersonalEnvironmentalClauseFallback2,
        temporalClauseFallback: temporalClauseFallback2
      };
    };
  }
});

// src/parser/detectors/existential/spatial.js
var require_spatial = __commonJS({
  "src/parser/detectors/existential/spatial.js"(exports2, module2) {
    "use strict";
    module2.exports = function createDetectors(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        fullSpanSingleConstruction: fullSpanSingleConstruction2,
        isToken: isToken2,
        locativePredicatePhraseFromNodes: locativePredicatePhraseFromNodes2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function spatialLocalizerPhraseFromNodes(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length !== 2) return null;
        const [base, localizer] = compact;
        const baseLooksNominal = nodeCanFillSlot2(base, "np") || nodeCanFillSlot2(base, "head_noun") || nodeCanFillSlot2(base, "object") || nodeCanFillSlot2(base, "location");
        const localizerSyntax = String(localizer && localizer.syntax || "");
        if (!baseLooksNominal || !localizerSyntax.includes("spatial_localizer")) return null;
        const localizerChild = parserInactiveTokenClone2(localizer, {
          label: "where",
          syntax: `${localizerSyntax} postnominal_spatial_localizer`,
          slots: ["location", "spatial_localizer", "locative_domain"],
          reason: "The postnominal localizer supplies the spatial relation for the visible nominal base.",
          active_affordance_match: { role: "where", slot: "spatial_localizer", source: "construction_override" },
          preserve_existing_affordances: true
        });
        const children = [base, localizerChild];
        return construction2("LocativePlacePhrase", "Location", children, {
          note: "Nominal location base plus postnominal spatial localizer.",
          slots: cleanSlots2(["locative_phrase", "location", "goal", "locative_domain", "spatial_localizer", ...templateDerivedSlots2("LocativePlacePhrase", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "LocativePlacePhrase",
            template_family: "generative_template",
            template: ["location_base!", "spatial_localizer!"],
            assigned_slots: ["location_base", "spatial_localizer"],
            surfaces: children.map(flattenSurface2),
            subspan: true,
            location_relation: "nominal_base_plus_postnominal_localizer",
            subject_status: "not_assigned",
            not_claims: ["not_temporal_modifier", "not_directional_motion", "not_forced_subject"]
          })
        });
      }
      function existentialNPFromNodes(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        if (compact.length === 1 && (nodeCanFillSlot2(compact[0], "np") || nodeCanFillSlot2(compact[0], "head_noun") || nodeCanFillSlot2(compact[0], "object"))) return compact[0];
        const wrapped = applyConstructionPatterns2(compact);
        const full = fullSpanSingleConstruction2(wrapped, compact);
        if (full && (nodeCanFillSlot2(full, "np") || nodeCanFillSlot2(full, "head_noun") || nodeCanFillSlot2(full, "object"))) return full;
        return null;
      }
      function locativeDomainPrefix(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        if (compact.length >= 2) {
          const phrase = spatialLocalizerPhraseFromNodes(compact.slice(0, 2));
          if (phrase) return { node: phrase, consumed: 2 };
        }
        const first = compact[0];
        if (nodeCanFillSlot2(first, "location") || nodeCanFillSlot2(first, "goal")) {
          const location = first.kind === "token" ? parserInactiveTokenClone2(first, {
            label: "where",
            syntax: `${first.syntax || "place_or_goal"} locative_domain`,
            slots: ["location", "locative_domain"],
            reason: "The overt place expression establishes the spatial domain without being forced into grammatical subject or topic status.",
            active_affordance_match: { role: "where", slot: "locative_domain", source: "construction_override" },
            preserve_existing_affordances: true
          }) : first;
          return { node: location, consumed: 1 };
        }
        return null;
      }
      function presentationalLocativeCodaFromNodes(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        if (compact.length === 1 && isToken2(compact[0], "喺度")) {
          const child = parserInactiveTokenClone2(compact[0], {
            label: "where",
            pos: "location",
            syntax: "locative_deictic presentational_location_coda",
            slots: ["locative_phrase", "location", "presentational_coda"],
            reason: "After an introduced existential participant, 喺度 is a visible locative coda rather than progressive aspect.",
            active_affordance_match: { role: "where", slot: "presentational_coda", source: "construction_override" },
            preserve_existing_affordances: true
          });
          return construction2("LocativePlacePhrase", "Location", [child], {
            note: "Deictic locative coda inside an existential-presentational clause.",
            slots: cleanSlots2(["locative_phrase", "location", "presentational_coda"]),
            trace: traceInfo2("generative_template", {
              construction_type: "LocativePlacePhrase",
              template_family: "generative_template",
              template: ["presentational_location_coda!"],
              assigned_slots: ["presentational_coda"],
              surfaces: [flattenSurface2(child)],
              subspan: true,
              predicate_subtype: "presentational_locative_coda",
              not_claims: ["not_progressive_aspect"]
            })
          });
        }
        const hasExplicitLocativeMarker = compact.some((node) => isToken2(node, "喺") || isToken2(node, "喺度"));
        return hasExplicitLocativeMarker ? locativePredicatePhraseFromNodes2(compact) : null;
      }
      function placementPerfectiveVPFromNodes(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length < 4) return null;
        const [verb, aspect, ...themeNodes] = compact;
        if (!String(verb && verb.syntax || "").includes("positioning_verb")) return null;
        if (!nodeCanFillSlot2(aspect, "perfective_aspect") && !isToken2(aspect, "咗")) return null;
        const theme = existentialNPFromNodes(themeNodes);
        if (!theme) return null;
        const predicate = parserInactiveTokenClone2(verb, {
          label: "doing",
          syntax: "positioning_action_predicate locative_inversion_predicate",
          slots: ["action_verb", "main_verb", "predicate", "positioning_predicate"],
          reason: "The visible positioning verb predicates the postverbal theme inside a locative-inversion frame."
        });
        const children = [predicate, aspect, theme];
        return construction2("PerfectiveVP", "PerfVP", children, {
          note: "Perfective positioning predicate with an overt postverbal theme.",
          slots: cleanSlots2(["perfective_vp", "vp", "action_vp", "predicate", "perfective_aspect", "object", "theme", ...templateDerivedSlots2("PerfectiveVP", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "PerfectiveVP",
            template_family: "generative_template",
            template: ["positioning_predicate!", "perfective_aspect!", "theme!"],
            assigned_slots: ["positioning_predicate", "perfective_aspect", "theme"],
            surfaces: children.map(flattenSurface2),
            subspan: true,
            event_subtype: "perfective_positioning",
            not_claims: ["not_objectless_perfective"]
          })
        });
      }
      function nodeHasAnySlot(node, slots = []) {
        return slots.some((slot) => nodeCanFillSlot2(node, slot));
      }
      function nodeChildren(node) {
        return Array.isArray(node && node.children) ? node.children : [];
      }
      function nodeOrDescendantHasAnySlot(node, slots = []) {
        if (!node) return false;
        if (nodeHasAnySlot(node, slots)) return true;
        return nodeChildren(node).some((child) => nodeOrDescendantHasAnySlot(child, slots));
      }
      function introducedParticipantAllowed(participant) {
        if (!participant) return false;
        const surface = flattenSurface2(participant);
        if (!surface || /^啲/u.test(surface)) return false;
        if (/^(一日|一晚|一朝|一陣|一次|一段時間|可能|機會|事)/u.test(surface)) return false;
        if (nodeOrDescendantHasAnySlot(participant, ["time", "time_head", "quantified_time_np", "locative_phrase", "location", "goal", "vp", "action_vp", "predicate"])) return false;
        const trace = participant.trace || {};
        if (trace.fragment_subtype === "quantified_time_fragment") return false;
        if (trace.np_license_status && trace.construction_licensing_allowed === false) return false;
        return nodeHasAnySlot(participant, ["np", "head_noun", "object", "subject", "topic"]);
      }
      function presentationalPredicateSubtype(predicate) {
        if (!predicate) return "predicate";
        if (predicate.type === "LocativePlacePhrase") return "locative_predicate";
        if (predicate.type === "SubjectPredicateClause") return "subject_predicate";
        if (nodeCanFillSlot2(predicate, "locative_predicate") || nodeCanFillSlot2(predicate, "locative_phrase")) return "locative_predicate";
        if (nodeCanFillSlot2(predicate, "modal_vp") || predicate.type === "ModalVP") return "modal_predicate";
        if (nodeCanFillSlot2(predicate, "stative_predicate") || predicate.type === "StativePredicate" || predicate.type === "DegreeStativePredicate") return "property_predicate";
        if (nodeCanFillSlot2(predicate, "perfective_vp") || predicate.type === "PerfectiveVP") return "perfective_verbal_predicate";
        if (nodeCanFillSlot2(predicate, "copular_relation") || predicate.type === "CopularRelationFrame" || predicate.type === "CopularIdentificationFrame") return "copular_predicate";
        if (nodeCanFillSlot2(predicate, "vp") || nodeCanFillSlot2(predicate, "action_vp")) return "verbal_predicate";
        return "predicate";
      }
      function aa56PredicateFallbackFromNodes(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        const first = compact[0];
        const firstIsPredicate = nodeHasAnySlot(first, ["predicate", "action_verb", "main_verb", "movement_verb", "stative_predicate", "modal_vp", "modal", "perfective_vp", "vp", "action_vp"]);
        if (!firstIsPredicate) return null;
        if (nodeHasAnySlot(first, ["np", "head_noun", "object", "time", "time_head", "location", "goal"]) && !nodeHasAnySlot(first, ["predicate", "action_verb", "main_verb", "movement_verb", "stative_predicate"])) return null;
        const slots = ["subject_predicate_clause", "predicate", "np_linked_predicate", "clause"];
        if (nodeHasAnySlot(first, ["action_verb", "main_verb", "movement_verb", "vp", "action_vp"])) slots.push("vp", "action_vp");
        if (nodeHasAnySlot(first, ["stative_predicate"])) slots.push("stative_predicate");
        if (compact.some((node) => nodeCanFillSlot2(node, "perfective_aspect"))) slots.push("perfective_vp");
        const head = first.kind === "token" ? parserInactiveTokenClone2(first, {
          label: first.label || (nodeCanFillSlot2(first, "stative_predicate") ? "like" : "doing"),
          syntax: `${first.syntax || "predicate"} aa56_np_linked_predicate_head`,
          slots: cleanSlots2([...first.slots || [], "predicate", "np_linked_predicate"]),
          reason: "This predicate is linked to the overt participant introduced by positive 有 in AA56.",
          preserve_existing_affordances: true
        }) : first;
        const children = [head, ...compact.slice(1)];
        return construction2("SubjectPredicateClause", "Clause", children, {
          note: "Predicate linked to the overt participant introduced by positive 有; no hidden subject is inserted.",
          slots: cleanSlots2([...slots, ...templateDerivedSlots2("SubjectPredicateClause", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            template: ["np_linked_predicate_head!", "complement_or_aspect*"],
            assigned_slots: ["np_linked_predicate_head", ...compact.slice(1).map(() => "complement_or_aspect")],
            surfaces: children.map(flattenSurface2),
            subspan: true,
            predicate_relation: "linked_to_aa56_introduced_np",
            subject_status: "supplied_by_aa56_introduced_participant",
            hidden_subject_inserted: false,
            not_claims: ["not_independent_subject_predicate_claim", "not_hidden_subject"]
          })
        });
      }
      function presentationalPredicateFromNodes(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        const locative = presentationalLocativeCodaFromNodes(compact);
        if (locative) return { node: locative, assignedSlot: "np_linked_predicate", subtype: "locative_predicate" };
        const wrapped = applyConstructionPatterns2(compact);
        const predicate = fullSpanSingleConstruction2(wrapped, compact) || (compact.length === 1 ? compact[0] : null);
        if (predicate) {
          if (!nodeHasAnySlot(predicate, ["predicate", "vp", "action_vp", "stative_predicate", "modal_vp", "perfective_vp", "locative_predicate", "locative_phrase"])) return null;
          if (nodeHasAnySlot(predicate, ["np", "head_noun", "object"]) && !nodeHasAnySlot(predicate, ["predicate", "vp", "action_vp", "stative_predicate", "modal_vp", "perfective_vp"])) return null;
          return { node: predicate, assignedSlot: "np_linked_predicate", subtype: presentationalPredicateSubtype(predicate) };
        }
        const fallback = aa56PredicateFallbackFromNodes(compact);
        if (!fallback) return null;
        return { node: fallback, assignedSlot: "np_linked_predicate", subtype: presentationalPredicateSubtype(fallback) };
      }
      function positiveParticipantIntroductionConstruction(marker, participant, predicateInfo, particles = []) {
        if (!marker || !participant || !predicateInfo) return null;
        const predicate = parserInactiveTokenClone2(marker, {
          label: "func",
          syntax: "positive_existential_participant_introduction_marker",
          slots: ["existential", "participant_introduction_marker", "presentational_predicate", "predicate"],
          reason: `${flattenSurface2(marker)} introduces an overt indefinite participant before a linked predicate.`
        });
        const children = [predicate, participant, predicateInfo.node, ...particles];
        return construction2("ExistentialPresentationalClause", "Presentational", children, {
          note: "AA56 positive participant introduction: 有 + overt indefinite NP + overt NP-linked predicate. The legacy runtime label is retained as the compatibility surface for JauMarkedIndefiniteNPPredication.",
          slots: cleanSlots2(["existential_presentational_clause", "existential_clause", "predicate", "existential", "participant_introduction_marker", "introduced_participant", "np_linked_predicate", "clause", ...templateDerivedSlots2("ExistentialPresentationalClause", children)]),
          trace: traceInfo2("generative_template", {
            construction_type: "ExistentialPresentationalClause",
            template_family: "generative_template",
            template: ["existential!", "introduced_participant!", `${predicateInfo.assignedSlot}!`, "particle?"],
            assigned_slots: ["existential", "introduced_participant", predicateInfo.assignedSlot, ...particles.map(() => "particle")],
            surfaces: children.map(flattenSurface2),
            construction_uuid: "258c1d00-8a77-543c-a26f-2e66d3a37849",
            construction_code: "AA56",
            canonical_identity: "JauMarkedIndefiniteNPPredication",
            legacy_runtime_label: "ExistentialPresentationalClause",
            existential_subtype: "positive_indefinite_np_predication",
            predicate_subtype: predicateInfo.subtype,
            polarity: "positive",
            marker: "有",
            have_relation: "participant_introduction",
            subject_status: "impersonal",
            subjectless_type: "genuinely_subjectless_existential_presentational",
            hidden_subject_inserted: false,
            introduced_participant: "overt",
            predicate_relation: "linked_to_introduced_np",
            introduced_participant_surface: flattenSurface2(participant),
            np_linked_predicate_surface: flattenSurface2(predicateInfo.node),
            not_claims: ["not_negative_member", "not_possessive_have", "not_bare_existence", "not_hidden_subject", "not_mandatory_locative_coda", "not_unrestricted_any_np_any_predicate"]
          })
        });
      }
      function positiveParticipantIntroductionFromNodes(nodes = [], particles = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length < 3 || !isToken2(compact[0], "有")) return null;
        for (let split = 2; split < compact.length; split += 1) {
          const participant = existentialNPFromNodes(compact.slice(1, split));
          if (!introducedParticipantAllowed(participant)) continue;
          const predicateInfo = presentationalPredicateFromNodes(compact.slice(split));
          if (!predicateInfo) continue;
          return positiveParticipantIntroductionConstruction(compact[0], participant, predicateInfo, particles);
        }
        return null;
      }
      function existentialLocationPresentationalFallback2(core = []) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (!compact.length) return null;
        const existentialIndex = compact.findIndex((node) => isToken2(node, "有") || isToken2(node, "冇"));
        if (existentialIndex > 0 && existentialIndex < compact.length - 1) {
          const locationPrefix2 = locativeDomainPrefix(compact.slice(0, existentialIndex));
          if (locationPrefix2 && locationPrefix2.consumed === existentialIndex) {
            if (isToken2(compact[existentialIndex], "有")) {
              const aa56Core = positiveParticipantIntroductionFromNodes(compact.slice(existentialIndex), []);
              if (aa56Core) {
                const children = [locationPrefix2.node, aa56Core, ...particles];
                return construction2("LocativeExistentialClause", "LocExist", children, {
                  note: "Overt spatial-domain layer composed with an AA56 positive participant-introduction core.",
                  slots: cleanSlots2(["locative_existential_clause", "existential_clause", "location", "locative_domain", "predicate", "introduced_theme", "clause", ...templateDerivedSlots2("LocativeExistentialClause", children)]),
                  trace: traceInfo2("generative_template", {
                    construction_type: "LocativeExistentialClause",
                    template_family: "generative_template",
                    template: ["locative_domain!", "existential_presentational_clause!", "particle?"],
                    assigned_slots: ["locative_domain", "existential_presentational_clause", ...particles.map(() => "particle")],
                    surfaces: children.map(flattenSurface2),
                    existential_subtype: "locative_domain_plus_positive_participant_predication",
                    polarity: "positive",
                    have_relation: "existence_plus_participant_introduction",
                    location_relation: "external_overt_spatial_domain_not_absorbed_into_aa56_core",
                    subject_status: "impersonal",
                    hidden_subject_inserted: false,
                    not_claims: ["not_possessor_subject", "not_location_as_forced_subject", "not_location_as_forced_topic", "not_hidden_expletive_subject"]
                  })
                });
              }
            }
            const theme = existentialNPFromNodes(compact.slice(existentialIndex + 1));
            if (theme) {
              const marker = compact[existentialIndex];
              const negative = isToken2(marker, "冇");
              const predicate = parserInactiveTokenClone2(marker, {
                label: "func",
                syntax: negative ? "negated_locative_existential_predicate" : "locative_existential_predicate",
                slots: [negative ? "negated_existential" : "existential", "locative_existential_predicate", "predicate"],
                reason: `${flattenSurface2(marker)} predicates nonexistence/existence inside the overt spatial domain.`
              });
              const children = [locationPrefix2.node, predicate, theme, ...particles];
              return construction2("LocativeExistentialClause", "LocExist", children, {
                note: "Locative existential clause with overt spatial domain and introduced NP.",
                slots: cleanSlots2(["locative_existential_clause", "existential_clause", "location", "locative_domain", "predicate", negative ? "negated_existential" : "existential", "introduced_theme", "clause", ...templateDerivedSlots2("LocativeExistentialClause", children)]),
                trace: traceInfo2("generative_template", {
                  construction_type: "LocativeExistentialClause",
                  template_family: "generative_template",
                  template: ["locative_domain!", negative ? "negated_existential!" : "existential!", "introduced_theme!", "particle?"],
                  assigned_slots: ["locative_domain", negative ? "negated_existential" : "existential", "introduced_theme", ...particles.map(() => "particle")],
                  surfaces: children.map(flattenSurface2),
                  existential_subtype: "locative_existence",
                  polarity: negative ? "negative" : "positive",
                  have_relation: "existence",
                  location_relation: "overt_spatial_domain_not_forced_subject_or_topic",
                  subject_status: "impersonal",
                  subjectless_type: "genuinely_subjectless_locative_existential",
                  hidden_subject_inserted: false,
                  introduced_theme_surface: flattenSurface2(theme),
                  not_claims: ["not_possessor_subject", "not_location_as_forced_subject", "not_location_as_forced_topic", "not_hidden_expletive_subject"]
                })
              });
            }
          }
        }
        const positiveParticipantIntroduction = positiveParticipantIntroductionFromNodes(compact, particles);
        if (positiveParticipantIntroduction) return positiveParticipantIntroduction;
        const locationPrefix = locativeDomainPrefix(compact);
        if (!locationPrefix || locationPrefix.consumed >= compact.length) return null;
        const remainder = compact.slice(locationPrefix.consumed);
        const positioning = placementPerfectiveVPFromNodes(remainder);
        if (positioning) {
          const children = [locationPrefix.node, positioning, ...particles];
          return construction2("LocativeFrameClause", "LocativeFrame", children, {
            note: "Narrow locative-inversion frame with overt location and postverbal theme; grammatical subjecthood of the location is not forced.",
            slots: cleanSlots2(["locative_frame_clause", "location", "locative_domain", "predicate", "introduced_theme", "clause", ...templateDerivedSlots2("LocativeFrameClause", children)]),
            trace: traceInfo2("generative_template", {
              construction_type: "LocativeFrameClause",
              template_family: "generative_template",
              template: ["locative_domain!", "positioning_predicate!", "particle?"],
              assigned_slots: ["locative_domain", "positioning_predicate", ...particles.map(() => "particle")],
              surfaces: children.map(flattenSurface2),
              locative_frame_subtype: "locative_inversion",
              location_relation: "locative_frame_subjecthood_underdetermined",
              subject_status: "underdetermined_location_relation",
              hidden_subject_inserted: false,
              not_claims: ["not_locative_existential_have", "not_location_as_automatically_subject", "not_location_as_automatically_topic", "not_hidden_subject"]
            })
          });
        }
        const wrappedProperty = applyConstructionPatterns2(remainder);
        const property = fullSpanSingleConstruction2(wrappedProperty, remainder);
        if (property && ["DegreeStativePredicate", "StativePredicate", "NegatedStativePredicate"].includes(property.type)) {
          const children = [locationPrefix.node, property, ...particles];
          return construction2("LocativeFrameClause", "LocativeFrame", children, {
            note: "Location-framed property clause. The overt place establishes the domain; its subject/topic status is not forced.",
            slots: cleanSlots2(["locative_frame_clause", "location", "locative_domain", "predicate", "clause", ...templateDerivedSlots2("LocativeFrameClause", children)]),
            trace: traceInfo2("generative_template", {
              construction_type: "LocativeFrameClause",
              template_family: "generative_template",
              template: ["locative_domain!", "property_predicate!", "particle?"],
              assigned_slots: ["locative_domain", "property_predicate", ...particles.map(() => "particle")],
              surfaces: children.map(flattenSurface2),
              locative_frame_subtype: "location_property",
              location_relation: "frame_or_topic_status_underdetermined",
              subject_status: "underdetermined_location_relation",
              hidden_subject_inserted: false,
              not_claims: ["not_environmental_temperature_clause", "not_location_as_automatically_subject", "not_location_as_automatically_topic", "not_hidden_subject"]
            })
          });
        }
        return null;
      }
      return {
        spatialLocalizerPhraseFromNodes,
        existentialNPFromNodes,
        locativeDomainPrefix,
        presentationalLocativeCodaFromNodes,
        placementPerfectiveVPFromNodes,
        existentialLocationPresentationalFallback: existentialLocationPresentationalFallback2
      };
    };
  }
});

// src/parser/detectors/nominal-predication/clauses.js
var require_clauses5 = __commonJS({
  "src/parser/detectors/nominal-predication/clauses.js"(exports2, module2) {
    "use strict";
    module2.exports = function createDetectors(dependencies = {}) {
      const {
        categorySubspanFor: categorySubspanFor2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        stringIncludesAny: stringIncludesAny2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function nominalPredicateSubjectFromNodes(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        if (compact.length === 1) {
          const only = compact[0];
          if (nodeCanFillSlot2(only, "subject") || nodeCanFillSlot2(only, "np") || nodeCanFillSlot2(only, "head_noun") || nodeCanFillSlot2(only, "location")) return only;
          return null;
        }
        const templated = categorySubspanFor2(compact, [
          "OvertHeadDemonstrativeClassifierNP",
          "QuantifiedClassifierNP",
          "QuantifiedPersonNP",
          "DiMarkedNP",
          "OrdinalClassifierNP",
          "ClassifierObjectNP",
          "ModifiedNP",
          "NominalHeadSpan",
          "CoordinatedNP"
        ]);
        if (!templated) return null;
        if (!nodeCanFillSlot2(templated, "subject") && !nodeCanFillSlot2(templated, "np") && !nodeCanFillSlot2(templated, "head_noun") && !nodeCanFillSlot2(templated, "location")) return null;
        return templated;
      }
      function nominalPredicateTokens(node) {
        if (!node) return [];
        if (node.kind === "token") return [node];
        if (node.kind === "construction") return (node.children || []).flatMap(nominalPredicateTokens);
        return [];
      }
      function nominalPredicateSubjectClass(node) {
        const tokens = nominalPredicateTokens(node || {});
        if (tokens.some((item) => item.label === "who")) return "person";
        if (tokens.some((item) => item.label === "when")) return "time";
        if (nodeCanFillSlot2(node, "location") || tokens.some((item) => item.label === "where")) return "location";
        const areaCompatibleSyntax = ["house_noun", "interior_location", "building_shop", "room_noun", "property_noun"];
        if (tokens.some((item) => stringIncludesAny2(String(item.syntax || ""), areaCompatibleSyntax))) return "area_measurable_nominal";
        return "nominal";
      }
      function nominalPredicateQuantityClone(node, constructionType) {
        return parserInactiveTokenClone2(node, {
          label: "how",
          pos: "numeral",
          syntax: "quantity count_value nominal_predicate_quantity",
          slots: ["quantity", "count_value"],
          reason: `The visible numeral contributes the measured value inside ${constructionType}, rather than acting as a property predicate.`,
          active_affordance_match: { role: "how", slot: "quantity", source: "construction_override" }
        });
      }
      function nominalPredicateUnitClone(node, domain) {
        const surface = flattenSurface2(node);
        if (domain === "price") {
          return parserInactiveTokenClone2(node, {
            label: "what",
            pos: "noun",
            syntax: "currency_unit nominal_measure_unit price_measure_unit",
            slots: ["currency_unit", "nominal_measure_unit", "measure_unit"],
            reason: `${surface} is the overt currency unit in a restricted price nominal predicate.`,
            active_affordance_match: { role: "what", slot: "currency_unit", source: "construction_override" }
          });
        }
        return parserInactiveTokenClone2(node, {
          label: "measure_word",
          pos: "measure",
          syntax: domain === "age" ? "age_unit nominal_measure_unit" : domain === "area" ? "measure_unit area_measure_unit nominal_measure_unit" : "measure_unit length_measure_unit nominal_measure_unit",
          slots: domain === "age" ? ["age_unit", "nominal_measure_unit", "measure_unit"] : domain === "area" ? ["measure_unit", "area_measure_unit", "nominal_measure_unit"] : ["measure_unit", "length_measure_unit", "nominal_measure_unit"],
          reason: `${surface} is the overt ${domain} unit inside a restricted copula-less nominal predicate.`,
          active_affordance_match: {
            role: "measure_word",
            slot: domain === "age" ? "age_unit" : domain === "area" ? "area_measure_unit" : "length_measure_unit",
            source: "construction_override"
          }
        });
      }
      function nominalPredicateDimensionClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "like",
          pos: "stative",
          syntax: "stative_predicate scalar_dimension_predicate length_dimension_predicate",
          slots: ["dimension_predicate", "stative_predicate", "predicate"],
          reason: "The overt dimensional predicate identifies the measured dimension and remains distinct from the numeric value and unit.",
          active_affordance_match: { role: "like", slot: "dimension_predicate", source: "construction_override" }
        });
      }
      function nominalMeasurePredicateFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length < 3) return null;
        if (compact.some((node) => isToken2(node, "係") || isToken2(node, "喺"))) return null;
        let unitIndex = -1;
        let domain = "";
        for (let index = 1; index < compact.length; index++) {
          const node = compact[index];
          if (nodeCanFillSlot2(node, "age_unit")) {
            unitIndex = index;
            domain = "age";
            break;
          }
          if (isToken2(node, "蚊")) {
            unitIndex = index;
            domain = "price";
            break;
          }
          if (nodeCanFillSlot2(node, "measure_unit")) {
            unitIndex = index;
            domain = nodeCanFillSlot2(node, "area_measure_unit") ? "area_or_length" : "length";
            break;
          }
        }
        if (unitIndex < 1) return null;
        let quantityStart = unitIndex;
        while (quantityStart > 0 && nodeCanFillSlot2(compact[quantityStart - 1], "quantity")) quantityStart--;
        if (quantityStart === unitIndex || quantityStart === 0) return null;
        const quantityNodes = compact.slice(quantityStart, unitIndex);
        if (!quantityNodes.every((node) => nodeCanFillSlot2(node, "quantity"))) return null;
        const subjectNodes = compact.slice(0, quantityStart);
        const subject = nominalPredicateSubjectFromNodes(subjectNodes);
        if (!subject || flattenSurface2(subject) !== subjectNodes.map(flattenSurface2).join("")) return null;
        const subjectClass = nominalPredicateSubjectClass(subject);
        const afterUnit = compact.slice(unitIndex + 1);
        let dimension = null;
        if (afterUnit.length) {
          if (afterUnit.length !== 1 || !nodeCanFillSlot2(afterUnit[0], "dimension_predicate")) return null;
          dimension = afterUnit[0];
        }
        if (domain === "age" && subjectClass !== "person") return null;
        if (domain === "price" && ["person", "time", "location"].includes(subjectClass)) return null;
        if (domain === "area_or_length") {
          if (dimension) domain = "length";
          else if (["location", "area_measurable_nominal"].includes(subjectClass)) domain = "area";
          else return null;
        }
        if (domain === "length" && !dimension) return null;
        if (domain === "area" && dimension) return null;
        if (domain === "price" && dimension) return null;
        if (domain === "age" && dimension) return null;
        const quantityChildren = quantityNodes.map((node) => nominalPredicateQuantityClone(node, "MeasureExpression"));
        const unit = nominalPredicateUnitClone(compact[unitIndex], domain);
        const dimensionChild = dimension ? nominalPredicateDimensionClone(dimension) : null;
        const measureChildren = [...quantityChildren, unit, ...dimensionChild ? [dimensionChild] : []];
        const measure = construction2("MeasureExpression", "Measure", measureChildren, {
          slots: cleanSlots2([
            "measure_expression",
            "nominal_predicate",
            "predicate",
            "quantity",
            "nominal_measure_unit",
            domain === "age" ? "age_unit" : "",
            domain === "price" ? "currency_unit" : "",
            domain === "area" ? "area_measure_unit" : "",
            domain === "length" ? "length_measure_unit" : "",
            dimensionChild ? "dimension_predicate" : ""
          ]),
          note: `Restricted ${domain} measure expression used as a copula-less nominal predicate.`,
          trace: traceInfo2("generative_template", {
            construction_type: "MeasureExpression",
            internal_representation_scope: "overt_measure_child_span",
            independent_grammar_licensing: false,
            licensing_parent: "NominalPredicateClause",
            template_family: "generative_template",
            template: ["quantity+", `${domain}_unit!`, ...dimensionChild ? ["dimension_predicate!"] : []],
            assigned_slots: [...quantityChildren.map(() => "quantity"), `${domain}_unit`, ...dimensionChild ? ["dimension_predicate"] : []],
            surfaces: measureChildren.map(flattenSurface2),
            measure_domain: domain,
            quantity_surface: quantityChildren.map(flattenSurface2).join(""),
            unit_surface: flattenSurface2(unit),
            dimension_surface: dimensionChild ? flattenSurface2(dimensionChild) : "",
            subspan: true
          })
        });
        const children = [subject, measure, ...particles];
        return construction2("NominalPredicateClause", "NomPred", children, {
          slots: cleanSlots2(["nominal_predicate_clause", "subject", "predicate", "nominal_predicate", "measure_expression", "clause"]),
          note: `Restricted copula-less ${domain} nominal-predicate clause with an overt subject and overt measure expression.`,
          trace: traceInfo2("generative_template", {
            construction_type: "NominalPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "measure_expression!", "particle?"],
            assigned_slots: ["subject", "measure_expression", ...particles.map(() => "particle")],
            surfaces: children.map(flattenSurface2),
            nominal_predicate_type: domain,
            copula_status: "licensed_omission_in_restricted_measure_predication",
            subject_status: "overt",
            subject_surface: flattenSurface2(subject),
            predicate_surface: flattenSurface2(measure),
            hidden_subject_inserted: false,
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            not_claims: [
              "not_general_np_np_copula_omission",
              "not_topic_by_initial_position_alone",
              "not_hidden_copula_token",
              "not_hidden_subject"
            ]
          })
        });
      }
      return {
        nominalPredicateSubjectFromNodes,
        nominalPredicateTokens,
        nominalPredicateSubjectClass,
        nominalPredicateQuantityClone,
        nominalPredicateUnitClone,
        nominalPredicateDimensionClone,
        nominalMeasurePredicateFallback: nominalMeasurePredicateFallback2
      };
    };
  }
});

// src/parser/detectors/aspect/postverbal-zo.js
var require_postverbal_zo = __commonJS({
  "src/parser/detectors/aspect/postverbal-zo.js"(exports2, module2) {
    "use strict";
    module2.exports = function createPostverbalZoDetectors2(dependencies = {}) {
      const {
        cleanSlots: cleanSlots2,
        compositionalNpSubspanFor: compositionalNpSubspanFor2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeCanLicenseEvidenceGatedObject: nodeCanLicenseEvidenceGatedObject2,
        nodeNpLicenseStatus: nodeNpLicenseStatus2,
        templateConstructionFor: templateConstructionFor2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function postverbalZoPerfectiveFromRawNodes2(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length < 3) return null;
        const subjectOffset = nodeCanFillSlot2(compact[0], "subject") ? 1 : 0;
        if (compact.length - subjectOffset < 3) return null;
        const action = compact[subjectOffset];
        const aspect = compact[subjectOffset + 1];
        if (!nodeCanFillSlot2(action, "action_verb") || !nodeCanFillSlot2(aspect, "perfective_aspect")) return null;
        const objectNodes = compact.slice(subjectOffset + 2);
        if (objectNodes.length < 2) return null;
        const objectNode = compositionalNpSubspanFor2(objectNodes) || (objectNodes.length === 1 && nodeCanLicenseEvidenceGatedObject2(objectNodes[0]) ? objectNodes[0] : null);
        if (!objectNode || !nodeCanLicenseEvidenceGatedObject2(objectNode)) return null;
        const children = [action, aspect, objectNode];
        const perfective = construction2("PostverbalZoPerfectiveVP", "PerfectiveVP", children, {
          slots: cleanSlots2(["perfective_vp", "vp", "action_vp", "predicate", "action_verb", "perfective_aspect", "object", "np"]),
          note: "Compositional postverbal 咗 perfective with an overt licensed NP object. NP assembly is independent parser infrastructure and does not broaden the construction's linguistic status.",
          trace: traceInfo2("generative_template", {
            construction_type: "PostverbalZoPerfectiveVP",
            template_family: "generative_template",
            template: ["action_verb!", "perfective_aspect!", "licensed_np_object!"],
            assigned_slots: ["action_verb", "perfective_aspect", "object"],
            surfaces: children.map((node) => flattenSurface2(node)),
            object_np_license_status: nodeNpLicenseStatus2(objectNode),
            object_np_construction: objectNode.kind === "construction" ? objectNode.type : "bare_nominal_token",
            hidden_object_inserted: false,
            evidence_scope_unchanged: true,
            reason: "v0.5.184 composes the complete postverbal object as a reusable NP before broad VP subspan wrapping can consume only its first token."
          })
        });
        if (!subjectOffset) return [perfective];
        const clause = templateConstructionFor2([compact[0], perfective], ["SubjectPredicateClause"]);
        return clause ? [clause] : [compact[0], perfective];
      }
      function postverbalZoPerfectiveFromWrappedNodes2(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length < 2) return null;
        for (let index = 0; index < compact.length - 1; index += 1) {
          const first = compact[index];
          const objectNode = compact[index + 1];
          if (!nodeCanLicenseEvidenceGatedObject2(objectNode)) continue;
          let action = null;
          let aspect = null;
          if (first && first.kind === "construction" && first.type === "PerfectiveVP") {
            const children2 = withoutIgnorableSpaceText2(first.children || []);
            if (children2.length === 2 && nodeCanFillSlot2(children2[0], "action_verb") && nodeCanFillSlot2(children2[1], "perfective_aspect")) {
              [action, aspect] = children2;
            }
          } else if (index + 2 < compact.length && nodeCanFillSlot2(first, "action_verb") && nodeCanFillSlot2(compact[index + 1], "perfective_aspect") && nodeCanLicenseEvidenceGatedObject2(compact[index + 2])) {
            action = first;
            aspect = compact[index + 1];
          }
          if (!action || !aspect) continue;
          const actualObject = first && first.kind === "construction" ? objectNode : compact[index + 2];
          const consumed = first && first.kind === "construction" ? 2 : 3;
          if (index + consumed !== compact.length) continue;
          const children = [action, aspect, actualObject];
          const perfective = construction2("PostverbalZoPerfectiveVP", "PerfectiveVP", children, {
            slots: cleanSlots2(["perfective_vp", "vp", "action_vp", "predicate", "action_verb", "perfective_aspect", "object", "np"]),
            note: "Compositional postverbal 咗 perfective with an overt licensed NP object. NP assembly is independent parser infrastructure and does not broaden the construction's linguistic status.",
            trace: traceInfo2("generative_template", {
              construction_type: "PostverbalZoPerfectiveVP",
              template_family: "generative_template",
              template: ["action_verb!", "perfective_aspect!", "licensed_np_object!"],
              assigned_slots: ["action_verb", "perfective_aspect", "object"],
              surfaces: children.map((node) => flattenSurface2(node)),
              object_np_license_status: nodeNpLicenseStatus2(actualObject),
              object_np_construction: actualObject.kind === "construction" ? actualObject.type : "bare_nominal_token",
              hidden_object_inserted: false,
              evidence_scope_unchanged: true,
              reason: "v0.5.184 recomposes V+咗 with a reusable licensed NP after NP-internal parsing, instead of enumerating complete object strings."
            })
          });
          return [...compact.slice(0, index), perfective, ...compact.slice(index + consumed)];
        }
        return null;
      }
      return { postverbalZoPerfectiveFromRawNodes: postverbalZoPerfectiveFromRawNodes2, postverbalZoPerfectiveFromWrappedNodes: postverbalZoPerfectiveFromWrappedNodes2 };
    };
  }
});

// src/parser/detectors/definition/copular-relations.js
var require_copular_relations = __commonJS({
  "src/parser/detectors/definition/copular-relations.js"(exports2, module2) {
    "use strict";
    module2.exports = function createDefinitionCopularDetectors2(dependencies = {}) {
      const {
        construction: construction2,
        coordinatedNPFromParts: coordinatedNPFromParts2,
        copulaClone: copulaClone2,
        deicticClassifierTopicFromParts: deicticClassifierTopicFromParts2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isParticle: isParticle2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        nominalComplementFromNodes: nominalComplementFromNodes2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        token: token3,
        traceInfo: traceInfo2,
        transparentDeicticClassifierTopicFromNode: transparentDeicticClassifierTopicFromNode2,
        transparentTopicContentFromNodes: transparentTopicContentFromNodes2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function isDefinitionExplanatoryParticle(node) {
        return isParticle2(node) && ["㗎", "架", "嘅", "啊", "呀", "啦"].includes(flattenSurface2(node));
      }
      function isDefinitionExplanatoryParticleSequence(nodes) {
        if (!nodes.length) return false;
        if (!nodes.every(isDefinitionExplanatoryParticle)) return false;
        return nodes.some((node) => ["㗎", "架"].includes(flattenSurface2(node)));
      }
      function isDefinitionCopulaNode2(node) {
        return isToken2(node, "係");
      }
      function isDefinitionFormulaCopulaWhNode(node) {
        return node && node.kind === "construction" && node.type === "FormulaDiscourseUnit" && flattenSurface2(node) === "係咩";
      }
      function definitionExplanatoryComplementKind(nodes) {
        if (!nodes.length) return "";
        if (nodes.some((node) => ["咩", "乜嘢"].includes(flattenSurface2(node)))) return "wh_definition_complement";
        const hasNominalAffordance = nodes.some((node) => {
          const slots = nodeSlots2(node);
          return ["np", "object", "topic", "head_noun", "classifier", "quantity", "location"].some((slot) => slots.includes(slot));
        });
        if (hasNominalAffordance) return "np_definition_complement";
        const raw = nodes.map(flattenSurface2).join("");
        if (/^[\p{Script=Han}A-Za-z0-9]+$/u.test(raw)) return "lexicon_gap_np_definition_complement";
        return "";
      }
      function containsPredicateLikeDefinitionComplement(nodes) {
        return nodes.some((node) => {
          const slots = nodeSlots2(node);
          return ["vp", "action_vp", "predicate", "directional_motion_vp", "motion_predicate", "urgency_marker", "imperative_adverb"].some((slot) => slots.includes(slot));
        });
      }
      function definitionFrameCopulaToken(node) {
        const source = isToken2(node, "係") ? node : token3("係");
        return parserInactiveTokenClone2(source, {
          label: "func",
          pos: "function",
          syntax: "definition_copula",
          slots: ["definition_copula", "copula"],
          reason: "係 is the copula inside a bounded 係...嚟㗎 definition/explanatory frame; it should not become a separate formula or predicate island here."
        });
      }
      function definitionFrameWhToken(surface) {
        return token3(surface, {
          label: "what",
          syntax: "wh_thing definition_wh_complement",
          note: `${surface} asks for the definition/identity inside a 係...嚟㗎 frame; learner role stays what, with wh/question syntax rather than a new wh-question role.`,
          trace: traceInfo2("generative_template", {
            construction_type: "DefinitionExplanatoryFrame",
            surface,
            reason: "Contextual reanalysis of protected 係咩 only inside the bounded definition/explanatory frame.",
            role_resolution_note: "咩 remains learner role what; wh-definition/question behavior is encoded in syntax/slots, not a new learner role."
          })
        });
      }
      function definitionFrameLaiMarker(node) {
        return parserInactiveTokenClone2(node, {
          label: "func",
          pos: "function",
          syntax: "definition_explanatory_lai_marker",
          slots: ["definition_lai_marker", "explanatory_linker"],
          reason: "嚟 is inside a bounded 係...嚟㗎 definition/explanatory frame, so it is the explanatory linker here, not a directional-motion VP.",
          role_resolution_note: "嚟 is func only in this bounded explanatory-linker context; motion contexts keep 嚟 as doing/deictic motion."
        });
      }
      function definitionTopicFromNodes(topicNodes) {
        const compact = withoutIgnorableSpaceText2(topicNodes || []);
        if (!compact.length) return null;
        if (compact.length === 1) {
          const split = transparentDeicticClassifierTopicFromNode2(compact[0], "DefinitionExplanatoryFrame");
          if (split) return split;
        }
        const transparent = transparentTopicContentFromNodes2(compact);
        if (transparent && nodeCanFillSlot2(transparent, "topic")) {
          return construction2("Topic", "Topic", [transparent], {
            slots: ["definition_topic", "topic", "np"],
            note: "Topic being defined or identified in a bounded 係...嚟㗎 frame, preserving transparent NP structure where available.",
            trace: traceInfo2("generative_template", {
              construction_type: "Topic",
              template_family: "generative_template",
              template: ["topic!"],
              assigned_slots: ["topic"],
              surfaces: [flattenSurface2(transparent)],
              reason: "Definition frame topic preserves transparent child structure instead of flattening the topic span."
            })
          });
        }
        return construction2("Topic", "Topic", compact, {
          slots: ["definition_topic", "topic", "np"],
          note: "Topic being defined or identified in a bounded 係...嚟㗎 frame.",
          trace: traceInfo2("generative_template", {
            construction_type: "DefinitionExplanatoryFrame",
            assigned_slot: "topic",
            surfaces: compact.map((node) => flattenSurface2(node))
          })
        });
      }
      function copularExplanatoryCompositionFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        if (!bareCore.length || !isDefinitionExplanatoryParticleSequence(particles)) return null;
        const laiIndex = bareCore.findIndex((node) => isToken2(node, "嚟"));
        if (laiIndex <= 0 || laiIndex !== bareCore.length - 1) return null;
        let copulaIndex = bareCore.findIndex(isDefinitionCopulaNode2);
        let formulaCopulaWh = false;
        if (copulaIndex < 0) {
          copulaIndex = bareCore.findIndex(isDefinitionFormulaCopulaWhNode);
          formulaCopulaWh = copulaIndex >= 0;
        }
        if (copulaIndex <= 0 || copulaIndex >= laiIndex) return null;
        const topicNodes = bareCore.slice(0, copulaIndex);
        const copulaNode = bareCore[copulaIndex];
        const complementNodes = formulaCopulaWh ? [definitionFrameWhToken("咩")] : bareCore.slice(copulaIndex + 1, laiIndex);
        if (!topicNodes.length || !complementNodes.length) return null;
        if (!formulaCopulaWh && containsPredicateLikeDefinitionComplement(complementNodes)) return null;
        const complementKind = definitionExplanatoryComplementKind(complementNodes);
        if (!complementKind) return null;
        const topic = definitionTopicFromNodes(topicNodes);
        if (!topic) return null;
        const copula = definitionFrameCopulaToken(copulaNode);
        const complement = complementNodes.map((node) => parserInactiveTokenClone2(firstToken2(node) || node, {
          label: complementKind === "wh_definition_complement" ? "what" : (firstToken2(node) || node).label || "what",
          pos: "np",
          syntax: `${(firstToken2(node) || node).syntax || "nominal"} copular_complement`,
          slots: ["copular_complement", "object", "np", "topic_or_object"],
          reason: "The overt wh/NP material is the visible complement of 係; no dedicated definition-complement wrapper is introduced."
        }));
        const lai = definitionFrameLaiMarker(bareCore[laiIndex]);
        const children = [topic, copula, ...complement, lai, ...particles];
        return construction2("CopularRelationFrame", "Copular", children, {
          slots: ["copular_relation_frame", "copular_clause", "explanatory_clause", "topic", "object", "np", "predicate", "clause"],
          note: "Compositional copular/explanatory clause: topic + 係 + wh/NP complement + 嚟 + explanatory particle. The visible pieces remain transparent without reviving the retired dedicated wrapper.",
          trace: traceInfo2("generative_template", {
            construction_type: "CopularRelationFrame",
            template: ["topic!", "copula!", "copular_complement!", "explanatory_linker!", "explanatory_particle!"],
            constraints: {
              required_copula: "係",
              required_lai_marker: "嚟",
              required_explanatory_particle: "㗎/架",
              complement_kind: complementKind,
              formula_guard_reanalysis: formulaCopulaWh ? "係咩 split only inside 係...嚟㗎" : "not_needed"
            },
            assigned_slots: ["topic", "copula", ...complement.map(() => "copular_complement"), "explanatory_linker", "explanatory_particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Preserves the overt copular composition and prevents explanatory 嚟 from being mislabeled as literal DirectionalMotionVP.",
            not_claims: ["not_dedicated_definition_frame", "not_directional_motion_lai"]
          })
        });
      }
      function copularIdentificationFrameFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        const copulaIndex = compact.findIndex((node) => isToken2(node, "係"));
        if (copulaIndex <= 0 || copulaIndex >= compact.length - 1) return null;
        const before = compact.slice(0, copulaIndex);
        const after = compact.slice(copulaIndex + 1);
        if (after.some((node) => isToken2(node, "嚟") || isToken2(node, "用嚟"))) return null;
        let subjectOrTopic = null;
        let frameType = "CopularIdentificationFrame";
        let label = "Ident";
        if (before.length === 2) subjectOrTopic = deicticClassifierTopicFromParts2(before);
        if (!subjectOrTopic && before.length === 3) {
          subjectOrTopic = coordinatedNPFromParts2(before);
          if (subjectOrTopic) {
            frameType = "CopularRelationFrame";
            label = "Relation";
          }
        }
        if (!subjectOrTopic && before.length === 1 && (nodeCanFillSlot2(before[0], "topic") || nodeCanFillSlot2(before[0], "subject"))) subjectOrTopic = before[0];
        if (!subjectOrTopic) return null;
        const complement = nominalComplementFromNodes2(after);
        if (!complement) return null;
        if (frameType === "CopularIdentificationFrame" && !nodeCanFillSlot2(subjectOrTopic, "topic")) return null;
        if (frameType === "CopularRelationFrame" && !nodeCanFillSlot2(subjectOrTopic, "subject")) return null;
        const copula = copulaClone2(compact[copulaIndex], "copula identification_copula", ["identification_copula"], "係 is interpreted as the copula inside a bounded identification/relation frame, not as a broad fallback.");
        const children = [subjectOrTopic, copula, complement, ...particles];
        return construction2(frameType, label, children, {
          note: frameType === "CopularRelationFrame" ? "v0.5.33 bounded copular relation frame: coordinated subject + 係 + visible nominal complement." : "v0.5.33 bounded copular identification frame: topic/deictic classifier + 係 + visible nominal complement.",
          slots: templateDerivedSlots2(frameType, children),
          trace: traceInfo2("generative_template", {
            construction_type: frameType,
            template: frameType === "CopularRelationFrame" ? ["subject!", "copula!", "copular_complement!", "particle?"] : ["topic!", "copula!", "np!", "particle?"],
            assigned_slots: frameType === "CopularRelationFrame" ? ["subject", "copula", "copular_complement", ...particles.map(() => "particle")] : ["topic", "copula", "np", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Promotes only bounded active corpus copular identification/relation shapes, leaving definition/explanatory 係...嚟㗎 to its existing frame."
          })
        });
      }
      return {
        copularExplanatoryCompositionFallback: copularExplanatoryCompositionFallback2,
        copularIdentificationFrameFallback: copularIdentificationFrameFallback2,
        isDefinitionCopulaNode: isDefinitionCopulaNode2
      };
    };
  }
});

// src/parser/detectors/definition/intended-function.js
var require_intended_function = __commonJS({
  "src/parser/detectors/definition/intended-function.js"(exports2, module2) {
    "use strict";
    module2.exports = function createIntendedFunctionDetectors2(dependencies = {}) {
      const {
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isDefinitionCopulaNode: isDefinitionCopulaNode2,
        isModalToken: isModalToken2,
        isToken: isToken2,
        isVerbLike: isVerbLike2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        traceInfo: traceInfo2,
        transparentTopicContentFromNodes: transparentTopicContentFromNodes2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function isIntendedFunctionUseNode(node) {
        return isToken2(node, "用") && (String(node.syntax || "").includes("use_verb") || nodeCanFillSlot2(node, "action_verb"));
      }
      function isIntendedFunctionLaiNode(node) {
        return node && node.kind === "token" && ["嚟", "來"].includes(flattenSurface2(node));
      }
      function intendedFunctionUseToken(node) {
        return parserInactiveTokenClone2(node, {
          label: "func",
          syntax: "intended_function_use_marker",
          slots: ["purpose_use_verb", "intended_function_marker"],
          reason: "用 marks the intended-function relation in a bounded topic/object + 用嚟 + predicate sequence; it is not the ordinary lexical-use verb in this context."
        });
      }
      function intendedFunctionLaiToken(node) {
        const writtenForm = flattenSurface2(node);
        return parserInactiveTokenClone2(node, {
          label: "func",
          syntax: "intended_function_lai_linker purposive_linker",
          slots: ["purpose_lai_marker", "purpose_linker"],
          jyutping: writtenForm === "來" ? "loi4" : node.jyutping || "lai4",
          note: "links the object or resource to its intended use / function",
          reason: "嚟/來 is the intended-function linker inside this bounded relation, not a deictic motion predicate.",
          role_resolution_note: "嚟/來 is func only in the intended-function relation; directional-motion contexts keep the motion role.",
          trace_detail: { orthographic_form: writtenForm, relation_subtype: "intended_function" }
        });
      }
      function intendedFunctionPersonLike(node) {
        if (!node) return false;
        const first = firstToken2(node) || node;
        return first && first.label === "who" || nodeCanFillSlot2(node, "subject") && !nodeCanFillSlot2(node, "object") && !nodeCanFillSlot2(node, "head_noun");
      }
      function intendedFunctionNonPersonNominal(node) {
        if (!node || intendedFunctionPersonLike(node)) return false;
        const slots = nodeSlots2(node);
        return ["topic", "np", "head_noun", "object", "location", "instrument"].some((slot) => slots.includes(slot));
      }
      function intendedFunctionWrappedLinkPlan(node) {
        if (!node || node.kind !== "construction" || node.type !== "MotionPurposeChain") return null;
        const children = withoutIgnorableSpaceText2(node.children || []);
        if (children.length < 2) return null;
        const link = children[0];
        if (!link || link.kind !== "construction" || link.type !== "VerbComplementVP") return null;
        const linkChildren = withoutIgnorableSpaceText2(link.children || []);
        if (linkChildren.length !== 2 || !isIntendedFunctionUseNode(linkChildren[0])) return null;
        const laiWrapper = linkChildren[1];
        if (!laiWrapper || laiWrapper.kind !== "construction" || laiWrapper.type !== "DirectionalMotionVP") return null;
        const laiChildren = withoutIgnorableSpaceText2(laiWrapper.children || []);
        if (laiChildren.length !== 1 || !isIntendedFunctionLaiNode(laiChildren[0])) return null;
        return {
          useNode: linkChildren[0],
          laiNode: laiChildren[0],
          predicateNodes: children.slice(1),
          retiredWrapperTypes: ["MotionPurposeChain", "VerbComplementVP", "DirectionalMotionVP"]
        };
      }
      function intendedFunctionTopicPlan(nodes) {
        let compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        let userSubject = null;
        if (compact.length === 1 && compact[0].kind === "construction") {
          const children = withoutIgnorableSpaceText2(compact[0].children || []);
          if (children.length >= 2 && intendedFunctionPersonLike(children[children.length - 1]) && children.slice(0, -1).some(intendedFunctionNonPersonNominal)) {
            userSubject = children[children.length - 1];
            compact = children.slice(0, -1);
          }
        }
        if (!userSubject && compact.length >= 2 && intendedFunctionPersonLike(compact[compact.length - 1])) {
          const resourcePrefix = compact.slice(0, -1);
          const resourcePrefixSurface = resourcePrefix.map((node) => flattenSurface2(node)).join("");
          const resourcePrefixHasNominal = resourcePrefix.some(intendedFunctionNonPersonNominal);
          const resourcePrefixHasDemonstrativeClassifierShape = /^[呢嗰].+/u.test(resourcePrefixSurface) && resourcePrefix.some((node) => nodeCanFillSlot2(node, "classifier") || nodeCanFillSlot2(node, "measure_word"));
          if (resourcePrefixHasNominal || resourcePrefixHasDemonstrativeClassifierShape) {
            userSubject = compact[compact.length - 1];
            compact = resourcePrefix;
          }
        }
        if (!compact.length || compact.every(intendedFunctionPersonLike)) return null;
        const transparent = transparentTopicContentFromNodes2(compact);
        const hasDemonstrativeLead = ["呢", "嗰"].includes(flattenSurface2(compact[0]));
        const firstSlots = nodeSlots2(compact[0]);
        const hasClassifierLead = compact.length >= 2 && (firstSlots.includes("classifier") || firstSlots.includes("measure_word"));
        if (!transparent && !compact.some(intendedFunctionNonPersonNominal) && !hasDemonstrativeLead && !hasClassifierLead) return null;
        const resourceNodes = transparent ? [transparent] : compact;
        if (!userSubject) {
          return {
            resourceNodes,
            userSubject: null,
            resourceRelationStatus: "direct_function_resource_subject_topic_underdetermined"
          };
        }
        const topic = construction2("Topic", "Topic", resourceNodes, {
          slots: ["topic", "np", "function_topic"],
          note: "Thing or resource placed before the user to state what it is used for.",
          trace: traceInfo2("generative_template", {
            construction_type: "Topic",
            template_family: "generative_template",
            assigned_slots: ["topic"],
            surfaces: resourceNodes.map((node) => flattenSurface2(node)),
            relation_subtype: "intended_function",
            learner_gloss_lines: [
              "thing or resource being discussed",
              "Placed before the user to state what the resource is used for."
            ],
            reason: "An overt user follows the resource, supporting a narrow left-dislocated topic representation without generalizing direct function subjects as topics."
          })
        });
        return {
          resourceNodes: [topic],
          userSubject,
          resourceRelationStatus: "overt_resource_topic_with_separate_user"
        };
      }
      function intendedFunctionRelationFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (!compact.length) return null;
        let useNode = null;
        let laiNode = null;
        let beforeUse = [];
        let predicateNodes = [];
        let inheritedWrapperRetirement = [];
        const useIndex = compact.findIndex(isIntendedFunctionUseNode);
        if (useIndex > 0 && useIndex < compact.length - 2 && isIntendedFunctionLaiNode(compact[useIndex + 1])) {
          useNode = compact[useIndex];
          laiNode = compact[useIndex + 1];
          beforeUse = compact.slice(0, useIndex);
          predicateNodes = compact.slice(useIndex + 2);
        } else {
          const wrapperIndex = compact.findIndex((node) => intendedFunctionWrappedLinkPlan(node));
          if (wrapperIndex <= 0 || wrapperIndex !== compact.length - 1) return null;
          const wrapped = intendedFunctionWrappedLinkPlan(compact[wrapperIndex]);
          useNode = wrapped.useNode;
          laiNode = wrapped.laiNode;
          beforeUse = compact.slice(0, wrapperIndex);
          predicateNodes = wrapped.predicateNodes;
          inheritedWrapperRetirement = wrapped.retiredWrapperTypes;
        }
        if (!predicateNodes.length) return null;
        if (predicateNodes.some((node) => node.kind === "text" && /[，,；;]/u.test(String(node.text || "")) || ["同埋", "或者"].includes(flattenSurface2(node)))) return null;
        const controls = [];
        const controlSlots = [];
        const lastBefore = () => beforeUse[beforeUse.length - 1];
        if (beforeUse.length && isToken2(lastBefore(), "唔係")) {
          const negatedCopula = beforeUse.pop();
          controls.push(parserInactiveTokenClone2(negatedCopula, {
            label: "func",
            syntax: "intended_function_negated_copula",
            slots: ["negator", "copula"],
            reason: "Fused 唔係 negates the copular intended-function relation and must not be absorbed into the function-bearing topic."
          }));
          controlSlots.push("negator", "copula");
        } else if (beforeUse.length && isModalToken2(lastBefore())) {
          const modal = beforeUse.pop();
          if (beforeUse.length && isToken2(lastBefore(), "唔")) {
            const negator = beforeUse.pop();
            controls.push(parserInactiveTokenClone2(negator, {
              label: "func",
              syntax: "intended_function_modal_negator",
              slots: ["negator"],
              reason: "唔 scopes over the modal in the intended-function statement."
            }));
            controlSlots.push("negator");
          }
          controls.push(parserInactiveTokenClone2(modal, {
            label: "func",
            syntax: `${modal.syntax || "modal"} intended_function_modal`,
            slots: ["modal"],
            reason: "The modal scopes over the intended-function relation."
          }));
          controlSlots.push("modal");
        } else if (beforeUse.length && isDefinitionCopulaNode2(lastBefore())) {
          const copula = beforeUse.pop();
          if (beforeUse.length && isToken2(lastBefore(), "唔")) {
            const negator = beforeUse.pop();
            controls.push(parserInactiveTokenClone2(negator, {
              label: "func",
              syntax: "intended_function_copular_negator",
              slots: ["negator"],
              reason: "唔 negates the copular intended-function statement."
            }));
            controlSlots.push("negator");
          }
          controls.push(parserInactiveTokenClone2(copula, {
            label: "func",
            syntax: "intended_function_copula",
            slots: ["copula"],
            reason: "Optional 係 introduces a copular intended-function statement but is not required by the relation."
          }));
          controlSlots.push("copula");
        } else if (beforeUse.length && isToken2(lastBefore(), "唔")) {
          return null;
        }
        const plan = intendedFunctionTopicPlan(beforeUse);
        if (!plan) return null;
        const actualUseEventAmbiguity = predicateNodes.some((node) => flattenSurface2(node).includes("咗") || nodeCanFillSlot2(node, "perfective_aspect"));
        const modalAffordanceAmbiguity = controlSlots.includes("modal");
        const overtActionPredicate = predicateNodes.some((node) => isVerbLike2(node) || nodeCanFillSlot2(node, "action_vp") || nodeCanFillSlot2(node, "action_verb"));
        const directResourceFunctionProfile = !plan.userSubject && controlSlots.length === 0 && plan.resourceRelationStatus === "direct_function_resource_subject_topic_underdetermined" && !actualUseEventAmbiguity && !modalAffordanceAmbiguity && overtActionPredicate;
        const relationConstructionType = directResourceFunctionProfile ? "ResourceUseLaiFunctionRelation" : "IntendedFunctionRelation";
        const children = [...plan.resourceNodes];
        const assignedSlots = ["function_topic"];
        if (plan.userSubject) {
          children.push(plan.userSubject);
          assignedSlots.push("user_subject");
        }
        children.push(...controls, intendedFunctionUseToken(useNode), intendedFunctionLaiToken(laiNode), ...predicateNodes, ...particles);
        assignedSlots.push(...controlSlots, "purpose_use_verb", "purpose_lai_marker", "purpose_predicate");
        if (particles.length) assignedSlots.push("particle");
        return construction2(relationConstructionType, "Use / function", children, {
          slots: ["intended_function_relation", "function_relation", "function_topic", "purpose_use_verb", "purpose_lai_marker", "purpose_predicate", "predicate", "vp", "action_vp", "clause", ...controlSlots, ...plan.userSubject ? ["topic", "user_subject", "subject"] : []],
          note: "Connects an overt object, resource, or artifact to a stated use or function. Direct resource NPs remain neutral between subject and topic unless a separate overt user supports topicalization.",
          trace: traceInfo2("generative_template", {
            construction_type: relationConstructionType,
            template_family: "generative_template",
            template: ["function_topic!", "user_subject?", "negator?", "copula_or_modal?", "purpose_use_verb!", "purpose_lai_marker!", "purpose_predicate!", "particle?"],
            assigned_slots: assignedSlots,
            surfaces: children.map((node) => flattenSurface2(node)),
            relation_subtype: directResourceFunctionProfile ? "direct_resource_use_lai_function" : "intended_function",
            typed_relation: "object_or_resource_to_intended_use",
            evidence_status: directResourceFunctionProfile ? "research_pending_np_infrastructure" : "broad_provisional",
            relation_reading_status: actualUseEventAmbiguity ? "ambiguous_intended_function_vs_actual_use_event" : modalAffordanceAmbiguity ? "ambiguous_intended_function_vs_available_use_or_affordance" : "intended_function_candidate",
            semantic_review_flags: [
              ...actualUseEventAmbiguity ? ["intended_function_actual_use_event_ambiguity"] : [],
              ...modalAffordanceAmbiguity ? ["intended_function_vs_affordance_ambiguity"] : []
            ],
            function_resource_relation_status: plan.resourceRelationStatus,
            learner_gloss_lines: [
              "use or function",
              "Connects a thing or resource to what it is used for."
            ],
            reason: "CP019-r2 retains one narrow evidence-grounded 用嚟 relation, avoids forcing direct function resources into a public Topic analysis, and keeps perfective-event and modal-affordance readings explicitly review-bearing.",
            inherited_wrapper_retirement: inheritedWrapperRetirement,
            direct_resource_profile: directResourceFunctionProfile,
            overt_resource_required: directResourceFunctionProfile,
            adjacent_use_lai_required: directResourceFunctionProfile,
            overt_action_predicate_required: directResourceFunctionProfile,
            separate_user_subject_allowed: directResourceFunctionProfile ? false : null,
            modal_or_copular_controls_allowed: directResourceFunctionProfile ? false : null,
            actual_use_event_licensing: directResourceFunctionProfile ? false : null,
            affordance_licensing: directResourceFunctionProfile ? false : null,
            hidden_argument_insertion: directResourceFunctionProfile ? false : null,
            ordinary_lexical_use_licensing: directResourceFunctionProfile ? false : null,
            general_purpose_lai_licensing: directResourceFunctionProfile ? false : null,
            resource_subject_topic_status: directResourceFunctionProfile ? "underdetermined" : plan.resourceRelationStatus,
            not_claims: ["not_directional_motion", "not_ordinary_instrumental_use", "not_lexical_transitive_use", "not_general_purpose_serialization", ...directResourceFunctionProfile ? ["not_actual_use_event", "not_modal_affordance", "not_separate_user_frame", "not_hidden_resource"] : []]
          })
        });
      }
      return {
        intendedFunctionRelationFallback: intendedFunctionRelationFallback2
      };
    };
  }
});

// src/parser/detectors/naming/self-introduction.js
var require_self_introduction = __commonJS({
  "src/parser/detectors/naming/self-introduction.js"(exports2, module2) {
    "use strict";
    module2.exports = function createNamingDetectors2(dependencies = {}) {
      const {
        construction: construction2,
        firstToken: firstToken2,
        flattenSurface: flattenSurface2,
        isToken: isToken2,
        nameTokenClone: nameTokenClone2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2,
        withoutTrailingParticles: withoutTrailingParticles2
      } = dependencies;
      function namingVerbClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "doing",
          pos: "verb",
          syntax: "naming_verb personal_name_predicate",
          slots: ["naming_verb", "main_verb", "predicate"],
          reason: "叫 is the source-linked personal naming predicate in this bounded clause."
        });
      }
      function namingSelfIntroductionFrameFallback2(core) {
        const { core: bareCore, particles } = withoutTrailingParticles2(core);
        const compact = withoutIgnorableSpaceText2(bareCore);
        if (compact.length !== 3) return null;
        const [subject, naming, name] = compact;
        if (!nodeCanFillSlot2(subject, "subject")) return null;
        if (!isToken2(naming, "叫")) return null;
        if (!nodeCanFillSlot2(name, "np") && !nodeCanFillSlot2(name, "head_noun") && !nodeCanFillSlot2(name, "subject")) return null;
        const children = [subject, namingVerbClone(naming), nameTokenClone2(firstToken2(name) || name), ...particles];
        return construction2("NamingClause", "Called", children, {
          note: "Source-linked personal naming clause: subject + 叫 + visible personal name.",
          slots: templateDerivedSlots2("NamingClause", children),
          trace: traceInfo2("generative_template", {
            construction_type: "NamingClause",
            template: ["subject!", "naming_verb!", "name!", "particle?"],
            assigned_slots: ["subject", "naming_verb", "name", ...particles.map(() => "particle")],
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: "Retains the sourced personal 叫 + name relation while keeping 叫做 definition/category-label uses separate."
          })
        });
      }
      return {
        namingSelfIntroductionFrameFallback: namingSelfIntroductionFrameFallback2
      };
    };
  }
});

// src/parser/clause-relations/graph.js
var require_graph = __commonJS({
  "src/parser/clause-relations/graph.js"(exports2, module2) {
    "use strict";
    module2.exports = function createClauseRelationGraph2(dependencies = {}) {
      const {
        CLAUSE_LINKER_SURFACES: CLAUSE_LINKER_SURFACES2,
        CLAUSE_RELATION_SUBTYPE_REGISTRY: CLAUSE_RELATION_SUBTYPE_REGISTRY2,
        applyConstructionPatterns: applyConstructionPatterns2,
        applyTopicChainNullObjectLinkage: applyTopicChainNullObjectLinkage2,
        clauseLinkerInventory: clauseLinkerInventory2,
        clauseLinkingWrapperCoverage: clauseLinkingWrapperCoverage2,
        wrapperCoverageForConstructionNode: wrapperCoverageForConstructionNode2,
        cleanSlots: cleanSlots2,
        cognitionContentFrameFallback: cognitionContentFrameFallback2,
        construction: construction2,
        firstToken: firstToken2,
        flattenNodes: flattenNodes2,
        flattenSurface: flattenSurface2,
        hasConstruction: hasConstruction2,
        isClauseSequenceSeparator: isClauseSequenceSeparator2,
        isClauseSequenceTerminal: isClauseSequenceTerminal2,
        learnerDisplaySlots: learnerDisplaySlots2,
        nodeCanFillSlot: nodeCanFillSlot2,
        nodeSlots: nodeSlots2,
        opinionStanceFrameFallback: opinionStanceFrameFallback2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        reportedSpeechFrameFallback: reportedSpeechFrameFallback2,
        surfaceOf: surfaceOf2,
        token: token3,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function isClauseSequenceMeaningfulNode(node) {
        if (!node || node.kind !== "construction") return false;
        if (node.type === "ClauseSequence" || node.type === "ClauseRelationGraph") return false;
        return true;
      }
      function meaningfulClauseConstructionCount(nodes = []) {
        return nodes.filter(isClauseSequenceMeaningfulNode).length;
      }
      function parsedClauseNodes(segment = []) {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (!compact.length) return [];
        const parsed = applyConstructionPatterns2(compact);
        if (parsed.length === 1 && parsed[0] && parsed[0].kind === "construction" && parsed[0].type === "NominalHeadSpan") {
          const productiveChild = (parsed[0].children || []).find((child) => child && child.kind === "construction" && child.type === "ProductiveVO" && nodeCanFillSlot2(child, "vp"));
          if (productiveChild && flattenSurface2(productiveChild) === flattenSurface2(parsed[0])) return [productiveChild];
        }
        return parsed;
      }
      function buildGovernedClauseRelationGraph(children = [], detail = {}) {
        const compact = withoutIgnorableSpaceText2(children || []);
        if (meaningfulClauseConstructionCount(compact) < 2) return null;
        const wrapperCoverage = clauseLinkingWrapperCoverage2(compact);
        if (wrapperCoverage.unaccounted_wrapper_token_count > 0) return null;
        return construction2("ClauseRelationGraph", "ClauseLink", compact, {
          note: detail.note || "Linked clauses. This governed discourse/coordination wrapper preserves all child constructions and explicitly accounts for linker/separator material; it does not replace or flatten the child clauses.",
          trace: traceInfo2("governed_discourse_wrapper", {
            rule: detail.rule || "connector-governed clause-linking sequence",
            reason: detail.reason || "Connector-governed clause linking groups independently parsed clause-like children while keeping linkers and separators visible and accounted for.",
            graph_container_semantic_status: "neutral_container_only",
            independent_grammar_licensing: false,
            relation_semantics_source: "preexisting_child_constructions_and_linker_rules_only",
            child_constructions: compact.filter((node) => node && node.kind === "construction").map((node) => node.type),
            linkers: clauseLinkerInventory2(compact),
            separators: compact.filter(isClauseSequenceSeparator2).map((node) => node.text),
            wrapper_coverage: wrapperCoverage,
            ...detail.trace_detail || {}
          })
        });
      }
      function connectorLeadingClauseSegment(segment = []) {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 2) return null;
        const first = compact[0];
        if (!first || first.kind !== "token" || !CLAUSE_LINKER_SURFACES2.has(first.surface || "")) return null;
        if (!["但係", "不過", "所以", "然後", "跟住", "跟住就", "就", "咁", "噉"].includes(first.surface || "")) return null;
        const parsedRest = parsedClauseNodes(compact.slice(1));
        if (!parsedRest.length || meaningfulClauseConstructionCount(parsedRest) < 1) return null;
        return [first, ...parsedRest];
      }
      function connectorPairClauseLinkingSegment(segment = []) {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 4) return null;
        const surfaceAt = (index) => compact[index] && compact[index].kind === "token" ? compact[index].surface || "" : "";
        const firstSurface = surfaceAt(0);
        const pairedPatterns = [
          {
            opener: "如果",
            pivot: "就",
            rule: "如果...就 condition-result clause-linking sequence",
            reason: "如果 introduces the condition and 就 introduces the result; both sides are parsed independently under a broad ClauseRelationGraph wrapper.",
            subtype: "condition_result"
          },
          {
            opener: "因為",
            pivot: "所以",
            rule: "因為...所以 reason-result clause-linking sequence",
            reason: "因為 introduces the reason and 所以 introduces the result; both sides are parsed independently under a broad ClauseRelationGraph wrapper.",
            subtype: "reason_result"
          }
        ];
        for (const pattern of pairedPatterns) {
          if (firstSurface !== pattern.opener) continue;
          const pivotIndex = compact.findIndex((node, index) => index > 1 && node && node.kind === "token" && (node.surface || "") === pattern.pivot);
          if (pivotIndex < 0) continue;
          const left = parsedClauseNodes(compact.slice(1, pivotIndex));
          const right = parsedClauseNodes(compact.slice(pivotIndex + 1));
          const wrapper = buildGovernedClauseRelationGraph([compact[0], ...left, compact[pivotIndex], ...right], {
            rule: pattern.rule,
            reason: pattern.reason,
            trace_detail: { clause_linking_subtype: pattern.subtype }
          });
          if (wrapper) return wrapper;
        }
        const temporalIndex = compact.findIndex((node, index) => index > 0 && node && node.kind === "token" && ["之後", "之前"].includes(node.surface || ""));
        if (temporalIndex > 0 && temporalIndex < compact.length - 1) {
          const left = parsedClauseNodes(compact.slice(0, temporalIndex));
          const right = parsedClauseNodes(compact.slice(temporalIndex + 1));
          const linker = compact[temporalIndex];
          const wrapper = buildGovernedClauseRelationGraph([...left, linker, ...right], {
            rule: `${linker.surface} temporal clause-linking sequence`,
            reason: `${linker.surface} links a preceding event/clause-like unit to a following event/clause-like unit; the wrapper keeps both children visible.`,
            trace_detail: { clause_linking_subtype: "temporal_sequence" }
          });
          if (wrapper) return wrapper;
        }
        const contrastIndex = compact.findIndex((node, index) => index > 0 && node && node.kind === "token" && ["但係", "不過"].includes(node.surface || ""));
        if (contrastIndex > 0 && contrastIndex < compact.length - 1) {
          const left = parsedClauseNodes(compact.slice(0, contrastIndex));
          const right = parsedClauseNodes(compact.slice(contrastIndex + 1));
          const linker = compact[contrastIndex];
          const wrapper = buildGovernedClauseRelationGraph([...left, linker, ...right], {
            rule: `${linker.surface} contrast clause-linking sequence`,
            reason: `${linker.surface} links two contrasting clause-like units; the wrapper keeps both child parses and the contrast marker visible.`,
            trace_detail: { clause_linking_subtype: "contrast" }
          });
          if (wrapper) return wrapper;
        }
        return null;
      }
      function connectorAwareClauseLinkingForTerminal2(segment = []) {
        const pairWrapper = connectorPairClauseLinkingSegment(segment);
        if (pairWrapper) return [pairWrapper];
        const leading = connectorLeadingClauseSegment(segment);
        if (leading) return leading;
        return null;
      }
      function clauseRelationLeafNodes(node) {
        if (!node) return [];
        if (node.kind === "token" || node.kind === "text") return [node];
        if (node.kind === "construction") return (node.children || []).flatMap(clauseRelationLeafNodes);
        return [];
      }
      function clauseRelationSegmentNodes(segment = []) {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length === 1 && compact[0] && compact[0].kind === "construction" && ["RelativeClauseNP"].includes(compact[0].type)) return compact.slice();
        return compact.flatMap(clauseRelationLeafNodes).filter((node) => !(node.kind === "text" && !String(node.text || "").trim()));
      }
      function clauseRelationSurfaceList(nodes = []) {
        return (nodes || []).map((node) => flattenSurface2(node));
      }
      function clauseRelationSurfaceIndex2(nodes = [], surfaces = [], start = 0) {
        const wanted = new Set(surfaces || []);
        for (let index = Math.max(0, start); index < nodes.length; index += 1) {
          if (wanted.has(flattenSurface2(nodes[index]))) return index;
        }
        return -1;
      }
      function clauseRelationContainsSurface(nodes = [], surface = "") {
        return clauseRelationSurfaceIndex2(nodes, [surface]) >= 0;
      }
      function clauseRelationActionVPFor(nodes = [], options = {}) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        const first = compact[0];
        const firstTok = firstToken2(first);
        if (!firstTok || !nodeCanFillSlot2(first, "action_verb")) return null;
        if (compact.length === 1) {
          return construction2("IntransitiveVP", "VP", compact, {
            note: "Broad intransitive/action VP used as a visible clause-relation member.",
            trace: traceInfo2("generative_template", {
              construction_type: "IntransitiveVP",
              template_family: "generative_template",
              template: ["action_verb!"],
              assigned_slots: ["action_verb"],
              relation_member_scope: true,
              reason: "A visible action predicate without an overt object is preserved as a VP member; no subject or object token is inserted.",
              not_claims: ["not_fabricated_subject", "not_fabricated_object"]
            })
          });
        }
        const objectNodes = compact.slice(1);
        const objectParsed = applyConstructionPatterns2(objectNodes);
        const objectLike = objectParsed.length === 1 ? objectParsed[0] : objectNodes.length === 1 ? objectNodes[0] : null;
        if (objectLike && (nodeCanFillSlot2(objectLike, "object") || nodeCanFillSlot2(objectLike, "np") || nodeCanFillSlot2(objectLike, "head_noun") || nodeCanFillSlot2(objectLike, "subject"))) {
          const children = [first, objectLike];
          return construction2("TransitiveVP", "V+O", children, {
            note: "Transparent transitive VP used inside a clause relation.",
            trace: traceInfo2("generative_template", {
              construction_type: "TransitiveVP",
              template_family: "generative_template",
              template: ["action_verb!", "object!"],
              assigned_slots: ["action_verb", "object"],
              relation_member_scope: true,
              reason: "The relation-member parser preserves an overt action verb and overt nominal object as one transparent VP."
            })
          });
        }
        return null;
      }
      function clauseRelationParsedChunk2(nodes = [], options = {}) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return [];
        if (compact.length === 1 && compact[0] && compact[0].kind === "construction") return compact.slice();
        if (options.immediate_temporal_trigger && compact.some((node) => flattenSurface2(node) === "見到")) {
          const immediatePredicate = clauseRelationActionVPFor(compact, options);
          if (immediatePredicate) return [immediatePredicate];
        }
        const parsed = applyConstructionPatterns2(compact);
        if (parsed.some((node) => node && node.kind === "construction")) return parsed;
        if (nodeCanFillSlot2(compact[0], "subject") && compact.length >= 2) {
          const subject = compact[0];
          const predicateNodes = compact.slice(1);
          let predicateChildren = applyConstructionPatterns2(predicateNodes);
          if (!predicateChildren.some((node) => node && node.kind === "construction")) {
            const vp2 = clauseRelationActionVPFor(predicateNodes, options);
            if (vp2) predicateChildren = [vp2];
          }
          if (predicateChildren.some((node) => node && (node.kind === "construction" || nodeCanFillSlot2(node, "predicate") || nodeCanFillSlot2(node, "vp")))) {
            const children = [subject, ...predicateChildren];
            return [construction2("SubjectPredicateClause", "Clause", children, {
              note: "Subject plus visible predicate material inside a typed clause relation.",
              trace: traceInfo2("generative_template", {
                construction_type: "SubjectPredicateClause",
                template_family: "generative_template",
                template: ["subject!", "predicate!"],
                assigned_slots: ["subject", "predicate"],
                relation_member_scope: true,
                subject_status: "overt",
                reason: "The relation architecture groups an overt subject with its visible predicate while preserving every token and without inserting omitted material."
              })
            })];
          }
        }
        const vp = clauseRelationActionVPFor(compact, options);
        if (vp) return [vp];
        return parsed;
      }
      function clauseRelationLinkerClone(node, relationSubtype, side, semanticRole, relationId) {
        if (!node || node.kind !== "token") return node;
        const surface = flattenSurface2(node);
        const label = surface === "一" ? "func" : node.label || "func";
        return parserInactiveTokenClone2(node, {
          label,
          pos: "function",
          syntax: `${node.syntax || "clause_relation_linker"} clause_relation_linker ${relationSubtype}_relation_linker`,
          slots: cleanSlots2([...node.slots || [], "clause_relation_linker", side === "left" ? "linker_left" : "linker_right"]),
          reason: `The overt linker ${surface} is owned by the local ${relationSubtype} clause relation on the ${side} side; no absent partner is inserted.`,
          trace_detail: {
            clause_relation_id: relationId,
            relation_subtype: relationSubtype,
            linker_side: side,
            linker_semantic_role: semanticRole,
            linker_ownership_status: "owned_by_local_clause_relation"
          }
        });
      }
      function clauseRelationMember(nodes = [], options = {}) {
        const raw = clauseRelationSegmentNodes(nodes);
        if (!raw.length) return null;
        const linkerSpecs = Array.isArray(options.linkers) ? options.linkers.slice().sort((a, b) => a.index - b.index) : [];
        const linkerByIndex = new Map(linkerSpecs.map((item) => [item.index, item]));
        const children = [];
        let chunk = [];
        const flushChunk = () => {
          if (!chunk.length) return;
          children.push(...clauseRelationParsedChunk2(chunk, options));
          chunk = [];
        };
        raw.forEach((node, index) => {
          const spec = linkerByIndex.get(index);
          if (!spec) {
            if (node && node.kind === "token" && flattenSurface2(node) === "仲" && !node.jyutping) {
              chunk.push(token3("仲", {
                label: "how",
                jyutping: "zung6",
                syntax: "focus_adverb continuative_adverb",
                note: "still / furthermore; resolved locally inside a typed clause relation"
              }));
            } else {
              chunk.push(node);
            }
            return;
          }
          flushChunk();
          children.push(clauseRelationLinkerClone(node, options.relation_subtype, spec.side, spec.semantic_role, options.relation_id));
        });
        flushChunk();
        if (!children.length) return null;
        const firstClauseChild = children.find((child) => {
          if (!child) return false;
          if (child.kind === "token") return !(child.slots || []).includes("clause_relation_linker");
          return child.kind === "construction";
        }) || null;
        let overtSubject = null;
        if (firstClauseChild && firstClauseChild.kind === "token" && (firstClauseChild.slots || []).includes("subject")) {
          overtSubject = firstClauseChild;
        } else if (firstClauseChild && firstClauseChild.kind === "construction") {
          const subjectBearing = [
            "ClauseSpan",
            "NominalHeadSpan",
            "RelativeClauseNP",
            "OpinionStanceFrame",
            "CognitionContentFrame",
            "ReportedSpeech"
          ].includes(firstClauseChild.type);
          if (subjectBearing) {
            overtSubject = flattenNodes2([firstClauseChild]).find((row) => row.kind === "token" && (row.slots || []).includes("subject")) || null;
          }
        }
        const childConstructions = children.filter((node) => node && node.kind === "construction").map((node) => node.type);
        return construction2("ClauseRelationMemberSpan", options.role === "left" ? "Relation-L" : "Relation-R", children, {
          note: "Transparent source-order member of a typed clause relation. Linkers remain visible and carry explicit local ownership.",
          trace: traceInfo2("generative_template", {
            construction_type: "ClauseRelationMemberSpan",
            template_family: "generative_template",
            template: ["clause_material!", "relation_linker?"],
            assigned_slots: [options.role === "left" ? "left_relation_member" : "right_relation_member"],
            relation_member_role: options.role,
            relation_subtype: options.relation_subtype,
            relation_subtype_provenance: "inherited_from_parent_clause_relation_edge",
            clause_relation_id: options.relation_id,
            independent_grammar_licensing: false,
            context_resolution_capability: false,
            overt_subject_surface: overtSubject ? overtSubject.surface : "",
            subject_status: overtSubject ? "overt" : "not_overt",
            child_constructions: childConstructions,
            source_surface: raw.map((node) => flattenSurface2(node)).join(""),
            reason: "A relation member is grouped as one clause-like span while retaining source order, overt linkers, parsed predicate children, and visible subjects.",
            not_claims: ["not_fabricated_subject", "not_fabricated_clause", "not_hidden_linker"]
          })
        });
      }
      function clauseRelationSubjectSurface(member) {
        if (!member || member.kind !== "construction") return "";
        const memberTrace = member.trace || {};
        return String(memberTrace.overt_subject_surface || "");
      }
      function clauseRelationSubjectLinkage(leftMember, rightMember, relationSubtype) {
        const left = clauseRelationSubjectSurface(leftMember);
        const right = clauseRelationSubjectSurface(rightMember);
        if (left && right) return { status: "overt_subject_on_both_members", inherited_surface: "" };
        if (left && !right && ["concessive", "committed_preference", "ordered_preference", "premise_response", "sequential", "asyndetic_sequence"].includes(relationSubtype)) {
          return { status: "shared_overt_subject_inherited_by_right_member", inherited_surface: left };
        }
        if (left && !right) return { status: "left_subject_overt_right_subject_unresolved", inherited_surface: "" };
        if (!left && right) return { status: "right_subject_overt_left_subject_unresolved", inherited_surface: "" };
        if (!left && !right) return { status: "no_overt_subject_unresolved", inherited_surface: "" };
        return { status: "overt_subject_on_both_members", inherited_surface: "" };
      }
      function clauseRelationPairStatus(leftLinkers = [], rightLinkers = [], licensedAsyndetic = false) {
        if (leftLinkers.length && rightLinkers.length) return "both_overt";
        if (leftLinkers.length) return "left_overt_right_absent";
        if (rightLinkers.length) return "left_absent_right_overt";
        return licensedAsyndetic ? "both_absent_licensed_asyndetic" : "not_applicable";
      }
      function clauseRelationSemanticTrace(subtype, sourceOrder, leftMember, rightMember) {
        const leftSurface = flattenSurface2(leftMember);
        const rightSurface = flattenSurface2(rightMember);
        if (subtype === "conditional") return {
          antecedent_clause: leftSurface,
          consequent_clause: rightSurface
        };
        if (subtype === "causal") {
          const reasonOnLeft = sourceOrder !== "result_then_reason";
          return {
            reason_clause: reasonOnLeft ? leftSurface : rightSurface,
            result_clause: reasonOnLeft ? rightSurface : leftSurface,
            causal_source_order: sourceOrder
          };
        }
        if (subtype === "concessive") return {
          concession_clause: leftSurface,
          counterexpectation_clause: rightSurface
        };
        if (subtype === "committed_preference") return {
          chosen_option: leftSurface,
          rejected_option: rightSurface
        };
        if (subtype === "ordered_preference") return {
          disfavored_option: leftSurface,
          preferred_option: rightSurface
        };
        if (subtype === "premise_response") return {
          established_premise: leftSurface,
          response_clause: rightSurface
        };
        if (["sequential", "asyndetic_sequence"].includes(subtype)) return {
          earlier_event: leftSurface,
          later_event: rightSurface
        };
        if (subtype === "temporal_subordinate") return {
          temporal_subordinate: leftSurface,
          matrix_clause: rightSurface
        };
        return {};
      }
      function buildClauseRelationEdge(spec = {}) {
        if (!CLAUSE_RELATION_SUBTYPE_REGISTRY2.has(spec.relation_subtype)) return null;
        const relationId = `clause-relation:${spec.relation_subtype}:${clauseRelationSurfaceList(spec.left_nodes).join("")}:${clauseRelationSurfaceList(spec.right_nodes).join("")}`;
        const leftMember = clauseRelationMember(spec.left_nodes, {
          role: "left",
          relation_subtype: spec.relation_subtype,
          relation_id: relationId,
          linkers: spec.left_linkers || [],
          immediate_temporal_trigger: Boolean(spec.immediate_temporal_trigger)
        });
        const rightMember = clauseRelationMember(spec.right_nodes, {
          role: "right",
          relation_subtype: spec.relation_subtype,
          relation_id: relationId,
          linkers: spec.right_linkers || []
        });
        if (!leftMember || !rightMember) return null;
        const subjectLinkage = clauseRelationSubjectLinkage(leftMember, rightMember, spec.relation_subtype);
        const pairStatus = clauseRelationPairStatus(spec.left_linkers || [], spec.right_linkers || [], Boolean(spec.licensed_asyndetic));
        const children = [leftMember, spec.separator, rightMember].filter(Boolean);
        const leftLinkerSurfaces = (spec.left_linkers || []).map((item) => flattenSurface2(clauseRelationSegmentNodes(spec.left_nodes)[item.index]));
        const rightLinkerSurfaces = (spec.right_linkers || []).map((item) => flattenSurface2(clauseRelationSegmentNodes(spec.right_nodes)[item.index]));
        return construction2("ClauseRelationEdge", "ClauseRel", children, {
          note: "Typed local relation between two transparent clause-like members. The relation owns overt linkers and records optional-pair and shared-subject provenance without hidden tokens.",
          trace: traceInfo2("generative_template", {
            construction_type: "ClauseRelationEdge",
            template_family: "generative_template",
            template: ["left_relation_member!", "separator?", "right_relation_member!"],
            assigned_slots: ["left_relation_member", ...spec.separator ? ["separator"] : [], "right_relation_member"],
            clause_relation_id: relationId,
            relation_subtype: spec.relation_subtype,
            relation_subtype_provenance: spec.relation_subtype_provenance || "inherited_mapped_clause_relation_rule",
            relation_subtype_registry_status: "validated_against_clause_relation_subtype_registry",
            independent_grammar_licensing: false,
            relation_context_status: "context_not_required",
            source_order: spec.source_order || "left_then_right",
            linker_left: leftLinkerSurfaces,
            linker_right: rightLinkerSurfaces,
            linker_pair_status: pairStatus,
            linker_ownership_status: "all_overt_linkers_owned_once",
            subject_linkage_status: subjectLinkage.status,
            inherited_subject_surface: subjectLinkage.inherited_surface,
            ...clauseRelationSemanticTrace(spec.relation_subtype, spec.source_order || "left_then_right", leftMember, rightMember),
            immediate_temporal_trigger: Boolean(spec.immediate_temporal_trigger),
            asyndetic_license: spec.licensed_asyndetic ? spec.asyndetic_license || "licensed_by_event_order_and_visible_separator" : "not_applicable",
            surfaces: children.map((node) => flattenSurface2(node)),
            reason: spec.reason || "Overt linker evidence or a constrained asyndetic event-order pattern licenses one local typed clause relation instead of a flat discourse wrapper.",
            not_claims: [
              "not_fabricated_linker",
              "not_fabricated_subject",
              "not_fabricated_clause",
              "not_punctuation_only_relation"
            ]
          })
        });
      }
      function completionThenClauseRelation2(nodes = []) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        const markerIndex = compact.findIndex(
          (node) => node && node.kind === "token" && flattenSurface2(node) === "就"
        );
        if (markerIndex <= 0 || markerIndex >= compact.length - 1) return null;
        let leftNodes = compact.slice(0, markerIndex);
        let rightNodes = compact.slice(markerIndex);
        let rightLinkerIndex = 0;
        const preMarkerSubject = compact[markerIndex - 1];
        const completionBeforePreMarkerSubject = hasConstruction2(
          compact.slice(0, markerIndex - 1),
          "CompletionVP"
        );
        if (preMarkerSubject && nodeCanFillSlot2(preMarkerSubject, "subject") && completionBeforePreMarkerSubject) {
          leftNodes = compact.slice(0, markerIndex - 1);
          rightNodes = [preMarkerSubject, ...compact.slice(markerIndex)];
          rightLinkerIndex = clauseRelationSegmentNodes([preMarkerSubject]).length;
        }
        if (!hasConstruction2(leftNodes, "CompletionVP")) return null;
        if (!rightNodes.slice(1).some((node) => flattenSurface2(node))) return null;
        return buildClauseRelationEdge({
          relation_subtype: "sequential",
          left_nodes: leftNodes,
          right_nodes: rightNodes,
          right_linkers: [{
            index: rightLinkerIndex,
            side: "right",
            semantic_role: "later_event_linker"
          }],
          source_order: "earlier_completion_then_later_event",
          relation_subtype_provenance: "source_linked_completion_sequence_rule",
          reason: "An overt CompletionVP before 就 and a nonempty later member license a sequential relation; each member retains its own visible subject and predicate structure."
        });
      }
      function clauseRelationTimeHead(node) {
        const rows = flattenNodes2([node]);
        return rows.some((row) => row.kind === "token" && ["時候", "時間"].includes(row.surface || ""));
      }
      function clauseRelationMarkerSpec(nodes, surface, side, semanticRole, start = 0) {
        const index = clauseRelationSurfaceIndex2(nodes, [surface], start);
        return index >= 0 ? { index, side, semantic_role: semanticRole } : null;
      }
      function clauseRelationPrefixEmbedding(prefixNodes = [], relation = null) {
        const prefix = withoutIgnorableSpaceText2(prefixNodes || []);
        if (!prefix.length || !relation) return null;
        return opinionStanceFrameFallback2([...prefix, relation]) || cognitionContentFrameFallback2([...prefix, relation]) || reportedSpeechFrameFallback2([...prefix, relation]);
      }
      function hierarchicalClauseRelationEdgeFromChildren(children = []) {
        const segments = clauseSequenceSegments2(children);
        if (segments.length !== 2) return null;
        const separator = children.find(isClauseSequenceSeparator2) || null;
        const leftRaw = clauseRelationSegmentNodes(segments[0]);
        const rightRaw = clauseRelationSegmentNodes(segments[1]);
        if (!leftRaw.length || !rightRaw.length) return null;
        let prefix = [];
        let leftNodes = leftRaw.slice();
        let relationSubtype = "";
        let sourceOrder = "left_then_right";
        let licensedAsyndetic = false;
        let asyndeticLicense = "";
        let immediateTemporalTrigger = false;
        const leftLinkers = [];
        const rightLinkers = [];
        const leftIf = clauseRelationSurfaceIndex2(leftNodes, ["如果"]);
        const leftZi = clauseRelationSurfaceIndex2(leftNodes, ["只"]);
        const leftJiu = clauseRelationSurfaceIndex2(leftNodes, ["要"]);
        const leftHave = clauseRelationSurfaceIndex2(leftNodes, ["有"]);
        const leftNing = clauseRelationSurfaceIndex2(leftNodes, ["寧"]);
        const leftJyun = clauseRelationSurfaceIndex2(leftNodes, ["願"]);
        const leftGei = clauseRelationSurfaceIndex2(leftNodes, ["既"]);
        const leftJin = clauseRelationSurfaceIndex2(leftNodes, ["然"]);
        const leftBecause = clauseRelationSurfaceIndex2(leftNodes, ["因為"]);
        const leftAlthough = clauseRelationSurfaceIndex2(leftNodes, ["雖然"]);
        const rightBecause = clauseRelationSurfaceIndex2(rightRaw, ["因為"]);
        const rightSo = clauseRelationSurfaceIndex2(rightRaw, ["所以"]);
        const rightBut = clauseRelationSurfaceIndex2(rightRaw, ["但係", "不過"]);
        const rightThen = clauseRelationSurfaceIndex2(rightRaw, ["然後", "再"]);
        const leftFirst = clauseRelationSurfaceIndex2(leftNodes, ["先"]);
        const rightConditional = clauseRelationSurfaceIndex2(rightRaw, ["就"]);
        const rightNecessary = clauseRelationSurfaceIndex2(rightRaw, ["先至", "先"]);
        const rightDou = clauseRelationSurfaceIndex2(rightRaw, ["都"]);
        const rightM4 = clauseRelationSurfaceIndex2(rightRaw, ["唔"]);
        const rightM4Hou2 = clauseRelationSurfaceIndex2(rightRaw, ["唔好"]);
        const rightSoeng2 = clauseRelationSurfaceIndex2(rightRaw, ["想"]);
        const rightHang2 = clauseRelationSurfaceIndex2(rightRaw, ["肯"]);
        const rightGam2 = clauseRelationSurfaceIndex2(rightRaw, ["噉", "咁"]);
        const rightBat1Jyu4 = clauseRelationSurfaceIndex2(rightRaw, ["不如"]);
        const rightDang2 = clauseRelationSurfaceIndex2(rightRaw, ["等"]);
        const rightNgo5AfterDang2 = rightDang2 >= 0 ? clauseRelationSurfaceIndex2(rightRaw, ["我"], rightDang2 + 1) : -1;
        const rejectionMarkerIndexes = rightM4Hou2 === rightDou + 1 ? [rightDou, rightM4Hou2] : rightM4 === rightDou + 1 && (rightSoeng2 === rightM4 + 1 || rightHang2 === rightM4 + 1) ? [rightDou, rightM4, rightSoeng2 === rightM4 + 1 ? rightSoeng2 : rightHang2] : [];
        const committedPreferenceProfile = leftNing >= 0 && leftJyun === leftNing + 1 && rejectionMarkerIndexes.length > 0 && leftNodes.some((node, index) => index !== leftNing && index !== leftJyun && flattenSurface2(node)) && rightRaw.some((node, index) => !rejectionMarkerIndexes.includes(index) && flattenSurface2(node));
        let premiseResponseMarkerIndexes = [];
        let premiseResponseMarkerProfile = "unmarked";
        if (rightConditional === 0) {
          premiseResponseMarkerIndexes = [rightConditional];
          premiseResponseMarkerProfile = "zau6";
        } else if (rightGam2 === 0) {
          premiseResponseMarkerIndexes = [rightGam2];
          premiseResponseMarkerProfile = "gam2";
        } else if (rightBat1Jyu4 === 0) {
          premiseResponseMarkerIndexes = [rightBat1Jyu4];
          premiseResponseMarkerProfile = "bat1jyu4";
        } else if (rightDang2 === 0 && rightNgo5AfterDang2 === 1) {
          premiseResponseMarkerIndexes = [rightDang2, rightNgo5AfterDang2];
          premiseResponseMarkerProfile = "dang2ngo5";
        }
        const premiseResponseProfile = leftGei === 0 && leftJin === 1 && leftNodes.some((node, index) => index !== leftGei && index !== leftJin && flattenSurface2(node)) && rightRaw.some((node, index) => !premiseResponseMarkerIndexes.includes(index) && flattenSurface2(node));
        const necessaryConditionLeftContent = leftNodes.slice(leftHave + 1).flatMap((node) => clauseRelationLeafNodes(node));
        const necessaryConditionHasPredicateOrFrame = necessaryConditionLeftContent.some((node) => {
          if (!node || node.kind !== "token") return false;
          const slots = nodeSlots2(node);
          return slots.some((slot) => [
            "action_verb",
            "comment_predicate",
            "copula",
            "coverb_marker",
            "existential",
            "locative_marker",
            "main_verb",
            "modal",
            "negated_existential",
            "predicate",
            "stative_predicate"
          ].includes(slot));
        });
        const sufficientConditionProfile = leftZi === 0 && leftJiu === leftZi + 1 && rightConditional >= 0 && leftNodes.some((node, index) => index !== leftZi && index !== leftJiu && flattenSurface2(node)) && rightRaw.some((node, index) => index !== rightConditional && flattenSurface2(node));
        const necessaryResultPrefix = rightNecessary > 0 ? parsedClauseNodes(rightRaw.slice(0, rightNecessary)) : [];
        const necessaryResultPrefixNode = necessaryResultPrefix.length === 1 ? necessaryResultPrefix[0] : null;
        const necessaryResultMarkerPositionLicensed = rightNecessary === 0 || necessaryResultPrefixNode && (nodeCanFillSlot2(necessaryResultPrefixNode, "subject") || nodeCanFillSlot2(necessaryResultPrefixNode, "np") || nodeCanFillSlot2(necessaryResultPrefixNode, "head_noun") || nodeCanFillSlot2(necessaryResultPrefixNode, "topic"));
        const necessaryConditionProfile = leftZi >= 0 && leftHave === leftZi + 1 && rightNecessary >= 0 && necessaryResultMarkerPositionLicensed && necessaryConditionHasPredicateOrFrame && leftNodes.some((node, index) => index !== leftZi && index !== leftHave && flattenSurface2(node)) && rightRaw.some((node, index) => index !== rightNecessary && flattenSurface2(node));
        let relationProfile = "";
        let relationResearchId = "";
        let relationProfileScope = "";
        const leftImmediateOne = clauseRelationSurfaceIndex2(leftNodes, ["一"]);
        const leftSurfaces = clauseRelationSurfaceList(leftNodes);
        const rightSurfaces = clauseRelationSurfaceList(rightRaw);
        const leftHasImmediatePredicate = leftSurfaces.includes("見到") || leftSurfaces.includes("見") && leftSurfaces.includes("到");
        const leftTemporalNominal = leftNodes.length === 1 && leftNodes[0].kind === "construction" && leftNodes[0].type === "RelativeClauseNP" && clauseRelationTimeHead(leftNodes[0]);
        if (premiseResponseProfile) {
          relationSubtype = "premise_response";
          relationProfile = "established_premise_response";
          relationResearchId = "PRQ2-009";
          relationProfileScope = "left_initial_gei3jin4_with_overt_premise_and_response";
          leftLinkers.push(
            { index: leftGei, side: "left", semantic_role: "established_premise_marker_component" },
            { index: leftJin, side: "left", semantic_role: "established_premise_marker_component" }
          );
          for (const index of premiseResponseMarkerIndexes) {
            rightLinkers.push({ index, side: "right", semantic_role: "response_marker_component" });
          }
        } else if (committedPreferenceProfile) {
          relationSubtype = "committed_preference";
          relationProfile = "rejection";
          relationResearchId = "PRQ2-035";
          relationProfileScope = "overt_ning4jyun6_and_negative_dou1_continuation_only";
          leftLinkers.push(
            { index: leftNing, side: "left", semantic_role: "chosen_option_marker_component" },
            { index: leftJyun, side: "left", semantic_role: "chosen_option_marker_component" }
          );
          for (const index of rejectionMarkerIndexes) {
            rightLinkers.push({ index, side: "right", semantic_role: "rejected_option_marker_component" });
          }
        } else if (leftTemporalNominal) {
          relationSubtype = "temporal_subordinate";
        } else if (leftBecause >= 0 || rightBecause >= 0 || rightSo >= 0) {
          relationSubtype = "causal";
          if (leftBecause >= 0) leftLinkers.push({ index: leftBecause, side: "left", semantic_role: "reason_introducer" });
          if (rightSo >= 0) rightLinkers.push({ index: rightSo, side: "right", semantic_role: "result_linker" });
          if (rightBecause >= 0) {
            rightLinkers.push({ index: rightBecause, side: "right", semantic_role: "reason_introducer" });
            sourceOrder = "result_then_reason";
          }
        } else if (leftAlthough >= 0 || rightBut >= 0) {
          relationSubtype = "concessive";
          if (leftAlthough >= 0) leftLinkers.push({ index: leftAlthough, side: "left", semantic_role: "concession_introducer" });
          if (rightBut >= 0) rightLinkers.push({ index: rightBut, side: "right", semantic_role: "counterexpectation_linker" });
        } else if (necessaryConditionProfile) {
          relationSubtype = "conditional";
          relationProfile = "necessary_condition";
          relationResearchId = "PRQ2-014";
          relationProfileScope = "overt_left_marker_with_predicate_or_frame_and_overt_right_linker_only";
          leftLinkers.push(
            { index: leftZi, side: "left", semantic_role: "necessary_condition_marker_component" },
            { index: leftHave, side: "left", semantic_role: "necessary_condition_marker_component" }
          );
          rightLinkers.push({ index: rightNecessary, side: "right", semantic_role: "necessary_result_linker" });
        } else if (sufficientConditionProfile) {
          relationSubtype = "conditional";
          relationProfile = "sufficient_condition";
          relationResearchId = "PRQ2-008";
          relationProfileScope = "left_initial_overt_marker_and_overt_right_linker_only";
          leftLinkers.push(
            { index: leftZi, side: "left", semantic_role: "sufficient_condition_marker_component" },
            { index: leftJiu, side: "left", semantic_role: "sufficient_condition_marker_component" }
          );
          rightLinkers.push({ index: rightConditional, side: "right", semantic_role: "consequent_linker" });
        } else if (leftImmediateOne >= 0 && leftHasImmediatePredicate) {
          relationSubtype = "temporal_subordinate";
          immediateTemporalTrigger = true;
          leftLinkers.push({ index: leftImmediateOne, side: "left", semantic_role: "immediate_temporal_trigger" });
          if (rightConditional >= 0) rightLinkers.push({ index: rightConditional, side: "right", semantic_role: "result_linker" });
        } else if (leftHasImmediatePredicate && rightConditional >= 0) {
          relationSubtype = "temporal_subordinate";
          immediateTemporalTrigger = true;
          rightLinkers.push({ index: rightConditional, side: "right", semantic_role: "result_linker" });
        } else if (leftIf >= 0 || rightConditional >= 0) {
          relationSubtype = "conditional";
          if (leftIf >= 0) {
            if (leftIf > 0) {
              prefix = leftNodes.slice(0, leftIf);
              leftNodes = leftNodes.slice(leftIf);
            }
            leftLinkers.push({ index: 0, side: "left", semantic_role: "condition_introducer" });
          }
          const adjustedRightConditional = clauseRelationSurfaceIndex2(rightRaw, ["就"]);
          if (adjustedRightConditional >= 0) rightLinkers.push({ index: adjustedRightConditional, side: "right", semantic_role: "consequent_linker" });
        } else if (leftFirst >= 0 || rightThen >= 0 || leftSurfaces.includes("完")) {
          relationSubtype = leftFirst < 0 && rightThen < 0 ? "asyndetic_sequence" : "sequential";
          if (leftFirst >= 0) leftLinkers.push({ index: leftFirst, side: "left", semantic_role: "earlier_event_marker" });
          if (rightThen >= 0) rightLinkers.push({ index: rightThen, side: "right", semantic_role: "later_event_marker" });
          if (relationSubtype === "asyndetic_sequence") {
            licensedAsyndetic = true;
            asyndeticLicense = "left_completion_event_plus_following_event";
          } else if (leftFirst >= 0 && rightThen < 0) {
            licensedAsyndetic = true;
            asyndeticLicense = "overt_left_sequence_marker_with_omitted_right_marker";
          }
        }
        if (!relationSubtype) return null;
        if (immediateTemporalTrigger) {
          leftNodes = leftNodes.map((node) => node && node.kind === "token" ? token3(flattenSurface2(node)) : node);
        }
        const relation = buildClauseRelationEdge({
          relation_subtype: relationSubtype,
          left_nodes: leftNodes,
          right_nodes: rightRaw,
          left_linkers: leftLinkers,
          right_linkers: rightLinkers,
          separator,
          source_order: sourceOrder,
          licensed_asyndetic: licensedAsyndetic,
          asyndetic_license: asyndeticLicense,
          immediate_temporal_trigger: immediateTemporalTrigger,
          relation_subtype_provenance: "inherited_mapped_hierarchical_clause_relation_rule"
        });
        if (!relation) return null;
        if (relationProfile) {
          relation.trace = {
            ...relation.trace || {},
            relation_profile: relationProfile,
            research_id: relationResearchId,
            relation_profile_scope: relationProfileScope,
            ...relationSubtype === "premise_response" ? {
              response_marker_profile: premiseResponseMarkerProfile
            } : {}
          };
        }
        if (prefix.length) {
          const embedded = clauseRelationPrefixEmbedding(prefix, relation);
          if (embedded) return { node: embedded, relation, embedded: true };
          return null;
        }
        const wrapperCoverage = {
          status: "PASS",
          policy: "A hierarchical ClauseRelationGraph accounts for its complete local ClauseRelationEdge child; the separator and overt linkers remain visible inside that typed relation.",
          accounted_children: [{ surface: flattenSurface2(relation), construction: "ClauseRelationEdge", role: "local_typed_relation" }],
          accounted_linkers: [],
          accounted_separators: separator ? [{ surface: flattenSurface2(separator), role: "nested_visible_separator" }] : [],
          unaccounted_tokens: [],
          unaccounted_wrapper_token_count: 0
        };
        const wrapper = construction2("ClauseRelationGraph", "ClauseLink", [relation], {
          note: "Outer discourse wrapper containing one local typed clause relation. It preserves the established wrapper boundary without flattening relation semantics.",
          trace: traceInfo2("governed_discourse_wrapper", {
            rule: "hierarchical typed clause relation under discourse wrapper",
            reason: "The outer wrapper remains for discourse/root accounting, while ClauseRelationEdge represents the typed local relation.",
            child_constructions: ["ClauseRelationEdge"],
            local_relation_construction: "ClauseRelationEdge",
            graph_container_semantic_status: "neutral_container_only",
            independent_grammar_licensing: false,
            relation_semantics_source: "typed_child_edge_with_inherited_subtype_provenance",
            clause_linking_subtype: ["committed_preference", "premise_response"].includes(relationSubtype) ? relationSubtype : relationProfile || relationSubtype,
            relation_subtype: relationSubtype,
            ...relationProfile ? {
              relation_profile: relationProfile,
              research_id: relationResearchId,
              relation_profile_scope: relationProfileScope,
              ...relationSubtype === "premise_response" ? {
                response_marker_profile: premiseResponseMarkerProfile
              } : {}
            } : {},
            linkers: [...relation.trace && relation.trace.linker_left || [], ...relation.trace && relation.trace.linker_right || []],
            separators: separator ? [flattenSurface2(separator)] : [],
            wrapper_coverage: wrapperCoverage
          })
        });
        return { node: wrapper, relation, embedded: false };
      }
      function standaloneClauseRelationEdgeFragmentForTerminal2(segment = [], terminalText = "") {
        if (!/[。！？.!?]/u.test(String(terminalText || ""))) return null;
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 2 || compact[0].kind !== "token") return null;
        const surface = flattenSurface2(compact[0]);
        const subtypeBySurface = {
          "然後": "sequential",
          "再": "sequential",
          "但係": "concessive",
          "不過": "concessive",
          "所以": "causal",
          "因為": "causal",
          "如果": "conditional",
          "雖然": "concessive",
          "就": "conditional"
        };
        const relationSubtype = subtypeBySurface[surface];
        if (!relationSubtype) return null;
        const relationId = `clause-relation-fragment:${relationSubtype}:${compact.map((node) => flattenSurface2(node)).join("")}`;
        const missingSide = ["因為", "如果", "雖然"].includes(surface) ? "right_relation_member" : "left_relation_member";
        const semanticRole = surface === "因為" ? "reason_introducer" : surface === "如果" ? "condition_introducer" : surface === "雖然" ? "concession_introducer" : surface === "所以" ? "result_linker" : ["但係", "不過"].includes(surface) ? "counterexpectation_linker" : ["然後", "再"].includes(surface) ? "later_event_marker" : "consequent_linker";
        const member = clauseRelationMember(compact, {
          role: missingSide === "left_relation_member" ? "right" : "left",
          relation_subtype: relationSubtype,
          relation_id: relationId,
          linkers: [{ index: 0, side: missingSide === "left_relation_member" ? "right" : "left", semantic_role: semanticRole }]
        });
        if (!member) return null;
        return construction2("NeedsContext", "needs context", [member], {
          note: "Standalone relation fragment with an overt linker but no visible relation partner.",
          trace: traceInfo2("special_ambiguity_rule", {
            fragment_subtype: "standalone_clause_relation_fragment",
            relation_subtype: relationSubtype,
            clause_relation_id: relationId,
            relation_context_status: "context_required",
            context_requirement_status: "context_required",
            antecedent_status: "not_observed",
            missing_argument_slots: [missingSide],
            missing_slot_details: [{ slot: missingSide, license_status: "unresolved" }],
            overt_linker_surface: surface,
            linker_ownership_status: "owned_by_incomplete_local_relation",
            discourse_license_not_observed: true,
            semantic_review_flags: ["standalone_relation_partner_missing", "context_required_unresolved"],
            reason: "The overt linker requires a prior or following relation partner that is absent from the visible sentence. The following clause/VP remains transparent and no partner is fabricated.",
            not_claims: ["not_ignored_linker", "not_fabricated_relation_partner", "not_clean_context_free_clause"]
          })
        });
      }
      function clauseSequenceSegments2(nodes = []) {
        const segments = [];
        let current = [];
        for (const node of nodes || []) {
          if (isClauseSequenceSeparator2(node)) {
            if (withoutIgnorableSpaceText2(current).length) segments.push(withoutIgnorableSpaceText2(current));
            current = [];
          } else {
            current.push(node);
          }
        }
        if (withoutIgnorableSpaceText2(current).length) segments.push(withoutIgnorableSpaceText2(current));
        return segments;
      }
      function wrapClauseSequenceByPunctuation2(nodes) {
        const hasSeparator = nodes.some(isClauseSequenceSeparator2);
        if (!hasSeparator) return nodes;
        const finalOnly = nodes.length > 0 && isClauseSequenceTerminal2(nodes[nodes.length - 1]) ? nodes[nodes.length - 1] : null;
        const children = finalOnly ? nodes.slice(0, -1) : nodes.slice();
        const separatorIndex = children.findIndex(isClauseSequenceSeparator2);
        if (separatorIndex >= 0) {
          const linkerOnlySide = (side) => {
            const meaningfulSide = side.filter((node) => node && node.kind !== "text");
            return meaningfulSide.length === 1 && CLAUSE_LINKER_SURFACES2.has(flattenSurface2(meaningfulSide[0]));
          };
          if (linkerOnlySide(children.slice(0, separatorIndex)) || linkerOnlySide(children.slice(separatorIndex + 1))) {
            return nodes;
          }
        }
        const orderedPreferenceOpenerIndex = children.findIndex(
          (node, index) => index < separatorIndex && node && node.kind === "token" && (node.surface || "") === "與其"
        );
        const rightLeaves = separatorIndex >= 0 ? clauseRelationSegmentNodes(children.slice(separatorIndex + 1)) : [];
        if (orderedPreferenceOpenerIndex >= 0 && separatorIndex > orderedPreferenceOpenerIndex && rightLeaves.length > 1) {
          if (surfaceOf2(rightLeaves[0]) === "不如") {
            const preferredMember = parsedClauseNodes(rightLeaves.slice(1));
            const disfavoredMember = children.slice(orderedPreferenceOpenerIndex + 1, separatorIndex);
            const prefix = children.slice(0, orderedPreferenceOpenerIndex);
            if (meaningfulClauseConstructionCount(disfavoredMember) >= 1 && meaningfulClauseConstructionCount(preferredMember) >= 1) {
              const leftNodes = [
                ...prefix,
                children[orderedPreferenceOpenerIndex],
                ...disfavoredMember
              ];
              const rightNodes = [
                rightLeaves[0],
                ...preferredMember
              ];
              const leftLinkerIndex = clauseRelationSegmentNodes(prefix).length;
              const relation = buildClauseRelationEdge({
                relation_subtype: "ordered_preference",
                left_nodes: leftNodes,
                right_nodes: rightNodes,
                left_linkers: [{
                  index: leftLinkerIndex,
                  side: "left",
                  semantic_role: "disfavored_option_introducer"
                }],
                right_linkers: [{
                  index: 0,
                  side: "right",
                  semantic_role: "preferred_option_introducer"
                }],
                separator: children[separatorIndex],
                source_order: "disfavored_then_preferred",
                relation_subtype_provenance: "inherited_mapped_hierarchical_clause_relation_rule",
                reason: "The overt 與其 and 不如 pair orders two nonempty alternatives; the typed edge retains both members, owns both markers, and records which option is disfavored and which is preferred."
              });
              if (relation) {
                const wrapperCoverage2 = {
                  status: "PASS",
                  policy: "A hierarchical ClauseRelationGraph accounts for its complete local ClauseRelationEdge child; the separator and overt linkers remain visible inside that typed relation.",
                  accounted_children: [{ surface: flattenSurface2(relation), construction: "ClauseRelationEdge", role: "local_typed_relation" }],
                  accounted_linkers: [],
                  accounted_separators: [{ surface: flattenSurface2(children[separatorIndex]), role: "nested_visible_separator" }],
                  unaccounted_tokens: [],
                  unaccounted_wrapper_token_count: 0
                };
                const wrapper = construction2("ClauseRelationGraph", "ClauseLink", [relation], {
                  note: "Outer discourse wrapper containing the overt paired ordered-preference relation.",
                  trace: traceInfo2("governed_discourse_wrapper", {
                    rule: "與其...不如 ordered-preference clause-linking sequence",
                    reason: "The outer wrapper preserves discourse/root accounting while the typed child edge owns the paired markers and alternative-order semantics.",
                    child_constructions: ["ClauseRelationEdge"],
                    local_relation_construction: "ClauseRelationEdge",
                    graph_container_semantic_status: "neutral_container_only",
                    independent_grammar_licensing: false,
                    relation_semantics_source: "typed_child_edge_with_inherited_subtype_provenance",
                    relation_subtype: "ordered_preference",
                    linkers: [...relation.trace && relation.trace.linker_left || [], ...relation.trace && relation.trace.linker_right || []],
                    separators: [flattenSurface2(children[separatorIndex])],
                    wrapper_coverage: wrapperCoverage2,
                    clause_linking_subtype: "ordered_preference",
                    marker_profile: "jyu5kei4_bat1jyu4",
                    research_id: "PRQ2-013"
                  })
                });
                return [wrapper, ...finalOnly ? [finalOnly] : []];
              }
            }
          }
        }
        const hierarchicalRelation = hierarchicalClauseRelationEdgeFromChildren(children);
        if (hierarchicalRelation && hierarchicalRelation.node) {
          return [hierarchicalRelation.node, ...finalOnly ? [finalOnly] : []];
        }
        const meaningful = children.filter(isClauseSequenceMeaningfulNode);
        if (meaningful.length < 2) return nodes;
        const conditionalChildIndex = children.findIndex((node) => node && node.kind === "construction" && node.type === "ConditionalClause");
        if (conditionalChildIndex >= 0) {
          const separatorAfterCondition = children.findIndex((node, index) => index > conditionalChildIndex && isClauseSequenceSeparator2(node));
          const resultChild = children.find((node, index) => index > separatorAfterCondition && node && node.kind === "construction");
          if (separatorAfterCondition >= 0 && resultChild) {
            const conditional = children[conditionalChildIndex];
            conditional.trace = {
              ...conditional.trace || {},
              context_requirement_status: "context_licensed",
              missing_argument_slots: [],
              missing_slot_details: [{ slot: "result_clause", license_status: "licensed", licensed_by: "following_result_clause" }],
              result_clause_status: "overt_following_clause",
              result_construction: resultChild.type,
              not_claims: Array.from(/* @__PURE__ */ new Set([...conditional.trace && conditional.trace.not_claims || [], "not_standalone_incomplete_condition"]))
            };
          }
        }
        const wrapperCoverage = clauseLinkingWrapperCoverage2(children);
        const topicChainTrace = applyTopicChainNullObjectLinkage2(children);
        return [
          construction2("ClauseRelationGraph", topicChainTrace && topicChainTrace.topic_chain_status === "licensed_overt_topic_chain" ? "TopicChain" : "ClauseLink", children, {
            note: topicChainTrace && topicChainTrace.topic_chain_status === "licensed_overt_topic_chain" ? "Topic chain: one overt recoverable topic supplies the understood object/domain of later linked predicates without hidden tokens." : "Linked clauses. This governed discourse/coordination wrapper preserves all child constructions and explicitly accounts for linker/separator material; it does not replace or flatten the child clauses.",
            trace: traceInfo2("governed_discourse_wrapper", {
              rule: "comma-separated clause-linking sequence",
              reason: "Native speech often links short clauses or discourse units with comma-like punctuation. Add a parent wrapper only when at least two meaningful clause-like constructions are separated by visible punctuation; do not model this as a phrase-internal generative template, and do not hide unaccounted wrapper holes.",
              graph_container_semantic_status: "neutral_container_only",
              independent_grammar_licensing: false,
              relation_semantics_source: "none_or_preexisting_discourse_linkage_only",
              child_constructions: children.filter((node) => node && node.kind === "construction").map((node) => node.type),
              linkers: clauseLinkerInventory2(children),
              separators: children.filter(isClauseSequenceSeparator2).map((node) => node.text),
              ...conditionalChildIndex >= 0 ? { clause_linking_subtype: "condition_result", condition_construction: "ConditionalClause" } : {},
              ...topicChainTrace ? { clause_linking_subtype: topicChainTrace.topic_chain_status === "licensed_overt_topic_chain" ? "topic_chain_null_object" : "topic_chain_compatibility_review", ...topicChainTrace } : {},
              wrapper_coverage: wrapperCoverage
            })
          }),
          ...finalOnly ? [finalOnly] : []
        ];
      }
      function clauseSequenceHasVisiblePunctuation(node) {
        if (!node || node.kind !== "construction" || node.type !== "ClauseSequence" && node.type !== "ClauseRelationGraph") return false;
        const traceSeparators = node.trace && Array.isArray(node.trace.separators) ? node.trace.separators : [];
        if (traceSeparators.length) return true;
        return (node.children || []).some(isClauseSequenceSeparator2);
      }
      function shouldCollapseClauseSequenceForDisplay2(node, options = {}) {
        if (!node || node.kind !== "construction" || node.type !== "ClauseSequence") return false;
        if (options && options.showDiagnostics) return false;
        return clauseSequenceHasVisiblePunctuation(node);
      }
      function shouldCollapseGreedyWrapperForDisplay2(node, options = {}) {
        if (!node || node.kind !== "construction") return false;
        if (options && options.showDiagnostics) return false;
        return node.type === "ModalANotAQuestion";
      }
      return {
        connectorAwareClauseLinkingForTerminal: connectorAwareClauseLinkingForTerminal2,
        completionThenClauseRelation: completionThenClauseRelation2,
        standaloneClauseRelationEdgeFragmentForTerminal: standaloneClauseRelationEdgeFragmentForTerminal2,
        clauseRelationSurfaceIndex: clauseRelationSurfaceIndex2,
        clauseRelationParsedChunk: clauseRelationParsedChunk2,
        clauseSequenceSegments: clauseSequenceSegments2,
        wrapClauseSequenceByPunctuation: wrapClauseSequenceByPunctuation2,
        shouldCollapseClauseSequenceForDisplay: shouldCollapseClauseSequenceForDisplay2,
        shouldCollapseGreedyWrapperForDisplay: shouldCollapseGreedyWrapperForDisplay2
      };
    };
  }
});

// src/parser/detectors/relative-clauses/clauses.js
var require_clauses6 = __commonJS({
  "src/parser/detectors/relative-clauses/clauses.js"(exports2, module2) {
    "use strict";
    module2.exports = function createRelativeClauseDetectors2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        clauseRelationParsedChunk: clauseRelationParsedChunk2,
        clauseRelationSurfaceIndex: clauseRelationSurfaceIndex2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        cp021bIsBei2Marker: cp021bIsBei2Marker2,
        flattenSurface: flattenSurface2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function relativeClauseGapPredicate(leftNodes = [], headSurface = "") {
        const compact = withoutIgnorableSpaceText2(leftNodes || []);
        if (compact.length !== 2 || !nodeCanFillSlot2(compact[0], "subject")) return null;
        const verb = compact[1];
        if (!nodeCanFillSlot2(verb, "action_verb")) return null;
        const vp = construction2("TransitiveVP", "V+O", [verb], {
          note: "Transitive predicate with an overt relative-clause head licensing the object gap; no hidden object token is inserted.",
          trace: traceInfo2("generative_template", {
            construction_type: "TransitiveVP",
            template_family: "generative_template",
            template: ["action_verb!", "relative_object_gap!"],
            assigned_slots: ["action_verb"],
            relative_gap_status: "licensed_by_overt_head_noun",
            relative_head_surface: headSurface,
            missing_argument_slots: ["object"],
            reason: "The relative-clause head noun supplies the overt nominal domain for the visible transitive predicate. The parser records the dependency without inserting an object token.",
            not_claims: ["not_fabricated_object_token"]
          })
        });
        return construction2("SubjectPredicateClause", "Clause", [compact[0], vp], {
          note: "Relative modifier clause with overt subject and a head-licensed object gap.",
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "predicate!"],
            assigned_slots: ["subject", "predicate"],
            relative_gap_status: "licensed_by_overt_head_noun",
            relative_head_surface: headSurface,
            reason: "The overt subject and visible transitive predicate form the relative modifier clause; the following head noun licenses the object dependency.",
            not_claims: ["not_fabricated_object_token"]
          })
        });
      }
      function relativeClauseNPForTerminal2(segment = []) {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.filter(cp021bIsBei2Marker2).length >= 2) return null;
        const linkerIndex = clauseRelationSurfaceIndex2(compact, ["嘅"]);
        if (linkerIndex <= 0 || linkerIndex >= compact.length - 1) return null;
        const head = compact[linkerIndex + 1];
        if (!(nodeCanFillSlot2(head, "head_noun") || nodeCanFillSlot2(head, "time") || nodeCanFillSlot2(head, "time_head") || ["時候", "時間"].includes(flattenSurface2(head)))) return null;
        const left = compact.slice(0, linkerIndex);
        const headSurface = flattenSurface2(head);
        const temporalHead = ["時候", "時間"].includes(headSurface);
        const leftHasActionPredicate = left.some((node) => nodeCanFillSlot2(node, "action_verb"));
        if (!temporalHead && !leftHasActionPredicate) return null;
        const leftFinal = left[left.length - 1] || null;
        const leftFinalLooksNominal = leftFinal && !nodeCanFillSlot2(leftFinal, "action_verb") && (nodeCanFillSlot2(leftFinal, "np") || nodeCanFillSlot2(leftFinal, "head_noun") || nodeCanFillSlot2(leftFinal, "location") || nodeCanFillSlot2(leftFinal, "object"));
        if (!temporalHead && left.length >= 2 && leftFinalLooksNominal) return null;
        let modifierParsed = applyConstructionPatterns2(left);
        let modifierClause = modifierParsed.length === 1 && modifierParsed[0].kind === "construction" ? modifierParsed[0] : null;
        if (!modifierClause) modifierClause = relativeClauseGapPredicate(left, headSurface);
        if (!modifierClause) {
          const chunk = clauseRelationParsedChunk2(left, { relative_head_surface: headSurface });
          if (chunk.length === 1 && chunk[0].kind === "construction") modifierClause = chunk[0];
        }
        if (!modifierClause || !(nodeCanFillSlot2(modifierClause, "clause") || nodeCanFillSlot2(modifierClause, "predicate") || nodeCanFillSlot2(modifierClause, "vp"))) return null;
        const linker = parserInactiveTokenClone2(compact[linkerIndex], {
          label: "particle",
          pos: "particle",
          syntax: "nominal_linker relative_clause_linker",
          slots: cleanSlots2([...compact[linkerIndex].slots || [], "nominal_linker", "relative_clause_linker"]),
          reason: "嘅 links the visible modifier clause to its overt head noun inside a relative-clause NP."
        });
        const relativeNP = construction2("RelativeClauseNP", "RelNP", [modifierClause, linker, head], {
          note: "Relative-clause noun phrase with a visible modifier clause, overt 嘅 linker, and overt head noun.",
          trace: traceInfo2("generative_template", {
            construction_type: "RelativeClauseNP",
            template_family: "generative_template",
            template: ["relative_clause!", "nominal_linker!", "head_noun!"],
            assigned_slots: ["relative_clause", "nominal_linker", "head_noun"],
            relation_subtype: "relative_nominal_modifier",
            relative_clause_construction: modifierClause.type,
            head_noun_surface: headSurface,
            temporal_head: ["時候", "時間"].includes(headSurface),
            reason: "The modifier clause attaches inside the NP before any matrix predicate or temporal-subordinate relation is built.",
            not_claims: ["not_flat_clause_sequence", "not_hidden_head_noun", "not_hidden_relative_gap_token"]
          })
        });
        const tail = compact.slice(linkerIndex + 2);
        if (!tail.length) return relativeNP;
        const predicate = clauseRelationParsedChunk2(tail);
        if (!predicate.length || !predicate.some((node) => node && (node.kind === "construction" || nodeCanFillSlot2(node, "predicate") || nodeCanFillSlot2(node, "vp")))) return null;
        return construction2("SubjectPredicateClause", "Clause", [relativeNP, ...predicate], {
          note: "Matrix clause whose overt subject/topic is a relative-clause NP.",
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "predicate!"],
            assigned_slots: ["subject", "predicate"],
            relative_np_subject: true,
            reason: "The relative-clause NP is completed before attachment to the visible matrix predicate."
          })
        });
      }
      return { relativeClauseNPForTerminal: relativeClauseNPForTerminal2 };
    };
  }
});

// src/parser/detectors/topic-comment.js
var require_topic_comment = __commonJS({
  "src/parser/detectors/topic-comment.js"(exports2, module2) {
    "use strict";
    module2.exports = function createTopicCommentDetectors(dependencies = {}) {
      const { cleanSlots: cleanSlots2, construction: construction2, isTopicCandidate: isTopicCandidate2, templateDerivedSlots: templateDerivedSlots2, traceInfo: traceInfo2, wrapPredicate: wrapPredicate2 } = dependencies;
      function topicCommentFallback2(core) {
        if (!core.length || !isTopicCandidate2(core[0]) || core.length < 2) return null;
        const topic = construction2("Topic", "topic", [core[0]], {
          primary: "topic",
          note: "Topic with secondary semantic role what."
        });
        const commentChildren = wrapPredicate2(core.slice(1));
        const children = [topic, ...commentChildren];
        return construction2("TopicComment", "TopicComment", children, {
          note: "Topic-comment construction with comment represented as predicate-role metadata rather than a redundant child wrapper.",
          slots: cleanSlots2(["topic_comment", "topic", "comment", "comment_predicate", "predicate", "clause", ...templateDerivedSlots2("TopicComment", children)]),
          trace: traceInfo2("generative_or_heuristic_slot_rule", {
            rule: "topic candidate followed by typed comment predicate",
            reason: "Structural heuristic; the comment relation is carried by TopicComment slots rather than a standalone Comment construction."
          })
        });
      }
      return { topicCommentFallback: topicCommentFallback2 };
    };
  }
});

// src/parser/terminal/questions/final-particles.js
var require_final_particles = __commonJS({
  "src/parser/terminal/questions/final-particles.js"(exports2, module2) {
    "use strict";
    module2.exports = function createFinalParticleQuestions2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        fullSpanSingleConstruction: fullSpanSingleConstruction2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function isExplicitWhQuestionConstruction(node) {
        if (!node || node.kind !== "construction") return false;
        if ([
          "ProgressiveWhObjectQuestion",
          "ExistentialWhQuestion",
          "ScalarValueQuestion",
          "PlaceQuestion"
        ].includes(node.type)) return true;
        return (node.children || []).some(isExplicitWhQuestionConstruction);
      }
      function isProtectedMeReactionFormula(node) {
        return !!(node && node.kind === "construction" && node.type === "FormulaDiscourseUnit" && node.trace && node.trace.formula_type === "confirmation_surprise_question");
      }
      function propositionLikeHostForFinalMe(nodes) {
        if (!nodes || !nodes.length) return null;
        const wrapped = applyConstructionPatterns2(nodes);
        const host = fullSpanSingleConstruction2(wrapped, nodes);
        if (!host) return null;
        if (["NeedsContext", "MalformedCandidate", "FragmentQuestion", "FragmentAnswer", "NominalHeadSpan"].includes(host.type)) return null;
        const trace = host.trace || {};
        if (["context_required", "context_incompatible"].includes(trace.context_requirement_status)) return null;
        if (!nodeCanFillSlot2(host, "predicate") && !nodeCanFillSlot2(host, "clause") && !nodeCanFillSlot2(host, "vp") && !nodeCanFillSlot2(host, "modal_vp")) return null;
        return host;
      }
      function finalMeQuestionParticleClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_polar_question_particle biased_question_particle",
          slots: ["particle", "yes_no_question_marker", "question_marker"],
          jyutping: "me1",
          note: "sentence-final polar question particle; often conveys surprise or negative expectation",
          reason: "Final 咩 scopes over a complete proposition-like host as a biased polar-question particle, not as a wh object."
        });
      }
      function finalMePolarQuestionFallbackForPunctuation2(segment, terminalText = "", ordinaryWrapped = null) {
        if (!/[？?]/u.test(String(terminalText || ""))) return null;
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 2 || !isToken2(compact[compact.length - 1], "咩")) return null;
        const ordinaryTop = fullSpanSingleConstruction2(ordinaryWrapped, compact);
        if (ordinaryTop && (isExplicitWhQuestionConstruction(ordinaryTop) || isProtectedMeReactionFormula(ordinaryTop))) return null;
        const hostNodes = compact.slice(0, -1);
        const host = propositionLikeHostForFinalMe(hostNodes);
        if (!host) return null;
        const particle = finalMeQuestionParticleClone(compact[compact.length - 1]);
        const children = [host, particle];
        return construction2("PolarQuestionFrame", "YesNo?", children, {
          note: "Biased polar question formed by a complete proposition-like host plus sentence-final 咩.",
          slots: templateDerivedSlots2("PolarQuestionFrame", children),
          trace: traceInfo2("generative_template", {
            construction_type: "PolarQuestionFrame",
            template_family: "generative_template",
            template: ["proposition_host!", "polar_question_particle!"],
            assigned_slots: ["proposition_host", "polar_question_particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            question_family: "polar",
            question_subtype: "biased_sentence_final_me1",
            bias: "negative_expectation_or_surprise",
            proposition_host_construction: host.type,
            proposition_host_surface: flattenSurface2(host),
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            antecedent_status: "not_applicable",
            not_claims: ["not_wh_object", "not_wh_determiner", "not_a_not_a_question", "not_fabricated_proposition"],
            reason: "A complete proposition-like host licenses sentence-final 咩 as a biased polar particle. Existing explicit wh-question constructions and protected 係咩 reaction formulae retain precedence."
          })
        });
      }
      return { finalMePolarQuestionFallbackForPunctuation: finalMePolarQuestionFallbackForPunctuation2 };
    };
  }
});

// src/parser/terminal/particles/discourse.js
var require_discourse = __commonJS({
  "src/parser/terminal/particles/discourse.js"(exports2, module2) {
    "use strict";
    module2.exports = function createTerminalDiscourseParticles2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        fullSpanSingleConstruction: fullSpanSingleConstruction2,
        hasSurface: hasSurface2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function isQuestionLikeScopeHost(node) {
        if (!node || node.kind !== "construction") return false;
        if (/Question$/.test(String(node.type || ""))) return true;
        return [
          "question_fragment",
          "question_marker",
          "yes_no_question_marker",
          "wh_object",
          "wh_determiner",
          "identity_question",
          "location_question",
          "time_question"
        ].some((slot) => nodeCanFillSlot2(node, slot));
      }
      function propositionLikeHostForScopedDiscourseParticle2(nodes) {
        if (!nodes || !nodes.length) return null;
        const wrapped = applyConstructionPatterns2(nodes);
        const host = fullSpanSingleConstruction2(wrapped, nodes);
        if (!host) return null;
        if ([
          "NeedsContext",
          "MalformedCandidate",
          "FragmentQuestion",
          "FragmentAnswer",
          "ComplementEllipsisFragment",
          "NominalHeadSpan",
          "FormulaDiscourseUnit",
          "ClauseSequence",
          "ClauseRelationGraph"
        ].includes(host.type)) return null;
        if (isQuestionLikeScopeHost(host)) return null;
        const trace = host.trace || {};
        if (["context_required", "context_incompatible"].includes(trace.context_requirement_status)) return null;
        if (!nodeCanFillSlot2(host, "predicate") && !nodeCanFillSlot2(host, "clause") && !nodeCanFillSlot2(host, "vp") && !nodeCanFillSlot2(host, "modal_vp")) return null;
        return host;
      }
      function epistemicScopeParticleClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle epistemic_uncertainty_particle",
          slots: ["particle", "epistemic_scope_particle", "discourse_scope_particle"],
          jyutping: "gwaa3",
          note: "probably / perhaps; marks uncertainty or probability over the preceding proposition",
          reason: "Final 啩 scopes epistemic uncertainty over a complete proposition-like host and does not supply a missing proposition."
        });
      }
      function scopedEpistemicDiscourseParticleFallback2(segment, terminalText = "") {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 2) return null;
        const finalNode = compact[compact.length - 1];
        if (!nodeCanFillSlot2(finalNode, "epistemic_scope_particle")) return null;
        const host = propositionLikeHostForScopedDiscourseParticle2(compact.slice(0, -1));
        if (!host) return null;
        const particle = epistemicScopeParticleClone(finalNode);
        const children = [host, particle];
        return construction2("DiscourseParticleFrame", "Uncertain", children, {
          note: "Proposition-like host plus a sentence-final discourse particle carrying epistemic uncertainty or probability.",
          slots: templateDerivedSlots2("DiscourseParticleFrame", children),
          trace: traceInfo2("generative_template", {
            construction_type: "DiscourseParticleFrame",
            template_family: "generative_template",
            template: ["proposition_host!", "epistemic_scope_particle!"],
            assigned_slots: ["proposition_host", "epistemic_scope_particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            discourse_particle_family: "epistemic_stance",
            particle_subtype: "uncertainty_probability_gwaa3",
            epistemic_scope: "uncertainty_or_probability",
            proposition_host_construction: host.type,
            proposition_host_surface: flattenSurface2(host),
            punctuation_hint: /[？?]/u.test(String(terminalText || "")) ? "question_marked" : "declarative_or_unmarked",
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            antecedent_status: "not_applicable",
            not_claims: [
              "not_fragment_licensor",
              "not_noun_host",
              "not_question_host",
              "not_fabricated_proposition",
              "not_tone_specific_beyond_gwaa3"
            ],
            reason: "A complete non-question proposition-like host licenses final 啩 as an epistemic uncertainty/probability particle. Bare particles, noun hosts, unresolved hosts, and question hosts remain outside this wrapper."
          })
        });
      }
      function evidentialScopeParticleClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle evidential_noteworthiness_particle",
          slots: ["particle", "evidential_scope_particle", "discourse_scope_particle"],
          jyutping: "wo3",
          note: "reported / noteworthy / reminder stance; exact subtype depends on tone and context",
          reason: "Final 喎 scopes broad evidential/noteworthiness stance over a complete proposition-like host. Written form alone does not select an exact tone-specific subtype."
        });
      }
      function scopedEvidentialDiscourseParticleFallback2(segment, terminalText = "") {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 2 || !isToken2(compact[compact.length - 1], "喎")) return null;
        const host = propositionLikeHostForScopedDiscourseParticle2(compact.slice(0, -1));
        if (!host) return null;
        const particle = evidentialScopeParticleClone(compact[compact.length - 1]);
        const children = [host, particle];
        return construction2("DiscourseParticleFrame", "Notice", children, {
          note: "Proposition-like host plus a sentence-final discourse particle carrying broad evidential or noteworthiness stance.",
          slots: templateDerivedSlots2("DiscourseParticleFrame", children),
          trace: traceInfo2("generative_template", {
            construction_type: "DiscourseParticleFrame",
            template_family: "generative_template",
            template: ["proposition_host!", "evidential_scope_particle!"],
            assigned_slots: ["proposition_host", "evidential_scope_particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            discourse_particle_family: "evidential_noteworthiness",
            broad_particle_class: "EvidentialDiscourseParticle",
            particle_subtype: "written_wo_family_underdetermined",
            evidential_subtype: "not_selected_without_tone_or_context",
            evidential_scope: "reportative_noteworthiness_reminder_or_counterexpectation",
            tone_source: "canonical_written_form_reading_only",
            tone_certainty: "underdetermined_from_character_alone",
            proposition_host_construction: host.type,
            proposition_host_surface: flattenSurface2(host),
            punctuation_hint: /[？?]/u.test(String(terminalText || "")) ? "question_marked_surface_without_tone_inference" : "declarative_or_unmarked",
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            antecedent_status: "not_applicable",
            not_claims: [
              "not_fragment_licensor",
              "not_noun_host",
              "not_question_host",
              "not_fabricated_proposition",
              "not_exact_tone_inference",
              "not_exact_evidential_subtype"
            ],
            reason: "A complete non-question proposition-like host licenses final 喎 as a broad evidential/noteworthiness discourse particle. The written character and canonical wo3 display reading do not justify selecting a narrower tone-specific function."
          })
        });
      }
      function protectedFullSpanFormulaForParticleFallback(ordinaryWrapped, sourceNodes) {
        const top = fullSpanSingleConstruction2(ordinaryWrapped, sourceNodes);
        return !!(top && top.type === "FormulaDiscourseUnit");
      }
      function directiveLikeHostForFinalLaa1(nodes) {
        if (!nodes || !nodes.length) return null;
        const wrapped = applyConstructionPatterns2(nodes);
        const host = fullSpanSingleConstruction2(wrapped, nodes);
        if (!host) return standaloneWalkHostForFinalLaaParticle(nodes);
        if (![
          "LexicalGiveRelation",
          "PostThemeParticipantRelation",
          "SubjectPredicateClause",
          "DirectionalMotionVP",
          "MotionGoalVP",
          "ProhibitiveImperative",
          "PriorityMarkerClause",
          "SerialVerbPurposeChain",
          "MotionPurposeChain"
        ].includes(host.type)) return null;
        if (isQuestionLikeScopeHost(host)) return null;
        const trace = host.trace || {};
        if (["context_required", "context_incompatible"].includes(trace.context_requirement_status)) return null;
        return host;
      }
      function standaloneWalkMotionVp(node, reason = "") {
        if (!isToken2(node, "走")) return null;
        const movement = parserInactiveTokenClone2(node, {
          label: "doing",
          pos: "verb",
          syntax: "intransitive_motion_verb transition_motion_predicate",
          slots: ["action_verb", "main_verb", "movement_verb", "predicate"],
          jyutping: "zau2",
          note: "leave / go away",
          reason: reason || "Standalone 走 is the independent movement predicate 'leave/go away', not a postverbal result complement.",
          active_affordance_match: "standalone_motion_predicate"
        });
        return construction2("DirectionalMotionVP", "MotionVP", [movement], {
          slots: ["directional_motion_vp", "vp", "action_vp", "predicate", "movement_verb", "motion_predicate"],
          note: "One-word transition motion predicate headed by standalone 走.",
          trace: traceInfo2("generative_template", {
            construction_type: "DirectionalMotionVP",
            template_family: "generative_template",
            template: ["transition_motion_verb!"],
            assigned_slots: ["transition_motion_verb"],
            surfaces: ["走"],
            contextual_role_resolution: "standalone_motion_predicate_not_result_complement",
            subspan: true,
            reason: reason || "Standalone 走 is an independent motion predicate announcing departure."
          })
        });
      }
      function locativeFragmentFromWrappedPlacePhrase(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length !== 1) return null;
        const only = compact[0];
        let phrase = null;
        if (only && only.kind === "construction" && only.type === "LocativePlacePhrase") {
          phrase = only;
        } else if (only && only.kind === "construction" && only.type === "NominalHeadSpan" && Array.isArray(only.children) && only.children.length === 1 && only.children[0].kind === "construction" && only.children[0].type === "LocativePlacePhrase") {
          phrase = only.children[0];
        }
        if (!phrase) return null;
        if (hasSurface2([phrase], "邊度") || nodeCanFillSlot2(phrase, "location_question")) return null;
        const phraseChildren = withoutIgnorableSpaceText2(phrase.children || []);
        if (!phraseChildren.length || !isToken2(phraseChildren[0], "喺")) return null;
        return construction2("LocativeFragment", "Location", [phrase], {
          slots: cleanSlots2(["locative_fragment", "location", "clause"]),
          note: "Locative fragment: 喺 + overt place, with the located figure understood from discourse.",
          trace: traceInfo2("generative_template", {
            construction_type: "LocativeFragment",
            template_family: "generative_template",
            template: ["locative_place_phrase!"],
            assigned_slots: ["locative_place_phrase"],
            surfaces: [flattenSurface2(phrase)],
            fragment_subtype: "locative_answer_or_predicate_fragment",
            omitted_element_description: "located figure or subject understood from discourse",
            reason: "The location is overt; only the person or thing located there is understood from context.",
            not_claims: ["not_missing_location", "not_full_subject_predicate_clause"]
          })
        });
      }
      function completionThenStandaloneWalkResolution2(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        const completionIndex = compact.findIndex((node) => node && node.kind === "construction" && node.type === "CompletionVP");
        if (completionIndex < 0 || completionIndex > 1) return nodes;
        if (completionIndex === 1 && !nodeCanFillSlot2(compact[0], "subject")) return nodes;
        if (!isToken2(compact[completionIndex + 1], "就") || !isToken2(compact[completionIndex + 2], "走")) return nodes;
        const trailing = compact.slice(completionIndex + 3);
        if (trailing.length > 1 || trailing.length === 1 && !nodeCanFillSlot2(trailing[0], "particle")) return nodes;
        const target = compact[completionIndex + 2];
        const motion = standaloneWalkMotionVp(
          target,
          "After a completed VP plus 就, standalone 走 is the follow-up movement predicate 'leave/go away'; it is not the result complement of the earlier verb."
        );
        if (!motion) return nodes;
        return (nodes || []).map((node) => node === target ? motion : node);
      }
      function standaloneWalkHostForFinalLaaParticle(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (compact.length !== 2 || !nodeCanFillSlot2(compact[0], "subject") || !isToken2(compact[1], "走")) return null;
        const subject = compact[0];
        const motion = standaloneWalkMotionVp(
          compact[1],
          "Standalone 走 after an overt subject is the movement predicate 'leave/go away'; the final discourse particle contributes scope separately and does not determine the verb's category."
        );
        const children = [subject, motion];
        return construction2("SubjectPredicateClause", "SubjPred", children, {
          slots: templateDerivedSlots2("SubjectPredicateClause", children),
          note: "Subject plus an independent transition-motion predicate.",
          trace: traceInfo2("generative_template", {
            construction_type: "SubjectPredicateClause",
            template_family: "generative_template",
            template: ["subject!", "predicate!"],
            assigned_slots: ["subject", "predicate"],
            surfaces: children.map((node) => flattenSurface2(node)),
            predicate_subtype: "transition_motion",
            reason: "The subject and standalone movement predicate form a proposition host before final 啦 or 喇 adds its own discourse scope."
          })
        });
      }
      function changeStateHostForFinalLaa3(nodes) {
        const ordinary = propositionLikeHostForScopedDiscourseParticle2(nodes);
        if (ordinary && ![
          "LexicalGiveRelation",
          "PostThemeParticipantRelation",
          "CoverbFrame",
          "FormulaDiscourseUnit",
          "AcceptabilityClause"
        ].includes(ordinary.type)) return ordinary;
        return standaloneWalkHostForFinalLaaParticle(nodes);
      }
      function directiveScopeParticleClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle directive_closure_particle",
          slots: ["particle", "directive_scope_particle", "discourse_scope_particle"],
          jyutping: "laa1",
          note: "directive / suggestion / invitation / interpersonal closure",
          reason: "Final 啦 laa1 scopes directive, suggestion, invitation, or interpersonal closure over a licensed host and does not repair incomplete argument structure."
        });
      }
      function scopedDirectiveClosureParticleFallback2(segment, terminalText = "", ordinaryWrapped = null) {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 2 || !isToken2(compact[compact.length - 1], "啦")) return null;
        if (fullSpanSingleConstruction2(ordinaryWrapped, compact)) return null;
        const host = directiveLikeHostForFinalLaa1(compact.slice(0, -1));
        if (!host) return null;
        const particle = directiveScopeParticleClone(compact[compact.length - 1]);
        const children = [host, particle];
        return construction2("DiscourseParticleFrame", "Prompt", children, {
          note: "Licensed directive-like host plus final 啦 laa1 carrying directive/interpersonal closure scope.",
          slots: templateDerivedSlots2("DiscourseParticleFrame", children),
          trace: traceInfo2("generative_template", {
            construction_type: "DiscourseParticleFrame",
            template_family: "generative_template",
            template: ["directive_host!", "directive_scope_particle!"],
            assigned_slots: ["directive_host", "directive_scope_particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            discourse_particle_family: "directive_interpersonal_closure",
            broad_particle_class: "DirectiveClosureParticle",
            particle_subtype: "directive_suggestion_invitation_or_closure_laa1",
            tone_source: "canonical_written_form_reading",
            tone_certainty: "canonical_for_written_laa_character_but_real_spelling_varies",
            orthographic_uncertainty: "啦_and_喇_may_be_spelled_inconsistently_in_ordinary_writing",
            scope_host_construction: host.type,
            scope_host_surface: flattenSurface2(host),
            punctuation_hint: /[？?]/u.test(String(terminalText || "")) ? "question_marked_surface" : "declarative_or_unmarked",
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            antecedent_status: "not_applicable",
            not_claims: ["not_fragment_licensor", "not_noun_host", "not_unresolved_host", "not_change_state_laa3", "not_exact_illocution_beyond_broad_family"],
            reason: "A licensed directive/clause host permits final 啦 as broad directive, suggestion, invitation, or interpersonal closure. Bare particles, nouns, and unresolved predicates remain outside the wrapper."
          })
        });
      }
      function changeStateScopeParticleClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: "sentence_final_particle change_state_current_relevance_particle",
          slots: ["particle", "change_state_scope_particle", "discourse_scope_particle"],
          jyutping: "laa3",
          note: "changed situation / current relevance / transition",
          reason: "Final 喇 laa3 scopes change of situation, current relevance, or transition over a proposition-like host and remains distinct from VP-internal perfective 咗."
        });
      }
      function scopedChangeStateParticleFallback2(segment, terminalText = "", ordinaryWrapped = null) {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 2 || !isToken2(compact[compact.length - 1], "喇")) return null;
        if (fullSpanSingleConstruction2(ordinaryWrapped, compact)) return null;
        const host = changeStateHostForFinalLaa3(compact.slice(0, -1));
        if (!host) return null;
        const particle = changeStateScopeParticleClone(compact[compact.length - 1]);
        const children = [host, particle];
        return construction2("DiscourseParticleFrame", "Change", children, {
          note: "Proposition-like host plus final 喇 laa3 carrying change-of-situation/current-relevance scope.",
          slots: templateDerivedSlots2("DiscourseParticleFrame", children),
          trace: traceInfo2("generative_template", {
            construction_type: "DiscourseParticleFrame",
            template_family: "generative_template",
            template: ["proposition_host!", "change_state_scope_particle!"],
            assigned_slots: ["proposition_host", "change_state_scope_particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            discourse_particle_family: "change_state_current_relevance",
            broad_particle_class: "ChangeStateDiscourseParticle",
            particle_subtype: "change_of_situation_current_relevance_laa3",
            aspect_relation: "sentence_final_particle_distinct_from_vp_internal_perfective_zo2",
            tone_source: "canonical_written_form_reading",
            tone_certainty: "canonical_for_written_laa_character_but_real_spelling_varies",
            orthographic_uncertainty: "啦_and_喇_may_be_spelled_inconsistently_in_ordinary_writing",
            proposition_host_construction: host.type,
            proposition_host_surface: flattenSurface2(host),
            punctuation_hint: /[？?]/u.test(String(terminalText || "")) ? "question_marked_surface" : "declarative_or_unmarked",
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            antecedent_status: "not_applicable",
            not_claims: ["not_fragment_licensor", "not_noun_host", "not_unresolved_host", "not_directive_laa1", "not_perfective_aspect_marker"],
            reason: "A complete proposition-like host permits final 喇 as change-of-situation/current-relevance stance. It remains a separate discourse layer above any visible 咗 perfective event."
          })
        });
      }
      return {
        completionThenStandaloneWalkResolution: completionThenStandaloneWalkResolution2,
        propositionLikeHostForScopedDiscourseParticle: propositionLikeHostForScopedDiscourseParticle2,
        scopedChangeStateParticleFallback: scopedChangeStateParticleFallback2,
        scopedDirectiveClosureParticleFallback: scopedDirectiveClosureParticleFallback2,
        scopedEpistemicDiscourseParticleFallback: scopedEpistemicDiscourseParticleFallback2,
        scopedEvidentialDiscourseParticleFallback: scopedEvidentialDiscourseParticleFallback2
      };
    };
  }
});

// src/parser/terminal/particles/focus.js
var require_focus = __commonJS({
  "src/parser/terminal/particles/focus.js"(exports2, module2) {
    "use strict";
    module2.exports = function createTerminalFocusParticles2(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        construction: construction2,
        flattenSurface: flattenSurface2,
        fullSpanSingleConstruction: fullSpanSingleConstruction2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function restrictiveFocusHostHasUnresolvedClassifierHeadFusion(host) {
        if (!host || host.kind !== "construction" || host.type !== "QuantifiedClassifierNP") return false;
        const missingSlots = Array.isArray(host.trace && host.trace.missing_argument_slots) ? host.trace.missing_argument_slots : [];
        if (!missingSlots.includes("nominal_head")) return false;
        return (host.children || []).some((child) => {
          if (!child || child.kind !== "token" || !String(child.syntax || "").includes("classifier")) return false;
          const originalRole = String(child.trace && child.trace.original_role || "");
          return originalRole && originalRole !== "measure_word";
        });
      }
      function restrictiveScalarHostForFinalParticle(nodes) {
        const compact = withoutIgnorableSpaceText2(nodes || []);
        if (!compact.length) return null;
        const wrapped = applyConstructionPatterns2(compact);
        const host = fullSpanSingleConstruction2(wrapped, compact);
        if (host && [
          "QuantifiedClassifierNP",
          "QuantifiedPersonNP",
          "QuantifiedTimeNP",
          "QuantityNP",
          "ApproximateQuantity",
          "DiMarkedNP"
        ].includes(host.type)) {
          if (restrictiveFocusHostHasUnresolvedClassifierHeadFusion(host)) return null;
          return host;
        }
        if (compact.length === 1) {
          const only = compact[0];
          const syntax = String(only && only.syntax || "");
          if (nodeCanFillSlot2(only, "quantity") || /quantity|scalar|amount|degree/.test(syntax)) return only;
        }
        return null;
      }
      function restrictiveFocusMarkerClone(node) {
        return parserInactiveTokenClone2(node, {
          label: "how",
          pos: "adverb",
          syntax: "restrictive_focus_adverb scalar_limiter",
          slots: ["focus_adverb", "restriction_marker", "scalar_limiter", "degree"],
          jyutping: "dak1",
          note: "only / just; limits the following amount or scalar host",
          reason: "Before a visible quantity or scalar host and final 啫/咋, 得 is the restrictive focus marker 'only', not the acceptability predicate 'okay/can'."
        });
      }
      function restrictiveFocusParticleClone(node) {
        const surface = flattenSurface2(node);
        const isZe = surface === "啫";
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: isZe ? "sentence_final_particle restrictive_focus_particle minimizing_particle" : "sentence_final_particle restrictive_focus_particle exhaustive_limit_particle",
          slots: ["particle", "restrictive_focus_particle", "focus_scope_particle"],
          jyutping: isZe ? "ze1" : "zaa3",
          note: isZe ? "only / just; minimizes the visible amount" : "only / that's all; presents the visible amount as the limit",
          reason: isZe ? "Final 啫 marks restrictive/minimizing focus over the visible scalar host." : "Final 咋 marks restrictive/exhaustive limitation over the visible scalar host."
        });
      }
      function restrictiveFocusParticleFallback2(segment, terminalText = "", ordinaryWrapped = null) {
        const compact = withoutIgnorableSpaceText2(segment || []);
        if (compact.length < 3 || !isToken2(compact[0], "得")) return null;
        const finalNode = compact[compact.length - 1];
        if (!isToken2(finalNode, "啫") && !isToken2(finalNode, "咋")) return null;
        if (fullSpanSingleConstruction2(ordinaryWrapped, compact)) return null;
        const host = restrictiveScalarHostForFinalParticle(compact.slice(1, -1));
        if (!host) return null;
        const marker = restrictiveFocusMarkerClone(compact[0]);
        const particle = restrictiveFocusParticleClone(finalNode);
        const children = [marker, host, particle];
        const particleSurface = flattenSurface2(particle);
        const hostTrace = host && host.kind === "construction" ? host.trace || {} : {};
        const inheritedMissingSlots = Array.isArray(hostTrace.missing_argument_slots) ? hostTrace.missing_argument_slots.slice() : [];
        const inheritedContextRequired = hostTrace.context_requirement_status === "context_required" || inheritedMissingSlots.length > 0;
        const inheritedAntecedentStatus = inheritedContextRequired ? hostTrace.antecedent_status || "not_observed" : "not_applicable";
        return construction2("FocusParticleFrame", "Focus", children, {
          note: "Restrictive focus frame: 得 limits a visible quantity or scalar host, and final 啫/咋 marks minimization or an exhaustive limit.",
          slots: templateDerivedSlots2("FocusParticleFrame", children),
          trace: traceInfo2("generative_template", {
            construction_type: "FocusParticleFrame",
            template_family: "generative_template",
            template: ["restriction_marker!", "scalar_host!", "restrictive_focus_particle!"],
            assigned_slots: ["restriction_marker", "scalar_host", "restrictive_focus_particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            focus_relation: "scalar_restriction",
            broad_particle_class: "RestrictiveFocusParticle",
            particle_subtype: particleSurface === "啫" ? "restrictive_minimizing_ze1" : "restrictive_exhaustive_limit_zaa3",
            restriction_marker_surface: "得",
            scalar_host_construction: host.kind === "construction" ? host.type : "scalar_token",
            scalar_host_surface: flattenSurface2(host),
            punctuation_hint: /[？?]/u.test(String(terminalText || "")) ? "question_marked_surface" : "declarative_or_unmarked",
            context_requirement_status: inheritedContextRequired ? "context_required" : "context_not_required",
            missing_argument_slots: inheritedMissingSlots,
            missing_slot_details: inheritedMissingSlots.map((slot) => ({ slot, license_status: "unresolved" })),
            antecedent_status: inheritedAntecedentStatus,
            discourse_license_not_observed: inheritedContextRequired && inheritedAntecedentStatus !== "linked",
            not_claims: [
              "not_acceptability_predicate_dak1",
              "not_bare_particle",
              "not_unrestricted_np",
              "not_fabricated_quantity",
              ...inheritedMissingSlots.includes("nominal_head") ? ["not_fabricated_nominal_head"] : [],
              "not_exact_pragmatic_force_beyond_restriction"
            ],
            reason: inheritedContextRequired ? "The reusable 得 + scalar/quantity host + 啫/咋 pattern expresses restrictive focus, but the particle frame does not resolve discourse-dependent slots inherited from the scalar host." : "The reusable 得 + scalar/quantity host + 啫/咋 pattern expresses restrictive focus. The construction reassigns 得 from acceptability to a scalar limiter only when both the host and final restrictive particle are overt."
          })
        });
      }
      return { restrictiveFocusParticleFallback: restrictiveFocusParticleFallback2 };
    };
  }
});

// src/runtime-resources/grammar/ordered-particle-clusters.js
var require_ordered_particle_clusters = __commonJS({
  "src/runtime-resources/grammar/ordered-particle-clusters.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      sequenceEvidence: {
        "㗎喇": {
          status: "validated_supported_order",
          evidence_grade: "attested",
          evidence_note: "Attested modern Cantonese cluster in the project research corpus; licensed for transparent learner display."
        },
        "㗎咩": {
          status: "validated_supported_order",
          evidence_grade: "accepted_common_pattern",
          evidence_note: "Accepted inner assertive particle plus outer biased-polar-question pattern."
        }
      },
      descriptors: {
        "㗎": {
          rank: 10,
          layer: "inner_propositional_assertion",
          broad_function: "assertion_or_current_relevance",
          syntax: "sentence_final_particle inner_propositional_particle assertive_relevance_particle",
          slots: ["particle", "discourse_scope_particle"],
          jyutping: "gaa3",
          note: "assertive / currently relevant; inner particle in an ordered sentence-final cluster",
          fusion_status: "surface_fused_particle_preserved_no_internal_split"
        },
        "啫": {
          rank: 20,
          layer: "restrictive_focus",
          broad_function: "restrictive_or_minimizing_scope",
          syntax: "sentence_final_particle restrictive_focus_particle minimizing_particle",
          slots: ["particle", "restrictive_focus_particle", "focus_scope_particle"],
          jyutping: "ze1",
          note: "only / just; restrictive or minimizing scope over the preceding host",
          fusion_status: "separate_visible_particle"
        },
        "咋": {
          rank: 20,
          layer: "restrictive_focus",
          broad_function: "restrictive_or_exhaustive_limit",
          syntax: "sentence_final_particle restrictive_focus_particle exhaustive_limit_particle",
          slots: ["particle", "restrictive_focus_particle", "focus_scope_particle"],
          jyutping: "zaa3",
          note: "only / that's all; restrictive or exhaustive limit over the preceding host",
          fusion_status: "separate_visible_particle"
        },
        "啦": {
          rank: 30,
          layer: "change_state_or_interpersonal",
          broad_function: "directive_suggestion_invitation_or_closure",
          syntax: "sentence_final_particle directive_closure_particle",
          slots: ["particle", "directive_scope_particle", "discourse_scope_particle"],
          jyutping: "laa1",
          note: "directive / suggestion / invitation / interpersonal closure",
          fusion_status: "separate_visible_particle"
        },
        "喇": {
          rank: 30,
          layer: "change_state_or_interpersonal",
          broad_function: "change_of_situation_or_current_relevance",
          syntax: "sentence_final_particle change_state_current_relevance_particle",
          slots: ["particle", "change_state_scope_particle", "discourse_scope_particle"],
          jyutping: "laa3",
          note: "changed situation / current relevance / transition",
          fusion_status: "separate_visible_particle"
        },
        "咩": {
          rank: 40,
          layer: "polar_question",
          broad_function: "biased_polar_question",
          syntax: "sentence_final_polar_question_particle biased_question_particle",
          slots: ["particle", "yes_no_question_marker", "question_marker"],
          jyutping: "me1",
          note: "sentence-final polar question particle; often conveys surprise or negative expectation",
          fusion_status: "separate_visible_particle"
        },
        "喎": {
          rank: 50,
          layer: "outer_speaker_stance",
          broad_function: "evidential_or_noteworthiness_stance",
          syntax: "sentence_final_particle evidential_noteworthiness_particle",
          slots: ["particle", "evidential_scope_particle", "discourse_scope_particle"],
          jyutping: "wo3",
          note: "reported / noteworthy / reminder stance; exact subtype depends on tone and context",
          fusion_status: "separate_visible_particle"
        },
        "啩": {
          rank: 50,
          layer: "outer_speaker_stance",
          broad_function: "epistemic_uncertainty_or_probability",
          syntax: "sentence_final_particle epistemic_uncertainty_particle",
          slots: ["particle", "epistemic_scope_particle", "discourse_scope_particle"],
          jyutping: "gwaa3",
          note: "probably / perhaps; uncertainty or probability over the preceding host",
          fusion_status: "separate_visible_particle"
        }
      }
    };
  }
});

// src/parser/terminal/particles/ordered-cluster.js
var require_ordered_cluster = __commonJS({
  "src/parser/terminal/particles/ordered-cluster.js"(exports2, module2) {
    "use strict";
    var {
      descriptors: ORDERED_PARTICLE_CLUSTER_DESCRIPTORS2,
      sequenceEvidence: ORDERED_PARTICLE_CLUSTER_SEQUENCE_EVIDENCE2
    } = require_ordered_particle_clusters();
    module2.exports = function createOrderedParticleClusters2(dependencies = {}) {
      const {
        construction: construction2,
        flattenSurface: flattenSurface2,
        hasSentencePunctuation: hasSentencePunctuation2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        propositionLikeHostForScopedDiscourseParticle: propositionLikeHostForScopedDiscourseParticle2,
        templateDerivedSlots: templateDerivedSlots2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function orderedParticleClusterDescriptor(node) {
        const surface = flattenSurface2(node);
        const descriptor = ORDERED_PARTICLE_CLUSTER_DESCRIPTORS2[surface];
        return descriptor ? { surface, ...descriptor } : null;
      }
      function orderedParticleClusterMemberClone(node, descriptor) {
        return parserInactiveTokenClone2(node, {
          label: "particle",
          pos: "particle",
          syntax: descriptor.syntax,
          slots: descriptor.slots,
          jyutping: descriptor.jyutping,
          note: descriptor.note,
          reason: `Within a visible sentence-final particle cluster, ${descriptor.surface} occupies the broad ${descriptor.layer} layer. The parser preserves the written token and does not infer a narrower tone-specific subtype.`
        });
      }
      function orderedParticleClusterTailInfo2(segment, terminalText = "") {
        const compact = withoutIgnorableSpaceText2(segment || []).slice();
        while (compact.length && compact[compact.length - 1].kind === "text" && hasSentencePunctuation2(compact[compact.length - 1].text)) compact.pop();
        if (compact.length < 2) return null;
        let clusterStart = compact.length;
        const reversedDescriptors = [];
        while (clusterStart > 0) {
          const descriptor = orderedParticleClusterDescriptor(compact[clusterStart - 1]);
          if (!descriptor) break;
          reversedDescriptors.push(descriptor);
          clusterStart -= 1;
        }
        const descriptors = reversedDescriptors.reverse();
        if (descriptors.length < 2) return null;
        const visibleParticleSequence = descriptors.map((descriptor) => descriptor.surface);
        const sequenceKey = visibleParticleSequence.join("");
        const ranks = descriptors.map((descriptor) => descriptor.rank);
        const strictlyIncreasing = ranks.every((rank, index) => index === 0 || ranks[index - 1] < rank);
        const questionLayerIsOutermost = descriptors.every((descriptor, index) => descriptor.surface !== "咩" || index === descriptors.length - 1);
        const questionPunctuationCompatible = !descriptors.some((descriptor) => descriptor.surface === "咩") || /[？?]/u.test(String(terminalText || ""));
        const layerOrderCompatible = strictlyIncreasing && questionLayerIsOutermost && questionPunctuationCompatible;
        const sequenceEvidence = layerOrderCompatible ? ORDERED_PARTICLE_CLUSTER_SEQUENCE_EVIDENCE2[sequenceKey] || null : null;
        const supportedOrder = Boolean(sequenceEvidence);
        const orderStatus = supportedOrder ? sequenceEvidence.status : layerOrderCompatible ? "layer_order_compatible_unvalidated_review" : "unsupported_or_unvalidated_order_review";
        return {
          compact,
          clusterStart,
          descriptors,
          sequenceKey,
          sequenceEvidence,
          supportedOrder,
          layerOrderCompatible,
          orderStatus,
          visibleParticleSequence,
          particleSequenceJyutping: descriptors.map((descriptor) => descriptor.jyutping),
          particleScopeLayers: descriptors.map((descriptor) => descriptor.layer),
          particleScopeFunctions: descriptors.map((descriptor) => descriptor.broad_function),
          fusionStatus: descriptors.some((descriptor) => descriptor.fusion_status.startsWith("surface_fused")) ? "surface_fused_particle_preserved_no_internal_split" : "separate_visible_particles"
        };
      }
      function orderedParticleClusterInfo2(segment, terminalText = "") {
        const tailInfo = orderedParticleClusterTailInfo2(segment, terminalText);
        if (!tailInfo || tailInfo.clusterStart < 1) return null;
        const host = propositionLikeHostForScopedDiscourseParticle2(tailInfo.compact.slice(0, tailInfo.clusterStart));
        if (!host) return null;
        return {
          ...tailInfo,
          host
        };
      }
      function orderedParticleClusterFallback2(segment, terminalText = "", clusterInfo = null) {
        const info = clusterInfo || orderedParticleClusterInfo2(segment, terminalText);
        if (!info || !info.supportedOrder) return null;
        const {
          compact,
          clusterStart,
          descriptors,
          host,
          visibleParticleSequence,
          particleSequenceJyutping,
          particleScopeLayers,
          particleScopeFunctions,
          fusionStatus
        } = info;
        const particles = descriptors.map((descriptor, index) => orderedParticleClusterMemberClone(compact[clusterStart + index], descriptor));
        const outerDescriptor = descriptors[descriptors.length - 1];
        const isQuestionCluster = outerDescriptor.surface === "咩";
        const type = isQuestionCluster ? "PolarQuestionFrame" : "DiscourseParticleFrame";
        const label = isQuestionCluster ? "YesNo?" : "Particles";
        const children = [host, ...particles];
        return construction2(type, label, children, {
          note: isQuestionCluster ? "Biased polar question containing an ordered sentence-final particle cluster." : "Statement with an ordered sentence-final particle cluster.",
          slots: templateDerivedSlots2(type, children),
          trace: traceInfo2("generative_template", {
            construction_type: type,
            template_family: "generative_template",
            template: ["scope_host!", "cluster_particle+!"],
            assigned_slots: ["scope_host", "particle"],
            surfaces: children.map((node) => flattenSurface2(node)),
            particle_cluster: true,
            particle_cluster_root: true,
            particle_cluster_member_count: descriptors.length,
            visible_particle_sequence: visibleParticleSequence,
            particle_sequence_jyutping: particleSequenceJyutping,
            particle_scope_layers: particleScopeLayers,
            particle_scope_functions: particleScopeFunctions,
            outer_particle_surface: outerDescriptor.surface,
            outer_scope_layer: outerDescriptor.layer,
            outer_scope_function: outerDescriptor.broad_function,
            scope_direction: "inside_to_outside",
            surface_order_preserved: true,
            cluster_order_status: info.orderStatus,
            cluster_evidence_grade: info.sequenceEvidence ? info.sequenceEvidence.evidence_grade : "",
            cluster_evidence_note: info.sequenceEvidence ? info.sequenceEvidence.evidence_note : "",
            fusion_status: fusionStatus,
            tone_certainty: "broad_written_form_readings_only",
            host_construction: host.type,
            host_surface: flattenSurface2(host),
            learner_display_structure: "single_cluster_frame_with_direct_particle_children",
            punctuation_hint: /[？?]/u.test(String(terminalText || "")) ? "question_marked" : "declarative_or_unmarked",
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            antecedent_status: "not_applicable",
            not_claims: [
              "not_unordered_particle_bag",
              "not_hidden_particle_reordering",
              "not_internal_split_of_fused_surface_particle",
              "not_exact_tone_specific_interpretation",
              "not_repeated_generic_stance_layers"
            ],
            reason: `This exact visible particle sequence has current evidence-backed support and follows the broad layer order. One learner-visible frame keeps the proposition host and every particle transparent, while ordered trace metadata records inside-to-outside scope without repeating a generic construction layer for each particle.`
          })
        });
      }
      return {
        orderedParticleClusterFallback: orderedParticleClusterFallback2,
        orderedParticleClusterInfo: orderedParticleClusterInfo2,
        orderedParticleClusterTailInfo: orderedParticleClusterTailInfo2
      };
    };
  }
});

// src/parser/orchestration/wrap-core.js
var require_wrap_core = __commonJS({
  "src/parser/orchestration/wrap-core.js"(exports2, module2) {
    "use strict";
    module2.exports = function createWrapCore(dependencies = {}) {
      const {
        aNotAQuestionFallback: aNotAQuestionFallback2,
        acceptabilityANotAQuestionFallback: acceptabilityANotAQuestionFallback2,
        availabilityPredicateWrapCoreFallback: availabilityPredicateWrapCoreFallback2,
        ambiguousNeedsContextCandidate: ambiguousNeedsContextCandidate2,
        approximateQuantityFallback: approximateQuantityFallback2,
        bareNumeralObjectMalformedCandidate: bareNumeralObjectMalformedCandidate2,
        completionQuestionFallback: completionQuestionFallback2,
        completionQuestionWithPerfectiveMarkerFallback: completionQuestionWithPerfectiveMarkerFallback2,
        completionThenClauseRelation: completionThenClauseRelation2,
        completionThenStandaloneWalkResolution: completionThenStandaloneWalkResolution2,
        conditionalGeWaaClauseFallback: conditionalGeWaaClauseFallback2,
        coordinatedNPFragmentFallback: coordinatedNPFragmentFallback2,
        coordinatedSubjectModalPredicateClauseFallback: coordinatedSubjectModalPredicateClauseFallback2,
        copularANotAQuestionFallback: copularANotAQuestionFallback2,
        copularExplanatoryCompositionFallback: copularExplanatoryCompositionFallback2,
        copularIdentificationFrameFallback: copularIdentificationFrameFallback2,
        cognitionContentFrameFallback: cognitionContentFrameFallback2,
        cognitionStatementFallback: cognitionStatementFallback2,
        coverbFrameFallback: coverbFrameFallback2,
        cp021bBoundaryReviewFallback: cp021bBoundaryReviewFallback2,
        desiderativeANotAQuestionFallback: desiderativeANotAQuestionFallback2,
        desiderativeVPWrapCoreFallback: desiderativeVPWrapCoreFallback2,
        directionalCompositionFallback: directionalCompositionFallback2,
        downwardMotionPredicateFallback: downwardMotionPredicateFallback2,
        durativeAspectCompositionFallback: durativeAspectCompositionFallback2,
        existentialLocationPresentationalFallback: existentialLocationPresentationalFallback2,
        existentialQuestionWithVpMalformedCandidate: existentialQuestionWithVpMalformedCandidate2,
        existentialWhQuestionFallback: existentialWhQuestionFallback2,
        experientialQuestionBoundaryFallback: experientialQuestionBoundaryFallback2,
        experientialYesNoQuestionFallback: experientialYesNoQuestionFallback2,
        fragmentQuestionFallback: fragmentQuestionFallback2,
        hasSurface: hasSurface2,
        impersonalEnvironmentalClauseFallback: impersonalEnvironmentalClauseFallback2,
        inlineANotAQuestionFallback: inlineANotAQuestionFallback2,
        incompatibleAspectCompositionMalformedCandidate: incompatibleAspectCompositionMalformedCandidate2,
        incompleteContextualPredicateCandidate: incompleteContextualPredicateCandidate2,
        incompleteLocativeNeedsContextCandidate: incompleteLocativeNeedsContextCandidate2,
        incompleteModalNeedsContextCandidate: incompleteModalNeedsContextCandidate2,
        incompletePotentialResultCandidate: incompletePotentialResultCandidate2,
        incompleteProhibitiveNeedsContextCandidate: incompleteProhibitiveNeedsContextCandidate2,
        incompleteRestrictiveFocusBoundaryCandidate: incompleteRestrictiveFocusBoundaryCandidate2,
        intendedFunctionRelationFallback: intendedFunctionRelationFallback2,
        interiorExistentialFrameFallback: interiorExistentialFrameFallback2,
        interestDomainExistentialQuestionFallback: interestDomainExistentialQuestionFallback2,
        leaveTakingFormulaFallback: leaveTakingFormulaFallback2,
        lexicalGiveRelationFallback: lexicalGiveRelationFallback2,
        locativePostureVPFallback: locativePostureVPFallback2,
        locativeWhQuestionFallback: locativeWhQuestionFallback2,
        mandarinNegatorNeedsContextCandidate: mandarinNegatorNeedsContextCandidate2,
        mandarinReviewNeedsContextCandidate: mandarinReviewNeedsContextCandidate2,
        mannerAdverbialVPFallback: mannerAdverbialVPFallback2,
        modalPredicateWrapCoreFallback: modalPredicateWrapCoreFallback2,
        motionEventSpatialFallback: motionEventSpatialFallback2,
        namingSelfIntroductionFrameFallback: namingSelfIntroductionFrameFallback2,
        negativeCognitionFragmentFallback: negativeCognitionFragmentFallback2,
        nominalMeasurePredicateFallback: nominalMeasurePredicateFallback2,
        opinionSeemingFallback: opinionSeemingFallback2,
        opinionStanceFrameFallback: opinionStanceFrameFallback2,
        overtObjectSelectionReviewCandidate: overtObjectSelectionReviewCandidate2,
        passivePermissiveRelationFallback: passivePermissiveRelationFallback2,
        perfectiveResultCompositionFallback: perfectiveResultCompositionFallback2,
        permissionANotAQuestionFallback: permissionANotAQuestionFallback2,
        politePathImperativeFallback: politePathImperativeFallback2,
        politeRequestAdjustmentFallback: politeRequestAdjustmentFallback2,
        polarQuestionFrameFallback: polarQuestionFrameFallback2,
        possessiveFragmentAnswerCandidate: possessiveFragmentAnswerCandidate2,
        postThemeParticipantRelationFallback: postThemeParticipantRelationFallback2,
        postverbalZoPerfectiveFromRawNodes: postverbalZoPerfectiveFromRawNodes2,
        postverbalZoPerfectiveFromWrappedNodes: postverbalZoPerfectiveFromWrappedNodes2,
        potentialResultComplementFallback: potentialResultComplementFallback2,
        potentialResultVPFallback: potentialResultVPFallback2,
        predicateOmissionCandidate: predicateOmissionCandidate2,
        preferenceVPWrapCoreFallback: preferenceVPWrapCoreFallback2,
        progressiveWhObjectQuestionFallback: progressiveWhObjectQuestionFallback2,
        prohibitiveImperativeFallback: prohibitiveImperativeFallback2,
        protectedOpaqueFormulaPassthrough: protectedOpaqueFormulaPassthrough2,
        purposeLinkingMotionFallback: purposeLinkingMotionFallback2,
        rawPreferenceTemplateFallback: rawPreferenceTemplateFallback2,
        reportedSpeechFrameFallback: reportedSpeechFrameFallback2,
        reportedSpeechSurfaceFallback: reportedSpeechSurfaceFallback2,
        restorativeRepetitiveComplementFallback: restorativeRepetitiveComplementFallback2,
        scalarEvaluationFallback: scalarEvaluationFallback2,
        scalarValueQuestionFallback: scalarValueQuestionFallback2,
        sourceLinkedDegreeMannerModifiedVPFallback: sourceLinkedDegreeMannerModifiedVPFallback2,
        sourceLinkedIntentionFrameFallback: sourceLinkedIntentionFrameFallback2,
        sourceLinkedPreferenceVPFallback: sourceLinkedPreferenceVPFallback2,
        sourceLinkedPriorityMarkerClauseFallback: sourceLinkedPriorityMarkerClauseFallback2,
        sourceMotionClauseFallback: sourceMotionClauseFallback2,
        subjectLocativePredicateClauseFallback: subjectLocativePredicateClauseFallback2,
        subjectStativePredicateClauseFallback: subjectStativePredicateClauseFallback2,
        suggestionQuestionFallback: suggestionQuestionFallback2,
        templateConstructionFor: templateConstructionFor2,
        temporalClauseFallback: temporalClauseFallback2,
        topicCommentFallback: topicCommentFallback2,
        transitionMotionPredicateFallback: transitionMotionPredicateFallback2,
        transparentDiscourseFormulaFallback: transparentDiscourseFormulaFallback2,
        wrapAgreementResponseSubspans: wrapAgreementResponseSubspans2,
        wrapCategorySubspans: wrapCategorySubspans2,
        wrapChangeIntoPredicateSubspans: wrapChangeIntoPredicateSubspans2,
        wrapDirectionalMotionSubspans: wrapDirectionalMotionSubspans2,
        wrapNegatedVPSubspans: wrapNegatedVPSubspans2,
        wrapPermissionAcceptabilitySubspans: wrapPermissionAcceptabilitySubspans2,
        wrapPossessiveClassifierNPSubspans: wrapPossessiveClassifierNPSubspans2,
        wrapPredicate: wrapPredicate2,
        wrapPriorityMarkerSubspans: wrapPriorityMarkerSubspans2,
        wrapSerialPurposeTemplateSubspans: wrapSerialPurposeTemplateSubspans2,
        wrapSerialVerbPurposeSubspans: wrapSerialVerbPurposeSubspans2
      } = dependencies;
      function wrapCore(core) {
        if (!core.length) return core;
        const ambiguousNeedsContext = ambiguousNeedsContextCandidate2(core);
        if (ambiguousNeedsContext) return [ambiguousNeedsContext];
        const fragmentQuestion = fragmentQuestionFallback2(core);
        if (fragmentQuestion) return [fragmentQuestion];
        const conditionalClause = conditionalGeWaaClauseFallback2(core);
        if (conditionalClause) return [conditionalClause];
        const earlyCp021bBoundaryReviewSpan = cp021bBoundaryReviewFallback2(core);
        if (earlyCp021bBoundaryReviewSpan) return [earlyCp021bBoundaryReviewSpan];
        const environmentalClause = impersonalEnvironmentalClauseFallback2(core);
        if (environmentalClause) return [environmentalClause];
        const downwardMotionSpan = downwardMotionPredicateFallback2(core);
        if (downwardMotionSpan) return [downwardMotionSpan];
        const existentialLocationPresentational = existentialLocationPresentationalFallback2(core);
        if (existentialLocationPresentational) return [existentialLocationPresentational];
        const nominalMeasurePredicate = nominalMeasurePredicateFallback2(core);
        if (nominalMeasurePredicate) return [nominalMeasurePredicate];
        const availabilityPredicateSpan = availabilityPredicateWrapCoreFallback2(core);
        if (availabilityPredicateSpan) return availabilityPredicateSpan;
        const motionEventSpatial = motionEventSpatialFallback2(core);
        if (motionEventSpatial) return [motionEventSpatial];
        const incompatibleAspectComposition = incompatibleAspectCompositionMalformedCandidate2(core);
        if (incompatibleAspectComposition) return [incompatibleAspectComposition];
        const durativeAspectComposition = durativeAspectCompositionFallback2(core);
        if (durativeAspectComposition) return [durativeAspectComposition];
        const perfectiveResultComposition = perfectiveResultCompositionFallback2(core);
        if (perfectiveResultComposition) return [perfectiveResultComposition];
        const copularExplanatoryComposition = copularExplanatoryCompositionFallback2(core);
        if (copularExplanatoryComposition) return [copularExplanatoryComposition];
        const directionalComposition = directionalCompositionFallback2(core);
        if (directionalComposition) return [directionalComposition];
        const restorativeRepetitiveComposition = restorativeRepetitiveComplementFallback2(core);
        if (restorativeRepetitiveComposition) return [restorativeRepetitiveComposition];
        const purposeLinkingMotion = purposeLinkingMotionFallback2(core);
        if (purposeLinkingMotion) return [purposeLinkingMotion];
        const bareNumeralMalformed = bareNumeralObjectMalformedCandidate2(core);
        if (bareNumeralMalformed) return [bareNumeralMalformed];
        const existentialVpMalformed = existentialQuestionWithVpMalformedCandidate2(core);
        if (existentialVpMalformed) return [existentialVpMalformed];
        const mandarinNegatorNeedsContext = mandarinNegatorNeedsContextCandidate2(core);
        if (mandarinNegatorNeedsContext) return [mandarinNegatorNeedsContext];
        const incompleteProhibitiveNeedsContext = incompleteProhibitiveNeedsContextCandidate2(core);
        if (incompleteProhibitiveNeedsContext) return [incompleteProhibitiveNeedsContext];
        const incompleteRestrictiveFocusBoundary = incompleteRestrictiveFocusBoundaryCandidate2(core);
        if (incompleteRestrictiveFocusBoundary) return [incompleteRestrictiveFocusBoundary];
        const typedPredicateOmission = predicateOmissionCandidate2(core);
        if (typedPredicateOmission) return [typedPredicateOmission];
        const incompleteModalNeedsContext = incompleteModalNeedsContextCandidate2(core);
        if (incompleteModalNeedsContext) return [incompleteModalNeedsContext];
        const incompleteContextualPredicate = incompleteContextualPredicateCandidate2(core);
        if (incompleteContextualPredicate) return [incompleteContextualPredicate];
        const incompleteLocativeNeedsContext = incompleteLocativeNeedsContextCandidate2(core);
        if (incompleteLocativeNeedsContext) return [incompleteLocativeNeedsContext];
        const possessiveFragmentAnswer = possessiveFragmentAnswerCandidate2(core);
        if (possessiveFragmentAnswer) return [possessiveFragmentAnswer];
        const mandarinReviewNeedsContext = mandarinReviewNeedsContextCandidate2(core);
        if (mandarinReviewNeedsContext) return [mandarinReviewNeedsContext];
        const copularANotAQuestion = copularANotAQuestionFallback2(core);
        if (copularANotAQuestion) return [copularANotAQuestion];
        const rawDesiderativeANotAQuestion = desiderativeANotAQuestionFallback2(core);
        if (rawDesiderativeANotAQuestion) return [rawDesiderativeANotAQuestion];
        const rawPermissionANotAQuestion = permissionANotAQuestionFallback2(core);
        if (rawPermissionANotAQuestion) return [rawPermissionANotAQuestion];
        const templateANotAQuestion = templateConstructionFor2(core, ["ANotAQuestion"]);
        if (templateANotAQuestion) return [templateANotAQuestion];
        const rawANotAQuestion = aNotAQuestionFallback2(core);
        if (rawANotAQuestion) return [rawANotAQuestion];
        const potentialResultSpan = potentialResultVPFallback2(core);
        if (potentialResultSpan) return [potentialResultSpan];
        const incompletePotentialResult = incompletePotentialResultCandidate2(core);
        if (incompletePotentialResult) return [incompletePotentialResult];
        const transitionMotionSpan = transitionMotionPredicateFallback2(core);
        if (transitionMotionSpan) return [transitionMotionSpan];
        const sourceLinkedDegreeMannerSpan = sourceLinkedDegreeMannerModifiedVPFallback2(core);
        if (sourceLinkedDegreeMannerSpan) return [sourceLinkedDegreeMannerSpan];
        const sourceLinkedPrioritySpan = sourceLinkedPriorityMarkerClauseFallback2(core);
        if (sourceLinkedPrioritySpan) return [sourceLinkedPrioritySpan];
        const sourceLinkedPreferenceSpan = sourceLinkedPreferenceVPFallback2(core);
        if (sourceLinkedPreferenceSpan) return [sourceLinkedPreferenceSpan];
        const rawPreferenceSpan = rawPreferenceTemplateFallback2(core);
        if (rawPreferenceSpan) return [rawPreferenceSpan];
        const negativeCognitionSpan = negativeCognitionFragmentFallback2(core);
        if (negativeCognitionSpan) return [negativeCognitionSpan];
        const cognitionStatementSpan = cognitionStatementFallback2(core);
        if (cognitionStatementSpan) return [cognitionStatementSpan];
        const cognitionContentSpan = cognitionContentFrameFallback2(core);
        if (cognitionContentSpan) return [cognitionContentSpan];
        const opinionStanceSpan = opinionStanceFrameFallback2(core);
        if (opinionStanceSpan) return [opinionStanceSpan];
        const reportedSpeechSpan = reportedSpeechFrameFallback2(core);
        if (reportedSpeechSpan) return [reportedSpeechSpan];
        const intendedFunctionSpan = intendedFunctionRelationFallback2(core);
        if (intendedFunctionSpan) return [intendedFunctionSpan];
        const sourceLinkedIntentionSpan = sourceLinkedIntentionFrameFallback2(core);
        if (sourceLinkedIntentionSpan) return [sourceLinkedIntentionSpan];
        const namingSelfIntroductionSpan = namingSelfIntroductionFrameFallback2(core);
        if (namingSelfIntroductionSpan) return [namingSelfIntroductionSpan];
        const politeRequestAdjustmentSpan = politeRequestAdjustmentFallback2(core);
        if (politeRequestAdjustmentSpan) return [politeRequestAdjustmentSpan];
        const transparentDiscourseFormulaSpan = transparentDiscourseFormulaFallback2(core);
        if (transparentDiscourseFormulaSpan) return [transparentDiscourseFormulaSpan];
        const leaveTakingFormulaSpan = leaveTakingFormulaFallback2(core);
        if (leaveTakingFormulaSpan) return [leaveTakingFormulaSpan];
        const politePathImperativeSpan = politePathImperativeFallback2(core);
        if (politePathImperativeSpan) return [politePathImperativeSpan];
        const polarQuestionSpan = polarQuestionFrameFallback2(core);
        if (polarQuestionSpan) return [polarQuestionSpan];
        const interiorExistentialSpan = interiorExistentialFrameFallback2(core);
        if (interiorExistentialSpan) return [interiorExistentialSpan];
        const copularIdentificationSpan = copularIdentificationFrameFallback2(core);
        if (copularIdentificationSpan) return [copularIdentificationSpan];
        const passivePermissiveSpan = passivePermissiveRelationFallback2(core);
        if (passivePermissiveSpan) return [passivePermissiveSpan];
        const lexicalGiveSpan = lexicalGiveRelationFallback2(core);
        if (lexicalGiveSpan) return [lexicalGiveSpan];
        const postThemeParticipantSpan = postThemeParticipantRelationFallback2(core);
        if (postThemeParticipantSpan) return [postThemeParticipantSpan];
        const mannerAdverbialSpan = mannerAdverbialVPFallback2(core);
        if (mannerAdverbialSpan) return [mannerAdverbialSpan];
        const sourceMotionSpan = sourceMotionClauseFallback2(core);
        if (sourceMotionSpan) return [sourceMotionSpan];
        const locativePostureSpan = locativePostureVPFallback2(core);
        if (locativePostureSpan) return [locativePostureSpan];
        const subjectLocativePredicateSpan = subjectLocativePredicateClauseFallback2(core);
        if (subjectLocativePredicateSpan) return [subjectLocativePredicateSpan];
        const coordinatedNPFragmentSpan = coordinatedNPFragmentFallback2(core);
        if (coordinatedNPFragmentSpan) return [coordinatedNPFragmentSpan];
        const coverbFrameSpan = coverbFrameFallback2(core);
        if (coverbFrameSpan) return [coverbFrameSpan];
        const coordinatedSubjectModalSpan = coordinatedSubjectModalPredicateClauseFallback2(core);
        if (coordinatedSubjectModalSpan) return [coordinatedSubjectModalSpan];
        const rawCompositionalPostverbalZo = postverbalZoPerfectiveFromRawNodes2(core);
        if (rawCompositionalPostverbalZo) return rawCompositionalPostverbalZo;
        core = wrapAgreementResponseSubspans2(core);
        core = wrapDirectionalMotionSubspans2(core);
        core = wrapSerialPurposeTemplateSubspans2(core);
        core = wrapSerialVerbPurposeSubspans2(core);
        core = wrapPriorityMarkerSubspans2(core);
        core = wrapChangeIntoPredicateSubspans2(core);
        core = wrapPossessiveClassifierNPSubspans2(core);
        core = wrapPermissionAcceptabilitySubspans2(core);
        core = wrapCategorySubspans2(core);
        core = wrapNegatedVPSubspans2(core);
        core = wrapCategorySubspans2(core);
        core = wrapCategorySubspans2(core);
        const postSubspanMotionEventSpatial = motionEventSpatialFallback2(core);
        if (postSubspanMotionEventSpatial) return [postSubspanMotionEventSpatial];
        const postSubspanTransitionMotionSpan = transitionMotionPredicateFallback2(core);
        if (postSubspanTransitionMotionSpan) return [postSubspanTransitionMotionSpan];
        const recomposedPostverbalZo = postverbalZoPerfectiveFromWrappedNodes2(core);
        if (recomposedPostverbalZo) core = recomposedPostverbalZo;
        const postSubspanPerfectiveResultComposition = perfectiveResultCompositionFallback2(core);
        if (postSubspanPerfectiveResultComposition) return [postSubspanPerfectiveResultComposition];
        const postSubspanExistentialVpMalformed = existentialQuestionWithVpMalformedCandidate2(core);
        if (postSubspanExistentialVpMalformed) return [postSubspanExistentialVpMalformed];
        const postSubspanMandarinNegatorNeedsContext = mandarinNegatorNeedsContextCandidate2(core);
        if (postSubspanMandarinNegatorNeedsContext) return [postSubspanMandarinNegatorNeedsContext];
        const postSubspanPossessiveFragmentAnswer = possessiveFragmentAnswerCandidate2(core);
        if (postSubspanPossessiveFragmentAnswer) return [postSubspanPossessiveFragmentAnswer];
        const postSubspanMandarinReviewNeedsContext = mandarinReviewNeedsContextCandidate2(core);
        if (postSubspanMandarinReviewNeedsContext) return [postSubspanMandarinReviewNeedsContext];
        const progressiveWhObjectSpan = progressiveWhObjectQuestionFallback2(core);
        if (progressiveWhObjectSpan) return [progressiveWhObjectSpan];
        const subjectStativeSpan = subjectStativePredicateClauseFallback2(core);
        if (subjectStativeSpan) return [subjectStativeSpan];
        const scalarValueQuestionSpan = scalarValueQuestionFallback2(core);
        if (scalarValueQuestionSpan) return [scalarValueQuestionSpan];
        const protectedOpaqueFormulaSpan = protectedOpaqueFormulaPassthrough2(core);
        if (protectedOpaqueFormulaSpan) return [protectedOpaqueFormulaSpan];
        if (core.length === 1 && core[0].kind === "construction") {
          const reviewedResolvedConstruction = overtObjectSelectionReviewCandidate2(core);
          if (reviewedResolvedConstruction) return [reviewedResolvedConstruction];
        }
        core = completionThenStandaloneWalkResolution2(core);
        const generativeSpan = templateConstructionFor2(core);
        if (generativeSpan) {
          const reviewedGenerativeSpan = overtObjectSelectionReviewCandidate2([generativeSpan]);
          return [reviewedGenerativeSpan || generativeSpan];
        }
        const completionQuestion = completionQuestionFallback2(core);
        if (completionQuestion) {
          const reviewedCompletionQuestion = overtObjectSelectionReviewCandidate2([completionQuestion]);
          return [reviewedCompletionQuestion || completionQuestion];
        }
        if (core.length === 1 && core[0].kind === "construction") {
          const reviewedExistingConstruction = overtObjectSelectionReviewCandidate2(core);
          return [reviewedExistingConstruction || core[0]];
        }
        const opinionSeemingSpan = opinionSeemingFallback2(core);
        if (opinionSeemingSpan) return [opinionSeemingSpan];
        const experientialYesNoQuestion = experientialYesNoQuestionFallback2(core);
        if (experientialYesNoQuestion) return [experientialYesNoQuestion];
        const interestDomainExistentialQuestion = interestDomainExistentialQuestionFallback2(core);
        if (interestDomainExistentialQuestion) return [interestDomainExistentialQuestion];
        const locativeWhQuestion = locativeWhQuestionFallback2(core);
        if (locativeWhQuestion) return [locativeWhQuestion];
        const completionThenRelation = completionThenClauseRelation2(core);
        if (completionThenRelation) return [completionThenRelation];
        const reportedSpeechSurfaceSpan = reportedSpeechSurfaceFallback2(core);
        if (reportedSpeechSurfaceSpan) return [reportedSpeechSurfaceSpan];
        const experientialQuestionBoundary = experientialQuestionBoundaryFallback2(core);
        if (experientialQuestionBoundary) return [experientialQuestionBoundary];
        const desiderativeSpan = desiderativeVPWrapCoreFallback2(core);
        if (desiderativeSpan) return [desiderativeSpan];
        const scalarEvaluationSpan = scalarEvaluationFallback2(core);
        if (scalarEvaluationSpan) return [scalarEvaluationSpan];
        if (hasSurface2(core, "幾錢")) {
          const scalar = scalarValueQuestionFallback2(core);
          if (scalar) return [scalar];
        }
        const approximateQuantitySpan = approximateQuantityFallback2(core);
        if (approximateQuantitySpan) return [approximateQuantitySpan];
        const suggestionQuestion = suggestionQuestionFallback2(core);
        if (suggestionQuestion) return [suggestionQuestion];
        const acceptabilityANotAQuestion = acceptabilityANotAQuestionFallback2(core);
        if (acceptabilityANotAQuestion) return [acceptabilityANotAQuestion];
        const existentialWhQuestion = existentialWhQuestionFallback2(core);
        if (existentialWhQuestion) return [existentialWhQuestion];
        const preferenceFallbackSpan = preferenceVPWrapCoreFallback2(core);
        if (preferenceFallbackSpan) return [preferenceFallbackSpan];
        const temporalClauseSpan = temporalClauseFallback2(core);
        if (temporalClauseSpan) return [temporalClauseSpan];
        const topicCommentSpan = topicCommentFallback2(core);
        if (topicCommentSpan) return [topicCommentSpan];
        const prohibitiveImperativeSpan = prohibitiveImperativeFallback2(core);
        if (prohibitiveImperativeSpan) return [prohibitiveImperativeSpan];
        const inlineANotAQuestion = inlineANotAQuestionFallback2(core);
        if (inlineANotAQuestion) return [inlineANotAQuestion];
        const completionQuestionWithPerfectiveMarker = completionQuestionWithPerfectiveMarkerFallback2(core);
        if (completionQuestionWithPerfectiveMarker) return [completionQuestionWithPerfectiveMarker];
        const potentialResultComplementSpan = potentialResultComplementFallback2(core);
        if (potentialResultComplementSpan) return [potentialResultComplementSpan];
        const modalPredicateWrapSpan = modalPredicateWrapCoreFallback2(core);
        if (modalPredicateWrapSpan) return modalPredicateWrapSpan;
        return wrapPredicate2(core);
      }
      return {
        wrapCore
      };
    };
  }
});

// src/parser/orchestration/apply-terminal-patterns.js
var require_apply_terminal_patterns = __commonJS({
  "src/parser/orchestration/apply-terminal-patterns.js"(exports2, module2) {
    "use strict";
    module2.exports = function createApplyTerminalPatterns(dependencies = {}) {
      const {
        applyConstructionPatterns: applyConstructionPatterns2,
        boundedAcknowledgementRepetitionForPunctuation: boundedAcknowledgementRepetitionForPunctuation2,
        connectorAwareClauseLinkingForTerminal: connectorAwareClauseLinkingForTerminal2,
        finalMePolarQuestionFallbackForPunctuation: finalMePolarQuestionFallbackForPunctuation2,
        hasSentencePunctuation: hasSentencePunctuation2,
        haveOrNotQuestionFallbackForPunctuation: haveOrNotQuestionFallbackForPunctuation2,
        orderedParticleClusterFallback: orderedParticleClusterFallback2,
        orderedParticleClusterInfo: orderedParticleClusterInfo2,
        relativeClauseNPForTerminal: relativeClauseNPForTerminal2,
        repeatedNegatedExistentialResponseForPunctuation: repeatedNegatedExistentialResponseForPunctuation2,
        restrictiveFocusParticleFallback: restrictiveFocusParticleFallback2,
        scalarDimensionQuestionFallbackForPunctuation: scalarDimensionQuestionFallbackForPunctuation2,
        scopedChangeStateParticleFallback: scopedChangeStateParticleFallback2,
        scopedDirectiveClosureParticleFallback: scopedDirectiveClosureParticleFallback2,
        scopedEpistemicDiscourseParticleFallback: scopedEpistemicDiscourseParticleFallback2,
        scopedEvidentialDiscourseParticleFallback: scopedEvidentialDiscourseParticleFallback2,
        standaloneClauseRelationEdgeFragmentForTerminal: standaloneClauseRelationEdgeFragmentForTerminal2,
        wrapClauseSequenceByPunctuation: wrapClauseSequenceByPunctuation2
      } = dependencies;
      function terminalSurface(node) {
        if (!node) return "";
        if (node.kind === "token") return String(node.surface || node.parser_surface || "");
        if (node.kind === "text") return String(node.text || "");
        if (node.kind === "construction") return (node.children || []).map(terminalSurface).join("");
        return "";
      }
      function constructionTypePresent(nodes, expectedType) {
        return (nodes || []).some((node) => {
          if (!node || typeof node !== "object") return false;
          if (node.kind === "construction" && node.type === expectedType) return true;
          return constructionTypePresent(node.children, expectedType);
        });
      }
      function withCopularANotATerminalQuestionSurface(nodes, terminalText = "") {
        if (!/[？?]/u.test(String(terminalText || ""))) return nodes;
        if (!Array.isArray(nodes) || nodes.length !== 1) return nodes;
        const [node] = nodes;
        if (!node || node.kind !== "construction" || node.type !== "CopularANotAQuestion") return nodes;
        const punctuationNode = {
          kind: "text",
          text: terminalText,
          trace: { kind: "punctuation_or_plain_text", surface: terminalText }
        };
        return [{
          ...node,
          children: [...node.children || [], punctuationNode],
          trace: {
            ...node.trace || {},
            terminal_question_punctuation_preserved: true
          }
        }];
      }
      function locativeWhQuestionForTerminal(segment, terminalText = "", ordinaryWrapped = null) {
        if (!/[？?]/u.test(String(terminalText || "")) || !segment || !segment.length) return null;
        const surface = segment.map(terminalSurface).join("");
        if (!surface.includes("邊度")) return null;
        const wrapped = ordinaryWrapped || applyConstructionPatterns2(segment);
        if (constructionTypePresent(wrapped, "LocativeWhQuestion")) return null;
        if (constructionTypePresent(wrapped, "NeedsContext")) return null;
        const bareSurface = surface.replace(/[呀啊㗎呢嚹喎啦唧吖嘛嘅啫囉掛]+$/u, "");
        const whIndex = bareSurface.indexOf("邊度");
        const prefix = bareSurface.slice(0, whIndex);
        if (/(?:知唔知|唔知|知道|問|諗緊)/u.test(prefix)) return null;
        if (/邊度都/u.test(bareSurface) || /邊度會/u.test(bareSurface) || /邊度.*得晒/u.test(bareSurface)) return null;
        const bareMotionGoalFragment = bareSurface === "去邊度";
        const independentlyTypedMotion = [
          "MotionGoalVP",
          "ExperientialMotionGoalVP",
          "MotionPurposeChain",
          "DirectionalMotionVP"
        ].some((type) => constructionTypePresent(wrapped, type));
        if (independentlyTypedMotion && !bareMotionGoalFragment) return null;
        const children = wrapped && wrapped.length ? wrapped : segment;
        const parserSurface = children.map(terminalSurface).join("");
        return {
          kind: "construction",
          type: "LocativeWhQuestion",
          compatibility_alias: "",
          internal_representation_scope: "",
          internal_only: false,
          label: "WhereQ",
          children,
          display_surface: parserSurface,
          parser_surface: parserSurface,
          primary: "",
          note: "Matrix locative wh-question licensed by overt 邊度 plus independently marked question force.",
          slots: ["question_fragment", "location_question", "location", "predicate", "vp", "clause"],
          trace: {
            kind: "terminal_question_force",
            construction_type: "LocativeWhQuestion",
            template_family: "punctuation_licensed_wrapper",
            overt_wh_place: "邊度",
            question_force_source: "terminal_question_punctuation",
            semantic_role_source: "overt_typed_children",
            wrapper_scope: "matrix_clause_or_fragment",
            rule: "matrix interrogative clause/fragment + overt 邊度",
            reason: "AA82 attested boundary packet R1 adds the compatibility wrapper only after ordinary typed composition and preserves embedded, rhetorical, indefinite, motion/aspect, and fragment boundaries.",
            surfaces: children.map(terminalSurface)
          }
        };
      }
      function applyConstructionPatternsForTerminal2(segment, terminalText = "") {
        const haveOrNotQuestion = haveOrNotQuestionFallbackForPunctuation2(segment, terminalText);
        if (haveOrNotQuestion) return [haveOrNotQuestion];
        const scalarQuestion = scalarDimensionQuestionFallbackForPunctuation2(segment, terminalText);
        if (scalarQuestion) return [scalarQuestion];
        const relationFragment = standaloneClauseRelationEdgeFragmentForTerminal2(segment, terminalText);
        if (relationFragment) return [relationFragment];
        const relativeClauseNP = relativeClauseNPForTerminal2(segment);
        if (relativeClauseNP) return [relativeClauseNP];
        const connectorLinked = connectorAwareClauseLinkingForTerminal2(segment);
        if (connectorLinked) return connectorLinked;
        const ordinaryWrapped = applyConstructionPatterns2(segment);
        const terminalAwareOrdinaryWrapped = withCopularANotATerminalQuestionSurface(ordinaryWrapped, terminalText);
        const locativeWhQuestion = locativeWhQuestionForTerminal(segment, terminalText, ordinaryWrapped);
        if (locativeWhQuestion) return [locativeWhQuestion];
        const particleClusterInfo = orderedParticleClusterInfo2(segment, terminalText);
        if (particleClusterInfo && !particleClusterInfo.supportedOrder) return terminalAwareOrdinaryWrapped;
        const orderedParticleCluster = orderedParticleClusterFallback2(segment, terminalText, particleClusterInfo);
        if (orderedParticleCluster) return [orderedParticleCluster];
        const restrictiveFocusParticle = restrictiveFocusParticleFallback2(segment, terminalText, ordinaryWrapped);
        if (restrictiveFocusParticle) return [restrictiveFocusParticle];
        const scopedDirectiveParticle = scopedDirectiveClosureParticleFallback2(segment, terminalText, ordinaryWrapped);
        if (scopedDirectiveParticle) return [scopedDirectiveParticle];
        const scopedChangeStateParticle = scopedChangeStateParticleFallback2(segment, terminalText, ordinaryWrapped);
        if (scopedChangeStateParticle) return [scopedChangeStateParticle];
        const scopedEvidentialParticle = scopedEvidentialDiscourseParticleFallback2(segment, terminalText);
        if (scopedEvidentialParticle) return [scopedEvidentialParticle];
        const scopedEpistemicParticle = scopedEpistemicDiscourseParticleFallback2(segment, terminalText);
        if (scopedEpistemicParticle) return [scopedEpistemicParticle];
        const finalMePolarQuestion = finalMePolarQuestionFallbackForPunctuation2(segment, terminalText, ordinaryWrapped);
        if (finalMePolarQuestion) return [finalMePolarQuestion];
        return terminalAwareOrdinaryWrapped;
      }
      function applyConstructionPatternsByPunctuation2(nodes) {
        const boundedAcknowledgementRepetition = boundedAcknowledgementRepetitionForPunctuation2(nodes);
        if (boundedAcknowledgementRepetition) return boundedAcknowledgementRepetition;
        const repeatedNegatedExistentialResponse = repeatedNegatedExistentialResponseForPunctuation2(nodes);
        if (repeatedNegatedExistentialResponse) return repeatedNegatedExistentialResponse;
        const rendered = [];
        let segment = [];
        const flush = (terminalText = "") => {
          if (segment.length) {
            rendered.push(...applyConstructionPatternsForTerminal2(segment, terminalText));
            segment = [];
          }
        };
        for (const node of nodes) {
          if (node.kind === "text" && hasSentencePunctuation2(node.text)) {
            const beforeFlushLength = rendered.length;
            flush(node.text);
            const terminalAbsorbed = rendered.length === beforeFlushLength + 1 && rendered[beforeFlushLength] && rendered[beforeFlushLength].kind === "construction" && rendered[beforeFlushLength].trace && rendered[beforeFlushLength].trace.terminal_question_punctuation_preserved;
            if (!terminalAbsorbed) rendered.push(node);
          } else {
            segment.push(node);
          }
        }
        flush();
        return wrapClauseSequenceByPunctuation2(rendered);
      }
      return {
        applyConstructionPatternsByPunctuation: applyConstructionPatternsByPunctuation2,
        applyConstructionPatternsForTerminal: applyConstructionPatternsForTerminal2
      };
    };
  }
});

// src/parser/context/descriptors.js
var require_descriptors = __commonJS({
  "src/parser/context/descriptors.js"(exports2, module2) {
    "use strict";
    module2.exports = function createContextDescriptors(dependencies = {}) {
      const {
        firstToken: firstToken2,
        flattenNodes: flattenNodes2,
        flattenSurface: flattenSurface2,
        getConstructionAffordances: getConstructionAffordances2,
        nodeCanFillSlot: nodeCanFillSlot2,
        normalizeSurface: normalizeSurface2,
        PREDICATE_OMISSION_PROFILES: PREDICATE_OMISSION_PROFILES2,
        predicateOmissionProfileForHead: predicateOmissionProfileForHead2,
        predicateOmissionProfileForQuestionForm: predicateOmissionProfileForQuestionForm2,
        predicateProfilesCompatible: predicateProfilesCompatible2,
        tokenSemanticDomains: tokenSemanticDomains2
      } = dependencies;
      function tokenRowsForAnalysis2(analysis) {
        return flattenNodes2(analysis && analysis.nodes || []).filter((row) => row.kind === "token");
      }
      function questionDescriptorForContextTurn2(turn) {
        const analysis = turn && turn.analysis;
        if (!analysis) return null;
        const tokens = tokenRowsForAnalysis2(analysis);
        const surfaces = tokens.map((row) => row.surface);
        const topTypes = flattenNodes2(analysis.nodes || []).filter((row) => row.kind === "construction" && row.depth === 0).map((row) => row.type);
        const questionMarked = /[？?]/u.test(turn.source) || topTypes.some((type) => String(type || "").includes("Question"));
        if (!questionMarked) return null;
        for (let index = 0; index < surfaces.length; index += 1) {
          const profile = predicateOmissionProfileForQuestionForm2(surfaces[index]);
          if (!profile) continue;
          let spanEnd = surfaces.length;
          while (spanEnd > index + 1 && (tokens[spanEnd - 1].slots || []).includes("particle")) spanEnd -= 1;
          return {
            family: profile.parser_family === "modal" ? "modal_a_not_a" : "a_not_a",
            head_surface: (profile.positive_heads || [surfaces[index]])[0],
            positive_alternative: (profile.positive_heads || [surfaces[index]])[0],
            negative_alternative: `唔${(profile.positive_heads || [surfaces[index]])[0]}`,
            antecedent_span: surfaces.slice(index, spanEnd).join(""),
            question_domain_surface: surfaces.slice(index + 1, spanEnd).join(""),
            context_turn_id: turn.id,
            question_id: turn.id,
            raw_source: turn.source,
            predicate_omission_profile: profile.id,
            question_match_family: profile.question_match_family,
            complement_type: profile.complement_type
          };
        }
        for (let index = 0; index < surfaces.length - 2; index += 1) {
          const first = surfaces[index];
          const negator = surfaces[index + 1];
          const second = surfaces[index + 2];
          if (negator !== "唔" || !first || !second) continue;
          if (first !== second && !first.startsWith(second) && !second.startsWith(first)) continue;
          const head = first.length >= second.length ? first : second;
          const firstRow = tokens[index];
          const secondRow = tokens[index + 2];
          const modal = (firstRow.slots || []).includes("modal") || (secondRow.slots || []).includes("modal");
          let spanEnd = surfaces.length;
          while (spanEnd > index + 3 && (tokens[spanEnd - 1].slots || []).includes("particle")) spanEnd -= 1;
          const profile = predicateOmissionProfileForHead2(head);
          return {
            family: profile && profile.parser_family === "modal" || modal ? "modal_a_not_a" : "a_not_a",
            head_surface: profile ? profile.positive_heads[0] : head,
            positive_alternative: profile ? profile.positive_heads[0] : head,
            negative_alternative: `唔${profile ? profile.positive_heads[0] : head}`,
            antecedent_span: surfaces.slice(index, spanEnd).join(""),
            question_domain_surface: surfaces.slice(index + 3, spanEnd).join(""),
            context_turn_id: turn.id,
            question_id: turn.id,
            raw_source: turn.source,
            predicate_omission_profile: profile ? profile.id : "",
            question_match_family: profile ? profile.question_match_family : "",
            complement_type: profile ? profile.complement_type : ""
          };
        }
        const finalMeiIndex = surfaces.lastIndexOf("未");
        if (topTypes.includes("CompletionQuestion") || finalMeiIndex > 0) {
          const beforeMei = finalMeiIndex > 0 ? tokens.slice(0, finalMeiIndex) : tokens;
          const predicate = beforeMei.find((row) => (row.slots || []).includes("action_verb") || (row.slots || []).includes("predicate"));
          if (predicate) {
            const startIndex = tokens.indexOf(predicate);
            const questionDomain = tokens.slice(startIndex + 1, finalMeiIndex >= 0 ? finalMeiIndex : tokens.length).filter((row) => !["咗", "過", "緊"].includes(row.surface) && !(row.slots || []).includes("particle")).map((row) => row.surface).join("");
            return {
              family: "completion_question",
              head_surface: predicate.surface,
              positive_alternative: `${predicate.surface}咗`,
              negative_alternative: `未${predicate.surface}`,
              antecedent_span: surfaces.slice(startIndex, finalMeiIndex >= 0 ? finalMeiIndex + 1 : surfaces.length).join(""),
              question_domain_surface: questionDomain,
              context_turn_id: turn.id,
              question_id: turn.id,
              raw_source: turn.source
            };
          }
        }
        return null;
      }
      function targetDescriptorForContext2(nodes) {
        const rows = flattenNodes2(nodes || []);
        const tokens = rows.filter((row) => row.kind === "token");
        const constructions = rows.filter((row) => row.kind === "construction");
        const top = constructions.find((row) => row.depth === 0);
        const topTrace = top && top.trace ? top.trace : {};
        const subject = tokens.find((row) => (row.slots || []).includes("subject"));
        const particles = tokens.filter((row) => (row.slots || []).includes("particle")).map((row) => row.surface);
        const polarity = tokens.some((row) => row.surface === "未") ? "not_yet" : tokens.some((row) => row.surface === "唔") ? "negative" : "positive";
        const candidate = tokens.find((row) => {
          const slots2 = row.slots || [];
          return slots2.includes("preference_predicate") || slots2.includes("modal") || slots2.includes("cognition_predicate") || slots2.includes("stance_predicate") || slots2.includes("speech_verb") || slots2.includes("acceptability_predicate") || slots2.includes("action_verb") || slots2.includes("stative_predicate");
        });
        if (!candidate) return null;
        const slots = candidate.slots || [];
        const profile = predicateOmissionProfileForHead2(candidate.surface);
        const candidateIndex = tokens.indexOf(candidate);
        const complementTokens = tokens.slice(candidateIndex + 1).filter((row) => {
          const rowSlots = row.slots || [];
          return !rowSlots.includes("particle") && !["唔", "未", "咗", "過", "緊", "喇", "呀", "啊"].includes(row.surface);
        });
        const hasOvertObject = complementTokens.some((row) => {
          const rowSlots = row.slots || [];
          return rowSlots.some((slot) => ["object", "theme", "head_noun", "np"].includes(slot));
        });
        const hasOvertPredicateComplement = complementTokens.some((row) => {
          const rowSlots = row.slots || [];
          return rowSlots.some((slot) => ["predicate", "vp", "action_verb", "movement_verb", "stative_predicate"].includes(slot));
        });
        let predicateFamily = "predicate";
        let missingSlots = [];
        if (profile) {
          predicateFamily = profile.parser_family;
          if (!hasOvertObject && !hasOvertPredicateComplement && topTrace.omission_status !== "conventional_bare_statement") {
            missingSlots = [profile.missing_slot_type];
          }
        } else if (slots.includes("modal")) {
          predicateFamily = "modal";
          if (!hasOvertObject && !hasOvertPredicateComplement) {
            missingSlots = [candidate.surface === "想" ? "desired_or_cognitive_content" : "modal_or_volitional_complement"];
          }
        } else if (slots.includes("preference_predicate")) {
          predicateFamily = "preference";
          if (!hasOvertObject) missingSlots = ["preference_object_or_domain"];
        } else if (slots.includes("action_verb") && tokenSemanticDomains2({ kind: "token", ...candidate }).includes("consumption")) {
          predicateFamily = "consumption";
          if (!hasOvertObject) missingSlots = ["object_or_activity_domain"];
        } else if (slots.includes("action_verb")) {
          predicateFamily = slots.includes("movement_verb") ? "motion" : "action";
          const objectSelecting = getConstructionAffordances2(candidate).can_head_productive_vo === true;
          if (objectSelecting && !hasOvertObject) missingSlots = ["object_or_activity_domain"];
          if (!subject && constructions.some((row) => ["PerfectiveVP", "PostverbalZoPerfectiveVP"].includes(row.type))) {
            missingSlots = Array.from(/* @__PURE__ */ new Set([...missingSlots, "subject"]));
          }
        } else if (slots.includes("stative_predicate")) {
          predicateFamily = "stative";
          if (!subject) missingSlots = ["subject_or_topic"];
        }
        if (Array.isArray(topTrace.missing_argument_slots) && topTrace.missing_argument_slots.length) {
          missingSlots = topTrace.missing_argument_slots.slice();
        }
        if (!subject && missingSlots.length && !missingSlots.includes("subject") && predicateFamily !== "stative") {
          missingSlots.push("subject");
        }
        return {
          head_surface: candidate.surface,
          predicate_family: predicateFamily,
          selected_alternative: polarity,
          subject_status: subject ? "explicit" : "omitted_unlicensed",
          missing_argument_slots: missingSlots,
          particle_contribution: particles,
          aspect: tokens.some((row) => row.surface === "咗") ? "overt_perfective" : "",
          top_type: top ? top.type : "",
          top_trace: topTrace,
          has_overt_object: hasOvertObject,
          has_perfective_structure: constructions.some((row) => ["PerfectiveVP", "PostverbalZoPerfectiveVP"].includes(row.type)),
          predicate_omission_profile: profile ? profile.id : topTrace.predicate_omission_profile || "",
          question_match_family: profile ? profile.question_match_family : topTrace.question_match_family || "",
          complement_type: profile ? profile.complement_type : topTrace.complement_type || "",
          conventionality_status: topTrace.conventionality_status || (profile ? profile.conventional_bare_status : ""),
          speech_event_use: topTrace.speech_event_use || "",
          legacy_context_metadata_active: slots.includes("preference_predicate") || slots.includes("modal") || slots.includes("action_verb") || slots.includes("stative_predicate")
        };
      }
      function compatibleContextQuestion2(target, contextTurns) {
        if (!target) return { descriptor: null, supplied: contextTurns.length > 0 };
        if (!contextTurns.length) return { descriptor: null, supplied: false, sawQuestion: false };
        const latestTurn = contextTurns[contextTurns.length - 1];
        const descriptor = questionDescriptorForContextTurn2(latestTurn);
        if (!descriptor) return { descriptor: null, supplied: true, sawQuestion: false, stale_context_blocked: contextTurns.length > 1 };
        const questionProfile = descriptor.predicate_omission_profile ? PREDICATE_OMISSION_PROFILES2.find((profile) => profile.id === descriptor.predicate_omission_profile) : predicateOmissionProfileForHead2(descriptor.head_surface);
        const responseProfile = target.predicate_omission_profile ? PREDICATE_OMISSION_PROFILES2.find((profile) => profile.id === target.predicate_omission_profile) : predicateOmissionProfileForHead2(target.head_surface);
        if (questionProfile || responseProfile) {
          if (!predicateProfilesCompatible2(questionProfile, responseProfile)) return { descriptor: null, supplied: true, sawQuestion: true };
        } else if (descriptor.head_surface !== target.head_surface) {
          return { descriptor: null, supplied: true, sawQuestion: true };
        }
        const completionTarget = target.has_perfective_structure || target.selected_alternative === "not_yet";
        if (descriptor.family === "completion_question" && !completionTarget) return { descriptor: null, supplied: true, sawQuestion: true };
        if (descriptor.family !== "completion_question" && completionTarget) return { descriptor: null, supplied: true, sawQuestion: true };
        if (descriptor.family === "modal_a_not_a" && target.predicate_family !== "modal") return { descriptor: null, supplied: true, sawQuestion: true };
        if (descriptor.family === "a_not_a" && target.predicate_family === "modal") return { descriptor: null, supplied: true, sawQuestion: true };
        return { descriptor, supplied: true, sawQuestion: true };
      }
      function existentialQuestionDescriptorForContextTurn2(turn) {
        const analysis = turn && turn.analysis;
        if (!analysis) return null;
        const rows = flattenNodes2(analysis.nodes || []);
        const constructions = rows.filter((row) => row.kind === "construction");
        const tokens = rows.filter((row) => row.kind === "token");
        const markerIndex = tokens.findIndex((row) => row.surface === "有冇" || (row.slots || []).includes("existential_question"));
        if (markerIndex < 0) return null;
        const haveOrNotConstruction = constructions.find((row) => {
          const detail2 = row.trace || {};
          return detail2.question_family === "have_or_not" || ["ExistentialQuestion", "ExperientialYesNoQuestion", "ANotAQuestion"].includes(row.type);
        });
        const questionMarked = /[？?]/u.test(String(turn.source || ""));
        if (!questionMarked || !haveOrNotConstruction) return null;
        const domainTokens = tokens.slice(markerIndex + 1).filter((row) => !(row.slots || []).includes("particle"));
        const domainSurface = domainTokens.map((row) => row.surface).join("");
        const detail = haveOrNotConstruction.trace || {};
        return {
          family: "existential_question",
          question_subtype: detail.question_subtype || detail.existential_subtype || "have_or_not",
          head_surface: "冇",
          positive_alternative: "有",
          negative_alternative: "冇",
          antecedent_span: tokens.slice(markerIndex).map((row) => row.surface).join(""),
          question_domain_surface: domainSurface,
          question_domain_status: domainSurface ? "overt" : "elliptical_in_question",
          context_turn_id: turn.id,
          question_id: turn.id,
          raw_source: turn.source
        };
      }
      function positiveResponseDiscourseAntecedentDescriptor2(turn) {
        if (!turn || !turn.analysis) return null;
        const source = String(turn.source || "").trim();
        const normalized = normalizeSurface2(source);
        if (!normalized) return null;
        const existentialQuestion = existentialQuestionDescriptorForContextTurn2(turn);
        if (existentialQuestion) {
          return {
            family: "existential_or_possessive_question",
            antecedent_span: existentialQuestion.antecedent_span || source,
            domain_surface: existentialQuestion.question_domain_surface || "",
            domain_status: existentialQuestion.question_domain_status || "question_domain",
            context_turn_id: turn.id,
            question_id: turn.id,
            question_subtype: existentialQuestion.question_subtype || "",
            antecedent_parse_status: "typed_question_observed"
          };
        }
        const rows = flattenNodes2(turn.analysis.nodes || []);
        const tokens = rows.filter((row) => row.kind === "token");
        const meaningful = tokens.filter((row) => !(row.slots || []).includes("particle"));
        const topConstructions = rows.filter((row) => row.kind === "construction" && row.depth === 0);
        const topIsDiscourseFormula = topConstructions.some(
          (row) => ["FormulaDiscourseUnit", "NegatedExistentialFragment"].includes(row.type) || (row.slots || []).includes("discourse_response")
        );
        const topIsProposition = topConstructions.some((row) => {
          if (["NeedsContext", "MalformedCandidate", "FragmentQuestion"].includes(row.type)) return false;
          if (/Question$/u.test(String(row.type || ""))) return false;
          const slots = row.slots || [];
          return slots.some((slot) => ["clause", "clause_like", "predicate", "vp", "action_vp"].includes(slot)) || /(?:Clause|Frame|Sequence)$/u.test(String(row.type || ""));
        });
        const hasSubjectOrTopic = meaningful.some((row) => {
          const slots = row.slots || [];
          return slots.includes("subject") || slots.includes("topic");
        });
        const hasPredicate = meaningful.some((row) => {
          const slots = row.slots || [];
          return slots.some((slot) => [
            "predicate",
            "action_verb",
            "stative_predicate",
            "cognition_predicate",
            "speech_verb",
            "modal",
            "existential",
            "negated_existential"
          ].includes(slot)) || /(?:verb|predicate|stative)/u.test(String(row.syntax || ""));
        });
        const questionMarked = /[？?]/u.test(source) || topConstructions.some((row) => /Question$/u.test(String(row.type || "")));
        const standalonePredicateResponse = !questionMarked && meaningful.length > 0 && meaningful.length <= 2 && hasPredicate;
        const propositionLike = !questionMarked && (topIsProposition || hasSubjectOrTopic && hasPredicate);
        if (!topIsDiscourseFormula && !standalonePredicateResponse && !propositionLike) return null;
        const existentialDomain = existentialDomainDescriptorForContextTurn2(turn);
        const family = propositionLike ? "asserted_proposition_turn" : "discourse_response_turn";
        return {
          family,
          antecedent_span: source,
          domain_surface: existentialDomain ? existentialDomain.domain_surface : normalized,
          domain_status: existentialDomain ? "overt_existential_domain" : "discourse_proposition",
          context_turn_id: turn.id,
          question_id: "",
          question_subtype: "",
          antecedent_parse_status: topIsProposition ? "propositional_construction_observed" : propositionLike ? "subject_predicate_evidence_observed" : topIsDiscourseFormula ? "discourse_formula_observed" : "elliptical_predicate_response_observed"
        };
      }
      function existentialDomainDescriptorForContextTurn2(turn) {
        const analysis = turn && turn.analysis;
        if (!analysis) return null;
        const tokens = tokenRowsForAnalysis2(analysis);
        const markerIndex = tokens.findIndex((row) => row.surface === "有" || row.surface === "冇" || (row.slots || []).includes("existential") || (row.slots || []).includes("negated_existential"));
        if (markerIndex < 0) return null;
        const excluded = /* @__PURE__ */ new Set(["我", "你", "佢", "我哋", "你哋", "佢哋", "連", "都", "又", "就", "有", "冇"]);
        const domainTokens = tokens.filter((row, index) => {
          if (index === markerIndex) return false;
          if (excluded.has(row.surface)) return false;
          const slots = row.slots || [];
          if (slots.includes("particle") || slots.includes("subject") || slots.includes("focus_adverb")) return false;
          return true;
        });
        const domainSurface = domainTokens.map((row) => row.surface).join("");
        if (!domainSurface) return null;
        return {
          family: "existential_domain_statement",
          domain_surface: domainSurface,
          antecedent_span: turn.source,
          context_turn_id: turn.id,
          raw_source: turn.source
        };
      }
      function eventDomainDescriptorForContextTurn2(turn, expectedHeadSurface = "") {
        const analysis = turn && turn.analysis;
        if (!analysis) return null;
        const tokens = tokenRowsForAnalysis2(analysis);
        const headIndex = tokens.findIndex((row) => row.surface === expectedHeadSurface && (row.slots || []).includes("action_verb"));
        if (headIndex < 0) return null;
        const tail = tokens.slice(headIndex + 1);
        const totalityIndex = tail.findIndex((row) => row.surface === "晒" && (row.slots || []).includes("completion_marker"));
        if (totalityIndex < 0) return null;
        const between = tail.slice(0, totalityIndex);
        if (between.some((row) => {
          const slots = row.slots || [];
          return !slots.includes("negator") && !slots.includes("m4_negator") && !slots.includes("focus_adverb") && !slots.includes("particle");
        })) return null;
        const eventTokens = [tokens[headIndex], ...tail.slice(0, totalityIndex + 1)];
        return {
          family: "same_event_totality_domain_statement",
          event_head_surface: expectedHeadSurface,
          event_surface: eventTokens.map((row) => row.surface).join(""),
          antecedent_span: turn.source,
          context_turn_id: turn.id,
          raw_source: turn.source
        };
      }
      function opinionContextSupportsMissingSlot2(turn, slot) {
        if (!turn || !turn.analysis) return false;
        const rows = flattenNodes2(turn.analysis.nodes || []);
        const tokens = rows.filter((row) => row.kind === "token");
        const constructions = rows.filter((row) => row.kind === "construction");
        if (slot === "existential_domain") {
          return tokens.some((row) => {
            const slots = row.slots || [];
            return ["有", "冇", "有冇"].includes(row.surface) || slots.includes("existential") || slots.includes("negated_existential") || slots.includes("existential_question");
          });
        }
        if (slot === "copular_complement") {
          const overtCopularFamily = tokens.some((row) => ["係", "唔係", "係咪"].includes(row.surface) || /(^|\s)(?:copula|negated_copula)(\s|$)/.test(String(row.syntax || ""))) || constructions.some((row) => ["CopularRelationFrame", "CopularIdentificationFrame", "CopularANotAQuestion"].includes(row.type));
          if (overtCopularFamily) return true;
          return constructions.some((row) => {
            const slots = row.slots || [];
            return slots.includes("clause") || slots.includes("clause_like") || slots.includes("predicate") || slots.includes("vp");
          }) || tokens.some((row) => {
            const slots = row.slots || [];
            return slots.some((name) => ["predicate", "modal", "action_verb", "stative_predicate", "seeming_marker"].includes(name));
          });
        }
        return false;
      }
      function quantifiedClassifierEllipsisDescriptor2(node) {
        if (!node || node.kind !== "construction" || node.type !== "QuantifiedClassifierNP") return null;
        const trace = node.trace || {};
        if (trace.fragment_subtype !== "quantified_classifier_head_ellipsis") return null;
        const rows = flattenNodes2([node]);
        const classifier = rows.find((row) => row.kind === "token" && (row.slots || []).includes("classifier"));
        const quantity = rows.find((row) => row.kind === "token" && (row.slots || []).includes("quantity"));
        if (!classifier || !quantity) return null;
        return {
          classifier_surface: classifier.surface || flattenSurface2(classifier),
          quantity_surface: quantity.surface || flattenSurface2(quantity)
        };
      }
      function contextSupportsQuantifiedClassifierFragment2(turn, phrase) {
        if (!turn || !turn.analysis) return false;
        const descriptor = quantifiedClassifierEllipsisDescriptor2(phrase);
        if (!descriptor || !descriptor.classifier_surface) return false;
        const source = normalizeSurface2(turn.source || "");
        if (!source) return false;
        const rows = flattenNodes2(turn.analysis.nodes || []);
        const tokens = rows.filter((row) => row.kind === "token");
        const constructions = rows.filter((row) => row.kind === "construction");
        const sameClassifierTokenIndexes = tokens.reduce((indexes, row, index) => {
          const slots = row.slots || [];
          if (row.surface === descriptor.classifier_surface && slots.includes("classifier")) indexes.push(index);
          return indexes;
        }, []);
        const sameClassifierWithFollowingHead = sameClassifierTokenIndexes.some(
          (classifierIndex) => tokens.slice(classifierIndex + 1).some((row) => {
            const slots = row.slots || [];
            return slots.includes("head_noun") || /noun|_np/.test(String(row.syntax || "")) && ["what", "who", "where"].includes(row.role || row.label);
          })
        );
        const sameClassifierConstructions = constructions.filter((row) => [
          "QuantifiedClassifierNP",
          "OvertHeadDemonstrativeClassifierNP",
          "ClassifierObjectNP",
          "OrdinalClassifierNP",
          "WhClassifierQuestion"
        ].includes(row.type) && String(row.surface || "").includes(descriptor.classifier_surface));
        const incompatibleAntecedent = sameClassifierConstructions.some((row) => {
          const trace = row.trace || {};
          return trace.classifier_head_compatibility_status === "incompatible";
        });
        const compatibleNpConstruction = sameClassifierConstructions.some((row) => {
          const trace = row.trace || {};
          if (trace.classifier_head_compatibility_status === "incompatible") return false;
          if (row.type === "QuantifiedClassifierNP") {
            return (row.children || []).some((child) => child && child.kind === "token" && (child.slots || []).includes("head_noun"));
          }
          return true;
        });
        const sameClassifierQuestionCue = source.includes(`幾${descriptor.classifier_surface}`) || source.includes(`邊${descriptor.classifier_surface}`) || source.includes(`呢${descriptor.classifier_surface}`) || source.includes(`嗰${descriptor.classifier_surface}`);
        if (incompatibleAntecedent && !compatibleNpConstruction) return false;
        return compatibleNpConstruction || sameClassifierQuestionCue || sameClassifierWithFollowingHead;
      }
      function conventionalZiDurationDescriptor2(structural) {
        if (!Array.isArray(structural) || !structural.length || structural.length > 2) return null;
        const phrase = structural[0];
        if (!phrase || phrase.kind !== "construction" || phrase.type !== "QuantifiedClassifierNP") return null;
        const children = (phrase.children || []).filter((node) => node && node.kind === "token");
        if (children.length !== 3) return null;
        const quantity = children.find((node) => (node.slots || []).includes("quantity"));
        const classifier = children.find((node) => (node.slots || []).includes("classifier"));
        const head = children.find((node) => (node.slots || []).includes("head_noun"));
        if (!quantity || !classifier || !head) return null;
        if (classifier.surface !== "個" || head.surface !== "字") return null;
        const particle = structural.length === 2 && structural[1] && structural[1].kind === "token" && (structural[1].slots || []).includes("particle") ? structural[1] : null;
        if (structural.length === 2 && !particle) return null;
        return { phrase, quantity, classifier, head, particle };
      }
      function conventionalZiContextDomain2(turn) {
        if (!turn) return "";
        const source = normalizeSurface2(turn.source || "");
        if (!source) return "";
        const durationCue = /(幾耐|幾多時間|時間|分鐘|鐘頭|點鐘|仲有幾耐|要等|等幾耐|等多|等咗|遲幾耐|早幾耐|車程|路程|先到|先返|先完|先得|過多耐|過幾耐)/.test(source);
        if (durationCue) return "duration";
        const literalCue = /(寫|讀|睇|句|文章|作文|標題|名字|個名|字數|文字|字符|字眼|詞語|單詞|幾多個字|幾個字)/.test(source);
        if (literalCue) return "literal_character_count";
        return "";
      }
      function contextSupportsQuantifiedTimeFragment2(turn) {
        if (!turn || !turn.analysis) return false;
        const source = normalizeSurface2(turn.source || "");
        if (!source) return false;
        const rows = flattenNodes2(turn.analysis.nodes || []);
        const tokens = rows.filter((row) => row.kind === "token");
        const constructions = rows.filter((row) => row.kind === "construction");
        const hasTimeMaterial = tokens.some((row) => {
          const slots = row.slots || [];
          return slots.includes("time") || slots.includes("time_head") || slots.includes("temporal_modifier");
        });
        const hasTimeConstruction = constructions.some((row) => [
          "QuantifiedTimeNP",
          "TimeNP",
          "TemporalClause",
          "ScalarValueQuestion"
        ].includes(row.type));
        const durationOrRateCue = /(幾耐|幾多(?:年|月|日)|時間|年|月|日|禮拜|星期|半年|人工|月薪|年薪|萬|蚊|錢|價|好未)/.test(source);
        return hasTimeMaterial || hasTimeConstruction || durationOrRateCue;
      }
      return {
        tokenRowsForAnalysis: tokenRowsForAnalysis2,
        questionDescriptorForContextTurn: questionDescriptorForContextTurn2,
        targetDescriptorForContext: targetDescriptorForContext2,
        compatibleContextQuestion: compatibleContextQuestion2,
        existentialQuestionDescriptorForContextTurn: existentialQuestionDescriptorForContextTurn2,
        positiveResponseDiscourseAntecedentDescriptor: positiveResponseDiscourseAntecedentDescriptor2,
        existentialDomainDescriptorForContextTurn: existentialDomainDescriptorForContextTurn2,
        eventDomainDescriptorForContextTurn: eventDomainDescriptorForContextTurn2,
        opinionContextSupportsMissingSlot: opinionContextSupportsMissingSlot2,
        quantifiedClassifierEllipsisDescriptor: quantifiedClassifierEllipsisDescriptor2,
        contextSupportsQuantifiedClassifierFragment: contextSupportsQuantifiedClassifierFragment2,
        conventionalZiDurationDescriptor: conventionalZiDurationDescriptor2,
        conventionalZiContextDomain: conventionalZiContextDomain2,
        contextSupportsQuantifiedTimeFragment: contextSupportsQuantifiedTimeFragment2
      };
    };
  }
});

// src/parser/context/turns.js
var require_turns = __commonJS({
  "src/parser/context/turns.js"(exports2, module2) {
    "use strict";
    module2.exports = function createContextTurns(dependencies = {}) {
      const {
        analyzeLine: analyzeLine2,
        diagnosticSummary: diagnosticSummary2,
        normalizeSurface: normalizeSurface2
      } = dependencies;
      function explicitContextTurns2(input) {
        if (!input) return [];
        const rawTurns = typeof input === "string" ? [{ source: input }] : Array.isArray(input) ? input : Array.isArray(input.turns) ? input.turns : input.source ? [input] : [];
        return rawTurns.map((turn) => typeof turn === "string" ? { source: turn } : turn || {}).filter((turn) => normalizeSurface2(turn.source)).map((turn, index) => ({
          id: String(turn.id || `context-${index + 1}`),
          source: String(turn.source || ""),
          directive_line_index: Number.isInteger(turn.directive_line_index) ? turn.directive_line_index : null,
          target_source_line_index: Number.isInteger(turn.target_source_line_index) ? turn.target_source_line_index : null,
          raw_directive: turn.raw_directive ? String(turn.raw_directive) : "",
          directive_kind: turn.directive_kind ? String(turn.directive_kind) : "explicit_context_next_source"
        }));
      }
      function analyzedExplicitContext2(input) {
        const turns = explicitContextTurns2(input).map((turn) => {
          const analysis = analyzeLine2(turn.source);
          return { ...turn, analysis };
        });
        return {
          turns,
          public: {
            supplied: turns.length > 0,
            turns: turns.map((turn) => ({
              id: turn.id,
              source: turn.source,
              directive_kind: turn.directive_kind,
              directive_line_index: turn.directive_line_index,
              target_source_line_index: turn.target_source_line_index,
              raw_directive: turn.raw_directive,
              parser_shadow_source: turn.analysis.parser_shadow_source,
              top_constructions: diagnosticSummary2(turn.analysis).top_constructions || []
            }))
          }
        };
      }
      return {
        explicitContextTurns: explicitContextTurns2,
        analyzedExplicitContext: analyzedExplicitContext2
      };
    };
  }
});

// src/parser/context/licensed-fragments.js
var require_licensed_fragments = __commonJS({
  "src/parser/context/licensed-fragments.js"(exports2, module2) {
    "use strict";
    module2.exports = function createLicensedContextFragments(dependencies = {}) {
      const {
        PREDICATE_OMISSION_PROFILES: PREDICATE_OMISSION_PROFILES2,
        applyRoleOverrides: applyRoleOverrides2,
        cleanSlots: cleanSlots2,
        construction: construction2,
        contextSupportsQuantifiedClassifierFragment: contextSupportsQuantifiedClassifierFragment2,
        contextSupportsQuantifiedTimeFragment: contextSupportsQuantifiedTimeFragment2,
        conventionalZiContextDomain: conventionalZiContextDomain2,
        conventionalZiDurationDescriptor: conventionalZiDurationDescriptor2,
        eventDomainDescriptorForContextTurn: eventDomainDescriptorForContextTurn2,
        existentialDomainDescriptorForContextTurn: existentialDomainDescriptorForContextTurn2,
        existentialQuestionDescriptorForContextTurn: existentialQuestionDescriptorForContextTurn2,
        firstToken: firstToken2,
        flattenNodes: flattenNodes2,
        flattenSurface: flattenSurface2,
        hasSentencePunctuation: hasSentencePunctuation2,
        isToken: isToken2,
        nodeCanFillSlot: nodeCanFillSlot2,
        normalizeSurface: normalizeSurface2,
        opinionContextSupportsMissingSlot: opinionContextSupportsMissingSlot2,
        parserInactiveTokenClone: parserInactiveTokenClone2,
        positiveResponseDiscourseAntecedentDescriptor: positiveResponseDiscourseAntecedentDescriptor2,
        predicateOmissionProfileForHead: predicateOmissionProfileForHead2,
        predicateProfilesCompatible: predicateProfilesCompatible2,
        quantifiedClassifierEllipsisDescriptor: quantifiedClassifierEllipsisDescriptor2,
        questionDescriptorForContextTurn: questionDescriptorForContextTurn2,
        templateDerivedSlots: templateDerivedSlots2,
        tokenRowsForAnalysis: tokenRowsForAnalysis2,
        traceInfo: traceInfo2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function splitTerminalContextNodes2(nodes) {
        const structural = nodes.slice();
        const terminal = [];
        while (structural.length && structural[structural.length - 1].kind === "text" && hasSentencePunctuation2(structural[structural.length - 1].text)) {
          terminal.unshift(structural.pop());
        }
        return { structural, terminal };
      }
      function contextRequiredTrace2(detail = {}) {
        const missing = Array.isArray(detail.missing_argument_slots) ? detail.missing_argument_slots : [];
        return traceInfo2("special_ambiguity_rule", {
          construction_type: "NeedsContext",
          reason: detail.reason || "The overt structure is valid, but an explicit compatible discourse antecedent is required.",
          context_requirement_status: detail.context_requirement_status || "context_required",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "unresolved" })),
          antecedent_status: detail.antecedent_status || "not_observed",
          discourse_license_not_observed: detail.antecedent_status !== "linked",
          ...detail
        });
      }
      function needsContextAroundExisting2(structural, detail = {}) {
        if (structural.length === 1 && structural[0].kind === "construction" && structural[0].type === "NeedsContext") {
          structural[0].trace = contextRequiredTrace2({ ...structural[0].trace || {}, ...detail });
          return structural[0];
        }
        return construction2("NeedsContext", "needs context", structural, {
          slots: cleanSlots2(["needs_context", "review_candidate", "problem_span"]),
          note: "Valid internal structure with an unresolved discourse-dependent slot.",
          trace: contextRequiredTrace2(detail)
        });
      }
      function fragmentChildrenFromStructural2(structural) {
        if (structural.length === 1 && structural[0].kind === "construction" && structural[0].type === "NeedsContext") {
          return structural[0].children || [];
        }
        return structural;
      }
      function licensedFragmentAnswer2(structural, target, question) {
        const children = fragmentChildrenFromStructural2(structural);
        const missing = target.missing_argument_slots || [];
        const selected = target.selected_alternative === "not_yet" ? "negative_not_yet" : target.selected_alternative;
        const objectDomainMissing = missing.some((slot) => slot.includes("object") || slot.includes("domain"));
        const overtQuestionDomain = Boolean(question.question_domain_surface);
        const fragmentType = target.predicate_family === "modal" ? "modal_response" : target.aspect ? "perfective_predicate_repetition_answer" : "predicate_repetition_answer";
        return construction2("FragmentAnswer", "fragment answer", children, {
          slots: cleanSlots2(["fragment_answer", "answer_fragment", "clause", target.subject_status === "explicit" ? "subject" : ""]),
          note: "Explicitly context-linked fragment answer. Overt target material remains visible; omitted slots link to the prior question without fabricated words.",
          trace: traceInfo2("governed_discourse_wrapper", {
            construction_type: "FragmentAnswer",
            fragment_subtype: fragmentType,
            context_requirement_status: "context_licensed",
            missing_argument_slots: missing,
            missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: question.context_turn_id })),
            antecedent_status: "linked",
            context_turn_id: question.context_turn_id,
            question_id: question.question_id,
            antecedent_span: question.antecedent_span,
            selected_alternative: selected,
            subject_status: target.subject_status === "explicit" ? "explicit" : "null_licensed",
            null_subject: target.subject_status === "explicit" ? "not_omitted" : "licensed",
            null_object: objectDomainMissing ? overtQuestionDomain ? "licensed" : "underdetermined_object_or_activity_domain" : "not_applicable",
            null_object_link: objectDomainMissing ? overtQuestionDomain ? "licensed_to_question_domain" : "question_licenses_activity_domain_without_overt_object_antecedent" : "not_applicable",
            aspect: target.aspect || "not_overt",
            complement_antecedent: target.predicate_family === "modal" ? "question_domain_unspecified" : "",
            complement_antecedent_span: target.predicate_family === "modal" ? question.antecedent_span : "",
            particle_contribution: target.particle_contribution || [],
            overt_head: target.head_surface,
            predicate_omission_profile: target.predicate_omission_profile || "",
            omission_status: "context_licensed_ellipsis",
            complement_type: target.complement_type || (target.predicate_family === "modal" ? "unspecified_np_vp_or_proposition" : ""),
            polarity: target.selected_alternative || "positive",
            conventionality_status: target.conventionality_status || "",
            speech_event_use: target.speech_event_use || "not_applicable",
            question_match_family: question.question_match_family || "",
            omission_analysis_candidates: target.predicate_family === "modal" ? ["modal_complement_ellipsis", "predicate_repetition_answer"] : ["argument_drop", "predicate_repetition_answer"],
            not_claims: ["not_fabricated_antecedent", "not_synthetic_missing_child", "not_invariant_yes_no_response"]
          })
        });
      }
      function licensedContextFragmentQuestion2(structural, explicitContext) {
        if (!explicitContext || !explicitContext.turns || !explicitContext.turns.length) return null;
        if (structural.length !== 1 || structural[0].kind !== "construction") return null;
        const only = structural[0];
        const fragment = only.type === "FragmentQuestion" ? only : only.type === "NeedsContext" ? (only.children || []).find((child) => child && child.kind === "construction" && child.type === "FragmentQuestion") : null;
        if (!fragment) return null;
        const latestTurn = explicitContext.turns[explicitContext.turns.length - 1];
        if (!latestTurn || !normalizeSurface2(latestTurn.source)) return null;
        const missing = fragment.trace && fragment.trace.missing_argument_slots || ["topic_or_alternative"];
        fragment.trace = {
          ...fragment.trace || {},
          context_requirement_status: "context_licensed",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latestTurn.id })),
          antecedent_status: "linked",
          context_turn_id: latestTurn.id,
          antecedent_span: latestTurn.source,
          discourse_license_not_observed: false,
          not_claims: Array.from(/* @__PURE__ */ new Set([...fragment.trace && fragment.trace.not_claims || [], "not_fabricated_antecedent"]))
        };
        return fragment;
      }
      function licensedContextNegatedExistentialFragment2(structural, explicitContext) {
        if (!explicitContext || !explicitContext.turns || !explicitContext.turns.length) return null;
        if (structural.length !== 1 || structural[0].kind !== "construction" || structural[0].type !== "NegatedExistentialFragment") return null;
        const fragment = structural[0];
        const latestTurn = explicitContext.turns[explicitContext.turns.length - 1];
        const question = existentialQuestionDescriptorForContextTurn2(latestTurn);
        if (!question) return null;
        const missing = fragment.trace && fragment.trace.missing_argument_slots || ["negative_response_domain"];
        fragment.trace = {
          ...fragment.trace || {},
          context_requirement_status: "context_licensed",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latestTurn.id })),
          antecedent_status: "linked",
          context_turn_id: latestTurn.id,
          question_id: latestTurn.id,
          antecedent_span: question.antecedent_span,
          selected_alternative: "negative",
          question_subtype: question.question_subtype,
          question_domain_status: question.question_domain_status,
          response_domain_surface: question.question_domain_surface,
          discourse_license_not_observed: false,
          not_claims: Array.from(/* @__PURE__ */ new Set([
            ...fragment.trace && fragment.trace.not_claims || [],
            "not_fabricated_domain",
            "not_syntactic_null_object_claim"
          ]))
        };
        return fragment;
      }
      function contextualPositiveExistentialAcknowledgementRepetition2(structural, explicitContext) {
        if (structural.length !== 1 || structural[0].kind !== "construction" || structural[0].type !== "FormulaDiscourseUnit") return null;
        const formula = structural[0];
        const trace = formula.trace || {};
        if (trace.formula_type !== "acknowledgement_repetition" || trace.acknowledgement_family !== "positive_existential") return null;
        if (!explicitContext || !explicitContext.turns || !explicitContext.turns.length) return formula;
        const latestTurn = explicitContext.turns[explicitContext.turns.length - 1];
        const descriptor = positiveResponseDiscourseAntecedentDescriptor2(latestTurn);
        if (!descriptor) return formula;
        const missing = Array.isArray(trace.missing_argument_slots) && trace.missing_argument_slots.length ? trace.missing_argument_slots.slice() : ["positive_response_domain"];
        formula.trace = {
          ...trace,
          context_requirement_status: "context_licensed",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latestTurn.id })),
          antecedent_status: "linked",
          context_turn_id: latestTurn.id,
          question_id: descriptor.question_id || "",
          antecedent_span: descriptor.antecedent_span,
          antecedent_family: descriptor.family,
          antecedent_parse_status: descriptor.antecedent_parse_status,
          selected_alternative: "positive",
          question_subtype: descriptor.question_subtype || "",
          response_domain_status: descriptor.domain_status,
          response_domain_surface: descriptor.domain_surface || "",
          discourse_license_not_observed: false,
          reason: "A structurally discourse-bearing preceding turn licenses the typed positive-response domain. The resolver uses broad question, proposition, and discourse-response categories; it does not require a memorized antecedent sentence or force semantic agreement with the previous polarity.",
          not_claims: Array.from(/* @__PURE__ */ new Set([...trace.not_claims || [], "not_fabricated_domain", "not_syntactic_null_object_claim"]))
        };
        return formula;
      }
      function licensedContextShortExperientialQuestion2(structural, explicitContext) {
        if (!explicitContext || !explicitContext.turns || !explicitContext.turns.length) return null;
        if (structural.length !== 1 || structural[0].kind !== "construction" || structural[0].type !== "NeedsContext") return null;
        const candidate = structural[0];
        const trace = candidate.trace || {};
        if (trace.candidate_construction_type !== "ExperientialQuestion" || trace.candidate_profile !== "objectless_experiential_final_mei" || trace.context_requirement_status !== "context_required") return null;
        const headSurface = String(trace.event_head_surface || "");
        if (headSurface !== "食") return null;
        const latestTurn = explicitContext.turns[explicitContext.turns.length - 1];
        if (!latestTurn || !latestTurn.analysis) return null;
        const contextTokens = flattenNodes2(latestTurn.analysis.nodes || []).filter((row) => row.kind === "token");
        const headIndex = contextTokens.findIndex((row) => row.surface === headSurface && (row.slots || []).includes("action_verb"));
        if (headIndex < 0) return null;
        const tail = contextTokens.slice(headIndex + 1);
        const nextPredicateIndex = tail.findIndex((row) => {
          const slots = row.slots || [];
          return slots.includes("action_verb") || slots.includes("main_verb") || slots.includes("stative_predicate");
        });
        const sameEventTail = nextPredicateIndex < 0 ? tail : tail.slice(0, nextPredicateIndex);
        const domainTokens = sameEventTail.filter((row) => {
          const slots = row.slots || [];
          return !slots.includes("subject") && (slots.includes("object") || slots.includes("theme"));
        });
        const domainSurface = domainTokens.map((row) => row.surface).join("");
        if (!domainSurface) return null;
        const children = candidate.children || [];
        const missing = trace.missing_argument_slots || ["object_or_experiential_domain"];
        const assignedSlots = children.map((node) => {
          if (nodeCanFillSlot2(node, "subject")) return "subject";
          if (nodeCanFillSlot2(node, "experiential_vp")) return "experiential_vp";
          if (isToken2(node, "未")) return "question_marker";
          if (nodeCanFillSlot2(node, "particle")) return "particle";
          return "retained_material";
        });
        return construction2("ExperientialQuestion", "Exp未", children, {
          slots: templateDerivedSlots2("ExperientialQuestion", children),
          note: "Source-backed discourse-recoverable 食過未 short profile. The omitted food/activity domain is linked to explicit compatible context; no hidden token is inserted.",
          trace: traceInfo2("governed_discourse_wrapper", {
            construction_type: "ExperientialQuestion",
            structural_scope: "clause",
            rule: "subject? + 食過 + 未 + particle?, licensed by explicit same-event overt-object context",
            assigned_slots: assignedSlots,
            surfaces: children.map((node) => flattenSurface2(node)),
            context_requirement_status: "context_licensed",
            missing_argument_slots: missing,
            missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latestTurn.id })),
            antecedent_status: "linked",
            discourse_license_not_observed: false,
            context_turn_id: latestTurn.id,
            antecedent_span: latestTurn.source,
            event_head_surface: headSurface,
            event_domain_antecedent_surface: domainSurface,
            omission_status: "context_licensed_discourse_recovery",
            hidden_object_insertion: false,
            source_backed_short_profile: "SRC-WFB-K3-NOODLES-2025",
            reason: "The reviewed school transcript supports objectless 食過未 only with a recoverable food referent. Runtime licensing therefore requires explicit preceding same-head event structure with an overt object/domain.",
            not_claims: ["not_context_free_aa61", "not_fabricated_object", "not_generalized_object_omission", "not_syntactic_null_object_claim"]
          })
        });
      }
      function licensedContextHaveOrNotEventQuestion2(structural, explicitContext) {
        if (!explicitContext || !explicitContext.turns || !explicitContext.turns.length) return null;
        if (structural.length !== 1 || structural[0].kind !== "construction") return null;
        const question = structural[0];
        if (!["ANotAQuestion", "ExperientialYesNoQuestion"].includes(question.type)) return null;
        if (!question.trace || question.trace.question_family !== "have_or_not") return null;
        if (question.trace.context_requirement_status !== "context_required") return null;
        const missing = question.trace.missing_argument_slots || [];
        if (!missing.includes("object_or_activity_domain")) return null;
        const expectedHeadSurface = String(question.trace.event_head_surface || "");
        if (!expectedHeadSurface) return null;
        const latestTurn = explicitContext.turns[explicitContext.turns.length - 1];
        const descriptor = eventDomainDescriptorForContextTurn2(latestTurn, expectedHeadSurface);
        if (!descriptor) return null;
        question.trace = {
          ...question.trace,
          context_requirement_status: "context_licensed",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latestTurn.id })),
          antecedent_status: "linked",
          context_turn_id: latestTurn.id,
          antecedent_span: descriptor.antecedent_span,
          event_domain_antecedent_surface: descriptor.event_surface,
          discourse_license_not_observed: false,
          not_claims: Array.from(/* @__PURE__ */ new Set([...question.trace.not_claims || [], "not_fabricated_event_domain", "not_syntactic_null_object_claim"]))
        };
        return question;
      }
      function licensedContextEllipticalExistentialQuestion2(structural, explicitContext) {
        if (!explicitContext || !explicitContext.turns || !explicitContext.turns.length) return null;
        if (structural.length !== 1 || structural[0].kind !== "construction" || structural[0].type !== "ExistentialQuestion") return null;
        const question = structural[0];
        if (!question.trace || question.trace.existential_subtype !== "elliptical_domain") return null;
        const latestTurn = explicitContext.turns[explicitContext.turns.length - 1];
        const descriptor = existentialDomainDescriptorForContextTurn2(latestTurn);
        if (!descriptor) return null;
        const missing = question.trace.missing_argument_slots || ["existential_domain"];
        question.trace = {
          ...question.trace,
          context_requirement_status: "context_licensed",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latestTurn.id })),
          antecedent_status: "linked",
          context_turn_id: latestTurn.id,
          antecedent_span: descriptor.antecedent_span,
          existential_domain_surface: descriptor.domain_surface,
          discourse_license_not_observed: false,
          not_claims: Array.from(/* @__PURE__ */ new Set([...question.trace.not_claims || [], "not_fabricated_object"]))
        };
        return question;
      }
      function typedContextDependentFragmentBoundary2(structural, explicitContext) {
        if (structural.length !== 1 || structural[0].kind !== "construction") return null;
        const fragment = structural[0];
        if (!["FragmentQuestion", "NegatedExistentialFragment"].includes(fragment.type)) return null;
        if (!fragment.trace || !fragment.trace.context_requirement_status) return null;
        if (explicitContext && explicitContext.turns && explicitContext.turns.length && fragment.trace.context_requirement_status === "context_required") {
          fragment.trace = {
            ...fragment.trace,
            context_requirement_status: "context_incompatible",
            antecedent_status: "incompatible",
            discourse_license_not_observed: true,
            reason: "Explicit context was supplied, but it does not provide the compatible discourse relation required by this typed fragment.",
            not_claims: Array.from(/* @__PURE__ */ new Set([...fragment.trace.not_claims || [], "not_fabricated_antecedent"]))
          };
        }
        return fragment;
      }
      function saturatedCompletionBoundary2(structural) {
        if (!Array.isArray(structural) || structural.length !== 1) return null;
        const only = structural[0];
        if (!only || only.kind !== "construction") return null;
        const rows = flattenNodes2(structural);
        const tokens = rows.filter((row) => row.kind === "token");
        const constructions = rows.filter((row) => row.kind === "construction");
        const hasOvertSubject = tokens.some((row) => (row.slots || []).includes("subject"));
        const hasOvertObject = tokens.some((row) => {
          const slots = row.slots || [];
          return !slots.includes("subject") && slots.some((slot) => ["object", "theme"].includes(slot));
        });
        if (only.type === "CompletionQuestion") {
          const questionMarker = tokens.find((row) => (row.slots || []).includes("question_marker") || row.surface === "未");
          if (!questionMarker) return null;
          const hasPerfectivePredicate = constructions.some((row) => ["PerfectiveVP", "PostverbalZoPerfectiveVP"].includes(row.type));
          return {
            boundary_type: "completion_question",
            completion_boundary_status: "constructionally_complete",
            activity_domain_status: hasOvertObject ? "overt_object" : hasPerfectivePredicate ? "unspecified_activity_reading_not_syntactic_null_object" : "constructionally_complete",
            reason: "A completion/not-yet question is a saturated polar-question construction; an objectless perfective predicate may denote the relevant activity without requiring an external antecedent.",
            not_claims: ["not_context_required", "not_fragment_answer", "not_syntactic_null_object_claim"]
          };
        }
        if (only.type === "ClauseSpan") {
          const perfective = constructions.find((row) => ["PerfectiveVP", "PostverbalZoPerfectiveVP"].includes(row.type));
          const changedStateParticle = tokens.find((row) => row.surface === "喇" && (row.slots || []).includes("particle"));
          if (!hasOvertSubject || !perfective || !changedStateParticle || hasOvertObject) return null;
          return {
            boundary_type: "perfective_changed_state_particle_clause",
            completion_boundary_status: "constructionally_complete",
            activity_domain_status: "unspecified_activity_reading_not_syntactic_null_object",
            particle_contribution: "changed_state_or_current_relevance_assertion",
            reason: "An overt-subject perfective predicate with changed-state particle 喇 forms a complete aspect-plus-particle clause; the activity reading is not treated as a missing syntactic object.",
            not_claims: ["not_context_required", "not_fragment_answer", "not_syntactic_null_object_claim"]
          };
        }
        return null;
      }
      function licensedContextOpinionStanceFrame2(structural, explicitContext) {
        if (!structural || structural.length !== 1) return null;
        const frame = structural[0];
        if (!frame || frame.kind !== "construction" || frame.type !== "OpinionStanceFrame") return null;
        const trace = frame.trace || {};
        if (trace.context_requirement_status !== "context_required") return null;
        const turns = explicitContext && explicitContext.turns || [];
        if (!turns.length) return null;
        const latest = turns[turns.length - 1];
        const missing = Array.isArray(trace.missing_argument_slots) ? trace.missing_argument_slots.slice() : [];
        if (!missing.length || !missing.every((slot) => opinionContextSupportsMissingSlot2(latest, slot))) return null;
        const linkedTrace = {
          ...trace,
          context_requirement_status: "context_licensed",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latest.id })),
          antecedent_status: "linked",
          discourse_license_not_observed: false,
          context_turn_id: latest.id,
          antecedent_span: latest.source,
          reason: "Explicit preceding discourse contains a compatible existential or copular predicate family, licensing the typed omitted complement without inserting hidden words.",
          not_claims: Array.from(/* @__PURE__ */ new Set([...trace.not_claims || [], "not_fabricated_antecedent", "not_context_free_ellipsis"]))
        };
        frame.trace = linkedTrace;
        const visit = (node) => {
          if (!node || node.kind !== "construction") return;
          if (node.type === "ComplementEllipsisFragment" && node.trace && node.trace.context_requirement_status === "context_required") {
            node.trace = {
              ...node.trace,
              context_requirement_status: "context_licensed",
              missing_argument_slots: missing,
              missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latest.id })),
              antecedent_status: "linked",
              discourse_license_not_observed: false,
              context_turn_id: latest.id,
              antecedent_span: latest.source
            };
          }
          for (const child of node.children || []) visit(child);
        };
        visit(frame);
        return frame;
      }
      function licensedContextConventionalCognitionStatement2(structural, explicitContext) {
        if (!structural || structural.length !== 1) return null;
        const statement = structural[0];
        if (!statement || statement.kind !== "construction" || statement.type !== "CognitionStatementClause") return null;
        const trace = statement.trace || {};
        const statementTokens = flattenNodes2([statement]).filter((row) => row.kind === "token");
        const cognitionHead = statementTokens.find((row) => row.surface === "知");
        if (!cognitionHead) return null;
        if (trace.predicate_omission_profile && trace.predicate_omission_profile !== "factive_cognition") return null;
        const turns = explicitContext && explicitContext.turns || [];
        if (!turns.length) return null;
        const latest = turns[turns.length - 1];
        const descriptor = questionDescriptorForContextTurn2(latest);
        if (!descriptor) return null;
        const questionProfile = descriptor.predicate_omission_profile ? PREDICATE_OMISSION_PROFILES2.find((profile) => profile.id === descriptor.predicate_omission_profile) : predicateOmissionProfileForHead2(descriptor.head_surface);
        const responseProfile = PREDICATE_OMISSION_PROFILES2.find((profile) => profile.id === "factive_cognition");
        if (!predicateProfilesCompatible2(questionProfile, responseProfile)) return null;
        const missing = [responseProfile.missing_slot_type];
        statement.trace = {
          ...trace,
          omission_status: "context_licensed_ellipsis",
          context_requirement_status: "context_licensed",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latest.id })),
          antecedent_status: "linked",
          discourse_license_not_observed: false,
          context_turn_id: latest.id,
          question_id: latest.id,
          antecedent_span: descriptor.antecedent_span || latest.source,
          question_match_family: descriptor.question_match_family || "",
          complement_type: responseProfile.complement_type,
          conventionality_status: "conventional_bare_statement_with_context_link",
          reason: "The conventional bare cognition statement remains a CognitionStatementClause, while explicit compatible polar context supplies its proposition/interrogative domain. No content token is inserted.",
          not_claims: Array.from(/* @__PURE__ */ new Set([...trace.not_claims || [], "not_fabricated_content_clause", "not_forced_fragment_answer", "not_literal_surface_equality_match"]))
        };
        return statement;
      }
      function licensedContextStancePredicateAnswer2(structural, explicitContext) {
        if (!structural || structural.length !== 1) return null;
        const turns = explicitContext && explicitContext.turns || [];
        if (!turns.length) return null;
        const latest = turns[turns.length - 1];
        if (!latest || !latest.analysis) return null;
        const targetRows = flattenNodes2(structural);
        const targetTokens = targetRows.filter((row) => row.kind === "token");
        const stance = targetTokens.find((row) => predicateOmissionProfileForHead2(row.surface)?.parser_family === "stance");
        if (!stance) return null;
        const stanceProfile = predicateOmissionProfileForHead2(stance.surface);
        const stanceIndex = targetTokens.indexOf(stance);
        const targetContent = targetTokens.slice(stanceIndex + 1).find((row) => {
          const slots = row.slots || [];
          return !slots.includes("particle") && row.surface !== "唔" && slots.some((slot) => ["stative_predicate", "predicate", "action_verb"].includes(slot));
        });
        if (!targetContent) return null;
        const questionTokens = tokenRowsForAnalysis2(latest.analysis);
        const questionStanceIndex = questionTokens.findIndex((row) => row.surface === stance.surface);
        if (questionStanceIndex < 0) return null;
        let repeated = false;
        for (let i = questionStanceIndex + 1; i < questionTokens.length - 2; i += 1) {
          if (questionTokens[i].surface === targetContent.surface && questionTokens[i + 1].surface === "唔" && questionTokens[i + 2].surface === targetContent.surface) {
            repeated = true;
            break;
          }
        }
        if (!repeated) return null;
        const missing = ["embedded_content_subject_or_topic"];
        return construction2("FragmentAnswer", "fragment answer", structural, {
          slots: cleanSlots2(["fragment_answer", "answer_fragment", "clause", "subject"]),
          note: "Context-linked stance response with visible stance predicate and selected content alternative.",
          trace: traceInfo2("governed_discourse_wrapper", {
            construction_type: "FragmentAnswer",
            fragment_subtype: "stance_predicate_content_answer",
            predicate_omission_profile: stanceProfile.id,
            omission_status: "context_licensed_ellipsis",
            context_requirement_status: "context_licensed",
            missing_argument_slots: missing,
            missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latest.id })),
            complement_type: stanceProfile.complement_type,
            antecedent_status: "linked",
            context_turn_id: latest.id,
            question_id: latest.id,
            antecedent_span: latest.source,
            selected_alternative: targetContent.surface,
            subject_status: "explicit",
            polarity: "positive",
            conventionality_status: "context_linked_response",
            speech_event_use: "not_applicable",
            overt_head: stance.surface,
            not_claims: ["not_fabricated_content_subject", "not_hidden_proposition", "not_literal_sentence_match"]
          })
        });
      }
      function contextualQuantifiedClassifierNPBoundary2(structural, explicitContext) {
        if (!Array.isArray(structural) || !structural.length) return null;
        const content = fragmentChildrenFromStructural2(structural);
        const candidates = [];
        const visit = (nodes) => {
          for (const node of nodes || []) {
            if (!node) continue;
            if (node.kind === "construction" && quantifiedClassifierEllipsisDescriptor2(node)) candidates.push(node);
            if (Array.isArray(node.children)) visit(node.children);
          }
        };
        visit(content);
        if (candidates.length !== 1) return null;
        const phrase = candidates[0];
        const missing = Array.isArray(phrase.trace && phrase.trace.missing_argument_slots) && phrase.trace.missing_argument_slots.length ? phrase.trace.missing_argument_slots.slice() : ["nominal_head"];
        const turns = explicitContext && explicitContext.turns || [];
        if (!turns.length) {
          const wrapper2 = needsContextAroundExisting2(structural, {
            context_requirement_status: "context_required",
            missing_argument_slots: missing,
            antecedent_status: "not_observed",
            discourse_license_not_observed: true,
            embedded_construction: "QuantifiedClassifierNP",
            fragment_subtype: "quantified_classifier_head_ellipsis",
            reason: "The visible numeral + classifier structure has an omitted nominal head that requires compatible explicit discourse; no hidden noun is inserted.",
            not_claims: ["not_fabricated_nominal_head", "not_context_free_head_recovery"]
          });
          return { nodes: [wrapper2], resolution: wrapper2.trace };
        }
        const latest = turns[turns.length - 1];
        if (contextSupportsQuantifiedClassifierFragment2(latest, phrase)) {
          phrase.trace = {
            ...phrase.trace || {},
            context_requirement_status: "context_licensed",
            missing_argument_slots: missing,
            missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latest.id })),
            antecedent_status: "linked",
            discourse_license_not_observed: false,
            context_turn_id: latest.id,
            antecedent_span: latest.source,
            np_license_status: "licensed_np",
            construction_licensing_allowed: true,
            structural_np_status: "context_licensed_head_ellipsis",
            downstream_argument_licensing: "allowed_by_context_link",
            np_license_reason: "Compatible explicit discourse licenses recovery of the omitted nominal domain without inserting a hidden noun token.",
            reason: "The immediately supplied discourse contains a compatible classifier and overt nominal domain, licensing the omitted noun head without inserting a hidden token.",
            not_claims: Array.from(/* @__PURE__ */ new Set([...phrase.trace && phrase.trace.not_claims || [], "not_fabricated_nominal_head", "not_context_free_head_recovery"]))
          };
          return { nodes: content, resolution: phrase.trace };
        }
        const wrapper = needsContextAroundExisting2(structural, {
          context_requirement_status: "context_incompatible",
          missing_argument_slots: missing,
          antecedent_status: "incompatible",
          discourse_license_not_observed: true,
          context_turn_id: latest.id,
          antecedent_span: latest.source,
          embedded_construction: "QuantifiedClassifierNP",
          fragment_subtype: "quantified_classifier_head_ellipsis",
          reason: "Explicit context was supplied, but it does not provide a compatible classifier/nominal domain for the omitted head.",
          not_claims: ["not_fabricated_nominal_head", "not_context_free_head_recovery"]
        });
        return { nodes: [wrapper], resolution: wrapper.trace };
      }
      function conventionalZiDurationConstruction2(descriptor, status = {}) {
        const template = {
          type: "QuantifiedTimeNP",
          role_overrides: {
            quantity: {
              label: "how",
              syntax: "quantity count_value numeral",
              slots: ["quantity"],
              note: "Visible numeral inside a conventional Cantonese clock-duration expression."
            },
            classifier: {
              label: "measure_word",
              syntax: "general_classifier classifier time_measure_classifier conventional_duration_measure",
              slots: ["classifier"],
              note: "Visible measure word inside the conventional duration expression."
            },
            time_head: {
              label: "when",
              syntax: "time_head temporal conventional_duration_unit five_minute_increment",
              slots: ["time", "time_head"],
              note: "In Cantonese clock-time expressions, 字 denotes a conventional five-minute interval."
            }
          }
        };
        const assignments = [
          { slot: "quantity", node: descriptor.quantity },
          { slot: "classifier", node: descriptor.classifier },
          { slot: "time_head", node: descriptor.head }
        ];
        if (descriptor.particle) assignments.push({ slot: "particle", node: descriptor.particle });
        const children = applyRoleOverrides2(assignments, template);
        const assignedSlots = assignments.map((item) => item.slot);
        const missing = status.missing_argument_slots || ["discourse_relation"];
        const trace = traceInfo2("generative_template", {
          construction_type: "QuantifiedTimeNP",
          template_family: "generative_template",
          template: ["quantity!", "classifier!", "time_head!", "particle?"],
          constraints: { conventional_time_head_surface: ["字"], classifier_surface: ["個"] },
          assigned_slots: assignedSlots,
          surfaces: children.map((node) => flattenSurface2(node)),
          role_overrides: template.role_overrides,
          subspan: true,
          fragment_subtype: "conventional_clock_duration_fragment",
          ambiguity_set: ["conventional_clock_duration", "literal_character_count"],
          selected_alternative: "conventional_clock_duration",
          conventional_unit_value_minutes: 5,
          context_requirement_status: status.context_requirement_status || "context_required",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({
            slot,
            license_status: status.context_requirement_status === "context_licensed" ? "licensed" : "unresolved",
            ...status.context_turn_id && status.context_requirement_status === "context_licensed" ? { licensed_by: status.context_turn_id } : {}
          })),
          antecedent_status: status.antecedent_status || "not_observed",
          discourse_license_not_observed: status.context_requirement_status !== "context_licensed",
          context_turn_id: status.context_turn_id || "",
          antecedent_span: status.antecedent_span || "",
          ambiguity_resolution_status: status.ambiguity_resolution_status || "conventional_default_context_needed",
          reason: status.reason || "The standalone numeral + 個 + 字 sequence selects the conventional Cantonese clock-duration reading by default, while compatible writing/text context may select the literal character-count NP; no minutes token is inserted.",
          not_claims: [
            "not_global_reanalysis_of_zi_as_time",
            "not_literal_character_count_in_this_reading",
            "not_fabricated_minute_token",
            "not_context_free_clause"
          ]
        });
        return construction2("QuantifiedTimeNP", "Time", children, {
          slots: ["quantified_time_np", "time", "time_head", "quantity", "classifier", "particle", "np", "topic"],
          note: "Conventional Cantonese clock-duration NP. 字 contributes a five-minute interval only in this resolved construction; the visible tokens remain transparent.",
          trace
        });
      }
      function contextualConventionalZiDurationBoundary2(structural, explicitContext) {
        const descriptor = conventionalZiDurationDescriptor2(structural);
        if (!descriptor) return null;
        const turns = explicitContext && explicitContext.turns || [];
        const latest = turns.length ? turns[turns.length - 1] : null;
        const domain = conventionalZiContextDomain2(latest);
        if (domain === "literal_character_count") {
          const phrase = descriptor.phrase;
          phrase.trace = {
            ...phrase.trace || {},
            ambiguity_set: ["conventional_clock_duration", "literal_character_count"],
            selected_alternative: "literal_character_count",
            ambiguity_resolution_status: "context_resolved_literal",
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            missing_slot_details: [],
            antecedent_status: "not_applicable",
            discourse_license_not_observed: false,
            context_turn_id: latest && latest.id || "",
            antecedent_span: latest && latest.source || "",
            reason: "Compatible writing/text discourse selects the ordinary literal character-count QuantifiedClassifierNP; 字 remains a visible noun head and is not reanalysed as a time unit.",
            not_claims: Array.from(/* @__PURE__ */ new Set([...phrase.trace && phrase.trace.not_claims || [], "not_conventional_duration_reading", "not_hidden_time_unit"]))
          };
          return { nodes: structural, resolution: phrase.trace };
        }
        if (domain === "duration") {
          const time2 = conventionalZiDurationConstruction2(descriptor, {
            context_requirement_status: "context_licensed",
            antecedent_status: "linked",
            context_turn_id: latest.id,
            antecedent_span: latest.source,
            ambiguity_resolution_status: "context_resolved_duration",
            reason: "Compatible duration/time discourse selects the conventional Cantonese clock-duration reading; each 字 denotes a five-minute interval without inserting an invisible 分鐘 token."
          });
          return { nodes: [time2], resolution: time2.trace };
        }
        if (latest) {
          const time2 = conventionalZiDurationConstruction2(descriptor, {
            context_requirement_status: "context_incompatible",
            antecedent_status: "incompatible",
            context_turn_id: latest.id,
            antecedent_span: latest.source,
            ambiguity_resolution_status: "context_incompatible_conventional_default",
            reason: "Explicit context was supplied, but it contains neither a duration/time cue nor a writing/text cue. The conventional duration candidate remains typed but unlicensed by that discourse."
          });
          return { nodes: [time2], resolution: time2.trace };
        }
        const time = conventionalZiDurationConstruction2(descriptor);
        return { nodes: [time], resolution: time.trace };
      }
      function licensedContextQuantifiedTimeNP2(structural, explicitContext) {
        if (!explicitContext || !explicitContext.turns || !explicitContext.turns.length) return null;
        if (structural.length !== 1 || structural[0].kind !== "construction" || structural[0].type !== "QuantifiedTimeNP") return null;
        const phrase = structural[0];
        const trace = phrase.trace || {};
        if (trace.context_requirement_status !== "context_required") return null;
        const latestTurn = explicitContext.turns[explicitContext.turns.length - 1];
        if (!latestTurn || !normalizeSurface2(latestTurn.source) || !contextSupportsQuantifiedTimeFragment2(latestTurn)) return null;
        const missing = Array.isArray(trace.missing_argument_slots) && trace.missing_argument_slots.length ? trace.missing_argument_slots.slice() : ["discourse_relation"];
        phrase.trace = {
          ...trace,
          context_requirement_status: "context_licensed",
          missing_argument_slots: missing,
          missing_slot_details: missing.map((slot) => ({ slot, license_status: "licensed", licensed_by: latestTurn.id })),
          antecedent_status: "linked",
          discourse_license_not_observed: false,
          context_turn_id: latestTurn.id,
          antecedent_span: latestTurn.source,
          reason: "A complete quantified time NP can stand as a fragment answer or duration/time specification when the immediately supplied discourse contains a compatible time, duration, quantity, or rate domain; no hidden clause or time unit is fabricated.",
          not_claims: Array.from(/* @__PURE__ */ new Set([...trace.not_claims || [], "not_fabricated_clause", "not_context_free_clause"]))
        };
        return phrase;
      }
      return {
        splitTerminalContextNodes: splitTerminalContextNodes2,
        contextRequiredTrace: contextRequiredTrace2,
        needsContextAroundExisting: needsContextAroundExisting2,
        fragmentChildrenFromStructural: fragmentChildrenFromStructural2,
        licensedFragmentAnswer: licensedFragmentAnswer2,
        licensedContextFragmentQuestion: licensedContextFragmentQuestion2,
        licensedContextNegatedExistentialFragment: licensedContextNegatedExistentialFragment2,
        contextualPositiveExistentialAcknowledgementRepetition: contextualPositiveExistentialAcknowledgementRepetition2,
        licensedContextShortExperientialQuestion: licensedContextShortExperientialQuestion2,
        licensedContextHaveOrNotEventQuestion: licensedContextHaveOrNotEventQuestion2,
        licensedContextEllipticalExistentialQuestion: licensedContextEllipticalExistentialQuestion2,
        typedContextDependentFragmentBoundary: typedContextDependentFragmentBoundary2,
        saturatedCompletionBoundary: saturatedCompletionBoundary2,
        licensedContextOpinionStanceFrame: licensedContextOpinionStanceFrame2,
        licensedContextConventionalCognitionStatement: licensedContextConventionalCognitionStatement2,
        licensedContextStancePredicateAnswer: licensedContextStancePredicateAnswer2,
        contextualQuantifiedClassifierNPBoundary: contextualQuantifiedClassifierNPBoundary2,
        conventionalZiDurationConstruction: conventionalZiDurationConstruction2,
        contextualConventionalZiDurationBoundary: contextualConventionalZiDurationBoundary2,
        licensedContextQuantifiedTimeNP: licensedContextQuantifiedTimeNP2
      };
    };
  }
});

// src/parser/context/apply-context-contract.js
var require_apply_context_contract = __commonJS({
  "src/parser/context/apply-context-contract.js"(exports2, module2) {
    "use strict";
    module2.exports = function createContextContractApplication(dependencies = {}) {
      const {
        compatibleContextQuestion: compatibleContextQuestion2,
        contextRequiredTrace: contextRequiredTrace2,
        contextualConventionalZiDurationBoundary: contextualConventionalZiDurationBoundary2,
        contextualPositiveExistentialAcknowledgementRepetition: contextualPositiveExistentialAcknowledgementRepetition2,
        contextualQuantifiedClassifierNPBoundary: contextualQuantifiedClassifierNPBoundary2,
        flattenNodes: flattenNodes2,
        flattenSurface: flattenSurface2,
        hasSentencePunctuation: hasSentencePunctuation2,
        licensedContextConventionalCognitionStatement: licensedContextConventionalCognitionStatement2,
        licensedContextEllipticalExistentialQuestion: licensedContextEllipticalExistentialQuestion2,
        licensedContextFragmentQuestion: licensedContextFragmentQuestion2,
        licensedContextHaveOrNotEventQuestion: licensedContextHaveOrNotEventQuestion2,
        licensedContextNegatedExistentialFragment: licensedContextNegatedExistentialFragment2,
        licensedContextOpinionStanceFrame: licensedContextOpinionStanceFrame2,
        licensedContextQuantifiedTimeNP: licensedContextQuantifiedTimeNP2,
        licensedContextShortExperientialQuestion: licensedContextShortExperientialQuestion2,
        licensedContextStancePredicateAnswer: licensedContextStancePredicateAnswer2,
        licensedFragmentAnswer: licensedFragmentAnswer2,
        needsContextAroundExisting: needsContextAroundExisting2,
        saturatedCompletionBoundary: saturatedCompletionBoundary2,
        splitTerminalContextNodes: splitTerminalContextNodes2,
        targetDescriptorForContext: targetDescriptorForContext2,
        typedContextDependentFragmentBoundary: typedContextDependentFragmentBoundary2,
        withoutIgnorableSpaceText: withoutIgnorableSpaceText2
      } = dependencies;
      function sentenceContextGroups2(nodes = []) {
        const groups = [];
        let current = [];
        for (const node of nodes || []) {
          current.push(node);
          if (node && node.kind === "text" && hasSentencePunctuation2(node.text)) {
            if (withoutIgnorableSpaceText2(current).length) groups.push(current);
            current = [];
          }
        }
        if (withoutIgnorableSpaceText2(current).length) groups.push(current);
        return groups;
      }
      function applyExplicitContextContract2(nodes, explicitContext) {
        const sentenceGroups = sentenceContextGroups2(nodes || []);
        if (sentenceGroups.length > 1) {
          const noCrossSentenceContext = { turns: [], public: { supplied: false, turns: [] } };
          const applied = sentenceGroups.map((group) => applyExplicitContextContract2(group, noCrossSentenceContext));
          const resolutions = applied.map((item) => item.resolution).filter(Boolean);
          const unresolved = resolutions.find((trace2) => ["context_required", "context_incompatible"].includes(trace2.context_requirement_status));
          return {
            nodes: applied.flatMap((item) => item.nodes || []),
            resolution: unresolved || resolutions[resolutions.length - 1] || null
          };
        }
        const { structural, terminal } = splitTerminalContextNodes2(nodes || []);
        if (!structural.length) return { nodes, resolution: null };
        const completionBoundary = saturatedCompletionBoundary2(structural);
        if (completionBoundary) {
          const top = structural[0];
          top.trace = {
            ...top.trace || {},
            context_requirement_status: "context_not_required",
            missing_argument_slots: [],
            antecedent_status: "not_applicable",
            completion_boundary_status: completionBoundary.completion_boundary_status,
            completion_boundary_type: completionBoundary.boundary_type,
            activity_domain_status: completionBoundary.activity_domain_status,
            ...completionBoundary.particle_contribution ? { particle_contribution: completionBoundary.particle_contribution } : {},
            completion_boundary_reason: completionBoundary.reason,
            not_claims: Array.from(/* @__PURE__ */ new Set([...top.trace && top.trace.not_claims || [], ...completionBoundary.not_claims]))
          };
          return { nodes: [...structural, ...terminal], resolution: null };
        }
        const licensedQuestionFragment = licensedContextFragmentQuestion2(structural, explicitContext);
        if (licensedQuestionFragment) {
          return { nodes: [licensedQuestionFragment, ...terminal], resolution: licensedQuestionFragment.trace };
        }
        const licensedShortExperientialQuestion = licensedContextShortExperientialQuestion2(structural, explicitContext);
        if (licensedShortExperientialQuestion) {
          return { nodes: [licensedShortExperientialQuestion, ...terminal], resolution: licensedShortExperientialQuestion.trace };
        }
        const licensedNegatedExistentialFragment = licensedContextNegatedExistentialFragment2(structural, explicitContext);
        if (licensedNegatedExistentialFragment) {
          return { nodes: [licensedNegatedExistentialFragment, ...terminal], resolution: licensedNegatedExistentialFragment.trace };
        }
        const contextualPositiveExistentialRepetition = contextualPositiveExistentialAcknowledgementRepetition2(structural, explicitContext);
        if (contextualPositiveExistentialRepetition) {
          return { nodes: [contextualPositiveExistentialRepetition, ...terminal], resolution: contextualPositiveExistentialRepetition.trace };
        }
        const typedFragmentBoundary = typedContextDependentFragmentBoundary2(structural, explicitContext);
        if (typedFragmentBoundary) {
          return { nodes: [typedFragmentBoundary, ...terminal], resolution: typedFragmentBoundary.trace };
        }
        const licensedEllipticalExistentialQuestion = licensedContextEllipticalExistentialQuestion2(structural, explicitContext);
        if (licensedEllipticalExistentialQuestion) {
          return { nodes: [licensedEllipticalExistentialQuestion, ...terminal], resolution: licensedEllipticalExistentialQuestion.trace };
        }
        const licensedHaveOrNotEventQuestion = licensedContextHaveOrNotEventQuestion2(structural, explicitContext);
        if (licensedHaveOrNotEventQuestion) {
          return { nodes: [licensedHaveOrNotEventQuestion, ...terminal], resolution: licensedHaveOrNotEventQuestion.trace };
        }
        const licensedConventionalCognition = licensedContextConventionalCognitionStatement2(structural, explicitContext);
        if (licensedConventionalCognition) {
          return { nodes: [licensedConventionalCognition, ...terminal], resolution: licensedConventionalCognition.trace };
        }
        const licensedStanceAnswer = licensedContextStancePredicateAnswer2(structural, explicitContext);
        if (licensedStanceAnswer) {
          return { nodes: [licensedStanceAnswer, ...terminal], resolution: licensedStanceAnswer.trace };
        }
        const licensedOpinionStance = licensedContextOpinionStanceFrame2(structural, explicitContext);
        if (licensedOpinionStance) {
          return { nodes: [licensedOpinionStance, ...terminal], resolution: licensedOpinionStance.trace };
        }
        const conventionalZiDurationBoundary = contextualConventionalZiDurationBoundary2(structural, explicitContext);
        if (conventionalZiDurationBoundary) {
          return {
            nodes: [...conventionalZiDurationBoundary.nodes, ...terminal],
            resolution: conventionalZiDurationBoundary.resolution
          };
        }
        const quantifiedClassifierBoundary = contextualQuantifiedClassifierNPBoundary2(structural, explicitContext);
        if (quantifiedClassifierBoundary) {
          const boundaryNodes = Array.isArray(quantifiedClassifierBoundary.nodes) ? quantifiedClassifierBoundary.nodes : [quantifiedClassifierBoundary];
          const resolution = quantifiedClassifierBoundary.resolution || (quantifiedClassifierBoundary.trace || null);
          return { nodes: [...boundaryNodes, ...terminal], resolution };
        }
        const licensedQuantifiedTime = licensedContextQuantifiedTimeNP2(structural, explicitContext);
        if (licensedQuantifiedTime) {
          return { nodes: [licensedQuantifiedTime, ...terminal], resolution: licensedQuantifiedTime.trace };
        }
        const target = targetDescriptorForContext2(structural);
        const only = structural.length === 1 && structural[0].kind === "construction" ? structural[0] : null;
        if (only && only.type === "ClauseRelationGraph" && only.trace && only.trace.topic_chain_status) {
          return { nodes: [...structural, ...terminal], resolution: only.trace };
        }
        const targetTokens = flattenNodes2(structural).filter((row) => row.kind === "token");
        const targetHasOvertObject = targetTokens.some((row) => {
          const slots = row.slots || [];
          return !slots.includes("subject") && slots.some((slot) => ["object", "theme", "head_noun"].includes(slot));
        });
        const targetNeedsObjectDomain = Boolean(target && (target.missing_argument_slots || []).some((slot) => slot.includes("object") || slot.includes("domain")));
        const unsaturatedPerfective = only && target && target.has_perfective_structure && (targetNeedsObjectDomain || target.subject_status !== "explicit") && !targetHasOvertObject;
        if (unsaturatedPerfective) {
          target.missing_argument_slots = Array.from(/* @__PURE__ */ new Set([
            ...target.missing_argument_slots || [],
            ...target.subject_status === "explicit" ? [] : ["subject"]
          ]));
        }
        const ineligibleMultiSpan = structural.some((node) => node.kind === "text" && hasSentencePunctuation2(node.text)) || only && ["ClauseSequence", "ClauseRelationGraph"].includes(only.type);
        const contextMatch = ineligibleMultiSpan ? { descriptor: null, supplied: (explicitContext.turns || []).length > 0, sawQuestion: false } : compatibleContextQuestion2(target, explicitContext.turns || []);
        if (target && contextMatch.descriptor && contextMatch.descriptor.question_domain_surface && !target.has_overt_object) {
          if (target.predicate_family === "action") {
            target.missing_argument_slots = Array.from(/* @__PURE__ */ new Set([...target.missing_argument_slots || [], "object_or_activity_domain"]));
          }
        }
        if (target && contextMatch.descriptor && target.subject_status !== "explicit" && target.predicate_family !== "stative") {
          target.missing_argument_slots = Array.from(/* @__PURE__ */ new Set([...target.missing_argument_slots || [], "subject"]));
        }
        if (target && target.missing_argument_slots.length && contextMatch.descriptor) {
          const fragment = licensedFragmentAnswer2(structural, target, contextMatch.descriptor);
          return {
            nodes: [fragment, ...terminal],
            resolution: fragment.trace
          };
        }
        const trace = only && only.trace ? only.trace : {};
        const traceRequiresContext = trace.context_requirement_status === "context_required";
        const shouldWrapExisting = traceRequiresContext && only && only.type !== "NeedsContext";
        const shouldWrapPerfective = Boolean(unsaturatedPerfective);
        if (shouldWrapExisting || shouldWrapPerfective) {
          const missing = traceRequiresContext ? trace.missing_argument_slots : target.missing_argument_slots;
          const status = contextMatch.supplied ? "context_incompatible" : "context_required";
          const wrapper = needsContextAroundExisting2(structural, {
            context_requirement_status: status,
            missing_argument_slots: missing || [],
            antecedent_status: contextMatch.supplied ? "incompatible" : "not_observed",
            discourse_license_not_observed: true,
            overt_head: trace.overt_head !== void 0 ? trace.overt_head : target && target.legacy_context_metadata_active ? target.head_surface : null,
            subject_status: trace.subject_status !== void 0 ? trace.subject_status : target && target.legacy_context_metadata_active ? target.subject_status : null,
            particle_contribution: trace.particle_contribution !== void 0 ? trace.particle_contribution : target && target.legacy_context_metadata_active ? target.particle_contribution : null,
            aspect: trace.aspect !== void 0 ? trace.aspect : target && target.legacy_context_metadata_active ? target.aspect : null,
            embedded_construction: only.type,
            reason: contextMatch.supplied ? "Explicit context was supplied, but it does not contain a compatible question/antecedent for the target's missing slots." : "The internal construction is valid, but its omitted argument/domain requires explicit compatible context.",
            not_claims: ["not_fabricated_antecedent", "not_clean_context_free_clause"]
          });
          return { nodes: [wrapper, ...terminal], resolution: wrapper.trace };
        }
        if (only && only.type === "NeedsContext" && contextMatch.supplied && !contextMatch.descriptor) {
          only.trace = contextRequiredTrace2({
            ...only.trace || {},
            context_requirement_status: "context_incompatible",
            antecedent_status: "incompatible",
            discourse_license_not_observed: true,
            reason: "Explicit context was supplied, but it does not license the target's typed missing slot."
          });
          return { nodes: [...structural, ...terminal], resolution: only.trace };
        }
        return {
          nodes: [...structural, ...terminal],
          resolution: only && only.trace && only.trace.context_requirement_status ? only.trace : null
        };
      }
      return {
        sentenceContextGroups: sentenceContextGroups2,
        applyExplicitContextContract: applyExplicitContextContract2
      };
    };
  }
});

// src/parser/diagnostics/trace-bindings.js
var require_trace_bindings = __commonJS({
  "src/parser/diagnostics/trace-bindings.js"(exports2, module2) {
    "use strict";
    var nodeShape = require_node_shape()({});
    var TRACE_BINDING_SCHEMA = "canto-span-trace-bindings-v1";
    var OFFSET_UNIT = "utf16_code_unit";
    module2.exports = function createTraceBindingAnnotator(dependencies = {}) {
      const nodeParserSurface2 = dependencies.nodeParserSurface || nodeShape.flattenSurface;
      const nodeDisplaySurface2 = dependencies.nodeDisplaySurface || nodeShape.flattenDisplaySurface;
      const externalNodeCanFillSlot = dependencies.nodeCanFillSlot;
      function absoluteSpan(start, end, relativeTo) {
        return { status: "unique", start, end, unit: OFFSET_UNIT, relative_to: relativeTo };
      }
      function relativeSpan(start, end) {
        return { status: "unique", start, end, unit: OFFSET_UNIT, relative_to: "construction_start" };
      }
      function orderedSurfaceSpan(text, surface, cursor) {
        const source = String(text || "");
        const target = String(surface || "");
        if (!target) return { start: cursor, end: cursor };
        const found = source.indexOf(target, cursor);
        if (found >= 0) return { start: found, end: found + target.length };
        return { start: cursor, end: cursor + target.length };
      }
      function isPunctuationOnly(component) {
        if (!component) return false;
        const surface = String(component.parser_surface || component.display_surface || "");
        return component.kind === "text" || /^[\s。、，！？?!；：…]+$/u.test(surface);
      }
      function directComponent(node, index, childResult, constructionParserStart, constructionSourceStart, parserText, sourceText) {
        const parserStart = childResult.parserStart;
        const parserEnd = childResult.parserEnd;
        const sourceStart = childResult.sourceStart;
        const sourceEnd = childResult.sourceEnd;
        return {
          index,
          kind: node && node.kind || "unknown",
          construction: node && node.kind === "construction" ? node.compatibility_alias || node.type || "" : "",
          internal_construction: node && node.kind === "construction" ? node.type || "" : "",
          parser_surface: parserText.slice(parserStart, parserEnd),
          normalized_parser_surface: nodeParserSurface2(node),
          display_surface: sourceText.slice(sourceStart, sourceEnd),
          normalized_display_surface: nodeDisplaySurface2(node),
          source_surface: sourceText.slice(sourceStart, sourceEnd),
          slots: Array.isArray(node && node.slots) ? [...node.slots] : [],
          parser_span: absoluteSpan(parserStart, parserEnd, "parser_shadow_source"),
          source_span: absoluteSpan(sourceStart, sourceEnd, "raw_source"),
          display_span: absoluteSpan(sourceStart, sourceEnd, "raw_source"),
          relative_parser_span: relativeSpan(parserStart - constructionParserStart, parserEnd - constructionParserStart),
          relative_source_span: relativeSpan(sourceStart - constructionSourceStart, sourceEnd - constructionSourceStart),
          relative_display_span: relativeSpan(sourceStart - constructionSourceStart, sourceEnd - constructionSourceStart),
          _node: node
        };
      }
      function combinedBinding(slot, components, constructionProvenance, bindingScope, provenance, parserText, sourceText) {
        if (!components.length) {
          return {
            slot,
            binding_kind: "semantic_slot",
            binding_scope: "construction_span",
            child_start: 0,
            child_end: 0,
            parser_surface: constructionProvenance.parser_surface,
            display_surface: constructionProvenance.display_surface,
            source_surface: constructionProvenance.source_surface,
            parser_span: { ...constructionProvenance.parser_span },
            source_span: { ...constructionProvenance.source_span },
            display_span: { ...constructionProvenance.display_span },
            relative_parser_span: relativeSpan(0, constructionProvenance.parser_span.end - constructionProvenance.parser_span.start),
            relative_source_span: relativeSpan(0, constructionProvenance.source_span.end - constructionProvenance.source_span.start),
            relative_display_span: relativeSpan(0, constructionProvenance.display_span.end - constructionProvenance.display_span.start),
            provenance
          };
        }
        const first = components[0];
        const last = components[components.length - 1];
        return {
          slot,
          binding_kind: "semantic_slot",
          binding_scope: bindingScope,
          child_start: first.index,
          child_end: last.index + 1,
          parser_surface: parserText.slice(first.parser_span.start, last.parser_span.end),
          display_surface: sourceText.slice(first.display_span.start, last.display_span.end),
          source_surface: sourceText.slice(first.source_span.start, last.source_span.end),
          parser_span: absoluteSpan(first.parser_span.start, last.parser_span.end, "parser_shadow_source"),
          source_span: absoluteSpan(first.source_span.start, last.source_span.end, "raw_source"),
          display_span: absoluteSpan(first.display_span.start, last.display_span.end, "raw_source"),
          relative_parser_span: relativeSpan(first.parser_span.start - constructionProvenance.parser_span.start, last.parser_span.end - constructionProvenance.parser_span.start),
          relative_source_span: relativeSpan(first.source_span.start - constructionProvenance.source_span.start, last.source_span.end - constructionProvenance.source_span.start),
          relative_display_span: relativeSpan(first.display_span.start - constructionProvenance.display_span.start, last.display_span.end - constructionProvenance.display_span.start),
          provenance
        };
      }
      function localNodeCanFillSlot(node, slot) {
        if (!node) return false;
        const slots = Array.isArray(node.slots) ? node.slots : [];
        if (slots.includes(slot)) return true;
        const normalizedSlot = String(slot || "");
        const type = node.kind === "construction" ? String(node.compatibility_alias || node.type || "") : "";
        const label = node.kind === "token" ? String(node.label || "") : "";
        const syntax = node.kind === "token" ? String(node.syntax || "") : "";
        const surface = nodeParserSurface2(node);
        if (/topic/.test(normalizedSlot) && (/(?:NP|Nominal|Topic)/.test(type) || ["who", "what", "where"].includes(label))) return true;
        if (/(?:predicate|vp|content_clause|reported_content)/.test(normalizedSlot) && (/(?:VP|Predicate|Clause|Frame)$/.test(type) || label === "doing" || label === "like" || /verb|stative/.test(syntax))) return true;
        if (/subject/.test(normalizedSlot) && (/(?:NP|Nominal)$/.test(type) || label === "who" || /pronoun/.test(syntax))) return true;
        if (/copula_(?:positive|negative)_arm/.test(normalizedSlot) && surface === "係") return true;
        if (/^copular_(?:predicate|nominal)_complement$/.test(normalizedSlot) && surface && surface !== "係" && surface !== "唔" && label !== "particle" && !/^[\s。、，！？?!；：…]+$/u.test(surface)) return true;
        if (/negator/.test(normalizedSlot) && (/negat/.test(syntax) || surface === "唔" || surface === "冇")) return true;
        if (/particle|marker/.test(normalizedSlot) && (label === "particle" || /particle|marker/.test(syntax))) return true;
        if (/modal/.test(normalizedSlot) && /modal/.test(syntax)) return true;
        if (/use_verb/.test(normalizedSlot) && surface === "用") return true;
        if (/lai_marker/.test(normalizedSlot) && surface === "嚟") return true;
        if (/host/.test(normalizedSlot) && label !== "particle") return true;
        return false;
      }
      function childCanFillSlot(component, slot) {
        if (!component || !component._node) return false;
        if (typeof externalNodeCanFillSlot === "function") {
          try {
            if (externalNodeCanFillSlot(component._node, slot)) return true;
          } catch (_error) {
          }
        }
        return localNodeCanFillSlot(component._node, slot);
      }
      function orderedSlotStarts(slots, components) {
        const starts = [];
        let cursor = 0;
        for (const slot of slots) {
          let found = -1;
          for (let index = cursor; index < components.length; index += 1) {
            if (childCanFillSlot(components[index], slot)) {
              found = index;
              break;
            }
          }
          if (found < 0) return null;
          starts.push(found);
          cursor = found + 1;
        }
        return starts;
      }
      function semanticBindingsForConstruction(node, components, constructionProvenance, parserText, sourceText) {
        const trace = node.trace || {};
        const slots = Array.isArray(trace.assigned_slots) ? trace.assigned_slots.filter(Boolean) : [];
        if (!slots.length) return { status: "not_applicable", bindings: [], resolution: "no_semantic_slots_declared" };
        if (slots.length === 1 && trace.relation_member_role && /_relation_member$/.test(slots[0])) {
          return {
            status: "complete",
            bindings: [combinedBinding(slots[0], components, constructionProvenance, "construction_span", "runtime_construction_span_role", parserText, sourceText)],
            resolution: "construction_span_role"
          };
        }
        if (slots.length === components.length) {
          return {
            status: "complete",
            bindings: slots.map((slot, index) => combinedBinding(slot, [components[index]], constructionProvenance, "direct_child", "runtime_child_identity", parserText, sourceText)),
            resolution: "direct_child_identity"
          };
        }
        const starts = orderedSlotStarts(slots, components);
        if (starts) {
          const bindings = slots.map((slot, index) => {
            const start = starts[index];
            const nextStart = index + 1 < starts.length ? starts[index + 1] : components.length;
            const range = components.slice(start, Math.max(start + 1, nextStart));
            while (range.length > 1 && isPunctuationOnly(range[range.length - 1])) range.pop();
            return combinedBinding(
              slot,
              range,
              constructionProvenance,
              range.length === 1 ? "direct_child" : "contiguous_child_range",
              range.length === 1 ? "runtime_child_affordance" : "runtime_child_range_affordance",
              parserText,
              sourceText
            );
          });
          return { status: "complete", bindings, resolution: "ordered_child_affordance_ranges" };
        }
        return { status: "legacy_unresolved", bindings: [], resolution: "no_deterministic_child_binding" };
      }
      function publicComponent(component) {
        const { _node, ...rest } = component;
        return rest;
      }
      function annotateNode(node, parserCursor, sourceCursor, parserText, sourceText) {
        const normalizedParserSurface = nodeParserSurface2(node);
        const normalizedDisplaySurface = nodeDisplaySurface2(node);
        if (!node || node.kind !== "construction") {
          const parserSpan = orderedSurfaceSpan(parserText, normalizedParserSurface, parserCursor);
          const sourceSpan = parserText === sourceText ? { start: parserSpan.start, end: parserSpan.end } : orderedSurfaceSpan(sourceText, normalizedDisplaySurface, sourceCursor);
          return {
            parserStart: parserSpan.start,
            parserEnd: parserSpan.end,
            sourceStart: sourceSpan.start,
            sourceEnd: sourceSpan.end
          };
        }
        const children = node.children || [];
        const childResults = [];
        let childParserCursor = parserCursor;
        let childSourceCursor = sourceCursor;
        for (const child of children) {
          const result = annotateNode(child, childParserCursor, childSourceCursor, parserText, sourceText);
          childResults.push(result);
          childParserCursor = result.parserEnd;
          childSourceCursor = result.sourceEnd;
        }
        const fallbackParserSpan = orderedSurfaceSpan(parserText, normalizedParserSurface, parserCursor);
        const fallbackSourceSpan = parserText === sourceText ? { start: fallbackParserSpan.start, end: fallbackParserSpan.end } : orderedSurfaceSpan(sourceText, normalizedDisplaySurface, sourceCursor);
        const parserStart = childResults.length ? childResults[0].parserStart : fallbackParserSpan.start;
        const parserEnd = childResults.length ? childResults[childResults.length - 1].parserEnd : fallbackParserSpan.end;
        const sourceStart = childResults.length ? childResults[0].sourceStart : fallbackSourceSpan.start;
        const sourceEnd = childResults.length ? childResults[childResults.length - 1].sourceEnd : fallbackSourceSpan.end;
        const constructionProvenance = {
          parser_surface: parserText.slice(parserStart, parserEnd),
          normalized_parser_surface: normalizedParserSurface,
          display_surface: sourceText.slice(sourceStart, sourceEnd),
          normalized_display_surface: normalizedDisplaySurface,
          source_surface: sourceText.slice(sourceStart, sourceEnd),
          parser_span: absoluteSpan(parserStart, parserEnd, "parser_shadow_source"),
          source_span: absoluteSpan(sourceStart, sourceEnd, "raw_source"),
          display_span: absoluteSpan(sourceStart, sourceEnd, "raw_source")
        };
        const components = children.map((child, index) => directComponent(
          child,
          index,
          childResults[index],
          parserStart,
          sourceStart,
          parserText,
          sourceText
        ));
        const semantic = semanticBindingsForConstruction(node, components, constructionProvenance, parserText, sourceText);
        node.trace = {
          ...node.trace || {},
          trace_binding_schema: TRACE_BINDING_SCHEMA,
          binding_contract_status: semantic.status,
          binding_resolution: semantic.resolution,
          construction_provenance: constructionProvenance,
          bindings: semantic.bindings,
          components: components.map(publicComponent)
        };
        return { parserStart, parserEnd, sourceStart, sourceEnd };
      }
      function annotateTraceBindings(nodes = [], options = {}) {
        const parserText = String(options.parserSource || "");
        const sourceText = String(options.rawSource || "");
        let parserCursor = 0;
        let sourceCursor = 0;
        for (const node of nodes || []) {
          const result = annotateNode(node, parserCursor, sourceCursor, parserText, sourceText);
          parserCursor = result.parserEnd;
          sourceCursor = result.sourceEnd;
        }
        return {
          schema: TRACE_BINDING_SCHEMA,
          parser_alignment_status: parserCursor === parserText.length ? "PASS" : "WARN",
          source_alignment_status: sourceCursor === sourceText.length ? "PASS" : "WARN",
          parser_consumed_length: parserCursor,
          parser_source_length: parserText.length,
          source_consumed_length: sourceCursor,
          raw_source_length: sourceText.length
        };
      }
      return { TRACE_BINDING_SCHEMA, annotateTraceBindings };
    };
  }
});

// src/parser/analyze-line.js
var require_analyze_line = __commonJS({
  "src/parser/analyze-line.js"(exports2, module2) {
    "use strict";
    var defaultTraceBindingAnnotator = require_trace_bindings()();
    var { annotateTraceTaxonomy: defaultAnnotateTraceTaxonomy } = require_trace_metadata();
    module2.exports = function createAnalyzeLine(dependencies = {}) {
      const {
        analyzedExplicitContext: analyzedExplicitContext2,
        annotateRawDisplaySurfaces: annotateRawDisplaySurfaces2,
        annotateTraceBindings = defaultTraceBindingAnnotator.annotateTraceBindings,
        annotateTraceTaxonomy = defaultAnnotateTraceTaxonomy,
        applyConstructionPatternsByPunctuation: applyConstructionPatternsByPunctuation2,
        applyExplicitContextContract: applyExplicitContextContract2,
        normalizeInputForParser: normalizeInputForParser2,
        normalizeSurface: normalizeSurface2,
        tokenizeLine: tokenizeLine2
      } = dependencies;
      function analyzeLine2(source, explicitContextInput = null) {
        const warnings = [];
        const explicitContext = analyzedExplicitContext2(explicitContextInput);
        const input_normalization = normalizeInputForParser2(source);
        const parserSource = input_normalization.parser_shadow_source;
        const normalized = normalizeSurface2(parserSource);
        if (normalized === "唔好食") {
          warnings.push("Needs context: 唔好食 can mean 唔 + 好食 = not tasty, or 唔好 + 食 = don't eat.");
        }
        const tokens = annotateRawDisplaySurfaces2(tokenizeLine2(parserSource), source, parserSource);
        const initialNodes = annotateRawDisplaySurfaces2(applyConstructionPatternsByPunctuation2(tokens), source, parserSource);
        const contextApplied = applyExplicitContextContract2(initialNodes, explicitContext);
        const nodes = annotateRawDisplaySurfaces2(contextApplied.nodes, source, parserSource);
        const trace_taxonomy_provenance = annotateTraceTaxonomy(nodes);
        const trace_binding_provenance = annotateTraceBindings(nodes, {
          rawSource: source,
          parserSource
        });
        return {
          source,
          parser_shadow_source: parserSource,
          input_normalization,
          normalization_trace: input_normalization.normalization_trace,
          normalization_review_suggestions: input_normalization.review_suggestions,
          warnings,
          tokens,
          nodes,
          trace_taxonomy_provenance,
          trace_binding_provenance,
          explicit_context: explicitContext.public,
          context_resolution: contextApplied.resolution,
          diagnostics: true
        };
      }
      return {
        analyzeLine: analyzeLine2
      };
    };
  }
});

// src/runtime-resources/presentation/diagnostic-reminders.js
var require_diagnostic_reminders = __commonJS({
  "src/runtime-resources/presentation/diagnostic-reminders.js"(exports2, module2) {
    "use strict";
    module2.exports = {
      coverage: "Coverage/test reminder: even when coverage is clean, inspect source coverage, duplicate sources, extraction warnings, semantic-review candidates, and top constructions before accepting behavior.",
      normalization: "Normalization reminder: even when normalization has no warnings, inspect raw_source, parser_shadow_source, normalization_trace, and review_suggestions to ensure learner-visible text was not silently replaced.",
      normalization_audit: "Normalization-audit reminder: even when the normalization audit passes, inspect raw source, parser shadow, trace counts, review-suggestion counts, unsafe-normalization counts, and raw-display preservation before accepting input behavior.",
      jyutping: "Jyutping reminder: even when pronunciation data passes, inspect token surfaces, syllable counts, and dictionary expectations for readings that may still be contextually wrong.",
      registry: "Registry reminder: even when registry labels pass, inspect learner roles, slot names, construction labels, and parser-decision trace labels for doctrinal fit.",
      learner_display: "Learner-display reminder: even when learner display passes, inspect role/slot visibility to ensure internal grammar metadata did not leak and learner-facing labels still fit the sentence.",
      learner_ui_hover: "Hover-gloss reminder: even when hover audit passes, inspect hover titles and glosses for generic wording, misleading translations, or raw/internal metadata leaks.",
      wrapper_coverage: "Wrapper-coverage reminder: even when wrapper coverage passes, inspect wrapper children, linkers, separators, and assigned-slot coverage for hidden holes or over-broad grouping.",
      label_transition: "Label-transition reminder: even when label transition passes, inspect trace kinds, template families, and construction names for broad-real-grammar fit and regression risk.",
      runtime_construction_registry: "Runtime-registry reminder: PASS means emitted construction labels are registered; it does not establish linguistic validity or promotion eligibility."
    };
  }
});

// src/plugin-entry.js
var {
  contextual: LEARNER_CONTEXTUAL_GLOSSES,
  surface: LEARNER_SURFACE_GLOSSES,
  construction: LEARNER_CONSTRUCTION_GLOSSES
} = require_learner_glosses();
var createLearnerDisplay = require_learner_display();
var createCantoSpanPlugin = require_canto_span_plugin();
var CANTO_SPAN_RUNTIME_VERSION = "0.5.236";
var {
  runtimeConstructionRegistryVersion: RUNTIME_CONSTRUCTION_REGISTRY_VERSION,
  constructionLabelRegistry,
  clauseSpanCompatibilityInputs,
  clauseRelationSubtypeRegistry,
  retiredConstructionLabelRegistry,
  retiredConstructionLabelAliases,
  internalConstructionCompatibilityAliases,
  internalOnlyConstructionScopes,
  constructionLabelPolicy
} = require_runtime_label_registry();
var CONSTRUCTION_LABEL_REGISTRY = new Set(constructionLabelRegistry);
var CLAUSE_SPAN_COMPATIBILITY_INPUTS = new Set(clauseSpanCompatibilityInputs);
var CLAUSE_RELATION_SUBTYPE_REGISTRY = new Set(clauseRelationSubtypeRegistry);
var RETIRED_CONSTRUCTION_LABEL_REGISTRY = new Map(retiredConstructionLabelRegistry);
var RETIRED_CONSTRUCTION_LABEL_ALIASES = new Map(retiredConstructionLabelAliases);
var INTERNAL_CONSTRUCTION_COMPATIBILITY_ALIASES = Object.freeze(internalConstructionCompatibilityAliases);
var INTERNAL_ONLY_CONSTRUCTION_SCOPES = Object.freeze(internalOnlyConstructionScopes);
var CONSTRUCTION_LABEL_POLICY = Object.freeze(constructionLabelPolicy);
var JYUTPING_REVIEW_EXPECTATIONS = Object.fromEntries(
  require_jyutping_review_expectations()
);
var PRODUCTIVE_VO = Object.fromEntries(require_productive_vo());
var VERB_OBJECT_COMPOUNDS = Object.fromEntries(require_verb_object_compounds());
var PRODUCTIVE_VO_COMPONENT_RULES = Object.freeze({ ...PRODUCTIVE_VO, ...VERB_OBJECT_COMPOUNDS });
var TOKEN_LEXICON_ENTRIES = require_token_lexicon();
var TOKEN_LEXICON = Object.fromEntries(TOKEN_LEXICON_ENTRIES);
var { buildLexicalAnalysisIndex } = require_lexical_analyses();
var TOKEN_LEXICAL_ANALYSES = buildLexicalAnalysisIndex(TOKEN_LEXICON_ENTRIES);
var FORMULAS = require_formulas();
var {
  protectedAddressTerms,
  addressSuffixes: ADDRESS_SUFFIXES,
  addressPrefixes: ADDRESS_PREFIXES,
  addressFollowers: ADDRESS_FOLLOWERS,
  commonSurnameCharacters
} = require_address_terms();
var PROTECTED_ADDRESS_TERMS = new Set(protectedAddressTerms);
var COMMON_SURNAME_CHARS = new Set(commonSurnameCharacters);
var COMPOSITIONAL_LEXICAL_PHRASES = new Set(
  require_compositional_lexical_phrases()
);
var UNKNOWN_CJK_JYUTPING_FALLBACK = Object.freeze(Object.fromEntries(
  require_unknown_cjk_jyutping_fallback()
));
function runtimeConstructionStateFor(type) {
  const construction2 = String(type || "");
  const active = CONSTRUCTION_LABEL_REGISTRY.has(construction2);
  return { construction: construction2, active, registry_missing: !active };
}
var {
  environmentalEventPredicates: ENVIRONMENTAL_EVENT_PREDICATES,
  predicateOmissionProfiles: PREDICATE_OMISSION_PROFILES
} = require_predicate_profiles();
var PRODUCTIVE_TERMS = Object.keys(PRODUCTIVE_VO_COMPONENT_RULES).sort((a, b) => b.length - a.length || a.localeCompare(b));
var PRODUCTIVE_VO_GENERATIVE_SURFACES = new Set(Object.keys(PRODUCTIVE_VO));
function predicateOmissionProfileForHead(surface) {
  const value = String(surface || "");
  return PREDICATE_OMISSION_PROFILES.find((profile) => (profile.positive_heads || []).includes(value)) || null;
}
function predicateOmissionProfileForQuestionForm(surface) {
  const value = String(surface || "");
  return PREDICATE_OMISSION_PROFILES.find((profile) => (profile.question_forms || []).includes(value)) || null;
}
function predicateProfilesCompatible(questionProfile, responseProfile) {
  if (!questionProfile || !responseProfile) return false;
  if (questionProfile.id === responseProfile.id) return true;
  if (questionProfile.question_match_family === responseProfile.question_match_family) return true;
  if (responseProfile.id === "factive_cognition" && questionProfile.question_match_family === "epistemic_event_polar") return true;
  return false;
}
var {
  learnerRoleLabels,
  slotNameDisallowedPrefixes: SLOT_NAME_DISALLOWED_PREFIXES,
  learnerDisplaySlotNames,
  learnerDisplaySlotLabels: LEARNER_DISPLAY_SLOT_LABELS
} = require_learner_display2();
var LEARNER_ROLE_LABELS = new Set(learnerRoleLabels);
var LEARNER_DISPLAY_SLOT_NAMES = new Set(learnerDisplaySlotNames);
function internalConstructionTypeFor(type = "") {
  return CLAUSE_SPAN_COMPATIBILITY_INPUTS.has(type) ? "ClauseSpan" : type;
}
function clauseSpanProfileForCompatibilityType(type = "") {
  if (type === "SubjectModalPredicateClause") return "subject_modal";
  if (type === "LocativeModalPredicateClause") return "locative_modal";
  if (type === "TopicModalPredicateClause") return "topic_modal";
  if (type === "CoordinatedSubjectModalPredicateClause") return "coordinated_subject_modal";
  return "subject_predicate";
}
function diagnosticCompatibilityConstructionType(type = "") {
  return INTERNAL_CONSTRUCTION_COMPATIBILITY_ALIASES[type] || type;
}
function diagnosticCompatibilityTrace(trace, compatibilityAlias = "") {
  if (!trace || typeof trace !== "object") return trace;
  const out = { ...trace };
  for (const key of ["construction_type", "preserved_root_construction"]) {
    if (out[key]) {
      out[key] = compatibilityAlias && out[key] === "ClauseSpan" ? compatibilityAlias : diagnosticCompatibilityConstructionType(out[key]);
    }
  }
  for (const key of ["child_constructions", "top_constructions", "top_child_constructions"]) {
    if (Array.isArray(out[key])) out[key] = out[key].map(diagnosticCompatibilityConstructionType);
  }
  return out;
}
var {
  templateTracePassthroughKeys: TEMPLATE_TRACE_PASSTHROUGH_KEYS,
  parserDecisionTraceKindRegistry,
  templateFamilyRegistry,
  parserDecisionLabelPolicy,
  labelTransitionKindPolicy: LABEL_TRANSITION_KIND_POLICY
} = require_trace_metadata();
var PARSER_DECISION_TRACE_KIND_REGISTRY = new Map(parserDecisionTraceKindRegistry);
var TEMPLATE_FAMILY_REGISTRY = new Map(templateFamilyRegistry);
var PARSER_DECISION_LABEL_POLICY = Object.freeze(parserDecisionLabelPolicy);
function templateFamilyForDefinition(template = {}) {
  if (template.template_family && TEMPLATE_FAMILY_REGISTRY.has(template.template_family)) return template.template_family;
  const constraints = template.constraints || {};
  const hasSurfaceConstraint = Boolean(
    constraints.slot_surface_in || constraints.slot_surface_not_in || constraints.first_node_must_have_surface
  );
  const hasChildOverride = Boolean(
    template.child_parser_inactive_overrides && Object.keys(template.child_parser_inactive_overrides).length
  );
  const note = String(template.note || "");
  const boundedLanguage = /\b(bounded|controlled|constrained|reviewed|closed-list|formula|protected|surface-specific)\b/i.test(note);
  return hasSurfaceConstraint || hasChildOverride || boundedLanguage ? "construction_template" : "generative_template";
}
function learnerDisplaySlotLabel(slot) {
  return LEARNER_DISPLAY_SLOT_LABELS[slot] || slot;
}
function learnerDisplaySlots(slots = []) {
  return cleanSlots(slots).filter((slot) => LEARNER_DISPLAY_SLOT_NAMES.has(slot)).map(learnerDisplaySlotLabel);
}
function internalOnlySlots(slots = []) {
  return cleanSlots(slots).filter((slot) => !LEARNER_DISPLAY_SLOT_NAMES.has(slot));
}
function normalizeLearnerLabel(label, surface = "", syntax = "") {
  if (LEARNER_ROLE_LABELS.has(label)) return label;
  if (label === "classifier") return "measure_word";
  if (label === "stance") {
    if (surface === "鍾意") return "like";
    if (syntax.includes("suggestion_marker") || syntax.includes("possibility") || syntax.includes("seeming") || syntax.includes("should")) return "func";
    return "doing";
  }
  return "neutral";
}
var {
  slotNameRegistryIssue,
  isCleanSlotName,
  cleanSlots
} = require_clean_slots();
function learnerRoleRegistryRows(analysis) {
  return flattenNodes(analysis.nodes).filter((row) => row.kind === "token").filter((row) => !LEARNER_ROLE_LABELS.has(row.role || "")).map((row) => ({
    surface: row.surface,
    role: row.role || "",
    syntax: row.syntax || "",
    parent: row.parent || "",
    reason: "Token learner role is outside the controlled learner-role registry."
  }));
}
function slotNameRegistryRows(analysis) {
  const rows = [];
  for (const row of flattenNodes(analysis.nodes)) {
    for (const slot of row.slots || []) {
      const issue = slotNameRegistryIssue(slot);
      if (!issue) continue;
      rows.push({
        surface: row.surface || "",
        kind: row.kind || "",
        construction: row.type || "",
        role: row.role || "",
        slot,
        issue,
        reason: issue === "implementation_phase_marker_in_slot" ? "Phase/custom implementation markers belong in trace or feature bundles, not slot names." : "Slot names must be lower snake_case registry labels, not construction names or display labels."
      });
    }
  }
  return rows;
}
function constructionLabelRegistryRows(analysis) {
  return flattenNodes(analysis.nodes).filter((row) => row.kind === "construction").map((row) => {
    const construction2 = row.type || "";
    const retiredReason = RETIRED_CONSTRUCTION_LABEL_REGISTRY.get(construction2);
    if (retiredReason) {
      return {
        surface: row.surface || "",
        construction: construction2,
        issue: "retired_or_over_specific_construction_label",
        reason: retiredReason,
        policy: CONSTRUCTION_LABEL_POLICY
      };
    }
    if (!CONSTRUCTION_LABEL_REGISTRY.has(construction2)) {
      return {
        surface: row.surface || "",
        construction: construction2,
        issue: "construction_label_outside_controlled_registry",
        reason: "Construction label is outside the controlled construction-label registry. New labels require grammar evidence first; explicit approval is required only for useful project-specific labels that lack external or recurring grammar justification.",
        policy: CONSTRUCTION_LABEL_POLICY
      };
    }
    return null;
  }).filter(Boolean);
}
function parserDecisionLabelRegistryRows(analysis) {
  const rows = [];
  for (const row of flattenNodes(analysis.nodes)) {
    const trace = row.trace || {};
    const kind = trace.kind || "";
    if (kind && !PARSER_DECISION_TRACE_KIND_REGISTRY.has(kind)) {
      rows.push({
        surface: row.surface || "",
        kind: row.kind || "",
        construction: row.type || "",
        trace_kind: kind,
        issue: "parser_decision_trace_kind_outside_registry",
        reason: "Trace kind is used in parser diagnostics or parser decisions but is outside the controlled parser-decision trace registry.",
        policy: PARSER_DECISION_LABEL_POLICY
      });
    }
    const templateFamily = trace.template_family || "";
    if (templateFamily && !TEMPLATE_FAMILY_REGISTRY.has(templateFamily)) {
      rows.push({
        surface: row.surface || "",
        kind: row.kind || "",
        construction: row.type || "",
        template_family: templateFamily,
        issue: "template_family_outside_registry",
        reason: "Template family must remain one of the controlled names while construction_template and generative_template are still distinct.",
        policy: PARSER_DECISION_LABEL_POLICY
      });
    }
  }
  return rows;
}
function registryAuditSummary(analysis) {
  const invalidLearnerRoles = learnerRoleRegistryRows(analysis);
  const invalidSlotNames = slotNameRegistryRows(analysis);
  const invalidConstructionLabels = constructionLabelRegistryRows(analysis);
  const invalidParserDecisionLabels = parserDecisionLabelRegistryRows(analysis);
  return {
    status: invalidLearnerRoles.length || invalidSlotNames.length || invalidConstructionLabels.length || invalidParserDecisionLabels.length ? "WARN" : "PASS",
    learner_role_registry_status: invalidLearnerRoles.length ? "WARN" : "PASS",
    slot_name_registry_status: invalidSlotNames.length ? "WARN" : "PASS",
    construction_label_registry_status: invalidConstructionLabels.length ? "WARN" : "PASS",
    parser_decision_label_registry_status: invalidParserDecisionLabels.length ? "WARN" : "PASS",
    invalid_learner_role_count: invalidLearnerRoles.length,
    invalid_slot_name_count: invalidSlotNames.length,
    invalid_construction_label_count: invalidConstructionLabels.length,
    invalid_parser_decision_label_count: invalidParserDecisionLabels.length,
    invalid_learner_roles: invalidLearnerRoles,
    invalid_slot_names: invalidSlotNames,
    invalid_construction_labels: invalidConstructionLabels,
    invalid_parser_decision_labels: invalidParserDecisionLabels,
    parser_decision_label_policy: PARSER_DECISION_LABEL_POLICY
  };
}
function learnerDisplayAuditRows(analysis) {
  return flattenNodes(analysis.nodes).map((row) => {
    const hidden = internalOnlySlots(row.slots || []);
    return {
      surface: row.surface || "",
      kind: row.kind || "",
      construction: row.type || "",
      role: row.role || "",
      learner_display_slots: learnerDisplaySlots(row.slots || []),
      hidden_internal_slots: hidden
    };
  }).filter((row) => row.hidden_internal_slots.length);
}
function learnerDisplayAuditSummary(analysis) {
  const rows = learnerDisplayAuditRows(analysis);
  const hiddenCounts = {};
  for (const row of rows) {
    for (const slot of row.hidden_internal_slots) hiddenCounts[slot] = (hiddenCounts[slot] || 0) + 1;
  }
  return {
    status: "PASS",
    learner_display_slot_status: "PASS",
    policy: "Parser/internal slots remain available in diagnostics. Hover titles may expose concise role/syntax metadata plus learner glosses, but raw slots and internal counts remain diagnostics-only.",
    nodes_with_hidden_internal_slots: rows.length,
    hidden_internal_slot_reference_count: Object.values(hiddenCounts).reduce((sum, count) => sum + count, 0),
    hidden_internal_slot_counts: hiddenCounts,
    sample_hidden_internal_slot_rows: rows.slice(0, 12)
  };
}
var SLOT_GENERATION_RULES = require_slot_generation_rules();
var CONSTRUCTION_TEMPLATES = require_construction_templates();
var CATEGORY_SPAN_TEMPLATES = require_category_span_templates();
var SLOT_ALIASES = require_slot_aliases();
var PUNCT_RE = require_punctuation();
var {
  normalizeSurface,
  foldedPinyinFalloutRepair,
  normalizeInputForParser,
  nodeParserSurface,
  nodeDisplaySurface,
  annotateRawDisplaySurfaces,
  inputNormalizationHasFindings
} = require_normalize_input();
function normalizationReviewSuggestionsForAnalysis(analysis) {
  if (!analysis) return [];
  if (analysis.input_normalization && Array.isArray(analysis.input_normalization.review_suggestions)) {
    return analysis.input_normalization.review_suggestions;
  }
  return Array.isArray(analysis.normalization_review_suggestions) ? analysis.normalization_review_suggestions : [];
}
function normalizationReviewSuggestionDisplayRows(analysis) {
  return normalizationReviewSuggestionsForAnalysis(analysis).map((suggestion) => ({
    index: suggestion.index,
    length: suggestion.length || Array.from(String(suggestion.raw || "")).length || 1,
    raw: suggestion.raw,
    suggested: suggestion.suggested,
    display: suggestion.display || `${suggestion.raw} → ${suggestion.suggested}`,
    source: suggestion.source || "character",
    status: suggestion.status || "review_only_not_applied",
    applied_to_parser_shadow: suggestion.applied_to_parser_shadow === true,
    learner_display_replaced: suggestion.learner_display_replaced === true
  }));
}
function foldedLexicalRepairTraceRowsForAnalysis(analysis) {
  if (!analysis) return [];
  const trace = analysis.input_normalization && Array.isArray(analysis.input_normalization.normalization_trace) ? analysis.input_normalization.normalization_trace : Array.isArray(analysis.normalization_trace) ? analysis.normalization_trace : [];
  return trace.filter((item) => item.type === "pinyin_fallout_cantonese_lexical_repair");
}
function foldedLexicalRepairDisplayRows(analysis) {
  return foldedLexicalRepairTraceRowsForAnalysis(analysis).map((item) => ({
    index: item.index,
    length: 1,
    raw: item.raw,
    normalized: item.normalized,
    display: `${item.raw} → ${item.normalized}`,
    source: item.source || "pinyin_simplified_typing_fallout",
    status: item.status || "folded_parser_shadow_only",
    confidence: item.confidence || "high",
    applied_to_parser_shadow: item.applied_to_parser_shadow === true,
    learner_display_replaced: item.learner_display_replaced === true,
    one_to_one_character_mapping: item.one_to_one_character_mapping === true,
    note: item.note || "Folded pinyin-fallout lexical repair applied to parser shadow only."
  }));
}
function parserShadowRepairTraceRowsForAnalysis(analysis) {
  if (!analysis) return [];
  const trace = analysis.input_normalization && Array.isArray(analysis.input_normalization.normalization_trace) ? analysis.input_normalization.normalization_trace : Array.isArray(analysis.normalization_trace) ? analysis.normalization_trace : [];
  return trace.filter((item) => item && item.applied_to_parser_shadow === true && item.raw !== item.normalized);
}
function parserShadowRepairKind(item) {
  if (!item) return "unknown";
  if (item.type === "pinyin_fallout_cantonese_lexical_repair") return "folded_lexical_repair";
  if (item.type === "simplified_to_traditional") return "character_form_normalization";
  return item.type || "unknown";
}
function parserShadowRepairDisplayRows(analysis) {
  return parserShadowRepairTraceRowsForAnalysis(analysis).map((item) => {
    const note = String(item.note || "").trim();
    return {
      index: item.index,
      length: item.length || 1,
      raw: item.raw,
      normalized: item.normalized,
      display: `${item.raw} → ${item.normalized}`,
      type: item.type || "unknown",
      repair_kind: parserShadowRepairKind(item),
      source: item.source || "unknown",
      status: item.status || "applied_parser_shadow_only",
      confidence: item.confidence || "unknown",
      applied_to_parser_shadow: item.applied_to_parser_shadow === true,
      learner_display_replaced: item.learner_display_replaced === true,
      one_to_one_character_mapping: item.one_to_one_character_mapping === true,
      note: note || null,
      note_coverage_status: note ? "PASS" : "WARN"
    };
  });
}
function parserShadowRepairTypeCounts(rows) {
  return (rows || []).reduce((acc, row) => {
    const key = row.repair_kind || row.type || "unknown";
    acc[key] = (acc[key] || 0) + 1;
    return acc;
  }, {});
}
function parserShadowRepairNoteCoverageSummary(rows) {
  const repairs = rows || [];
  const missing = repairs.filter((row) => !row.note || row.note_coverage_status !== "PASS");
  return {
    status: missing.length ? "WARN" : "PASS",
    parser_shadow_repair_count: repairs.length,
    missing_shadow_repair_note_count: missing.length
  };
}
function parserShadowRepairKindLabel(row) {
  const kind = row && (row.repair_kind || row.type);
  if (kind === "folded_lexical_repair") return "folded lexical";
  if (kind === "character_form_normalization") return "character form";
  return String(kind || "shadow repair").replace(/_/g, " ");
}
function safeClass(value) {
  return String(value || "neutral").replace(/[^a-z0-9_-]/gi, "-").toLowerCase();
}
var {
  hasAny,
  stringIncludesAny,
  contextualRoleAffordances,
  inferTokenFeatures,
  compactFeatureSummary,
  featureList,
  syntaxHas,
  featureBundleFor,
  getFeatureBundle,
  getParserFeatures,
  getConstructionAffordances,
  getCoreDimensions,
  getLexicalizationType,
  hasPredicateSubtype,
  isLexicalizedStative,
  isNegativeLexicalizedStative,
  isOrdinaryStative,
  isStativePredicateByBundle,
  isGradablePredicate,
  canHeadComment,
  bundleCanFillStativeSlot,
  parserActiveStativeSlotsForBundle,
  conditionMatches,
  generateTokenSlots
} = require_token_features()({
  normalizeLearnerLabel,
  cleanSlots
});
var {
  traceInfo,
  traceKind,
  isSurfaceSpecificTrace,
  sameNodeSequence,
  phraseMatch,
  flattenSurface,
  flattenDisplaySurface,
  firstToken,
  lastToken,
  isToken,
  isVerbLike,
  isObjectLike,
  isProductiveVo,
  isModalToken,
  isParticle,
  isStativeToken,
  isTopicCandidate,
  surfaceOf,
  hasSurface,
  indexOfSurface,
  hasConstruction
} = require_node_shape()({
  bundleCanFillStativeSlot
});
var {
  mergeUnique,
  constructionSlotsByType,
  nodeSlots,
  slotAlternatives,
  nodeCanFillSlot,
  isBareQuantityTokenObject,
  templateDerivedSlots
} = require_slot_primitives()({
  cleanSlots,
  bundleCanFillStativeSlot
});
var {
  parseTemplateSlot,
  templateConstraintsPass,
  matchTemplate
} = require_template_matcher()({
  firstToken,
  flattenSurface,
  isBareQuantityTokenObject,
  nodeCanFillSlot,
  nodeCanLicenseEvidenceGatedObject,
  nodeSlots
});
function attachSharedSubjectProvenanceToPurposePredicate(templateType, assignments, children) {
  if (templateType !== "SubjectPredicateClause") return children;
  const subjectIndex = assignments.findIndex((item) => item.slot === "subject");
  const predicateIndex = assignments.findIndex((item) => item.slot === "predicate");
  if (subjectIndex < 0 || predicateIndex < 0) return children;
  const subject = children[subjectIndex];
  const predicate = children[predicateIndex];
  if (!predicate || predicate.kind !== "construction" || !["SerialVerbPurposeChain", "MotionPurposeChain"].includes(predicate.type)) return children;
  if (predicate.trace && predicate.trace.shared_subject_provenance) return children;
  const enrichedPredicate = {
    ...predicate,
    trace: {
      ...predicate.trace || traceInfo("generative_template", { construction_type: predicate.type }),
      shared_subject_provenance: {
        overt_subject_surface: flattenSurface(subject),
        licensed_members: (predicate.children || []).map((node) => flattenSurface(node)),
        hidden_subject_inserted: false
      }
    }
  };
  return children.map((child, index) => index === predicateIndex ? enrichedPredicate : child);
}
function templateConstructionFor(nodes, allowedTypes = null) {
  for (const template of CONSTRUCTION_TEMPLATES) {
    if (allowedTypes && !allowedTypes.includes(template.type)) continue;
    const assignments = matchTemplate(nodes, template.template);
    if (assignments && templateConstraintsPass(assignments, template)) {
      let children = applyRoleOverrides(assignments, template);
      children = attachSharedSubjectProvenanceToPurposePredicate(template.type, assignments, children);
      const assignedSlots = assignments.map((item) => item.slot);
      const traceDetail = {
        construction_type: template.type,
        template_family: templateFamilyForDefinition(template),
        template: template.template,
        constraints: template.constraints || {},
        assigned_slots: assignedSlots,
        surfaces: children.map((node) => flattenSurface(node))
      };
      for (const key of TEMPLATE_TRACE_PASSTHROUGH_KEYS) {
        if (Object.prototype.hasOwnProperty.call(template, key)) traceDetail[key] = template[key];
      }
      const wrapperCoverage = assignedSlotWrapperCoverage(template.type, children, assignedSlots);
      if (wrapperCoverage) traceDetail.wrapper_coverage = wrapperCoverage;
      return construction(template.type, template.label, children, {
        note: `${template.note} Matched by generated slot affordances: ${assignedSlots.join(" → ")}.`,
        slots: template.output_slots ? cleanSlots(template.output_slots) : templateDerivedSlots(template.type, children),
        trace: traceInfo("generative_template", traceDetail)
      });
    }
  }
  return null;
}
function applyRoleOverrides(assignments, template = {}) {
  const overrides = template.role_overrides || {};
  const inactiveOverrides = template.child_parser_inactive_overrides || {};
  return assignments.map((item) => {
    const inactiveOverride = inactiveOverrides[item.slot];
    if (inactiveOverride && item.node && item.node.kind === "token") {
      return parserInactiveTokenClone(item.node, {
        label: inactiveOverride.label || item.node.label,
        pos: inactiveOverride.pos || void 0,
        syntax: inactiveOverride.syntax || item.node.syntax,
        slots: inactiveOverride.slots || void 0,
        reason: inactiveOverride.reason || `Token is parser-inactive inside ${template.type}; the parent exposes the construction affordance.`
      });
    }
    const override = overrides[item.slot];
    if (!override || !item.node || item.node.kind !== "token") return item.node;
    const overrideLabel = override.label || item.node.label;
    const overrideSyntax = override.syntax || item.node.syntax;
    const overrideEntry = { ...TOKEN_LEXICON[item.node.surface] || {}, label: overrideLabel, syntax: overrideSyntax };
    const overrideFeatures = inferTokenFeatures(item.node.surface, overrideEntry, {
      label: overrideLabel,
      syntax: overrideSyntax
    });
    const overrideSlots = override.slots ? cleanSlots(override.slots) : generateTokenSlots(overrideFeatures);
    const overrideFeatureBundle = featureBundleFor(item.node.surface, overrideEntry, overrideFeatures, overrideSlots);
    const traceDetail = {
      surface: item.node.surface,
      generated_slots: overrideSlots,
      feature_summary: compactFeatureSummary(overrideFeatures),
      feature_bundle: overrideFeatureBundle,
      contextual_role_override: template.type,
      original_role: item.node.label,
      contextual_role_affordance_resolution: {
        lexical_default_role: item.node.label || "",
        active_role: overrideLabel,
        candidate_affordances: contextualRoleAffordances({
          surface: item.node.surface,
          role: item.node.label || overrideLabel,
          label: item.node.label || overrideLabel,
          syntax: item.node.syntax || overrideSyntax,
          slots: item.node.slots || [],
          features: item.node.features || {}
        }),
        active_affordance_source: template.type,
        note: "Construction role override selects the active learner role without deleting other lexical affordances."
      }
    };
    if (item.node.trace && item.node.trace.selection_decision) {
      traceDetail.selection_decision = item.node.trace.selection_decision;
    }
    const overrideJyutping = override.jyutping_by_surface && override.jyutping_by_surface[item.node.surface] ? override.jyutping_by_surface[item.node.surface] : override.jyutping || item.node.jyutping;
    if (overrideJyutping && overrideJyutping !== item.node.jyutping) {
      traceDetail.contextual_jyutping_override = {
        lexical_default_jyutping: item.node.jyutping || "",
        active_jyutping: overrideJyutping,
        active_affordance_source: template.type,
        note: "Construction context selects the active pronunciation without deleting other lexical affordances."
      };
    }
    return token2(item.node.surface, {
      label: overrideLabel,
      syntax: overrideSyntax,
      slots: overrideSlots,
      jyutping: overrideJyutping,
      display_surface: item.node.display_surface || void 0,
      parser_surface: item.node.parser_surface || item.node.surface,
      note: override.note || item.node.note,
      review: item.node.review,
      trace: traceInfo("atomic_lexicon", traceDetail)
    });
  });
}
var environmentalClauseDetectors = null;
function conventionalEnvironmentalEventConstruction(nodes = []) {
  return environmentalClauseDetectors.conventionalEnvironmentalEventConstruction(nodes);
}
function categorySubspanFor(nodes, allowedTypes = null) {
  const conventionalEnvironmental = conventionalEnvironmentalEventConstruction(nodes);
  if (conventionalEnvironmental && (!allowedTypes || allowedTypes.includes("ImpersonalEnvironmentalClause"))) {
    return conventionalEnvironmental;
  }
  for (const template of CATEGORY_SPAN_TEMPLATES) {
    if (allowedTypes && !allowedTypes.includes(template.type)) continue;
    const assignments = matchTemplate(nodes, template.template);
    if (assignments && templateConstraintsPass(assignments, template)) {
      let children = applyRoleOverrides(assignments, template);
      children = attachSharedSubjectProvenanceToPurposePredicate(template.type, assignments, children);
      const matchingSlots = assignments.map((item) => item.slot);
      const traceKind2 = template.trace_kind || "generative_template";
      const assignedSlots = Object.prototype.hasOwnProperty.call(template, "trace_assigned_slots") ? cleanSlots(template.trace_assigned_slots || []) : matchingSlots;
      const traceDetail = {
        construction_type: template.type,
        template: template.template,
        constraints: template.constraints || {},
        assigned_slots: assignedSlots,
        surfaces: children.map((node) => flattenSurface(node)),
        role_overrides: template.role_overrides || {},
        subspan: true
      };
      if (traceKind2 === "generative_template" || traceKind2 === "construction_template") {
        traceDetail.template_family = templateFamilyForDefinition(template);
      }
      for (const key of TEMPLATE_TRACE_PASSTHROUGH_KEYS) {
        if (Object.prototype.hasOwnProperty.call(template, key)) traceDetail[key] = template[key];
      }
      const wrapperCoverage = assignedSlotWrapperCoverage(template.type, children, assignedSlots);
      if (wrapperCoverage) traceDetail.wrapper_coverage = wrapperCoverage;
      return construction(template.type, template.label, children, {
        note: `${template.note} Matched by generated category slots: ${matchingSlots.join(" → ")}.`,
        slots: template.output_slots ? cleanSlots(template.output_slots) : templateDerivedSlots(template.type, children),
        trace: traceInfo(traceKind2, traceDetail)
      });
    }
  }
  return null;
}
function shouldDeferPostverbalZoForFollowingComplement(candidate, nodes, index, length) {
  if (!candidate || candidate.type !== "PostverbalZoPerfectiveVP") return false;
  const next = nodes[index + length];
  if (!next) return false;
  return nodeCanFillSlot(next, "directional_motion_vp") || nodeCanFillSlot(next, "verb_complement") || nodeCanFillSlot(next, "result_complement") || nodeCanFillSlot(next, "directional_complement");
}
function shouldDeferActionStativeForDegreeMannerComplement(candidate, nodes, index, length) {
  if (!candidate || candidate.type !== "ActionStativeVP") return false;
  const next = nodes[index + length];
  if (!next) return false;
  return nodeCanFillSlot(next, "degree_particle") || flattenSurface(next) === "啲";
}
function shouldDeferTransitiveWhDeterminerObject(candidate, nodes, index, length) {
  if (!candidate || candidate.type !== "TransitiveVP") return false;
  const objectNode = candidate.children && candidate.children[1];
  if (!objectNode || !nodeCanFillSlot(objectNode, "direct_nominal_wh_determiner")) return false;
  const followingHead = nodes[index + length];
  if (!followingHead || !nodeCanFillSlot(followingHead, "head_noun")) return false;
  return true;
}
function shouldDeferCompletionForPerfectiveComposition(candidate, nodes, index, length) {
  if (!candidate || candidate.type !== "CompletionVP") return false;
  const assignedSlots = candidate.trace && Array.isArray(candidate.trace.assigned_slots) ? candidate.trace.assigned_slots : [];
  if (assignedSlots.includes("perfective_aspect") && assignedSlots.includes("object")) return true;
  const followingAspect = nodes[index + length];
  if (!followingAspect || !nodeCanFillSlot(followingAspect, "perfective_aspect")) return false;
  return nodes.slice(index + length + 1).some(
    (node) => node && node.kind !== "text" && !nodeCanFillSlot(node, "particle")
  );
}
function wrapCategorySubspansOnce(nodes) {
  const result = [];
  let i = 0;
  const maxWindow = 7;
  while (i < nodes.length) {
    let matched = null;
    const remaining = nodes.length - i;
    for (let length = Math.min(maxWindow, remaining); length >= 2; length--) {
      const window2 = nodes.slice(i, i + length);
      if (window2.some((node) => node.kind === "text")) continue;
      const candidate = categorySubspanFor(window2);
      if (candidate) {
        if (shouldDeferPostverbalZoForFollowingComplement(candidate, nodes, i, length)) continue;
        if (shouldDeferTransitiveWhDeterminerObject(candidate, nodes, i, length)) continue;
        if (shouldDeferCompletionForPerfectiveComposition(candidate, nodes, i, length)) continue;
        if (shouldDeferApproximateQuantityForUnlicensedGovernor(candidate, nodes, i)) continue;
        if (shouldDeferActionStativeForDegreeMannerComplement(candidate, nodes, i, length)) continue;
        if (candidate.type === "QuantifiedClassifierNP" && candidate.trace && candidate.trace.fragment_subtype === "quantified_classifier_head_ellipsis" && i + length < nodes.length) continue;
        matched = { node: candidate, length };
        break;
      }
    }
    if (matched) {
      result.push(matched.node);
      i += matched.length;
    } else {
      result.push(nodes[i]);
      i += 1;
    }
  }
  return result;
}
function wrapCategorySubspans(nodes) {
  let current = nodes;
  for (let pass = 0; pass < 4; pass++) {
    const next = wrapCategorySubspansOnce(current);
    if (sameNodeSequence(current, next)) return next;
    current = next;
  }
  return current;
}
var {
  pronunciationOnlyJyutpingForUnknown,
  token: token2,
  textNode,
  construction,
  parserInactiveTokenClone,
  learnerDisplayOnlyTokenClone,
  contextualLearnerRoleOnlyTokenClone
} = require_node_factories()({
  TOKEN_LEXICON,
  TOKEN_LEXICAL_ANALYSES,
  UNKNOWN_CJK_JYUTPING_FALLBACK,
  normalizeLearnerLabel,
  cleanSlots,
  contextualRoleAffordances,
  inferTokenFeatures,
  compactFeatureSummary,
  featureBundleFor,
  generateTokenSlots,
  traceInfo,
  traceKind,
  constructionSlotsByType,
  nodeParserSurface,
  nodeDisplaySurface,
  INTERNAL_CONSTRUCTION_COMPATIBILITY_ALIASES,
  INTERNAL_ONLY_CONSTRUCTION_SCOPES,
  internalConstructionTypeFor,
  clauseSpanProfileForCompatibilityType,
  npLicenseMetadata
});
var resultComplementModule = require_result();
var resultFramePartClone = resultComplementModule.createResultFramePartClone({
  firstToken,
  flattenSurface,
  parserInactiveTokenClone,
  token: token2
});
var createNpDetectors = require_core();
var {
  approximateQuantityFallback,
  classifierObjectNPFromNodes,
  compositionalNpSubspanFor,
  coordinatedNPFragmentFallback,
  coordinatedNPFromParts,
  deicticClassifierTopicFromParts,
  nominalComplementFromNodes,
  possessiveClassifierNPFromNodes,
  shouldDeferApproximateQuantityForUnlicensedGovernor,
  wrapCompositionalNpSubspans,
  wrapPossessiveClassifierNPSubspans
} = createNpDetectors({
  categorySubspanFor,
  cleanSlots,
  construction,
  firstToken,
  flattenSurface,
  hasSurface,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  parserInactiveTokenClone,
  resultFramePartClone,
  templateConstructionFor,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var {
  lexicalizedStativeRegistryKind,
  transparentClassifierObjectParts,
  shouldForceCompositional,
  ALL_LEXICON_TERMS,
  LEXICON_TERMS,
  LEXICALIZED_STATIVE_SELECTION_WEIGHT,
  lexicalCandidateScore,
  followingLexicalizedStativeAfterChoice,
  lexicalSelectionReason,
  lexicalSelectionDecision,
  selectionDecisionForSurface,
  selectLexiconTerm
} = require_lexical_selection()({
  TOKEN_LEXICON,
  PRODUCTIVE_VO,
  COMPOSITIONAL_LEXICAL_PHRASES,
  inferTokenFeatures,
  featureBundleFor,
  getLexicalizationType
});
var COMPOSITIONAL_NP_TYPES = /* @__PURE__ */ new Set([
  "NominalHeadSpan",
  "QuantifiedClassifierNP",
  "OvertHeadDemonstrativeClassifierNP",
  "DemonstrativeClassifierNP",
  "AssociativeNP",
  "CoordinatedNP"
]);
var PROVISIONAL_NP_TYPES = /* @__PURE__ */ new Set([
  "HeadlessDemonstrativeClassifierNP"
]);
function nodeContainsUnknownNominalMaterial(node) {
  if (!node) return true;
  if (node.kind === "text") return true;
  if (node.kind === "token") {
    const traceKind2 = node.trace && node.trace.kind || "";
    const syntax = String(node.syntax || "");
    return traceKind2 === "unknown_atomic" || /(?:^|[ _-])unknown(?:[ _-]|$)/iu.test(syntax) || /unknown_cjk_or_text/iu.test(syntax);
  }
  return (node.children || []).some(nodeContainsUnknownNominalMaterial);
}
var UNIT_WORD_EVIDENCE_MODEL = require_unit_word_evidence();
var LICENSED_CLASSIFIER_HEAD_RULES = require_classifier_head_rules();
var UNIT_WORD_SENSES_BY_ID = new Map(
  UNIT_WORD_EVIDENCE_MODEL.unit_word_senses.map((entry) => [entry.unit_word_sense_id, Object.freeze(entry)])
);
var UNIT_WORD_RULES_BY_SURFACE = new Map(
  UNIT_WORD_EVIDENCE_MODEL.noun_choice_rule_records.map((entry) => [entry.surface, Object.freeze(entry)])
);
function unitWordMetadataForSurface(surface) {
  const rule = UNIT_WORD_RULES_BY_SURFACE.get(String(surface || ""));
  if (!rule) return null;
  const sense = UNIT_WORD_SENSES_BY_ID.get(rule.unit_word_sense_id);
  if (!sense) return null;
  return {
    unit_word_evidence_model: UNIT_WORD_EVIDENCE_MODEL.schema,
    unit_word_evidence_version: UNIT_WORD_EVIDENCE_MODEL.version,
    unit_word_evidence_id: rule.evidence_id,
    unit_word_sense_id: sense.unit_word_sense_id,
    unit_word_type: sense.unit_word_type,
    semantic_unit_relation: sense.semantic_unit_relation,
    unit_word_provenance_state: sense.provenance_state,
    unit_word_source_scope: sense.source_scope,
    unit_word_construction_profiles: sense.construction_profiles.slice(),
    lexical_choice_status: rule.pair_status
  };
}
function lexicalClassifierClassesForNode(node) {
  if (!node) return [];
  if (node.kind === "token") {
    const entry = TOKEN_LEXICON[node.surface] || {};
    return Array.isArray(entry.classifier_classes) ? entry.classifier_classes.slice() : [];
  }
  const classes = /* @__PURE__ */ new Set();
  for (const child of node.children || []) {
    for (const value of lexicalClassifierClassesForNode(child)) classes.add(value);
  }
  return Array.from(classes);
}
function classifierHeadCompatibility(children = [], trace = {}) {
  const assigned = Array.isArray(trace.assigned_slots) ? trace.assigned_slots : [];
  const classifierIndex = assigned.indexOf("classifier");
  const headIndex = assigned.indexOf("head_noun");
  if (classifierIndex < 0 || headIndex < 0) return null;
  const classifier = children[classifierIndex];
  const head = children[headIndex];
  const classifierSurface = flattenSurface(classifier);
  const acceptedClasses = LICENSED_CLASSIFIER_HEAD_RULES[classifierSurface];
  const headClasses = lexicalClassifierClassesForNode(head);
  const unitWordMetadata = unitWordMetadataForSurface(classifierSurface);
  const evidenceMetadata = unitWordMetadata || {
    unit_word_evidence_model: UNIT_WORD_EVIDENCE_MODEL.schema,
    unit_word_evidence_version: UNIT_WORD_EVIDENCE_MODEL.version,
    lexical_choice_status: "unreviewed"
  };
  if (!acceptedClasses) {
    return {
      status: "unverified",
      classifier_surface: classifierSurface,
      head_surface: flattenSurface(head),
      observed_head_classes: headClasses,
      ...evidenceMetadata,
      downstream_argument_licensing: "blocked_by_conservative_policy",
      reason: "This unit word has no reviewed bounded runtime rule; its noun choice remains unreviewed and downstream consumption is blocked by conservative policy."
    };
  }
  const matchedClasses = acceptedClasses.filter((value) => headClasses.includes(value));
  if (matchedClasses.length) {
    return {
      status: "verified_compatible",
      classifier_surface: classifierSurface,
      head_surface: flattenSurface(head),
      matched_head_classes: matchedClasses,
      ...evidenceMetadata,
      downstream_argument_licensing: "allowed_by_bounded_runtime_rule",
      reason: "The pair matches a bounded runtime class and may license downstream structure; pair-level linguistic evidence remains separately unreviewed."
    };
  }
  if (!headClasses.length) {
    return {
      status: "unverified",
      classifier_surface: classifierSurface,
      head_surface: flattenSurface(head),
      expected_head_classes: acceptedClasses,
      ...evidenceMetadata,
      downstream_argument_licensing: "blocked_by_conservative_policy",
      reason: "The noun has no recorded compatibility class; the pair remains unreviewed and downstream consumption is blocked by conservative policy."
    };
  }
  return {
    status: "incompatible",
    classifier_surface: classifierSurface,
    head_surface: flattenSurface(head),
    expected_head_classes: acceptedClasses,
    observed_head_classes: headClasses,
    ...evidenceMetadata,
    downstream_argument_licensing: "blocked_by_conservative_policy",
    reason: "The pair fails the bounded runtime class allowlist; this is a parser-policy control, not a categorical Cantonese grammaticality judgment."
  };
}
function npLicenseMetadata(type, children = [], trace = {}) {
  if (!COMPOSITIONAL_NP_TYPES.has(type) && !PROVISIONAL_NP_TYPES.has(type)) return null;
  const surface = children.map((child) => flattenSurface(child)).join("");
  const headlessQuantified = type === "QuantifiedClassifierNP" && (trace.fragment_subtype === "quantified_classifier_head_ellipsis" || trace.np_subtype === "quantified_classifier_head_ellipsis" || Array.isArray(trace.missing_argument_slots) && trace.missing_argument_slots.includes("nominal_head"));
  if (PROVISIONAL_NP_TYPES.has(type) || headlessQuantified || nodeContainsUnknownNominalMaterial({ kind: "construction", children })) {
    return {
      np_license_status: "provisional_np_candidate",
      construction_licensing_allowed: false,
      structural_np_status: "structurally_incomplete_or_unknown",
      lexical_choice_status: "not_assessed",
      downstream_argument_licensing: "blocked_by_structural_or_unknown_material",
      np_license_reason: PROVISIONAL_NP_TYPES.has(type) || headlessQuantified ? "The NP lacks an overt nominal head; no hidden noun is reconstructed." : "At least one token is unknown or only pronunciation-backed."
    };
  }
  if (type === "QuantifiedClassifierNP" || type === "OvertHeadDemonstrativeClassifierNP" || type === "DemonstrativeClassifierNP") {
    const compatibility2 = classifierHeadCompatibility(children, trace);
    if (compatibility2 && compatibility2.status !== "verified_compatible") {
      return {
        np_license_status: "provisional_np_candidate",
        construction_licensing_allowed: false,
        structural_np_status: "visible_profile_complete",
        lexical_choice_status: compatibility2.lexical_choice_status,
        downstream_argument_licensing: compatibility2.downstream_argument_licensing,
        classifier_head_compatibility_status: compatibility2.status,
        classifier_head_compatibility: compatibility2,
        np_license_reason: compatibility2.reason
      };
    }
    if (compatibility2) {
      trace = {
        ...trace,
        structural_np_status: "visible_profile_complete",
        lexical_choice_status: compatibility2.lexical_choice_status,
        downstream_argument_licensing: compatibility2.downstream_argument_licensing,
        classifier_head_compatibility_status: compatibility2.status,
        classifier_head_compatibility: compatibility2
      };
    }
  }
  if (type === "AssociativeNP" && /^啲/u.test(surface)) {
    return {
      np_license_status: "ambiguous_licensed_np",
      construction_licensing_allowed: true,
      structural_np_status: "visible_profile_complete",
      lexical_choice_status: "not_applicable",
      downstream_argument_licensing: "allowed",
      np_attachment_ambiguity: ["啲 [A 嘅 N]", "[啲 A] 嘅 N"],
      np_license_reason: "The outer NP span is licensed, but internal 啲/嘅 attachment remains unresolved."
    };
  }
  const compatibility = type === "QuantifiedClassifierNP" || type === "OvertHeadDemonstrativeClassifierNP" || type === "DemonstrativeClassifierNP" ? classifierHeadCompatibility(children, trace) : null;
  return {
    np_license_status: "licensed_np",
    construction_licensing_allowed: true,
    structural_np_status: "visible_profile_complete",
    lexical_choice_status: compatibility ? compatibility.lexical_choice_status : "not_applicable",
    downstream_argument_licensing: compatibility ? compatibility.downstream_argument_licensing : "allowed",
    classifier_head_compatibility_status: compatibility ? compatibility.status : void 0,
    classifier_head_compatibility: compatibility || void 0,
    np_license_reason: compatibility ? compatibility.reason : "All required overt components are sufficiently analyzed under a reusable NP rule."
  };
}
function nodeNpLicenseStatus(node) {
  if (!node) return "invalid_or_incomplete_np";
  if (node.kind === "construction") {
    const trace = node.trace || {};
    if (trace.np_license_status) return trace.np_license_status;
    if (COMPOSITIONAL_NP_TYPES.has(node.type)) return nodeContainsUnknownNominalMaterial(node) ? "provisional_np_candidate" : "licensed_np";
    return "invalid_or_incomplete_np";
  }
  if (node.kind === "token") {
    if (nodeContainsUnknownNominalMaterial(node)) return "provisional_np_candidate";
    const slots = node.slots || [];
    if (slots.includes("head_noun") || slots.includes("np") || slots.includes("object")) return "licensed_np";
  }
  return "invalid_or_incomplete_np";
}
function nodeCanLicenseEvidenceGatedObject(node) {
  const status = nodeNpLicenseStatus(node);
  return status === "licensed_np" || status === "ambiguous_licensed_np";
}
function contextualReportedSpeechLearnerChildren(nodes) {
  const children = (nodes || []).map((node) => node);
  const surfaceAt = (index) => index >= 0 && index < children.length ? flattenSurface(children[index]) : "";
  for (let i = 0; i < children.length; i += 1) {
    const surface = surfaceAt(i);
    if (surface === "冇" && surfaceAt(i + 1) === "房") {
      children[i] = learnerDisplayOnlyTokenClone(children[i], {
        note: "not have / not own in this housing condition"
      });
      children[i + 1] = parserInactiveTokenClone(children[i + 1], {
        label: "what",
        pos: "noun",
        syntax: "possessed_housing_np residential_property_noun",
        slots: ["head_noun", "np", "object"],
        note: "house / home / residential property",
        reason: "After negated possession 冇 in this housing condition, 房 is the thing not possessed rather than a location adjunct.",
        active_affordance_match: "negated_possession_housing_object",
        trace_detail: {
          learner_gloss_lines: ["house / home / residential property", "The thing not owned or available in this housing condition."]
        }
      });
      continue;
    }
    if (surface === "冇" && surfaceAt(i + 1) === "所" && surfaceAt(i + 2) === "謂") {
      const note = "part of 冇所謂: it does not matter";
      children[i] = learnerDisplayOnlyTokenClone(children[i], { note });
      children[i + 1] = learnerDisplayOnlyTokenClone(children[i + 1], { note });
      children[i + 2] = learnerDisplayOnlyTokenClone(children[i + 2], { note });
      i += 2;
      continue;
    }
    if (surface === "阿" && surfaceAt(i + 1) === "媽") {
      children[i] = contextualLearnerRoleOnlyTokenClone(children[i], {
        label: "func",
        pos: "prefix",
        note: "familiar kinship/name prefix; part of 阿媽",
        reason: "Inside 阿媽, 阿 is a familiar kinship/name prefix rather than the person referent itself.",
        active_affordance_match: "kinship_prefix_part",
        trace_detail: {
          learner_gloss_lines: ["familiar kinship/name prefix", "Part of 阿媽; the person meaning is carried by 媽."]
        }
      });
      children[i + 1] = contextualLearnerRoleOnlyTokenClone(children[i + 1], {
        label: "who",
        pos: "noun",
        note: "mum / mother",
        reason: "Inside 阿媽, 媽 is the kinship noun that identifies the person.",
        active_affordance_match: "kinship_person_head",
        trace_detail: {
          learner_gloss_lines: ["mum / mother", "The person-denoting head of 阿媽."]
        }
      });
      i += 1;
      continue;
    }
    if (surface === "返" && surfaceAt(i + 1) === "工") {
      const note = "part of 返工: go to work / be at work";
      children[i] = learnerDisplayOnlyTokenClone(children[i], { note });
      children[i + 1] = learnerDisplayOnlyTokenClone(children[i + 1], { note });
      i += 1;
      continue;
    }
    if (surface === "收" && surfaceAt(i + 1) === "入") {
      const note = "part of 收入: income";
      children[i] = learnerDisplayOnlyTokenClone(children[i], { note });
      children[i + 1] = parserInactiveTokenClone(children[i + 1], {
        label: "neutral",
        pos: "noun",
        syntax: "lexical_expression_part income_expression",
        slots: [],
        note,
        reason: "Inside the observed word 收入, 入 is not being taught as an independent inward-motion verb.",
        active_affordance_match: "income_expression_part",
        trace_detail: {
          learner_gloss_lines: ["part of 收入: income", "Not an independent inward-movement word in this expression."]
        }
      });
      i += 1;
      continue;
    }
    if (surface === "超" && traceKind(children[i]) === "unknown_atomic") {
      children[i] = learnerDisplayOnlyTokenClone(children[i], {
        note: "exceed / go over a limit"
      });
      continue;
    }
    if (surface === "住" && traceKind(children[i]) === "unknown_atomic" && surfaceAt(i - 1) === "房") {
      children[i] = learnerDisplayOnlyTokenClone(children[i], {
        note: "likely live / stay here; exact grammatical use still needs review"
      });
    }
  }
  return children;
}
function contextualOpinionPlaceholderChildren(nodes) {
  return (nodes || []).map((node) => {
    if (!node || node.kind !== "token") return node;
    const surface = String(node.surface || "");
    if (traceKind(node) === "unknown_atomic" && /^[A-Z]$/.test(surface)) {
      return learnerDisplayOnlyTokenClone(node, {
        note: "placeholder for an unspecified word or phrase"
      });
    }
    return node;
  });
}
function phase4CognitionActiveTokenClone(node, overrides = {}) {
  if (!node || node.kind !== "token") return node;
  const surface = node.surface;
  const label = normalizeLearnerLabel(overrides.label || node.label || "doing", surface, overrides.syntax || node.syntax || "cognition_predicate");
  const baseSyntax = overrides.syntax || node.syntax || "cognition_predicate";
  const syntax = baseSyntax.includes("phase4_cognition_promotion") ? baseSyntax : `${baseSyntax} phase4_cognition_promotion`;
  const slots = cleanSlots(mergeUnique([
    ...overrides.slots || [],
    "cognition_predicate",
    "predicate"
  ]));
  const entry = { ...TOKEN_LEXICON[surface] || {}, label, syntax };
  const features = inferTokenFeatures(surface, entry, { label, syntax });
  const featureBundle = featureBundleFor(surface, entry, features, slots);
  return {
    ...node,
    label,
    role: label,
    syntax,
    slots,
    features,
    feature_bundle: featureBundle,
    trace: traceInfo("phase4_controlled_grammar_promotion", {
      surface,
      original_trace: traceKind(node),
      promotion: "controlled_cognition_predicate",
      reason: overrides.reason || "Phase 4 controlled grammar promotion: cognition predicate is parser-active only inside approved cognition constructions.",
      feature_bundle: featureBundle
    })
  };
}
function phase4DesiderativeActiveTokenClone(node, overrides = {}) {
  if (!node || node.kind !== "token") return node;
  const surface = node.surface;
  const label = normalizeLearnerLabel(overrides.label || node.label || "func", surface, overrides.syntax || node.syntax || "modal_desiderative");
  const baseSyntax = overrides.syntax || node.syntax || "modal_desiderative";
  const syntax = baseSyntax.includes("phase4_desiderative_promotion") ? baseSyntax : `${baseSyntax} phase4_desiderative_promotion`;
  const slots = cleanSlots(mergeUnique([
    ...overrides.slots || [],
    "desiderative_modal",
    "modal"
  ]));
  const entry = { ...TOKEN_LEXICON[surface] || {}, label, syntax };
  const features = inferTokenFeatures(surface, entry, { label, syntax });
  const featureBundle = featureBundleFor(surface, entry, features, slots);
  return {
    ...node,
    label,
    role: label,
    syntax,
    slots,
    features,
    feature_bundle: featureBundle,
    trace: traceInfo("phase4_controlled_grammar_promotion", {
      surface,
      original_trace: traceKind(node),
      promotion: "controlled_desiderative_a_not_a_modal",
      reason: overrides.reason || "Phase 4 controlled grammar promotion: desiderative modal is parser-active only inside approved 想唔想 + VP A-not-A questions.",
      feature_bundle: featureBundle
    })
  };
}
function phase4PermissionActiveTokenClone(node, overrides = {}) {
  if (!node || node.kind !== "token") return node;
  const surface = node.surface;
  const label = normalizeLearnerLabel(overrides.label || node.label || "func", surface, overrides.syntax || node.syntax || "modal_permission_or_ability");
  const baseSyntax = overrides.syntax || node.syntax || "modal_permission_or_ability";
  const syntax = baseSyntax.includes("phase4_permission_promotion") ? baseSyntax : `${baseSyntax} phase4_permission_promotion`;
  const slots = cleanSlots(mergeUnique([
    ...overrides.slots || [],
    "permission_modal",
    "modal"
  ]));
  const entry = { ...TOKEN_LEXICON[surface] || {}, label, syntax };
  const features = inferTokenFeatures(surface, entry, { label, syntax });
  const featureBundle = featureBundleFor(surface, entry, features, slots);
  return {
    ...node,
    label,
    role: label,
    syntax,
    slots,
    features,
    feature_bundle: featureBundle,
    trace: traceInfo("phase4_controlled_grammar_promotion", {
      surface,
      original_trace: traceKind(node),
      promotion: "controlled_permission_a_not_a_modal",
      reason: overrides.reason || "Phase 4 controlled grammar promotion: permission modal A-not-A is parser-active only inside approved 可唔可以 + VP questions.",
      feature_bundle: featureBundle
    })
  };
}
function productiveVoComponentTokens(surface) {
  const rule = PRODUCTIVE_VO_COMPONENT_RULES[surface];
  if (!rule) return null;
  if (surface === "煮嘢食") {
    return [
      token2("煮", { label: "doing", syntax: "verb" }),
      token2("嘢食", { label: "what", jyutping: "je5 sik6", syntax: "food_noun object", note: "food / things to eat; phrase-local object token avoids changing general 嘢 + 食 analysis." })
    ];
  }
  if (surface === "下棋") {
    return [
      token2("下", { label: "doing", jyutping: "haa5", syntax: "verb", note: "play / make a move in a board game; phrase-specific reading in 下棋." }),
      token2("棋", { label: "what", syntax: "object" })
    ];
  }
  return [
    token2(rule.verb, { label: "doing", syntax: "verb" }),
    token2(rule.object, { label: "what", syntax: "object" })
  ];
}
function phase4OpinionStanceActiveTokenClone(node, overrides = {}) {
  if (!node || node.kind !== "token") return node;
  const surface = node.surface;
  const label = normalizeLearnerLabel(overrides.label || node.label || "doing", surface, overrides.syntax || node.syntax || "cognition_opinion_predicate");
  const baseSyntax = overrides.syntax || node.syntax || "cognition_opinion_predicate";
  const syntax = baseSyntax.includes("phase4_opinion_stance_promotion") ? baseSyntax : `${baseSyntax} phase4_opinion_stance_promotion`;
  const slots = cleanSlots(mergeUnique([
    ...overrides.slots || [],
    "opinion_stance_frame",
    "stance_predicate",
    "cognition_predicate",
    "predicate"
  ]));
  const entry = { ...TOKEN_LEXICON[surface] || {}, label, syntax };
  const features = inferTokenFeatures(surface, entry, { label, syntax });
  const featureBundle = featureBundleFor(surface, entry, features, slots);
  return {
    ...node,
    label,
    role: label,
    syntax,
    slots,
    features,
    feature_bundle: featureBundle,
    trace: traceInfo("phase4_controlled_grammar_promotion", {
      surface,
      original_trace: traceKind(node),
      promotion: "controlled_opinion_stance_predicate",
      reason: overrides.reason || "Phase 4 controlled grammar promotion: opinion stance predicate is parser-active only inside approved 覺得 + visible content frames.",
      feature_bundle: featureBundle
    })
  };
}
function phase4ReportedSpeechActiveTokenClone(node, overrides = {}) {
  if (!node || node.kind !== "token") return node;
  const surface = node.surface;
  const label = normalizeLearnerLabel(overrides.label || node.label || "doing", surface, overrides.syntax || node.syntax || "speech_reporting_verb");
  const baseSyntax = overrides.syntax || node.syntax || "speech_reporting_verb";
  const syntax = baseSyntax.includes("phase4_reported_speech_promotion") ? baseSyntax : `${baseSyntax} phase4_reported_speech_promotion`;
  const slots = cleanSlots(mergeUnique([
    ...overrides.slots || [],
    "reported_speech",
    "speech_verb",
    "reportative_source",
    "predicate"
  ]));
  const entry = { ...TOKEN_LEXICON[surface] || {}, label, syntax };
  const features = inferTokenFeatures(surface, entry, { label, syntax });
  const featureBundle = featureBundleFor(surface, entry, features, slots);
  return {
    ...node,
    label,
    role: label,
    syntax,
    slots,
    features,
    feature_bundle: featureBundle,
    trace: traceInfo("phase4_controlled_grammar_promotion", {
      surface,
      original_trace: traceKind(node),
      promotion: "controlled_reported_speech_predicate",
      reason: overrides.reason || "Phase 4 controlled grammar promotion: 話 is parser-active only inside approved ReportedSpeech frames with visible reviewed content.",
      feature_bundle: featureBundle
    })
  };
}
var { wrapAgreementResponseSubspans } = require_protected_formulas()({
  construction,
  flattenSurface,
  isParticle,
  isToken,
  parserInactiveTokenClone,
  traceInfo
});
var createPriorityDetectors = require_priority();
var {
  priorityMarkerClauseWithTrailingParticle,
  sourceLinkedPriorityMarkerClauseFallback,
  wrapPriorityMarkerSubspans
} = createPriorityDetectors({
  categorySubspanFor,
  cleanSlots,
  construction,
  flattenSurface,
  isParticle,
  isToken,
  isVerbLike,
  nodeCanFillSlot,
  nodeSlots,
  parserInactiveTokenClone,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createBasicPredicateDetectors = require_basic();
var {
  scalarEvaluationFallback,
  wrapNegatedVPSubspans
} = createBasicPredicateDetectors({
  construction,
  flattenSurface,
  hasSurface,
  isToken,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo
});
var { wrapPredicate } = require_wrap_predicate()({
  categorySubspanFor,
  construction,
  isStativeToken,
  isToken,
  nodeCanFillSlot,
  traceInfo
});
var createAcceptabilityDetectors = require_clauses();
var { wrapPermissionAcceptabilitySubspans } = createAcceptabilityDetectors({
  cleanSlots,
  construction,
  firstToken,
  flattenSurface,
  isParticle,
  isToken,
  nodeSlots,
  parserInactiveTokenClone,
  templateDerivedSlots,
  token: token2,
  traceInfo
});
var createMannerAdjustmentDetectors = require_adjustment();
var {
  mannerAdverbialVPFallback,
  sourceLinkedDegreeMannerModifiedVPFallback
} = createMannerAdjustmentDetectors({
  applyConstructionPatterns,
  categorySubspanFor,
  cleanSlots,
  construction,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nodeSurfaceMatches,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createImperativeDetectors = require_clauses2();
var {
  politePathImperativeFallback,
  politeRequestAdjustmentFallback,
  prohibitiveImperativeFallback
} = createImperativeDetectors({
  categorySubspanFor,
  construction,
  directPredicateCapableNode,
  flattenSurface,
  isToken,
  mergeUnique,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  pathPhraseFromParts,
  templateDerivedSlots,
  traceInfo,
  transparentDiscourseFormulaFallback,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var NEGATED_LEXICALIZED_STATIVE_SPLITS = [
  { surface: "唔好食", predicate: "好食", verb: "食", meaning_hint: "not tasty" },
  { surface: "唔好飲", predicate: "好飲", verb: "飲", meaning_hint: "not good to drink" },
  { surface: "唔好睇", predicate: "好睇", verb: "睇", meaning_hint: "not good to watch / not good-looking" },
  { surface: "唔好聽", predicate: "好聽", verb: "聽", meaning_hint: "not pleasant to hear" },
  { surface: "唔好味", predicate: "好味", verb: "味", meaning_hint: "not tasty / not delicious" }
];
var PROHIBITIVE_OBJECT_STARTERS = [
  "呢個",
  "嗰個",
  "呢啲",
  "嗰啲",
  "呢杯",
  "嗰杯",
  "呢套",
  "嗰套",
  "呢首",
  "嗰首",
  "呢件",
  "嗰件",
  "呢間",
  "嗰間",
  "一",
  "兩",
  "三",
  "杯",
  "套",
  "首",
  "件",
  "飯",
  "嘢",
  "水",
  "茶",
  "書",
  "歌",
  "戲",
  "湯",
  "意粉",
  "蘋果",
  "餐廳"
];
var createNpTokenSplits = require_token_splits();
var {
  quantifiedPersonNPFromFusedNode,
  transparentCupNounDemonstrativeNpFromRest,
  transparentDeicticClassifierTopicFromNode,
  transparentDemonstrativeClassifierSplitFromRest,
  transparentEllipticalDemonstrativeClassifierFromRest,
  transparentNominalDiDeterminerFromRest,
  transparentOneCountClassifierSplitFromRest,
  transparentQuantifiedPersonNpFromRest
} = createNpTokenSplits({
  PUNCT_RE,
  cleanSlots,
  construction,
  flattenSurface,
  nodeCanFillSlot,
  phraseMatch,
  selectLexiconTerm,
  token: token2,
  traceInfo
});
var createContextualLexiconOverrides = require_contextual_overrides();
var createVocativeAddressDetector = require_vocative();
var {
  contextualLexiconOverrides,
  pushNegatedLexicalizedStativeSplit,
  pushSpecialNotGoodEat
} = createContextualLexiconOverrides({
  PROHIBITIVE_OBJECT_STARTERS,
  TOKEN_LEXICON,
  generateTokenSlots,
  inferTokenFeatures,
  isTopicCandidate,
  normalizeSurface,
  selectLexiconTerm,
  selectionDecisionForSurface,
  token: token2,
  traceInfo
});
var { candidateNamedAddressFormFromRest } = createVocativeAddressDetector({
  ADDRESS_FOLLOWERS,
  ADDRESS_PREFIXES,
  ADDRESS_SUFFIXES,
  COMMON_SURNAME_CHARS,
  PROTECTED_ADDRESS_TERMS,
  PUNCT_RE,
  categorySubspanFor,
  construction,
  parserInactiveTokenClone,
  phraseMatch,
  token: token2,
  traceInfo
});
var {
  hasSentencePunctuation,
  isClauseSequenceSeparator,
  isClauseSequenceTerminal
} = require_punctuation2();
var createClauseRelationMarkers = require_markers();
var {
  CLAUSE_LINKER_SURFACES,
  isTopicFrameLinker,
  isRelationalCoverbLinker,
  directWrapperItemSurface,
  clauseLinkingPivotIndex,
  clauseLinkerRole,
  clauseLinkerInventory,
  clauseLinkingWrapperCoverage,
  wrapperSlotDisplayRole,
  assignedSlotWrapperCoverage,
  wrapperCoverageForConstructionNode
} = createClauseRelationMarkers({
  flattenSurface,
  isClauseSequenceSeparator,
  learnerDisplaySlots,
  nodeSlots
});
var createConditionalClauseRelations = require_conditional();
var { protectedConditionalMarkerToken, conditionalGeWaaClauseFallback } = createConditionalClauseRelations({
  cleanSlots,
  construction,
  flattenSurface,
  isToken,
  nodeSlots,
  parserInactiveTokenClone,
  token: token2,
  traceInfo,
  withoutTrailingParticles
});
var { tokenizeLine } = require_tokenize_line()({
  FORMULAS,
  NEGATED_LEXICALIZED_STATIVE_SPLITS,
  PRODUCTIVE_TERMS,
  candidateNamedAddressFormFromRest,
  construction,
  contextualLexiconOverrides,
  productiveVoComponentTokens,
  protectedConditionalMarkerToken,
  pushNegatedLexicalizedStativeSplit,
  pushSpecialNotGoodEat,
  selectLexiconTerm,
  textNode,
  token: token2,
  traceInfo,
  transparentCupNounDemonstrativeNpFromRest,
  transparentDemonstrativeClassifierSplitFromRest,
  transparentEllipticalDemonstrativeClassifierFromRest,
  transparentNominalDiDeterminerFromRest,
  transparentOneCountClassifierSplitFromRest,
  transparentQuantifiedPersonNpFromRest
});
var wrapCoreImplementation = null;
var applyConstructionPatternsImplementation = null;
var optionalSubjectOffsetImplementation = null;
var withoutTrailingParticlesImplementation = null;
function applyConstructionPatterns(...args) {
  if (!applyConstructionPatternsImplementation) throw new Error("applyConstructionPatterns implementation is not initialized.");
  return applyConstructionPatternsImplementation(...args);
}
function optionalSubjectOffset(...args) {
  if (!optionalSubjectOffsetImplementation) throw new Error("optionalSubjectOffset implementation is not initialized.");
  return optionalSubjectOffsetImplementation(...args);
}
function withoutTrailingParticles(...args) {
  if (!withoutTrailingParticlesImplementation) throw new Error("withoutTrailingParticles implementation is not initialized.");
  return withoutTrailingParticlesImplementation(...args);
}
var constructionPatternOrchestration = require_apply_construction_patterns()({
  isParticle,
  nodeSlots,
  priorityMarkerClauseWithTrailingParticle: (...args) => priorityMarkerClauseWithTrailingParticle(...args),
  serialVerbPurposeChainWithTrailingParticle: (...args) => serialVerbPurposeChainWithTrailingParticle(...args),
  wrapCore: (...args) => {
    if (!wrapCoreImplementation) throw new Error("wrapCore implementation is not initialized.");
    return wrapCoreImplementation(...args);
  }
});
applyConstructionPatternsImplementation = constructionPatternOrchestration.applyConstructionPatterns;
optionalSubjectOffsetImplementation = constructionPatternOrchestration.optionalSubjectOffset;
withoutTrailingParticlesImplementation = constructionPatternOrchestration.withoutTrailingParticles;
var {
  possessiveFragmentAnswerCandidate,
  fragmentQuestionFallback
} = require_fragments()({
  cleanSlots,
  construction,
  firstToken,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nodeSurfaceMatches,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutTrailingParticles
});
var createANotAQuestionDetectors = require_a_not_a();
var {
  aNotAQuestionFallback,
  acceptabilityANotAQuestionFallback,
  copularANotAQuestionFallback,
  desiderativeANotAQuestionFallback,
  inlineANotAQuestionFallback,
  permissionANotAQuestionFallback,
  polarQuestionFrameFallback
} = createANotAQuestionDetectors({
  assignedSlotWrapperCoverage,
  applyConstructionPatterns,
  cleanSlots,
  construction,
  directPredicateCapableNode,
  firstToken,
  flattenSurface,
  isParticle,
  isToken,
  isVerbLike,
  nodeCanFillSlot,
  optionalSubjectOffset,
  parserInactiveTokenClone,
  phase4DesiderativeActiveTokenClone,
  phase4PermissionActiveTokenClone,
  possessiveFragmentAnswerCandidate,
  surfaceOf,
  templateDerivedSlots,
  token: token2,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles,
  wrapCategorySubspans,
  yesNoQuestionMarkerClone
});
var createWhScalarQuestionDetectors = require_wh_scalar();
var {
  existentialWhQuestionFallback,
  locativeWhQuestionFallback,
  progressiveWhObjectQuestionFallback,
  scalarDimensionQuestionFallbackForPunctuation,
  scalarValueQuestionFallback,
  suggestionQuestionFallback
} = createWhScalarQuestionDetectors({
  applyConstructionPatterns,
  construction,
  firstToken,
  flattenSurface,
  hasSurface,
  isToken,
  nodeCanFillSlot,
  surfaceOf,
  templateDerivedSlots,
  token: token2,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createCompletionExperientialQuestionDetectors = require_completion_experiential();
var {
  completionQuestionFallback,
  completionQuestionWithPerfectiveMarkerFallback,
  experientialQuestionBoundaryFallback,
  experientialYesNoQuestionFallback,
  interestDomainExistentialQuestionFallback
} = createCompletionExperientialQuestionDetectors({
  construction,
  firstToken,
  flattenSurface,
  hasConstruction,
  hasSurface,
  isParticle,
  isProductiveVo,
  isToken,
  nodeCanFillSlot,
  optionalSubjectOffset,
  traceInfo
});
var createQuestionFallbacks = require_question_fallbacks();
var { haveOrNotQuestionFallbackForPunctuation } = createQuestionFallbacks({
  applyConstructionPatterns,
  cleanSlots,
  construction,
  flattenNodes,
  flattenSurface,
  hasConstruction,
  isParticle,
  isToken,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo
});
var createCognitionDetectors = require_fragments_content();
var {
  cognitionContentFrameFallback,
  cognitionStatementFallback,
  negativeCognitionFragmentFallback
} = createCognitionDetectors({
  aNotAQuestionFallback,
  applyConstructionPatterns,
  completionQuestionFallback,
  construction,
  desiderativeANotAQuestionFallback,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nodeSurfaceMatches,
  optionalSubjectOffset,
  permissionANotAQuestionFallback,
  phase4CognitionActiveTokenClone,
  predicateOmissionProfileForHead,
  templateConstructionFor,
  templateDerivedSlots,
  token: token2,
  traceInfo,
  withoutTrailingParticles,
  wrapCategorySubspans
});
var createModalPredicateDetectors = require_modal_predicates();
var {
  coordinatedSubjectModalPredicateClauseFallback,
  modalPredicateWrapCoreFallback,
  modalVPFromNodes
} = createModalPredicateDetectors({
  categorySubspanFor,
  construction,
  coordinatedNPFromParts,
  firstToken,
  flattenSurface,
  isModalToken,
  nodeCanFillSlot,
  templateConstructionFor,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createAvailabilityDetectors = require_availability();
var { availabilityPredicateWrapCoreFallback } = createAvailabilityDetectors({
  construction,
  constructionSlotsByType,
  flattenSurface,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateConstructionFor,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createIntentionPreferenceDetectors = require_intention_preference();
var {
  desiderativeVPWrapCoreFallback,
  preferenceVPWrapCoreFallback,
  rawPreferenceTemplateFallback,
  sourceLinkedIntentionFrameFallback,
  sourceLinkedPreferenceVPFallback
} = createIntentionPreferenceDetectors({
  categorySubspanFor,
  construction,
  flattenSurface,
  hasSurface,
  isToken,
  nodeCanFillSlot,
  templateConstructionFor,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createOpinionDetectors = require_stance();
var { opinionSeemingFallback, opinionStanceFrameFallback } = createOpinionDetectors({
  applyConstructionPatterns,
  categorySubspanFor,
  cleanSlots,
  construction,
  contextualOpinionPlaceholderChildren,
  copulaClone,
  firstToken,
  flattenSurface,
  hasSurface,
  isToken,
  modalVPFromNodes,
  nodeCanFillSlot,
  nodeSlots,
  nominalComplementFromNodes,
  parserInactiveTokenClone,
  phase4OpinionStanceActiveTokenClone,
  predicateOmissionProfileForHead,
  subjectStativePredicateClauseFallback,
  templateConstructionFor,
  templateDerivedSlots,
  traceInfo,
  withoutTrailingParticles,
  wrapCategorySubspans
});
var createReportedSpeechDetectors = require_composition();
var { reportedSpeechFrameFallback, reportedSpeechSurfaceFallback } = createReportedSpeechDetectors({
  applyConstructionPatterns,
  construction,
  contextualReportedSpeechLearnerChildren,
  firstToken,
  flattenSurface,
  indexOfSurface,
  nodeCanFillSlot,
  optionalSubjectOffset,
  parserInactiveTokenClone,
  phase4ReportedSpeechActiveTokenClone,
  subjectStativePredicateClauseFallback,
  templateConstructionFor,
  templateDerivedSlots,
  traceInfo,
  withoutTrailingParticles,
  wrapCategorySubspans
});
function transparentTopicContentFromNodes(nodes) {
  const compact = withoutIgnorableSpaceText(nodes || []);
  if (!compact.length) return null;
  if (compact.length === 1) return compact[0];
  return categorySubspanFor(compact, ["OvertHeadDemonstrativeClassifierNP", "QuantifiedClassifierNP", "QuantifiedPersonNP", "OrdinalClassifierNP", "DiMarkedNP", "ModifiedNP", "NominalHeadSpan", "CoordinatedNP"]);
}
var createPotentialResultDetectors = require_potential_result();
var { potentialResultComplementFallback, potentialResultVPFallback, incompletePotentialResultCandidate } = createPotentialResultDetectors({
  categorySubspanFor,
  classifierObjectNPFromNodes,
  cleanSlots,
  construction,
  flattenSurface,
  isToken,
  isVerbLike,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutTrailingParticles
});
function subjectStativePredicateClauseFallback(nodes) {
  const { core: bareCore, particles } = withoutTrailingParticles(nodes);
  if (bareCore.length !== 2) return null;
  const subject = bareCore[0];
  const predicate = bareCore[1];
  if (!nodeCanFillSlot(subject, "subject")) return null;
  if (!nodeCanFillSlot(predicate, "stative_predicate")) return null;
  if (nodeCanFillSlot(subject, "opinion_topic") && !nodeCanFillSlot(subject, "co_participant")) return null;
  const children = [subject, predicate, ...particles];
  return construction("SubjectPredicateClause", "SubjPred", children, {
    note: "Phase 4 controlled broad subject-predicate clause with stative predicate subtype. Keeps topic-comment evaluations and bare ambiguity cases separate.",
    slots: cleanSlots(["subject_predicate_clause", "subject", "predicate", "clause", "stative_predicate", "comment", "comment_predicate"]),
    trace: traceInfo("generative_template", {
      construction_type: "SubjectPredicateClause",
      retired_label_alias: "SubjectStativePredicateClause",
      template_family: "generative_template",
      predicate_subtype: "stative",
      rule: "subject + stative_predicate + particle?",
      template: ["subject!", "stative_predicate!", "particle?"],
      assigned_slots: ["subject", "stative_predicate", ...particles.map(() => "particle")],
      reason: "Promote only simple subject-led stative clauses under the broad SubjectPredicateClause category. Topic/comment food evaluations remain TopicComment; bare 唔好食 remains NeedsContext.",
      surfaces: children.map((node) => flattenSurface(node))
    })
  });
}
function isIgnorableSpaceText(node) {
  return node && node.kind === "text" && !hasSentencePunctuation(node.text) && !normalizeSurface(node.text);
}
function withoutIgnorableSpaceText(nodes) {
  return (nodes || []).filter((node) => !isIgnorableSpaceText(node));
}
function directNodeHasAnySlot(node, slots = []) {
  const actual = nodeSlots(node);
  return slots.some((slot) => actual.includes(slot));
}
function directPredicateCapableNode(node) {
  return directNodeHasAnySlot(node, ["predicate", "main_verb", "action_verb", "vp", "action_vp", "productive_vo", "directional_motion_vp", "modal_vp"]);
}
function nameTokenClone(node) {
  return parserInactiveTokenClone(node, {
    label: (firstToken(node) || node).label || "who",
    pos: "np",
    syntax: "proper_name name_np",
    slots: ["name", "head_noun", "np", "object", "topic"],
    reason: "The final NP is interpreted as the visible name in a bounded self-introduction frame."
  });
}
var {
  protectedOpaqueFormulaPassthrough,
  repeatedNegatedExistentialResponseForPunctuation,
  boundedAcknowledgementRepetitionForPunctuation,
  transparentDiscourseFormulaFallback,
  leaveTakingFormulaFallback
} = require_formula_responses()({
  cleanSlots,
  construction,
  flattenSurface,
  isClauseSequenceSeparator,
  isClauseSequenceTerminal,
  isParticle,
  isToken,
  mergeUnique,
  nodeCanFillSlot,
  nodeSlots,
  parserInactiveTokenClone,
  templateDerivedSlots,
  token: token2,
  traceInfo,
  traceKind,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var motionPathGoalSourceDetectors = null;
function pathPhraseFromParts(marker, path) {
  return motionPathGoalSourceDetectors.pathPhraseFromParts(marker, path);
}
function yesNoQuestionMarkerClone(node) {
  return parserInactiveTokenClone(node, {
    label: "func",
    pos: "function",
    syntax: "yes_no_question_marker polar_question_marker",
    slots: ["yes_no_question_marker", "question_marker"],
    reason: "係咪 is interpreted as the yes/no question marker inside a bounded polar-question frame."
  });
}
function copulaClone(node, syntax, extraSlots, reason) {
  return parserInactiveTokenClone(node, {
    label: "func",
    pos: "function",
    syntax,
    slots: ["copula", ...extraSlots || []],
    reason
  });
}
var createExistentialClauseDetectors = require_clauses3();
var { interiorExistentialFrameFallback } = createExistentialClauseDetectors({
  categorySubspanFor,
  construction,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var {
  resultTopicFromNodes,
  resultComplementFromNodes,
  makeChangeIntoPredicate,
  wrapChangeIntoPredicateSubspans
} = resultComplementModule.createResultComplementDetectors({
  categorySubspanFor,
  construction,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nominalComplementFromNodes,
  resultFramePartClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText
});
function bridgeFramePartClone(node, overrides = {}) {
  const surface = overrides.surface || flattenSurface(node);
  const base = firstToken(node) || token2(surface);
  return parserInactiveTokenClone(base, {
    label: overrides.label || base.label || "func",
    pos: overrides.pos || (overrides.label === "doing" ? "verb" : overrides.label === "particle" ? "particle" : overrides.label === "who" ? "np" : overrides.label === "what" ? "noun" : "function"),
    syntax: overrides.syntax || base.syntax || "bridge_frame_part",
    slots: overrides.slots || [],
    note: Object.prototype.hasOwnProperty.call(overrides, "note") ? overrides.note : base.note,
    trace_detail: overrides.trace_detail || void 0,
    reason: overrides.reason || "Token is parser-inactive inside a bounded v0.5.35 controlled clause-bridge frame; the parent exposes the clause relation while children remain visible."
  });
}
function bridgeNPFromNodes(nodes) {
  const compact = withoutIgnorableSpaceText(nodes || []);
  if (!compact.length) return null;
  const classifierObject = classifierObjectNPFromNodes(compact);
  if (classifierObject) return classifierObject;
  return resultComplementFromNodes(compact) || resultTopicFromNodes(compact) || nominalComplementFromNodes(compact);
}
var createLegacyRecipientDetectors = require_legacy_recipient();
var {
  benefactiveRecipientVPFallback,
  recipientFrameFallback,
  transferDitransitiveVPFallback,
  transferPredicateFromNodes
} = createLegacyRecipientDetectors({
  bridgeFramePartClone,
  bridgeNPFromNodes,
  categorySubspanFor,
  cleanSlots,
  construction,
  firstToken,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  rawNodeHasSlot,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createLexicalGiveDetectors = require_lexical_give();
var {
  cp021bArgumentSpan,
  cp021bIsBei2Marker,
  cp021bNodeIsPersonEvidence,
  cp021bSpanIsPersonNP,
  cp021bSpanIsThingNP,
  lexicalGiveRelationFallback
} = createLexicalGiveDetectors({
  bridgeFramePartClone,
  categorySubspanFor,
  classifierObjectNPFromNodes,
  cleanSlots,
  construction,
  firstToken,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createPostThemeDetectors = require_post_theme();
var {
  CP021B_POST_THEME_PREDICATE_PROFILES,
  cp021bBoundaryReviewFallback,
  cp021bMakePostThemeRelation,
  postThemeParticipantRelationFallback
} = createPostThemeDetectors({
  bridgeFramePartClone,
  cleanSlots,
  construction,
  cp020NodeIsPredicateEvidence: (...args) => cp020NodeIsPredicateEvidence(...args),
  cp021bArgumentSpan,
  cp021bIsBei2Marker,
  cp021bSpanIsPersonNP,
  cp021bSpanIsThingNP,
  firstToken,
  flattenSurface,
  isToken,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createPassivePermissiveDetectors = require_passive_permissive();
var {
  cp020NodeIsPersonEvidence,
  cp020NodeIsPredicateEvidence,
  passivePermissiveRelationFallback
} = createPassivePermissiveDetectors({
  bridgeFramePartClone,
  categorySubspanFor,
  cleanSlots,
  construction,
  cp021bNodeIsPersonEvidence,
  firstToken,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
function rawNodeHasSlot(node, slot) {
  return nodeSlots(node).includes(slot);
}
var createCoverbPlaceDetectors = require_coverb_place();
var {
  locativeCoverbPhraseFromNodes,
  locativePredicatePhraseFromNodes,
  subjectLocativePredicateClauseFallback
} = createCoverbPlaceDetectors({
  bridgeFramePartClone,
  cleanSlots,
  construction,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createLocativePostureDetectors = require_posture();
var {
  locativePostureVPFallback
} = createLocativePostureDetectors({
  bridgeFramePartClone,
  cleanSlots,
  construction,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createCoverbDetectors = require_frames();
var {
  coverbFrameFallback
} = createCoverbDetectors({
  bridgeFramePartClone,
  bridgeNPFromNodes,
  categorySubspanFor,
  construction,
  flattenSurface,
  isToken,
  locativeCoverbPhraseFromNodes,
  nodeCanFillSlot,
  nodeSlots,
  parserInactiveTokenClone,
  rawNodeHasSlot,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createDirectionalMotionDetectors = require_directional();
var {
  directionalCompositionFallback,
  downwardMotionPredicateFallback,
  transitionMotionPredicateFallback,
  wrapDirectionalMotionSubspans
} = createDirectionalMotionDetectors({
  categorySubspanFor,
  compositionPartClone,
  construction,
  constructionSlotsByType,
  firstToken,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  token: token2,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createMotionPurposeDetectors = require_purpose_chains();
var {
  purposeLinkingMotionFallback,
  serialVerbPurposeChainWithTrailingParticle,
  wrapSerialPurposeTemplateSubspans,
  wrapSerialVerbPurposeSubspans
} = createMotionPurposeDetectors({
  categorySubspanFor,
  compositionPartClone,
  construction,
  constructionSlotsByType,
  firstToken,
  flattenSurface,
  isParticle,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  parserInactiveTokenClone,
  token: token2,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createMotionOrderingReviewDetectors = require_ordering_review();
var { motionOrderingReviewCandidate } = createMotionOrderingReviewDetectors({
  cleanSlots,
  construction,
  flattenSurface,
  traceInfo
});
var createMotionPathGoalSourceDetectors = require_path_goal_source();
motionPathGoalSourceDetectors = createMotionPathGoalSourceDetectors({
  applyConstructionPatterns,
  categorySubspanFor,
  cleanSlots,
  construction,
  constructionSlotsByType,
  CP021B_POST_THEME_PREDICATE_PROFILES,
  cp021bMakePostThemeRelation,
  firstToken,
  flattenSurface,
  motionOrderingReviewCandidate,
  nodeCanFillSlot,
  nodeSurfaceMatches,
  parserInactiveTokenClone,
  templateDerivedSlots,
  token: token2,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var { motionEventSpatialFallback, sourceMotionClauseFallback } = motionPathGoalSourceDetectors;
var {
  bareNumeralObjectMalformedCandidate,
  existentialQuestionWithVpMalformedCandidate,
  overtObjectSelectionReviewCandidate
} = require_malformed()({
  cleanSlots,
  construction,
  firstToken,
  flattenNodes,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  parserInactiveTokenClone,
  productiveObjectHeadToken: (...args) => productiveObjectHeadToken(...args),
  tokenSemanticDomains,
  topicChainAntecedentCompatibility: (...args) => topicChainAntecedentCompatibility(...args),
  traceInfo,
  withoutIgnorableSpaceText
});
var {
  ambiguousNeedsContextCandidate,
  mandarinNegatorNeedsContextCandidate,
  incompleteProhibitiveNeedsContextCandidate,
  incompleteRestrictiveFocusBoundaryCandidate,
  predicateOmissionCandidate,
  incompleteModalNeedsContextCandidate,
  incompleteContextualPredicateCandidate,
  incompleteLocativeNeedsContextCandidate,
  mandarinReviewNeedsContextCandidate
} = require_needs_context()({
  cleanSlots,
  construction,
  firstToken,
  flattenDisplaySurface,
  flattenSurface,
  isModalToken,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  nodeSurfaceMatches,
  parserInactiveTokenClone,
  phase4CognitionActiveTokenClone,
  predicateOmissionProfileForHead,
  templateDerivedSlots,
  tokenSemanticDomains,
  traceInfo,
  withoutTrailingParticles
});
function nodeSurfaceMatches(node, surfaces) {
  const t = firstToken(node);
  const values = [
    flattenSurface(node),
    flattenDisplaySurface(node),
    t && t.surface,
    t && t.display_surface
  ].filter(Boolean);
  return values.some((value) => surfaces.includes(value));
}
function tokenSemanticDomains(node) {
  const tokenNode = firstToken(node);
  const bundle = tokenNode && (tokenNode.feature_bundle || tokenNode.trace && tokenNode.trace.feature_bundle);
  return bundle && bundle.parser_features && Array.isArray(bundle.parser_features.semantic_domain) ? bundle.parser_features.semantic_domain : [];
}
var createTopicChainLinkage = require_linkage();
var {
  applyTopicChainNullObjectLinkage,
  productiveObjectHeadToken,
  topicChainAntecedentCompatibility
} = createTopicChainLinkage({
  clauseSequenceSegments: (...args) => clauseSequenceSegments(...args),
  flattenNodes,
  flattenSurface,
  getConstructionAffordances,
  isClauseSequenceSeparator,
  isRelationalCoverbLinker,
  isTopicFrameLinker,
  nodeCanFillSlot,
  tokenSemanticDomains,
  withoutIgnorableSpaceText
});
function compositionPartClone(node, overrides = {}) {
  const surface = flattenSurface(node);
  const base = firstToken(node) || token2(surface);
  return parserInactiveTokenClone(base, {
    label: overrides.label || base.label || "func",
    pos: overrides.pos || (overrides.label === "doing" ? "verb" : overrides.label === "what" ? "noun" : overrides.label === "measure_word" ? "classifier" : "function"),
    syntax: overrides.syntax || base.syntax || "composition_part",
    slots: overrides.slots || [],
    reason: overrides.reason || "Visible token is owned by a transparent aspect/result/directional composition; no hidden material is inserted."
  });
}
var createAspectCompositionDetectors = require_composition2();
var {
  durativeAspectCompositionFallback,
  perfectiveResultCompositionFallback,
  restorativeRepetitiveComplementFallback,
  incompatibleAspectCompositionMalformedCandidate
} = createAspectCompositionDetectors({
  classifierObjectNPFromNodes,
  cleanSlots,
  compositionPartClone,
  construction,
  constructionSlotsByType,
  firstToken,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createEnvironmentalClauseDetectors = require_clauses4();
environmentalClauseDetectors = createEnvironmentalClauseDetectors({
  ENVIRONMENTAL_EVENT_PREDICATES,
  categorySubspanFor: (...args) => categorySubspanFor(...args),
  construction,
  constructionSlotsByType,
  firstToken,
  flattenSurface,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var { impersonalEnvironmentalClauseFallback, temporalClauseFallback } = environmentalClauseDetectors;
var createExistentialSpatialDetectors = require_spatial();
var { existentialLocationPresentationalFallback } = createExistentialSpatialDetectors({
  applyConstructionPatterns,
  cleanSlots,
  construction,
  flattenSurface,
  fullSpanSingleConstruction,
  isToken,
  locativePredicatePhraseFromNodes,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createNominalPredicationDetectors = require_clauses5();
var { nominalMeasurePredicateFallback } = createNominalPredicationDetectors({
  categorySubspanFor,
  cleanSlots,
  construction,
  flattenSurface,
  isToken,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  stringIncludesAny,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createPostverbalZoDetectors = require_postverbal_zo();
var {
  postverbalZoPerfectiveFromRawNodes,
  postverbalZoPerfectiveFromWrappedNodes
} = createPostverbalZoDetectors({
  cleanSlots,
  compositionalNpSubspanFor,
  construction,
  flattenSurface,
  nodeCanFillSlot,
  nodeCanLicenseEvidenceGatedObject,
  nodeNpLicenseStatus,
  templateConstructionFor,
  traceInfo,
  withoutIgnorableSpaceText
});
var createDefinitionCopularDetectors = require_copular_relations();
var {
  copularExplanatoryCompositionFallback,
  copularIdentificationFrameFallback,
  isDefinitionCopulaNode
} = createDefinitionCopularDetectors({
  construction,
  coordinatedNPFromParts,
  copulaClone,
  deicticClassifierTopicFromParts,
  firstToken,
  flattenSurface,
  isParticle,
  isToken,
  nodeCanFillSlot,
  nodeSlots,
  nominalComplementFromNodes,
  parserInactiveTokenClone,
  templateDerivedSlots,
  token: token2,
  traceInfo,
  transparentDeicticClassifierTopicFromNode,
  transparentTopicContentFromNodes,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createIntendedFunctionDetectors = require_intended_function();
var { intendedFunctionRelationFallback } = createIntendedFunctionDetectors({
  construction,
  firstToken,
  flattenSurface,
  isDefinitionCopulaNode,
  isModalToken,
  isToken,
  isVerbLike,
  nodeCanFillSlot,
  nodeSlots,
  parserInactiveTokenClone,
  traceInfo,
  transparentTopicContentFromNodes,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createNamingDetectors = require_self_introduction();
var { namingSelfIntroductionFrameFallback } = createNamingDetectors({
  construction,
  firstToken,
  flattenSurface,
  isToken,
  nameTokenClone,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText,
  withoutTrailingParticles
});
var createClauseRelationGraph = require_graph();
var {
  connectorAwareClauseLinkingForTerminal,
  completionThenClauseRelation,
  standaloneClauseRelationEdgeFragmentForTerminal,
  clauseRelationSurfaceIndex,
  clauseRelationParsedChunk,
  clauseSequenceSegments,
  wrapClauseSequenceByPunctuation,
  shouldCollapseClauseSequenceForDisplay,
  shouldCollapseGreedyWrapperForDisplay
} = createClauseRelationGraph({
  CLAUSE_LINKER_SURFACES,
  CLAUSE_RELATION_SUBTYPE_REGISTRY,
  applyConstructionPatterns,
  applyTopicChainNullObjectLinkage,
  clauseLinkerInventory,
  clauseLinkingWrapperCoverage,
  wrapperCoverageForConstructionNode,
  cleanSlots,
  cognitionContentFrameFallback,
  construction,
  firstToken,
  flattenNodes,
  flattenSurface,
  hasConstruction,
  isClauseSequenceSeparator,
  isClauseSequenceTerminal,
  learnerDisplaySlots,
  nodeCanFillSlot,
  nodeSlots,
  opinionStanceFrameFallback,
  parserInactiveTokenClone,
  reportedSpeechFrameFallback,
  surfaceOf,
  token: token2,
  traceInfo,
  withoutIgnorableSpaceText
});
var createRelativeClauseDetectors = require_clauses6();
var { relativeClauseNPForTerminal } = createRelativeClauseDetectors({
  applyConstructionPatterns,
  clauseRelationParsedChunk,
  clauseRelationSurfaceIndex,
  cleanSlots,
  construction,
  cp021bIsBei2Marker,
  flattenSurface,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  traceInfo,
  withoutIgnorableSpaceText
});
var { topicCommentFallback } = require_topic_comment()({
  cleanSlots,
  construction,
  isTopicCandidate,
  templateDerivedSlots,
  traceInfo,
  wrapPredicate
});
function fullSpanSingleConstruction(nodes, sourceNodes) {
  if (!Array.isArray(nodes) || nodes.length !== 1) return null;
  const only = nodes[0];
  if (!only || only.kind !== "construction") return null;
  const expectedSurface = (sourceNodes || []).map((node) => flattenSurface(node)).join("");
  return flattenSurface(only) === expectedSurface ? only : null;
}
var createFinalParticleQuestions = require_final_particles();
var { finalMePolarQuestionFallbackForPunctuation } = createFinalParticleQuestions({
  applyConstructionPatterns,
  construction,
  flattenSurface,
  fullSpanSingleConstruction,
  isToken,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText
});
var createTerminalDiscourseParticles = require_discourse();
var {
  completionThenStandaloneWalkResolution,
  propositionLikeHostForScopedDiscourseParticle,
  scopedChangeStateParticleFallback,
  scopedDirectiveClosureParticleFallback,
  scopedEpistemicDiscourseParticleFallback,
  scopedEvidentialDiscourseParticleFallback
} = createTerminalDiscourseParticles({
  applyConstructionPatterns,
  cleanSlots,
  construction,
  flattenSurface,
  fullSpanSingleConstruction,
  hasSurface,
  isToken,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText
});
var createTerminalFocusParticles = require_focus();
var { restrictiveFocusParticleFallback } = createTerminalFocusParticles({
  applyConstructionPatterns,
  construction,
  flattenSurface,
  fullSpanSingleConstruction,
  isToken,
  nodeCanFillSlot,
  parserInactiveTokenClone,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText
});
var createOrderedParticleClusters = require_ordered_cluster();
var {
  orderedParticleClusterFallback,
  orderedParticleClusterInfo,
  orderedParticleClusterTailInfo
} = createOrderedParticleClusters({
  construction,
  flattenSurface,
  hasSentencePunctuation,
  parserInactiveTokenClone,
  propositionLikeHostForScopedDiscourseParticle,
  templateDerivedSlots,
  traceInfo,
  withoutIgnorableSpaceText
});
var {
  sequenceEvidence: ORDERED_PARTICLE_CLUSTER_SEQUENCE_EVIDENCE,
  descriptors: ORDERED_PARTICLE_CLUSTER_DESCRIPTORS
} = require_ordered_particle_clusters();
wrapCoreImplementation = require_wrap_core()({
  aNotAQuestionFallback,
  acceptabilityANotAQuestionFallback,
  availabilityPredicateWrapCoreFallback,
  ambiguousNeedsContextCandidate,
  approximateQuantityFallback,
  bareNumeralObjectMalformedCandidate,
  completionQuestionFallback,
  completionQuestionWithPerfectiveMarkerFallback,
  completionThenClauseRelation,
  completionThenStandaloneWalkResolution,
  conditionalGeWaaClauseFallback,
  coordinatedNPFragmentFallback,
  coordinatedSubjectModalPredicateClauseFallback,
  copularANotAQuestionFallback,
  copularExplanatoryCompositionFallback,
  copularIdentificationFrameFallback,
  cognitionContentFrameFallback,
  cognitionStatementFallback,
  coverbFrameFallback,
  cp021bBoundaryReviewFallback,
  desiderativeANotAQuestionFallback,
  desiderativeVPWrapCoreFallback,
  directionalCompositionFallback,
  downwardMotionPredicateFallback,
  durativeAspectCompositionFallback,
  existentialLocationPresentationalFallback,
  existentialQuestionWithVpMalformedCandidate,
  existentialWhQuestionFallback,
  experientialQuestionBoundaryFallback,
  experientialYesNoQuestionFallback,
  fragmentQuestionFallback,
  hasSurface,
  impersonalEnvironmentalClauseFallback,
  inlineANotAQuestionFallback,
  incompatibleAspectCompositionMalformedCandidate,
  incompleteContextualPredicateCandidate,
  incompleteLocativeNeedsContextCandidate,
  incompleteModalNeedsContextCandidate,
  incompletePotentialResultCandidate,
  incompleteProhibitiveNeedsContextCandidate,
  incompleteRestrictiveFocusBoundaryCandidate,
  intendedFunctionRelationFallback,
  interiorExistentialFrameFallback,
  interestDomainExistentialQuestionFallback,
  leaveTakingFormulaFallback,
  lexicalGiveRelationFallback,
  locativePostureVPFallback,
  locativeWhQuestionFallback,
  mandarinNegatorNeedsContextCandidate,
  mandarinReviewNeedsContextCandidate,
  mannerAdverbialVPFallback,
  modalPredicateWrapCoreFallback,
  motionEventSpatialFallback,
  namingSelfIntroductionFrameFallback,
  negativeCognitionFragmentFallback,
  nominalMeasurePredicateFallback,
  opinionSeemingFallback,
  opinionStanceFrameFallback,
  overtObjectSelectionReviewCandidate,
  passivePermissiveRelationFallback,
  perfectiveResultCompositionFallback,
  permissionANotAQuestionFallback,
  politePathImperativeFallback,
  politeRequestAdjustmentFallback,
  polarQuestionFrameFallback,
  possessiveFragmentAnswerCandidate,
  postThemeParticipantRelationFallback,
  postverbalZoPerfectiveFromRawNodes,
  postverbalZoPerfectiveFromWrappedNodes,
  potentialResultComplementFallback,
  potentialResultVPFallback,
  predicateOmissionCandidate,
  preferenceVPWrapCoreFallback,
  progressiveWhObjectQuestionFallback,
  prohibitiveImperativeFallback,
  protectedOpaqueFormulaPassthrough,
  purposeLinkingMotionFallback,
  rawPreferenceTemplateFallback,
  reportedSpeechFrameFallback,
  reportedSpeechSurfaceFallback,
  restorativeRepetitiveComplementFallback,
  scalarEvaluationFallback,
  scalarValueQuestionFallback,
  sourceLinkedDegreeMannerModifiedVPFallback,
  sourceLinkedIntentionFrameFallback,
  sourceLinkedPreferenceVPFallback,
  sourceLinkedPriorityMarkerClauseFallback,
  sourceMotionClauseFallback,
  subjectLocativePredicateClauseFallback,
  subjectStativePredicateClauseFallback,
  suggestionQuestionFallback,
  templateConstructionFor,
  temporalClauseFallback,
  topicCommentFallback,
  transitionMotionPredicateFallback,
  transparentDiscourseFormulaFallback,
  wrapAgreementResponseSubspans,
  wrapCategorySubspans,
  wrapChangeIntoPredicateSubspans,
  wrapDirectionalMotionSubspans,
  wrapNegatedVPSubspans,
  wrapPermissionAcceptabilitySubspans,
  wrapPossessiveClassifierNPSubspans,
  wrapPredicate,
  wrapPriorityMarkerSubspans,
  wrapSerialPurposeTemplateSubspans,
  wrapSerialVerbPurposeSubspans
}).wrapCore;
var {
  applyConstructionPatternsByPunctuation,
  applyConstructionPatternsForTerminal
} = require_apply_terminal_patterns()({
  applyConstructionPatterns,
  boundedAcknowledgementRepetitionForPunctuation,
  connectorAwareClauseLinkingForTerminal,
  finalMePolarQuestionFallbackForPunctuation,
  hasSentencePunctuation,
  haveOrNotQuestionFallbackForPunctuation,
  orderedParticleClusterFallback,
  orderedParticleClusterInfo,
  relativeClauseNPForTerminal,
  repeatedNegatedExistentialResponseForPunctuation,
  restrictiveFocusParticleFallback,
  scalarDimensionQuestionFallbackForPunctuation,
  scopedChangeStateParticleFallback,
  scopedDirectiveClosureParticleFallback,
  scopedEpistemicDiscourseParticleFallback,
  scopedEvidentialDiscourseParticleFallback,
  standaloneClauseRelationEdgeFragmentForTerminal,
  wrapClauseSequenceByPunctuation
});
var contextDescriptors = require_descriptors()({
  firstToken,
  flattenNodes,
  flattenSurface,
  getConstructionAffordances,
  nodeCanFillSlot,
  normalizeSurface,
  PREDICATE_OMISSION_PROFILES,
  predicateOmissionProfileForHead,
  predicateOmissionProfileForQuestionForm,
  predicateProfilesCompatible,
  tokenSemanticDomains
});
var {
  tokenRowsForAnalysis,
  questionDescriptorForContextTurn,
  targetDescriptorForContext,
  compatibleContextQuestion,
  existentialQuestionDescriptorForContextTurn,
  positiveResponseDiscourseAntecedentDescriptor,
  existentialDomainDescriptorForContextTurn,
  eventDomainDescriptorForContextTurn,
  opinionContextSupportsMissingSlot,
  quantifiedClassifierEllipsisDescriptor,
  contextSupportsQuantifiedClassifierFragment,
  conventionalZiDurationDescriptor,
  conventionalZiContextDomain,
  contextSupportsQuantifiedTimeFragment
} = contextDescriptors;
var analyzeLineImplementation = null;
function analyzeLine(...args) {
  if (!analyzeLineImplementation) throw new Error("analyzeLine implementation is not initialized.");
  return analyzeLineImplementation(...args);
}
var {
  explicitContextTurns,
  analyzedExplicitContext
} = require_turns()({
  analyzeLine,
  diagnosticSummary,
  normalizeSurface
});
var licensedContextFragments = require_licensed_fragments()({
  PREDICATE_OMISSION_PROFILES,
  applyRoleOverrides,
  cleanSlots,
  construction,
  contextSupportsQuantifiedClassifierFragment,
  contextSupportsQuantifiedTimeFragment,
  conventionalZiContextDomain,
  conventionalZiDurationDescriptor,
  eventDomainDescriptorForContextTurn,
  existentialDomainDescriptorForContextTurn,
  existentialQuestionDescriptorForContextTurn,
  firstToken,
  flattenNodes,
  flattenSurface,
  hasSentencePunctuation,
  isToken,
  nodeCanFillSlot,
  normalizeSurface,
  opinionContextSupportsMissingSlot,
  parserInactiveTokenClone,
  positiveResponseDiscourseAntecedentDescriptor,
  predicateOmissionProfileForHead,
  predicateProfilesCompatible,
  quantifiedClassifierEllipsisDescriptor,
  questionDescriptorForContextTurn,
  templateDerivedSlots,
  tokenRowsForAnalysis,
  traceInfo,
  withoutIgnorableSpaceText
});
var {
  splitTerminalContextNodes,
  contextRequiredTrace,
  needsContextAroundExisting,
  fragmentChildrenFromStructural,
  licensedFragmentAnswer,
  licensedContextFragmentQuestion,
  licensedContextNegatedExistentialFragment,
  contextualPositiveExistentialAcknowledgementRepetition,
  licensedContextHaveOrNotEventQuestion,
  licensedContextEllipticalExistentialQuestion,
  licensedContextShortExperientialQuestion,
  typedContextDependentFragmentBoundary,
  saturatedCompletionBoundary,
  licensedContextOpinionStanceFrame,
  licensedContextConventionalCognitionStatement,
  licensedContextStancePredicateAnswer,
  contextualQuantifiedClassifierNPBoundary,
  conventionalZiDurationConstruction,
  contextualConventionalZiDurationBoundary,
  licensedContextQuantifiedTimeNP
} = licensedContextFragments;
var {
  sentenceContextGroups,
  applyExplicitContextContract
} = require_apply_context_contract()({
  compatibleContextQuestion,
  contextRequiredTrace,
  contextualConventionalZiDurationBoundary,
  contextualPositiveExistentialAcknowledgementRepetition,
  contextualQuantifiedClassifierNPBoundary,
  flattenNodes,
  flattenSurface,
  hasSentencePunctuation,
  licensedContextConventionalCognitionStatement,
  licensedContextEllipticalExistentialQuestion,
  licensedContextFragmentQuestion,
  licensedContextHaveOrNotEventQuestion,
  licensedContextNegatedExistentialFragment,
  licensedContextOpinionStanceFrame,
  licensedContextQuantifiedTimeNP,
  licensedContextShortExperientialQuestion,
  licensedContextStancePredicateAnswer,
  licensedFragmentAnswer,
  needsContextAroundExisting,
  saturatedCompletionBoundary,
  splitTerminalContextNodes,
  targetDescriptorForContext,
  typedContextDependentFragmentBoundary,
  withoutIgnorableSpaceText
});
analyzeLineImplementation = require_analyze_line()({
  analyzedExplicitContext,
  annotateRawDisplaySurfaces,
  applyConstructionPatternsByPunctuation,
  applyExplicitContextContract,
  normalizeInputForParser,
  normalizeSurface,
  tokenizeLine
}).analyzeLine;
function flattenNodes(nodes) {
  const out = [];
  const visit = (node, depth = 0, parent = "", parentCompatibilityAlias = "") => {
    if (!node) return;
    if (node.kind === "text") {
      out.push({ kind: "text", surface: node.text, display_surface: node.display_text || node.text, parser_surface: node.text, depth, parent, parent_compatibility_alias: parentCompatibilityAlias, trace: node.trace || {} });
      return;
    }
    if (node.kind === "token") {
      out.push({
        kind: "token",
        surface: node.surface,
        display_surface: node.display_surface || node.surface,
        parser_surface: node.parser_surface || node.surface,
        depth,
        parent,
        parent_compatibility_alias: parentCompatibilityAlias,
        role: node.label,
        syntax: node.syntax,
        slots: node.slots || [],
        jyutping: node.jyutping || "",
        features: node.features || {},
        feature_bundle: node.feature_bundle || node.trace && node.trace.feature_bundle || void 0,
        trace: node.trace || {}
      });
      return;
    }
    if (node.kind === "construction") {
      out.push({
        kind: "construction",
        surface: flattenSurface(node),
        display_surface: flattenDisplaySurface(node),
        parser_surface: flattenSurface(node),
        depth,
        parent,
        parent_compatibility_alias: parentCompatibilityAlias,
        type: node.type,
        compatibility_alias: node.compatibility_alias || "",
        internal_representation_scope: node.internal_representation_scope || "",
        internal_only: Boolean(node.internal_only),
        label: node.label,
        slots: node.slots || [],
        trace: node.trace || {}
      });
      for (const child of node.children || []) visit(child, depth + 1, node.type, node.compatibility_alias || INTERNAL_CONSTRUCTION_COMPATIBILITY_ALIASES[node.type] || "");
    }
  };
  for (const node of nodes || []) visit(node, 0, "");
  return out;
}
function traceCounts(rows) {
  const counts = {};
  for (const row of rows) {
    const kind = row.trace && row.trace.kind ? row.trace.kind : "unspecified";
    counts[kind] = (counts[kind] || 0) + 1;
  }
  return counts;
}
function templateFamilyCounts(rows) {
  const counts = {};
  for (const row of rows) {
    const family = row.trace && row.trace.template_family ? row.trace.template_family : "";
    if (!family) continue;
    counts[family] = (counts[family] || 0) + 1;
  }
  return counts;
}
function diagnosticLevel(kind) {
  if (kind === "generative_template") return "good";
  if (kind === "atomic_lexicon" || kind === "unknown_atomic") return "info";
  if (kind === "generative_or_heuristic_slot_rule" || kind === "predicate_heuristic") return "ok";
  if (kind === "governed_discourse_wrapper") return "allowed";
  if (kind === "protected_formula_table") return "allowed";
  if (kind === "surface_specific_phrase_rule" || kind === "legacy_surface_rule" || kind === "special_ambiguity_rule") return "review";
  return "info";
}
function labelTransitionTracePolicy(row) {
  const trace = row && row.trace ? row.trace : {};
  const kind = trace.kind || "unspecified";
  if (kind === "generative_template" && trace.template_family === "construction_template") {
    return LABEL_TRANSITION_KIND_POLICY.construction_template;
  }
  if (kind === "generative_template" && trace.template_family === "generative_template") {
    return LABEL_TRANSITION_KIND_POLICY.generative_template;
  }
  return LABEL_TRANSITION_KIND_POLICY[kind] || {
    bucket: "unclassified_trace_kind",
    status: "needs_registry_decision",
    priority: 5,
    action: "Classify this trace kind in the label-transition audit policy before promoting it."
  };
}
function wrapperCoverageAuditRows(analysis) {
  const rows = [];
  const visit = (node, depth = 0, parent = "") => {
    if (!node) return;
    if (node.kind === "construction") {
      const trace = node.trace || {};
      const coverage = wrapperCoverageForConstructionNode(node);
      if (coverage) {
        const row = {
          surface: flattenSurface(node),
          construction: node.type,
          label: node.label,
          depth,
          parent,
          trace_kind: trace.kind || "unspecified",
          coverage_kind: coverage.coverage_kind || (node.type === "ClauseRelationGraph" ? "clause_linking_wrapper" : "assigned_slot_wrapper"),
          status: coverage.status || "PASS",
          policy: coverage.policy || "Wrapper coverage must explicitly account for every direct item inside the wrapper.",
          unaccounted_tokens: coverage.unaccounted_tokens || [],
          unaccounted_wrapper_token_count: coverage.unaccounted_wrapper_token_count || 0
        };
        if (coverage.accounted_children) row.accounted_children = coverage.accounted_children;
        if (coverage.accounted_linkers) row.accounted_linkers = coverage.accounted_linkers;
        if (coverage.accounted_separators) row.accounted_separators = coverage.accounted_separators;
        if (coverage.accounted_parts) row.accounted_parts = coverage.accounted_parts;
        rows.push(row);
      }
      for (const child of node.children || []) visit(child, depth + 1, node.type);
    }
  };
  for (const node of analysis.nodes || []) visit(node, 0, "");
  return rows;
}
function wrapperCoverageAuditSummary(analysis) {
  const rows = wrapperCoverageAuditRows(analysis);
  const unaccountedWrapperTokenCount = rows.reduce((sum, row) => sum + (row.unaccounted_wrapper_token_count || 0), 0);
  const warningRows = rows.filter((row) => row.status === "WARN" || (row.unaccounted_wrapper_token_count || 0) > 0);
  return {
    status: warningRows.length ? "WARN" : "PASS",
    policy: "Wrapper coverage audit: ClauseRelationGraph must expose child/linker/separator coverage, and ModalANotAQuestion must expose direct assigned-slot coverage before it may be visually collapsed.",
    wrapper_row_count: rows.length,
    unaccounted_wrapper_token_count: unaccountedWrapperTokenCount,
    warning_rows: warningRows
  };
}
function labelTransitionAuditRows(analysis) {
  const finalRows = flattenNodes(analysis.nodes).filter((row) => row.kind === "construction");
  return finalRows.map((row) => {
    const trace = row.trace || {};
    const policy = labelTransitionTracePolicy(row);
    const detail = {
      surface: row.surface,
      construction: row.type,
      label: row.label,
      depth: row.depth,
      parent: row.parent,
      trace_kind: trace.kind || "unspecified",
      template_family: trace.template_family || "",
      transition_bucket: policy.bucket,
      transition_status: policy.status,
      transition_priority: policy.priority,
      recommended_action: policy.action
    };
    if (trace.rule) detail.rule = trace.rule;
    if (trace.reason) detail.reason = trace.reason;
    if (trace.surface) detail.trace_surface = trace.surface;
    if (trace.construction_type) detail.trace_construction_type = trace.construction_type;
    if (Array.isArray(trace.template)) detail.template = trace.template;
    if (trace.constraints && Object.keys(trace.constraints).length) detail.constraints = trace.constraints;
    if (Array.isArray(trace.assigned_slots)) detail.assigned_slots = trace.assigned_slots;
    if (trace.wrapper_coverage) detail.wrapper_coverage = trace.wrapper_coverage;
    return detail;
  });
}
function labelTransitionAuditSummary(analysis) {
  const rows = labelTransitionAuditRows(analysis);
  const countByBucket = {};
  const countByStatus = {};
  for (const row of rows) {
    countByBucket[row.transition_bucket] = (countByBucket[row.transition_bucket] || 0) + 1;
    countByStatus[row.transition_status] = (countByStatus[row.transition_status] || 0) + 1;
  }
  const migrationStatuses = /* @__PURE__ */ new Set(["migration_candidate"]);
  const migrationCandidates = rows.filter((row) => migrationStatuses.has(row.transition_status)).sort((a, b) => a.transition_priority - b.transition_priority || String(a.construction).localeCompare(String(b.construction)));
  const unknownRows = rows.filter((row) => row.transition_status === "needs_registry_decision");
  return {
    status: unknownRows.length ? "WARN" : "PASS",
    policy: "Diagnostic inventory separating accepted runtime architectures from actionable migration debt. Declarative templates are preferred where they cleanly fit; governed specialized implementations may remain first-class. PASS means every construction trace was classified; migration candidates may still remain.",
    construction_row_count: rows.length,
    accepted_template_count: (countByStatus.accepted_reusable_template || 0) + (countByStatus.accepted_bounded_template || 0),
    accepted_specialized_count: (countByStatus.accepted_specialized_implementation || 0) + (countByStatus.accepted_guardrail || 0) + (countByStatus.accepted_closed_table || 0) + (countByStatus.accepted_internal_support || 0),
    transition_review_count: countByStatus.transition_review || 0,
    already_generative_count: countByStatus.accepted_reusable_template || 0,
    migration_candidate_count: migrationCandidates.length,
    reviewed_table_or_guardrail_count: (countByStatus.accepted_specialized_implementation || 0) + (countByStatus.accepted_guardrail || 0) + (countByStatus.accepted_closed_table || 0),
    needs_registry_decision_count: unknownRows.length,
    transition_bucket_counts: countByBucket,
    transition_status_counts: countByStatus,
    recommended_next_promotions: migrationCandidates.slice(0, 10),
    unclassified_trace_rows: unknownRows
  };
}
function runtimeConstructionRegistryAuditRows(analysis) {
  return flattenNodes(analysis.nodes).filter((row) => row.kind === "construction").map((row) => ({
    surface: row.surface || flattenSurface(row),
    construction: row.type || "",
    label: row.label || "",
    depth: row.depth || 0,
    parent: row.parent || "",
    ...runtimeConstructionStateFor(row.type)
  }));
}
function runtimeConstructionRegistryAuditSummary(analysis) {
  const rows = runtimeConstructionRegistryAuditRows(analysis);
  const unregisteredRows = rows.filter((row) => row.registry_missing || !row.active);
  return {
    status: unregisteredRows.length ? "FAIL" : "PASS",
    registry_version: RUNTIME_CONSTRUCTION_REGISTRY_VERSION,
    policy: "Runtime audit only: every emitted construction label must be present in the active label registry. Linguistic status and evidence are validated from grammar/<linguistic-status>/*.md outside the plugin.",
    construction_row_count: rows.length,
    unique_construction_count: new Set(rows.map((row) => row.construction)).size,
    active_row_count: rows.length - unregisteredRows.length,
    unregistered_row_count: unregisteredRows.length,
    unregistered_rows: unregisteredRows
  };
}
function nonPassRuntimeConstructionRegistryRows(analysis) {
  return runtimeConstructionRegistryAuditRows(analysis).filter((row) => row.registry_missing || !row.active);
}
function topChildConstructionsForDiagnostic(constructionRows) {
  const internalTopConstructions = constructionRows.filter((row) => row.depth === 0).map((row) => row.type);
  if (!internalTopConstructions.includes("ClauseSequence") && !internalTopConstructions.includes("ClauseRelationGraph")) return [];
  return constructionRows.filter((row) => row.depth === 1 && (row.parent === "ClauseSequence" || row.parent === "ClauseRelationGraph")).map((row) => diagnosticCompatibilityConstructionType(row.type)).filter(Boolean);
}
function isIgnorableRootRemainderForDiagnostic(row) {
  if (!row) return true;
  if (row.kind === "text") {
    const surface = String(row.surface || row.text || "");
    return !surface.trim() || /^[\p{P}\p{S}\s]+$/u.test(surface);
  }
  if (row.kind !== "token") return false;
  const role = String(row.role || row.label || "");
  const syntax = String(row.syntax || "");
  return role === "particle" || /(^|\s)(?:sentence_final_(?:question_)?particle|discourse_particle|clause_final_particle)(\s|$)/.test(syntax);
}
function rootSpanCoverageForDiagnostic(finalRows) {
  const topLevelRows = (finalRows || []).filter((row) => row.depth === 0);
  const topLevelParticleRows = topLevelRows.filter((row) => {
    if (!row || row.kind !== "token") return false;
    const role = String(row.role || row.label || "");
    const syntax = String(row.syntax || "");
    return role === "particle" || /(^|\s)(?:sentence_final_(?:question_)?particle|discourse_particle|clause_final_particle)(\s|$)/.test(syntax);
  });
  const hasUnlicensedTopLevelParticleCluster = topLevelParticleRows.length >= 2;
  const meaningfulRows = topLevelRows.filter((row) => {
    if (hasUnlicensedTopLevelParticleCluster && topLevelParticleRows.includes(row)) return true;
    return !isIgnorableRootRemainderForDiagnostic(row);
  });
  const constructionRows = meaningfulRows.filter((row) => row.kind === "construction");
  const unwrappedRows = meaningfulRows.filter((row) => row.kind !== "construction");
  let status = "PASS";
  if (!constructionRows.length && unwrappedRows.length) status = "NO_TOP_CONSTRUCTION";
  else if (constructionRows.length && unwrappedRows.length) status = "PARTIAL";
  else if (constructionRows.length > 1) status = "MULTIPLE_ROOT_CONSTRUCTIONS";
  return {
    status,
    top_construction_count: constructionRows.length,
    top_construction_surfaces: constructionRows.map((row) => row.surface || ""),
    unwrapped_root_nonpunctuation_count: unwrappedRows.length,
    unwrapped_root_surfaces: unwrappedRows.map((row) => row.surface || row.text || ""),
    ignored_root_remainder_count: topLevelRows.length - meaningfulRows.length
  };
}
function rootSpanCoverageFieldsFromDiagnosticSummary(summary) {
  const source = summary || {};
  return {
    root_span_coverage_status: source.root_span_coverage_status || "PASS",
    root_top_construction_count: source.root_top_construction_count || 0,
    root_top_construction_surfaces: source.root_top_construction_surfaces || [],
    unwrapped_root_nonpunctuation_count: source.unwrapped_root_nonpunctuation_count || 0,
    unwrapped_root_surfaces: source.unwrapped_root_surfaces || [],
    ignored_root_remainder_count: source.ignored_root_remainder_count || 0
  };
}
function rootSpanCoverageObjectFromDiagnosticSummary(summary) {
  const fields = rootSpanCoverageFieldsFromDiagnosticSummary(summary);
  return {
    status: fields.root_span_coverage_status,
    top_construction_count: fields.root_top_construction_count,
    top_construction_surfaces: fields.root_top_construction_surfaces,
    unwrapped_root_nonpunctuation_count: fields.unwrapped_root_nonpunctuation_count,
    unwrapped_root_surfaces: fields.unwrapped_root_surfaces,
    ignored_root_remainder_count: fields.ignored_root_remainder_count
  };
}
var CP018_RETIRED_CONSTRUCTION_TYPES = /* @__PURE__ */ new Set([
  "ComitativeActionMotionVP",
  "ActionSourceFocusClause",
  "GoalDirectedActionPredicate",
  "ModalGoalBenefactivePurposeClause",
  "UseForPurposeFrame",
  "UseForPurposeVP",
  "UseForPurposePredicate"
]);
var CP018_DEBUNKED_SEMANTIC_CLAIM_TYPES = /* @__PURE__ */ new Set([
  "RecipientFrame",
  "TransferDitransitiveVP",
  "AffectednessFrame"
]);
var CP018_FRAGMENT_CONSTRUCTION_TYPES = /* @__PURE__ */ new Set([
  "ComplementEllipsisFragment",
  "FragmentAnswer",
  "FragmentQuestion",
  "NegativeCognitionFragment",
  "NegatedExistentialFragment"
]);
var CP018_RELATION_SENSITIVE_MARKERS = /* @__PURE__ */ new Set(["用", "畀", "俾", "同", "陪", "跟", "喺", "由", "對"]);
function semanticGuardComparisonSurface(value) {
  return String(value || "").replace(/[\s\u3000。！？?!，,、；;：:…—－（）()「」『』《》〈〉“”‘’"']/gu, "");
}
function semanticGuardNodePath(path, node) {
  const surface = flattenSurface(node);
  const label = node && node.kind === "construction" ? node.type : node && node.kind === "token" ? node.surface : node && node.kind;
  return [...path, `${label || "node"}${surface ? `[${surface}]` : ""}`];
}
function semanticGuardUnknownToken(node) {
  if (!node || node.kind !== "token") return false;
  const traceKind2 = node.trace && node.trace.kind || "";
  const syntax = String(node.syntax || "");
  const review = String(node.review || "");
  return traceKind2 === "unknown_atomic" || /(^|[ _-])unknown([ _-]|$)/i.test(syntax) || /unknown_cjk_or_text/i.test(syntax) || /unknown/i.test(review);
}
function semanticGuardTypedCoverbProvenance(node) {
  const trace = node && node.trace || {};
  return Boolean(trace.coverb_subtype || trace.relation_subtype || trace.typed_relation || trace.relation_provenance);
}
function semanticGuardGenericRelationMarker(node) {
  if (!node || node.kind !== "construction" || node.type !== "TransitiveVP") return "";
  const first = (node.children || [])[0];
  const surface = flattenSurface(first);
  for (const marker of CP018_RELATION_SENSITIVE_MARKERS) {
    if (surface === marker || surface.startsWith(marker)) return marker;
  }
  return "";
}
function semanticGuardLeafTokens(nodes) {
  const leaves = [];
  const walkLeaves = (node) => {
    if (!node) return;
    if (node.kind === "token") {
      leaves.push(node);
      return;
    }
    if (node.kind === "construction") {
      for (const child of node.children || []) walkLeaves(child);
    }
  };
  for (const node of nodes || []) walkLeaves(node);
  return leaves;
}
function semanticAcceptanceGuardForAnalysis(analysis, finalRows) {
  const blockers = [];
  const reviewReasons = [];
  const seenBlockers = /* @__PURE__ */ new Set();
  const seenReviews = /* @__PURE__ */ new Set();
  const contextResolution = analysis && analysis.context_resolution || {};
  const explicitContext = analysis && analysis.explicit_context || { supplied: false };
  const addBlocker = (code, node, path, detail = {}) => {
    const key = `${code}|${(path || []).join(" > ")}|${detail.surface || ""}`;
    if (seenBlockers.has(key)) return;
    seenBlockers.add(key);
    blockers.push({
      code,
      path: (path || []).join(" > "),
      construction: node && node.kind === "construction" ? node.type : "",
      surface: node ? flattenSurface(node) : "",
      depth: Math.max(0, (path || []).length - 1),
      ...detail
    });
  };
  const addReview = (code, node, path, detail = {}) => {
    const key = `${code}|${(path || []).join(" > ")}|${detail.surface || ""}`;
    if (seenReviews.has(key)) return;
    seenReviews.add(key);
    reviewReasons.push({
      code,
      path: (path || []).join(" > "),
      construction: node && node.kind === "construction" ? node.type : "",
      surface: node ? flattenSurface(node) : "",
      depth: Math.max(0, (path || []).length - 1),
      ...detail
    });
  };
  const walk = (node, path = []) => {
    if (!node) return;
    const nextPath = semanticGuardNodePath(path, node);
    if (node.kind === "token") {
      if (semanticGuardUnknownToken(node)) {
        addBlocker("unknown_lexical_material", node, nextPath, {
          syntax: node.syntax || "",
          trace_kind: node.trace && node.trace.kind || ""
        });
      } else if (/(^|\s)borrowed_verb(\s|$)/.test(String(node.syntax || ""))) {
        addReview("borrowed_or_code_switched_predicate_requires_manual_review", node, nextPath, {
          syntax: node.syntax || ""
        });
      }
      return;
    }
    if (node.kind !== "construction") return;
    if (CP018_RETIRED_CONSTRUCTION_TYPES.has(node.type)) {
      addBlocker("retired_construction_reachable", node, nextPath);
    }
    if (CP018_DEBUNKED_SEMANTIC_CLAIM_TYPES.has(node.type)) {
      addBlocker("debunked_language_claim_in_parse", node, nextPath, {
        disposition: "scheduled_for_narrow_replacement_or_retirement"
      });
    }
    if (node.type === "LexicalGiveRelation") {
      const profile = node.trace && node.trace.relation_profile || "";
      addReview(profile === "theme_recipient_baseline" ? "cp021b_provisional_lexical_give_relation" : "lexical_give_argument_order_unresolved", node, nextPath, {
        relation_profile: profile,
        evidence_basis: "CP021B-PD1 frozen lexical-GIVE design"
      });
    }
    if (node.type === "PostThemeParticipantRelation") {
      addReview("post_theme_participant_role_context_dependent", node, nextPath, {
        upstream_predicate_profile: node.trace && node.trace.upstream_predicate_profile || "",
        evidence_basis: "CP021B-PD1 frozen post-theme participant design"
      });
    }
    if (node.type === "CoverbFrame") {
      if (semanticGuardTypedCoverbProvenance(node)) {
        addReview("internal_coverb_umbrella_requires_relation_review", node, nextPath, {
          relation_subtype: node.trace && (node.trace.coverb_subtype || node.trace.relation_subtype) || ""
        });
      } else {
        addBlocker("coverb_frame_missing_typed_relation_provenance", node, nextPath);
      }
    }
    if (node.type === "NeedsContext") addBlocker("needs_context_construction", node, nextPath);
    if (node.type === "MalformedCandidate") addBlocker("malformed_candidate_construction", node, nextPath);
    if (CP018_FRAGMENT_CONSTRUCTION_TYPES.has(node.type)) {
      const licensed = explicitContext.supplied || ["context_licensed", "context_resolved"].includes(contextResolution.context_requirement_status) || contextResolution.antecedent_status === "linked";
      if (!licensed) addBlocker("context_dependent_fragment_without_licensed_context", node, nextPath);
      else addReview("context_licensed_fragment_requires_manual_review", node, nextPath);
    }
    const relationMarker = semanticGuardGenericRelationMarker(node);
    if (relationMarker) {
      addBlocker("relation_sensitive_marker_in_generic_transitive_analysis", node, nextPath, {
        relation_marker: relationMarker
      });
    }
    const traceKind2 = node.trace && node.trace.kind || "";
    if (traceKind2 === "legacy_surface_rule") {
      addReview("legacy_surface_rule_requires_semantic_review", node, nextPath, {
        trace_rule: node.trace && node.trace.rule || "",
        trace_reason: node.trace && node.trace.reason || "",
        disposition: "fallback_surface_analysis_not_semantically_validated"
      });
    }
    const traceFlags = node.trace && Array.isArray(node.trace.semantic_review_flags) ? node.trace.semantic_review_flags : [];
    if (traceFlags.length) {
      addReview("construction_trace_requires_semantic_review", node, nextPath, {
        trace_flags: traceFlags
      });
    }
    for (const child of node.children || []) walk(child, nextPath);
  };
  for (const node of analysis && analysis.nodes || []) walk(node, []);
  const sourceSurface = semanticGuardComparisonSurface(analysis && analysis.parser_shadow_source || analysis && analysis.source || "");
  if (/用[嚟來]$/u.test(sourceSurface) && !sourceSurface.includes("用來用去")) {
    addBlocker("incomplete_intended_function_linker_fragment", null, ["ROOT"], {
      surface: sourceSurface,
      evidence_basis: "CP015/CP019 incomplete-linker boundary"
    });
  }
  if (/^用[嚟來].+/u.test(sourceSurface) && !sourceSurface.startsWith("用來用去")) {
    addBlocker("intended_function_topic_or_resource_missing", null, ["ROOT"], {
      surface: sourceSurface,
      evidence_basis: "CP019 bare-linker fragment boundary"
    });
  }
  if (/^(?:我|你|佢|我哋|你哋|佢哋)用[嚟來].+/u.test(sourceSurface)) {
    addBlocker("intended_function_resource_missing_with_overt_user", null, ["ROOT"], {
      surface: sourceSurface,
      evidence_basis: "CP019 user-versus-function-topic boundary"
    });
  }
  const semanticLeafTokens = semanticGuardLeafTokens(analysis && analysis.nodes || []);
  const beiLeafIndex = semanticLeafTokens.findIndex((node, index) => index > 0 && ["畀", "俾"].includes(String(node && node.surface || "")));
  const hasPassivePermissiveRelation = (finalRows || []).some((row) => row && row.kind === "construction" && row.type === "PassivePermissiveRelation");
  if (beiLeafIndex > 0 && beiLeafIndex < semanticLeafTokens.length - 1 && !hasPassivePermissiveRelation) {
    const firstAfterBei = semanticLeafTokens[beiLeafIndex + 1];
    const firstAfterIsPerson = cp020NodeIsPersonEvidence(firstAfterBei);
    const firstAfterIsPredicate = cp020NodeIsPredicateEvidence(firstAfterBei);
    if (!firstAfterIsPerson && firstAfterIsPredicate) {
      addBlocker("passive_permissive_agent_missing_or_unresolved", null, ["ROOT"], {
        surface: sourceSurface,
        marker_surface: semanticLeafTokens[beiLeafIndex].surface || "",
        first_postmarker_surface: firstAfterBei && firstAfterBei.surface || "",
        evidence_basis: "CP014/CP020 agent-omitted passive-permissive boundary"
      });
    }
  }
  const cp021bBeiIndexes = semanticLeafTokens.map((node, index) => ["畀", "俾"].includes(String(node && node.surface || "")) ? index : -1).filter((index) => index >= 0);
  const hasLexicalGiveRelation = (finalRows || []).some((row) => row && row.kind === "construction" && row.type === "LexicalGiveRelation");
  const hasPostThemeParticipantRelation = (finalRows || []).some((row) => row && row.kind === "construction" && row.type === "PostThemeParticipantRelation");
  if (!hasLexicalGiveRelation && !hasPostThemeParticipantRelation && cp021bBeiIndexes.length >= 2) {
    addReview("lexical_give_double_marker_heavy_theme_unfrozen", null, ["ROOT"], {
      surface: sourceSurface,
      evidence_basis: "CP021B-PD1 heavy-theme double-marker boundary"
    });
  }
  if (!hasLexicalGiveRelation && !hasPostThemeParticipantRelation && cp021bBeiIndexes.length && sourceSurface.includes("將")) {
    addReview("lexical_give_zoeng_restructure_outside_design", null, ["ROOT"], {
      surface: sourceSurface,
      evidence_basis: "CP021B-PD1 zoeng restructuring boundary"
    });
  }
  if (!hasLexicalGiveRelation && !hasPostThemeParticipantRelation && !hasPassivePermissiveRelation && cp021bBeiIndexes.length === 1) {
    const beiIndex = cp021bBeiIndexes[0];
    const beforeBei = semanticLeafTokens.slice(0, beiIndex);
    const afterBei = semanticLeafTokens.slice(beiIndex + 1).filter((node) => !isToken(node, "咗") && !nodeCanFillSlot(node, "particle"));
    const twoPersonSplits = [];
    for (let index = 1; index < afterBei.length; index += 1) {
      if (cp021bSpanIsPersonNP(afterBei.slice(0, index)) && cp021bSpanIsPersonNP(afterBei.slice(index))) {
        twoPersonSplits.push(index);
      }
    }
    if (twoPersonSplits.length) {
      addBlocker("lexical_give_two_person_argument_roles_unresolved", null, ["ROOT"], {
        surface: sourceSurface,
        evidence_basis: "CP021B-PD1 two-person lexical-GIVE refusal"
      });
    } else if (!beforeBei.length && cp021bSpanIsPersonNP(afterBei)) {
      addBlocker("context_dependent_lexical_give_argument_omission", null, ["ROOT"], {
        surface: sourceSurface,
        omitted_local_arguments: ["subject", "theme"],
        evidence_basis: "CP021B-PD1 nonfull lexical-GIVE boundary"
      });
    } else if (cp021bSpanIsPersonNP(beforeBei) && cp021bSpanIsThingNP(afterBei)) {
      addBlocker("context_dependent_lexical_give_argument_omission", null, ["ROOT"], {
        surface: sourceSurface,
        omitted_local_arguments: ["recipient"],
        evidence_basis: "CP021B-PD1 nonfull lexical-GIVE boundary"
      });
    } else if (cp021bSpanIsPersonNP(afterBei)) {
      const frontedSplits = [];
      for (let index = 1; index < beforeBei.length; index += 1) {
        if (cp021bSpanIsThingNP(beforeBei.slice(0, index)) && cp021bSpanIsPersonNP(beforeBei.slice(index))) {
          frontedSplits.push(index);
        }
      }
      if (frontedSplits.length) {
        addReview("lexical_give_fronted_theme_outside_design", null, ["ROOT"], {
          surface: sourceSurface,
          evidence_basis: "CP021B-PD1 fronted-theme boundary"
        });
      }
    }
  }
  if (sourceSurface.includes("約埋")) {
    addReview("retired_invitation_bridge_surface_requires_reanalysis", null, ["ROOT"], {
      surface: sourceSurface,
      evidence_basis: "CP016 retired bridge family"
    });
  }
  if (sourceSurface.includes("掉落")) {
    addReview("retired_goal_bridge_surface_requires_lexical_path_review", null, ["ROOT"], {
      surface: sourceSurface,
      evidence_basis: "CP016 retired exact goal child"
    });
  }
  const expectedSurface = sourceSurface;
  const finalSurface = semanticGuardComparisonSurface((analysis && analysis.nodes || []).map(nodeParserSurface).join(""));
  if (expectedSurface !== finalSurface) {
    addBlocker("lexicon_or_surface_dropout", null, ["ROOT"], {
      expected_parser_surface: expectedSurface,
      final_parser_surface: finalSurface
    });
  }
  const rootCoverage = rootSpanCoverageForDiagnostic(finalRows || []);
  if (rootCoverage.status === "PARTIAL") {
    addBlocker("partial_root_span_unwrapped_material", null, ["ROOT"], {
      unwrapped_root_surfaces: rootCoverage.unwrapped_root_surfaces || []
    });
  } else if (rootCoverage.status === "NO_TOP_CONSTRUCTION") {
    addBlocker("no_top_construction", null, ["ROOT"]);
  }
  if ((rootCoverage.top_construction_count || 0) > 1) {
    addReview("multiple_top_constructions", null, ["ROOT"], {
      top_construction_surfaces: rootCoverage.top_construction_surfaces || []
    });
  }
  const topConstructionNodes = (analysis && analysis.nodes || []).filter((node) => node && node.kind === "construction");
  if (topConstructionNodes.length === 1 && /(?:NP|Nominal)$/.test(String(topConstructionNodes[0].type || "")) && !explicitContext.supplied) {
    addReview("standalone_nominal_requires_fragment_or_context_review", topConstructionNodes[0], ["ROOT", topConstructionNodes[0].type], {
      surface: flattenSurface(topConstructionNodes[0])
    });
  }
  const topConstructionCount = topConstructionNodes.length;
  const outerWrapperBlocked = topConstructionCount > 0 && blockers.some((row) => row.depth > 0);
  if (outerWrapperBlocked) {
    addReview("outer_wrapper_contains_semantic_blocker", null, ["ROOT"], {
      blocker_count_below_root: blockers.filter((row) => row.depth > 0).length
    });
  }
  const status = blockers.length ? "BLOCKED" : reviewReasons.length ? "REVIEW_REQUIRED" : "MANUAL_REVIEW_ELIGIBLE";
  return {
    status,
    automatic_acceptance: false,
    blocker_count: blockers.length,
    review_reason_count: reviewReasons.length,
    outer_wrapper_blocked: outerWrapperBlocked,
    blockers,
    review_reasons: reviewReasons,
    policy: "A clean outer construction and full root coverage cannot establish semantic acceptance when any decisive descendant is unknown, dropped, malformed, context-dependent, retired, debunked, or lacks required typed relation provenance."
  };
}
function diagnosticSummary(analysis) {
  const finalRows = flattenNodes(analysis.nodes);
  const constructionRows = finalRows.filter((row) => row.kind === "construction");
  const traceSummary = traceCounts(finalRows);
  const templateFamilySummary = templateFamilyCounts(constructionRows);
  const reviewRows = finalRows.filter((row) => diagnosticLevel(row.trace && row.trace.kind) === "review");
  const selectionRows = finalRows.filter((row) => row.trace && row.trace.selection_decision);
  const lexicalizedSelectionRows = selectionRows.filter((row) => row.trace.selection_decision.chosen_registry_kind);
  const featureBundleRows = finalRows.filter((row) => row.feature_bundle || row.trace && row.trace.feature_bundle);
  const parserActiveFeatureBundleRows = featureBundleRows.filter((row) => {
    const bundle = row.feature_bundle || row.trace && row.trace.feature_bundle || {};
    return bundle.evidence_controls && bundle.evidence_controls.parser_active;
  });
  const internalTopConstructions = constructionRows.filter((row) => row.depth === 0).map((row) => row.type);
  const topConstructions = constructionRows.filter((row) => row.depth === 0).map((row) => row.compatibility_alias || diagnosticCompatibilityConstructionType(row.type));
  const topChildConstructions = topChildConstructionsForDiagnostic(constructionRows);
  const normalizationReviewSuggestionRows = normalizationReviewSuggestionDisplayRows(analysis);
  const parserShadowRepairRows = parserShadowRepairDisplayRows(analysis);
  const parserShadowRepairNoteCoverage = parserShadowRepairNoteCoverageSummary(parserShadowRepairRows);
  const foldedLexicalRepairRows = foldedLexicalRepairDisplayRows(analysis);
  const contextResolution = analysis.context_resolution || {};
  const explicitContext = analysis.explicit_context || { supplied: false, turns: [] };
  const rootSpanCoverage = rootSpanCoverageForDiagnostic(finalRows);
  const semanticAcceptance = semanticAcceptanceGuardForAnalysis(analysis, finalRows);
  const summary = {
    source: analysis.source,
    parser_shadow_source: analysis.parser_shadow_source || analysis.source,
    raw_first_display: true,
    normalization_trace: analysis.normalization_trace || [],
    parser_shadow_repairs: parserShadowRepairRows,
    parser_shadow_repair_count: parserShadowRepairRows.length,
    parser_shadow_repair_type_counts: parserShadowRepairTypeCounts(parserShadowRepairRows),
    parser_shadow_repair_note_coverage_status: parserShadowRepairNoteCoverage.status,
    missing_shadow_repair_note_count: parserShadowRepairNoteCoverage.missing_shadow_repair_note_count,
    folded_lexical_repairs: foldedLexicalRepairRows,
    folded_lexical_repair_count: foldedLexicalRepairRows.length,
    normalization_review_suggestions: analysis.normalization_review_suggestions || [],
    normalization_review_suggestion_display: normalizationReviewSuggestionRows,
    normalization_review_suggestion_display_count: normalizationReviewSuggestionRows.length,
    token_count: flattenNodes(analysis.tokens).filter((row) => row.kind === "token").length,
    final_node_count: finalRows.length,
    construction_count: constructionRows.length,
    top_constructions: topConstructions,
    internal_top_constructions: internalTopConstructions,
    top_child_constructions: topChildConstructions,
    top_child_construction_count: topChildConstructions.length,
    trace_summary: traceSummary,
    template_family_summary: templateFamilySummary,
    review_count: reviewRows.length,
    selection_decision_count: selectionRows.length,
    lexicalized_selection_count: lexicalizedSelectionRows.length,
    feature_bundle_count: featureBundleRows.length,
    parser_active_feature_bundle_count: parserActiveFeatureBundleRows.length,
    explicit_context_supplied: Boolean(explicitContext.supplied),
    explicit_context_turns: explicitContext.turns || [],
    context_requirement_status: contextResolution.context_requirement_status || "context_not_required",
    antecedent_status: contextResolution.antecedent_status || "not_applicable",
    missing_argument_slots: contextResolution.missing_argument_slots || [],
    context_turn_id: contextResolution.context_turn_id || "",
    question_id: contextResolution.question_id || "",
    antecedent_span: contextResolution.antecedent_span || "",
    selected_alternative: contextResolution.selected_alternative || "",
    root_span_coverage_status: rootSpanCoverage.status,
    root_top_construction_count: rootSpanCoverage.top_construction_count,
    root_top_construction_surfaces: rootSpanCoverage.top_construction_surfaces,
    unwrapped_root_nonpunctuation_count: rootSpanCoverage.unwrapped_root_nonpunctuation_count,
    unwrapped_root_surfaces: rootSpanCoverage.unwrapped_root_surfaces,
    ignored_root_remainder_count: rootSpanCoverage.ignored_root_remainder_count,
    semantic_acceptance: semanticAcceptance,
    semantic_acceptance_status: semanticAcceptance.status,
    semantic_acceptance_blocker_count: semanticAcceptance.blocker_count,
    semantic_acceptance_review_reason_count: semanticAcceptance.review_reason_count,
    semantic_acceptance_outer_wrapper_blocked: semanticAcceptance.outer_wrapper_blocked
  };
  const topicChainRoot = constructionRows.find((row) => row.trace && row.trace.topic_chain_id && row.trace.topic_chain_status);
  if (topicChainRoot && topicChainRoot.trace) {
    summary.topic_chain_id = topicChainRoot.trace.topic_chain_id || "";
    summary.topic_chain_status = topicChainRoot.trace.topic_chain_status || "";
    summary.topic_antecedent_surface = topicChainRoot.trace.topic_antecedent_surface || "";
    summary.topic_antecedent_source = topicChainRoot.trace.topic_antecedent_source || "";
    summary.topic_antecedent_semantic_domains = topicChainRoot.trace.topic_antecedent_semantic_domains || [];
    summary.topic_frame_status = topicChainRoot.trace.topic_frame_status || "";
    summary.topic_frame_linker_surface = topicChainRoot.trace.topic_frame_linker_surface || "";
    summary.topic_frame_domain_surface = topicChainRoot.trace.topic_frame_domain_surface || "";
    summary.relational_frame_status = topicChainRoot.trace.relational_frame_status || "";
    summary.relational_coverb_linker_surface = topicChainRoot.trace.relational_coverb_linker_surface || "";
    summary.relational_coverb_domain_surface = topicChainRoot.trace.relational_coverb_domain_surface || "";
    summary.linked_null_object_count = topicChainRoot.trace.linked_null_object_count || 0;
    summary.linked_predicate_surfaces = topicChainRoot.trace.linked_predicate_surfaces || [];
    summary.unresolved_predicate_surfaces = topicChainRoot.trace.unresolved_predicate_surfaces || [];
    summary.null_object_link = topicChainRoot.trace.null_object_link || "";
  }
  const particleClusterRoot = constructionRows.find((row) => row.trace && row.trace.particle_cluster_root);
  if (particleClusterRoot && particleClusterRoot.trace) {
    summary.particle_cluster_order_status = particleClusterRoot.trace.cluster_order_status || "";
    summary.visible_particle_sequence = particleClusterRoot.trace.visible_particle_sequence || [];
    summary.particle_scope_layers = particleClusterRoot.trace.particle_scope_layers || [];
    summary.particle_scope_functions = particleClusterRoot.trace.particle_scope_functions || [];
    summary.particle_cluster_scope_direction = particleClusterRoot.trace.scope_direction || "";
    summary.particle_cluster_fusion_status = particleClusterRoot.trace.fusion_status || "";
  } else {
    const diagnosticClusterInfo = orderedParticleClusterInfo(analysis.tokens || [], analysis.source || "");
    const diagnosticTailInfo = diagnosticClusterInfo || orderedParticleClusterTailInfo(analysis.tokens || [], analysis.source || "");
    if (diagnosticClusterInfo && !diagnosticClusterInfo.supportedOrder) {
      summary.particle_cluster_order_status = diagnosticClusterInfo.orderStatus;
      summary.visible_particle_sequence = diagnosticClusterInfo.visibleParticleSequence;
      summary.particle_scope_layers = diagnosticClusterInfo.particleScopeLayers;
      summary.particle_scope_functions = diagnosticClusterInfo.particleScopeFunctions;
      summary.particle_cluster_scope_direction = diagnosticClusterInfo.layerOrderCompatible ? "surface_order_preserved_scope_not_evidence_licensed" : "surface_order_preserved_scope_not_licensed";
      summary.particle_cluster_fusion_status = diagnosticClusterInfo.fusionStatus;
    } else if (diagnosticTailInfo && (topConstructions.includes("ANotAQuestion") || topConstructions.includes("ExistentialWhQuestion"))) {
      summary.particle_cluster_order_status = "question_precedence_cluster_tail_review";
      summary.visible_particle_sequence = diagnosticTailInfo.visibleParticleSequence;
      summary.particle_scope_layers = diagnosticTailInfo.particleScopeLayers;
      summary.particle_scope_functions = diagnosticTailInfo.particleScopeFunctions;
      summary.particle_cluster_scope_direction = "surface_order_preserved_question_scope_not_resolved";
      summary.particle_cluster_fusion_status = diagnosticTailInfo.fusionStatus;
    }
  }
  const semanticReviewFlags = semanticReviewFlagsForSummary(summary);
  if (semanticReviewFlags.length) summary.semantic_review_flags = semanticReviewFlags;
  return summary;
}
function cjkCharacterCount(surface) {
  return Array.from(String(surface || "")).filter((char) => /\p{Script=Han}/u.test(char)).length;
}
function normalizeJyutpingValue(value) {
  return String(value || "").trim().replace(/\s+/g, " ").toLowerCase();
}
function jyutpingSyllableIssues(jyutping) {
  const syllables = splitJyutping(jyutping);
  return syllables.filter((syllable) => !/^[a-z]+[1-6]$/i.test(syllable));
}
function contextualJyutpingExpectationsForRow(row) {
  const surface = String(row && row.surface || "");
  const syntax = String(row && row.syntax || "");
  if (surface === "呢" && /(^|\s)(?:discourse_fragment_question|sentence_final_question_particle)(\s|$)/.test(syntax)) {
    return { readings: ["ne1", "le1"], source: "contextual_question_ne_expectation" };
  }
  if (surface === "呢" && /(^|\s)demonstrative_determiner(\s|$)/.test(syntax)) {
    return { readings: ["ni1"], source: "contextual_demonstrative_expectation" };
  }
  const readings = JYUTPING_REVIEW_EXPECTATIONS[surface] || null;
  return { readings, source: readings ? "review_expectation" : "token_lexicon_or_wrapper_child" };
}
function jyutpingAuditRowsForAnalysis(analysis) {
  const rows = flattenNodes(analysis.nodes).filter((row) => row.kind === "token");
  return rows.map((row) => {
    const surface = row.surface || "";
    const actual = normalizeJyutpingValue(row.jyutping || "");
    const contextualExpectation = contextualJyutpingExpectationsForRow(row);
    const expected = contextualExpectation.readings;
    const traceKind2 = row.trace && row.trace.kind ? row.trace.kind : "unspecified";
    const item = {
      surface,
      role: row.role,
      syntax: row.syntax,
      jyutping: actual || null,
      expected: expected || null,
      status: "ok",
      source: contextualExpectation.source,
      trace: traceKind2
    };
    if (!actual) {
      if (cjkCharacterCount(surface) === 0) {
        item.status = "not_applicable_non_cjk";
        item.source = "non_cjk_token";
        item.reason = "Jyutping is not required for a token with no CJK characters.";
        return item;
      }
      item.status = "missing_jyutping";
      item.source = traceKind2 === "unknown_atomic" ? "unknown_atomic" : "token_without_jyutping";
      item.reason = "Rendered CJK token has no Jyutping attached.";
      return item;
    }
    const invalidSyllables = jyutpingSyllableIssues(actual);
    if (invalidSyllables.length) {
      item.status = "invalid_jyutping_format";
      item.invalid_syllables = invalidSyllables;
      item.reason = "Jyutping syllables should end with tone numbers 1–6.";
      return item;
    }
    const syllableCount = splitJyutping(actual).length;
    const hanCount = cjkCharacterCount(surface);
    if (hanCount > 0 && syllableCount > 0 && syllableCount !== hanCount) {
      item.status = "jyutping_syllable_count_mismatch";
      item.han_character_count = hanCount;
      item.jyutping_syllable_count = syllableCount;
      item.reason = "Number of Jyutping syllables does not match number of Han characters in the rendered token.";
      return item;
    }
    if (expected && !expected.map(normalizeJyutpingValue).includes(actual)) {
      item.status = "dictionary_disagreement";
      item.reason = "Token Jyutping differs from the local review expectation table.";
      return item;
    }
    if (expected && expected.length > 1) {
      item.status = "ok_multi_pronunciation_allowed";
      item.reason = "One of multiple accepted review readings matched.";
      return item;
    }
    return item;
  });
}
function jyutpingAuditSummary(analysis) {
  const items = jyutpingAuditRowsForAnalysis(analysis);
  const countByStatus = {};
  for (const item of items) countByStatus[item.status] = (countByStatus[item.status] || 0) + 1;
  const needsReviewStatuses = /* @__PURE__ */ new Set([
    "missing_jyutping",
    "invalid_jyutping_format",
    "jyutping_syllable_count_mismatch",
    "dictionary_disagreement"
  ]);
  const needsReview = items.filter((item) => needsReviewStatuses.has(item.status));
  return {
    status: needsReview.length ? "WARN" : "PASS",
    token_count: items.length,
    checked_count: items.filter((item) => item.status !== "missing_jyutping").length,
    missing_jyutping_count: countByStatus.missing_jyutping || 0,
    invalid_jyutping_format_count: countByStatus.invalid_jyutping_format || 0,
    syllable_count_mismatch_count: countByStatus.jyutping_syllable_count_mismatch || 0,
    dictionary_disagreement_count: countByStatus.dictionary_disagreement || 0,
    multi_pronunciation_allowed_count: countByStatus.ok_multi_pronunciation_allowed || 0,
    status_counts: countByStatus,
    items_needing_review: needsReview
  };
}
var {
  cleanLearnerNote,
  learnerGlossFromLexicon,
  contextualLearnerGlossLinesForToken,
  learnerGlossLinesForToken,
  learnerGlossLinesForConstruction,
  compactLearnerHoverLines,
  learnerVisibleSyntax,
  learnerUiHoverTitleForToken,
  learnerUiHoverTitleForConstruction,
  learnerUiHoverTitleForConstructionLayer
} = createLearnerDisplay({
  TOKEN_LEXICON,
  LEARNER_CONTEXTUAL_GLOSSES,
  LEARNER_SURFACE_GLOSSES,
  LEARNER_CONSTRUCTION_GLOSSES,
  diagnosticCompatibilityConstructionType,
  nodeDisplaySurface
});
function learnerUiHoverLeakFlags(title) {
  const text = String(title || "");
  const checks = [
    ["exposes_raw_learner_slot_list", /(^|\n)learner slots:\s*/i],
    ["exposes_hidden_internal_slot_count", /internal slots hidden from learner display/i],
    ["exposes_raw_internal_slot_list", /(^|\n)slots:\s*[a-z_]+/i],
    ["exposes_parser_or_template_note", /\b(parser|diagnostic|template|construction context|role override|internal|slots?)\b/i],
    ["exposes_contextual_implementation_note", /\b(contextual|active affordance|without deleting|transparent time expression|motion affordance|time affordance|keeps? soeng|while keeping)\b/i]
  ];
  return checks.filter(([, pattern]) => pattern.test(text)).map(([flag]) => flag);
}
function learnerUiHoverLooksLikePlainGloss(title) {
  const lines = String(title || "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
  return lines.some((line) => !/^[a-z_ ]+:/i.test(line) && !/^[a-z]+[1-6](?:\s+[a-z]+[1-6])*$/i.test(line) && /[A-Za-z]/.test(line) && !/\b(parser|diagnostic|template|syntax|slot|construction|internal|contextual)\b/i.test(line));
}
function learnerUiHoverGenericGlossFlags(title) {
  const lines = String(title || "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
  const flags = [];
  const generic = lines.filter((line) => /^(Cantonese word\.?|Cantonese text\.?|Cantonese phrase\.?)$/i.test(line));
  if (generic.length) flags.push("generic_learner_gloss");
  return flags;
}
function learnerUiHoverContextMismatchFlags(row) {
  if (!row || row.target_kind !== "token_title") return [];
  const surface = String(row.surface || "");
  const syntax = String(row.syntax || "");
  const title = String(row.current_default_hover_title || "");
  const flags = [];
  const conditionalMarker = /(^|\s)conditional_marker(\s|$)/.test(syntax);
  const reportedSpeech = /(^|\s)speech_reporting_verb(\s|$)/.test(syntax) && !conditionalMarker;
  if (conditionalMarker && ["嘅話", "話"].includes(surface)) {
    if (/\b(?:say|report)\b/i.test(title)) flags.push("conditional_marker_speech_gloss_leak");
    if (!/(?:\bif\b|in the case that)/i.test(title)) flags.push("conditional_marker_missing_conditional_gloss");
  }
  if (reportedSpeech && surface === "話") {
    if (/(?:in the case that|part of [“"']?if)/i.test(title)) flags.push("reported_speech_conditional_gloss_leak");
    if (!/\b(?:say|report)\b/i.test(title)) flags.push("reported_speech_missing_speech_gloss");
  }
  const conventionalDurationUnit = surface === "字" && /(^|\s)(?:conventional_duration_unit|five_minute_increment)(\s|$)/.test(syntax);
  if (conventionalDurationUnit) {
    if (/(?:characters?|writing)/i.test(title)) flags.push("conventional_duration_literal_gloss_leak");
    if (!/(?:five[- ]minute|5[- ]minute)/i.test(title)) flags.push("conventional_duration_missing_time_gloss");
  }
  const postClassifierApproximation = surface === "度" && /(^|\s)post_classifier_approximation_marker(\s|$)/.test(syntax);
  if (postClassifierApproximation) {
    if (/(?:locative|location|place|at here)/i.test(title)) flags.push("approximation_marker_locative_gloss_leak");
    if (!/(?:about|approximately|approximate)/i.test(title)) flags.push("approximation_marker_missing_approximation_gloss");
  }
  const finalQuestionParticle = surface === "呢" && /(^|\s)sentence_final_question_particle(\s|$)/.test(syntax);
  if (finalQuestionParticle) {
    if (/\bthis\b/i.test(title)) flags.push("question_particle_demonstrative_gloss_leak");
    if (!/(?:question particle|asks for an answer|what about|and what about)/i.test(title)) flags.push("question_particle_missing_question_gloss");
    if (/\bni1\b/i.test(title)) flags.push("question_particle_demonstrative_reading_leak");
  }
  return flags;
}
function learnerUiHoverAuditRows(analysis) {
  const rows = [];
  const pushRow = (row) => {
    row.leak_flags = mergeUnique([
      ...learnerUiHoverLeakFlags(row.current_default_hover_title),
      ...learnerUiHoverContextMismatchFlags(row)
    ]);
    row.has_plain_learner_gloss_line = learnerUiHoverLooksLikePlainGloss(row.current_default_hover_title);
    row.gloss_quality_flags = learnerUiHoverGenericGlossFlags(row.current_default_hover_title);
    rows.push(row);
  };
  for (const row of flattenNodes(analysis.nodes)) {
    if (row.kind === "token") {
      const title = learnerUiHoverTitleForToken(row);
      pushRow({
        surface: row.surface,
        target_kind: "token_title",
        parent: row.parent,
        role: row.role || row.label,
        syntax: row.syntax,
        jyutping: row.jyutping,
        learner_display_slots: learnerDisplaySlots(row.slots || []),
        current_default_hover_title: title,
        current_default_hover_lines: title.split(/\r?\n/)
      });
    }
    if (row.kind === "construction") {
      const title = learnerUiHoverTitleForConstruction(row);
      pushRow({
        surface: row.surface,
        target_kind: "construction_title",
        construction: row.type,
        label: row.label,
        learner_display_slots: learnerDisplaySlots(row.slots || []),
        current_default_hover_title: title,
        current_default_hover_lines: title.split(/\r?\n/)
      });
      const layerTitle = learnerUiHoverTitleForConstructionLayer(row);
      pushRow({
        surface: row.surface,
        target_kind: "construction_layer_title",
        construction: row.type,
        label: row.label,
        learner_display_slots: learnerDisplaySlots(row.slots || []),
        current_default_hover_title: layerTitle,
        current_default_hover_lines: layerTitle.split(/\r?\n/)
      });
    }
  }
  return rows;
}
function learnerUiHoverAuditSummary(analysis) {
  const rows = learnerUiHoverAuditRows(analysis);
  const flagCounts = {};
  for (const row of rows) {
    for (const flag of row.leak_flags || []) flagCounts[flag] = (flagCounts[flag] || 0) + 1;
    for (const flag of row.gloss_quality_flags || []) flagCounts[flag] = (flagCounts[flag] || 0) + 1;
  }
  const flaggedRows = rows.filter((row) => row.leak_flags && row.leak_flags.length || row.gloss_quality_flags && row.gloss_quality_flags.length);
  const missingPlainGlossRows = rows.filter((row) => !row.has_plain_learner_gloss_line);
  return {
    status: flaggedRows.length || missingPlainGlossRows.length ? "WARN" : "PASS",
    policy: "Diagnostic-only mirror of the current default hover/title payload. PASS rows are silent in rendered diagnostics; WARN rows surface raw-slot leaks, missing English learner glosses, or generic fallback glosses such as Cantonese word/text/phrase.",
    hover_target_count: rows.length,
    flagged_hover_target_count: flaggedRows.length,
    missing_plain_learner_gloss_count: missingPlainGlossRows.length,
    leak_flag_counts: flagCounts,
    sample_flagged_hover_rows: flaggedRows.concat(missingPlainGlossRows).slice(0, 8)
  };
}
function diagnosticContextualRoleAffordances(row = {}) {
  if (!row || row.kind !== "token") return [];
  const trace = row.trace || {};
  const resolution = trace.contextual_role_affordance_resolution || {};
  const candidates = Array.isArray(resolution.candidate_affordances) ? resolution.candidate_affordances.map((item) => ({ ...item })) : [];
  const activeRole = resolution.active_role || row.role || row.label || "";
  const lexicalDefaultRole = resolution.lexical_default_role || "";
  if (!Object.keys(resolution).length) {
    return contextualRoleAffordances(row).map((item) => ({
      ...item,
      active_in_final_construction: item.role === activeRole
    }));
  }
  if (lexicalDefaultRole && !candidates.some((item) => item.source === "lexical_default" && item.role === lexicalDefaultRole)) {
    candidates.unshift({
      role: lexicalDefaultRole,
      source: "lexical_default",
      active_before_construction_wrapping: true,
      active_in_final_construction: lexicalDefaultRole === activeRole,
      note: lexicalDefaultRole === "neutral" ? "No learner role was licensed before construction wrapping." : "Default learner role from the lexical entry before construction wrapping."
    });
  }
  const activeMatch = resolution.active_affordance_match || null;
  let activeFound = false;
  for (const item of candidates) {
    if (activeMatch) {
      item.active_in_final_construction = item.role === (activeMatch.role || activeRole) && (!activeMatch.source || item.source === activeMatch.source) && (!activeMatch.slot || item.slot === activeMatch.slot);
    } else {
      item.active_in_final_construction = item.role === activeRole && (item.source !== "lexical_default" || activeRole === lexicalDefaultRole);
    }
    if (item.active_in_final_construction) activeFound = true;
  }
  if (activeRole && !activeFound) {
    const activeSource = activeMatch && activeMatch.source ? activeMatch.source : activeRole === lexicalDefaultRole ? "lexical_default" : "construction_override";
    candidates.push({
      role: activeMatch && activeMatch.role ? activeMatch.role : activeRole,
      slot: activeMatch && activeMatch.slot ? activeMatch.slot : void 0,
      source: activeSource,
      active_before_construction_wrapping: activeSource === "lexical_default" && activeRole === lexicalDefaultRole,
      active_in_final_construction: true,
      activated_by: resolution.active_affordance_source || "",
      note: activeSource === "lexical_default" ? "Lexical default remains active in the final construction." : "Construction context activates this role/slot reading; competing lexical affordances remain visible but inactive."
    });
  }
  return candidates;
}
function diagnosticTokenRows(analysis) {
  return flattenNodes(analysis.tokens).map((row) => ({
    surface: row.surface,
    display_surface: row.display_surface,
    parser_surface: row.parser_surface,
    kind: row.kind,
    role: row.role,
    syntax: row.syntax,
    jyutping: row.jyutping,
    slots: row.slots,
    learner_display_slots: learnerDisplaySlots(row.slots || []),
    internal_slots_hidden_from_learner_display: internalOnlySlots(row.slots || []),
    contextual_role_affordances: row.kind === "token" ? contextualRoleAffordances(row) : [],
    trace: row.trace && row.trace.kind,
    features: row.features ? compactFeatureSummary(row.features) : void 0,
    feature_bundle: row.feature_bundle || row.trace && row.trace.feature_bundle,
    selection_decision: row.trace && row.trace.selection_decision
  }));
}
function diagnosticFinalRows(analysis) {
  return flattenNodes(analysis.nodes).map((row) => ({
    depth: row.depth,
    parent: row.parent_compatibility_alias || diagnosticCompatibilityConstructionType(row.parent || ""),
    internal_parent: row.parent || "",
    surface: row.surface,
    display_surface: row.display_surface,
    parser_surface: row.parser_surface,
    kind: row.kind,
    construction: row.compatibility_alias || diagnosticCompatibilityConstructionType(row.type || ""),
    internal_construction: row.type || "",
    compatibility_alias: row.compatibility_alias || INTERNAL_CONSTRUCTION_COMPATIBILITY_ALIASES[row.type] || "",
    internal_representation_scope: row.internal_representation_scope || "",
    label: row.label,
    role: row.role,
    syntax: row.syntax,
    jyutping: row.jyutping,
    slots: row.slots,
    learner_display_slots: learnerDisplaySlots(row.slots || []),
    internal_slots_hidden_from_learner_display: internalOnlySlots(row.slots || []),
    contextual_role_affordances: row.kind === "token" ? diagnosticContextualRoleAffordances(row) : [],
    trace: row.trace && row.trace.kind,
    feature_bundle: row.feature_bundle || row.trace && row.trace.feature_bundle,
    trace_detail: diagnosticCompatibilityTrace(row.trace, row.compatibility_alias || ""),
    internal_trace_detail: row.trace,
    runtime_registry: row.kind === "construction" ? runtimeConstructionStateFor(row.type) : null
  }));
}
function diagnosticLegend() {
  return {
    generative_template: "Best signal: matched by the slot-template engine over generated affordances. During the transition, trace detail may show template_family=construction_template for bounded templates that are not yet fully POS-general.",
    construction_template: "Temporary template-family label: bounded or vocabulary/surface-anchored template retained until the grammar can be expressed as a fully generative POS/slot pattern.",
    generative_or_heuristic_slot_rule: "Acceptable interim: structural/slot heuristic, not a full memorized sentence.",
    governed_discourse_wrapper: "Accepted structural wrapper for clause-linking or discourse/coordination sequences. It groups already-parsed clause-like children and is intentionally not a phrase-internal generative template.",
    predicate_heuristic: "Acceptable interim: local predicate wrapper from token features.",
    atomic_lexicon: "Atomic vocabulary lookup. In v0.4.40, registry-backed lexicalized stative tokens may include selection_decision explaining why lexicalized vs compositional won.",
    selection_decision: "Diagnostic detail inside token trace: explains lexicalized-stative registry scoring, forced-compositional exclusions, and ordinary lookup decisions.",
    feature_bundle: "Derived internal feature bundle. Phase 3 permits parser-active use only for migrated stative predicates; non-stative bundles remain parser-inactive.",
    contextual_role_affordances: "Diagnostic-only list of possible context-sensitive roles carried by a token. Context may activate one role, such as doing/movement for 上 in 上嚟, without deleting another valid affordance, such as when/time for 上個禮拜.",
    protected_formula_table: "Allowed opacity: formula intentionally protected.",
    surface_specific_phrase_rule: "Review: useful transparent rule, but still surface-specific.",
    legacy_surface_rule: "Review: older fallback rule; consider replacing with slot template.",
    special_ambiguity_rule: "Review/expected: explicit ambiguity handling.",
    jyutping_audit: "Separate pronunciation-quality lane. WARN does not fail parser coverage; it exposes missing/invalid/disagreement Jyutping data for review.",
    registry_audit: "Separate label-hygiene lane. WARN means a token learner role, construction label, parser-decision trace kind/template-family label, or slot name escaped the controlled registry.",
    learner_display_audit: "Separate UI-label lane. Parser/internal slots stay in diagnostics, while hover titles expose concise role/syntax metadata plus learner glosses.",
    learner_ui_hover_audit: "Diagnostic-only mirror of current default hover/title strings. PASS rows are silent; WARN rows show learner-facing raw-slot leaks, missing English learner glosses, or generic fallback glosses.",
    wrapper_coverage_audit: "Diagnostic-only wrapper integrity lane. ClauseRelationGraph must expose child/linker/separator coverage; ModalANotAQuestion must expose direct assigned-slot coverage before normal display may collapse it.",
    label_transition_audit: "Diagnostic-only inventory of construction trace families. PASS rows are silent; WARN rows classify unknown trace kinds. Migration-candidate rows are review inventory, not automatic acceptance or failure.",
    runtime_construction_registry_audit: "Checks only that every emitted construction label is active in the shipped runtime registry. Linguistic status and evidence are authoring-time data outside the plugin."
  };
}
function formatJsonBlock(value) {
  return JSON.stringify(value, null, 2);
}
function auditSummaryHasFindings(summary) {
  return !!(summary && summary.status && summary.status !== "PASS");
}
function appendDiagnosticSection(parts, title, value) {
  if (value === null || value === void 0) return;
  if (Array.isArray(value) && !value.length) return;
  if (typeof value === "object" && !Array.isArray(value) && !Object.keys(value).length) return;
  parts.push(title, formatJsonBlock(value));
}
var DIAGNOSTIC_REVIEW_REMINDERS = require_diagnostic_reminders();
function diagnosticReviewReminder(key) {
  return DIAGNOSTIC_REVIEW_REMINDERS[key] || "Diagnostic reminder: even when this lane passes, inspect the detailed diagnostics before accepting behavior.";
}
function withDiagnosticReviewReminder(summary, key) {
  if (!summary || typeof summary !== "object" || Array.isArray(summary)) return summary;
  return { ...summary, review_reminder: diagnosticReviewReminder(key) };
}
function diagnosticReviewReminderSummary(keys = Object.keys(DIAGNOSTIC_REVIEW_REMINDERS)) {
  const out = {};
  for (const key of keys) out[key] = diagnosticReviewReminder(key);
  return out;
}
function expectedNormalizationChangeCount(rawSource, parserShadowSource) {
  const rawChars = Array.from(String(rawSource || ""));
  const shadowChars = Array.from(String(parserShadowSource || ""));
  const length = Math.max(rawChars.length, shadowChars.length);
  let count = 0;
  for (let index = 0; index < length; index += 1) {
    if ((rawChars[index] || "") !== (shadowChars[index] || "")) count += 1;
  }
  return count;
}
function normalizationTraceMap(trace) {
  const map = /* @__PURE__ */ new Map();
  for (const item of trace || []) {
    map.set(item.index, item);
  }
  return map;
}
function untracedNormalizationChangeCount(rawSource, parserShadowSource, trace) {
  const rawChars = Array.from(String(rawSource || ""));
  const shadowChars = Array.from(String(parserShadowSource || ""));
  const traceByIndex = normalizationTraceMap(trace || []);
  const length = Math.max(rawChars.length, shadowChars.length);
  let count = 0;
  for (let index = 0; index < length; index += 1) {
    const raw = rawChars[index] || "";
    const normalized = shadowChars[index] || "";
    if (raw === normalized) continue;
    const item = traceByIndex.get(index);
    if (!item || item.raw !== raw || item.normalized !== normalized || item.applied_to_parser_shadow !== true) count += 1;
  }
  return count;
}
function normalizationAuditSummary(analysis) {
  const input = analysis && analysis.input_normalization ? analysis.input_normalization : {};
  const rawSource = input.raw_source !== void 0 ? input.raw_source : analysis && analysis.source || "";
  const parserShadowSource = input.parser_shadow_source !== void 0 ? input.parser_shadow_source : analysis && analysis.parser_shadow_source || rawSource;
  const trace = Array.isArray(input.normalization_trace) ? input.normalization_trace : [];
  const reviewSuggestions = Array.isArray(input.review_suggestions) ? input.review_suggestions : Array.isArray(analysis && analysis.normalization_review_suggestions) ? analysis.normalization_review_suggestions : [];
  const expectedChangeCount = expectedNormalizationChangeCount(rawSource, parserShadowSource);
  const untracedChangeCount = untracedNormalizationChangeCount(rawSource, parserShadowSource, trace);
  const allowedAppliedTraceTypes = /* @__PURE__ */ new Set([
    "simplified_to_traditional",
    "pinyin_fallout_cantonese_lexical_repair"
  ]);
  const unsafeTraceRows = trace.filter((item) => item.applied_to_parser_shadow && (item.confidence !== "high" || !allowedAppliedTraceTypes.has(item.type) || item.learner_display_replaced === true || item.type === "pinyin_fallout_cantonese_lexical_repair" && item.one_to_one_character_mapping !== true));
  const parserShadowRepairRows = trace.filter((item) => item && item.applied_to_parser_shadow === true && item.raw !== item.normalized);
  const parserShadowRepairDisplay = parserShadowRepairDisplayRows(analysis);
  const parserShadowRepairNoteCoverage = parserShadowRepairNoteCoverageSummary(parserShadowRepairDisplay);
  const foldedLexicalRepairRows = trace.filter((item) => item.type === "pinyin_fallout_cantonese_lexical_repair");
  const appliedReviewSuggestionRows = reviewSuggestions.filter((item) => item.applied_to_parser_shadow || item.learner_display_replaced);
  const rawDisplayReplacementRows = trace.filter((item) => item.learner_display_replaced === true);
  const rawDisplayPreservationStatus = rawDisplayReplacementRows.length ? "WARN" : "PASS";
  const status = unsafeTraceRows.length || appliedReviewSuggestionRows.length || rawDisplayReplacementRows.length || untracedChangeCount || parserShadowRepairNoteCoverage.status !== "PASS" ? "WARN" : "PASS";
  const summary = {
    status,
    policy: "Normalization audit lane: parser-shadow normalization may apply high-confidence Simplified-to-Traditional character-form changes and narrow one-character pinyin-fallout Cantonese lexical repairs with visible trace. Raw learner-visible text must not be replaced. Every parser-shadow repair must be represented in parser_shadow_repairs with a note.",
    raw_source: rawSource,
    parser_shadow_source: parserShadowSource,
    parser_shadow_differs_from_raw: rawSource !== parserShadowSource,
    expected_normalization_change_count: expectedChangeCount,
    normalization_trace_count: trace.length,
    parser_shadow_repair_count: parserShadowRepairRows.length,
    parser_shadow_repair_type_counts: parserShadowRepairTypeCounts(parserShadowRepairDisplay),
    parser_shadow_repair_note_coverage_status: parserShadowRepairNoteCoverage.status,
    missing_shadow_repair_note_count: parserShadowRepairNoteCoverage.missing_shadow_repair_note_count,
    review_suggestion_count: reviewSuggestions.length,
    folded_lexical_repair_count: foldedLexicalRepairRows.length,
    untraced_shadow_change_count: untracedChangeCount,
    unsafe_normalization_count: unsafeTraceRows.length,
    applied_review_suggestion_count: appliedReviewSuggestionRows.length,
    raw_display_replacement_count: rawDisplayReplacementRows.length,
    raw_display_preservation_status: rawDisplayPreservationStatus,
    review_reminder: diagnosticReviewReminder("normalization_audit")
  };
  if (parserShadowRepairNoteCoverage.status !== "PASS") summary.parser_shadow_repairs_missing_notes = parserShadowRepairDisplay.filter((row) => !row.note || row.note_coverage_status !== "PASS");
  if (unsafeTraceRows.length) summary.unsafe_normalization_rows = unsafeTraceRows;
  if (appliedReviewSuggestionRows.length) summary.applied_review_suggestion_rows = appliedReviewSuggestionRows;
  if (rawDisplayReplacementRows.length) summary.raw_display_replacement_rows = rawDisplayReplacementRows;
  return summary;
}
function nonPassAuditSummary(summary) {
  return auditSummaryHasFindings(summary) ? summary : null;
}
function nonPassWrapperCoverageRows(analysis) {
  return wrapperCoverageAuditRows(analysis).filter((row) => row.status && row.status !== "PASS");
}
function nonPassLabelTransitionRows(analysis) {
  return labelTransitionAuditRows(analysis).filter((row) => row.transition_status === "needs_registry_decision");
}
function normalizeDiagnosticSourceForReview(source) {
  return String(source || "").trim().replace(/[。！？!?，,、；;：:]+$/u, "");
}
function hasSuspiciousBareNumeralObjectSource(source) {
  const normalized = normalizeDiagnosticSourceForReview(source);
  return /^(食|飲|睇|講|買|寫|做|聽|攞|放|還|摘|試|諗|Book)[一二三四五六七八九十兩]+$/u.test(normalized);
}
function hasLexicalSemanticFeatureReviewSource(source) {
  const normalized = normalizeDiagnosticSourceForReview(source);
  return false;
}
function semanticReviewFlagsForSummary(summary) {
  const flags = [];
  const top = summary && Array.isArray(summary.top_constructions) ? summary.top_constructions : [];
  const source = summary && summary.source ? summary.source : "";
  if (!top.length) flags.push("no_top_construction");
  if (top.length > 1) flags.push("multiple_top_constructions");
  if (top.includes("NeedsContext")) flags.push("needs_context_parse");
  if (top.includes("MalformedCandidate")) flags.push("malformed_candidate_parse");
  if (summary && summary.context_requirement_status === "context_required") flags.push("context_required_unresolved");
  if (summary && summary.context_requirement_status === "context_incompatible") flags.push("context_incompatible");
  if (summary && summary.topic_chain_status === "antecedent_predicate_compatibility_review") flags.push("topic_chain_antecedent_predicate_compatibility_review");
  if (summary && summary.root_span_coverage_status === "PARTIAL") flags.push("partial_root_span_unwrapped_material");
  if ((summary && summary.review_count) > 0) flags.push("review_trace_present");
  if (summary && [
    "unsupported_or_unvalidated_order_review",
    "layer_order_compatible_unvalidated_review",
    "question_precedence_cluster_tail_review"
  ].includes(summary.particle_cluster_order_status)) flags.push("particle_cluster_order_review");
  if (hasSuspiciousBareNumeralObjectSource(source)) flags.push("suspicious_bare_numeral_object");
  if (hasLexicalSemanticFeatureReviewSource(source)) flags.push("lexical_semantic_feature_review");
  return Array.from(new Set(flags));
}
function formatDiagnosticMarkdown(analysis) {
  const summary = diagnosticSummary(analysis);
  const parts = [
    "Summary",
    formatJsonBlock(summary)
  ];
  appendDiagnosticSection(parts, "Diagnostic review reminders", diagnosticReviewReminderSummary([
    "normalization",
    "normalization_audit",
    "jyutping",
    "registry",
    "learner_display",
    "learner_ui_hover",
    "wrapper_coverage",
    "label_transition",
    "runtime_construction_registry"
  ]));
  appendDiagnosticSection(parts, "Input normalization trace", inputNormalizationHasFindings(analysis.input_normalization) ? withDiagnosticReviewReminder(analysis.input_normalization, "normalization") : null);
  const parserShadowRepairRows = parserShadowRepairDisplayRows(analysis);
  appendDiagnosticSection(parts, "Parser-shadow repair inventory", parserShadowRepairRows.length ? {
    status: parserShadowRepairNoteCoverageSummary(parserShadowRepairRows).status,
    policy: "Comprehensive inventory of every raw → parser_shadow_source repair, including character-form normalization and folded lexical repairs. Raw learner-visible text is preserved.",
    parser_shadow_repair_count: parserShadowRepairRows.length,
    parser_shadow_repair_type_counts: parserShadowRepairTypeCounts(parserShadowRepairRows),
    missing_shadow_repair_note_count: parserShadowRepairNoteCoverageSummary(parserShadowRepairRows).missing_shadow_repair_note_count,
    repairs: parserShadowRepairRows,
    review_reminder: diagnosticReviewReminder("normalization")
  } : null);
  const foldedRepairRows = foldedLexicalRepairDisplayRows(analysis);
  appendDiagnosticSection(parts, "Folded pinyin-fallout lexical repairs", foldedRepairRows.length ? {
    status: "PASS",
    policy: "Subset of parser-shadow repairs that are narrow one-character pinyin-fallout lexical repairs. See Parser-shadow repair inventory for all shadow repairs.",
    repairs: foldedRepairRows,
    review_reminder: diagnosticReviewReminder("normalization")
  } : null);
  const reviewSuggestionRows = normalizationReviewSuggestionDisplayRows(analysis);
  appendDiagnosticSection(parts, "Review-only normalization suggestions", reviewSuggestionRows.length ? {
    status: "PASS",
    policy: "Compatibility lane only. Current v0.5.107 pinyin-fallout repairs should not depend on review-only suggestions.",
    suggestions: reviewSuggestionRows,
    review_reminder: diagnosticReviewReminder("normalization")
  } : null);
  appendDiagnosticSection(parts, "Normalization audit findings", withDiagnosticReviewReminder(nonPassAuditSummary(normalizationAuditSummary(analysis)), "normalization_audit"));
  appendDiagnosticSection(parts, "Jyutping audit findings", withDiagnosticReviewReminder(nonPassAuditSummary(jyutpingAuditSummary(analysis)), "jyutping"));
  appendDiagnosticSection(parts, "Registry audit findings", withDiagnosticReviewReminder(nonPassAuditSummary(registryAuditSummary(analysis)), "registry"));
  appendDiagnosticSection(parts, "Learner display audit findings", withDiagnosticReviewReminder(nonPassAuditSummary(learnerDisplayAuditSummary(analysis)), "learner_display"));
  const hoverSummary = learnerUiHoverAuditSummary(analysis);
  appendDiagnosticSection(parts, "Learner UI hover audit findings", withDiagnosticReviewReminder(nonPassAuditSummary(hoverSummary), "learner_ui_hover"));
  if (auditSummaryHasFindings(hoverSummary)) {
    appendDiagnosticSection(parts, "Learner UI hover warning rows", learnerUiHoverAuditRows(analysis).filter((row) => (row.leak_flags || []).length || (row.gloss_quality_flags || []).length || !row.has_plain_learner_gloss_line));
  }
  const wrapperSummary = wrapperCoverageAuditSummary(analysis);
  appendDiagnosticSection(parts, "Wrapper coverage audit findings", withDiagnosticReviewReminder(nonPassAuditSummary(wrapperSummary), "wrapper_coverage"));
  if (auditSummaryHasFindings(wrapperSummary)) appendDiagnosticSection(parts, "Wrapper coverage warning rows", nonPassWrapperCoverageRows(analysis));
  const labelSummary = labelTransitionAuditSummary(analysis);
  appendDiagnosticSection(parts, "Label transition audit findings", withDiagnosticReviewReminder(nonPassAuditSummary(labelSummary), "label_transition"));
  if (auditSummaryHasFindings(labelSummary)) appendDiagnosticSection(parts, "Label transition warning rows", nonPassLabelTransitionRows(analysis));
  const runtimeRegistrySummary = runtimeConstructionRegistryAuditSummary(analysis);
  appendDiagnosticSection(parts, "Runtime construction registry audit", withDiagnosticReviewReminder(runtimeRegistrySummary, "runtime_construction_registry"));
  if (auditSummaryHasFindings(runtimeRegistrySummary)) appendDiagnosticSection(parts, "Runtime construction registry warning rows", nonPassRuntimeConstructionRegistryRows(analysis));
  appendDiagnosticSection(parts, "Tokenization before construction wrapping", diagnosticTokenRows(analysis));
  appendDiagnosticSection(parts, "Final construction tree", diagnosticFinalRows(analysis));
  appendDiagnosticSection(parts, "Legend", diagnosticLegend());
  appendDiagnosticSection(parts, "Diagnostic final review reminder", {
    reminder: "Final diagnostic reminder: regardless of PASS/WARN/FAIL state, inspect the summary, parser-shadow/raw-display fields, top constructions, learner roles, trace details, and semantic-review flags before accepting this parse."
  });
  return parts.join("\n");
}
function extractCantoSpanCodeBlocks(markdown) {
  const text = String(markdown || "");
  const lines = text.split(/\r?\n/);
  const blocks = [];
  let open = null;
  for (let index = 0; index < lines.length; index += 1) {
    const line = lines[index];
    if (!open) {
      const match = line.match(/^\s*(```+|~~~+)\s*canto-span(?:\s+[^`]*)?\s*$/);
      if (match) {
        open = {
          fence: match[1].startsWith("~") ? "~" : "`",
          length: match[1].length,
          startLine: index + 1,
          lines: []
        };
      }
      continue;
    }
    const closePattern = open.fence === "~" ? /^\s*~~~+\s*$/ : /^\s*```+\s*$/;
    if (closePattern.test(line)) {
      blocks.push({
        source: open.lines.join("\n"),
        startLine: open.startLine,
        endLine: index + 1
      });
      open = null;
      continue;
    }
    open.lines.push(line);
  }
  return blocks;
}
function countCantoSpanFenceOpeners(markdown) {
  return String(markdown || "").split(/\r?\n/).filter((line) => /^\s*(```+|~~~+)\s*canto-span(?:\s+[^`]*)?\s*$/.test(line)).length;
}
function collectCantoSpanDiagnosticEntries(markdown) {
  const blocks = extractCantoSpanCodeBlocks(markdown);
  const entries = [];
  const errors = [];
  const contextDirectives = [];
  const directiveWarnings = [];
  blocks.forEach((block, blockIndex) => {
    const options = parseBlockOptions(block.source);
    contextDirectives.push(...(options.context_directives || []).map((directive) => ({
      block_index: blockIndex + 1,
      block_start_line: block.startLine,
      ...directive
    })));
    directiveWarnings.push(...(options.directive_warnings || []).map((warning) => ({
      block_index: blockIndex + 1,
      block_start_line: block.startLine,
      ...warning
    })));
    options.entries.forEach((optionEntry) => {
      const line = optionEntry.line;
      if (!normalizeSurface(line)) return;
      try {
        entries.push({
          block_index: blockIndex + 1,
          block_start_line: block.startLine,
          source_line_index: optionEntry.source_line_index,
          source: line,
          context_directive: optionEntry.context_directive || null,
          explicit_context: optionEntry.context,
          analysis: analyzeLine(line, optionEntry.context)
        });
      } catch (error) {
        errors.push({
          block_index: blockIndex + 1,
          block_start_line: block.startLine,
          source_line_index: optionEntry.source_line_index,
          source: line,
          error: error && error.message ? error.message : String(error)
        });
      }
    });
  });
  return {
    blocks,
    entries,
    errors,
    context_directives: contextDirectives,
    directive_warnings: directiveWarnings
  };
}
function noteDiagnosticCoverageSummary(markdown, collected) {
  const noteText = String(markdown || "");
  const expectedFenceOpeners = countCantoSpanFenceOpeners(noteText);
  const blocks = collected && collected.blocks ? collected.blocks : [];
  const entries = collected && collected.entries ? collected.entries : [];
  const errors = collected && collected.errors ? collected.errors : [];
  const contextDirectives = collected && collected.context_directives ? collected.context_directives : [];
  const directiveWarnings = collected && collected.directive_warnings ? collected.directive_warnings : [];
  const sourceCounts = /* @__PURE__ */ new Map();
  entries.forEach((entry) => {
    sourceCounts.set(entry.source, (sourceCounts.get(entry.source) || 0) + 1);
  });
  const duplicateSources = Array.from(sourceCounts.entries()).filter(([, count]) => count > 1).map(([source, count]) => ({ source, count }));
  const renderedSources = entries.map((entry) => {
    const parseSummary = diagnosticSummary(entry.analysis);
    const normalizationSummary = normalizationAuditSummary(entry.analysis);
    const jyutpingSummary = jyutpingAuditSummary(entry.analysis);
    const registrySummary = registryAuditSummary(entry.analysis);
    const runtimeRegistrySummary = runtimeConstructionRegistryAuditSummary(entry.analysis);
    return {
      block_index: entry.block_index,
      block_start_line: entry.block_start_line,
      source_line_index: entry.source_line_index,
      source: entry.source,
      context_directive: entry.context_directive || null,
      explicit_context: entry.analysis.explicit_context || { supplied: false, turns: [] },
      parser_shadow_source: entry.analysis.parser_shadow_source || entry.source,
      normalization_trace: entry.analysis.normalization_trace || [],
      parser_shadow_repairs: parserShadowRepairDisplayRows(entry.analysis),
      parser_shadow_repair_count: normalizationSummary.parser_shadow_repair_count,
      parser_shadow_repair_type_counts: normalizationSummary.parser_shadow_repair_type_counts,
      parser_shadow_repair_note_coverage_status: normalizationSummary.parser_shadow_repair_note_coverage_status,
      missing_shadow_repair_note_count: normalizationSummary.missing_shadow_repair_note_count,
      normalization_review_suggestions: entry.analysis.normalization_review_suggestions || [],
      normalization_review_suggestion_display: normalizationReviewSuggestionDisplayRows(entry.analysis),
      folded_lexical_repairs: foldedLexicalRepairDisplayRows(entry.analysis),
      folded_lexical_repair_count: normalizationSummary.folded_lexical_repair_count,
      normalization_audit_status: normalizationSummary.status,
      normalization_trace_count: normalizationSummary.normalization_trace_count,
      normalization_review_suggestion_count: normalizationSummary.review_suggestion_count,
      unsafe_normalization_count: normalizationSummary.unsafe_normalization_count,
      applied_review_suggestion_count: normalizationSummary.applied_review_suggestion_count,
      raw_display_replacement_count: normalizationSummary.raw_display_replacement_count,
      untraced_shadow_change_count: normalizationSummary.untraced_shadow_change_count,
      raw_display_preservation_status: normalizationSummary.raw_display_preservation_status,
      top_constructions: parseSummary.top_constructions,
      top_child_constructions: parseSummary.top_child_constructions,
      top_child_construction_count: parseSummary.top_child_construction_count,
      context_requirement_status: parseSummary.context_requirement_status,
      antecedent_status: parseSummary.antecedent_status,
      missing_argument_slots: parseSummary.missing_argument_slots,
      context_turn_id: parseSummary.context_turn_id,
      selected_alternative: parseSummary.selected_alternative,
      review_count: parseSummary.review_count,
      parser_active_feature_bundle_count: parseSummary.parser_active_feature_bundle_count,
      semantic_acceptance: parseSummary.semantic_acceptance || null,
      semantic_acceptance_status: parseSummary.semantic_acceptance_status || "REVIEW_REQUIRED",
      semantic_acceptance_blocker_count: parseSummary.semantic_acceptance_blocker_count || 0,
      semantic_acceptance_review_reason_count: parseSummary.semantic_acceptance_review_reason_count || 0,
      semantic_acceptance_outer_wrapper_blocked: Boolean(parseSummary.semantic_acceptance_outer_wrapper_blocked),
      semantic_review_flags: parseSummary.semantic_review_flags || [],
      ...rootSpanCoverageFieldsFromDiagnosticSummary(parseSummary),
      jyutping_audit_status: jyutpingSummary.status,
      missing_jyutping_count: jyutpingSummary.missing_jyutping_count,
      invalid_jyutping_format_count: jyutpingSummary.invalid_jyutping_format_count,
      syllable_count_mismatch_count: jyutpingSummary.syllable_count_mismatch_count,
      dictionary_disagreement_count: jyutpingSummary.dictionary_disagreement_count,
      registry_audit_status: registrySummary.status,
      invalid_learner_role_count: registrySummary.invalid_learner_role_count,
      invalid_slot_name_count: registrySummary.invalid_slot_name_count,
      invalid_construction_label_count: registrySummary.invalid_construction_label_count,
      parser_decision_label_registry_status: registrySummary.parser_decision_label_registry_status,
      invalid_parser_decision_label_count: registrySummary.invalid_parser_decision_label_count,
      learner_display_audit_status: learnerDisplayAuditSummary(entry.analysis).status,
      hidden_internal_slot_reference_count: learnerDisplayAuditSummary(entry.analysis).hidden_internal_slot_reference_count,
      learner_ui_hover_audit_status: learnerUiHoverAuditSummary(entry.analysis).status,
      learner_ui_flagged_hover_target_count: learnerUiHoverAuditSummary(entry.analysis).flagged_hover_target_count,
      learner_ui_missing_plain_gloss_count: learnerUiHoverAuditSummary(entry.analysis).missing_plain_learner_gloss_count,
      wrapper_coverage_audit_status: wrapperCoverageAuditSummary(entry.analysis).status,
      wrapper_coverage_row_count: wrapperCoverageAuditSummary(entry.analysis).wrapper_row_count,
      unaccounted_wrapper_token_count: wrapperCoverageAuditSummary(entry.analysis).unaccounted_wrapper_token_count,
      label_transition_audit_status: labelTransitionAuditSummary(entry.analysis).status,
      label_transition_migration_candidate_count: labelTransitionAuditSummary(entry.analysis).migration_candidate_count,
      label_transition_already_generative_count: labelTransitionAuditSummary(entry.analysis).already_generative_count,
      label_transition_needs_registry_decision_count: labelTransitionAuditSummary(entry.analysis).needs_registry_decision_count,
      runtime_construction_registry_audit_status: runtimeRegistrySummary.status,
      runtime_construction_registry_unique_construction_count: runtimeRegistrySummary.unique_construction_count,
      runtime_construction_registry_active_row_count: runtimeRegistrySummary.active_row_count,
      runtime_construction_registry_unregistered_row_count: runtimeRegistrySummary.unregistered_row_count
    };
  });
  const normalizationAggregate = renderedSources.reduce((acc, row) => {
    acc.normalization_trace_count += row.normalization_trace_count || 0;
    acc.parser_shadow_repair_count += row.parser_shadow_repair_count || 0;
    acc.missing_shadow_repair_note_count += row.missing_shadow_repair_note_count || 0;
    Object.entries(row.parser_shadow_repair_type_counts || {}).forEach(([key, value]) => {
      acc.parser_shadow_repair_type_counts[key] = (acc.parser_shadow_repair_type_counts[key] || 0) + value;
    });
    acc.review_suggestion_count += row.normalization_review_suggestion_count || 0;
    acc.folded_lexical_repair_count += row.folded_lexical_repair_count || 0;
    acc.unsafe_normalization_count += row.unsafe_normalization_count || 0;
    acc.applied_review_suggestion_count += row.applied_review_suggestion_count || 0;
    acc.raw_display_replacement_count += row.raw_display_replacement_count || 0;
    acc.untraced_shadow_change_count += row.untraced_shadow_change_count || 0;
    if (row.parser_shadow_source && row.parser_shadow_source !== row.source) acc.normalized_source_count += 1;
    if (row.normalization_audit_status && row.normalization_audit_status !== "PASS") {
      acc.warning_rows.push({
        source: row.source,
        parser_shadow_source: row.parser_shadow_source,
        normalization_trace_count: row.normalization_trace_count || 0,
        parser_shadow_repair_count: row.parser_shadow_repair_count || 0,
        parser_shadow_repair_type_counts: row.parser_shadow_repair_type_counts || {},
        missing_shadow_repair_note_count: row.missing_shadow_repair_note_count || 0,
        review_suggestion_count: row.normalization_review_suggestion_count || 0,
        folded_lexical_repair_count: row.folded_lexical_repair_count || 0,
        unsafe_normalization_count: row.unsafe_normalization_count || 0,
        applied_review_suggestion_count: row.applied_review_suggestion_count || 0,
        raw_display_replacement_count: row.raw_display_replacement_count || 0,
        untraced_shadow_change_count: row.untraced_shadow_change_count || 0,
        raw_display_preservation_status: row.raw_display_preservation_status || ""
      });
    }
    return acc;
  }, {
    normalization_trace_count: 0,
    parser_shadow_repair_count: 0,
    parser_shadow_repair_type_counts: {},
    missing_shadow_repair_note_count: 0,
    review_suggestion_count: 0,
    folded_lexical_repair_count: 0,
    normalized_source_count: 0,
    unsafe_normalization_count: 0,
    applied_review_suggestion_count: 0,
    raw_display_replacement_count: 0,
    untraced_shadow_change_count: 0,
    warning_rows: []
  });
  normalizationAggregate.parser_shadow_repair_note_coverage_status = normalizationAggregate.missing_shadow_repair_note_count ? "WARN" : "PASS";
  normalizationAggregate.raw_display_preservation_status = normalizationAggregate.raw_display_replacement_count ? "WARN" : "PASS";
  normalizationAggregate.status = normalizationAggregate.unsafe_normalization_count || normalizationAggregate.applied_review_suggestion_count || normalizationAggregate.raw_display_replacement_count || normalizationAggregate.untraced_shadow_change_count || normalizationAggregate.missing_shadow_repair_note_count ? "WARN" : "PASS";
  normalizationAggregate.policy = "Normalization audit lane: high-confidence Simplified-to-Traditional parser-shadow changes and narrow one-character pinyin-fallout Cantonese lexical repairs are allowed with trace; raw learner display must not be replaced. parser_shadow_repairs is the comprehensive shadow-repair inventory.";
  normalizationAggregate.review_reminder = diagnosticReviewReminder("normalization_audit");
  const jyutpingAggregate = renderedSources.reduce((acc, row) => {
    acc.missing_jyutping_count += row.missing_jyutping_count || 0;
    acc.invalid_jyutping_format_count += row.invalid_jyutping_format_count || 0;
    acc.syllable_count_mismatch_count += row.syllable_count_mismatch_count || 0;
    acc.dictionary_disagreement_count += row.dictionary_disagreement_count || 0;
    return acc;
  }, {
    missing_jyutping_count: 0,
    invalid_jyutping_format_count: 0,
    syllable_count_mismatch_count: 0,
    dictionary_disagreement_count: 0
  });
  jyutpingAggregate.status = Object.values(jyutpingAggregate).some((value) => typeof value === "number" && value > 0) ? "WARN" : "PASS";
  const registryAggregate = renderedSources.reduce((acc, row) => {
    acc.invalid_learner_role_count += row.invalid_learner_role_count || 0;
    acc.invalid_slot_name_count += row.invalid_slot_name_count || 0;
    acc.invalid_construction_label_count += row.invalid_construction_label_count || 0;
    acc.invalid_parser_decision_label_count += row.invalid_parser_decision_label_count || 0;
    return acc;
  }, {
    invalid_learner_role_count: 0,
    invalid_slot_name_count: 0,
    invalid_construction_label_count: 0,
    invalid_parser_decision_label_count: 0
  });
  registryAggregate.learner_role_registry_status = registryAggregate.invalid_learner_role_count ? "WARN" : "PASS";
  registryAggregate.slot_name_registry_status = registryAggregate.invalid_slot_name_count ? "WARN" : "PASS";
  registryAggregate.construction_label_registry_status = registryAggregate.invalid_construction_label_count ? "WARN" : "PASS";
  registryAggregate.parser_decision_label_registry_status = registryAggregate.invalid_parser_decision_label_count ? "WARN" : "PASS";
  registryAggregate.status = registryAggregate.invalid_learner_role_count || registryAggregate.invalid_slot_name_count || registryAggregate.invalid_construction_label_count || registryAggregate.invalid_parser_decision_label_count ? "WARN" : "PASS";
  const learnerDisplayAggregate = renderedSources.reduce((acc, row) => {
    acc.hidden_internal_slot_reference_count += row.hidden_internal_slot_reference_count || 0;
    return acc;
  }, { hidden_internal_slot_reference_count: 0 });
  learnerDisplayAggregate.status = "PASS";
  const learnerUiHoverAggregate = renderedSources.reduce((acc, row) => {
    acc.flagged_hover_target_count += row.learner_ui_flagged_hover_target_count || 0;
    acc.missing_plain_learner_gloss_count += row.learner_ui_missing_plain_gloss_count || 0;
    return acc;
  }, { flagged_hover_target_count: 0, missing_plain_learner_gloss_count: 0 });
  learnerUiHoverAggregate.status = learnerUiHoverAggregate.flagged_hover_target_count || learnerUiHoverAggregate.missing_plain_learner_gloss_count ? "WARN" : "PASS";
  const wrapperCoverageAggregate = renderedSources.reduce((acc, row) => {
    acc.wrapper_row_count += row.wrapper_coverage_row_count || 0;
    acc.unaccounted_wrapper_token_count += row.unaccounted_wrapper_token_count || 0;
    return acc;
  }, { wrapper_row_count: 0, unaccounted_wrapper_token_count: 0 });
  wrapperCoverageAggregate.status = wrapperCoverageAggregate.unaccounted_wrapper_token_count ? "WARN" : "PASS";
  const labelTransitionAggregate = entries.reduce((acc, entry) => {
    const summary = labelTransitionAuditSummary(entry.analysis);
    acc.migration_candidate_count += summary.migration_candidate_count || 0;
    acc.already_generative_count += summary.already_generative_count || 0;
    acc.reviewed_table_or_guardrail_count += summary.reviewed_table_or_guardrail_count || 0;
    acc.needs_registry_decision_count += summary.needs_registry_decision_count || 0;
    for (const [bucket, count] of Object.entries(summary.transition_bucket_counts || {})) {
      acc.transition_bucket_counts[bucket] = (acc.transition_bucket_counts[bucket] || 0) + count;
    }
    for (const [status, count] of Object.entries(summary.transition_status_counts || {})) {
      acc.transition_status_counts[status] = (acc.transition_status_counts[status] || 0) + count;
    }
    acc.recommended_next_promotions.push(...(summary.recommended_next_promotions || []).map((row) => ({
      source: entry.source,
      ...row
    })));
    return acc;
  }, {
    migration_candidate_count: 0,
    already_generative_count: 0,
    reviewed_table_or_guardrail_count: 0,
    needs_registry_decision_count: 0,
    transition_bucket_counts: {},
    transition_status_counts: {},
    recommended_next_promotions: []
  });
  labelTransitionAggregate.recommended_next_promotions = labelTransitionAggregate.recommended_next_promotions.sort((a, b) => a.transition_priority - b.transition_priority || String(a.construction).localeCompare(String(b.construction))).slice(0, 12);
  labelTransitionAggregate.status = labelTransitionAggregate.needs_registry_decision_count ? "WARN" : "PASS";
  const runtimeConstructionRegistryAggregate = entries.reduce((acc, entry) => {
    const summary = runtimeConstructionRegistryAuditSummary(entry.analysis);
    acc.construction_row_count += summary.construction_row_count || 0;
    acc.active_row_count += summary.active_row_count || 0;
    acc.unregistered_row_count += summary.unregistered_row_count || 0;
    for (const row of summary.unregistered_rows || []) acc.unregistered_rows.push({ source: entry.source, ...row });
    for (const row of runtimeConstructionRegistryAuditRows(entry.analysis)) acc.constructions.add(row.construction);
    return acc;
  }, {
    construction_row_count: 0,
    active_row_count: 0,
    unregistered_row_count: 0,
    unregistered_rows: [],
    constructions: /* @__PURE__ */ new Set()
  });
  runtimeConstructionRegistryAggregate.unique_construction_count = runtimeConstructionRegistryAggregate.constructions.size;
  delete runtimeConstructionRegistryAggregate.constructions;
  runtimeConstructionRegistryAggregate.status = runtimeConstructionRegistryAggregate.unregistered_row_count ? "FAIL" : "PASS";
  runtimeConstructionRegistryAggregate.registry_version = RUNTIME_CONSTRUCTION_REGISTRY_VERSION;
  runtimeConstructionRegistryAggregate.policy = "Runtime audit only: emitted construction labels must be active. Linguistic status and evidence are validated from grammar/<linguistic-status>/*.md outside the plugin.";
  const semanticAcceptanceAggregate = renderedSources.reduce((acc, row) => {
    const status = row.semantic_acceptance_status || "REVIEW_REQUIRED";
    acc.status_counts[status] = (acc.status_counts[status] || 0) + 1;
    acc.blocker_count += row.semantic_acceptance_blocker_count || 0;
    acc.review_reason_count += row.semantic_acceptance_review_reason_count || 0;
    if (row.semantic_acceptance_outer_wrapper_blocked) acc.outer_wrapper_blocked_count += 1;
    const guard = row.semantic_acceptance || {};
    for (const blocker of guard.blockers || []) {
      if (blocker && blocker.code) acc.blocker_code_counts[blocker.code] = (acc.blocker_code_counts[blocker.code] || 0) + 1;
    }
    for (const reason of guard.review_reasons || []) {
      if (reason && reason.code) acc.review_code_counts[reason.code] = (acc.review_code_counts[reason.code] || 0) + 1;
    }
    return acc;
  }, {
    status_counts: {},
    blocker_count: 0,
    review_reason_count: 0,
    outer_wrapper_blocked_count: 0,
    blocker_code_counts: {},
    review_code_counts: {}
  });
  semanticAcceptanceAggregate.automatic_acceptance_count = 0;
  semanticAcceptanceAggregate.policy = "Semantic-acceptance status is an explicit review gate, not parser success. BLOCKED and REVIEW_REQUIRED rows cannot be accepted automatically; MANUAL_REVIEW_ELIGIBLE rows still require human semantic review.";
  return {
    runtime_version: CANTO_SPAN_RUNTIME_VERSION,
    status: runtimeConstructionRegistryAggregate.status === "FAIL" ? "FAIL" : errors.length || directiveWarnings.length || expectedFenceOpeners !== blocks.length || normalizationAggregate.status === "WARN" || registryAggregate.status === "WARN" || wrapperCoverageAggregate.status === "WARN" ? "WARN" : "PASS",
    normalization_audit_status: normalizationAggregate.status,
    normalization_audit: normalizationAggregate,
    jyutping_audit_status: jyutpingAggregate.status,
    jyutping_audit: jyutpingAggregate,
    registry_audit_status: registryAggregate.status,
    registry_audit: registryAggregate,
    learner_display_audit_status: learnerDisplayAggregate.status,
    learner_display_audit: learnerDisplayAggregate,
    learner_ui_hover_audit_status: learnerUiHoverAggregate.status,
    learner_ui_hover_audit: learnerUiHoverAggregate,
    wrapper_coverage_audit_status: wrapperCoverageAggregate.status,
    wrapper_coverage_audit: wrapperCoverageAggregate,
    label_transition_audit_status: labelTransitionAggregate.status,
    label_transition_audit: labelTransitionAggregate,
    runtime_construction_registry_audit_status: runtimeConstructionRegistryAggregate.status,
    runtime_construction_registry_audit: runtimeConstructionRegistryAggregate,
    semantic_acceptance_gate: semanticAcceptanceAggregate,
    canto_span_fence_openers_found: expectedFenceOpeners,
    canto_span_blocks_parsed: blocks.length,
    source_lines_with_diagnostics: entries.length,
    generated_diagnostics: entries.length,
    context_directive_count: contextDirectives.length,
    context_linked_source_count: entries.filter((entry) => Boolean(entry.context_directive)).length,
    context_directive_warnings: directiveWarnings,
    context_directives: contextDirectives,
    unique_sources: sourceCounts.size,
    duplicate_source_count: duplicateSources.length,
    duplicate_sources: duplicateSources,
    extraction_warnings: expectedFenceOpeners !== blocks.length ? [`Found ${expectedFenceOpeners} canto-span fence opener(s), but parsed ${blocks.length} closed block(s). Check for an unclosed fence.`] : [],
    diagnostic_errors: errors,
    rendered_sources: renderedSources
  };
}
function coverageRenderedSourceDisplayRow(row) {
  const out = {
    block_index: row.block_index,
    block_start_line: row.block_start_line,
    source_line_index: row.source_line_index,
    source: row.source,
    context_directive: row.context_directive || null,
    explicit_context: row.explicit_context || { supplied: false, turns: [] },
    parser_shadow_source: row.parser_shadow_source,
    normalization_trace: row.normalization_trace || [],
    parser_shadow_repairs: row.parser_shadow_repairs || [],
    parser_shadow_repair_count: row.parser_shadow_repair_count || 0,
    parser_shadow_repair_type_counts: row.parser_shadow_repair_type_counts || {},
    parser_shadow_repair_note_coverage_status: row.parser_shadow_repair_note_coverage_status || "PASS",
    missing_shadow_repair_note_count: row.missing_shadow_repair_note_count || 0,
    normalization_review_suggestions: row.normalization_review_suggestions || [],
    normalization_review_suggestion_display: row.normalization_review_suggestion_display || [],
    folded_lexical_repairs: row.folded_lexical_repairs || [],
    folded_lexical_repair_count: row.folded_lexical_repair_count || 0,
    normalization_audit_status: row.normalization_audit_status,
    normalization_trace_count: row.normalization_trace_count || 0,
    normalization_review_suggestion_count: row.normalization_review_suggestion_count || 0,
    unsafe_normalization_count: row.unsafe_normalization_count || 0,
    applied_review_suggestion_count: row.applied_review_suggestion_count || 0,
    raw_display_replacement_count: row.raw_display_replacement_count || 0,
    untraced_shadow_change_count: row.untraced_shadow_change_count || 0,
    raw_display_preservation_status: row.raw_display_preservation_status || "",
    top_constructions: row.top_constructions,
    top_child_constructions: row.top_child_constructions,
    top_child_construction_count: row.top_child_construction_count,
    review_count: row.review_count,
    parser_active_feature_bundle_count: row.parser_active_feature_bundle_count,
    semantic_acceptance: row.semantic_acceptance || null,
    semantic_acceptance_status: row.semantic_acceptance_status || "REVIEW_REQUIRED",
    semantic_acceptance_blocker_count: row.semantic_acceptance_blocker_count || 0,
    semantic_acceptance_review_reason_count: row.semantic_acceptance_review_reason_count || 0,
    semantic_acceptance_outer_wrapper_blocked: Boolean(row.semantic_acceptance_outer_wrapper_blocked),
    runtime_construction_registry_audit_status: row.runtime_construction_registry_audit_status || "PASS",
    runtime_construction_registry_unique_construction_count: row.runtime_construction_registry_unique_construction_count || 0,
    runtime_construction_registry_active_row_count: row.runtime_construction_registry_active_row_count || 0,
    runtime_construction_registry_unregistered_row_count: row.runtime_construction_registry_unregistered_row_count || 0,
    ...rootSpanCoverageFieldsFromDiagnosticSummary(row)
  };
  const semanticFlags = semanticReviewFlagsForSummary(row);
  if (semanticFlags.length) out.semantic_review_flags = semanticFlags;
  const auditFindings = {};
  if (row.normalization_audit_status && row.normalization_audit_status !== "PASS") {
    auditFindings.normalization = {
      status: row.normalization_audit_status,
      normalization_trace_count: row.normalization_trace_count || 0,
      parser_shadow_repair_count: row.parser_shadow_repair_count || 0,
      parser_shadow_repair_type_counts: row.parser_shadow_repair_type_counts || {},
      missing_shadow_repair_note_count: row.missing_shadow_repair_note_count || 0,
      review_suggestion_count: row.normalization_review_suggestion_count || 0,
      unsafe_normalization_count: row.unsafe_normalization_count || 0,
      applied_review_suggestion_count: row.applied_review_suggestion_count || 0,
      raw_display_replacement_count: row.raw_display_replacement_count || 0,
      untraced_shadow_change_count: row.untraced_shadow_change_count || 0,
      raw_display_preservation_status: row.raw_display_preservation_status || ""
    };
  }
  if (row.jyutping_audit_status && row.jyutping_audit_status !== "PASS") {
    auditFindings.jyutping = {
      status: row.jyutping_audit_status,
      missing_jyutping_count: row.missing_jyutping_count,
      invalid_jyutping_format_count: row.invalid_jyutping_format_count,
      syllable_count_mismatch_count: row.syllable_count_mismatch_count,
      dictionary_disagreement_count: row.dictionary_disagreement_count
    };
  }
  if (row.registry_audit_status && row.registry_audit_status !== "PASS") {
    auditFindings.registry = {
      status: row.registry_audit_status,
      invalid_learner_role_count: row.invalid_learner_role_count,
      invalid_slot_name_count: row.invalid_slot_name_count,
      invalid_construction_label_count: row.invalid_construction_label_count,
      invalid_parser_decision_label_count: row.invalid_parser_decision_label_count
    };
  }
  if (row.learner_display_audit_status && row.learner_display_audit_status !== "PASS") {
    auditFindings.learner_display = { status: row.learner_display_audit_status };
  }
  if (row.learner_ui_hover_audit_status && row.learner_ui_hover_audit_status !== "PASS") {
    auditFindings.learner_ui_hover = {
      status: row.learner_ui_hover_audit_status,
      flagged_hover_target_count: row.learner_ui_flagged_hover_target_count,
      missing_plain_gloss_count: row.learner_ui_missing_plain_gloss_count
    };
  }
  if (row.wrapper_coverage_audit_status && row.wrapper_coverage_audit_status !== "PASS") {
    auditFindings.wrapper_coverage = {
      status: row.wrapper_coverage_audit_status,
      wrapper_coverage_row_count: row.wrapper_coverage_row_count,
      unaccounted_wrapper_token_count: row.unaccounted_wrapper_token_count
    };
  }
  if (row.label_transition_audit_status && row.label_transition_audit_status !== "PASS") {
    auditFindings.label_transition = {
      status: row.label_transition_audit_status,
      migration_candidate_count: row.label_transition_migration_candidate_count,
      needs_registry_decision_count: row.label_transition_needs_registry_decision_count
    };
  }
  if (row.runtime_construction_registry_audit_status && row.runtime_construction_registry_audit_status !== "PASS") {
    auditFindings.runtime_construction_registry = {
      status: row.runtime_construction_registry_audit_status,
      unique_construction_count: row.runtime_construction_registry_unique_construction_count || 0,
      active_row_count: row.runtime_construction_registry_active_row_count || 0,
      unregistered_row_count: row.runtime_construction_registry_unregistered_row_count || 0
    };
  }
  if (Object.keys(auditFindings).length) out.audit_findings = auditFindings;
  return out;
}
function diagnosticCoverageDisplaySummary(summary) {
  const renderedSources = (summary.rendered_sources || []).map(coverageRenderedSourceDisplayRow);
  const semanticReviewCandidates = renderedSources.filter((row) => row.semantic_review_flags && row.semantic_review_flags.length);
  const auditFindings = {};
  if (summary.normalization_audit_status && summary.normalization_audit_status !== "PASS") auditFindings.normalization = summary.normalization_audit;
  if (summary.jyutping_audit_status && summary.jyutping_audit_status !== "PASS") auditFindings.jyutping = summary.jyutping_audit;
  if (summary.registry_audit_status && summary.registry_audit_status !== "PASS") auditFindings.registry = summary.registry_audit;
  if (summary.learner_display_audit_status && summary.learner_display_audit_status !== "PASS") auditFindings.learner_display = summary.learner_display_audit;
  if (summary.learner_ui_hover_audit_status && summary.learner_ui_hover_audit_status !== "PASS") auditFindings.learner_ui_hover = summary.learner_ui_hover_audit;
  if (summary.wrapper_coverage_audit_status && summary.wrapper_coverage_audit_status !== "PASS") auditFindings.wrapper_coverage = summary.wrapper_coverage_audit;
  if (summary.label_transition_audit_status && summary.label_transition_audit_status !== "PASS") auditFindings.label_transition = summary.label_transition_audit;
  if (summary.runtime_construction_registry_audit_status && summary.runtime_construction_registry_audit_status !== "PASS") auditFindings.runtime_construction_registry = summary.runtime_construction_registry_audit;
  const noteWarnings = [];
  if (summary.duplicate_source_count) noteWarnings.push({ kind: "duplicate_sources", duplicate_source_count: summary.duplicate_source_count, duplicate_sources: summary.duplicate_sources || [] });
  if ((summary.extraction_warnings || []).length) noteWarnings.push({ kind: "extraction_warnings", warnings: summary.extraction_warnings });
  if ((summary.diagnostic_errors || []).length) noteWarnings.push({ kind: "diagnostic_errors", errors: summary.diagnostic_errors });
  if ((summary.context_directive_warnings || []).length) noteWarnings.push({ kind: "context_directive_warnings", warnings: summary.context_directive_warnings });
  const out = {
    runtime_version: summary.runtime_version || CANTO_SPAN_RUNTIME_VERSION,
    policy: "Audit-detail policy: detailed PASS payloads are omitted, but audit-specific review reminders always surface. This inventory is not semantic acceptance; review normalization audit status, top_constructions, semantic_review_candidates, learner roles, and hover/gloss findings before choosing a patch.",
    review_reminders: diagnosticReviewReminderSummary([
      "coverage",
      "normalization",
      "normalization_audit",
      "jyutping",
      "registry",
      "learner_display",
      "learner_ui_hover",
      "wrapper_coverage",
      "label_transition",
      "runtime_construction_registry"
    ]),
    canto_span_fence_openers_found: summary.canto_span_fence_openers_found,
    canto_span_blocks_parsed: summary.canto_span_blocks_parsed,
    source_lines_with_diagnostics: summary.source_lines_with_diagnostics,
    generated_diagnostics: summary.generated_diagnostics,
    context_directive_count: summary.context_directive_count || 0,
    context_linked_source_count: summary.context_linked_source_count || 0,
    context_directives: summary.context_directives || [],
    unique_sources: summary.unique_sources,
    rendered_sources: renderedSources
  };
  if (Object.keys(auditFindings).length) out.actionable_audit_findings = auditFindings;
  if (noteWarnings.length) out.diagnostic_note_warnings = noteWarnings;
  if (semanticReviewCandidates.length) out.semantic_review_candidates = semanticReviewCandidates;
  out.final_review_reminder = "Final inventory reminder: regardless of PASS/WARN/FAIL state, inspect coverage counts, rendered sources, semantic-review candidates, parser-shadow normalization, top constructions, learner roles, and audit-specific reminders before accepting the note.";
  return out;
}
function formatDiagnosticCoverageMarkdown(summary) {
  return [
    "Diagnostic inventory",
    formatJsonBlock(diagnosticCoverageDisplaySummary(summary))
  ].join("\n");
}
function formatDiagnosticMarkdownForNote(markdown) {
  const collected = collectCantoSpanDiagnosticEntries(markdown);
  const coverage = noteDiagnosticCoverageSummary(markdown, collected);
  const diagnostics = collected.entries.map((entry) => formatDiagnosticMarkdown(entry.analysis));
  const parts = [formatDiagnosticCoverageMarkdown(coverage)];
  if (diagnostics.length) parts.push(diagnostics.join("\n\n---\n\n"));
  return parts.join("\n\n---\n\n");
}
function diagnosticFullJsonPayloadForNote(markdown, notePath = "", metadata = {}) {
  const collected = collectCantoSpanDiagnosticEntries(markdown);
  const coverage = noteDiagnosticCoverageSummary(markdown, collected);
  return {
    schema: "canto-span-note-full-diagnostics-json-v1",
    artifact_kind: "full_diagnostics",
    runtime_version: CANTO_SPAN_RUNTIME_VERSION,
    generated_at: metadata.generated_at || (/* @__PURE__ */ new Date()).toISOString(),
    export_id: metadata.export_id || "",
    note_path: notePath || "",
    companion_acceptance_summary_path: metadata.summary_path || "",
    coverage,
    diagnostic_errors: collected.errors || [],
    diagnostics: collected.entries.map((entry, diagnosticIndex) => {
      const analysis = entry.analysis;
      return {
        diagnostic_index: diagnosticIndex,
        block_index: entry.block_index,
        block_start_line: entry.block_start_line,
        block_end_line: entry.block_end_line,
        source_line_index: entry.source_line_index,
        source: entry.source,
        context_directive: entry.context_directive || null,
        explicit_context: analysis.explicit_context || { supplied: false, turns: [] },
        parser_shadow_source: analysis.parser_shadow_source || entry.source,
        summary: diagnosticSummary(analysis),
        normalization_audit: normalizationAuditSummary(analysis),
        parser_shadow_repairs: parserShadowRepairDisplayRows(analysis),
        folded_lexical_repairs: foldedLexicalRepairDisplayRows(analysis),
        normalization_review_suggestions: normalizationReviewSuggestionDisplayRows(analysis),
        jyutping_audit: jyutpingAuditSummary(analysis),
        registry_audit: registryAuditSummary(analysis),
        learner_display_audit: learnerDisplayAuditSummary(analysis),
        learner_ui_hover_audit: learnerUiHoverAuditSummary(analysis),
        learner_ui_hover_rows: learnerUiHoverAuditRows(analysis),
        wrapper_coverage_audit: wrapperCoverageAuditSummary(analysis),
        wrapper_coverage_warning_rows: nonPassWrapperCoverageRows(analysis),
        label_transition_audit: labelTransitionAuditSummary(analysis),
        label_transition_warning_rows: nonPassLabelTransitionRows(analysis),
        runtime_construction_registry_audit: runtimeConstructionRegistryAuditSummary(analysis),
        runtime_construction_registry_warning_rows: nonPassRuntimeConstructionRegistryRows(analysis),
        tokenization_before_construction_wrapping: diagnosticTokenRows(analysis),
        final_construction_tree: diagnosticFinalRows(analysis),
        legend: diagnosticLegend(),
        diagnostic_markdown: formatDiagnosticMarkdown(analysis)
      };
    }),
    final_review_reminder: "Full diagnostic artifact: use it whenever the acceptance summary marks a row for full review, and spot-check it whenever the compact structure or learner roles look suspicious."
  };
}
function compactRoleOverridesForAcceptance(roleOverrides) {
  const out = {};
  for (const [slot, value] of Object.entries(roleOverrides || {})) {
    const row = value || {};
    out[slot] = {
      label: row.label || "",
      syntax: row.syntax || "",
      slots: row.slots || []
    };
  }
  return out;
}
function compactContextualAffordancesForAcceptance(affordances) {
  return (affordances || []).map((row) => ({
    role: row.role || "",
    slot: row.slot || "",
    source: row.source || "",
    active_before_construction_wrapping: Boolean(row.active_before_construction_wrapping),
    active_in_final_construction: Boolean(row.active_in_final_construction),
    activated_by: row.activated_by || ""
  }));
}
function compactTraceDetailForAcceptance(row) {
  const detail = row && row.trace_detail || {};
  if (!Object.keys(detail).length) return null;
  const out = {};
  const scalarKeys = [
    "kind",
    "construction_type",
    "template_family",
    "subspan",
    "np_subtype",
    "fragment_subtype",
    "context_requirement_status",
    "antecedent_status",
    "context_turn_id",
    "question_id",
    "antecedent_span",
    "selected_alternative",
    "ambiguity_resolution_status",
    "conventional_unit_value_minutes",
    "approximation_scope",
    "approximation_marker_surface",
    "object_semantics",
    "head_recovery_status",
    "contextual_role_override",
    "original_role",
    "particle_cluster",
    "particle_cluster_root",
    "particle_cluster_member_index",
    "particle_cluster_member_count",
    "current_particle_surface",
    "current_scope_layer",
    "current_scope_function",
    "scope_direction",
    "surface_order_preserved",
    "cluster_order_status",
    "cluster_evidence_grade",
    "cluster_evidence_note",
    "fusion_status",
    "tone_certainty",
    "outer_particle_surface",
    "outer_scope_layer",
    "outer_scope_function",
    "host_construction",
    "learner_display_structure",
    "host_surface",
    "punctuation_hint",
    "subject_status",
    "subjectless_type",
    "hidden_subject_inserted",
    "environmental_subtype",
    "location_relation",
    "existential_subtype",
    "polarity",
    "have_relation",
    "locative_frame_subtype",
    "introduced_theme_surface",
    "introduced_participant_surface",
    "presentational_coda_surface",
    "nominal_predicate_type",
    "copula_status",
    "subject_surface",
    "predicate_surface",
    "measure_domain",
    "quantity_surface",
    "unit_surface",
    "dimension_surface",
    "passive_subtype",
    "participant_surface",
    "postmarker_participant_surface",
    "retained_patient_candidate_surface",
    "reason"
  ];
  for (const key of scalarKeys) {
    if (detail[key] !== void 0 && detail[key] !== "" && detail[key] !== null) out[key] = detail[key];
  }
  const arrayKeys = [
    "template",
    "assigned_slots",
    "surfaces",
    "missing_argument_slots",
    "missing_slot_details",
    "ambiguity_set",
    "not_claims",
    "reading_candidates",
    "semantic_review_flags",
    "learner_gloss_lines",
    "generated_slots",
    "visible_particle_sequence",
    "particle_sequence_jyutping",
    "particle_scope_layers",
    "particle_scope_functions"
  ];
  for (const key of arrayKeys) {
    if (Array.isArray(detail[key]) && detail[key].length) out[key] = detail[key];
  }
  if (detail.constraints && Object.keys(detail.constraints).length) out.constraints = detail.constraints;
  if (detail.role_overrides && Object.keys(detail.role_overrides).length) {
    out.role_overrides = compactRoleOverridesForAcceptance(detail.role_overrides);
  }
  const resolution = detail.contextual_role_affordance_resolution || {};
  if (Object.keys(resolution).length) {
    out.role_resolution = {
      lexical_default_role: resolution.lexical_default_role || "",
      active_role: resolution.active_role || "",
      active_affordance_source: resolution.active_affordance_source || ""
    };
  }
  const selection = detail.selection_decision || {};
  const excluded = selection.excluded_compositional_candidates || [];
  if (selection.chosen_registry_kind || excluded.length || selection.forced_compositional) {
    out.selection_decision = {
      rule: selection.rule || "",
      chosen: selection.chosen || "",
      chosen_registry_kind: selection.chosen_registry_kind || "",
      chosen_score: selection.chosen_score,
      forced_compositional: Boolean(selection.forced_compositional),
      excluded_compositional_candidates: excluded
    };
  }
  return Object.keys(out).length ? out : null;
}
function compactFinalTreeForAcceptance(rows) {
  return (rows || []).map((row) => {
    const out = {
      depth: row.depth,
      parent: row.parent || "",
      surface: row.surface || "",
      kind: row.kind || "",
      trace: row.trace || ""
    };
    if (row.display_surface && row.display_surface !== row.surface) out.display_surface = row.display_surface;
    if (row.parser_surface && row.parser_surface !== row.surface) out.parser_surface = row.parser_surface;
    if (row.construction) out.construction = row.construction;
    if (row.label) out.label = row.label;
    if (row.role) out.role = row.role;
    if (row.syntax) out.syntax = row.syntax;
    if (row.jyutping) out.jyutping = row.jyutping;
    if ((row.slots || []).length) out.slots = row.slots;
    if ((row.learner_display_slots || []).length) out.learner_display_slots = row.learner_display_slots;
    if ((row.internal_slots_hidden_from_learner_display || []).length) {
      out.internal_slots_hidden_from_learner_display = row.internal_slots_hidden_from_learner_display;
    }
    const affordances = compactContextualAffordancesForAcceptance(row.contextual_role_affordances);
    if (affordances.length) out.contextual_role_affordances = affordances;
    const traceDetail = compactTraceDetailForAcceptance(row);
    if (traceDetail) out.trace_detail = traceDetail;
    if (row.runtime_registry) out.runtime_registry = { active: Boolean(row.runtime_registry.active) };
    return out;
  });
}
function auditStatusesForAcceptanceDiagnostic(diagnostic) {
  return {
    normalization: diagnostic.normalization_audit && diagnostic.normalization_audit.status || "PASS",
    jyutping: diagnostic.jyutping_audit && diagnostic.jyutping_audit.status || "PASS",
    registry: diagnostic.registry_audit && diagnostic.registry_audit.status || "PASS",
    learner_display: diagnostic.learner_display_audit && diagnostic.learner_display_audit.status || "PASS",
    learner_ui_hover: diagnostic.learner_ui_hover_audit && diagnostic.learner_ui_hover_audit.status || "PASS",
    wrapper_coverage: diagnostic.wrapper_coverage_audit && diagnostic.wrapper_coverage_audit.status || "PASS",
    label_transition: diagnostic.label_transition_audit && diagnostic.label_transition_audit.status || "PASS",
    runtime_construction_registry: diagnostic.runtime_construction_registry_audit && diagnostic.runtime_construction_registry_audit.status || "PASS"
  };
}
function fullReviewReasonsForAcceptanceDiagnostic(diagnostic, compactTree, auditStatuses) {
  const summary = diagnostic.summary || {};
  const reasons = [];
  const top = summary.top_constructions || [];
  const semanticFlags = summary.semantic_review_flags || [];
  if (summary.semantic_acceptance_status === "BLOCKED") reasons.push("semantic_acceptance_blocked");
  if (summary.semantic_acceptance_status === "REVIEW_REQUIRED") reasons.push("semantic_acceptance_review_required");
  if (summary.semantic_acceptance_outer_wrapper_blocked) reasons.push("outer_wrapper_contains_semantic_blocker");
  if (!top.length) reasons.push("no_top_construction");
  if (top.length > 1) reasons.push("multiple_top_constructions");
  if ((summary.top_child_construction_count || 0) > 0 || (compactTree || []).some((row) => row.kind === "construction" && row.trace_detail && row.trace_detail.particle_cluster)) {
    reasons.push("nested_or_wrapper_structure");
  }
  if ((summary.review_count || 0) > 0) reasons.push("review_trace_present");
  if (semanticFlags.length) reasons.push(...semanticFlags.map((flag) => `semantic:${flag}`));
  if ((summary.missing_argument_slots || []).length) reasons.push("missing_argument_slots");
  if (summary.context_requirement_status && summary.context_requirement_status !== "context_not_required") {
    reasons.push(`context:${summary.context_requirement_status}`);
  }
  if (summary.selected_alternative) reasons.push("ambiguity_or_alternative_selected");
  if ((summary.parser_active_feature_bundle_count || 0) > 0) reasons.push("parser_active_feature_bundle");
  if ((summary.normalization_trace || []).length) reasons.push("normalization_trace_present");
  if ((summary.parser_shadow_repairs || []).length) reasons.push("parser_shadow_repairs_present");
  if ((summary.folded_lexical_repairs || []).length) reasons.push("folded_lexical_repairs_present");
  if ((summary.normalization_review_suggestions || []).length) reasons.push("normalization_review_suggestions_present");
  for (const [name, status] of Object.entries(auditStatuses || {})) {
    if (status !== "PASS") reasons.push(`audit:${name}:${status}`);
  }
  const reviewTraceKinds = /* @__PURE__ */ new Set([
    "legacy_surface_rule",
    "surface_specific_phrase_rule",
    "special_ambiguity_rule",
    "protected_formula_table",
    "construction_template",
    "governed_discourse_wrapper"
  ]);
  for (const row of compactTree || []) {
    if (reviewTraceKinds.has(row.trace)) reasons.push(`trace:${row.trace}`);
    const detail = row.trace_detail || {};
    const roleResolution = detail.role_resolution || {};
    if (roleResolution.lexical_default_role && roleResolution.active_role && roleResolution.lexical_default_role !== roleResolution.active_role) {
      reasons.push("contextual_role_change");
    }
    if (roleResolution.lexical_default_role || roleResolution.active_role) {
      const affordances = row.contextual_role_affordances || [];
      const lexicalRows = affordances.filter((item) => item.source === "lexical_default");
      const activeRows = affordances.filter((item) => item.active_in_final_construction);
      const lexicalDefaultMissing = roleResolution.lexical_default_role && !lexicalRows.some((item) => item.role === roleResolution.lexical_default_role);
      const activeMislabelledAsLexical = roleResolution.active_role && roleResolution.active_role !== roleResolution.lexical_default_role && lexicalRows.some((item) => item.role === roleResolution.active_role);
      const activeProvenanceMissing = roleResolution.active_role && !activeRows.some((item) => item.role === roleResolution.active_role);
      if (lexicalDefaultMissing || activeMislabelledAsLexical || activeProvenanceMissing) {
        reasons.push("contextual_role_provenance_mismatch");
      }
    }
    if (detail.selection_decision) reasons.push("lexical_selection_decision");
  }
  return Array.from(new Set(reasons));
}
function acceptanceCoverageFromFull(fullPayload) {
  const coverage = fullPayload.coverage || {};
  return {
    status: coverage.status || "PASS",
    runtime_version: coverage.runtime_version || fullPayload.runtime_version || CANTO_SPAN_RUNTIME_VERSION,
    canto_span_fence_openers_found: coverage.canto_span_fence_openers_found || 0,
    canto_span_blocks_parsed: coverage.canto_span_blocks_parsed || 0,
    source_lines_with_diagnostics: coverage.source_lines_with_diagnostics || 0,
    generated_diagnostics: coverage.generated_diagnostics || 0,
    context_directive_count: coverage.context_directive_count || 0,
    context_linked_source_count: coverage.context_linked_source_count || 0,
    context_directives: coverage.context_directives || [],
    unique_sources: coverage.unique_sources || 0,
    duplicate_source_count: coverage.duplicate_source_count || 0,
    duplicate_sources: coverage.duplicate_sources || [],
    extraction_warnings: coverage.extraction_warnings || [],
    diagnostic_errors: coverage.diagnostic_errors || [],
    context_directive_warnings: coverage.context_directive_warnings || [],
    audit_statuses: {
      normalization: coverage.normalization_audit_status || "PASS",
      jyutping: coverage.jyutping_audit_status || "PASS",
      registry: coverage.registry_audit_status || "PASS",
      learner_display: coverage.learner_display_audit_status || "PASS",
      learner_ui_hover: coverage.learner_ui_hover_audit_status || "PASS",
      wrapper_coverage: coverage.wrapper_coverage_audit_status || "PASS",
      label_transition: coverage.label_transition_audit_status || "PASS",
      runtime_construction_registry: coverage.runtime_construction_registry_audit_status || "PASS"
    },
    audit_aggregates: {
      normalization: coverage.normalization_audit || {},
      jyutping: coverage.jyutping_audit || {},
      registry: coverage.registry_audit || {},
      learner_display: coverage.learner_display_audit || {},
      learner_ui_hover: coverage.learner_ui_hover_audit || {},
      wrapper_coverage: coverage.wrapper_coverage_audit || {},
      label_transition: coverage.label_transition_audit || {},
      runtime_construction_registry: coverage.runtime_construction_registry_audit || {},
      semantic_acceptance: coverage.semantic_acceptance_gate || {}
    }
  };
}
function diagnosticAcceptanceSummaryPayloadFromFull(fullPayload, metadata = {}) {
  const rows = (fullPayload.diagnostics || []).map((diagnostic, diagnosticIndex) => {
    const compactTree = compactFinalTreeForAcceptance(diagnostic.final_construction_tree || []);
    const auditStatuses = auditStatusesForAcceptanceDiagnostic(diagnostic);
    const reasons = fullReviewReasonsForAcceptanceDiagnostic(diagnostic, compactTree, auditStatuses);
    const summary = diagnostic.summary || {};
    return {
      diagnostic_index: diagnostic.diagnostic_index !== void 0 ? diagnostic.diagnostic_index : diagnosticIndex,
      block_index: diagnostic.block_index,
      block_start_line: diagnostic.block_start_line,
      block_end_line: diagnostic.block_end_line,
      source_line_index: diagnostic.source_line_index,
      source: diagnostic.source,
      context_directive: diagnostic.context_directive || null,
      explicit_context: diagnostic.explicit_context || { supplied: false, turns: [] },
      parser_shadow_source: diagnostic.parser_shadow_source || diagnostic.source,
      raw_display_preservation_status: diagnostic.normalization_audit && diagnostic.normalization_audit.raw_display_preservation_status || "PASS",
      normalization_trace: summary.normalization_trace || [],
      parser_shadow_repairs: diagnostic.parser_shadow_repairs || [],
      folded_lexical_repairs: diagnostic.folded_lexical_repairs || [],
      normalization_review_suggestions: diagnostic.normalization_review_suggestions || [],
      top_constructions: summary.top_constructions || [],
      top_child_constructions: summary.top_child_constructions || [],
      trace_summary: summary.trace_summary || {},
      template_family_summary: summary.template_family_summary || {},
      review_count: summary.review_count || 0,
      parser_active_feature_bundle_count: summary.parser_active_feature_bundle_count || 0,
      context: {
        requirement_status: summary.context_requirement_status || "context_not_required",
        antecedent_status: summary.antecedent_status || "not_applicable",
        missing_argument_slots: summary.missing_argument_slots || [],
        context_turn_id: summary.context_turn_id || "",
        question_id: summary.question_id || "",
        antecedent_span: summary.antecedent_span || "",
        selected_alternative: summary.selected_alternative || ""
      },
      semantic_review_flags: summary.semantic_review_flags || [],
      semantic_acceptance: summary.semantic_acceptance || null,
      semantic_acceptance_status: summary.semantic_acceptance_status || "REVIEW_REQUIRED",
      semantic_acceptance_blocker_count: summary.semantic_acceptance_blocker_count || 0,
      semantic_acceptance_review_reason_count: summary.semantic_acceptance_review_reason_count || 0,
      semantic_acceptance_outer_wrapper_blocked: Boolean(summary.semantic_acceptance_outer_wrapper_blocked),
      topic_chain: summary.topic_chain_id ? {
        id: summary.topic_chain_id,
        status: summary.topic_chain_status || "",
        antecedent_surface: summary.topic_antecedent_surface || "",
        antecedent_source: summary.topic_antecedent_source || "",
        antecedent_semantic_domains: summary.topic_antecedent_semantic_domains || [],
        topic_frame_status: summary.topic_frame_status || "",
        topic_frame_linker_surface: summary.topic_frame_linker_surface || "",
        topic_frame_domain_surface: summary.topic_frame_domain_surface || "",
        relational_frame_status: summary.relational_frame_status || "",
        relational_coverb_linker_surface: summary.relational_coverb_linker_surface || "",
        relational_coverb_domain_surface: summary.relational_coverb_domain_surface || "",
        linked_null_object_count: summary.linked_null_object_count || 0,
        linked_predicate_surfaces: summary.linked_predicate_surfaces || [],
        unresolved_predicate_surfaces: summary.unresolved_predicate_surfaces || [],
        null_object_link: summary.null_object_link || ""
      } : null,
      particle_cluster: summary.particle_cluster_order_status ? {
        order_status: summary.particle_cluster_order_status,
        visible_sequence: summary.visible_particle_sequence || [],
        scope_layers: summary.particle_scope_layers || [],
        scope_functions: summary.particle_scope_functions || [],
        scope_direction: summary.particle_cluster_scope_direction || "",
        fusion_status: summary.particle_cluster_fusion_status || ""
      } : null,
      root_span_coverage: rootSpanCoverageObjectFromDiagnosticSummary(summary),
      audit_statuses: auditStatuses,
      compact_final_construction_tree: compactTree,
      full_review: {
        required: reasons.length > 0,
        reasons,
        full_diagnostics_path: metadata.full_path || fullPayload.companion_full_diagnostics_path || "",
        full_diagnostic_index: diagnostic.diagnostic_index !== void 0 ? diagnostic.diagnostic_index : diagnosticIndex
      }
    };
  });
  const rowsRequiringFullReview = rows.filter((row) => row.full_review.required).map((row) => ({
    diagnostic_index: row.diagnostic_index,
    source: row.source,
    reasons: row.full_review.reasons
  }));
  return {
    schema: "canto-span-note-acceptance-summary-json-v1",
    artifact_kind: "acceptance_summary",
    runtime_version: fullPayload.runtime_version || CANTO_SPAN_RUNTIME_VERSION,
    generated_at: fullPayload.generated_at || metadata.generated_at || (/* @__PURE__ */ new Date()).toISOString(),
    export_id: fullPayload.export_id || metadata.export_id || "",
    note_path: fullPayload.note_path || "",
    companion_full_diagnostics_path: metadata.full_path || "",
    inspection_policy: {
      summary_is_not_semantic_acceptance: true,
      row_coverage_guarantee: "Every generated diagnostic has exactly one summary row with compact final structure, learner roles, Jyutping, context state, audit statuses, and a pointer into the full artifact.",
      required_process: [
        "Inspect every summary row, not only rows marked for full review.",
        "Open the matching full diagnostic whenever full_review.required is true.",
        "Open the matching full diagnostic whenever compact structure, role assignment, Jyutping, context linkage, doctrine fit, or runtime-label registration looks suspicious even if no trigger fired.",
        "Spot-check at least one row not marked for full review against the full artifact to verify compact/full alignment.",
        "Do not accept a patch solely because audits pass.",
        "Consult the matching grammar/<linguistic-status>/*.md note for linguistic status, sources, speaker scope, boundaries, and promotion eligibility; the runtime export intentionally omits these authoring records.",
        "Do not treat parser fixtures, generated probes, or corpus candidate hits as independent validation of a Cantonese grammar claim."
      ],
      conservative_full_review_triggers: [
        "semantic review flag or review trace",
        "missing or incompatible context",
        "ambiguity selection",
        "normalization or parser-shadow activity",
        "non-PASS audit",
        "nested/wrapper structure",
        "contextual learner-role change",
        "review-oriented trace family",
        "parser-active feature bundle",
        "missing or multiple top constructions",
        "partial root span with unwrapped nonpunctuation material",
        "contextual-role provenance mismatch",
        "grammar legitimacy is provisional, pending, heuristic, lexicalized-only, or unsupported"
      ]
    },
    coverage: acceptanceCoverageFromFull(fullPayload),
    diagnostic_row_count: rows.length,
    full_review_required_count: rowsRequiringFullReview.length,
    rows_requiring_full_review: rowsRequiringFullReview,
    rows,
    final_review_reminder: "This summary is a loss-aware inspection index, not a replacement for the full diagnostics. Review all rows and drill into the companion full file whenever required or suspicious."
  };
}
function diagnosticExportPathsForNotePath(notePath = "") {
  const path = String(notePath || "").trim();
  if (!path) return { summaryPath: "", fullPath: "" };
  const directory = path.includes("/") ? path.slice(0, path.lastIndexOf("/") + 1) : "";
  const filename = path.includes("/") ? path.slice(path.lastIndexOf("/") + 1) : path;
  const basename = filename.replace(/\.[^/.]+$/, "") || filename || "untitled";
  return {
    summaryPath: `${directory}${basename}.canto-span-acceptance-summary.json`,
    fullPath: `${directory}${basename}.canto-span-full-diagnostics.json`
  };
}
function diagnosticExportPayloadsForNote(markdown, notePath = "") {
  const paths = diagnosticExportPathsForNotePath(notePath);
  const generatedAt = (/* @__PURE__ */ new Date()).toISOString();
  const exportId = `canto-span-${CANTO_SPAN_RUNTIME_VERSION}-${generatedAt}-${notePath || "untitled"}`;
  const full = diagnosticFullJsonPayloadForNote(markdown, notePath, {
    generated_at: generatedAt,
    export_id: exportId,
    summary_path: paths.summaryPath
  });
  full.companion_acceptance_summary_path = paths.summaryPath;
  const summary = diagnosticAcceptanceSummaryPayloadFromFull(full, {
    generated_at: generatedAt,
    export_id: exportId,
    full_path: paths.fullPath
  });
  return { paths, summary, full };
}
function formatNoteMarkdownWithDiagnostics(markdown, notePath = "") {
  const noteText = String(markdown || "");
  const diagnostics = formatDiagnosticMarkdownForNote(noteText);
  if (!diagnostics.trim()) return noteText;
  const header = [
    "# Canto Span note diagnostics",
    notePath ? `Note: ${notePath}` : "",
    "",
    diagnostics,
    "",
    "---",
    "",
    "# Original note",
    ""
  ].filter((line, index) => line || index !== 1).join("\n");
  return `${header}${noteText}`;
}
function splitJyutping(jyutping) {
  return String(jyutping || "").trim().split(/\s+/).filter(Boolean);
}
module.exports = createCantoSpanPlugin({
  CANTO_SPAN_RUNTIME_VERSION,
  analyzeLine,
  normalizeSurface,
  formatDiagnosticMarkdownForNote,
  diagnosticExportPayloadsForNote,
  formatNoteMarkdownWithDiagnostics,
  formatDiagnosticMarkdown,
  diagnosticSummary,
  jyutpingAuditSummary,
  foldedLexicalRepairDisplayRows,
  normalizationReviewSuggestionDisplayRows,
  learnerUiHoverAuditSummary,
  learnerUiHoverAuditRows,
  diagnosticTokenRows,
  diagnosticFinalRows,
  diagnosticLegend,
  safeClass,
  nodeDisplaySurface,
  learnerUiHoverTitleForToken,
  learnerUiHoverTitleForConstruction,
  learnerUiHoverTitleForConstructionLayer,
  shouldCollapseClauseSequenceForDisplay,
  shouldCollapseGreedyWrapperForDisplay,
  parserShadowRepairDisplayRows,
  parserShadowRepairKindLabel,
  splitJyutping
});
