#!/usr/bin/env node
const fs = require('fs');
const path = require('path');
const vm = require('vm');
function parseArgs(argv){const o={};for(let i=0;i<argv.length;i++){if(argv[i].startsWith('--')){o[argv[i].slice(2)]=argv[i+1];i++;}}if(!o.packet||!o.output)throw new Error('Usage: run-lexicon-baseline-probes.js --packet FILE --output FILE');return o;}
function parseTsv(text){const lines=text.replace(/^\uFEFF/,'').trimEnd().split(/\r?\n/);const h=lines.shift().split('\t');return lines.filter(Boolean).map(line=>{const v=line.split('\t');return Object.fromEntries(h.map((x,i)=>[x,v[i]||'']));});}
function loadApi(mainPath){class Plugin{} class PluginSettingTab{} class Setting{} class Notice{} const moduleShim={exports:{}};const context={module:moduleShim,exports:moduleShim.exports,require:(id)=>id==='obsidian'?{Plugin,PluginSettingTab,Setting,Notice}:require(id),console,setTimeout,clearTimeout,Buffer};vm.runInNewContext(fs.readFileSync(mainPath,'utf8')+'\nmodule.exports.__researchApi={analyzeLine,diagnosticFinalRows,diagnosticSummary,rootSpanCoverageForDiagnostic,runtimeVersion:CANTO_SPAN_RUNTIME_VERSION};',context,{filename:mainPath});return moduleShim.exports.__researchApi;}
function compact(row){return {depth:row.depth,kind:row.kind,surface:row.surface,construction:row.construction||row.type||'',internal_construction:row.internal_construction||'',parent:row.parent||'',internal_parent:row.internal_parent||'',role:row.role||row.label||'',syntax:row.syntax||'',jyutping:row.jyutping||''};}
const args=parseArgs(process.argv.slice(2));const root=path.resolve(__dirname,'..');const api=loadApi(path.join(root,'main.js'));const packet=parseTsv(fs.readFileSync(path.resolve(args.packet),'utf8'));
const items=packet.map(item=>{const analysis=api.analyzeLine(item.surface);const rows=api.diagnosticFinalRows(analysis);return {...item,root_coverage:api.rootSpanCoverageForDiagnostic(rows),semantic_summary:api.diagnosticSummary(analysis),rows:rows.map(compact)};});
const result={schema:'canto-span-lexicon-baseline-observation-v1',status:'OBSERVATION_ONLY',runtime_version:api.runtimeVersion,packet:path.basename(args.packet),items};fs.mkdirSync(path.dirname(path.resolve(args.output)),{recursive:true});fs.writeFileSync(path.resolve(args.output),JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({status:result.status,item_count:items.length,output:path.resolve(args.output)},null,2));
