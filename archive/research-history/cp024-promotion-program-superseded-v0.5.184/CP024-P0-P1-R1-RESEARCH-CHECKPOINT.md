# CP024-P0/P1-R1 productive-promotion checkpoint

Date: 2026-07-20  
Accepted runtime: **v0.5.182**  
Runtime behavior changed: **no**  
`supported_productive` delta: **0**

## Work completed

1. Defined the standards-preserving completion endpoint for all 179 active labels.
2. Classified every runtime label and all 53 canonical families by required terminal route.
3. Established a two-track program: narrow promotion throughput plus dependency unblocking.
4. Selected `OvertHeadDemonstrativeClassifierNP` as the first throughput target.
5. Froze a 20-case visible research packet and measured the unchanged v0.5.182 baseline.
6. Froze exact source claims, exclusions, a future implementation blueprint, and rollback conditions.
7. Froze L45 valency decomposition and inventoried current verb/valency tags without treating them as linguistic evidence.
8. Produced an external evaluator handoff for independently sealed prospective custody.

## DEMO01 baseline

- exact required spans: **8/9**
- required absent: **1** (`呢隻貓`)
- forbidden leakage: **2/8** (headless `呢個`, `呢本`)
- full-root coverage: **16/20**

This is parser observation only. It does not establish productivity or authorize implementation.

## Promotion blockers

### Assistant-owned and active

- preserve the five-record exact natural-attestation adjudication and its limitations;
- preserve controlled variation without inferring unrestricted classifier/noun compatibility;
- prepare the smallest candidate after custody exists;
- run focused, semantic, aggregate, documentation, and packaging gates;
- preserve headless, overt-numeral, 啲, wh, incomplete, and ambiguous exclusions.

### External/user-controlled

1. **Independent prospective custody** — required before implementation. Use the external handoff with a separate evaluator; return only the sealed archive and checksum.
2. **Obsidian render review** — required after a candidate exists. Render the locked note in the actual plugin and return diagnostics/review results.
3. **Native conflict review** — only when an exact-family conflict is documented. Not currently triggered for DEMO01.

## Consequence if external custody is unavailable

Research, source mapping, decomposition, visible packets, and non-grammar cleanup can continue. No new construction can be accepted as `supported_productive` under the current prospective-evaluation standard. Reusing visible cases or letting the implementer create its own “unseen” set would lower the standard and is prohibited.

## Next internal action

Rank the completed L45 constructor-dependency map into exact relation packets. Do not implement DEMO01 until a sealed external custody archive exists. The natural-attestation gate is closed PASS.
