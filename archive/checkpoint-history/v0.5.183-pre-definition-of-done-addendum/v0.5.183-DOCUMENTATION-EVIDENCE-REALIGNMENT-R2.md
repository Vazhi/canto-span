# v0.5.183 documentation and evidence-standard realignment R2

Date: 2026-07-20  
Software baseline: **v0.5.182 accepted**  
Active parser candidate: **v0.5.183-cp024-p1-demo01-r1**  
Parser behavior changed: **no**

## Purpose

This checkpoint consolidates the current documentation around the stricter evidence standard and removes active wording that treated older acceptance, corpus counts, or internal test completion as sufficient evidence of productive Cantonese grammar.

## Status corrections

The current `supported_productive` count is **0**.

The previous classifications are withdrawn pending retroactive review:

- `PostverbalZoPerfectiveVP` → `provisional_reaudit`
- `ResourceUseLaiFunctionRelation` → `provisional_reaudit`

The accepted v0.5.182 software behavior remains the baseline. Its earlier render, regression, and held-out results remain implementation evidence, but they do not preserve productive linguistic status under the new standard.

## Binding evidence changes

- Every formerly provisional or productive construction must have its cited sources reopened and independently checked.
- Raw corpus hit counts have zero evidence weight until each hit is manually classified.
- At least two independent native Cantonese speakers must review the same relevant positive and negative contrasts before productive acceptance.
- Internal render, regression, and held-out results are reported separately from linguistic confidence.
- Confidence for all provisional and re-audit claims is suspended as `not_assigned_pending_reaudit`.
- No previous acceptance is grandfathered.

## DEMO01 effect

`OvertHeadDemonstrativeClassifierNP` remains provisional. Its current parser results are preserved, and its Obsidian render may proceed, but the sealed held-out ZIP must remain unopened until:

1. source re-verification is complete;
2. a second independent speaker has reviewed the same core contrasts;
3. the runtime heuristic and documentation have been compared;
4. render review passes.

## Retirement program

A first retirement queue now identifies **10** labels with:

- zero accepted fixtures;
- zero runtime code references;
- an existing dormant-label disposition.

They are candidates for removal from the active registry after confirming that no alias, compatibility, serialized-diagnostic, or documentation dependency remains.

## New or rewritten authority files

- `docs/current/STATUS-AND-CONFIDENCE.md`
- `docs/current/DOCTRINE.md`
- `docs/current/EVIDENCE-AND-PROVENANCE.md`
- `docs/current/WORKFLOW.md`
- `docs/current/VALIDATION-AND-ACCEPTANCE.md`
- `docs/current/PROJECT-STATE.md`
- `docs/research/CONSTRUCTION-EVIDENCE-REAUDIT-LEDGER-v0.5.183-R1.tsv`
- `docs/research/SECOND-SPEAKER-REVIEW-QUEUE-v0.5.183-R1.tsv`
- `docs/research/DORMANT-LABEL-RETIREMENT-QUEUE-v0.5.183-R1.tsv`
- `docs/research/ACTIVE-SOURCE-ACCOUNTING-v0.5.183-cp024-p1-demo01-r2.tsv`

The earlier active documentation and generic audit tools are preserved under `archive/`.

## Known remaining mismatch

The v0.5.182/v0.5.183 runtime's embedded legitimacy metadata still reports two `supported_productive` rows. This checkpoint does not alter `main.js`, because changing the frozen candidate would change its hash and render target. Current governance records the mismatch explicitly. A future candidate must correct the metadata and revalidate diagnostics without silently changing parse behavior.

## Validation

- documentation and evidence realignment audit: **36/36 PASS**;
- current focused documentation audit: **PASS**;
- grammar legitimacy governance audit: **PASS**;
- source-accounting audit: **PASS**;
- active Markdown links: **0 broken**;
- changed JSON files: **valid**;
- runtime hashes: **unchanged**;
- runtime ZIP: **exactly three files and byte-identical to recovery runtime**;
- sealed external held-out set: **absent from recovery package**;
- pre-realignment archive: **28/28 files hash-verified**.
