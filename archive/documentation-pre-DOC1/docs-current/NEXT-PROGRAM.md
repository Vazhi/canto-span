> **Immediate next gate — I02 authorization:** `EP-CP022-I1A-I02-D1` is locked. Do not implement until the user explicitly authorizes this I02 scope. After authorization, change only the internal graph/edge/member architecture with intended semantic change `NONE`; render review must pass before opening the six held-out cases. I01 remains blocked and separately unauthorized.

# Next Program

## Immediate task

```text
Install and render CP021B-LX1 v0.5.176.
Review the focused note first, then the full study-suite note.
Do not accept the lexical checkpoint or resume grammar research until rendered output is confirmed.
```

Base research checkpoint is `CP021B-R31`; 10 language labels remain unmapped.

## Render-review inputs

- `../../render-review/CP021B-LX1-FOCUSED-RENDER-REVIEW.md`
- `../../render-review/CP021B-LX1-FULL-STUDY-SUITE-RENDER-REVIEW.md`

The focused review checks `咪`, `定`, `住`, token boundaries, roles, and Jyutping. The full suite checks broad learner-facing display. Grammar acceptance remains frozen.

## Research task after render confirmation

```text
Resume from CP021B-R31.
Rank the 10 remaining unmapped language labels and select one coherent family.
Return to evidence-only research; do not carry lexical render observations into grammar promotion.
```

## Completed rendered disposition

- Decision: **PASS**
- Review: **60/60 summary + 60/60 full**
- Verification: **1999/1999 PASS**
- Intended role corrections: **5/5 confirmed**
- Frozen routing and semantic inventories: **preserved**
- Productive grammar promotions: **0**

## Candidate constraints

- No legacy language claim is promotion-eligible without authority-origin reconstruction.
- Future grammar is not categorically frozen, but CP021A itself authorizes no implementation.
- `PassivePermissiveRelation` remains provisional.
- `supported_productive` remains zero.
- Native analysis validation remains zero.
- Rendered acceptance does not constitute productive-grammar promotion.
- Cosmetic learner-role polishing remains bundled; materially false learner semantics may be corrected separately under the documented harm exception.

## Earliest possible revision

After explicit confirmation, a separately versioned candidate may address the recorded misses. The revealed 36 rows can be used as named development/regression evidence but cannot remain held out. A new independent partition must be frozen before implementation and opened only after the next candidate is code-complete and hash-frozen. Rendered Obsidian review remains required only after the headless acceptance gates pass.

## Stop conditions

Stop a runtime branch for inaccessible material evidence or any failure to produce verified recovery artifacts. CP021B-IM1 is complete with `REVISE`. Do not modify the frozen v0.5.175 runtime, count the revealed partition as held out again, fill omitted response fields by inference, treat the AI adjudicator as an independent human expert, or begin a revision before user confirmation.
