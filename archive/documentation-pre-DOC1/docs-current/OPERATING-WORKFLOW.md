# Operating Workflow

> **Current authority — 2026-07-17 / CP020R2-RA1:** Use the authority-origin lifecycle. Runtime v0.5.174 is accepted after complete rendered review; there is no active candidate. The relation remains provisional and nonproductive.

## Local paths

```text
plugin:           $HOME/Sync/Everything/.obsidian/plugins/canto-span
notes:            $HOME/Sync/Everything/Canto Span
acceptance notes: $HOME/Sync/Everything/Canto Span/_diagnostics/acceptance
```

## Current audit-mode loop

1. Read `HANDOFF.md`, `CURRENT-STATE.md`, the master audit method, the recovery protocol, and the latest ledger.
2. Verify accepted baseline, active candidate, and freeze state.
3. Select one bounded, high-impact phenomenon; do not begin from a preferred parser claim.
4. Register a neutral question, discovery protocol, inclusion/exclusion rules, competing terminology, and stop criteria.
5. Create and export an in-progress recovery checkpoint before extensive research.
6. Screen broadly and record included, excluded, inaccessible, contradictory, and null-result sources.
7. Extract source propositions and locators before writing the project claim.
8. Export the frozen source-screening and proposition-extraction state.
9. Synthesize the narrowest external-evidence-origin claim and create explicit claim-source edges.
10. Separate language, attestation, representation, heuristic, and lexical claims.
11. Freeze design and held-out partitions; build positive, negative, ambiguous, and competing-analysis evidence.
12. Run parser probes without changing grammar.
13. Inspect every summary row and required/suspicious full diagnostic.
14. Assign dispositions and record remediation or implementation eligibility; do not implement inside the research batch.
15. Update authoritative documentation and the queue, export verified recovery artifacts, and provide download links.
16. Only then begin implementation or another research branch.

## Current prohibited cycle

Do not perform:

```text
research
→ infer a correction
→ edit parser
→ update expected fixtures
→ report PASS
```

The legacy path is:

```text
research
→ adjudicate evidence
→ document recommendation
→ downgrade/quarantine/retire or retain only source-supported scope
→ separate implementation only if authorized
```

A future authority-origin construction uses this path:

```text
neutral question
→ broad source screen and proposition extraction
→ source-derived claim and explicit edges
→ pass immutable held-out, negative-boundary, competing-analysis, aggregate-regression, and recovery gates
→ record separate acceptance
```

When a new analysis directly replaces an invalid construction, document the deletion/supersession path. Do not invent a replacement dependency merely to admit unrelated source-backed grammar.

## Task selection

Prioritize claims by:

- accepted runtime reach;
- learner-facing impact;
- evidence deficit;
- likelihood of wrapper/grammar confusion;
- known false-clean analyses;
- availability of independent sources;
- boundary complexity;
- risk of contaminating future stages.

## Validation surface

Keep a slim stable tool surface: one current focused gate, one aggregate inherited regression suite, and durable research/audit utilities. Do not add one runner per patch or source.

## Packaging exclusions

Do not package restricted corpora, raw completed native forms, virtual environments, generated Obsidian diagnostics, user vault files, backups, or unrelated working directories.

## Installation pattern

```bash
rm -rf "$HOME/Sync/Everything/.obsidian/plugins/canto-span"

unzip -q "$HOME/Downloads/<candidate>.zip"   -d "$HOME/Sync/Everything/.obsidian/plugins"

obsidian reload
```

## Learner-role bundling

Cosmetic learner-role adjustments should normally be folded into the substantive batch that exposes them. A materially false or misleading learner interpretation may justify a standalone correction; record why it could not responsibly wait. Role evidence does not establish grammar.
