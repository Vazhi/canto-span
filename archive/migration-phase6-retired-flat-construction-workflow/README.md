# Phase 6 retired flat construction workflow

The Phase 2 one-time exporter wrote all construction notes directly under `grammar/`. Phase 6 replaced that flat layout with `grammar/active/` and `grammar/archived/`.

The exporter is preserved here for history and recovery from the frozen pre-Obsidian registry. Running it against the current working tree would erase workflow triage and must not be treated as a current command.
