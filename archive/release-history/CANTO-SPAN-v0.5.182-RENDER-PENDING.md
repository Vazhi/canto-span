# Canto Span v0.5.182 — Render Pending

Accepted baseline: **v0.5.180**  
Rejected predecessor: **v0.5.181**  
Active candidate: **v0.5.182**

v0.5.181 passed replacement render but failed prospective evaluation on an overt separate-user boundary. Its seven cases are consumed.

v0.5.182 is a bounded remediation only:

- preserve `杯 bui1` as a noun after the tested `個/隻` classifier profiles;
- prevent an overt person following the resource from entering the narrow direct-resource subtype.

The source claim is unchanged. `ResourceUseLaiFunctionRelation` remains provisional, broad `IntendedFunctionRelation` remains provisional, and `supported_productive` remains 1.

The new eight-case evaluator partition was selected, hashed, and sealed before implementation. It must remain unopened until the 15-row render review passes and the exact candidate is frozen.
