# v0.5.183 Phase 0 status reconciliation R1

Date: 2026-07-20

## Scope

This checkpoint completes Phase 0 documentation and status repair. It changes no parser behavior.

## Corrected contradictions

- The current source-accounting table already classified 21 former provisional constructions as `research_pending`, but the evidence re-audit ledger and speaker queue still called them `provisional`. Both operational ledgers were replaced.
- The active CP021B claim-provenance and authority-origin records were brought into line with the current governance statuses.
- The live DEMO01 implementation checkpoint no longer calls the candidate linguistically provisional; its current linguistic status is `research_pending`.
- Historical CP021A current-status snapshots and superseded R1 ledgers were moved to the archive.

## New status model

`CONSTRUCTION-STATUS-REGISTRY-v0.5.183-R1.tsv` records, for every active label, both:

- current linguistic status under the Definition of Done;
- implementation lifecycle and test state.

The registry exposes 23 known mismatches between current governance and frozen runtime metadata: 21 embedded `provisional` values and two embedded `supported_productive` values. Current governance remains authoritative.

## Runtime decision

`main.js`, `manifest.json`, and `styles.css` remain byte-identical to the frozen v0.5.183 candidate. Correcting the embedded metadata is deferred to a new runtime candidate so the existing render candidate and sealed held-out setup are not silently altered.
