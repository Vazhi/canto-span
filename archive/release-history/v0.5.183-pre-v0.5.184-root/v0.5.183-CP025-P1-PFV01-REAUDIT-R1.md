# v0.5.183 CP025-P1-PFV01 re-audit checkpoint

Runtime: frozen v0.5.183 candidate, byte-identical  
Parser behavior changed: **no**  
Construction: `PostverbalZoPerfectiveVP`  
Current linguistic status: **`provisional_reaudit`**

## Completed

- Reverified four cited publications against the exact narrow claim.
- Manually classified five published exact positive examples and two published boundary examples.
- Reduced the old six-hit corpus aggregate to zero evidence weight because no row-level inventory can be recovered.
- Reclassified the six prior Speaker A sentences: only one exactly instantiates overt `V + 咗 + object`.
- Compared the runtime heuristic and public claim at 15 code-review points with exact code locations.
- Withdrew the categorical claim that the construction lacks completion, current-relevance, boundedness, or temporal implications.
- Prepared matching independent review forms for Speaker A and Speaker B.
- Ran a 14-case frozen-runtime probe.

## Material implementation finding

The narrow subtype appears for only **2 of 6** controlled positive surfaces. Four source-linked positives do not instantiate it:

- quantified and associative nominal objects remain separate from the subtype;
- `篇/文` and `牌子` expose lexical/Jyutping gaps;
- the final-`未` question wrapper contains broad `PerfectiveVP`, not the narrow subtype.

Seven structural boundaries remain outside the subtype. The anomalous `我食咗香港。` still builds the subtype and is blocked only by a later semantic review, confirming that subtype construction itself does not exhaustively enforce predicate–object compatibility.

## Status decision

No promotion is justified. `PostverbalZoPerfectiveVP` remains `provisional_reaudit` and does not currently satisfy even the ordinary provisional checklist because the controlled Speaker A form has not been returned.

Before productive status can be reconsidered:

1. Speaker A and Speaker B must complete the same packet independently.
2. The exact object policy must be narrowed or independently justified.
3. Source-linked quantified, associative, lexical, and question profiles must be implemented or excluded from a narrower claim.
4. Runtime semantic wording and stale status metadata must be corrected.
5. Code and documentation must then be compared again line by line.
6. Render, held-out, and regression gates must be rerun for the changed candidate.

Historical render, held-out, and regression success remains software evidence only.
