# Canto Span v0.5.180 acceptance

v0.5.180 is accepted.

## Scope

The accepted productive construction is internal `PostverbalZoPerfectiveVP`, publicly displayed as `PerfectiveVP`, for simple overt action predicate + 咗 + overt nominal object only.

## Evidence and review

- two positive pattern-specific source relationships for simple V–咗–O and aspect-not-tense;
- two adjacent `V完咗O` publications retained as boundary-only because the family is native-conflicted and unresolved;
- six independent natural examples across three documents and two corpus collections;
- six acceptable native-speaker surface judgments and no rejection on the exact promoted family;
- 18-case focused packet;
- 12/12 render rows and 91/91 render assertions PASS;
- seven prospective held-out commitments verified and seven evaluations PASS.

## Boundaries

The promotion excludes objectless 咗, `V完咗O`, motion-goal, placement, experiential, progressive, completion/result, hidden-object, and selectionally incompatible uses. Broad `PerfectiveVP` remains provisional.

## Integrity

The exact parser candidate was frozen before evaluator custody was opened. Hidden cases were evaluated once and are consumed. The acceptance metadata changes status only; parser behavior remains the frozen behavior that passed render and held-out evaluation.
