name: Temporary top-2000 lexical audit (disabled)

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Initial acquisition workflow disabled after checked-in deterministic audit; file will be removed before PR readiness."
