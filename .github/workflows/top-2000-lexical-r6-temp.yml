name: Temporary top-2000 lexical R6 (disabled)

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "R6 completed and disabled; file will be removed before PR readiness."
