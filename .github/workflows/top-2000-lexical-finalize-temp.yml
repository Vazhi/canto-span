name: Temporary finalize top-2000 lexical audit (disabled)

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "R1 finalizer disabled after verified R1 commit; file will be removed before PR readiness."
