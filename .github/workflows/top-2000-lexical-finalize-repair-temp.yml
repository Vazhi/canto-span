name: Temporary repair top-2000 lexical finalizer (disabled)

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete repair workflow disabled; file will be removed before PR readiness."
