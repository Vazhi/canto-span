name: Agent final-band cleanup and version sync

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Paused while the lexical ingestion scope is being rebuilt around the user-approved vernacular source boundary.'
          echo 'This temporary workflow will be removed before PR readiness.'
