name: Temporary top-2000 lexical R2 (disabled)

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "R2 completed and disabled; file will be removed before PR readiness."
