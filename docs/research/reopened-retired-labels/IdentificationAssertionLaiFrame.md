# IdentificationAssertionLaiFrame

**State:** `research_pending`; no runtime authorization.  
**Historical input:** retired parser heuristic `DefinitionExplanatoryFrame`.

## Narrow question

What is the language-level analysis of non-motion `嚟` in identity or assertion structures such as `係 + identity NP + 嚟 + 嘅/㗎`?

## Why reopened

Pedagogical material explicitly distinguishes motion `嚟` from assertive or sentence-particle `嚟` and teaches identity structures containing `係…嚟嘅/㗎`. The old internal heuristic was correctly retired, but the linguistic phenomenon requires a canonical research home.

## Research lead requiring repository verification

- Open Cantonese, *Cantonese Life 1*, identity and origin lessons.

## Required boundaries

Determine whether `嚟` is a linker, copular element, assertion marker, or construction-specific particle across declaratives, questions, reduced forms, and origin statements. Preserve possible ambiguity.

## Implementation safeguard

Do not restore `DefinitionExplanatoryFrame` or classify motion `嚟` as this profile. Future implementation must coordinate with copular and particle infrastructure without allowing an internal wrapper to license the language claim.