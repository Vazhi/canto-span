# TimeToActionFrame

**State:** research-only; no runtime authorization.  
**Historical input:** retired `TimeToActionFrame`.

## Narrow question

Is Cantonese `係時候 + (要) + VP` a dedicated construction, a conventionalized lexical frame, or ordinary copular and temporal composition?

## Why reopened

Institutional teaching materials attest examples such as `係時候要換機喇` and `係時候食藥`. This supports continued research but does not yet establish an atomic construction.

## Research leads requiring repository verification

- CUHK Cantonese Express examples with `係時候要換機喇`.
- Institutional Cantonese teaching material with `係時候 + VP`.

## Required boundaries

Verify optional `要`, subject realization, final particles, aspect, pragmatic force, lexical alternatives, and contrasts with ordinary `時候` temporal clauses.

## Implementation safeguard

This record cannot become a runtime label until construction-versus-composition is resolved with scope-matched sources and executable contrasts. The retired exact fallback remains retired.