# TemporalAdverbialClause

**State:** `research_pending`; no runtime authorization.  
**Historical input:** retired `TemporalAdverbialClause`.

## Narrow question

How should Cantonese temporal adverbial clauses be divided by subordinator, internal operator position, attachment height, and relation to the matrix clause?

## Why reopened

A Cantonese-specific 2024 study directly distinguishes two temporal-adverbial-clause types. The old constructorless label was correctly removed from runtime, but full research closure is no longer justified.

## Existing evidence to reverify

- `SRC-YIP-2024-TEMPORAL-ADVERBIAL-CLAUSES`

## Required boundaries

Begin with the source-defined `喺`-headed and `當`-headed profiles. Document subject behavior, matrix interaction, aspect, and contrasts with ordinary time NPs or time-initial `TemporalClause` analyses.

## Implementation safeguard

Do not treat every time-initial phrase as temporal subordination. Any future runtime representation should use independently parsed relation members and preserve the overt subordinator.