# v0.5.181 rendered-review correction: 杯 noun and 來 reading

Date: 2026-07-20

Status: bounded learner-display correction; rerender required; evaluator custody remains sealed.

## Render finding

The first rendered-review attempt preserved the target `ResourceUseLaiFunctionRelation` and all seven subtype boundaries, but it failed two learner-facing checks:

1. In `呢個杯用嚟飲水`, `杯` remained the classifier/cupful sense (`measure_word`) instead of the overt noun **cup**.
2. In `呢張紙用來寫字`, construction contextualization assigned written `來` the colloquial `嚟` reading `lai4` instead of preserving the ordinary written-character reading `loi4`.

These are lexical/Jyutping defects, not evidence against the narrow resource–function relation.

## External support

### 杯 as a noun

Words.hk, entry `杯`, records `bui1` as a noun meaning glass/cup/mug and gives `隻` as its measure word. CC-Canto independently lists `杯 bui1` as both the noun “cup” and a classifier for a container of liquid. The homograph therefore requires contextual sense selection rather than a single global learner role.

- https://words.hk/zidin/%E6%9D%AF
- https://cantonese.org/search.php?q=%E6%9D%AF

### 嚟 and 來 readings

Cheung (2018) distinguishes early `嚟` **lai** from `來` **loi**, discusses their partly overlapping written use, and separately notes that `用來/用嚟` has become a conventionalized disyllabic expression. The shared function relation does not require erasing the visible orthographic reading distinction.

- Cheung, Hung-nin Samuel. 2018. `緣自何來：早期粵語中「嚟」的來去踪跡`, pp. 17–39, especially pp. 17–20 and footnote 9.
- https://www.cuhk.edu.hk/ics/clrc/crcl_97_1/cheung_hung_nin_samuel.pdf

## Corrections

- `呢個杯` and `嗰個杯` receive a transparent demonstrative + general classifier + noun analysis.
- The visible `杯` token is `what`, `head_noun object_np container_noun`, Jyutping `bui1`.
- Independent classifier uses of `杯` remain unchanged.
- Inside the intended-function relation, `嚟` remains `lai4` and written `來` remains `loi4`.
- The target construction definition, sources, boundaries, public alias, and provisional status are unchanged.

## Acceptance effect

The original render attempt is rejected and cannot freeze the candidate. A replacement rendered export is required before evaluator custody may be opened.
