# CP022 controlled remediation sequence (CP021B-R37)

Date: 2026-07-19
Status: **one packet held-out locked; implementation not authorized**

The R36 sequence remains controlling. `EP-R37-I04` is now populated and held-out locked for the I04 `NominalAndMeasureSpan` internal-wrapper family. All other packet families remain at their R36 blueprint state.

No parser behavior may change until explicit authorization. The first potentially implementable target remains the I04 internal cleanup, limited to `ModifierPhrase` retirement/merge and bounded internal naming, with intended semantic change `NONE`. Stop immediately if token roles, Jyutping, spans, root coverage, learner-visible meaning, construction selection, or accepted surface behavior changes.

The separate evaluator-custody archive must remain unopened during implementation. LX1 remains render-pending and unaccepted.
