# Codex prompt — active-label evidence and ontology closure

Create a new clean branch from current `main` after the retired-label research-reopen records are merged. Do not reuse an older work branch.

Before editing, read `docs/research/ACTIVE-LABEL-EVIDENCE-ONTOLOGY-CLOSURE-R1.md`, `docs/research/RETIRED-LABEL-RESEARCH-SWEEP-R1.md`, `docs/research/CODEX-RETIRED-LABEL-REOPEN-ADDENDUM-R1.md`, `docs/current/DOCTRINE.md`, every affected note's `Evidence and ontology closure guidance` section, and the successor records under `docs/research/reopened-retired-labels/`. Run and record the unchanged baseline: `npm test`, `npm run verify`, `npm run verify:release`, and `git diff --check`.

Process the original 37 `unsupported_generalization` and 15 `parser_heuristic` labels in the handoff's family order. For each label, inspect its exact `main.js` paths, tests, A1/compatibility effects, cited research records, source scope, and related reopened-retirement research before implementing one disposition: retain narrow, rename, decompose/replace, internalize, quarantine, or retire.

Do not retire because research or panel evidence is incomplete. Historical retirement of an old matcher does not prove that its underlying construction family is nonexistent. Preserve the reopened narrow research questions without recreating old fallbacks. Retirement must satisfy the handoff gate and preserve valid surfaces, executable boundaries, ambiguity, learner-visible meaning, diagnostics, and A1/schema compatibility. Do not promote language profiles without meeting existing source and role-neutral panel thresholds. Give retained serialized internal nodes stable JSON semantics, invariant tests, and explicit nonlicensing behavior.

Update existing canonical runtime, grammar notes, tests, research links, retirement archive, release baseline, documentation, compatibility behavior, grammar index, retirement-review state, and release audit together. Do not create parallel ledgers or new verification frameworks.

Work autonomously in large coherent families and commit completed slices. Do not stop after producing only a plan or audit. Finish only when all original 52 labels have recorded dispositions, the reopened retired-label questions have explicit migration decisions, and `npm test`, `npm run verify`, `npm run verify:release`, and `git diff --check` pass.
