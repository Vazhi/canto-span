# Neutral follow-up brief — completion, location, and current relevance

## Study scope

Create a blinded follow-up with three compatible goals. Use Traditional and Simplified character versions of identical Cantonese stimuli. Preserve Cantonese-specific characters such as `咗`, `喇`, `喺`, and `完` in both script versions.

## Goal A — layered completion

Cross the same verbs and objects across:

1. `V完O`
2. `V完O喇`
3. `V咗O`
4. `V完咗O`
5. `V完咗O喇`

Use verb classes:

- consumption;
- reading/perception;
- writing/creation;
- movement/transfer;
- preparation;
- repair/assembly.

Cross contexts:

- no context;
- newly completed event;
- elapsed-time retrospective report;
- contrast with an earlier incomplete state;
- present-result relevance.

Collect:

- naturalness;
- interpretation;
- whether the sentence sounds redundant;
- lexical or regional alternative;
- optional correction.

## Goal B — result location versus event location

Use matched predicates with:

- placement;
- displacement;
- posture;
- appearance;
- self-motion;
- activity predicates that do not produce a result location.

Compare:

1. preverbal `喺 Location` event location;
2. postobject `喺 Location` result location;
3. directional complement;
4. goal NP;
5. path phrase.

Manipulate object definiteness and classifier form while keeping event plausibility constant.

Ask which participant is located at the endpoint:

- subject;
- object;
- another discourse referent;
- unclear.

## Goal C — `咗` and `喇`

Compare:

1. bare predicate;
2. verbal `咗`;
3. final `喇`;
4. `咗…喇`.

Cross:

- overt versus recoverable object;
- completed past report;
- newly completed event;
- state transition;
- imminent event;
- current relevance;
- continuation that preserves or cancels the result state.

Collect separate judgments for:

- event completion;
- change of state;
- current relevance;
- object recoverability.

## Sampling

Recruit separately for:

- Hong Kong Cantonese;
- Guangzhou Cantonese;
- Macau Cantonese;
- other Yue backgrounds.

Do not infer dialect from Traditional or Simplified script selection.

## Design controls

- one condition per lexical set per respondent;
- region-neutral vocabulary where possible;
- no semantically impossible controls;
- no participant-visible construction labels;
- no expected-answer coding in the blinded package;
- retain exact instrument version and selected script;
- treat corrections as hypothesis-generating evidence.
