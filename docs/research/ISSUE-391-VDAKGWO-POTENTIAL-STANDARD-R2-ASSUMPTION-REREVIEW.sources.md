# ISSUE-391 R2 source pointers

This companion file records source pointers used during the assumption-level rereview. It is not a source ledger and does not grade these sources as construction-level evidence.

## Lexical support

- Wiktionary `信得過`: Cantonese Jyutping `seon3 dak1 gwo3`; trustable / trustworthy / reliable senses; examples support lexical-predicate/adjectival use.
- CantoDict `信唔過`: Cantonese negative counterpart, glossed as unreliable / cannot be trusted.
- Words.hk / Jyut Dictionary search results: examples containing `信唔過` and `信唔信得過`-type strings.

## Background only

- General Cantonese `得` potential research is relevant to the broader analysis space, but does not itself prove that `信得過` instantiates a productive open `V得過` reliability-standard construction.

## Rereview consequence

These sources support preserving `信得過` / `信唔過` as lexical evidence and justify no promotion. They do not satisfy the parent issue's bounded corpus and negative-control endpoint.
