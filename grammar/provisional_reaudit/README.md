# Provisional — re-audit required

**Frontmatter status:** `provisional_reaudit`
**Current construction notes:** 0

Former acceptance has been withdrawn pending review under the current standard.

## Manual review focus

- Reconstruct the original claim and identify which evidence is legacy-only.
- Re-run source, boundary, panel, and code-document checks under current doctrine.
- Do not restore provisional status until all current requirements are explicit.

## Folder rule

Every construction note in this folder must carry `status: "provisional_reaudit"`. Workflow selection remains in `workflow_state` and does not change the file location. This `README.md` is explanatory and is not a construction record.
