# Lexicalized only

**Frontmatter status:** `lexicalized_only`
**Current construction notes:** 2

Only bounded lexicalized expressions are retained; productive grammar is not claimed.

## Manual review focus

- Check that the retained inventory is explicitly bounded.
- Separate lexical attestation from productive compositional behavior.
- Add new members only with direct lexical evidence and clear learner value.

## Folder rule

Every construction note in this folder must carry `status: "lexicalized_only"`. Workflow selection remains in `workflow_state` and does not change the file location. This `README.md` is explanatory and is not a construction record.
