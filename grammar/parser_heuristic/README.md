# Parser heuristic

**Frontmatter status:** `parser_heuristic`
**Current construction notes:** 15

Internal software representation; it is not a productive Cantonese grammar claim.

## Manual review focus

- Check whether the node remains useful for rendering or internal composition.
- Prevent heuristic labels from being presented as linguistic terminology.
- Retire or merge nodes that have no distinct observable implementation role.

## Folder rule

Every construction note in this folder must carry `status: "parser_heuristic"`. Workflow selection remains in `workflow_state` and does not change the file location. This `README.md` is explanatory and is not a construction record.
